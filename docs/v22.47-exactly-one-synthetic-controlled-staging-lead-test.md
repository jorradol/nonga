# v22.47 — Exactly One Synthetic Controlled Staging Lead Test

Date: 2026-07-10  
Branch: `feature/chat-image-attachment-v1`  
Execution type: `EXACTLY ONE SYNTHETIC CONTROLLED STAGING LEAD TEST / USE EXISTING LEAD SYSTEM / TEMPORARY LEAD CAPTURE ON / SYNTHETIC DATA ONLY / NO DEALER-FACING SEND / CLEANUP REQUIRED / RETURN LEAD CAPTURE OFF`

## 1) Executive summary

Owner Option **C** executed: exactly **one** synthetic Lead through the **existing** staging Lead system (`POST /api/buyer-leads`), on one approved Thor Auto Corolla listing. Recipient was server-derived. No Dealer-facing send. Synthetic Lead cleaned via existing seller **skip → withdrawn**. Lead Capture returned **OFF**. Marketplace remained **15**.

**Recommendation: PASS**

---

## 2) Owner authorization scope

Authorized:

- Staging only  
- Exactly one synthetic Lead  
- Existing Lead system only  
- One existing approved Thor Auto Listing  
- Temporary Lead Capture ON  
- No Dealer-facing send  
- Required cleanup + return OFF  

Not authorized / not done: real customer data, second Lead, durable storage, new Lead architecture, public signup, production.

---

## 3) Preflight baseline

| Check | Result |
|-------|--------|
| Repo / branch | `D:/nonga` · `feature/chat-image-attachment-v1` |
| HEAD before | `63a7745` (local = origin) |
| Cloud Run before | `nonga-staging-00192-6th` @ 100% |
| Hosting | `assets/index-Bsnqa-MN.js` |
| `leadCaptureEnabled` | **false** |
| `publicSignupEnabled` | **false** |
| Unauth buyer-lead | **401** |
| Marketplace | **15** |
| Corolla 2020/2021 | Public · `Thor Auto` · 5/5 images |
| Public DTO protected fields | **0** |
| Outbound notify integrations | **None** (no email/SMS/LINE/webhook in lead path) |
| Production | Untouched |

---

## 4) Selected listing (public descriptor only)

**Toyota Corolla 2020** (existing approved pilot; bound to `nonga-dealer`; public display `Thor Auto`)

---

## 5) Synthetic-data safety

| Item | Approach |
|------|----------|
| Name | Synthetic test label only |
| Phone | Non-customer placeholder digits (repo test convention style); **not printed** in this report |
| Free text | Marked controlled synthetic staging lead |
| Real PII | **None** |
| Report | No raw phone / lead id / private listing id |

---

## 6) Temporary Lead Capture enablement

| Item | Result |
|------|--------|
| Method | Staging Cloud Run env `NONGA_LEAD_CAPTURE_ENABLED=true` (operator-managed) |
| Instance pin | Temporary `min=1` / `max=1` for memory-backend coherence |
| Revision while ON | `nonga-staging-00193-t4w` @ 100% |
| Health while ON | `leadCaptureEnabled: true`, signup still **false** |

---

## 7) Exactly-one execution evidence

| Check | Result |
|-------|--------|
| Submit attempts | **1** |
| HTTP | **201** |
| Leads created | **1** |
| Listing association | Match to selected Corolla 2020 |
| Contact reveal on create | `locked` |
| Path | Existing `POST /api/buyer-leads` only (no InquireModal / DealerLeads) |
| Retry | **None** |

---

## 8) Recipient routing + buyer override

| Check | Result |
|-------|--------|
| Recipient source | Server `resolveListingSellerId(listing)` from listing ownership |
| Buyer override fields in body | Present as attack attempt (`sellerId` / `dealerId` / `ownerId`) — **ignored** |
| Wrong-dealer receipt | **No** |
| Public display vs private binding | `Thor Auto` public; private binding remains existing `nonga-dealer` ownership |

---

## 9) Masked queue verification

| Check | Result |
|-------|--------|
| Interest count before cleanup | **1** |
| Masked queue entries | **1** |
| Contact masked | **true** |
| Reveal status | `locked` |
| Phone leak in queue JSON | **false** |
| Reveal called | **No** |
| Outcome / notify | **No** |

---

## 10) Notification isolation

| Channel | Result |
|---------|--------|
| Email / SMS / LINE / webhook | **Not sent** (no integration on path) |
| Automatic Dealer notification | **None** |
| Dealer-facing message | **None** |
| External CRM | **None** |

---

## 11) Cleanup

| Item | Result |
|------|--------|
| Method | Existing seller skip → `queueLifecycle: withdrawn` / not actionable |
| Skip reason | Valid existing reason (`insufficient_info`) |
| Active queue after | **0** entries |
| Interest count after | **0** |
| Listings / dealer / marketplace mutated | **No** |

---

## 12) Return to OFF + post-test proof

| Check | Result |
|-------|--------|
| Capture disabled | Env var removed from staging Cloud Run |
| Scaling restored | `min=0` / `max=20` |
| Final revision | `nonga-staging-00194-72z` @ 100% |
| `leadCaptureEnabled` | **false** |
| `publicSignupEnabled` | **false** |
| Authenticated create while OFF | **403** (no second Lead created) |
| Unauth | **401** |
| Marketplace | **15** |
| Both Corollas / Thor Auto / 5/5 | **Preserved** |

---

## 13) Tests

| Check | Result |
|-------|--------|
| `npm run test:v22.47` | **PASS** |
| `npm run test:v22.30-lead-capture-kill-switch-unit` | **PASS** |
| `npm run test:v22.39` | **PASS** |
| `npm run test:v22.40` | **PASS** |
| `npm run test:v22.43` | **PASS** |
| `npm run test:v22.44` | **PASS** |
| `npm run test:v22.46` | **PASS** |
| `npm run test:v22.41` / `v22.42` / `v22.45` hosting-pin asserts | **Expected FAIL** — Hosting asset is now `Bsnqa-MN` (v22.46 UI deploy); historical pins to `CHSJ9MTc` are stale, not a lead-path regression |

---

## 14) Risks / deviations

1. Memory backend remains ephemeral — authorized under Option C; durable storage still Option D for real pilot.  
2. Temporary single-instance pin used only during ON window for memory coherence; restored after.  
3. First cleanup script used wrong queue field name / skip reason; **no second Lead**; cleanup corrected and completed successfully.  
4. Historical `test:v22.30` live marketplace==13 assertion may fail on current staging (count correctly **15**) — unit kill-switch evidence used.

---

## 15) Recommendation

**PASS**

Exactly one synthetic Lead proved the existing staging Lead path end-to-end with server-derived recipient, no buyer override, no Dealer send, successful cleanup, and Lead Capture returned OFF with post-test blocking proven. Marketplace and Thor Auto listings unchanged.

### Explicit non-actions

- Did not create a second Lead / retry submit  
- Did not enable public signup or touch production  
- Did not send Dealer-facing messages / reveal phone  
- Did not implement Firestore durability or new Lead architecture  
- Did not ask Owner for secrets / terminal work  
- Did not put phone / lead id / private ids in this report  
