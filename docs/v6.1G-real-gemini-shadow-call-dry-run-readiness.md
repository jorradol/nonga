# v6.1G — Real Gemini Shadow Call Dry-run Readiness / Admin-only / User-visible Off

**สถานะ:** Dry-run readiness (docs/tests only) — **ยังไม่เรียก paid Gemini, ยังไม่ deploy, ยังไม่ gcloud env update, ยังไม่เปิด NONGA_AI_USER_VISIBLE_ENABLED, user-visible chat ยัง legacy 100%**  
**วันที่:** 2026-06-08  
**Git baseline:** `c8f8a3399a13dfa4b377b0aab38ec16953a0750e` (v6.1F)  
**Branch:** `feature/chat-image-attachment-v1`  
**อ้างอิง:** v6.1F (human manual smoke + decision gate), v6.1E (Hosting deploy), v6.1D (read-only panel), v6.1C (Cloud Run admin route), v6.1B (server-side shadow debug)

> **v6.1G เตรียม dry-run readiness สำหรับ real Gemini shadow call แบบ admin-only** — บันทึกสถานะหลัง manual smoke ผ่าน, checklist ก่อนเปิด provider network, **DO NOT RUN YET** สำหรับคำสั่งจริง, และ acceptance criteria สำหรับ v6.1H

---

## สารบัญ

1. [Vision & Scope](#1-vision--scope)
2. [v6.1F Manual Smoke — Passed](#2-v61f-manual-smoke--passed)
3. [Environment Baseline](#3-environment-baseline)
4. [Forbidden Actions (v6.1G)](#4-forbidden-actions-v61g)
5. [Real Gemini Dry-run Readiness Checklist](#5-real-gemini-dry-run-readiness-checklist)
6. [DO NOT RUN YET — Future Command Plan (v6.1H)](#6-do-not-run-yet--future-command-plan-v61h)
7. [Future v6.1H Approval Phrase](#7-future-v61h-approval-phrase)
8. [Future v6.1H Acceptance Criteria](#8-future-v61h-acceptance-criteria)
9. [Real Stock Import Gate](#9-real-stock-import-gate)
10. [Compliance (v6.1G)](#10-compliance-v61g)

---

## 1. Vision & Scope

### เป้าหมาย v6.1G

เตรียม **dry-run readiness** สำหรับขั้น real Gemini shadow call แบบ admin-only บน staging หลังจาก v6.1F manual smoke ผ่านแล้ว โดย:

- **Docs/tests only** — ไม่เรียก Gemini จริง, ไม่แก้ runtime, ไม่ deploy
- **ไม่เปิด** `NONGA_AI_USER_VISIBLE_ENABLED=true`
- **ไม่แก้** `NONGA_AI_*` env หรือ secrets ใน slice นี้
- บันทึก checklist + rollback path + approval gate สำหรับ v6.1H

### สถานะปัจจุบัน (baseline v6.1F)

| Area | Status |
|------|--------|
| v6.1F pushed | ✅ `c8f8a3399a13dfa4b377b0aab38ec16953a0750e` |
| Human manual smoke | ✅ **ผ่านแล้ว** (ยืนยันโดย ops) |
| Admin Shadow Debug UI | ✅ live บน staging primary URL |
| Admin-only route | ✅ `POST /api/admin/sales-brain-shadow-smoke` |
| `NONGA_AI_USER_VISIBLE_ENABLED` | **`false`** |
| User-visible chat | legacy 100% |
| Shadow route | mock / read-only / `providerNetwork: false` |
| Paid Gemini call | ❌ ยังไม่มี |
| Real stock import | ❌ ยัง blocked |

### Out of scope (this slice)

- Paid Gemini API call หรือ fetch ไป AI provider
- Cloud Run / Hosting / Firestore rules deploy
- `gcloud run services update` หรือแก้ `NONGA_AI_*` env
- เปิด `NONGA_AI_USER_VISIBLE_ENABLED=true`
- Secrets create/update หรือ `gcloud secrets versions access`
- เปลี่ยน user-visible chat behavior
- Production / payment / invoice / settlement / buyer lead / seller reveal / outcome / public signup / real stock import

---

## 2. v6.1F Manual Smoke — Passed

ยืนยันโดย ops หลัง v6.1F checklist — สรุปผลที่ผ่านแล้ว:

| Check | Result |
|-------|--------|
| Guest gate ที่ `/admin/shadow-smoke` | ✅ **PASS** — login/admin gate, ไม่ใช่ public debug page |
| Dealer/member block + API 403 | ✅ **PASS** — ไม่เห็น admin menu, `POST /api/admin/sales-brain-shadow-smoke` → 403 |
| Admin UI SS-01..SS-08 | ✅ **PASS** — fixed buttons, mock provider, guardrail cases safe |
| Chat `"เริ่มใหม่"` legacy exact | ✅ **PASS** — `"ยกเลิกข้อมูลเดิมแล้วครับ พิมพ์ข้อมูลรถคันใหม่ที่ต้องการลงขายได้เลยครับ"` |
| No AI/Gemini response to public | ✅ **PASS** — user-visible chat ยัง legacy 100% |
| No PII / secret dump | ✅ **PASS** — redacted admin debug response only |
| Real stock import | ❌ **still blocked** |

**v6.1G ไม่รัน manual smoke ซ้ำ** — อ้างอิงผล v6.1F ที่ผ่านแล้วเป็น prerequisite

---

## 3. Environment Baseline

| Item | Value |
|------|-------|
| **Staging project** | `nonga-ce93c` |
| **Cloud Run service** | `nonga-staging` |
| **Region** | `asia-southeast1` |
| **Primary URL** | `https://nonga-ce93c.web.app` |
| **Admin UI path** | `https://nonga-ce93c.web.app/admin/shadow-smoke` |
| **Public chat path** | `https://nonga-ce93c.web.app/chat` |
| **Admin API route** | `POST /api/admin/sales-brain-shadow-smoke` |
| **Cloud Run revision** | `nonga-staging-00051-fbd` |
| **Live Hosting bundle** | `index-BpyOr2_V.js` |
| **Public signup** | **`false`** |
| **Production** | ❌ **excluded** — ไม่แตะ production project/service |
| **GEMINI_API_KEY ref** | `gemini-api-key:latest` (metadata only — **no value accessed**) |

### NONGA_AI_* flags (current staging — do not modify in v6.1G)

| Flag | Value |
|------|-------|
| `NONGA_AI_PROVIDER` | `gemini` |
| `NONGA_AI_MODE` | `high` |
| `NONGA_AI_FIRST_ENABLED` | `true` |
| `NONGA_AI_SHADOW_MODE_ENABLED` | `true` |
| `NONGA_AI_USER_VISIBLE_ENABLED` | **`false`** |
| `NONGA_AI_EMERGENCY_KILL_SWITCH` | `false` |
| `NONGA_AI_BUDGET_DAILY_LIMIT` | `5` |
| `NONGA_AI_BUDGET_MONTHLY_LIMIT` | `50` |

**Did not run** `gcloud secrets versions access` — no secret values in this record.

---

## 4. Forbidden Actions (v6.1G)

| Forbidden | v6.1G status |
|-----------|--------------|
| Paid Gemini API call (real) | ❌ **not done** — dry-run docs only |
| Fetch network to AI provider | ❌ **not done** |
| `NONGA_AI_USER_VISIBLE_ENABLED=true` | ❌ **not done** — remains **false** |
| `NONGA_AI_*` env changes | ❌ **not done** |
| Cloud Run / Hosting / Firestore deploy | ❌ **not done** |
| `gcloud run services update` | ❌ **not done** |
| Secrets create/update / `secrets versions access` | ❌ **not done** |
| User-visible chat / AI response to public | ❌ **not changed** |
| Production | ❌ **not touched** |
| Payment / invoice / settlement write logic | ❌ **not done** |
| Buyer lead / seller reveal / outcome changes | ❌ **not done** |
| Public signup enable | ❌ **not done** |
| Real stock import | ❌ **not done** — still blocked |
| Custom prompt / PII / real phone / real listing | ❌ **blocked** |

CI static test uses **readFileSync only** — no fetch, no gcloud, no Firebase CLI, no AI API.

---

## 5. Real Gemini Dry-run Readiness Checklist

**วัตถุประสงค์:** ยืนยันความพร้อมก่อนเปิด real provider network ใน v6.1H (ยังไม่รันใน v6.1G)

| # | Check | Expected | Ready |
|---|-------|----------|-------|
| R-01 | Staging only | `nonga-ce93c` / `nonga-staging` / `asia-southeast1` | ☐ |
| R-02 | Production excluded | ไม่แตะ production project/service/URL | ☐ |
| R-03 | `NONGA_AI_USER_VISIBLE_ENABLED` | **`false`** — ต้องคงไว้ตลอด shadow pilot | ☐ |
| R-04 | Provider | `NONGA_AI_PROVIDER=gemini` | ☐ |
| R-05 | Shadow mode | `NONGA_AI_SHADOW_MODE_ENABLED=true` | ☐ |
| R-06 | Budget daily | `NONGA_AI_BUDGET_DAILY_LIMIT=5` | ☐ |
| R-07 | Budget monthly | `NONGA_AI_BUDGET_MONTHLY_LIMIT=50` | ☐ |
| R-08 | Kill switch | `NONGA_AI_EMERGENCY_KILL_SWITCH` exists + rollback path known (§6) | ☐ |
| R-09 | Admin route guard | unauth **401**, dealer **403**, admin **200**, bad caseId **400** | ☐ |
| R-10 | No public endpoint | ไม่มี public route สำหรับ shadow smoke / real provider | ☐ |
| R-11 | Fixed SS cases only | SS-01, SS-02, SS-03, SS-04, SS-05, SS-06, SS-07, SS-08 synthetic — ไม่มี custom prompt | ☐ |
| R-12 | No PII / real stock | ไม่มี phone/listing/stock จริงใน payload | ☐ |
| R-13 | Redaction before provider | `redactPiiForSalesBrainLog` / redaction ก่อนส่ง provider payload | ☐ |
| R-14 | Admin debug panel only | response แสดงเฉพาะ admin debug panel — ไม่ใส่ public chat | ☐ |
| R-15 | Provider output isolation | provider output **ต้องไม่แทน** `userVisibleResponse` | ☐ |
| R-16 | Log safety | logs **ต้องไม่เก็บ** raw prompt / PII / secret value | ☐ |
| R-17 | v6.1F manual smoke | ผ่านแล้ว (§2) | ✅ |
| R-18 | Approval phrase v6.1H | ยังไม่ได้รับ — ห้าม real call จนกว่าจะมี (§7) | ☐ |

#### Fixed SS case IDs (synthetic only)

| Case ID | Label |
|---------|-------|
| SS-01 | Buyer generic search |
| SS-02 | Buyer mock listing detail |
| SS-03 | Seller start over |
| SS-04 | Dealer inventory count |
| SS-05 | Kill switch blocked |
| SS-06 | Budget missing blocked |
| SS-07 | User-visible blocked |
| SS-08 | Production off |

### Admin route auth matrix (required before v6.1H)

| Caller | `POST /api/admin/sales-brain-shadow-smoke` | Expected |
|--------|--------------------------------------------|----------|
| No auth | | **401** |
| Dealer/member token | | **403** |
| Admin/superadmin + valid `caseId` | | **200** |
| Admin + invalid/missing `caseId` | | **400** |

---

## 6. DO NOT RUN YET — Future Command Plan (v6.1H)

> ⚠️ **DO NOT RUN YET** — คำสั่งด้านล่างเป็น **future implementation slice v6.1H เท่านั้น**  
> **v6.1G ห้ามรัน** คำสั่งเหล่านี้ — ต้องมี approval phrase (§7) ก่อน

### Proposed future flag (v6.1H implementation)

อาจต้องเปิด provider network **เฉพาะ admin-only route** ด้วย flag แยก:

```
NONGA_AI_ADMIN_SHADOW_REAL_PROVIDER_ENABLED=true
```

ข้อกำหนดเมื่อ implement v6.1H:

| Requirement | Value |
|-------------|-------|
| `NONGA_AI_USER_VISIBLE_ENABLED` | **`false`** — ต้องคงไว้ |
| `NONGA_AI_SHADOW_MODE_ENABLED` | `true` |
| `NONGA_AI_PROVIDER` | `gemini` |
| Public chat path | **ไม่เปลี่ยน** — legacy orchestrator only |
| Provider network scope | admin debug route only — ไม่เปิด public endpoint |

### Future enable command (DO NOT RUN in v6.1G)

```bash
# DO NOT RUN YET — v6.1H only, requires approval phrase §7
gcloud run services update nonga-staging \
  --region=asia-southeast1 \
  --project=nonga-ce93c \
  --update-env-vars="NONGA_AI_ADMIN_SHADOW_REAL_PROVIDER_ENABLED=true,NONGA_AI_USER_VISIBLE_ENABLED=false,NONGA_AI_EMERGENCY_KILL_SWITCH=false"
```

**Did not run** `gcloud secrets versions access` — secret binding unchanged in dry-run record.

### Kill switch rollback command (DO NOT RUN in v6.1G)

```bash
# DO NOT RUN YET — emergency rollback for v6.1H if provider misbehaves
gcloud run services update nonga-staging \
  --region=asia-southeast1 \
  --project=nonga-ce93c \
  --update-env-vars="NONGA_AI_EMERGENCY_KILL_SWITCH=true,NONGA_AI_ADMIN_SHADOW_REAL_PROVIDER_ENABLED=false,NONGA_AI_USER_VISIBLE_ENABLED=false"
```

Rollback effect:

- Kill switch blocks shadow evaluation
- Admin route returns safe guardrail (mock/fallback) — ไม่เรียก provider network
- Public chat ยัง legacy — **ไม่เปลี่ยน user-visible path**

---

## 7. Future v6.1H Approval Phrase

**v6.1G เป็น dry-run readiness เท่านั้น** — ถ้ายังไม่ได้ approval phrase ด้านล่าง **ห้าม real Gemini call**

### Approval phrase (exact — สำหรับ v6.1H เท่านั้น)

> **อนุมัติให้ทดสอบ real Gemini shadow call เฉพาะ admin-only บน staging โดยไม่แสดงผลให้ผู้ใช้ทั่วไป ตาม v6.1H**

เงื่อนไขเมื่อใช้ phrase นี้:

- เฉพาะ `POST /api/admin/sales-brain-shadow-smoke` + admin debug UI
- `NONGA_AI_USER_VISIBLE_ENABLED` ยัง **`false`**
- SS-01 real Gemini shadow call เป็น smoke แรก — synthetic case only
- ยังไม่ production / public signup / real stock
- ยังไม่ `gcloud secrets versions access` ใน docs record

---

## 8. Future v6.1H Acceptance Criteria

เมื่อ implement และรัน v6.1H (หลัง approval phrase §7) ต้องผ่าน:

| # | Criterion | Expected |
|---|-----------|----------|
| H-01 | Admin-only SS-01 real Gemini shadow call | สำเร็จ — admin token + `caseId: SS-01` |
| H-02 | `providerNetwork` | **`true`** เฉพาะ admin debug route |
| H-03 | `userVisibleOff` | **`true`** |
| H-04 | Public chat | ยัง legacy — `"เริ่มใหม่"` exact response unchanged |
| H-05 | No raw PII | ไม่มี phone/email จริงใน response หรือ logs |
| H-06 | No secret dump | ไม่มี env/secret value ใน API response |
| H-07 | Budget observed | daily/monthly limits respected — no overrun without guardrail |
| H-08 | Kill switch rollback | tested or prepared — §6 rollback command ready |
| H-09 | No payment/lead/reveal/outcome | ไม่แตะ write paths |
| H-10 | No real stock | synthetic/mock listing only — real stock still blocked |
| H-11 | Provider output isolation | provider output ไม่แทน `userVisibleResponse` |
| H-12 | Dealer/guest still blocked | 401/403 unchanged for non-admin |

---

## 9. Real Stock Import Gate

การนำสต๊อกจริงจากเต็นท์เข้าระบบ **ยังห้าม** — แม้ v6.1F manual smoke ผ่านแล้ว

| Gate | Requirement |
|------|-------------|
| 1 | ผ่าน v6.1F admin shadow UI manual smoke ✅ |
| 2 | ผ่าน v6.1G dry-run readiness checklist (§5) |
| 3 | ผ่าน **real Gemini admin-only smoke** (v6.1H + approval phrase §7) |
| 4 | ผ่าน **AI conversation smoke user-visible controlled** — ก่อนเปิดสินค้าจริง |
| 5 | กำหนดชุดข้อมูลเต็นท์ตัวอย่างแบบ **sanitized** หรือ **approved dataset** ก่อน import |

ข้อห้ามที่ยังมีผล:

- ห้ามนำสต๊อกจริงจากเต็นท์เข้า
- ห้ามใช้ข้อมูลลูกค้าจริง / เบอร์โทรจริง / ข้อมูลรถเต็นท์จริง
- ห้าม listing input / real stock path ใน admin shadow UI

---

## 10. Compliance (v6.1G)

| Item | v6.1G status |
|------|--------------|
| Docs/tests only — dry-run readiness | ✅ |
| v6.1F manual smoke passed recorded | ✅ |
| Paid Gemini API call (real) | ❌ not done |
| Fetch network to AI provider | ❌ not done |
| `NONGA_AI_USER_VISIBLE_ENABLED=true` | ❌ not done — remains **false** |
| `NONGA_AI_*` env changes | ❌ not done |
| Cloud Run / Hosting / Firestore deploy | ❌ not done |
| `gcloud run services update` | ❌ not done (commands documented as DO NOT RUN YET) |
| Secrets create/update / `secrets versions access` | ❌ not done |
| User-visible chat / AI response to public | ❌ not changed |
| Production | ❌ not touched |
| Payment / invoice / settlement write logic | ❌ not done |
| Buyer lead / seller reveal / outcome changes | ❌ not done |
| Public signup enable | ❌ not done |
| Real stock import | ❌ not done — still blocked |
| DO NOT RUN YET commands | ✅ documented for v6.1H only |

CI static test: `npm run test:v61g-real-gemini-shadow-call-dry-run-readiness` — **readFileSync only**, no network, no gcloud, no AI API.
