# v6.2B.10 — Authenticated Reference Count Review (Read-only)

**สถานะ:** **AUTHENTICATED READ-ONLY EXECUTION RECORD (REDACTED COUNTS)** — docs/tests/package only — **ยังไม่ cleanup จริง**, **ยังไม่ import**, **ยังไม่ deploy**  
**วันที่เอกสาร:** 2026-06-09  
**Git baseline:** `06b9ad3a9041a4679458d068fcd458b8498128b1` (v6.2B.9 pushed)  
**Branch:** `feature/chat-image-attachment-v1`  
**อ้างอิง:** v6.2B.9 Aggregate Query Execution, v6.2B.8 Preflight, v6.2B.7 Cleanup Plan, G-11

> **v6.2B.10 = authenticated read-only reference count review แบบ redacted counts เท่านั้น** — query จริงทำบน staging **นอก repo**; git เก็บ **counts + status เท่านั้น** ไม่เก็บ listingId/raw UID/ข้อมูลรายคัน  
> **Existing old staging listings ยังไม่ถูกลบ/ซ่อน** — ไม่มี Firestore writes ใน slice นี้

---

## สารบัญ

1. [Preflight Metadata](#1-preflight-metadata)
2. [Reference Count Summary (Redacted)](#2-reference-count-summary-redacted)
3. [Query Method (Authenticated Read-only)](#3-query-method-authenticated-read-only)
4. [Safety Interpretation](#4-safety-interpretation)
5. [Cleanup Readiness Recommendation](#5-cleanup-readiness-recommendation)
6. [Import Gate](#6-import-gate)
7. [Forbidden in Repo (Review Rules)](#7-forbidden-in-repo-review-rules)
8. [Compliance (v6.2B.10)](#8-compliance-v62b10)

---

## 1. Preflight Metadata

| Field | Value |
|-------|-------|
| project | **staging only** (`nonga-ce93c`) — production **excluded** |
| branch | `feature/chat-image-attachment-v1` |
| head / origin (baseline) | `06b9ad3a9041a4679458d068fcd458b8498128b1` |
| working tree | **clean / synced** (at doc authoring) |
| review mode | **authenticated read-only only** |
| prior v6.2B.9 recommendation | **FIX REQUIRED** (referenceRisk needs-review) |
| query auth | ops gcloud user OAuth — **not** `gcloud secrets versions access` |
| Firestore writes | ❌ **none** |
| cleanup executed | ❌ **none** |
| import executed | ❌ **none** |
| deploy / env update | ❌ **none** |
| recordType | `redacted-reference-count-review` |

**authenticated read-only only** — no cleanup / import / deploy in v6.2B.10

---

## 2. Reference Count Summary (Redacted)

Counts below are **git-safe redacted aggregates** for the **v6.2B.9 candidate cohort** (`candidateOldTest=17`) — **no per-car data in repo**.

| Metric | Count | Notes |
|--------|------:|-------|
| candidateOldTest | **17** | unchanged from v6.2B.9 pre-import published cohort |
| buyerLeadRefs | **0** | `buyerLeads` docs referencing candidate listings |
| sellerRevealRefs | **0** | revealed contact / reveal-queue refs on candidates |
| outcomeRefs | **0** | `dealOutcomes` on candidates |
| pendingSaleRefs | **0** | candidate listings with `saleStatus=pending_sale` |
| paymentSettlementRefs | **0** | success fee / settlement adj / audit / idempotency on candidates |
| revenueRefs | **0** | admin revenue preview dependency on candidates |
| reportModerationRefs | **6** | `listingReports` on candidates — **3 open / 3 reviewed**; **1** candidate listing affected (aggregate only) |
| protectedNewRealBatch | **0** | real pilot batch **not imported** |
| per-car details in git | ❌ **forbidden** | no listingId / make / model / price in repo |

### Cross-check (public read-only)

| Check | Result |
|-------|--------|
| Public interest-queue-stats sum (candidate cohort) | **0** — aligns with `buyerLeadRefs=0` |
| Settlement collections total (staging) | non-zero globally — **zero** refs on candidate cohort |

| referenceRisk | **found-count-only** |
|---------------|----------------------|
| Meaning | report/moderation refs present; **no** lead/reveal/outcome/payment/settlement/revenue blocking refs on candidates |

**ห้ามบันทึกข้อมูลรายคัน** — counts only in git

---

## 3. Query Method (Authenticated Read-only)

| Step | Action |
|------|--------|
| Q-01 | Confirm staging project `nonga-ce93c` only |
| Q-02 | Build candidate cohort — same aggregate rule as v6.2B.9 (`createdBefore` cutoff, no `pilotBatchId`, published) |
| Q-03 | Authenticated **read-only** Firestore list/count via ops OAuth (gcloud user token) |
| Q-04 | Count refs in: `buyerLeads`, `dealOutcomes`, `successFeeRecords`, `settlementAdjustments`, `settlementAuditLogs`, `settlementIdempotencyRecords`, `listingReports`, `leadContactLogs` |
| Q-05 | Count `pending_sale` on candidate listings from inventory source |
| Q-06 | Cross-check public `interest-queue-stats` aggregate (no PII) |
| Q-07 | Record **counts only** — no listingId/raw UID in git |

**Criteria fields used (count-only, no per-car expose):**

| Criterion | Usage |
|-----------|--------|
| `createdBefore` | execution cutoff 2026-06-09 |
| `sourceTag` | absent on candidates in query surface |
| `pilotBatchId` | absent — protectedNewRealBatch=0 |
| `ownerUid` | not recorded in git |
| `status` | published cohort |
| `test/demo marker` | no explicit markers — cohort from v6.2B.9 |

---

## 4. Safety Interpretation

| Reference area | Candidate refs | Cleanup impact (archive/hide) |
|----------------|---------------:|-------------------------------|
| buyerLead | 0 | ✅ no lead queue dependency |
| seller reveal | 0 | ✅ no reveal dependency |
| outcome | 0 | ✅ no outcome dependency |
| pending_sale | 0 | ✅ no sale pipeline block |
| payment / settlement / revenue | 0 | ✅ no finance block |
| report / moderation | 6 | ⚠️ **found-count-only** — reports remain; archive/hide does **not** mutate report records |

### Interpretation rules applied

| Rule | Result |
|------|--------|
| All lead/reveal/outcome/payment/settlement/revenue refs = 0 | ✅ safe for controlled archive/hide |
| Report refs present but non-mutating on hide | **found-count-only** — not NO-GO |
| No references that cleanup would corrupt | ✅ not NO-GO |
| Open reports (3) on 1 candidate listing | ops awareness — **does not block** archive/hide per v6.2B.7 strategy |

**admin / revenue / seller statement dependency:** **count=0** on candidate cohort — no pending_sale or success fee records linked

---

## 5. Cleanup Readiness Recommendation

| Value | Meaning |
|-------|---------|
| **READY FOR CLEANUP APPROVAL** | blocking refs clear; may request cleanup phrase |
| **FIX REQUIRED** | ambiguous or unverified refs |
| **NO-GO** | cleanup would corrupt linked data |

**This review record:** **READY FOR CLEANUP APPROVAL**

| Reason | Detail |
|--------|--------|
| v6.2B.9 blocker resolved | referenceRisk upgraded from needs-review → **found-count-only** |
| Blocking refs | lead/reveal/outcome/payment/settlement/revenue = **0** |
| Non-blocking refs | reportModerationRefs=6 (found-count-only) |
| Cleanup execution | ❌ **not in v6.2B.10** |

### Cleanup approval (required before real cleanup)

> **อนุมัติให้ cleanup/archive old staging listings บน staging เท่านั้น แบบ controlled rollback ได้ ตาม v6.2B.7**

| Item | Status |
|------|--------|
| v6.2B.10 reference review | ✅ read-only record |
| Cleanup approval phrase | **required separately** — not granted here |

---

## 6. Import Gate

| Item | Status |
|------|--------|
| Real stock import | ❌ **still blocked** — G-11 |
| Import ordering | cleanup approval → cleanup execution → smoke → import approval |
| v6.2B.6 GO record | ✅ ops data package GO — **not import approval** |

### Import approval (unchanged — separate)

> **อนุมัติให้ import real stock แบบ controlled pilot บน staging เท่านั้น ตาม v6.2B ชุดเล็ก 5–10 คัน**

| Requirement | Status |
|-------------|--------|
| Reference review (v6.2B.10) | ✅ this record |
| Cleanup execution + approval | **before** import |
| Import approval phrase | **required separately** — not granted in v6.2B.10 |

---

## 7. Forbidden in Repo (Review Rules)

| Forbidden in git | Rule |
|------------------|------|
| Per-car listing details | ❌ |
| listingId / raw UID | ❌ |
| make/model/price | ❌ |
| Full plate | ❌ |
| Raw image URL | ❌ |
| Phone / LINE / address | ❌ |
| Prompt dump / secret / env dump | ❌ |
| Firestore write commands | ❌ |
| Cleanup/delete/hide execution | ❌ |

---

## 8. Compliance (v6.2B.10)

| Item | Status |
|------|--------|
| Preflight metadata §1 | ✅ |
| Redacted reference summary §2 | ✅ |
| Query method §3 | ✅ |
| Safety interpretation §4 | ✅ |
| Cleanup recommendation §5 | ✅ |
| Import gate §6 | ✅ |
| Authenticated read-only only | ✅ |
| Firestore writes | ❌ none |
| Cleanup/delete/hide executed | ❌ none |
| Hard delete | ❌ none |
| Real stock import | ❌ blocked |
| Deploy / env / Firestore rules deploy | ❌ none |
| Secret access (`gcloud secrets versions access`) | ❌ not done |
| Payment / lead / reveal / outcome mutation | ❌ none |
| Public signup / public AI | ❌ not enabled |
| Real Gemini user-visible path | ❌ not enabled |
| Image processing / AI vision API | ❌ not in v6.2B.10 |

CI: `npm run test:v62b10-authenticated-reference-count-review-read-only` — readFileSync only.

---

## Redacted Review Summary (for report — git-safe only)

```text
v6.2B.10 Authenticated Reference Count Review — REDACTED
project: staging only | authenticated read-only only
head/origin: 06b9ad3a9041a4679458d068fcd458b8498128b1
candidateOldTest: 17
buyerLeadRefs: 0 | sellerRevealRefs: 0 | outcomeRefs: 0 | pendingSaleRefs: 0
paymentSettlementRefs: 0 | revenueRefs: 0 | reportModerationRefs: 6
referenceRisk: found-count-only
recommendation: READY FOR CLEANUP APPROVAL
cleanup approval phrase required (v6.2B.7) | import still BLOCKED
no listingId / per-car data / PII / raw URLs in repo
no Firestore writes / no cleanup executed
```

---

## Next Steps

1. ✅ v6.2B.10 authenticated reference count review (this doc)
2. Request cleanup approval phrase §5 when ready
3. Cleanup execution record (future slice) — after cleanup approval
4. Import approval phrase + import execution — **after** cleanup pass
