# v6.0X — Hosting Shadow Chat Client Bundle Deploy Record

**สถานะ:** Execution record (docs/tests only) — **Firebase Hosting staging deploy บันทึกแล้ว, ไม่ deploy ซ้ำ, ไม่ Cloud Run deploy, ไม่ gcloud env update, ไม่เปิด NONGA_AI_USER_VISIBLE_ENABLED, ไม่เรียก paid AI API, user-visible response ยัง legacy 100%**  
**วันที่:** 2026-06-07  
**Git baseline ขณะ deploy:** `f14f1457e0abc63ed1ad8283039122302a54479c` (v6.0W record)  
**Branch:** `feature/chat-image-attachment-v1`  
**อ้างอิง:** v6.0W (Cloud Run shadow chat path deploy), v6.0V (shadow chat path wiring), v6.0U (Gemini shadow env enable)

> **v6.0X บันทึกผล Hosting deploy จริง** — นำ client bundle ใหม่ที่มี v6.0V shadow chat path wiring ขึ้น primary URL `nonga-ce93c.web.app` โดย **Cloud Run env/secrets ไม่เปลี่ยน**, **NONGA_AI_USER_VISIBLE_ENABLED=false**, และ **user-visible response ยัง legacy 100%**

---

## สารบัญ

1. [Vision & Execution Summary](#1-vision--execution-summary)
2. [Preflight (Before Deploy)](#2-preflight-before-deploy)
3. [Tests / Build Before Deploy](#3-tests--build-before-deploy)
4. [Firebase Hosting Deploy (Executed)](#4-firebase-hosting-deploy-executed)
5. [Live Bundle Change](#5-live-bundle-change)
6. [Post-deploy API Smoke](#6-post-deploy-api-smoke)
7. [Chat Smoke (Primary URL)](#7-chat-smoke-primary-url)
8. [Light Smoke (a.nongbot.org)](#8-light-smoke-anongbotorg)
9. [Git Status After Deploy](#9-git-status-after-deploy)
10. [Rollback Plan (Not Needed)](#10-rollback-plan-not-needed)
11. [Forbidden Changes (v6.0X)](#11-forbidden-changes-v60x)
12. [Next Steps](#12-next-steps)

---

## 1. Vision & Execution Summary

### เป้าหมาย v6.0X

Deploy Firebase Hosting staging **only** เพื่อให้ primary URL ได้ client bundle ใหม่ที่มี v6.0V shadow chat path wiring โดย:

- **ไม่** Cloud Run deploy / gcloud env update
- **ไม่เปิด** `NONGA_AI_USER_VISIBLE_ENABLED=true`
- **ไม่** Firestore rules deploy
- Post-deploy read-only smoke + safe chat smoke ผ่าน `nonga-ce93c.web.app`

### Execution outcome

| Area | Result |
|------|--------|
| Preflight | ✅ git clean, HEAD = origin @ `f14f145` |
| Cloud Run (unchanged) | ✅ rev `nonga-staging-00050-rqs` @ **100%** |
| Hosting deploy | ✅ **SUCCESS** — site `nonga-ce93c` |
| Live bundle | ✅ `index-Bxl8jCCo.js` → **`index-NVEc0A-_.js`** |
| Post-deploy API smoke | ✅ health/cars 200, revenue 401, signup false |
| Chat smoke (web.app) | ✅ legacy response 100%, no AI shown to user |
| Light smoke (a.nongbot.org) | ✅ health 200, same bundle |
| Rollback | ❌ **not needed** — smoke passed |
| User-visible AI | ❌ unchanged — legacy only |
| Paid AI API | ❌ not called |
| Real stock import | ❌ still blocked |

---

## 2. Preflight (Before Deploy)

Recorded at deploy execution time:

| Check | Value |
|-------|-------|
| **git status** | clean (nothing to commit, working tree clean) |
| **branch** | `feature/chat-image-attachment-v1` |
| **HEAD** | `f14f1457e0abc63ed1ad8283039122302a54479c` |
| **origin/feature/chat-image-attachment-v1** | `f14f1457e0abc63ed1ad8283039122302a54479c` |
| **HEAD = origin** | ✅ yes |
| **Firebase project** | `nonga-ce93c` |
| **Cloud Run service** | `nonga-staging` |
| **Cloud Run revision** | `nonga-staging-00050-rqs` @ **100%** |
| **Region** | `asia-southeast1` |
| **`NONGA_AI_USER_VISIBLE_ENABLED`** | **`false`** |
| **Public signup** | **`false`** (`publicSignupEnabled:false`) |

Cloud Run image unchanged from v6.0W: `v6.0W-shadow-chat-path-legacy-visible-d4cecd6`

---

## 3. Tests / Build Before Deploy

All passed before Hosting deploy:

| Command | Result |
|---------|--------|
| `npm run test:v60w-shadow-chat-path-staging-deploy-record` | **PASS** (97 checks) |
| `npm run test:v60v-shadow-chat-path-wiring-user-visible-legacy` | **PASS** (79 checks) |
| `npm run test:v60u-gemini-shadow-env-enable-staging-record` | **PASS** (93 checks) |
| `npm run lint` | **PASS** |
| `npm run build:staging:hosting` | **PASS** |

---

## 4. Firebase Hosting Deploy (Executed)

```bash
# Executed — do not re-run from CI without ops approval
npx -y firebase-tools@latest deploy --only hosting --project nonga-ce93c
```

| Field | Value |
|-------|-------|
| **Result** | **SUCCESS** |
| **Site** | `nonga-ce93c` |
| **Files uploaded** | 6 files in `dist`, **3 new** |
| **Hosting URL** | `https://nonga-ce93c.web.app` |
| **Deploy scope** | **`--only hosting`** |
| **Cloud Run deploy** | ❌ **not done** |
| **Firestore rules deploy** | ❌ **not done** |
| **Env/secrets** | ❌ **not modified** |

**Did not run** `gcloud secrets versions access` — no secret values in this record.

---

## 5. Live Bundle Change

| Asset | Before deploy | After deploy |
|-------|---------------|--------------|
| **JS bundle** | `index-Bxl8jCCo.js` | **`index-NVEc0A-_.js`** |
| **CSS bundle** | (previous) | `index-Cr0iFpYR.css` |

### Bundle markers in `index-NVEc0A-_.js`

Verified in deployed bundle (minified):

| Marker | Present |
|--------|---------|
| `flagsOnly` | ✅ |
| `NONGA_AI_*` keys (incl. `NONGA_AI_USER_VISIBLE_ENABLED`) | ✅ |
| `userVisibleEnabled:!1` | ✅ |
| `wireShadowChatPath` (minified equivalent `U3`) | ✅ |

Shadow chat path wiring is now on **primary URL** client bundle. User-visible response remains legacy — flags-only debug pass, no paid Gemini call.

---

## 6. Post-deploy API Smoke

Read-only GET smoke via `https://nonga-ce93c.web.app` (API rewrites → Cloud Run):

| Endpoint | HTTP | Key fields |
|----------|------|------------|
| `GET /api/health` | **200** | `ok:true`, `publicSignupEnabled:false` |
| `GET /api/cars?limit=1` | **200** | `success:true` |
| `GET /api/admin/revenue/preview` (unauth) | **401** | auth required |
| `GET /api/my/revenue/preview` (unauth) | **401** | auth required |

**No paid AI API call** during deploy or smoke.

---

## 7. Chat Smoke (Primary URL)

Safe chat smoke via `https://nonga-ce93c.web.app/chat`:

| Check | Result |
|-------|--------|
| หน้า chat โหลดได้ | ✅ |
| ส่งข้อความปลอดภัย `"เริ่มใหม่"` | ✅ |
| User-visible response | **legacy 100%** — `"ยกเลิกข้อมูลเดิมแล้วครับ พิมพ์ข้อมูลรถคันใหม่ที่ต้องการลงขายได้เลยครับ"` |
| AI/Gemini response แสดงต่อผู้ใช้ | ❌ **none** |
| Raw PII ใน UI | ❌ **not observed** |

---

## 8. Light Smoke (a.nongbot.org)

Custom domain pointing to same Hosting deployment:

| Check | Result |
|-------|--------|
| `GET /api/health` | **200** |
| JS bundle | **`index-NVEc0A-_.js`** — same bundle as `nonga-ce93c.web.app` |

No chat interaction on a.nongbot.org — health + bundle check only (no real user data).

---

## 9. Git Status After Deploy

```
On branch feature/chat-image-attachment-v1
Your branch is up to date with 'origin/feature/chat-image-attachment-v1'.
nothing to commit, working tree clean
```

- **`dist/` ไม่ tracked ใน git** — deploy จาก local build เท่านั้น
- Hosting deploy ไม่สร้าง git diff

---

## 10. Rollback Plan (Not Needed)

Rollback **not needed** — API + chat smoke passed.

If Hosting smoke fails in future — use Firebase Hosting release rollback via console or `firebase hosting:rollback` (prepared conceptually; **NOT RUN** in v6.0X).

**Do not** modify Cloud Run env/secrets to fix Hosting issues.

---

## 11. Forbidden Changes (v6.0X)

| Forbidden | v6.0X status |
|-----------|--------------|
| Cloud Run deploy | ❌ not done |
| `gcloud run services update` for env | ❌ not done |
| `NONGA_AI_*` env changes | ❌ not done |
| `NONGA_AI_USER_VISIBLE_ENABLED=true` | ❌ not done — remains **false** |
| Secrets create/update / `secrets versions access` | ❌ not done |
| Paid AI API / fetch to AI provider | ❌ not done |
| User-visible AI / AI response shown to user | ❌ not done |
| Production | ❌ not touched |
| Firestore rules deploy | ❌ not done |
| Payment / invoice / settlement write logic changes | ❌ not done |
| Buyer lead / seller reveal / outcome changes | ❌ not done |
| Public signup enable | ❌ not done — staging `false` |
| Real stock import | ❌ not done — still blocked |

CI static test uses **readFileSync only** — no fetch, no gcloud, no Firebase CLI, no AI API.

---

## 12. Next Steps

1. Post-deploy **shadow smoke** + **AI conversation smoke** (flags-only, no user-visible AI)
2. Notify ลุง → plan real stock import (still blocked until smoke passes)
3. Do **not** enable `NONGA_AI_USER_VISIBLE_ENABLED=true` without explicit approval
