# v19.40 - Step 2 Limited Private Pilot Readiness Packet

Date: 2026-07-07
Branch: `feature/chat-image-attachment-v1`
Execution type: `STEP 2 LIMITED PRIVATE PILOT READINESS PACKET ONLY / NO EXECUTION / NO DEPLOY / NO ONE-RUN`
Expected HEAD before work: `6cdbff2`

## 1) Step 1 completion summary

- v19.39 final decision: `PASS — controlled owner-only staging trial completed, Step 1 completed`
- one-run performed in v19.39: exactly 1
- retry/second-run/re-arm in v19.39: none
- Secret Manager-backed `NONGA_ADMIN_API_TOKEN` binding: confirmed
- current 5-step status:
  - Step 1 completed
  - Step 2 not started
  - Step 3 not started
  - Step 4 not started
  - Step 5 not started

## 2) Step 2 scope (limited private pilot only)

- staging only
- private only
- allowlist only
- owner-controlled testers only
- no public signup
- no production
- no real dealer action
- no real lead
- no real customer data
- no PII/phone/plate/VIN
- token/secret masked-only
- no uncontrolled provider call
- no retry/second-run without fresh approval

## 3) Allowed testers

- owner account (controller)
- explicit owner-approved allowlist testers only (Firebase-authenticated users)
- no guest users
- no non-allowlisted users

## 4) Allowed route/path candidates (static read-only findings)

1. `POST /api/ai/chat-user-visible-orchestrate`
   - authenticated server-trusted UID path (`getServerAuthContext`)
   - allowlist gate + default-deny logic available (`evaluateUserVisibleGate`)
   - masked diagnostics available (`userVisibleGateDiagnostic`, `userVisibleRuntimeDiagnostic`)
   - supports safe fallback/legacy when gate blocks
2. `POST /api/gemini/chat`
   - not allowlist-scoped for limited private pilot control
   - legacy public-style chat route; less strict for Step 2 boundary
3. `POST /api/gemini/chat-stream`
   - same concern as above; not preferred for first Step 2 private pilot
4. `POST /api/admin/inventory-import/commit`
   - admin-only import route used in Step 1; not user-visible pilot chat path

## 5) Recommended first Step 2 execution path (future v19.41 only)

- recommended path: `POST /api/ai/chat-user-visible-orchestrate`
- mode: staging + private + allowlisted authenticated tester only
- safety preference for first controlled run:
  - keep deterministic fallback active unless explicit fresh approval says otherwise
  - treat any gate mismatch or missing diagnostics as immediate HOLD

## 6) Gemini/provider involvement expectation

- route can invoke provider path through `maybeApplyUserVisibleRealProvider` only if all gates pass
- expected for safest first Step 2 controlled run: provider network should remain blocked/fallback unless explicitly approved in fresh phrase
- no uncontrolled provider call is allowed

## 7) Guardrails confirmed (already present)

- admin route token gate (`/api/admin/*` via `adminApiAuth`)
- user-visible orchestration route requires authenticated trusted UID
- allowlist gate checks (`allowlist_empty`, `uid_not_allowlisted`, `guest_uid_missing`)
- production default-off behavior in runtime flags and user-visible gate
- emergency kill-switch and prerequisite checks (provider/mode/budget/ai-first)
- redacted diagnostics and masked evidence fields
- output safety guards for lead/PII/phone echo and unsafe provider output fallback

## 8) Blocked actions (remain forbidden in Step 2)

- any public or production path
- any real dealer action
- any real lead creation
- any real customer data usage
- any PII/phone/plate/VIN exposure
- any token/secret/header value exposure
- any retry/second-run/re-arm without fresh owner approval
- any deployment

## 9) Evidence checklist required before future Step 2 execution

- branch/path/repo/HEAD/working-tree exact match re-confirmed
- v19.39 PASS continuity confirmed
- Step 1 completed and Step 2 still not started before run
- approved route fixed to `POST /api/ai/chat-user-visible-orchestrate`
- tester UID is allowlisted and masked-only in evidence
- staging-only target confirmed
- no token value shown anywhere (chat/log/docs/repo)
- provider involvement decision captured (blocked or explicitly approved)
- safety diagnostics captured (gate reason, fallback behavior, guard policy markers)
- no retry/no second-run plan confirmed

## 10) Rollback / stop criteria

- stop immediately on any route mismatch from approved packet
- stop immediately on auth/allowlist mismatch
- stop immediately on missing required diagnostics
- stop immediately on any signal of secret/token/header exposure risk
- stop immediately on any real dealer/lead/customer/PII risk
- final state after stop must remain: no deploy, no public, no production

## 11) Fresh owner approval wording draft (for future v19.41 only)

`FINAL AUTHORIZE v19.41 LIMITED PRIVATE PILOT CONTROLLED STAGING RUN / PRIVATE ALLOWLIST ONLY / STAGING ONLY / NO PUBLIC / NO PRODUCTION / NO REAL DEALER / NO REAL LEAD / NO REAL CUSTOMER DATA / NO PII PHONE PLATE VIN / NO TOKEN VALUE IN CHAT OR REPO / EXACTLY ONE RUN / NO RETRY / NO SECOND-RUN / NO RE-ARM`

This is draft wording for a future controlled Step 2 execution request only.
It is not approval for v19.40.

## 12) Explicit v19.40 decision boundary

- v19.40 prepares readiness packet only
- v19.40 does not execute Step 2
- v19.40 does not deploy
- v19.40 does not run one-run
