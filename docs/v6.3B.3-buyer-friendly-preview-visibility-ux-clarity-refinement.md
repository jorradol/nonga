# v6.3B.3 — Buyer-Friendly Preview Visibility & UX Clarity Refinement

**Track:** Buyer-Friendly Listing Copy  
**Branch:** `feature/chat-image-attachment-v1`  
**Base HEAD (start):** `8efa17d` (short)  
**Scope:** Runtime UI refinement, tests, docs — **NO deploy / production / DB / AI**

---

## Problem Statement

After v6.3B.2A staging deploy (bundle `index-9TBqPe8h.js`, flag ON, allowlist 1 UID), manual re-check found:

- Car detail page still shows **raw spec text only** (`AB2 + บ.หนัง + …`)
- **No buyer-friendly preview box** for typical staging accounts (admin / superadmin / dealer)
- v6.3B.2A execution record logged manual smoke **PASS** but preview visibility (MS-03/MS-04) should be **PARTIAL**

---

## Root Cause Analysis

| Check | Finding |
|-------|---------|
| Flag `true` in bundle | Confirmed |
| Staging host/project | `a.nongbot.org` / `nonga-ce93c` |
| Wire path | `App → DetailView → CarDetailsView` |
| Helper output (jargon sample) | `source: template-rich`, `guardPass: true` |
| Gate for allowlisted UID | Would render panel |
| Gate for admin/dealer UIDs | Silent deny — not in baked allowlist |
| Layout/CSS hide | Not the issue — component never mounted |

**Primary root cause:** Allowlist UID mismatch at deploy time (only test member UID baked). Signed-in staging users outside allowlist hit default-deny with zero UI feedback.

**Secondary UX gap:** Preview panel used engineer-facing badge only; no clear Thai heading that Nong A summarized the listing.

---

## v6.3B.2A Corrective Note (no history rewrite)

The execution record at `docs/v6.3B.2A-staging-hosting-deploy-execution-record.md` remains unchanged.

**Corrective assessment for v6.3B.3:**

| Item | v6.3B.2A recorded | Corrected |
|------|-------------------|-----------|
| MS-03 Detail preview visible (allowlisted) | PASS | PARTIAL — only if tester UID matches baked allowlist |
| MS-04 Buyer-friendly copy readable | PASS | PARTIAL — panel absent for non-allowlisted signed-in users |
| Deploy / bundle / flag bake | PASS | PASS (unchanged) |

**Follow-up for deploy approval:** Re-bake allowlist with intended tester UIDs (masked in deploy session only — never full UIDs in repo).

---

## Implementation (v6.3B.3)

### 1. Gate evaluation API

`src/config/buyerFriendlyCopyPreviewGate.ts`

- `evaluateBuyerFriendlyCopyPreviewGate()` — returns `{ visible, reason, featureActive, signedInEligible }`
- `isBuyerFriendlyCopyPreviewFeatureActive()` — flag + staging
- `shouldShowBuyerFriendlyCopyPreview()` — unchanged behavior, delegates to evaluate

### 2. Detail section orchestration

`src/components/listings/BuyerFriendlyListingCopyDetailSection.tsx`

| Gate state | UI |
|------------|-----|
| Allowlisted + signed-in | Full preview panel |
| Staging + flag + signed-in + not allowlisted | Dashed notice: preview is allowlist-only on staging |
| Staging + flag + signed-in + empty allowlist | Dashed notice: allowlist empty |
| Guest / unsigned / prod / flag off | Nothing (silent) |

No UID or PII in notices.

### 3. Preview panel UX

- Title: `น้องเอช่วยสรุปให้อ่านง่าย`
- Subtitle: `สรุปรายละเอียดสำหรับผู้ซื้อ — แปลภาษารถให้เข้าใจง่าย`
- Safety disclaimer in header
- `source: empty` → visible empty-state panel
- Original `car.description` unchanged above this section

### 4. Spec token expansion

Additive SPEC_MAP entries for Cruise Control, Engine Start, Smart Keyless, ไฟตัดหมอก, วิทยุ FM/AM, etc.

### 5. Card scope

**Out of scope for v6.3B.3** — marketplace cards keep original description; detail page is the additive surface.

---

## Test Plan

```bash
npm run test:v63b3-buyer-friendly-preview-visibility-ux-clarity
npm run test:v63b2-buyer-friendly-listing-copy-staging-ui-preview
npm run test:v63b-buyer-friendly-listing-copy-deterministic-helper
npm run lint
npm run build:staging:hosting
```

---

## Guardrails

- No production, Cloud Run, Firestore, DB persist, AI, deploy
- No full UID / PII in repo
