# v9.7 Admin-Only Gemini Runtime Proof Dry-Run Gate

> **DRY-RUN GATE ONLY / ADMIN-ONLY / NO REAL PROVIDER CALL**
> รอบนี้เพิ่ม dry-run decision gate และ readiness check เฉพาะฝั่ง admin runtime-proof เท่านั้น เพื่อเตรียมเงื่อนไขก่อน real provider proof ในอนาคต โดยยังไม่ยิง Gemini จริง

---

## 1. Scope of v9.7

- add dry-run gate state/check payload สำหรับ admin-only runtime proof
- add readiness evaluation for future owner-approved proof mode (future only)
- keep provider adapter blocked and deterministic fallback as source of truth
- no buyer/public route changes
- no deploy, no production

## 2. Dry-Run Gate Policy

- dry-run only
- dry-run guard is always active
- default OFF (`AI_RUNTIME_PROOF_ENABLED` stays default OFF)
- kill switch wins
- admin-only boundary required
- effectiveProviderEnabled remains false
- `effectiveProviderEnabled` remains false
- real provider call remains disallowed in v9.7

## 3. Readiness Check (Future-Proof, Not Activation)

v9.7 readiness check only evaluates whether prerequisites are complete for a **future explicit owner-approved proof mode**:

- runtime proof flag readiness
- admin-only boundary readiness
- secret guard readiness (env-only policy)
- quota guard readiness
- cost guard readiness
- log redaction readiness
- owner approval mode readiness

Even when all readiness checks pass:

- adapter remains disabled/blocked
- no real Gemini call
- no network/provider call
- deterministic flow remains source of truth

## 4. Safety and Guard Confirmations

- no real API key/secret in repo
- env secret must not be logged/exposed
- no normal test depends on real secret
- no deploy
- no production
- no public route
- no buyer-facing/user-visible AI
- no real lead sending
- no buyer/public access to runtime-proof path

## 5. Non-GO Confirmation

- NOT GO for production
- NOT GO for public route
- NOT GO for buyer-facing/user-visible AI
- NOT GO for real lead sending
- NOT GO for real provider activation

## 6. Evidence Summary in v9.7

- dry-run gate added in runtime-proof provider wiring
- admin payload now includes dry-run gate state/check information
- tests verify:
  - dry-run only
  - default OFF
  - kill switch wins
  - admin-only boundary
  - no buyer/public access
  - no real Gemini call
  - no network/provider call
  - no secret/API key in repo
  - no normal test depends on real secret
  - env secret must not be logged/exposed
  - `effectiveProviderEnabled` remains false in default/normal path
  - adapter remains disabled/blocked unless future explicit owner-approved proof mode exists
  - deterministic fallback remains source of truth

