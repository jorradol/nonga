# v12.9 Admin Auth / Runtime Env Gate Review — No Retry
Status: REVIEW ONLY — no retry, no Gemini execution, no smoke endpoint call

## Baseline

- Repo: `https://github.com/jorradol/nonga.git`
- Branch: `feature/chat-image-attachment-v1`
- Baseline commit: `1f7fa3d889879682f834ef247b04573b775aadfc` (`1f7fa3d`)
- Baseline verification result:
  - branch = `feature/chat-image-attachment-v1`
  - local = origin
  - working tree clean

## Source Event: v12.8

Reviewed source event (v12.8):

- Execution decision: `NEED REVIEW`
- authorization consumed: no
- run count: `0/1`
- endpoint called: no
- env gate: FAIL
- admin auth non-Gemini gate: FAIL
- observed status: `401` with provided masked token path
- client command safety gate: PASS
- diagnostics contract gate: PASS
- post-run health: `200`

## Interpretation

- v12.8 is not Gemini failure proof.
- v12.8 is not provider timeout proof.
- v12.8 did not call smoke endpoint.
- v12.8 did not execute one-run.
- failure boundary is pre-execution gate, specifically admin auth/runtime env verification.
- no retry is allowed in v12.9 review-only scope.

## Authorization Consumption Assessment

- `authorization consumed: no` is consistent with `run count: 0/1` and `endpoint called: no`.
- authorization remains available for a future authorized run only after auth/env gate is proven fixed.
- v12.9 is review-only and does not execute.

## Env Gate Review

Static findings from runtime/env code and docs:

- server runtime flags for admin shadow gate are defined in `src/services/ai/salesBrainRuntimeFlags.ts`
  - `NONGA_AI_LEGACY_PUBLIC_GEMINI_ENABLED`
  - `NONGA_AI_ADMIN_SHADOW_REAL_PROVIDER_ENABLED`
  - `NONGA_AI_ADMIN_SHADOW_MANUAL_SMOKE_ENABLED`
  - `NONGA_AI_ADMIN_SHADOW_MANUAL_SMOKE_CASE_ID`
  - `NONGA_AI_ADMIN_SHADOW_PROVIDER_TIMEOUT_MS`
- real-provider attempt gate is enforced in `src/services/ai/salesBrainAdminShadowRealProvider.ts`
  - `admin_shadow_real_provider_flag_off`
  - `admin_shadow_manual_smoke_disabled`
  - `admin_shadow_manual_smoke_case_mismatch`
- legacy/public Gemini gate is fail-closed in `src/server/security/legacyGeminiSafety.ts` by `NONGA_AI_LEGACY_PUBLIC_GEMINI_ENABLED`.

Observed risk:

- v12.8 env gate: FAIL is most consistent with runtime env unverifiable or misaligned at target runtime.
- review-only evidence cannot prove live staging env values without execution endpoint diagnostics.

## Admin Auth Header Review

Expected admin auth header in server:

- `Authorization: Bearer <token>` is accepted.
- fallback legacy header `x-api-token: <token>` is also accepted by `extractBearer(...)`.

Server implementation references:

- `src/server/apiAuth.ts` (`extractBearer`, `adminApiAuth`)
- `server.ts` applies `app.use("/api/admin", adminApiAuth)`.

Script/client header path references:

- `src/utils/apiAuthHeaders.ts` builds `Authorization: Bearer <token>`.
- `src/services/ai/adminShadowSmokeApi.ts` uses `adminAuthHeadersAsync()` and `safeApiFetch`.
- `scripts/v126-client-command-template-helper.mts` sanitized command template uses `Authorization: Bearer ***MASKED***`.

Alignment result:

- header format aligned at design level (`Authorization` + `Bearer`).

## Admin Token Env Name Alignment

Server-side admin token env:

- expected stub/admin token env key: `NONGA_ADMIN_API_TOKEN` (`src/server/apiAuth.ts`).

Client/helper token env:

- client helper token env key: `VITE_NONGA_ADMIN_API_TOKEN` (`src/utils/apiAuthHeaders.ts`).
- staging hosting build enforces `VITE_NONGA_ADMIN_API_TOKEN` blank for Firebase Auth mode (`scripts/build-staging-hosting.mts`).

Alignment assessment:

- env name alignment is partial and mode-dependent.
- server token env and browser helper token env are not the same key.
- in staging Firebase-auth mode, client is expected to use Firebase ID token path, not VITE admin token.

## Token Formatting Risk Review

Static risk review:

- `adminApiAuth` compares exact token string equality after `trim()`.
- trailing whitespace/newline at ends is reduced by `trim()` on both read and header extraction.
- quote characters inside token value are not stripped by server middleware.
- `Authorization: Bearer Bearer <token>` risk remains if caller accidentally prefixes twice.
- literal env name sent as token value would fail equality and produce `401`.
- stale secret/version mismatch between caller token source and server runtime env would produce `401`.
- local `.env` token present does not prove deployed runtime token equality.

Conclusion:

- token formatting risk: yes (possible), but not fully proven as sole root cause.

## Non-Gemini Admin Gate Review

Non-Gemini gate endpoint used in plan:

- `GET /api/admin/draft-inventory` (non-Gemini, non-smoke endpoint).

Route/auth linkage:

- route defined in `server.ts`.
- route sits under `/api/admin` prefix that is guarded by the same `adminApiAuth` middleware as smoke route.
- smoke route path is `POST /api/admin/sales-brain-shadow-smoke` from `src/services/ai/salesBrainServerShadowSmoke.ts`.

Assessment:

- non-Gemini gate and smoke route share admin auth middleware path.
- `401` on non-Gemini admin gate is relevant evidence of auth gate failure for smoke route entry.

## Smoke Endpoint Auth Alignment Assessment

- same admin auth middleware applies before both:
  - `GET /api/admin/draft-inventory`
  - `POST /api/admin/sales-brain-shadow-smoke`
- therefore, admin auth non-Gemini gate is aligned as pre-check for smoke route auth entry.
- this does not prove runtime env gate pass/fail details inside smoke handler because smoke endpoint was not called.

## Guardrail Confirmation

- no retry
- no Gemini execution
- no smoke endpoint call
- no `/api/gemini/*`
- no deploy
- no production
- no public route activation
- no buyer-facing AI
- no real lead
- no PII/customer data
- no secret/token/API key exposure
- no raw provider output
- no automatic retry
- no use of old authorization for execution

## Recommendation

READY FOR ADMIN AUTH / ENV PATCH — no Gemini execution

Reason:

- evidence supports auth/env mismatch risk unresolved at runtime boundary.
- no proof yet that staging admin auth/env gate is fixed end-to-end.
- do not proceed to fresh one-run retry until auth/env patch or configuration alignment is explicitly verified by non-execution-safe checks.
