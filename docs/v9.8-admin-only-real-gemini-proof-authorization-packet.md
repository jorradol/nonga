# v9.8 Admin-Only Real Gemini Proof Authorization Packet

> **AUTHORIZATION PACKET ONLY / DOCS-ONLY / STATIC VALIDATION / SCRIPT-ONLY**
> v9.8 จัดทำเอกสารอนุมัติขั้นสุดท้ายก่อนงาน proof จริงในอนาคต โดยรอบนี้ยังไม่ยิง Gemini จริง และยังไม่แตะ runtime behavior

---

## 1. Latest Status Summary (v9.2-v9.7)

สถานะล่าสุดที่ยืนยันแล้วจาก v9.2-v9.7:

- v9.2: admin-only runtime proof skeleton อยู่ในสถานะ disabled
- v9.3: provider wiring เป็น placeholder-only และ kill switch protected
- v9.4: smoke harness ยืนยัน no real provider invocation
- v9.5: owner readiness gate ยืนยัน non-GO boundary
- v9.6: env-safe prep ยืนยัน secret-safe + no buyer exposure
- v9.7: dry-run gate ยืนยัน fail-closed + readiness packet สำหรับ future mode

และสถานะปัจจุบันยังคงเป็น:

- no real Gemini call
- no network/provider call
- no buyer-facing AI
- no public route
- no real lead sending
- no deploy
- no production
- deterministic flow remains source of truth

## 2. Required Preconditions Before Any Future Real Admin-Only Gemini Proof

เงื่อนไขที่ต้องครบทุกข้อก่อนอนุญาต future real admin-only Gemini proof:

- owner explicit approval (written and explicit)
- admin-only route only (strict `/api/admin/*` boundary)
- secret env-only policy (`GEMINI_API_KEY` in environment only, never in repo)
- kill switch wins every flag
- quota/cost cap ต่ำและชัดเจน
- fallback deterministic ทำงานทันทีเมื่อ fail
- log redaction ป้องกัน prompt/PII/secret leakage
- no buyer/public path
- no real lead sending
- rollback instruction ต้องชัดและทำได้ทันที

## 3. Decision Wording (v9.8)

- `NOT GO` for production
- `NOT GO` for public route
- `NOT GO` for buyer-facing AI
- `NOT GO` for real lead sending
- `CONDITIONAL READY` only for future admin-only real Gemini proof after explicit owner approval

## 4. v9.9 Forward Checklist (If Real Admin-Only Proof Starts)

ก่อนเริ่ม v9.9 (real admin-only proof) ต้องมี:

- separated Pre-Commit and Pre-Push flow
- no conditional commit + push
- no normal test depends on real secret
- manual/admin-only proof instruction
- strict quota cap and cost cap
- kill switch rollback drill

## 5. Explicit v9.8 Safety Confirmation

- v9.8 is docs-only / static validation / script-only
- no `src/` changes
- no `server.ts` changes
- no route behavior change
- no provider runtime behavior change
- no deploy config change
- no real API key/secret in repo
- no real Gemini call
- no network/provider call
- no buyer-facing/user-visible AI
- no real lead sending
- deterministic flow remains source of truth

## 6. Owner Authorization Requirement

- v9.8 does not authorize execution by itself
- future real admin-only Gemini proof requires explicit owner approval again before v9.9 execution work

