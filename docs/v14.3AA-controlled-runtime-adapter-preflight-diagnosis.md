# v14.3AA — Controlled Runtime Adapter Preflight HOLD Diagnosis + Provider-Path Readiness Prep

## 1) Execution type

v14.3AA target type: controlled runtime adapter preflight HOLD diagnosis + provider-path readiness prep  
execution mode in this round: docs/tests/fixtures/static diagnosis + fail-closed readiness prep only  
one-run execution by agent: no  
retry by agent: no  
second run by agent: no  
provider/Gemini/runtime call by agent: no  
provider network call by agent: no  
deploy by agent: no  
runtime config mutation by agent: no

## 2) Boundary status

- no production: `yes`
- no public route activation: `yes`
- no buyer-facing AI release: `yes`
- no real lead sending: `yes`
- no real customer data / PII: `yes`
- no phone / plate / VIN: `yes`
- no token / secret / API key exposure: `yes`
- no Thor real data import: `yes`
- no dealer real inventory import: `yes`

## 3) Branch / HEAD / origin / working tree before work

- branch: `feature/chat-image-attachment-v1`
- local HEAD: `835f49c1e0306e554237dbad4b1dad860097bd86`
- origin HEAD: `835f49c1e0306e554237dbad4b1dad860097bd86`
- local equals origin: `yes`
- working tree clean: `yes`

## 4) Previous owner-local manual run result (owner-provided)

Sanitized snapshot from owner-local command:

- `owner-local-one-run-gate-v143u`
- `token: ***MASKED***`
- `executeApproved=true`
- `runtimeAdapter=entered`
- `runtimeAdapterPolicy=staging-owner-only-preflight`
- `providerCall=not_run`
- `Gemini/runtime=not_run`
- `providerNetwork=not_run`
- `deploy=not_run`
- `runtimeMutation=not_run`
- `tokenExposure=masked-only`
- `HOLD — runtime adapter reached under controlled preflight (v14.3Y)`

Important handling:

- this owner-local one-run attempt is already consumed for safety
- no retry and no second run in v14.3AA

## 5) Gate 0 repo sanity result

Executed:

```bash
git branch --show-current
git rev-parse HEAD
git fetch origin
git rev-parse origin/feature/chat-image-attachment-v1
git status --short
```

Result:

- branch check: pass
- local/origin sync: pass
- working tree clean: pass
- starting HEAD exact match: pass
- Gate 0 decision: `PASS`

## 6) Exact preflight HOLD source

Exact source file:

- `scripts/owner-local-one-run-gate-v143u.mts`

Exact constant/function/branch:

- `REQUIRED_APPROVAL_TEXT` (fresh approval exact-match gate)
- `parseArgs(...)` + strict unknown-args rejection (`exact command only`)
- `checkToken(...)` on `NONGA_ADMIN_API_TOKEN`
- one-run consume lock (`consumeLockOrHold`)
- `runControlledRuntimeAdapter(...)` hard-stop branch

Exact lines responsible for current HOLD state:

- `console.log("runtimeAdapter=entered")`
- `console.log("runtimeAdapterPolicy=staging-owner-only-preflight")`
- `console.log("providerCall=not_run")`
- `console.log("Gemini/runtime=not_run")`
- `console.log("providerNetwork=not_run")`
- `console.log("HOLD — runtime adapter reached under controlled preflight (v14.3Y)")`
- `process.exit(1)`

## 7) Root cause summary

Primary root cause for this specific HOLD:

- **intentional preflight stop** inside `runControlledRuntimeAdapter(...)`
- not an accidental gate miss
- not a network/provider runtime failure

Why adapter entered but provider did not run:

- wrapper design intentionally reaches adapter only to prove gate path
- adapter branch currently has no dispatch bridge and exits fail-closed

## 8) Existing provider path mapping (current codebase)

Confirmed existing/proven provider-enabled paths:

1. Admin-shadow path (token model: admin API token):
   - route: `/api/admin/sales-brain-shadow-smoke`
   - source: `src/services/ai/salesBrainServerShadowSmoke.ts`
   - gate evidence supports `real_provider_call_ok` and `providerNetwork=true`
   - auth can use `NONGA_ADMIN_API_TOKEN` via `adminApiAuth`

2. User-visible orchestrate path (token model: Firebase ID token):
   - route: `/api/ai/chat-user-visible-orchestrate`
   - source: `src/services/ai/salesBrainServerUserVisibleOrchestrationBridge.ts`
   - real-provider dispatch in `maybeApplyUserVisibleRealProvider(...)`
   - requires authenticated Firebase context (`getServerAuthContext`)
   - this path is where `pilotPathActive`, `fallbackToLegacy`, `skipGemini`, guard/Thai UX markers are surfaced

Auth-model mismatch found:

- wrapper gate uses `NONGA_ADMIN_API_TOKEN`
- user-visible orchestrate path expects Firebase ID token verification flow
- therefore direct bridge from wrapper to user-visible path is **not safely mapped yet** without explicit, reviewed auth-translation design

## 9) Controlled provider-path decision (v14.3AA)

Decision: `not patched` (runtime/provider dispatch not enabled in this round)

Reason:

- provider-path enablement from this wrapper currently has **auth-path ambiguity risk**
- forcing dispatch now may open wrong path (admin-shadow vs user-visible) and break safety intent
- v14.3AA remains diagnosis/readiness-prep only

Readiness prep completed:

- exact HOLD source isolated
- proven provider paths and required gate models mapped
- risk points documented for v14.3AB design review

## 10) No-run / no-retry / no-second-run confirmation

- one-run command executed by agent: no
- retry attempted by agent: no
- second run attempted by agent: no
- owner-local consumed attempt reused in this round: no

## 11) Runtime/provider/Gemini status during agent work

- runtime adapter execution by agent: `not_run`
- provider call by agent: `not_run`
- Gemini call by agent: `not_run`
- provider network by agent: `not_run`

## 12) Token/secret exposure confirmation

- token output remained masked-only (`***MASKED***`)
- no raw token/API key/secret printed or committed
- no Authorization header values exposed

## 13) Files changed

- `docs/v14.3AA-controlled-runtime-adapter-preflight-diagnosis.md`
- `docs/examples/v14.3AA-controlled-runtime-adapter-preflight.synthetic.json`
- `scripts/test-v143AA-controlled-runtime-adapter-preflight.mts`
- `package.json` (`test:v14.3AA`)

## 14) Tests / validation results

Required sequence for this round:

1. `npm run test:v14.3AA`
2. `npm run test:v14.3Y`
3. `npm run test:v14.3X`
4. `npm run test:v14.3W`
5. `npm run test:v14.3V`
6. `npm run test:v14.3U`
7. `npm run test:v14.3T`
8. `npm run test:v14.3S`

If any test fails:

- `HOLD — TEST FAILURE`

## 15) Commit hash / message

- commit hash: `(filled after commit)`
- commit message candidate (diagnosis/prep only):  
  `docs(ai): add v14.3AA runtime preflight diagnosis`

## 16) Push result

- `(filled after push)`

## 17) Branch / HEAD / origin / working tree after work

- `(filled after commit/push verification)`

## 18) Final decision

`NEED REVIEW — EXISTING PROVIDER PATH NOT SAFELY MAPPED`

## 19) Exact next owner action

No one-run command in v14.3AA.

Next step is design/approval review for v14.3AB with explicit safe mapping decision:

- either keep owner-local path on admin-shadow proof route only
- or define reviewed bridge from owner-local wrapper to user-visible orchestrate with correct Firebase-auth model

After that review only, use fresh owner approval and run exactly one owner-local command in same CMD session.
