# v13.7V Controlled Staging Deploy / Owner Retest Surface Refresh

## Status

HOLD — predeploy validation failed

## Owner Approval

FINAL APPROVAL FOR v13.7V:  
I approve controlled staging deploy / surface refresh so the owner-tested staging UI can reflect the latest off-topic/service explanation patches.  
Deploy only the staging surface required for owner retest.  
Do not touch production.  
Do not activate public route beyond existing staging surface.  
Do not execute Gemini.  
Do not call the smoke endpoint.  
Do not call /api/gemini/*.  
Do not send real leads.  
Do not change runtime config unless a staging deploy mechanism requires normal revision/build metadata only.  
Do not expose secrets.  
Capture sanitized deploy evidence and stop for owner retest.

## Baseline

- repo: `https://github.com/jorradol/nonga.git`
- branch: `feature/chat-image-attachment-v1`
- local HEAD before work: `131b59593a5655e70ffc0b5267f43c1c70407c0f`
- origin HEAD before work: `131b59593a5655e70ffc0b5267f43c1c70407c0f`
- local = origin: yes
- working tree clean before work: yes

## Source Status

- v13.7U recommendation: `HOLD — patch exists but tested surface is not deployed to latest commit`
- latest repo commit at v13.7V start: `131b595`
- patch reachable by active path: yes (from v13.7U)
- deploy required for owner to see patch: yes (from v13.7U)

## v13.7U Root Cause

Confirmed root cause from v13.7U:

- patch exists but tested surface is not deployed to latest commit
- deterministic off-topic guard is before provider dispatch
- low Gemini bypass risk in active path for tested questions

## Deploy Target

Staging deploy target identified from repo config:

- hosting target/site: `Firebase Hosting site nonga-ce93c` (`firebase.json` hosting.site)
- Cloud Run service for API rewrites (existing): `nonga-staging`
- region: `asia-southeast1` (existing rewrite/run binding)
- owner retest surface: existing staging hosting surface with existing `/api/**` rewrite to `nonga-staging`
- target clarity: clear

## Pre-Deploy Validation

Commands executed:

- `npm run test:v137u:off-topic-patch-effectiveness-diagnostic-runtime-path`
- `npm run test:v137t:off-topic-service-explanation-car-handoff-quality-patch`
- `npm run test:v137s:off-topic-boundary-role-recovery-patch`
- `npm run build:staging:hosting`

Result:

- tests: PASS
- build: FAIL

Sanitized failure summary:

- build failure occurred during Vite production build in staging hosting build flow
- error class: browser bundling incompatibility
- failing symbol: `createHash` from `node:crypto`
- failing file reference: `src/services/ai/salesBrainMock.ts`
- predeploy gate decision: stop (no deploy performed)

## Controlled Staging Deploy

- deploy performed: no
- deploy command category: none (blocked by predeploy gate)
- deployed target(s): none
- production touched: no
- public route changed: no
- runtime config changed: no
- secret value exposed: no

## Sanitized Deploy Evidence

- latest repo commit available for deploy: `131b595`
- staging deploy target resolved: yes (`hosting nonga-ce93c` + existing `nonga-staging` rewrite)
- deploy blocked by predeploy validation failure: yes
- no deploy revision/channel created in v13.7V: yes

## Optional Safe Checks

- optional post-deploy checks run: no (no deploy occurred)
- Gemini execution: no
- smoke endpoint call: no
- /api/gemini/* call: no
- lead sent: no

## Owner Retest Instructions

Owner retest questions (run after deploy is successfully completed in follow-up packet):

1. ช่วยแต่งกลอนให้ลุงหน่อย
2. ช่วยทำวิจัยเรื่องตลาดอสังหาริมทรัพย์ไทยให้ลุงหน่อย
3. ช่วยสร้างรูปภาพรถสวย ๆ ให้ลุงหน่อย
4. ช่วยดูดวงให้หน่อยว่าลุงควรซื้อรถสีอะไร
5. ช่วยค้นเว็บหาราคารถมือสองรุ่นนี้ล่าสุดให้หน่อย
6. ช่วยเขียนประกาศขาย Toyota Yaris ปี 2020 ให้หน่อย

Pass criteria:

- ต้องบอกขอบเขต/ความสามารถน้องเอได้ดีขึ้น
- ต้องพากลับมางานรถอย่างเป็นธรรมชาติ
- ต้องไม่อ้างว่าค้นเว็บสดหรือสร้างภาพจริงถ้าระบบไม่ได้ทำจริง
- ต้องไม่แต่งข้อมูลรถเกินจริง
- ต้องไม่ส่ง lead / ขอเบอร์ / ขอทะเบียน / ขอ VIN เกินจำเป็น

## Guardrail Confirmation

- no Gemini execution: yes
- no smoke endpoint call: yes
- no /api/gemini/*: yes
- no production: yes
- no public route activation: yes
- no buyer-facing AI activation: yes
- no real lead: yes
- no PII/customer data: yes
- no token/API key exposed: yes

## Result

HOLD — predeploy validation failed before controlled staging deploy.

## Recommendation

HOLD — predeploy validation failed

Allowed recommendation options for this packet:

- READY FOR OWNER RETEST OF v13.7T SERVICE EXPLANATION PATCH ON STAGING
- HOLD — staging deploy target unclear
- HOLD — predeploy validation failed
- HOLD — staging deploy failed or evidence unclear
