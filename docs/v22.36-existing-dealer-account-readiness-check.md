# v22.36 — Existing Dealer Account Readiness Check

Date: 2026-07-10  
Branch: `feature/chat-image-attachment-v1`  
Execution type: `EXISTING DEALER ACCOUNT READINESS CHECK / OWNER-BROWSER FLOW PREP ONLY / NO NEW DEALER BY DEFAULT / NO REAL LEAD / LEAD CAPTURE OFF / NO DEALER-FACING SEND / NO PRODUCTION / NO PUBLIC`

## 1) Purpose

Follow-up to **v22.35 HOLD** (operator lacked signed-in dealer/admin session). Owner concern: a Dealer may already exist — avoid duplicate dealer accounts and repeated testing loops.

This packet is **read-only readiness + owner-browser prep only**. It does **not**:

- create a new dealer
- create a new listing
- turn lead capture ON
- create real leads / send dealer-facing messages
- deploy production / enable public signup
- ask owner for token / header / cookie / secret / env / terminal work

## 2) Decision summary

**Decision code: `READY_EXISTING_DEALER`**

| Question | Answer |
|----------|--------|
| Does an existing invite/pilot dealer already exist? | **Yes** |
| Is it active and mapped? | **Yes** — `role=dealer`, `status=active`, `dealerId` bound |
| Is it Thor Auto? | **No** — separate from inventory partition `thor-auto` |
| Create another dealer? | **No — do not create** (duplicate risk) |
| Create a listing now? | **No** — wait for explicit owner approval |
| What is still needed for live posting test? | Owner/operator **browser login** as the existing dealer (and admin for approve/hold) |

Secondary note: live click-through still needs browser login (`NEED_OWNER_BROWSER_LOGIN_ONLY` for execution), but **account readiness itself is confirmed** — no new dealer provisioning.

## 3) Existing dealer status (PII-redacted)

Inspected via signed-in admin browser on staging Hosting (`/admin/pilot-users`). Emails, Firebase UIDs, phones, and cookies are **not** recorded here.

| Field | Safe value |
|-------|------------|
| Display label | **Nong A Staging Dealer** |
| Role | `dealer` |
| Status | `active` |
| Bound `dealerId` | **`nonga-dealer`** |
| Invite/pilot | Present in Closed Pilot user list |
| Linked Firebase user | Yes (Auth-backed pilot user; UID omitted) |
| Thor Auto? | **No** |
| Can access dealer posting routes (policy) | **Yes** — `RequireDealer` / `canAccessDealerPortal` for active dealer |
| Can create `pending_review` listings (policy) | **Yes** — same gate as v22.32–v22.34 (`POST /api/dealer/drafts/:id/publish` → pending) |
| Duplicate if another dealer created? | **Yes — avoid** |

### Related partitions (not a second “new dealer” to create)

| Partition | Role in staging | Notes |
|-----------|-----------------|-------|
| `thor-auto` | Marketplace inventory / import identity | Public slug on Thor cards; **not** the staging dealer pilot account above |
| `nonga-dealer` | Existing staging dealer pilot account | Use this for owner-browser dealer posting TEST |

Admin dashboard also showed dealer-related inventory signals (e.g. marketplace **13**, Thor cards present). Creating a second dealer account is unnecessary and risks confusion with `thor-auto` aliases (aliases normalize to `thor-auto`).

## 4) Owner/admin browser readiness

| Surface | Result |
|---------|--------|
| Signed-in staging admin session observed | **Yes** (Hosting admin dashboard + Closed Pilot list) |
| `/admin/pilot-users` | **Accessible** while signed in as admin |
| Dashboard entry 「คิวรออนุมัติประกาศเต็นท์」 | **Present** on admin dashboard |
| `/admin/pending-listings` approve / hold UI | **Present** in product (v22.34 PASS); use while signed in as admin |
| Token / terminal required | **No** — Firebase browser session only |
| Operator tab without login | Still gated (expected) |

**Admin readiness:** ready for pending review once signed in.  
**Dealer readiness:** existing `nonga-dealer` account is ready; owner must sign in as that dealer in browser for posting (do not provision a new one).

## 5) Minimal owner-browser checklist (existing dealer only)

Do **not** create a listing until owner explicitly says to proceed.

### Prep (no listing yet)

1. Open staging: `https://a.nongbot.org` (or the same Hosting origin already used for admin).
2. Sign in as the **existing** staging dealer account (display: Nong A Staging Dealer / `dealerId: nonga-dealer`).
3. Open Dealer Portal → Drafts (`/dealer/drafts`) and Inventory (`/dealer/inventory`).
4. Confirm you see **own** drafts/inventory only (no need to create anything yet).
5. Sign out; sign in as owner/admin; open `/admin/pending-listings` once to confirm the queue page loads.

### Only if owner explicitly approves a TEST listing later

1. Login as existing dealer again.  
2. Create draft titled `ทดสอบระบบประกาศ - ห้ามติดต่อจริง` (fake data only; no real PII/images of customers).  
3. Click 「ลงขาย」 → expect 「รออนุมัติ」; marketplace must stay **13** before approval.  
4. Admin: `/admin/pending-listings` → approve **or** hold.  
5. Cleanup/hide immediately; confirm marketplace returns to **13**.  
6. Keep lead capture OFF; no real lead; no dealer send.

## 6) Safety checks (this packet)

| Check | Result |
|-------|--------|
| `leadCaptureEnabled:false` | **PASS** (live health) |
| `publicSignupEnabled:false` | **PASS** |
| Unauth buyer-lead | **401** |
| Marketplace | **13** |
| New dealer created | **No** |
| New listing created | **No** |
| Real lead | **No** |
| Dealer-facing send | **No** |
| Production | untouched |
| Public signup | still OFF |
| Secrets/PII in report | **omitted** (emails/UIDs/phones not copied into doc) |
| Public DTO | no `ownerId`/`dealerId`/VIN/full plate; phones empty (live probe) |
| Thor Auto cards/images | preserved (3 imports) |
| Cloud Run | `nonga-staging-00191-jqq` @ **100%** |

## 7) Technical summary

### Evidence sources (read-only)

- Live: `/api/health`, `/api/cars`, unauth `/api/buyer-leads`, unauth dealer/admin APIs blocked  
- Browser (signed-in admin): `/admin/dashboard`, `/admin/pilot-users`  
- Code/policy: dealer portal publish → `pending_review`; admin approve/hold UI; pilot provision model  

### Files this step

| File | Change |
|------|--------|
| `docs/v22.36-existing-dealer-account-readiness-check.md` | This record |
| `scripts/test-v22.36-existing-dealer-account-readiness-check.mts` | Read-only automated probes |
| `package.json` | `test:v22.36` scripts |

### Tests

| Check | Result |
|-------|--------|
| `npm run test:v22.36` | **PASS** (automated read-only) |

## 8) Risk assessment

### Ready

- Existing active dealer `nonga-dealer` confirmed — **no new dealer needed**  
- Owner/admin can use browser pending queue (v22.34)  
- Staging safety baseline intact  

### Needs owner review

- Confirm the owner-browser TEST listing (if any) should use **`nonga-dealer`**, not Thor Auto import identity  
- Explicit go/no-go before creating any TEST listing  

### Must-fix before controlled live TEST listing

- Owner says proceed to create TEST listing  
- Login as existing dealer + admin; cleanup mandatory  

### Must-fix before real dealer posting / lead capture

- Separate packets; lead capture stays OFF here  

## 9) Recommendation

**PASS**

Existing staging dealer account is present, active, and mapped (`nonga-dealer`). Owner/admin pending UI is browser-ready. **Do not create a new dealer.** No listing was created in this step. Lead capture remains OFF; marketplace stays 13; production untouched.

Next step (only when owner explicitly approves): one owner-browser TEST listing using the **existing** dealer, then cleanup — per section 5.

### Explicit non-recommendations

- Not approval to create another dealer  
- Not approval to create a listing yet  
- Not approval to turn lead capture ON  
- Not approval for real leads / dealer send / public / production  

## 10) Explicit non-actions taken

- Did not create a new dealer  
- Did not create a new listing  
- Did not set `NONGA_LEAD_CAPTURE_ENABLED=true`  
- Did not create real leads or send dealer messages  
- Did not deploy production or enable public signup  
- Did not ask owner for token/header/cookie/secret/env/terminal  
- Did not copy emails/UIDs/phones into this report  
