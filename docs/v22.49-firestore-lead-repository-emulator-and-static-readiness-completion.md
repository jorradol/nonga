# v22.49 — Firestore Lead Repository Emulator and Static Readiness Completion

Date: 2026-07-10  
Branch: `feature/chat-image-attachment-v1`  
Execution type: `FIRESTORE LEAD REPOSITORY EMULATOR AND STATIC READINESS COMPLETION / PHASE 2 D1 / REUSE EXISTING LEAD SYSTEM / LEAD CAPTURE OFF / NO LIVE FIRESTORE WRITE / NO DEALER SEND / STAGING SAFETY PRESERVED`

## 1) Executive summary

Owner Option **D1** completed: the **existing** `FirestoreBuyerLeadRepository` was proven on the **Firebase Emulator** (create → masked queue → wrong-seller deny → skip/withdrawn → interest 0 → targeted one-lead cleanup), with minimum active-duplicate protection, skip `sellerId` alignment, Emulator isolation guards, and a prepared (not deployed) `leadDataBackend` health field.

**No live Staging repository switch. Lead Capture remained OFF. No live Lead write. No Dealer send. No production deploy.**

**Final recommendation: PASS — READY FOR OWNER DECISION**  
Next durable Staging test still requires **fresh Owner approval** (Option D2). Not auto-approved.

---

## 2) Owner-approved D1 scope

In scope: Emulator/static readiness, reuse existing Firestore repo, cleanup design, duplicate guard, health prep, operator checklist.  
Out of scope: live `NONGA_LEAD_DATA_BACKEND=firestore`, live capture ON, live Lead create, Dealer send, production.

---

## 3) Baseline (pre-work)

| Check | Result |
|-------|--------|
| Repo / branch | `D:/nonga` · `feature/chat-image-attachment-v1` |
| HEAD | `cdaf1f8` (local = origin before this packet) |
| Cloud Run | `nonga-staging-00194-72z` @ 100% |
| Hosting | `assets/index-Bsnqa-MN.js` |
| `leadCaptureEnabled` | **false** |
| Lead backend env | **unset** → memory |
| Signup | **false** |
| Unauth buyer-lead | **401** |
| Marketplace | **15** |
| Corollas | Public · `Thor Auto` · 5/5 |
| Public DTO | Protected fields **0** |
| Production | Untouched |

---

## 4) Emulator isolation design

| Control | Behavior |
|---------|----------|
| Host required | `FIRESTORE_EMULATOR_HOST` must be set |
| Project | `demo-nonga-v2249` (demo-* only) |
| Live markers blocked | `nonga-ce93c`, `nonga-prod`, etc. refused |
| Credentials | Cleared for Emulator path; Admin talks to local emulator |
| Live Staging/Prod | Never contacted by Emulator suite |
| Abort | Missing host or live project marker → hard fail |

Harness: `scripts/v22.49-firestore-lead-emulator-harness.mts`  
Lifecycle: `scripts/test-v22.49-firestore-lead-repository-emulator-lifecycle.mts` via `npm run test:v22.49-emulator`

---

## 5) Repository lifecycle results (Emulator)

| Step | Result |
|------|--------|
| Create one synthetic Lead | PASS |
| Server-derived `sellerId` | PASS |
| Masked seller queue | PASS |
| Wrong seller queue/reveal/skip/outcome | PASS (403 / deny) |
| Buyer recipient override ignored | PASS |
| Skip → withdrawn | PASS |
| Interest count → 0 | PASS |
| Targeted cleanup one Lead | PASS |
| Unrelated Seller B fixture intact | PASS |
| Kill switch still blocks when OFF | PASS |

---

## 6) Operation completeness matrix

| Operation | Status |
|-----------|--------|
| create / get / list / update / contact log | COMPLETE (Emulator) |
| masked queue | COMPLETE |
| reveal / skip / outcome auth | COMPLETE (service + Emulator) |
| active interest | COMPLETE |
| invalid listing / missing seller | COMPLETE |
| duplicate active create | COMPLETE (minimum guard) |
| cleanup (gated) | COMPLETE (Emulator / `NONGA_LEAD_TEST_CLEANUP=1`) |
| init failure without creds (non-emulator) | COMPLETE (existing fail-closed) |
| invalid backend → memory | COMPLETE |

---

## 7) Idempotency / duplicate protection

**Choice: Option III + small server guard (hybrid)**

- Operator rule for future live test: exactly one submit, no retry.
- Code: `buyerLeadDuplicateGuard.ts` — if same `buyerUserId` + `listingId` + normalized phone already has an **active** Lead, return existing Lead with `duplicate: true` (no second active record).
- Buyer still cannot set recipient.
- **Classification: PARTIAL — sufficient only for one controlled test** (not production exactly-once; no request-id transaction).

---

## 8) Cleanup design

| Item | Design |
|------|--------|
| Method | `deleteBuyerLeadByIdForControlledCleanup(leadId)` on Firestore repo |
| Gate | Emulator host **or** explicit `NONGA_LEAD_TEST_CLEANUP=1` |
| Scope | Exactly one document id |
| Public/Dealer routes | **None** |
| Production default | Cleanup disabled |
| Future Staging | Prefer skip→withdrawn first; optional gated Admin delete of one synthetic id after approval |
| Emulator proof | Deleted Lead A; Seller B seed remained |

---

## 9) Seller/Dealer isolation

Two synthetic sellers/listings in Emulator. Wrong seller cannot queue/reveal/skip/outcome. Skip service now checks `sellerId` mismatch (aligned with reveal/outcome). Route skip uses listing-derived seller id.

---

## 10) Restart / multi-instance simulation

| Proven (Emulator) | Not yet proven (live Staging) |
|-------------------|-------------------------------|
| New Admin app / repo instance reads same Lead | Live Cloud Run multi-instance with Firestore backend |
| Withdrawn + interest 0 after recreate | Live revision restart smoke |
| Memory pin **not** required for Emulator coherence | Live scale-out with `NONGA_LEAD_DATA_BACKEND=firestore` |

---

## 11) Failure-mode results

| Case | Behavior |
|------|----------|
| No Emulator host in Emulator suite | Abort |
| Live project marker | Abort |
| Cleanup without gate | Throw |
| Capture OFF | 403 create |
| Missing seller | 400 |
| Duplicate active | Return existing, no second Lead |
| Requested Firestore without creds (non-emulator) | Throw (no silent memory fallback once firestore selected) |

---

## 12) Health / diagnostic decision

- Added `leadDataBackend: "memory" | "firestore"` via `getActiveBuyerLeadDataBackend()`.
- No project id, credentials, collection names, or secrets.
- **Prepared in source; not deployed** this packet — live Staging health may omit the field until a future authorized revision.
- Not a security boundary.

---

## 13) Rules / indexes

| Item | Finding |
|------|---------|
| Client rules | Deny-all on `buyerLeads` / `leadContactLogs` (unchanged) |
| `test:v56f2` | Emulator rules PASS |
| Seller isolation | Server/API — not client rules |
| Indexes | `listingId ==` equality; no composite required for current query; no live index deploy |

---

## 14) Operator checklist (planning only)

### Pre-switch
1. Verify Lead Capture OFF (`leadCaptureEnabled: false`).
2. Verify Lead backend memory (env unset / health `leadDataBackend` memory after deploy of this field).
3. Record current Cloud Run revision.
4. Marketplace **15**; no active synthetic Lead; no outbound notify.

### Repository switch (capture still OFF)
1. Set Staging `NONGA_LEAD_DATA_BACKEND=firestore` only.
2. Keep `NONGA_LEAD_CAPTURE_ENABLED` unset/false.
3. Deploy new Staging revision.
4. Verify health `leadDataBackend: firestore`; no Lead write; marketplace unchanged.

### Controlled test (requires fresh Owner approval — D2)
1. Temporary capture ON.
2. Exactly one synthetic Lead; no retry; no Dealer send.
3. Verify routing, queue, masking, isolation; optional restart read.

### Cleanup
1. Skip→withdrawn and/or gated targeted delete of **one** synthetic id.
2. Interest count 0; unrelated records untouched.

### Return safe
1. Capture OFF; decide backend keep/rollback per approval.
2. Verify 401 unauth, 403 auth-OFF, marketplace/DTO, production untouched.

### Rollback
1. Prior revision/config; capture OFF; no second Lead; HOLD on deviation.

---

## 15) Remaining risks

- Duplicate guard is not transactional exactly-once under extreme concurrency.
- Live Staging Firestore Lead path still unproven.
- Health field not live until deploy authorized.
- Hard delete must stay gated forever in production.

---

## 16) Recommended next step

Present Owner options A / B / C2 / D2 / E. Prefer **C2** (switch-only, capture OFF) before **D2** (one durable synthetic Lead), unless Owner explicitly chooses D2 after reading this packet.

---

## 17) Future Owner decision options

| Option | Meaning |
|--------|---------|
| A | Keep memory + capture OFF |
| B | More static/emulator hardening |
| C2 | Staging repository-switch-only, capture OFF |
| D2 | Exactly one synthetic durable Staging Lead (if this packet PASS) |
| E | Dealer queue/reveal later |

None approved by this document.

---

## 18) Final recommendation

**PASS — READY FOR OWNER DECISION**

Emulator lifecycle, isolation, cleanup targeting, and minimum duplicate protection are sufficient to ask the Owner for the next controlled step. Do **not** execute D2 without fresh explicit approval.
