# v14.1A Controlled Staging Deploy + Guard Visibility Check

## 1) Execution type

controlled staging deploy verification only: yes  
staging scope only: yes  
pre-deploy validation + deploy + visibility check: yes  
Gemini run: no  
one-run click: no  
retry: no  
second run: no  
provider network call: no  
production deploy: no  
Cloud Run/runtime config change: no  
public route activation: no  
real lead path touched: no  
buyer-facing AI release: no  
real customer data / PII: no  
phone/plate/VIN exposure: no  
secret/token/API key exposure: no

## 2) Baseline before v14.1A

v14.1 repo patch was already complete at commit:

- `9716083d86c9d441bdee8379b3ba88c592761008`

Status before v14.1A:

- source/guards validated by static tests
- staging deploy had not been executed yet
- recommendation from v14.1: `READY WITH STAGING DEPLOY NEEDED — NO RETEST YET`

## 3) Pre-deploy validation

Executed and passed:

- `npm run test:v14.1`
- `npm run test:v14.0C`
- `npm run test:v14.0B`
- `npm run test:v14.0A`
- `npm run test:v14.0`
- `npm run build:staging:hosting`

Notes:

- build used staging hosting path only
- no production deploy/build route was used for release action
- no runtime/config mutation was performed

## 4) Controlled staging hosting deploy

Executed:

- `npx -y firebase-tools@latest deploy --only hosting --project nonga-ce93c`

Deploy result:

- staging hosting deploy: yes
- production deploy: no
- Cloud Run/runtime config change: no
- public route activation: no
- real lead path touched: no

## 5) Staging served bundle evidence

Checked staging URL:

- `https://a.nongbot.org/admin/dashboard?v=141a`

Served script URL:

- `https://a.nongbot.org/assets/index-CwW6cFCR.js`

Read-only bundle checks:

- found: `Evidence capture readiness: v14.0B contract active`
- found: `One-run evidence (sanitized): waiting for fresh owner-approved run`
- found: `owner-one-run-evidence-readiness`
- not found in served frontend bundle: `USER_VISIBLE_LEAD_PII_CUE_OUTPUT_PATTERNS`
- not found in served frontend bundle: `USER_VISIBLE_PHONE_ECHO_OUTPUT_PATTERNS`
- not found in served frontend bundle: `assertNoLeadOrPiiCueLanguage`
- not found in served frontend bundle: `assertNoPhoneEchoInChatText`
- not found in served frontend bundle: `ขั้นตอนยืนยันความสนใจอย่างปลอดภัย`

Legacy risky instruction strings were checked and not found:

- `ชวนฝากชื่อ/เบอร์ให้ทีมงานติดต่อกลับได้ครับ` -> not found
- `ฝากชื่อและเบอร์ติดต่อให้ทีมงานช่วยประสานรายละเอียดได้ครับ` -> not found

Interpretation:

- v14.0C owner helper readiness marker is present in served bundle
- v14.1 guard symbols/wording are not directly visible in served frontend bundle output from this check
- guard visibility is therefore not fully confirmed from hosting bundle alone in this step

## 6) Owner helper/evidence readiness status

v14.0C readiness markers remain present in served bundle:

- `Evidence capture readiness: v14.0B contract active`
- `One-run evidence (sanitized): waiting for fresh owner-approved run`

No one-run button was clicked in v14.1A.

## 7) Boundary reminder

v14.1A does not perform runtime quality execution:

- Gemini run: no
- one-run click: no
- retry: no
- second run: no

Before any next owner manual quality retest:

`READY FOR OWNER MANUAL GEMINI QUALITY RETEST — FRESH APPROVAL REQUIRED`

## 8) Final recommendation enum (v14.1A)

- `READY FOR OWNER MANUAL GEMINI QUALITY RETEST — FRESH APPROVAL REQUIRED`
- `HOLD — STAGING DEPLOY FAILED`
- `HOLD — STAGING BUNDLE NOT UPDATED`
- `HOLD — V14.1 GUARD VISIBILITY NOT CONFIRMED`
- `HOLD — OWNER HELPER EVIDENCE READINESS MISSING`
- `HOLD — GUARDRAIL GAP DETECTED`
- `HOLD — SECRET/PII/REAL LEAD RISK DETECTED`
