# v22.58 — Card–Answer Consistency + Comparison Grounding Integrity Fix

Date: 2026-07-10  
Branch: `feature/chat-image-attachment-v1`  
Execution type: `CONTROLLED STAGING CARD–ANSWER CONSISTENCY + NAMED COMPARISON GROUNDING INTEGRITY ROOT-CAUSE FIX / EXISTING SEARCH-ORCHESTRATOR-CONTEXT-GEMINI HYBRID ONLY / NO TEST LEAD / NO PILOT EXPANSION / NO PRODUCTION`

## Summary

Owner-browser after v22.57: Q1–Q3 PASS; Q4 FAIL — cards showed Corolla 2020 + Corolla 2021 while text claimed Corolla 2021 was unavailable. This slice makes the inventory-backed compare pair the single canonical source for cards, text, Gemini grounding, and deterministic fallback.

## Proven root cause (evidence)

1. Client orchestrator (sessionStorage) correctly resolved base=Corolla 2020 + target=Corolla 2021 → two cards + deterministic compare text.
2. Signed-in path called server `/api/ai/chat-user-visible-orchestrate`.
3. Server `tryOrchestrateChatReplyCore` used `loadChatCarContext` only — **empty on Node** (no sessionStorage).
4. Named compare failed `no_base`; `resolvePilotOrchestrationHint` fell back to **stale one-card** pilot session (2020).
5. `narrowPilotOrchestrationForExactInventoryAsk("…Corolla 2021")` could not find year 2021 in the one-card set → left 2020-only grounding.
6. Gemini (compare zone) claimed 2021 unavailable; prior guards only checked that year digits appear, so “ไม่มีข้อมูลปี 2021” still passed.
7. Client `applyChatUserVisibleServerBridge` replaced **text only**, kept local two cards → card/text contradiction.

Not a prompt-only defect.

## Fix (reuse existing mechanisms)

| Area | Change |
|------|--------|
| Server context | Bridge injects rehydrated `pilotSessionContext` as `contextCarsOverride` |
| Rehydrate | Match session cards to inventory by listingId / brand+model+year+price |
| listingId | Pilot grounded cards preserve marketplace listing id |
| Narrowing | Named compare never narrows Gemini payload to target-only year |
| Guards | `hasCardAnswerConsistencyFailure` + stronger `hasCompareIdentityFailure` |
| Gemini reject | Keep deterministic orchestrated text when output unsafe |
| Client | Apply server `carCards` with server text (one canonical set) |
| Compare copy | Buyer-oriented deltas (price/year/mileage) without definitive “better” |

No second compare store, search path, context system, Gemini layer, or Lead system.

## Tests

| Suite | Result |
|-------|--------|
| `test:v22.58` | PASS (25) |
| `test:v22.57` | PASS (56) |
| `test:v22.56` | PASS (43) |
| `test:v22.55` | PASS (38) |
| `test:v22.54` | PASS |

## Staging deploy

(filled after deploy)

## Final recommendation

**NEED REVIEW** — Owner-browser must confirm Q4 cards and textual comparison agree before PASS. v22.57 remains HOLD pending that confirmation.
