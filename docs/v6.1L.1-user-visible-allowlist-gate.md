# v6.1L.1 — User-visible Allowlist Gate Implementation

**สถานะ:** Implementation record (code/tests only) — **allowlist gate wired, default-deny, legacy fallback preserved, ยังไม่ deploy, ยังไม่ env update, ยังไม่ lift `SALES_BRAIN_V60R_USER_VISIBLE_BLOCKED`**  
**วันที่:** 2026-06-09  
**Git baseline:** `0cdc4f9d1ced8b6fc1b9d0f0029fd806834083ea` (v6.1L readiness)  
**Branch:** `feature/chat-image-attachment-v1`  
**อ้างอิง:** v6.1L (controlled user-visible AI pilot plan), v6.0V (shadow chat path legacy)

> **v6.1L.1 implement allowlist gate ใน code** — `NONGA_AI_USER_VISIBLE_ALLOWLIST_UIDS` reader + `evaluateUserVisibleGate()` — **ยังไม่เปิด user-visible AI จริง**

---

## สารบัญ

1. [Vision & Execution Summary](#1-vision--execution-summary)
2. [Code Changes](#2-code-changes)
3. [Gate Behavior](#3-gate-behavior)
4. [Runtime Behavior Changes](#4-runtime-behavior-changes)
5. [Tests / Build](#5-tests--build)
6. [Forbidden (Not Done)](#6-forbidden-not-done)
7. [Next Steps](#7-next-steps)

---

## 1. Vision & Execution Summary

### เป้าหมาย v6.1L.1

| Goal | Status |
|------|--------|
| Env reader `NONGA_AI_USER_VISIBLE_ALLOWLIST_UIDS` | ✅ `salesBrainRuntimeFlags.ts` + `salesBrainUserVisibleGate.ts` |
| `isUidAllowlistedForUserVisible()` | ✅ |
| `evaluateUserVisibleGate()` module | ✅ `salesBrainUserVisibleGate.ts` |
| Default-deny (empty/absent allowlist) | ✅ |
| Guest deny | ✅ `guest_uid_missing` |
| Non-allowlisted → legacy fallback | ✅ |
| Allowlisted + flags → still blocked by v60R | ✅ `user_visible_blocked_v60r` |
| Wire chat path (flags-only debug) | ✅ `wireShadowChatPath` |
| Wire shadow runtime diagnostics | ✅ `evaluateSalesBrainShadowRuntime` |
| Redacted diagnostics (no raw UID) | ✅ |
| Lift `SALES_BRAIN_V60R_USER_VISIBLE_BLOCKED` | ❌ **not done** — v6.1L.2 |
| Deploy / env update | ❌ **not done** |
| Real tester UIDs in env | ❌ **not done** |

---

## 2. Code Changes

| File | Change |
|------|--------|
| `src/services/ai/salesBrainRuntimeFlags.ts` | `NONGA_AI_USER_VISIBLE_ALLOWLIST_UIDS_ENV` constant |
| `src/services/ai/salesBrainUserVisibleGate.ts` | **new** — parse, allowlist check, gate evaluation |
| `src/services/ai/salesBrainShadowRuntime.ts` | gate diagnostics on shadow eval; re-export gate |
| `src/services/ai/salesBrainShadowChatPath.ts` | optional `firebaseUid`; redacted gate in `flagsOnlyDebug` |
| `src/services/ai/chat/chatSearchOrchestrator.ts` | optional `firebaseUid` in options |
| `src/hooks/chat/useChat.ts` | pass `user?.uid` to orchestrator + `wireShadowChatPath` |
| `scripts/test-v61l1-user-visible-allowlist-gate.mts` | **new** static + unit tests |
| `package.json` | `test:v61l1-user-visible-allowlist-gate` |

**No new public routes.** No client hardcoded UIDs.

---

## 3. Gate Behavior

### Blocked reason order

1. `production_default_off`
2. `emergency_kill_switch`
3. `user_visible_not_requested` (current staging default)
4. `guest_uid_missing`
5. `allowlist_empty`
6. `uid_not_allowlisted`
7. `provider_not_gemini` / `ai_first_disabled` / `ai_mode_off` / `budget_caps_missing`
8. `user_visible_blocked_v60r` (when all above pass but v60R constant true)

### Diagnostics (redacted)

- `allowlistEntryCount` — count only, **no UID values**
- `uidPresent`, `uidAllowlisted`, `blockedReason`, `v60rBlockActive`

---

## 4. Runtime Behavior Changes

| Surface | User-visible text | Change |
|---------|-------------------|--------|
| Public `/chat` `"เริ่มใหม่"` | exact legacy | **unchanged** |
| Guest / non-allowlisted | legacy orchestrator | **unchanged** |
| Allowlisted + `USER_VISIBLE_ENABLED=true` | legacy (v60R block) | **unchanged** — gate logs only |
| Shadow eval | legacy `userVisibleResponse` | **unchanged** |
| Debug logs / flagsOnlyDebug | redacted gate summary | **added** — dev console only |

**No payment / lead / reveal / outcome / revenue path changes.**

---

## 5. Tests / Build

| Command | Expected |
|---------|----------|
| `npm run test:v61l1-user-visible-allowlist-gate` | PASS |
| `npm run test:v61l-controlled-user-visible-ai-pilot-plan` | PASS |
| `npm run test:v60v-shadow-chat-path-wiring-user-visible-legacy` | PASS |
| `npm run test:v61k-chat-path-shadow-real-provider-sink-only` | PASS |
| `npm run test:v61k4-chat-shadow-global-kill-switch-fix` | PASS |
| `npm run lint` | PASS |
| `npm run build:staging:hosting` | PASS |

---

## 6. Forbidden (Not Done)

| Item | Status |
|------|--------|
| Cloud Run / Hosting deploy | ❌ |
| `gcloud run services update` | ❌ |
| `NONGA_AI_USER_VISIBLE_ENABLED=true` on staging | ❌ |
| `NONGA_AI_CHAT_SHADOW_REAL_PROVIDER_ENABLED=true` | ❌ |
| Lift `SALES_BRAIN_V60R_USER_VISIBLE_BLOCKED` | ❌ |
| Public AI | ❌ |
| Production | ❌ |
| Firestore rules deploy | ❌ |
| Secret access | ❌ |
| Real tester UIDs in env | ❌ |
| payment / settlement / lead / reveal / outcome mutation | ❌ |

---

## 7. Next Steps

1. Review v6.1L.1 diff + regression results
2. Commit/push when approved
3. **v6.1L.2** — staging execution with approval phrase + allowlist UIDs + §7/§8 smoke from v6.1L plan
