# v6.1L.1a — User-visible Allowlist Gate Staging Deploy Record

**สถานะ:** Execution record — **Cloud Run image-only deployed (v6.1L.1 allowlist gate code live), Hosting skipped, env unchanged, user-visible off**  
**วันที่:** 2026-06-09  
**Git commit (slice):** `e3e139ed6912f4cc42df095240079d09e60f1a6c` (v6.1L.1 allowlist gate)  
**Branch:** `feature/chat-image-attachment-v1`  
**อ้างอิง:** v6.1L.1 (allowlist gate implementation), v6.1L (pilot plan), v6.1K.5 (previous image baseline)

> **v6.1L.1a deploy allowlist gate code ขึ้น staging โดย image-only** — **ไม่** env update, **`NONGA_AI_USER_VISIBLE_ENABLED=false`**, ไม่ใส่ allowlist UID จริง, public chat legacy unchanged

---

## สารบัญ

1. [Vision & Execution Summary](#1-vision--execution-summary)
2. [Preflight (Before Deploy)](#2-preflight-before-deploy)
3. [Tests / Build Before Deploy](#3-tests--build-before-deploy)
4. [Cloud Build (Executed)](#4-cloud-build-executed)
5. [Cloud Run Deploy (Image-only)](#5-cloud-run-deploy-image-only)
6. [Post-deploy Smoke](#6-post-deploy-smoke)
7. [Final Env Summary](#7-final-env-summary)
8. [Hosting vs Cloud Run](#8-hosting-vs-cloud-run)
9. [Rollback Plan](#9-rollback-plan)
10. [Compliance (v6.1L.1a)](#10-compliance-v61l1a)

---

## 1. Vision & Execution Summary

### เป้าหมาย v6.1L.1a

Deploy commit `e3e139e` (v6.1L.1 allowlist gate) ไป Cloud Run staging **image-only** เพื่อให้ server-side gate code live โดยยังปิด user-visible AI ทั้งหมด

### Execution outcome

| Area | Result |
|------|--------|
| Preflight | ✅ HEAD = `e3e139e`, git clean |
| Pre-deploy tests / build | ✅ all PASS |
| Cloud Build | ✅ **SUCCESS** — `323b4f42-b563-46d0-9e0a-f2ad4f6346ee` |
| Cloud Run image deploy | ✅ rev **`nonga-staging-00067-f6v`** @ **100%** — **no env update** |
| Post-deploy smoke | ✅ health, auth guards, chat legacy — **PASS** |
| Firebase Hosting deploy | ❌ **SKIPPED** |
| Env update | ❌ **none** |
| User-visible AI | ❌ **off** — legacy chat only |
| Allowlist UIDs in env | ❌ **absent** |

---

## 2. Preflight (Before Deploy)

| Check | Value |
|-------|-------|
| **git status** | clean |
| **branch** | `feature/chat-image-attachment-v1` |
| **HEAD** | `e3e139ed6912f4cc42df095240079d09e60f1a6c` |
| **Firebase project** | `nonga-ce93c` |
| **Cloud Run service** | `nonga-staging` |
| **Region** | `asia-southeast1` |
| **Previous revision** | `nonga-staging-00066-hch` @ **100%** |
| **Previous image** | `v6.1K.5-chat-shadow-kill-switch-fix` |
| **Public signup** | **`false`** |

**Did not run** `gcloud secrets versions access` — no secret values in this record.

---

## 3. Tests / Build Before Deploy

| Command | Result |
|---------|--------|
| `npm run test:v61l1-user-visible-allowlist-gate` | **PASS** |
| `npm run test:v61l-controlled-user-visible-ai-pilot-plan` | **PASS** |
| `npm run test:v60v-shadow-chat-path-wiring-user-visible-legacy` | **PASS** |
| `npm run test:v61k-chat-path-shadow-real-provider-sink-only` | **PASS** |
| `npm run test:v61k4-chat-shadow-global-kill-switch-fix` | **PASS** |
| `npm run lint` | **PASS** |
| `npm run build:staging:hosting` | **PASS** (local Vite only — not deployed) |

---

## 4. Cloud Build (Executed)

```bash
# PowerShell — quoted substitutions required
gcloud builds submit --project=nonga-ce93c \
  --config=cloudbuild.v53f.yaml \
  --substitutions="_IMAGE_URI=asia-southeast1-docker.pkg.dev/nonga-ce93c/nonga-staging/nonga-staging:v6.1L.1-user-visible-allowlist-gate,..."
```

| Field | Value |
|-------|-------|
| **Cloud Build ID** | `323b4f42-b563-46d0-9e0a-f2ad4f6346ee` |
| **Status** | **SUCCESS** (~2m 6s) |
| **Image tag** | `v6.1L.1-user-visible-allowlist-gate` |
| **Image URI** | `asia-southeast1-docker.pkg.dev/nonga-ce93c/nonga-staging/nonga-staging:v6.1L.1-user-visible-allowlist-gate` |
| **Image digest** | `sha256:d016f74b48ce9c4a58a1dfd11a6ca55789e2c7b5691354ee478d8e5e9b7489e6` |

**Did not run** `gcloud secrets versions access` during build.

---

## 5. Cloud Run Deploy (Image-only)

```bash
# Executed — image-only, no --set-env-vars / --update-env-vars
gcloud run services update nonga-staging \
  --project=nonga-ce93c \
  --region=asia-southeast1 \
  --image=asia-southeast1-docker.pkg.dev/nonga-ce93c/nonga-staging/nonga-staging:v6.1L.1-user-visible-allowlist-gate
```

| Field | Value |
|-------|-------|
| **Deploy type** | **image-only** |
| **Previous revision** | `nonga-staging-00066-hch` |
| **New revision** | **`nonga-staging-00067-f6v`** @ **100%** |
| **Env/secrets** | **preserved** — no env update |
| **Service URL** | `https://nonga-staging-265743710806.asia-southeast1.run.app` |
| **Smoke base (Hosting rewrite)** | `https://a.nongbot.org` |

---

## 6. Post-deploy Smoke

Smoke base: **`https://a.nongbot.org`** — revision `nonga-staging-00067-f6v`

### Health & auth

| Check | HTTP | Result |
|-------|------|--------|
| `GET /api/health` | **200** | `ok:true`, `publicSignupEnabled:false` — **PASS** |
| `GET /api/admin/revenue/preview` (unauth) | **401** | **PASS** |
| `POST /api/admin/chat-shadow-sink` unauth | **401** | **PASS** |

### Public chat (guest — legacy)

| Check | Result |
|-------|--------|
| URL | `https://a.nongbot.org/chat` |
| Message | `"เริ่มใหม่"` |
| User-visible response | **exact legacy:** `ยกเลิกข้อมูลเดิมแล้วครับ พิมพ์ข้อมูลรถคันใหม่ที่ต้องการลงขายได้เลยครับ` — **PASS** |
| User-visible AI replacement | ❌ **none** — **PASS** |
| Provider call from public chat | ❌ **none observed** |

### Safety

| Check | Result |
|-------|--------|
| Raw PII in smoke responses | ❌ **none observed** |
| Raw UID in API responses | ❌ **none** |
| Secret / env dump | ❌ **none** |
| payment / lead / reveal / outcome mutation | ❌ **none** |

**Note:** Client bundle on Hosting unchanged — chat orchestrator legacy path verified; server allowlist gate live on Cloud Run API revision.

---

## 7. Final Env Summary

Revision `nonga-staging-00067-f6v` — **env preserved from 00066-hch** (no update in this slice)

| Key | Value |
|-----|-------|
| `NONGA_AI_PROVIDER` | `gemini` |
| `NONGA_AI_MODE` | `high` |
| `NONGA_AI_FIRST_ENABLED` | `true` |
| `NONGA_AI_SHADOW_MODE_ENABLED` | `true` |
| `NONGA_AI_USER_VISIBLE_ENABLED` | **`false`** |
| `NONGA_AI_EMERGENCY_KILL_SWITCH` | **`false`** |
| `NONGA_AI_BUDGET_DAILY_LIMIT` | `5` |
| `NONGA_AI_BUDGET_MONTHLY_LIMIT` | `50` |
| `NONGA_AI_ADMIN_SHADOW_REAL_PROVIDER_ENABLED` | `true` |
| `NONGA_AI_CHAT_SHADOW_REAL_PROVIDER_ENABLED` | **`false`** |
| `NONGA_AI_USER_VISIBLE_ALLOWLIST_UIDS` | **absent** |
| `GEMINI_API_KEY` | secret ref `gemini-api-key` (metadata only) |
| `FIREBASE_SERVICE_ACCOUNT_JSON` | secret ref (metadata only) |

**Did not dump** secret values.

---

## 8. Hosting vs Cloud Run

| Component | v6.1L.1a action |
|-----------|-----------------|
| **Cloud Run** | ✅ image deploy `v6.1L.1-user-visible-allowlist-gate` → `00067-f6v` |
| **Firebase Hosting** | ❌ **Not redeployed** — client bundle unchanged |
| **Firestore rules** | ❌ **not deployed** |
| **Production** | ❌ **not deployed** |

API routes via Hosting rewrite hit new Cloud Run revision; `/chat` client bundle unchanged.

---

## 9. Rollback Plan (Prepared — Not Executed)

```bash
gcloud run services update nonga-staging \
  --project=nonga-ce93c \
  --region=asia-southeast1 \
  --image=asia-southeast1-docker.pkg.dev/nonga-ce93c/nonga-staging/nonga-staging:v6.1K.5-chat-shadow-kill-switch-fix
```

Previous image: `v6.1K.5-chat-shadow-kill-switch-fix` (revision `nonga-staging-00066-hch`)

**Rollback not needed** — smoke passed.

---

## 10. Compliance (v6.1L.1a)

| Constraint | Status |
|------------|--------|
| Staging only `nonga-ce93c` | ✅ |
| Image-only deploy | ✅ |
| Env update | ❌ **not done** |
| `NONGA_AI_USER_VISIBLE_ENABLED=true` | ❌ **not set** |
| `NONGA_AI_CHAT_SHADOW_REAL_PROVIDER_ENABLED=true` | ❌ **not set** |
| Allowlist real UIDs | ❌ **not set** |
| Lift `SALES_BRAIN_V60R_USER_VISIBLE_BLOCKED` | ❌ **not done** (code in image; constant still true) |
| Public AI | ❌ **none** |
| `gcloud secrets versions access` | ❌ **not run** |
| Firebase Hosting deploy | ❌ **SKIPPED** |
| Firestore rules deploy | ❌ **not done** |
| Production | ❌ **not done** |
| payment / settlement / lead / reveal / outcome | ❌ **not touched** |
| public signup | ❌ unchanged `false` |
| real stock import | ❌ still blocked |
| public debug endpoint | ❌ **none** |

---

## Next Steps

1. Optional: Hosting deploy when client `firebaseUid` wiring should match live bundle (not required for server gate)
2. **v6.1L.2** — controlled pilot execution requires v6.1L approval phrase + env allowlist (separate slice)
