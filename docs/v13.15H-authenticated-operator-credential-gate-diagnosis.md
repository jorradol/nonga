# v13.15H Authenticated Operator Credential Gate Diagnosis (No Gemini Execution / No Runtime Change)

## 1) Execution type

- diagnosis/readiness only: yes
- no Gemini execution: yes
- no provider network call: yes
- no runtime config change: yes
- no deploy: yes
- no production: yes
- no public route activation: yes
- no buyer-facing release: yes
- no real lead: yes
- no real PII: yes
- no phone/plate/VIN: yes
- no secret/API key/token exposure: yes
- no automatic retry: yes
- no second run: yes

## 2) Baseline local/origin ก่อนเริ่ม

- branch: `feature/chat-image-attachment-v1`
- local `HEAD`: `e0d90766d6d1ca9d3e131db8e086c8f65f82e3c5`
- `origin/feature/chat-image-attachment-v1`: `e0d90766d6d1ca9d3e131db8e086c8f65f82e3c5`
- local = origin: yes
- working tree clean before run: yes

## 3) Credential/auth gate summary

Root finding for v13.15G hold:

1. pilot-path prequalification itself is healthy (`pilotPathActive=true` in runtime-shaped synthetic harness).
2. one-run dispatch path is blocked by operator/session credential readiness, not by pilot-path logic.
3. protected route `/api/ai/chat-user-visible-orchestrate` requires authenticated Firebase identity (`getServerAuthContext()`), not a guest request.
4. real provider dispatch also requires runtime key presence (`GEMINI_API_KEY`) on server runtime when dispatch path is reached.

## 4) Required credentials list (masked/presence-only)

Presence-only classification (no value shown):

- `NONGA_ADMIN_API_TOKEN`: missing in operator session
- `VITE_NONGA_ADMIN_API_TOKEN`: missing in operator session
- `NONGA_DEV_FIREBASE_TOKEN_MAP`: missing in operator session
- `GEMINI_API_KEY` in operator shell: missing

This diagnosis keeps values redacted/masked and does not print any raw secret.

## 5) Where each credential must live

### 5.1 Protected route auth path

- For `/api/ai/chat-user-visible-orchestrate`, server code uses `getServerAuthContext(req)` (Firebase ID token verification path).
- Therefore, required auth for execution is:
  - valid Firebase ID token for an admin/superadmin account (or dev-mock token mapping in non-production test harness).

### 5.2 Admin API token path

- `NONGA_ADMIN_API_TOKEN` is used by admin token helper/fallback tooling (`apiAuth`, `apiAuthHeaders`) and admin API route guard flows.
- It is not the direct credential contract for the user-visible orchestrate route when using Firebase auth context path.

### 5.3 Vite admin token fallback

- `VITE_NONGA_ADMIN_API_TOKEN` is optional fallback for client/operator helper mode in `apiAuthHeaders`.
- It is not the canonical runtime secret for Gemini provider calls.

### 5.4 Dev Firebase token map

- `NONGA_DEV_FIREBASE_TOKEN_MAP` is a non-production dev-mock identity map used by `serverAuthContext`.
- It should exist only in local/dev operator environment when using mock Firebase token flow.

### 5.5 Gemini provider key

- `GEMINI_API_KEY` must be present in server runtime environment (staging Cloud Run runtime) for actual provider dispatch.
- Operator local shell value is only relevant when executing local server/provider code path, not as proof of staging runtime value.

## 6) Whether owner must provide any value

Yes. For v13.15I one-run retry, owner/operator must provide or confirm a secure authenticated operator path:

1. either valid Firebase admin/superadmin ID token path for protected route invocation, or approved secure dev-mock auth path in non-production rehearsal context;
2. and confirmation that staging runtime has valid `GEMINI_API_KEY` mounted on service runtime (presence-only confirmation, no raw value).

## 7) Whether values already exist in staging runtime or only missing in operator session

- From this run, we can confirm only operator-session presence (missing in current shell).
- We cannot re-read live staging runtime secret values in this diagnosis without authenticated runtime access path.
- Last known records (v13.15D) indicated masked runtime evidence where Gemini key was present on staging windows, but that evidence is historical and not re-validated live in v13.15H.

## 8) Protected route auth requirement

Confirmed from code path:

- route handler `handleChatUserVisibleOrchestratePost()` immediately calls `getServerAuthContext(req)`.
- no anonymous execution path is allowed.
- missing/invalid Firebase ID token returns auth error (401/403 path), preventing one-run dispatch attempt.

## 9) Gemini provider credential requirement

Confirmed from code path:

- `maybeApplyUserVisibleRealProvider()` eligibility checks include `missing_gemini_key`.
- dispatch caller reads `GEMINI_API_KEY` from server env before provider call.
- key must exist on runtime service where provider invocation executes.

## 10) Pilot path status carried forward

Carried forward from v13.15F/G static runtime-shaped proof:

- `pilotPathActive=true`: yes
- `fallbackToLegacy=false`: yes
- synthetic sanitized context path works
- missing-context fallback remains safe (`pilot_path_inactive`)

## 11) One-run authorization consumed or not

- consumed: no
- run count executed: `0/1`
- second run attempted: no

## 12) Safety boundary confirmation

- no Gemini dispatch
- no provider network call
- no runtime mutation/deploy
- no public/prod/buyer-facing activation
- no lead/PII/phone/plate/VIN usage

## 13) Secret exposure prevention evidence

- diagnosis output is presence-only/masked-only
- no raw secret/token value in docs/test
- secret-like pattern scan included in validator
- no command in this slice prints raw credential material

## 14) Test result: `npm run test:v13.15H`

- pass

## 15) Files changed

- `docs/v13.15H-authenticated-operator-credential-gate-diagnosis.md`
- `scripts/test-v1315h-authenticated-operator-credential-gate-diagnosis.mts`
- `package.json`

## 16) Commit hash full/short

- full: (filled after commit)
- short: (filled after commit)

## 17) Push result

- (filled after push)

## 18) Local/origin sync result

- (filled after push verification)

## 19) Final recommendation

READY FOR OWNER ACTION TO PROVIDE/CONFIRM SECURE CREDENTIAL PATH BEFORE v13.15I ONE-RUN RETRY
