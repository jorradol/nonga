# v22.15 - Revenue-Safe Staging Pilot Readiness Packet

Date: 2026-07-09
Branch: `feature/chat-image-attachment-v1`
Execution type: `CONTROLLED STAGING REVENUE-SAFE PILOT READINESS PACKET ONLY / NO PRODUCTION / NO PUBLIC / NO REAL LEAD / NO DEALER-FACING SEND`

## 1) Purpose

Prepare the next safe readiness packet after Step 3 Thor Auto CSV staging import core PASS (`v22.14`).

This packet is readiness-only. It does **not** execute a revenue pilot run, create real leads, enable public signup, deploy production, or send dealer-facing messages.

## 2) Baseline from v22.14

`PASS — Step 3 Thor Auto real CSV staging import core is complete, and the project is ready for the next controlled staging revenue-safe pilot readiness step.`

Confirmed baseline:

- Thor Auto CSV import succeeded in staging
- Confirm Import succeeded
- imported listings remain after refresh
- staging only
- no production/public/real lead/dealer-facing action in Step 3 PASS record

## 3) Readiness scope (verify only)

Focus only on verifying these staging-safe surfaces:

1. marketplace list shows imported Thor cars
2. listing detail refresh keeps data/images
3. chat card can retrieve imported persisted listings
4. license plate remains masked and full plate is not echoed
5. no real lead is created
6. no dealer-facing send is triggered
7. public signup remains disabled

Out of scope for v22.15:

- production deploy
- public enable
- real lead creation
- dealer-facing send
- reworking already-resolved import mapping/auth/image/persistence/env-guard areas unless new evidence appears

## 4) Owner-friendly Thai readiness checklist (browser only)

สำหรับลุงเด่น — ตรวจความพร้อมรอบถัดไป (staging เท่านั้น):

1. เปิด `https://a.nongbot.org/marketplace` แล้วดูว่ารถที่นำเข้าจาก Thor ยังโชว์ในรายการ
2. เปิดหน้ารายละเอียดรถ 1 คัน แล้ว refresh หน้า — ข้อมูลและรูปต้องยังอยู่
3. เปิดแชท แล้วลองถามหารถที่เพิ่งนำเข้า — การ์ดรถต้องดึงจาก listing ที่ persist แล้วได้
4. ตรวจว่าทะเบียนที่โชว์สาธารณะเป็นแบบปิดบางส่วนเท่านั้น และไม่เห็นทะเบียนเต็ม/VIN/เบอร์โทร/ที่อยู่
5. ห้ามกดสร้าง lead จริง และห้ามส่งข้อความหาดีลเลอร์จริง
6. ยืนยันว่ายังเป็น staging และยังไม่เปิดสมัครสาธารณะ

หมายเหตุ:

- ใช้ browser login/session ตามปกติ
- ไม่ต้องใช้เทอร์มินัล
- ไม่ต้อง copy token/header/cookie/secret/env

## 5) Operator verification checklist (safe, no secrets)

Operator may confirm without exposing secrets:

- `/api/health` reports `dataBackend: "firestore"`
- `/api/health` reports `publicSignupEnabled: false`
- marketplace `/api/cars` still returns imported staging listings after refresh
- public DTO path continues to hide full plate / VIN / phone / address
- no real lead route is exercised in this readiness packet
- no dealer-facing send route is exercised in this readiness packet

## 6) Evidence rules

Capture only safe evidence:

- marketplace visibility of imported Thor cars (count / titles only if public-sale-safe)
- detail refresh still shows data/images
- chat card retrieves persisted listing
- masked plate present; full plate absent from public surfaces
- confirmation that no real lead was created
- confirmation that no dealer-facing send occurred
- confirmation that public signup remains disabled

Do not capture:

- tokens / headers / cookies / secrets / env values
- VIN / full plate / phone / address / buyer PII
- private dealer notes / cost / margin / payment data

## 7) PASS / NEED OWNER CHECK / NEED REVIEW / HOLD criteria

- PASS:
  - readiness packet prepared
  - Step 3 PASS baseline recorded
  - next verification checklist is clear and staging-safe
  - no production/public/real lead/dealer-facing action included
- NEED OWNER CHECK:
  - packet ready, but owner browser verification of marketplace/detail/chat privacy still pending
- NEED REVIEW:
  - evidence incomplete for one of the seven readiness checks
- HOLD:
  - any production/public/real lead/dealer-facing boundary violation

## 8) Step status in v22.15

- Step 1: `completed`
- Step 2: `completed`
- Step 3: `completed_thor_csv_staging_import_core`
- Step 3 completed?: `true`
- Step 4: `not_started`
- Step 5: `not_started`
- Revenue-safe staging pilot run: `not_started`
- This packet authorizes readiness preparation only, not pilot execution

## 9) Explicit non-approvals in v22.15

- no production deploy
- no public enable
- no real lead creation/send
- no dealer-facing send
- no Step 4 movement
- no automatic revenue pilot execution without a separate controlled authorization

## 10) Decision result

`PASS — v22.15 revenue-safe staging pilot readiness packet prepared; Step 3 import core remains complete; pilot run not started.`
