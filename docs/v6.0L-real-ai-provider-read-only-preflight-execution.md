# v6.0L — Real AI Provider Read-only Preflight Execution Record

**สถานะ:** Execution record (docs/tests only) — **read-only preflight บน staging จริงแล้ว, ยังไม่เปิด/แก้ env/secrets จริง, ไม่ deploy, ไม่เรียก paid AI API, ไม่ fetch network ใน CI, ไม่เปลี่ยน user-visible runtime behavior**  
**วันที่:** 2026-06-07  
**อ้างอิง:** v6.0H (enablement plan stage L), v6.0I (secret preflight), v6.0J (real provider stub), v6.0K (approval gate), v5.6J (SuperAdmin AI Control)

> **v6.0L ยังไม่เปลี่ยน runtime behavior** — Stage **L preflight execution** บันทึกผล read-only จาก staging จริงก่อนเปิด real paid AI provider ในอนาคต (หลัง v6.0K approval phrase)

---

## สารบัญ

1. [Vision & Execution Summary](#1-vision--execution-summary)
2. [Git Baseline (Recorded)](#2-git-baseline-recorded)
3. [Cloud Run Staging (Read-only Describe)](#3-cloud-run-staging-read-only-describe)
4. [AI Flags — Current (All Absent)](#4-ai-flags--current-all-absent)
5. [Secret Metadata (No Values)](#5-secret-metadata-no-values)
6. [API Smoke (Staging)](#6-api-smoke-staging)
7. [AI Runtime Status (Code)](#7-ai-runtime-status-code)
8. [Readiness Gaps Before Real Enablement](#8-readiness-gaps-before-real-enablement)
9. [Read-only Commands Executed](#9-read-only-commands-executed)
10. [Forbidden Actions (v6.0L)](#10-forbidden-actions-v60l)
11. [Relationship to v6.0H Stage L](#11-relationship-to-v60h-stage-l)

---

## 1. Vision & Execution Summary

### เป้าหมาย v6.0L

บันทึกผล **read-only preflight execution** บน staging จริง เพื่อตรวจความพร้อมก่อนเปิด real paid AI provider ในอนาคต โดย:

- **Read-only / describe / GET smoke เท่านั้น** — ไม่ update env, ไม่ access secret values
- ยืนยัน AI enablement flags ยัง **ไม่เปิดจริง**
- ยืนยัน revenue/settlement guardrails ยังทำงาน
- ยืนยัน Sales Brain real provider ยัง **network disabled** และ **ไม่ wire** ไป chat runtime
- **ไม่เรียก paid AI API** ใน slice นี้

### Execution outcome

| Area | Result |
|------|--------|
| Git baseline | ✅ clean, synced with origin |
| Cloud Run staging describe | ✅ recorded (redacted) |
| AI flags | ✅ all absent — AI First **not enabled** |
| Secret metadata | ⚠️ gemini exists; openai missing; name mapping gap documented |
| API smoke | ✅ health 200; revenue previews 401 unauth |
| AI runtime (code) | ✅ network disabled; not wired to chat |
| Ready for real enablement | ❌ — gaps remain (see §8); v6.0K approval phrase required |

---

## 2. Git Baseline (Recorded)

Recorded at preflight execution time:

| Check | Value |
|-------|-------|
| **git status** | clean (nothing to commit, working tree clean) |
| **branch** | `feature/chat-image-attachment-v1` |
| **HEAD** | `453202a33cdedc94b7090d19b92bf695db9255f6` |
| **origin/feature/chat-image-attachment-v1** | `453202a33cdedc94b7090d19b92bf695db9255f6` |
| **HEAD = origin** | ✅ yes |
| **Commit message** | `docs(ai): add staging real ai provider dry-run approval gate` (v6.0K) |

---

## 3. Cloud Run Staging (Read-only Describe)

### Scope

| Item | Value |
|------|-------|
| **GCP project** | `nonga-ce93c` |
| **Cloud Run service** | `nonga-staging` |
| **Region** | `asia-southeast1` |
| **Latest ready revision** | `nonga-staging-00047-7xc` |
| **Service URL** | `https://nonga-staging-dpf3rexexq-as.a.run.app` |
| **Hosting (API smoke)** | `https://nonga-ce93c.web.app` |

### Non-secret env (recorded, redacted)

| Env var | Value |
|---------|-------|
| `NODE_ENV` | `production` |
| `APP_URL` | `https://nonga-ce93c.web.app` |
| `VITE_NONGA_PUBLIC_SIGNUP_ENABLED` | **`false`** ✅ |
| `VITE_FIREBASE_PROJECT_ID` | `nonga-ce93c` |
| `FIREBASE_PROJECT_ID` | `nonga-ce93c` |
| `FIREBASE_STORAGE_BUCKET` | `nonga-ce93c.firebasestorage.app` |
| `NONGA_DATA_BACKEND` | **`firestore`** |
| `NONGA_IMAGE_BACKEND` | **`firebase-storage`** |
| `NONGA_SETTLEMENT_DATA_BACKEND` | `firestore` |
| `NONGA_SETTLEMENT_FIRESTORE_WRITES_ENABLED` | `true` |
| `NONGA_SUCCESS_FEE_RECORD_ENABLED` | `true` |

### Secret ref (name only — no value)

| Env var | Secret Manager ref |
|---------|-------------------|
| `GEMINI_API_KEY` | resource `gemini-api-key`, key `latest` |
| `FIREBASE_SERVICE_ACCOUNT_JSON` | resource `firebase-service-account-json`, key `latest` |

**ห้าม copy ค่า secret ลง repo, docs, Slack, หรือ test log**

---

## 4. AI Flags — Current (All Absent)

AI enablement flags on revision `nonga-staging-00047-7xc`:

| Flag | Status on staging |
|------|-------------------|
| `NONGA_AI_PROVIDER` | **absent** |
| `NONGA_AI_MODE` | **absent** |
| `NONGA_AI_FIRST_ENABLED` | **absent** |
| `NONGA_AI_EMERGENCY_KILL_SWITCH` | **absent** |
| `NONGA_AI_BUDGET_DAILY_LIMIT` | **absent** |
| `NONGA_AI_BUDGET_MONTHLY_LIMIT` | **absent** |
| `NONGA_AI_CONTROL_MODE` | **absent** — runtime default **`economy`** via `resolveAiControlConfig()` |

### Summary

**AI First ยังไม่เปิดจริง** — ไม่มี env enablement flags บน staging revision ปัจจุบัน; chat ยังใช้ legacy deterministic path (`chatSearchOrchestrator`)

---

## 5. Secret Metadata (No Values)

Preflight checked Secret Manager **metadata only**. **Did not run** `gcloud secrets versions access`.

### Cloud Run → Secret Manager mapping (recorded)

| Logical env var (Cloud Run) | SM resource name | Version ref |
|----------------------------|------------------|-------------|
| `GEMINI_API_KEY` | **`gemini-api-key`** | `latest` |

### Secret Manager describe results

| Secret resource name | Result | Metadata (no value) |
|---------------------|--------|---------------------|
| **`gemini-api-key`** | **exists** | created `2026-05-26T00:28:19Z`, replication `automatic` |
| **`openai-api-key`** | **NOT_FOUND** | — |
| **`GEMINI_API_KEY`** (canonical SM name in v6.0I/J/K docs) | **NOT_FOUND** | — |
| **`OPENAI_API_KEY`** (canonical SM name in v6.0I/J/K docs) | **NOT_FOUND** | — |

### Doc gap — logical name vs SM resource name

| Layer | Name |
|-------|------|
| **Logical env var** (Cloud Run container env) | `GEMINI_API_KEY` |
| **Secret Manager resource** (actual GCP resource) | `gemini-api-key` |
| **salesBrain module logical reference** (`defaultApiKeySecretName`) | `GEMINI_API_KEY` |

**Action before real enablement:** document and verify mapping **`GEMINI_API_KEY` → `gemini-api-key`** in ops runbook; create `openai-api-key` (or equivalent) if provider=openai selected.

**Confirmed:** `gcloud secrets versions access` was **not executed** in this preflight.

---

## 6. API Smoke (Staging)

Executed via GET to hosting URL `https://nonga-ce93c.web.app` (read-only smoke, not AI provider):

| Endpoint | Expected | Actual | Notes |
|----------|----------|--------|-------|
| `GET /api/health` | 200 | **200** ✅ | `ok:true`, `dataBackend:firestore`, `imageBackend:firebase-storage`, `publicSignupEnabled:false` |
| unauth `GET /api/admin/revenue/preview` | 401 | **401** ✅ | revenue guardrail intact |
| unauth `GET /api/my/revenue/preview` | 401 | **401** ✅ | revenue guardrail intact |

**revenue/settlement guardrails ยังทำงาน** — unauthenticated revenue preview endpoints reject with 401.

---

## 7. AI Runtime Status (Code)

Verified from repo at HEAD `453202a` (no runtime code changes in v6.0L):

| Check | Status |
|-------|--------|
| `SALES_BRAIN_REAL_PROVIDER_NETWORK_ENABLED` | **`false`** in `salesBrainRealProvider.ts` |
| `invokeRealProviderCall()` | throws **`SalesBrainRealProviderNetworkDisabledError`** |
| `salesBrainAdapter` / `salesBrainShadowMode` | modules exist under `src/services/ai/` |
| Wired to `useChat.ts` | **no** — chat uses `tryOrchestrateChatReply` from `chatSearchOrchestrator` |
| Wired to `chatSearchOrchestrator.ts` | **no** — no salesBrain imports |
| User-visible AI First response | **none** — no `NONGA_AI_FIRST_ENABLED` on staging |
| Shadow/mock user-visible | **not wired** — v6.0G shadow eval not connected to chat reply path |

---

## 8. Readiness Gaps Before Real Enablement

| Gap | Severity | Detail |
|-----|----------|--------|
| **v6.0K approval phrase** | **blocker** | Required before enable: `อนุมัติให้เปิด real paid AI provider บน staging closed pilot เท่านั้น ตาม v6.0K` |
| **OPENAI secret** | **blocker** (if openai) | `openai-api-key` / `OPENAI_API_KEY` SM resource **NOT_FOUND** |
| **Budget env** | **blocker** | `NONGA_AI_BUDGET_DAILY_LIMIT`, `NONGA_AI_BUDGET_MONTHLY_LIMIT` **absent** |
| **Kill switch env** | pre-enable | `NONGA_AI_EMERGENCY_KILL_SWITCH` **absent** — must be planned before enable |
| **AI flags absent** | **expected now** | All `NONGA_AI_*` enablement flags absent — correct for preflight; must be set only after approval |
| **Secret name mapping** | doc/ops gap | Logical `GEMINI_API_KEY` → SM `gemini-api-key` — must be documented before stage L enable |
| **Sales Brain not wired** | **expected** | Real provider adapter exists but not connected to chat — separate slice after approval |

### What passed

- Git clean and synced
- Staging healthy (`/api/health` 200)
- Public signup **false**
- Revenue preview guardrails **401** unauth
- Gemini secret metadata exists (no value accessed)
- AI First **not enabled** on staging
- Real provider **network disabled** in code

---

## 9. Read-only Commands Executed

Operator executed these commands during v6.0L preflight (recorded for audit):

```bash
# Git
git status
git branch --show-current
git rev-parse HEAD
git rev-parse origin/feature/chat-image-attachment-v1

# GCP project
gcloud config get-value project

# Cloud Run describe (redacted env names + non-secret values)
gcloud run services describe nonga-staging \
  --region=asia-southeast1 \
  --project=nonga-ce93c \
  --format="yaml(status.latestReadyRevisionName,status.url,spec.template.spec.containers[0].env)"

# Secret metadata only — NO versions access
gcloud secrets describe GEMINI_API_KEY --project=nonga-ce93c
gcloud secrets describe OPENAI_API_KEY --project=nonga-ce93c
gcloud secrets describe gemini-api-key --project=nonga-ce93c
gcloud secrets describe openai-api-key --project=nonga-ce93c
gcloud secrets list --project=nonga-ce93c --filter="name:api OR name:gemini OR name:openai OR name:AI"

# API smoke (GET only — not AI provider)
curl -s -w "\nHTTP:%{http_code}\n" "https://nonga-ce93c.web.app/api/health"
curl -s -o /dev/null -w "%{http_code}\n" "https://nonga-ce93c.web.app/api/admin/revenue/preview"
curl -s -o /dev/null -w "%{http_code}\n" "https://nonga-ce93c.web.app/api/my/revenue/preview"
```

### Not executed (forbidden)

- `gcloud run services update`
- `gcloud secrets versions access`
- `gcloud run deploy`
- `firebase deploy`
- Any paid AI API call (Gemini `generateContent`, OpenAI chat completions, etc.)

---

## 10. Forbidden Actions (v6.0L)

v6.0L slice (docs + static test) **must not**:

| Forbidden | v6.0L status |
|-----------|--------------|
| Deploy Cloud Run / Hosting / Firestore rules | ❌ not done |
| `gcloud run services update` | ❌ not done |
| Open or modify env/secrets | ❌ not done |
| Create new secrets | ❌ not done |
| Access secret values / `secrets versions access` | ❌ not done |
| Call paid AI API | ❌ not done |
| Fetch network to AI provider | ❌ not done |
| Change user-visible runtime behavior | ❌ not done |
| Touch production | ❌ not done |
| Modify payment / invoice / settlement write logic | ❌ not done |
| Modify buyer lead / seller reveal / outcome flow | ❌ not done |
| Enable public signup | ❌ not done — staging `publicSignupEnabled=false` |

CI static test (`test:v60l-real-ai-provider-read-only-preflight-execution`) uses **readFileSync only** — no fetch, no gcloud, no Firebase, no AI API.

---

## 11. Relationship to v6.0H Stage L

| Stage | Slice | This record |
|-------|-------|-------------|
| **H** | v6.0H readiness plan | prerequisite |
| **I** | v6.0I secret/env preflight plan | prerequisite |
| **J** | v6.0J real provider stub | prerequisite |
| **K** | v6.0K approval gate | approval phrase still required |
| **L preflight** | **v6.0L (this doc)** | ✅ read-only execution recorded |
| **L enable** | future — after approval | ❌ not started — DO NOT RUN enablement commands yet |

Next step after v6.0L: await explicit v6.0K approval phrase + close readiness gaps (budget, kill switch, openai secret if needed, secret mapping doc) before any staging AI First enablement.

---

## Appendix — Redaction Rules

- Record env **names** and non-secret values only
- Record secret **resource names** and existence — never values
- Ops log must not contain `AIza…`, `sk-…`, or `sk-proj-…` patterns
- Repo docs must not contain secret values
