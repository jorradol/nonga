# v22.57 — Comparison Vehicle Identity Regression Fix + Exact-Result Sales Copy

Date: 2026-07-10  
Branch: `feature/chat-image-attachment-v1`  
Execution type: `CONTROLLED STAGING COMPARISON VEHICLE IDENTITY REGRESSION ROOT-CAUSE FIX + EXACT-RESULT SALES COPY POLISH / EXISTING SEARCH-CONTEXT-GEMINI HYBRID ONLY / NO TEST LEAD / NO PILOT EXPANSION / NO PRODUCTION`

## Summary

Owner-browser after v22.56 found a **vehicle-identity integrity failure** on `เทียบกับ Corolla 2021 ให้หน่อย`: the answer compared Corolla 2020 with itself (duplicate headings/facts). v22.56 mileage/family paths remain HOLD pending Owner recheck; this slice fixes comparison identity and polishes exact-result sales wording on the existing hybrid path.

## Proven root cause

Evidence (local repro with session = Corolla 2020 only):

1. `เทียบกับ Corolla 2021` is classified as pilot direct-compare follow-up.
2. Bridge `preferPilotSessionFirst` skipped the inventory orchestrator and kept the single session card.
3. Pilot copy fallback used `{ a: 1, b: Math.min(2, sessionCount) }` → `{ a: 1, b: 1 }`.
4. `resolveCarCardsFromSessionContext([card], [1, 1])` returned the **same card twice**.
5. Formatter/`buildBuyerComparePilotCopy` rendered `คันที่ 1 … Corolla ปี 2020 … 399,000 … 88,000` twice.
6. Separately, Gemini grounding preferred stale one-card session context over orchestrated cards, and compare guidance said “เทียบคันที่ 1 กับ 2” even with one grounded listing.
7. Corolla 2021 was never resolved from inventory for this follow-up path.

This is **not** a wording-only / prompt-only defect.

## Fix (reuse existing mechanisms)

| Area | Change |
|------|--------|
| Named inventory compare | `inventoryBackedCompare.ts` — base = session active vehicle; target = existing `parseMarketplaceSearchQuery` + `searchMarketplaceForChat` |
| Identity invariants | Distinct listing ids; no target→base fallback; fail closed if missing/mismatch/duplicate facts |
| Orchestrator | Early named-compare path; session-only compare skips named targets |
| Bridge | Do not prefer session-first for named compare; Gemini grounding prefers orchestrated 2-card payload |
| Pilot copy / session resolve | Reject `a===b` and duplicate slot picks |
| Gemini | Stronger compare guidance + `hasCompareIdentityFailure` output guard |
| Exact-result copy | Salesperson tone for exact model/year single match (facts woven, one next step, less rigid dump) |

No new AI/Lead/Pilot architecture. No Lead create. Production untouched.

## Hard reload / new chat

Named compare requires **session-scoped** car cards. Empty context after new chat / hard reload → fail-closed clarification (does not invent a pair from stale global `lastSelectedCarId` alone).

## Tests

| Suite | Result |
|-------|--------|
| `test:v22.57` | PASS (56) |
| `test:v22.56` | PASS (43) |
| `test:v22.55` | PASS (38) |
| `test:v22.54` | PASS |

## Staging deploy

(Filled after deploy.)

## Final recommendation

**NEED REVIEW** until Owner-browser confirms corrected comparison + improved first-answer quality.

Owner-browser (fresh chat, signed-in Admin/Owner allowlist, **no Lead submit**):

1. `มีรถ โตโยต้า Corolla 2020 ไหม`
2. `คันนี้เหมาะกับใช้ครอบครัวไหม`
3. `ไมล์ 88,000 เยอะไปไหม`
4. `เทียบกับ Corolla 2021 ให้หน่อย` → must show **2020 vs 2021**, distinct facts, no self-compare
