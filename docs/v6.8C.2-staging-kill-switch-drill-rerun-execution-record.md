# v6.8C.2 — Staging Kill Switch Drill Rerun Execution Record

**สถานะ:** Execution record — **push verified; staging image deployed; kill switch drill PASS (all paths); rollback DONE**  
**วันที่:** 2026-06-15  
**Git baseline (deployed):** `7ec9992c9a7154c1b7607a7217b0ca11260904f9` (v6.8C.1)  
**Branch:** `feature/chat-image-attachment-v1`  
**อ้างอิง:** v6.8C manual smoke (`docs/v6.8C-manual-staging-real-gemini-shadow-smoke-execution-record.md`), v6.8B legacy Gemini safety, v6.8C.1 admin SS-01 kill switch patch

> **Kill switch drill on staging only.** No secrets, tokens, or API key values are recorded in this document. **No paid Gemini API calls during drill.**

---

## 1. Execution summary

| Item | Result |
|------|--------|
| Push `7ec9992` | **Already synced** — local HEAD = origin HEAD |
| Staging host | `https://nonga-ce93c.web.app` (Firebase Hosting → Cloud Run `nonga-staging`) |
| Cloud Run image deploy | **DONE** — `v6.8C.2-gemini-kill-switch-align-7ec9992` |
| Kill switch drill | **PASS** — SS-01, CP-02, legacy `/api/gemini/chat`, user-visible orchestrate |
| Paid Gemini during drill | **None** |
| Rollback flags | **DONE** — revision `nonga-staging-00083-96g` |
| User-visible real Gemini | **Not opened** |
| Production | **Not touched** |
| v6.8D readiness | **Ready** — separate approval required |

---

## 2. Push verification (v6.8C.1)

| Field | Value |
|-------|-------|
| Commit | **`7ec9992`** — `fix(ai): block admin SS-01 real provider on emergency kill switch` |
| Full SHA | `7ec9992c9a7154c1b7607a7217b0ca11260904f9` |
| Branch | `feature/chat-image-attachment-v1` |
| Push range | Already on origin at session start — no additional push required |
| Sync | local HEAD = `origin/feature/chat-image-attachment-v1` = `7ec9992` |
| Working tree | clean |

---

## 3. Pre-deploy checks

| Check | Result |
|-------|--------|
| Branch | `feature/chat-image-attachment-v1` |
| HEAD | `7ec9992` (= origin) |
| Working tree | clean |
| gcloud project | `nonga-ce93c` |
| Pre-deploy revision | `nonga-staging-00080-xdg` |
| Pre-deploy image | `v6.2E.4-fuel-economy-refine-fix` (predates v6.8B + v6.8C.1) |

### Tests before deploy (passed)

```bash
npm run lint
npm run test:v68c1-admin-shadow-kill-switch-patch
npm run test:v68c-staging-real-gemini-shadow-smoke-readiness
npm run test:v68b-gemini-safety-alignment
```

---

## 4. Staging Cloud Run deploy (image-only)

### Cloud Build

```bash
# Executed — do not re-run from CI without operator approval
gcloud builds submit --project=nonga-ce93c \
  --config=cloudbuild.v53f.yaml \
  --substitutions="_IMAGE_URI=asia-southeast1-docker.pkg.dev/nonga-ce93c/nonga-staging/nonga-staging:v6.8C.2-gemini-kill-switch-align-7ec9992,..."
```

| Field | Value |
|-------|-------|
| Cloud Build ID | **`433d9670-71e5-4f0b-b1f6-8b734757df00`** |
| Status | **SUCCESS** (~2m16s) |
| Image tag | **`v6.8C.2-gemini-kill-switch-align-7ec9992`** |
| Image URI | `asia-southeast1-docker.pkg.dev/nonga-ce93c/nonga-staging/nonga-staging:v6.8C.2-gemini-kill-switch-align-7ec9992` |
| Image digest | **`sha256:d421d9fbc05a7d0f9c029981d44997164bff9f48bfeef3416773214477b66921`** |

### Cloud Run deploy (image only — preserve env/secrets)

```bash
# Executed — no --set-env-vars / --update-env-vars on image deploy
gcloud run services update nonga-staging \
  --project=nonga-ce93c \
  --region=asia-southeast1 \
  --image=asia-southeast1-docker.pkg.dev/nonga-ce93c/nonga-staging/nonga-staging:v6.8C.2-gemini-kill-switch-align-7ec9992
```

| Field | Value |
|-------|-------|
| Service | `nonga-staging` |
| Region | `asia-southeast1` |
| Previous revision | `nonga-staging-00080-xdg` |
| Image deploy revision | **`nonga-staging-00081-p2d`** @ **100%** |
| Code included | v6.8B legacy Gemini kill switch (`9cb467d`) + v6.8C.1 SS-01 patch (`7ec9992`) |
| Hosting deploy | **SKIPPED** — Cloud Run backend only |
| Production `nonga-api` | **unchanged** — `nonga-api-00003-fg4` |

---

## 5. Staging revisions (operator timeline)

| Revision | Action |
|----------|--------|
| `nonga-staging-00080-xdg` | Pre-v6.8C.2 — rollback from v6.8C manual smoke; old image |
| `nonga-staging-00081-p2d` | **Image deploy** — `v6.8C.2-gemini-kill-switch-align-7ec9992` |
| `nonga-staging-00082-864` | **Kill switch drill env** — kill switch `true`, shadow real provider flags `true`, user-visible `false` |
| `nonga-staging-00083-96g` | **Rollback** — safe flags restored @ 100% |

---

## 6. Kill switch drill env (revision `00082-864`)

| Flag | Value |
|------|--------|
| `NONGA_AI_EMERGENCY_KILL_SWITCH` | **`true`** |
| `NONGA_AI_ADMIN_SHADOW_REAL_PROVIDER_ENABLED` | **`true`** (drill only — prove kill switch blocks despite flag) |
| `NONGA_AI_CHAT_SHADOW_REAL_PROVIDER_ENABLED` | **`true`** (drill only) |
| `NONGA_AI_USER_VISIBLE_ENABLED` | **`false`** |
| `GEMINI_API_KEY` | Mounted from Secret Manager `gemini-api-key:latest` (not invoked during drill) |

Auth: Firebase admin ID token (staging test admin). Guest token not used.

---

## 7. Kill switch drill results — **PASS**

Base: `https://nonga-ce93c.web.app`

### A. SS-01 admin shadow — **PASS**

**Route:** `POST /api/admin/sales-brain-shadow-smoke` · `{ "caseId": "SS-01" }`

| Field | Observed |
|-------|----------|
| HTTP | `200` |
| `providerNetwork` | **`false`** |
| `realProviderGateReason` | **`emergency_kill_switch`** |
| `userVisibleOff` | **`true`** |
| Real Gemini invoke | **None** |

### B. CP-02 chat shadow sink — **PASS**

**Route:** `POST /api/admin/chat-shadow-sink` · `{ "scenarioId": "CP-02" }`

| Field | Observed |
|-------|----------|
| HTTP | `200` |
| `providerNetwork` | **`false`** |
| `realProviderGateReason` | **`emergency_kill_switch`** |
| `userVisibleOff` | **`true`** |
| `sinkOnly` | **`true`** |
| Real Gemini invoke | **None** |

### C. Legacy `/api/gemini/chat` — **PASS**

**Route:** `POST /api/gemini/chat` · synthetic message (v6.8B `legacyGeminiSafety` on new image)

| Field | Observed |
|-------|----------|
| HTTP | `200` |
| `success` | `true` |
| `isMock` | **`true`** |
| Real Gemini invoke | **None** |

### D. User-visible orchestrate — **PASS**

**Route:** `POST /api/ai/chat-user-visible-orchestrate` · `{ "userMessage": "งบ 4 แสน มีรถอะไรน่าเล่น" }`

| Field | Observed |
|-------|----------|
| HTTP | `200` |
| `pilotPathActive` | **`false`** |
| `fallbackToLegacy` | **`true`** |
| `skipGemini` | **`true`** |
| Real Gemini user-visible output | **None** |

---

## 8. Operator note — drill script rerun

Initial inline `npx tsx -e` drill invocation on PowerShell **hung** (no output; process terminated). Drill was **rerun** with a separate one-off staging script using correct request shapes (`userMessage` for orchestrate route). **Second run: all checks PASS.** Temporary drill script was removed after execution; results recorded here only.

---

## 9. Cost / usage observation

| Item | Observation |
|------|-------------|
| Paid Gemini during drill | **0 invocations** |
| Contrast with v6.8C PARTIAL drill | v6.8C had unintended SS-01 paid call (pre-v6.8C.1 gap) |
| Budget env caps | daily `5` / monthly `50` — unchanged |
| Consumption tracker | not present in runtime |

---

## 10. Rollback — DONE

Final revision **`nonga-staging-00083-96g`** @ **100%**:

```bash
# Executed — safe state restore
gcloud run services update nonga-staging \
  --project=nonga-ce93c --region=asia-southeast1 \
  --update-env-vars="NONGA_AI_EMERGENCY_KILL_SWITCH=true,NONGA_AI_ADMIN_SHADOW_REAL_PROVIDER_ENABLED=false,NONGA_AI_CHAT_SHADOW_REAL_PROVIDER_ENABLED=false,NONGA_AI_USER_VISIBLE_ENABLED=false"
```

| Flag | Final value |
|------|-------------|
| `NONGA_AI_EMERGENCY_KILL_SWITCH` | **`true`** |
| `NONGA_AI_ADMIN_SHADOW_REAL_PROVIDER_ENABLED` | **`false`** |
| `NONGA_AI_CHAT_SHADOW_REAL_PROVIDER_ENABLED` | **`false`** |
| `NONGA_AI_USER_VISIBLE_ENABLED` | **`false`** |

Post-rollback smoke: `GET /api/health` → **200**

---

## 11. v6.8D readiness

| Gate | Status |
|------|--------|
| v6.8C.1 on origin | ✅ |
| Staging image includes v6.8B + v6.8C.1 | ✅ |
| Kill switch drill all paths PASS | ✅ |
| User-visible real Gemini | ❌ still off |
| Production deploy | ❌ not done |

**Next slice (separate approval):** v6.8D — Real Gemini User-visible Allowlist Pilot (approved Firebase UIDs only; staging only).

---

## 12. Compliance

| Rule | Status |
|------|--------|
| Production deploy | ❌ not done |
| User-visible real Gemini | ❌ not enabled |
| Secrets in repo / this doc | ❌ none |
| Firestore writes | ❌ none |
| Lead system changes | ❌ none |
| Runtime code changes in this slice | ❌ docs/tests/package only |

---

## 13. Tests referenced (offline)

```bash
npm run lint
npm run test:v68c1-admin-shadow-kill-switch-patch
npm run test:v68c-staging-real-gemini-shadow-smoke-readiness
npm run test:v68b-gemini-safety-alignment
npm run test:v68c2-staging-kill-switch-drill-execution-record
```

Manual staging drill: **operator execution only** — not automated from CI.
