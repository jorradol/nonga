# v10.5 Admin-Only Real Gemini Manual Smoke Approval Request

> APPROVAL REQUEST PACKET ONLY / DOCS-ONLY / STATIC VALIDATION ONLY / DO NOT RUN
> v10.5 is an approval request packet only. This version does not execute real Gemini, does not change runtime, does not open a new admin route, does not deploy, does not enable production, does not open a public route, does not enable buyer-facing AI, and does not send a real lead.

---

## 1. Purpose

v10.5 is an approval request packet only for a future owner decision.

- This is approval request only.
- This is not real Gemini execution.
- This is not deploy.
- This is not production.
- This is not runtime/provider/env behavior change.
- This is not public route enablement.
- This is not buyer-facing AI.
- This is not real lead sending.
- This packet exists so the owner can issue a clear APPROVE / HOLD / REJECT decision before any future real run.

## 2. Current State Continuity

Progression chain:

- v10.0 = admin-only manual smoke path prep
- v10.1 = operator runbook
- v10.2 = manual smoke preflight gate/checklist
- v10.3 = manual smoke execution packet
- v10.4 = gated execution planning packet
- v10.5 = approval request packet before any real run

## 3. Approval Decision Options

### Option A - APPROVE LIMITED ADMIN-ONLY RUN

Owner may approve only a narrow boundary:

- one admin-only real Gemini manual smoke run
- synthetic data only
- no buyer-facing AI
- no public route
- no production
- no deploy
- no real lead sending
- no real customer data / PII
- no secret printed or committed
- abort if any guardrail fails

### Option B - HOLD / NOT YET APPROVED

- real Gemini run is not approved yet
- continue docs/static planning only
- do not execute Gemini

### Option C - REJECT / DO NOT RUN

- no real Gemini manual smoke run is approved
- deterministic fallback remains the source of truth
- keep deterministic/mock/fallback behavior for all approved paths

## 4. Owner Approval Wording (Copy/Paste)

### English Approval Text

I explicitly approve ONE admin-only real Gemini manual smoke run for Nong A / NongBot.
This approval is limited to:
- admin-only manual smoke
- synthetic data only
- no production
- no deploy
- no public route
- no buyer-facing AI
- no real lead sending
- no real customer data or PII
- no secret/API key printed, stored, or committed
- abort immediately if any guardrail fails
This approval does not authorize any production release, buyer-facing AI, public route, deployment, or real lead sending.

### Thai Approval Text

ลุงเด่นอนุมัติให้รัน real Gemini manual smoke ได้ 1 ครั้ง เฉพาะ admin-only เท่านั้น
การอนุมัตินี้จำกัดเฉพาะ:
- manual smoke เฉพาะ admin
- ใช้ข้อมูลทดสอบจำลองเท่านั้น
- ห้าม production
- ห้าม deploy
- ห้าม public route
- ห้าม buyer-facing AI
- ห้ามส่ง real lead
- ห้ามใช้ข้อมูลลูกค้าจริงหรือ PII จริง
- ห้ามพิมพ์/บันทึก/commit secret หรือ API key
- ต้องหยุดทันทีถ้า guardrail ใดผิดปกติ
การอนุมัตินี้ไม่อนุญาต production release, buyer-facing AI, public route, deployment หรือ real lead sending

## 5. Non-Approval Wording (Copy/Paste)

### HOLD (English)

HOLD: I do not approve running real Gemini yet. Continue only docs/static planning. Do not execute Gemini.

### REJECT (English)

REJECT: I do not approve any real Gemini run. Keep deterministic/mock/fallback as the source of truth.

### HOLD (Thai)

HOLD: ลุงเด่นยังไม่อนุมัติให้รัน real Gemini ตอนนี้ ให้ทำต่อได้เฉพาะเอกสารและ static planning เท่านั้น และห้าม execute Gemini จริง

### REJECT (Thai)

REJECT: ลุงเด่นไม่อนุมัติการรัน real Gemini ทุกกรณี ให้ใช้ deterministic/mock/fallback เป็นแหล่งความจริงต่อไป

## 6. Required Pre-Run Checks If Approved Later

Even if Option A is approved later, all checks must still pass before any future run:

- branch correct
- HEAD correct
- local = origin
- working tree clean
- env secret exists only outside repo
- kill switch verified
- deterministic fallback verified
- log redaction verified
- synthetic data reviewed
- no buyer/public route involvement
- no lead sending involvement
- quota/cost cap acknowledged
- abort criteria reviewed

## 7. Fail-Closed Rule

This packet is fail-closed by default:

- if no explicit owner approval = do not run
- if approval wording is incomplete = do not run
- if branch/HEAD mismatch = do not run
- if working tree is not clean = do not run
- if env/secret source is unclear = do not run
- if PII/secret/log risk is detected = do not run
- if buyer/public/lead/deploy path involvement is detected = do not run

## 8. Cost / Quota Boundary

Future Option A execution, if ever approved, must remain tightly bounded:

- one run only
- very low prompt count
- no loop
- no batch
- no normal test hitting Gemini
- abort if quota/cost is unexpected
- record quota/cost observation in sanitized form only

## 9. Evidence To Capture After Future Run (Sanitized Only)

If a future owner-approved run happens, capture sanitized evidence only:

- approval text/reference
- date/time
- operator
- branch
- HEAD
- command used
- synthetic data confirmation
- env confirmation without value
- result PASS / FAIL / ABORTED
- cost/quota observation
- redaction check
- fallback check
- no buyer/public/lead/deploy confirmation
- sanitized logs only

## 10. v10.5 Boundary Reminder

v10.5 contains no runnable Gemini instruction.

- no runtime changes
- no provider wiring changes
- no env behavior changes
- no route creation
- no deploy action
- no production action
- no buyer-facing AI action
- no real lead action

## 11. Next Step After v10.5

Next step is still not deploy, not production, not public route, not buyer-facing AI, and not real lead sending.

Candidate next packet:

- `v10.6 Admin-Only Real Gemini Manual Smoke Pre-Run Gate`

That next step is allowed only if the owner explicitly chooses Option A in a future decision.
