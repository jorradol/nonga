# v22.26 - Multi-Car Professional Sales Explanation Fix

Date: 2026-07-09  
Branch: `feature/chat-image-attachment-v1`  
Execution type: `CONTROLLED STAGING MULTI-CAR SALES EXPLANATION FIX / PROFESSIONAL THAI CAR SALES RESPONSE FOLLOW-UP / NO PRODUCTION / NO PUBLIC / NO REAL LEAD / NO DEALER-FACING SEND`

## 1) Purpose

Fix HOLD from owner-browser: budget multi-car answers showed cards but assistant text was only a generic intro/CTA. Restore per-car sales explanations in the text bubble.

## 2) Root cause

`buyerScoredMarketplaceSearch.ts` set `omitPerCarPitch: true` (v7.4 card `fitReason` fusion), so scored budget replies omitted per-car text from the assistant message.

## 3) Fix

| Module | Change |
|--------|--------|
| `buyerCarPitchCopy.ts` | Always emit per-car sales blocks (facts, why, who, safe model context) + compare summary; concise safety note last |
| `buyerScoredMarketplaceSearch.ts` | Stop forcing `omitPerCarPitch: true` |
| Tests v74 / v548c | Expect per-car text again |
| `test:v22.26` | Budget multi-car explanation coverage |

## 4) Deploy status

(filled after staging deploy)

## 5) Final recommendation

`NEED REVIEW — multi-car professional sales explanation fixed on staging; owner-browser review required.`
