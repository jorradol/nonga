# v12.3 New Authorized One-Run Plan — Prepare Only

## 1. Status

PREPARED — no Gemini execution, no smoke endpoint call

## 2. Baseline

Baseline commit: 75554b80c906eeda037a0f4ad9ae510378bef6ba  
v12.2 status: CLOSED / PUSHED / ACCEPTED  
v12.2 recommendation: READY TO PREPARE NEW AUTHORIZED ONE-RUN PLAN

## 3. Purpose

v12.3 is prepare-only planning for a new authorized one-run attempt.

- v12.0 found runtime-observed stages were not captured
- v12.1 added runtime diagnostics capture contract
- v12.2 static safety review passed with no new safety issue
- this round is still not execution and does not consume authorization

## 4. Fresh Authorization Requirement

Before any actual one-run execution, owner must send a fresh authorization message exactly in this form:

```text
FINAL EXECUTION AUTHORIZE:
ลุงเด่นในฐานะ owner ของ Nong A / NongBot อนุมัติให้รัน real Gemini manual smoke ได้ 1 ครั้ง เฉพาะ admin-only เท่านั้น
การอนุมัตินี้จำกัดเฉพาะ:
- manual smoke เฉพาะ admin 1 ครั้งเท่านั้น
- ใช้ข้อมูลทดสอบจำลองเท่านั้น
- ห้าม production
- ห้าม deploy
- ห้าม public route
- ห้าม buyer-facing AI
- ห้ามส่ง real lead
- ห้ามใช้ข้อมูลลูกค้าจริงหรือ PII จริง
- ห้ามพิมพ์/บันทึก/commit secret หรือ API key
- ต้องหยุดทันทีถ้า guardrail ใดผิดปกติ
- การอนุมัติหมดผลหลังรัน 1 ครั้ง หรือ abort 1 ครั้ง
การอนุมัตินี้ไม่ได้อนุญาต production release, deployment, public route, buyer-facing AI หรือ real lead sending
```

If fresh authorization is missing:

ABORTED — missing fresh FINAL EXECUTION AUTHORIZE

and smoke endpoint execution must not be attempted.

## 5. Pre-Execution Gate Checklist

### Repo gate

- branch = `feature/chat-image-attachment-v1`
- local HEAD = latest accepted commit at that time
- origin HEAD matches local
- local = origin yes
- working tree clean

### Health gate

- local server health endpoint returns `200`
- health probe uses explicit timeout
- if health probe stalls, stop and report; do not run smoke

### Runtime env gate

Must PASS:

- `NONGA_AI_LEGACY_PUBLIC_GEMINI_ENABLED=false`
- `NONGA_AI_ADMIN_SHADOW_REAL_PROVIDER_ENABLED=true`
- `NONGA_AI_ADMIN_SHADOW_MANUAL_SMOKE_ENABLED=true`
- `NONGA_AI_ADMIN_SHADOW_MANUAL_SMOKE_CASE_ID=SS-01`
- `NONGA_AI_EMERGENCY_KILL_SWITCH=false`
- `GEMINI_API_KEY` presence only (masked)
- `ADMIN_TOKEN` presence only (masked)
- `NONGA_ADMIN_API_TOKEN` presence only (masked)
- `NONGA_AI_ADMIN_SHADOW_PROVIDER_TIMEOUT_MS` configured/default-clamped PASS
- manual caller timeout/abort boundary present PASS
- diagnostics snapshot contract present PASS

### Admin auth non-Gemini gate

Non-Gemini endpoint check:

`GET /api/admin/draft-inventory`

- no-header status = `401`
- valid masked token/header status = `200`
- never print raw token values

If any gate fails, stop immediately and do not run smoke.

## 6. Authorized One-Run Command Plan

Only when all gates PASS and fresh authorization exists:

- Endpoint: `POST /api/admin/sales-brain-shadow-smoke`
- Payload:

```json
{ "caseId": "SS-01" }
```

- Header: use admin token/header combination previously validated by non-Gemini auth gate; report masked only

Hard limits:

- one run only
- no retry
- no second request
- no `/api/gemini/*`
- no deploy
- no public/buyer/lead actions

## 7. Diagnostics Capture Plan

Actual run must capture a sanitized diagnostic snapshot and explicitly separate:

- runtimeObservedStages
- codePathAvailableStages
- missingOrUnknownStages

Required fields to report when captured:

- `callerStatus`
- `handlerStatus`
- `providerStatus`
- `requestDispatched`
- `responseCaptured`
- `httpStatus`
- `timeout`
- `fallback`
- `runMode: admin_shadow_manual_smoke`
- `diagnosticSnapshotVersion`
- `sanitized: true`

Rules:

- runtimeObservedStages = stages observed in runtime only
- codePathAvailableStages = stage vocabulary available in code
- missingOrUnknownStages = available stages not observed in runtime evidence
- never mark a stage observed without runtime proof

## 8. Diagnostic Stages to Look For

- `admin_shadow_manual_caller_start`
- `admin_shadow_manual_caller_timeout`
- `admin_shadow_manual_caller_completed`
- `admin_shadow_manual_caller_aborted`
- `admin_shadow_request_handler_start`
- `admin_shadow_request_handler_return`
- `admin_shadow_gate_checked`
- `admin_shadow_provider_call_start`
- `admin_shadow_provider_call_success`
- `admin_shadow_provider_call_timeout`
- `admin_shadow_provider_call_error`
- `admin_shadow_fallback_returned`

## 9. Result Interpretation Plan

### PASS

Use only when evidence shows:

- endpoint completed
- sanitized result captured
- no retry
- no secret/PII/raw provider output
- diagnostics captured

Recommendation:

READY FOR OWNER-ONLY STAGING TRIAL PLAN

### TIMEOUT

Use when caller/provider/handler timeout is captured.

Recommendation:

HOLD — timeout observed; review v12.1 diagnostic snapshot

### FAIL

Use when guard/provider/server error is captured.

Recommendation:

HOLD — review provider/guard/server error

### ABORTED

Use when process was intentionally stopped, or missing authorization/gate fail path requires abort.

Recommendation:

HOLD — authorization consumed if one-run was attempted; review abort reason

If authorization is missing and no attempt occurred:

HOLD — missing authorization; no run occurred; authorization not consumed

### NEED REVIEW

Use when evidence is incomplete or ambiguous.

Recommendation:

HOLD — incomplete or ambiguous diagnostics require review

## 10. Sanitization Rules

Never report:

- raw provider output
- raw prompt containing sensitive data
- token
- secret
- API key
- Authorization header raw value
- PII
- customer data
- VIN
- plate
- phone จริง

Allowed reporting:

- masked/presence-only values
- sanitized statuses
- sanitized diagnostic stages
- sanitized booleans/enums
- HTTP status if available

## 11. Guardrail Confirmation

- no retry during v12.3
- no Gemini execution during v12.3
- no smoke endpoint call during v12.3
- no `/api/gemini/*`
- no deploy
- no production
- no public route activation
- no buyer-facing AI
- no real lead
- no PII/customer data
- no secret/token/API key exposure
- no raw provider output
- no automatic retry
- no runtime behavior change
- no authorization consumed during v12.3

## 12. Actual Run Report Template

# v12.4 Fresh Authorized One-Run Admin SS-01 Smoke Report

1. Execution decision
   - PASS / TIMEOUT / FAIL / ABORTED / NEED REVIEW
2. Authorization confirmation
   - fresh FINAL EXECUTION AUTHORIZE yes/no
   - authorization consumed yes/no
   - run count 0/1 or 1/1
3. Pre-execution gate
   - repo gate PASS/FAIL
   - health gate PASS/FAIL
   - env gate PASS/FAIL
   - admin auth non-Gemini PASS/FAIL
   - diagnostics contract PASS/FAIL
4. Command attempted sanitized
   - endpoint
   - payload caseId SS-01
   - header masked
   - no `/api/gemini/*` yes/no
5. Result summary
   - HTTP status if captured
   - success true/false if captured
   - timeout yes/no/unknown
   - fallback yes/no/unknown
   - realProviderGateReason if captured
   - fallback reason if captured
   - providerNetwork true/false/unknown if captured
6. Diagnostic snapshot
   - runtimeObservedStages
   - codePathAvailableStages
   - missingOrUnknownStages
   - callerStatus
   - handlerStatus
   - providerStatus
   - requestDispatched
   - responseCaptured
   - diagnosticSnapshotVersion
   - sanitized true/false
7. Provider call status
   - confirmed real provider call yes/no/unknown
   - confirmed real provider response yes/no/unknown
   - confirmed provider output captured yes/no
   - do not guess
8. Sanitized response assessment
   - no raw provider output
   - no secret
   - no PII
   - no customer data
   - no VIN/plate/phone
9. Cost/quota observation
   - observed sanitized / unknown
10. Policy confirmation
   - one run only
   - no retry
   - no deploy
   - no production
   - no public route activation
   - no buyer-facing AI
   - no real lead
   - no `/api/gemini/*`
   - no runtime behavior change after run
11. Files changed
   - should be none
12. Git status after execution
   - local HEAD
   - origin HEAD
   - local = origin
   - working tree clean
13. Final recommendation
   - if PASS: `READY FOR OWNER-ONLY STAGING TRIAL PLAN`
   - if TIMEOUT: `HOLD — timeout observed; review v12.1 diagnostic snapshot`
   - if FAIL: `HOLD — review provider/guard/server error`
   - if ABORTED: `HOLD — authorization consumed if one-run was attempted; review abort reason`
   - if NEED REVIEW: `HOLD — incomplete or ambiguous diagnostics require review`

## 13. Recommendation

READY FOR OWNER DECISION ON FRESH FINAL EXECUTION AUTHORIZE FOR v12.4 ONE-RUN
