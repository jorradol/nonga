# v6.0T — Shadow Runtime Reader Staging Deploy Record

**สถานะ:** Execution record (docs/tests only) — **Cloud Run staging image-only deploy บันทึกแล้ว, ไม่ deploy ซ้ำ, ไม่ gcloud env update, ไม่เปิด NONGA_AI_* flags, ไม่เรียก paid AI API, ไม่เปลี่ยน user-visible runtime behavior**  
**วันที่:** 2026-06-07  
**Git baseline ขณะ deploy:** `db29970ece7c2f15d15b35d8425d2d58cb7bc46b` (v6.0S)  
**Branch:** `feature/chat-image-attachment-v1`  
**อ้างอิง:** v6.0S (deploy readiness plan), v6.0R (runtime flag reader), v6.0Q (live preflight)

> **v6.0T บันทึกผล deploy จริง** — นำโค้ด v6.0R shadow runtime flag reader ขึ้น staging revision `nonga-staging-00048-nwc` โดย **AI flags ยัง absent/off** และ **user-visible AI ยังปิด**

---

## สารบัญ

1. [Vision & Execution Summary](#1-vision--execution-summary)
2. [Preflight (Before Deploy)](#2-preflight-before-deploy)
3. [Tests / Build Before Deploy](#3-tests--build-before-deploy)
4. [Cloud Build (Executed)](#4-cloud-build-executed)
5. [Cloud Run Deploy (Image-only)](#5-cloud-run-deploy-image-only)
6. [Post-deploy Smoke](#6-post-deploy-smoke)
7. [AI Flags After Deploy](#7-ai-flags-after-deploy)
8. [Unchanged Env / Secret Refs](#8-unchanged-env--secret-refs)
9. [Rollback Plan (Prepared — Not Run)](#9-rollback-plan-prepared--not-run)
10. [Forbidden Changes (v6.0T)](#10-forbidden-changes-v60t)
11. [Next Steps](#11-next-steps)

---

## 1. Vision & Execution Summary

### เป้าหมาย v6.0T

Deploy Cloud Run staging **image-only** จาก branch ที่มี v6.0R runtime shadow flag reader โดย:

- **Preserve** env/secrets เดิม — ไม่ `--set-env-vars` / `--update-env-vars`
- **ไม่เปิด** `NONGA_AI_*` flags
- **ไม่** Hosting / Firestore rules deploy
- Post-deploy read-only smoke

### Execution outcome

| Area | Result |
|------|--------|
| Preflight | ✅ git clean, HEAD = origin @ `db29970` |
| Before deploy | rev `nonga-staging-00047-7xc`, AI flags **absent/off** |
| Cloud Build | ✅ **SUCCESS** — `0a847fe3-53b3-4313-a12e-40e0e923d2e9` |
| Cloud Run deploy | ✅ rev **`nonga-staging-00048-nwc`**, traffic **100%** |
| AI flags after deploy | ✅ all 8 **absent/off** |
| Post-deploy smoke | ✅ health/cars 200, revenue 401, signup false |
| User-visible AI | ❌ unchanged — legacy only |
| Real stock import | ❌ still blocked |

---

## 2. Preflight (Before Deploy)

Recorded at deploy execution time:

| Check | Value |
|-------|-------|
| **git status** | clean (nothing to commit, working tree clean) |
| **branch** | `feature/chat-image-attachment-v1` |
| **HEAD** | `db29970ece7c2f15d15b35d8425d2d58cb7bc46b` |
| **origin/feature/chat-image-attachment-v1** | `db29970ece7c2f15d15b35d8425d2d58cb7bc46b` |
| **HEAD = origin** | ✅ yes |
| **gcloud active project** | `nonga-ce93c` |
| **Cloud Run service** | `nonga-staging` |
| **Region** | `asia-southeast1` |
| **Previous revision** | `nonga-staging-00047-7xc` |
| **Previous image** | `v5.6I.22-current-branch-runtime-cebc7a6` |
| **Public signup** | **`false`** (`VITE_NONGA_PUBLIC_SIGNUP_ENABLED=false`) |

### AI flags before deploy (all absent/off)

| Flag | Status |
|------|--------|
| `NONGA_AI_PROVIDER` | **absent/off** |
| `NONGA_AI_MODE` | **absent/off** |
| `NONGA_AI_FIRST_ENABLED` | **absent/off** |
| `NONGA_AI_SHADOW_MODE_ENABLED` | **absent/off** |
| `NONGA_AI_USER_VISIBLE_ENABLED` | **absent/off** |
| `NONGA_AI_EMERGENCY_KILL_SWITCH` | **absent/off** |
| `NONGA_AI_BUDGET_DAILY_LIMIT` | **absent/off** |
| `NONGA_AI_BUDGET_MONTHLY_LIMIT` | **absent/off** |

---

## 3. Tests / Build Before Deploy

All passed before Cloud Build + deploy:

| Command | Result |
|---------|--------|
| `npm run test:v60s-shadow-runtime-wiring-smoke-readiness-not-deployed` | **PASS** |
| `npm run test:v60r-runtime-shadow-flag-reader-user-visible-off` | **PASS** |
| `npm run test:v60q-gemini-shadow-live-preflight-read-only` | **PASS** |
| `npm run lint` | **PASS** |
| `npm run build:staging:hosting` | **PASS** (local vite only — not deployed to Hosting) |

---

## 4. Cloud Build (Executed)

```bash
# Executed — do not re-run from CI without ops approval
gcloud builds submit --project=nonga-ce93c \
  --config=cloudbuild.v53f.yaml \
  --substitutions=_IMAGE_URI=asia-southeast1-docker.pkg.dev/nonga-ce93c/nonga-staging/nonga-staging:v6.0T-shadow-runtime-reader-db29970,...
```

| Field | Value |
|-------|-------|
| **Cloud Build ID** | `0a847fe3-53b3-4313-a12e-40e0e923d2e9` |
| **Status** | **SUCCESS** (~2m8s) |
| **Image tag** | `v6.0T-shadow-runtime-reader-db29970` |
| **Image URI** | `asia-southeast1-docker.pkg.dev/nonga-ce93c/nonga-staging/nonga-staging:v6.0T-shadow-runtime-reader-db29970` |
| **Image digest** | `sha256:5fd0107aab9b00ae870ac1627781f759950592870d71e4425b6545867f4e46e3` |

**Did not run** `gcloud secrets versions access` — no secret values in this record.

---

## 5. Cloud Run Deploy (Image-only)

```bash
# Executed — image-only, no --set-env-vars / --update-env-vars
gcloud run deploy nonga-staging \
  --image asia-southeast1-docker.pkg.dev/nonga-ce93c/nonga-staging/nonga-staging:v6.0T-shadow-runtime-reader-db29970 \
  --project=nonga-ce93c \
  --region=asia-southeast1 \
  --allow-unauthenticated \
  --service-account=nonga-staging-runner@nonga-ce93c.iam.gserviceaccount.com
```

| Field | Value |
|-------|-------|
| **Deploy type** | **image-only** |
| **Previous revision** | `nonga-staging-00047-7xc` |
| **New revision** | **`nonga-staging-00048-nwc`** |
| **Traffic** | **100%** → new revision |
| **Env/secrets** | **preserved** — no `--set-env-vars`, no `--update-env-vars` |
| **Service account** | `nonga-staging-runner@nonga-ce93c.iam.gserviceaccount.com` |
| **Service URL** | `https://nonga-staging-265743710806.asia-southeast1.run.app` |
| **Hosting smoke base** | `https://nonga-ce93c.web.app` |

---

## 6. Post-deploy Smoke

Read-only GET smoke via hosting URL (not AI provider):

| Endpoint | HTTP | Key fields |
|----------|------|------------|
| `GET /api/health` | **200** | `ok:true`, `dataBackend:firestore`, `publicSignupEnabled:false` |
| `GET /api/cars?limit=1` | **200** | `success:true`, `count:17` |
| `GET /api/admin/revenue/preview` (unauth) | **401** | auth required |
| `GET /api/my/revenue/preview` (unauth) | **401** | auth required |

**No paid AI API call** during deploy or smoke. **User-visible AI response** unchanged — legacy orchestrator/template only.

---

## 7. AI Flags After Deploy

On revision `nonga-staging-00048-nwc` — **all still absent/off** (expected):

| Flag | Status after deploy |
|------|---------------------|
| `NONGA_AI_PROVIDER` | **absent/off** |
| `NONGA_AI_MODE` | **absent/off** |
| `NONGA_AI_FIRST_ENABLED` | **absent/off** |
| `NONGA_AI_SHADOW_MODE_ENABLED` | **absent/off** |
| `NONGA_AI_USER_VISIBLE_ENABLED` | **absent/off** |
| `NONGA_AI_EMERGENCY_KILL_SWITCH` | **absent/off** |
| `NONGA_AI_BUDGET_DAILY_LIMIT` | **absent/off** |
| `NONGA_AI_BUDGET_MONTHLY_LIMIT` | **absent/off** |

Code for v6.0R flag reader is **on staging** but flags remain off — shadow not enabled until future env enable slice.

---

## 8. Unchanged Env / Secret Refs

Secret refs (names only — **no values**):

| Env var | Ref |
|---------|-----|
| `GEMINI_API_KEY` | `gemini-api-key:latest` |
| `FIREBASE_SERVICE_ACCOUNT_JSON` | `firebase-service-account-json:latest` |

Settlement env **unchanged** from pre-deploy:

| Env | Value |
|-----|-------|
| `NONGA_SETTLEMENT_DATA_BACKEND` | `firestore` |
| `NONGA_SETTLEMENT_FIRESTORE_WRITES_ENABLED` | `true` |
| `NONGA_SUCCESS_FEE_RECORD_ENABLED` | `true` |

Other non-secret env preserved: `NONGA_DATA_BACKEND=firestore`, `NONGA_IMAGE_BACKEND=firebase-storage`, `VITE_NONGA_PUBLIC_SIGNUP_ENABLED=false`.

---

## 9. Rollback Plan (Prepared — Not Run)

If health/smoke fails — **prepared but not executed** in v6.0T:

```bash
# Prepared — NOT RUN in v6.0T (smoke passed)
gcloud run services update-traffic nonga-staging \
  --project=nonga-ce93c \
  --region=asia-southeast1 \
  --to-revisions=nonga-staging-00047-7xc=100
```

**Do not** modify env/secrets to fix issues — rollback revision first.

---

## 10. Forbidden Changes (v6.0T)

| Forbidden | v6.0T status |
|-----------|--------------|
| `NONGA_AI_*` env enablement | ❌ not done |
| `gcloud run services update` for env | ❌ not done |
| Secrets create/update / `secrets versions access` | ❌ not done |
| Paid AI API / fetch to AI provider | ❌ not done |
| User-visible AI / AI response shown to user | ❌ not done |
| Production | ❌ not touched |
| Firestore rules deploy | ❌ not done |
| Hosting deploy | ❌ not done |
| Payment / invoice / settlement write logic changes | ❌ not done |
| Buyer lead / seller reveal / outcome changes | ❌ not done |
| Public signup enable | ❌ not done — staging `false` |
| Real stock import | ❌ not done — still blocked |

CI static test uses **readFileSync only** — no fetch, no gcloud, no Firebase, no AI API.

---

## 11. Next Steps

1. Await **v6.0P approval phrase** from ลุง
2. Future slice: **env enable** (`NONGA_AI_SHADOW_*` + budget 5/50) — separate from v6.0T
3. Post-enable **shadow runtime smoke** + **AI conversation smoke**
4. Notify ลุง → plan real stock import (still blocked until smoke passes)
