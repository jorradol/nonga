# v12.15 Admin Token Source Inventory / Access Handoff — No Secret Exposure

Status: SOURCE INVENTORY ONLY — no live auth recheck, no Gemini execution, no smoke endpoint call

## Baseline

- Repo: `https://github.com/jorradol/nonga.git`
- Branch: `feature/chat-image-attachment-v1`
- Latest accepted pushed commit before v12.15: `22da08b244c86dcc12df151ba8263c76e8911a9b` (`22da08b`)
- Baseline verification commands:
  - `git status`
  - `git rev-parse HEAD`
  - `git fetch origin`
  - `git rev-parse origin/feature/chat-image-attachment-v1`
- Baseline result:
  - branch: `feature/chat-image-attachment-v1`
  - local HEAD: `22da08b244c86dcc12df151ba8263c76e8911a9b`
  - origin HEAD: `22da08b244c86dcc12df151ba8263c76e8911a9b`
  - local = origin: yes
  - working tree clean: yes

## Source Event

- `NONGA_ADMIN_API_TOKEN in current session env: missing`
- `operator/admin secret value is not available in session`
- execution type in this slice: source-inventory-only, static checks, no token usage

## Current Blocker

- `NONGA_ADMIN_API_TOKEN` is still missing from current operator session.
- admin token value is not retrievable from current session context.
- live auth recheck is blocked by policy and token absence.

## Token Source Inventory

Observed token-source names only (no values):

- server runtime primary env: `NONGA_ADMIN_API_TOKEN`
- optional explicit fallback env: `VITE_NONGA_ADMIN_API_TOKEN`
- generic alias found in docs/scripts: `ADMIN_API_TOKEN` context references only

Metadata-only runtime checks (no secret read, no value output):

- Cloud Run service env-name inventory (`nonga-staging`, `asia-southeast1`) does not include `NONGA_ADMIN_API_TOKEN`.
- Secret Manager name inventory contains `firebase-service-account-json` and `gemini-api-key`.
- no explicit admin-token secret name found by metadata-only list.

## Repo / Docs References

Key references inspected for source mapping (name-only context):

- `src/server/apiAuth.ts` - admin guard resolves primary token from `process.env.NONGA_ADMIN_API_TOKEN`.
- `src/utils/apiAuthHeaders.ts` - server-mode resolver uses `NONGA_ADMIN_API_TOKEN` primary and `VITE_NONGA_ADMIN_API_TOKEN` fallback only when explicitly allowed.
- `scripts/check-admin-token-session-env.mts` - safe session presence/format checker; prints masked marker only.
- `scripts/build-staging-hosting.mts` - enforces `VITE_NONGA_ADMIN_API_TOKEN` blank in staging Firebase Auth build path.
- `docs/v5-staging-environment-setup.md` - documents optional limited beta-token env placeholders for `NONGA_ADMIN_API_TOKEN` and `VITE_NONGA_ADMIN_API_TOKEN`.
- `docs/v12.13-admin-token-session-env-setup-check-no-gemini-execution.md` and `docs/v12.14-admin-token-session-presence-confirmation-no-live-auth-recheck.md` - prior operator/session setup checkpoints and HOLD state.

## Runtime / Secret Source Candidates

- runtime env variable: `NONGA_ADMIN_API_TOKEN` (primary)
- possible client env (explicit fallback only): `VITE_NONGA_ADMIN_API_TOKEN`
- possible secret manager name for admin token: unknown (not found by metadata-only list in current project)
- Firebase config candidate: unknown for admin token (no direct admin token key found in Firebase config guard paths)
- Cloud Run service env candidate: not present by metadata-only env-name inventory for `nonga-staging`
- likely source path for current operation: owner/operator out-of-band secret access -> operator session env injection
- source identified: partial (env target identified, value source path still owner/operator-controlled)

## Local Env Protection

- `.gitignore` includes:
  - `.env.local`
  - `.env.*.local`
- `.env` and environment-local files are configured as non-committed.
- local env protection for token files is present.

## Safe Operator Handoff

ACTION REQUIRED — owner/operator with secret access must set `NONGA_ADMIN_API_TOKEN` in session.

Safe setup command pattern (placeholder only, no secret exposure):

```bash
export NONGA_ADMIN_API_TOKEN="$(<SAFE_SECRET_READ_COMMAND_THAT_DOES_NOT_PRINT>)"
```

PowerShell equivalent placeholder:

```powershell
$env:NONGA_ADMIN_API_TOKEN = "$( <SAFE_SECRET_READ_COMMAND_THAT_DOES_NOT_PRINT> )"
```

Session-only verification should remain masked/presence-only; do not print the token value.

## Guardrail Confirmation

- no live auth recheck
- no call `/api/admin/duplicates`
- no smoke endpoint call
- no Gemini execution
- no `/api/gemini/*`
- no deploy
- no production
- no public route activation
- no buyer-facing AI
- no real lead
- no PII/customer data
- no print token
- no commit token
- no secret/token/API key exposed

## Recommendation

`HOLD — secret access required from owner/operator`
