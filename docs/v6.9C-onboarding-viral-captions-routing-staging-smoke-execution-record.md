# v6.9C — Onboarding + Viral Captions Routing Staging Route Smoke Execution Record

**สถานะ:** **PASS** — routable scenarios **9/9 PASS**; `/onboarding` and `/viral-captions` **fixed**  
**วันที่:** 2026-06-16  
**Git baseline (deployed):** `84529d95913dbc9b262f2254421499af7d68f476` (`84529d9`)  
**Branch:** `feature/chat-image-attachment-v1`  
**Commit message:** `fix(routing): map /onboarding and /viral-captions to correct views (v6.9C)`

> Staging hosting-only route smoke. No secrets, tokens, API keys, raw prompts, or PII in this document.

---

## 1. Overall verdict

| Item | Result |
|------|--------|
| Routable scenarios | **9/9 PASS** |
| `/onboarding` routing | **Fixed** — renders OnboardingView, not chat |
| `/viral-captions` routing | **Fixed** — renders CaptionEngineDashboard, not chat |
| Regression routes (`/home`, `/chat`, `/search`, `/login`) | **Intact** |
| Staging host | `https://nonga-ce93c.web.app` |
| Deploy type | Firebase Hosting only — UI/static assets |
| Cloud Run | **Unchanged** — `nonga-staging-00154-q7l` @ 100% |
| Production | **Not touched** |
| **v6.9 Controlled Production Pilot** | **NOT READY** |

---

## 2. Baseline

| Field | Value |
|-------|-------|
| Branch | `feature/chat-image-attachment-v1` |
| HEAD | `84529d95913dbc9b262f2254421499af7d68f476` |
| Short hash | `84529d9` |
| Staging project | `nonga-ce93c` |
| Staging URL | `https://nonga-ce93c.web.app` |
| Live CSS | `index-ButDouqC.css` (unchanged from v6.9B) |
| Live JS | `index-DmYSxXpo.js` (new — was `index-mW0K4gyL.js` pre-v6.9C) |
| Deploy type | Firebase Hosting only / UI-only |

---

## 3. Deploy summary

| Item | Result |
|------|--------|
| Deploy scope | **Staging only** — project `nonga-ce93c` |
| Production target | **Not selected / not touched** |
| Deploy command | `npm run build:staging:hosting` → `firebase deploy --only hosting --project nonga-ce93c` |
| Cloud Run image deploy | **NO** — hosting-only |
| Env / secrets | **Unchanged** |
| Code change during deploy/smoke | **NO** |
| Commit during deploy/smoke | **NO** |

---

## 4. Pre-deploy tests

| Check | Result |
|-------|--------|
| Branch | `feature/chat-image-attachment-v1` |
| HEAD | `84529d9` (= origin) |
| Working tree | clean |
| `npm run lint` | **PASS** |
| `npm run test:v69c-onboarding-viral-captions-routing-guard` | **PASS** (27/27) |
| `npm run test:v547g-car-detail-routing` | **PASS** (10/10) |
| `npm run build:staging:hosting` | **PASS** |

---

## 5. Asset verification

| Check | Result |
|-------|--------|
| Staging `index.html` references live CSS | **YES** — `index-ButDouqC.css` |
| Staging `index.html` references live JS | **YES** — `index-DmYSxXpo.js` |
| Browser loaded live CSS | **YES** — verified before smoke |
| Browser loaded live JS | **YES** — verified before smoke |
| Old JS loaded (`index-mW0K4gyL.js`) | **NO** |
| Old CSS loaded (`index-8qfUkJSm.css`) | **NO** |
| Route strings `/onboarding` and `/viral-captions` in live JS bundle | **YES** — verified in `index-DmYSxXpo.js` |
| Stale cache issue | **None observed** |
| Fresh navigate + cache-bust URLs | **YES** — `?v=84529d9` |

---

## 6. Route smoke matrix — 9/9 PASS

| # | Scenario | Result | Notes |
|---|----------|--------|-------|
| 1 | `/onboarding` direct open | **PASS** | OnboardingView — persona cards, heading `เลือกสไตล์ของ "น้องเอ AI Sales" 🪄`; no chat input |
| 2 | `/onboarding` refresh | **PASS** | URL `/onboarding` preserved; OnboardingView preserved |
| 3 | `/viral-captions` direct open | **PASS** | CaptionEngineDashboard — `ระบบแต่งแคปชั่นสลักไวรัล`, spec form; no chat input |
| 4 | `/viral-captions` refresh | **PASS** | URL `/viral-captions` preserved; CaptionEngine view preserved |
| 5 | `/home` regression | **PASS** | HomeView — simulator section; path `/home` |
| 6 | `/chat` regression | **PASS** | AIChatView — chat input present; path `/chat` |
| 7 | `/search` regression | **PASS** | SearchPageView — filter UI; path `/search` |
| 8 | `/login` regression | **PASS** | LoginView — `เข้าสู่ระบบเพื่อใช้งาน Nong A`; path `/login` |
| 9 | Header → viral captions | **PASS** | Nav `เขียนแคปชั่น 👑` → URL `/viral-captions` |
| — | Profile → onboarding (guest) | **N/A** | Pre-existing auth menu gate — requires signed-in profile; out of v6.9C scope |

---

## 7. Direct URL evidence

| Route | Pre-v6.9C (bug) | Post-v6.9C (live staging) |
|-------|-----------------|------------------------------|
| `/onboarding` | Fell through to chat (`resolveViewFromPathname` default) | **OnboardingView** — no chat input |
| `/viral-captions` | Fell through to chat | **CaptionEngineDashboard** — no chat input |

Root cause (reference): missing mappings in `resolveViewFromPathname` / `resolvePathnameForView` in `src/utils/appRouteSync.ts` — fixed in commit `84529d9`.

---

## 8. Refresh evidence

| Route | URL preserved | View preserved |
|-------|---------------|----------------|
| `/onboarding` | **YES** | **YES** — OnboardingView |
| `/viral-captions` | **YES** | **YES** — CaptionEngineDashboard |

`enforcePathnameView()` correctly resolves pathname on reload; pathname pin effect (`/`, `/chat`, `/home` only) does not override these routes.

---

## 9. Regression evidence

| Route | Expected view | Result |
|-------|---------------|--------|
| `/home` | HomeView | **PASS** |
| `/chat` | AIChatView | **PASS** |
| `/search` | SearchPageView | **PASS** |
| `/login` | LoginView | **PASS** |

---

## 10. Remaining backlog

| Item | Status |
|------|--------|
| Onboarding in-app via profile menu (guest) | Pre-existing — auth menu gate requires sign-in |
| Similar unmapped routes (`/car-vision`, `/car-post-generator`, `/seo-landing`) | Phase 2 backlog — same pattern as pre-v6.9C bug |
| v6.9 Controlled Production Pilot gates | Still pending — pilot **NOT READY** |

---

## 11. Safety confirmations

| Item | Result |
|------|--------|
| Production deploy | **NO** |
| Production pilot started | **NOT STARTED** |
| Public / nationwide opened | **NO** |
| Cloud Run image deploy | **NO** |
| Env / secrets / deploy config | **Unchanged** |
| AI runtime / Gemini provider | **Not touched** |
| Guard / fallback logic | **Not touched** |
| Firestore write | **Not touched** |
| Lead system / DealerLeads | **Not touched** |
| Payment / invoice | **Not touched** |
| Production config | **Not touched** |
| Dealer dashboard cluster | **Not touched** |
| Chat theme | **Not touched** |
| **v6.9 Controlled Production Pilot** | **NOT READY** |

---

## 12. Rollback readiness

| Item | Detail |
|------|--------|
| Rollback scope | Firebase Hosting only |
| Method | Firebase Hosting release history — redeploy prior release (pre-v6.9C JS `index-mW0K4gyL.js`) |
| Cloud Run | **Unchanged** — `nonga-staging-00154-q7l` @ 100%; **no Cloud Run rollback needed** |
| Git code rollback | Revert commit `84529d9` if code rollback required |
| Production | **Not touched** |

---

## 13. v6.9C change summary (reference)

| File | Change |
|------|--------|
| `src/utils/appRouteSync.ts` | Map `/onboarding` → `onboarding`, `/viral-captions` → `viral-captions` in `resolveViewFromPathname` and `resolvePathnameForView` |
| `scripts/test-v69c-onboarding-viral-captions-routing-guard.mts` | Static route guard (27 assertions) |
| `package.json` | npm script for v6.9C routing guard |

---

## 14. Comparison — pre-v6.9C vs v6.9C staging smoke

| Item | Pre-v6.9C (v6.9B staging) | v6.9C (`84529d9`) |
|------|---------------------------|-------------------|
| `/onboarding` | Chat view (routing bug) | **OnboardingView** |
| `/viral-captions` | Chat view (routing bug) | **CaptionEngineDashboard** |
| Live JS hash | `index-mW0K4gyL.js` | **`index-DmYSxXpo.js`** |
| Routable scenarios | 2 routes blocked | **9/9 PASS** |

---

*End of v6.9C onboarding + viral captions routing staging route smoke execution record.*
