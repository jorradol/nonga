# v19.38A - Secret Manager Binding Remediation Packet

Date: 2026-07-07
Branch: `feature/chat-image-attachment-v1`
Execution type: `SECRET MANAGER BINDING REMEDIATION PREPARATION / OWNER APPROVAL REQUIRED / NO ONE-RUN`
Expected HEAD before work: `ab2671e`

## 1) Why v19.38 is HOLD

- v19.38 masked verification found `literal env token risk: yes` on staging runtime.
- this means `NONGA_ADMIN_API_TOKEN` is present but currently bound in literal env style.
- literal env style blocks safe progression to one-run until secret binding is migrated.

## 2) Why this blocks one-run

- one-run is blocked because token binding is not in Secret Manager-backed `secretKeyRef` style yet.
- continuing without remediation increases leakage/handling risk and violates staging hard boundaries.
- v19.38A therefore remains preparation-only unless owner gives exact fresh approval.

## 3) Fresh owner approval required for runtime mutation

- exact required phrase before any platform/runtime binding change:

`FINAL AUTHORIZE v19.38A SECRET MANAGER BINDING REMEDIATION FOR NONGA_ADMIN_API_TOKEN ONLY / STAGING ONLY / CLOUD RUN NONGA-STAGING ONLY / NO ONE-RUN / NO PUBLIC / NO PRODUCTION / NO REAL DEALER / NO REAL LEAD / NO REAL CUSTOMER DATA / NO TOKEN VALUE IN CHAT OR REPO`

- without this exact phrase, do not mutate Cloud Run config and do not mutate Secret Manager.

## 4) Remediation target (staging only)

- Cloud Run service: `nonga-staging`
- region: `asia-southeast1`
- app env/secret name: `NONGA_ADMIN_API_TOKEN`
- desired binding style: Secret Manager-backed / `secretKeyRef` style
- production/public targets: out of scope and untouched

## 5) Runtime code path confirmation (static)

- `src/server/apiAuth.ts` uses `process.env.NONGA_ADMIN_API_TOKEN`
- `server.ts` protects `/api/admin` and `/api/dealer`
- `firebase.json` rewrites `/api/**` to Cloud Run service `nonga-staging`

## 6) Masked-only post-remediation verification plan

- verify `NONGA_ADMIN_API_TOKEN: present`
- verify `length: nonzero`
- verify `format: valid`
- verify `leading/trailing whitespace: no`
- verify `contains newline: no`
- verify `quoted value risk: no`
- verify `literal env token risk: no`
- verify `secret manager binding: confirmed`
- verify `starts with Bearer prefix: no`
- report token only as `***MASKED***`

## 7) Boundary guardrail checklist

- no token/secret/API key/platform auth credential/Authorization header exposure
- no token value in chat/repo/docs/report
- no one-run
- no retry
- no second-run
- no re-arm/reset/delete/mutate lock
- no public
- no production
- no real dealer action
- no real lead
- no real customer data
- no PII/phone/plate/VIN exposure

## 8) Current status and next decision

- runtime/platform mutation in v19.38A: not performed (approval absent)
- deploy: not performed
- one-run: not performed
- final decision: `NEED OWNER APPROVAL — Secret Manager binding remediation ready, no runtime mutation`

## 9) Current 5-step status

- Step 1 still not completed until controlled owner-only staging trial PASS
- Step 2 not started
- Step 3 not started
- Step 4 not started
- Step 5 not started
