# v6.0Q — Gemini Shadow Live Preflight Read-only Execution Record

**สถานะ:** Execution record (docs/tests only) — **live read-only preflight บน staging จริงแล้ว, ยังไม่ gcloud update, ยังไม่เปิด/แก้ env/secrets จริง, ไม่ deploy, ไม่เรียก paid AI API, ไม่ fetch network ใน CI, ไม่เปลี่ยน user-visible runtime behavior**  
**วันที่:** 2026-06-07  
**อ้างอิง:** v6.0P (pre-enable checklist), v6.0O (shadow dry-run), v6.0N (Gemini wiring), v6.0G (shadow module)

> **v6.0Q ยังไม่เปลี่ยน runtime behavior** — บันทึกผล **live read-only preflight** รอบสุดท้ายก่อนเปิด Gemini **shadow mode** บน staging (หลัง v6.0P approval phrase + budget caps)

---

## สารบัญ

1. [Vision & Execution Summary](#1-vision--execution-summary)
2. [Git Baseline (Recorded)](#2-git-baseline-recorded)
3. [Cloud Run Staging (Read-only Describe)](#3-cloud-run-staging-read-only-describe)
4. [AI Flags — Current (All Absent)](#4-ai-flags--current-all-absent)
5. [Secret Metadata (No Values)](#5-secret-metadata-no-values)
6. [API Smoke (Staging)](#6-api-smoke-staging)
7. [Budget Recommendation (Not Applied)](#7-budget-recommendation-not-applied)
8. [Approval Gate (v6.0P)](#8-approval-gate-v60p)
9. [Enablement Command — DO NOT RUN YET](#9-enablement-command--do-not-run-yet)
10. [Rollback Command — DO NOT RUN YET](#10-rollback-command--do-not-run-yet)
11. [AI Runtime Status & Readiness Gaps](#11-ai-runtime-status--readiness-gaps)
12. [Read-only Commands Executed](#12-read-only-commands-executed)
13. [Forbidden Actions (v6.0Q)](#13-forbidden-actions-v60q)
14. [Relationship to v6.0P](#14-relationship-to-v60p)

---

## 1. Vision & Execution Summary

### เป้าหมาย v6.0Q

บันทึกผล **live read-only preflight** บน staging จริง ก่อนเปิด Gemini shadow mode โดย:

- **Read-only / describe / GET health เท่านั้น** — ไม่ `gcloud run services update`
- ยืนยัน AI shadow flags ยัง **absent/off**
- ยืนยัน budget recommendation พร้อม (ยังไม่ set env จริง)
- บันทึก DO NOT RUN YET enable/rollback commands
- **ไม่เรียก paid AI API** ใน slice นี้

### Execution outcome

| Area | Result |
|------|--------|
| Git baseline | ✅ clean, synced — v6.0P at `eaee886` |
| Cloud Run describe | ✅ recorded (redacted) |
| AI flags | ✅ all absent — shadow **not enabled** |
| Secret metadata | ✅ `gemini-api-key` exists |
| API smoke | ✅ health 200; `publicSignupEnabled:false` |
| Budget recommendation | ✅ daily=5, monthly=50 — **not applied** |
| Approval phrase | ❌ **not received** yet |
| Ready for shadow enable | ❌ — gaps remain (§11) |

---

## 2. Git Baseline (Recorded)

Recorded at live preflight execution time:

| Check | Value |
|-------|-------|
| **git status** | clean (nothing to commit, working tree clean) |
| **branch** | `feature/chat-image-attachment-v1` |
| **HEAD** | `eaee8869a01df7ff03b82da1403275c930739c75` |
| **origin/feature/chat-image-attachment-v1** | `eaee8869a01df7ff03b82da1403275c930739c75` |
| **HEAD = origin** | ✅ yes |
| **Commit message** | `docs(ai): add staging gemini shadow pre-enable checklist` (v6.0P) |

---

## 3. Cloud Run Staging (Read-only Describe)

### Scope

| Item | Value |
|------|-------|
| **GCP project** | `nonga-ce93c` |
| **Cloud Run service** | `nonga-staging` |
| **Region** | `asia-southeast1` |
| **Latest ready revision** | `nonga-staging-00047-7xc` |
| **Service URL** | `https://nonga-staging-dpf3rexexq-as.a.run.app` |
| **Hosting (API smoke)** | `https://nonga-ce93c.web.app` |

### Non-secret env (recorded, redacted)

| Env var | Value |
|---------|-------|
| `NODE_ENV` | `production` |
| `APP_URL` | `https://nonga-ce93c.web.app` |
| `VITE_NONGA_PUBLIC_SIGNUP_ENABLED` | **`false`** ✅ |
| `VITE_FIREBASE_PROJECT_ID` | `nonga-ce93c` |
| `FIREBASE_PROJECT_ID` | `nonga-ce93c` |
| `FIREBASE_STORAGE_BUCKET` | `nonga-ce93c.firebasestorage.app` |
| `NONGA_DATA_BACKEND` | **`firestore`** |
| `NONGA_IMAGE_BACKEND` | **`firebase-storage`** |
| `NONGA_SETTLEMENT_DATA_BACKEND` | `firestore` |
| `NONGA_SETTLEMENT_FIRESTORE_WRITES_ENABLED` | `true` |
| `NONGA_SUCCESS_FEE_RECORD_ENABLED` | `true` |

### Secret ref (name only — no value)

| Env var | Secret Manager ref |
|---------|-------------------|
| `GEMINI_API_KEY` | resource **`gemini-api-key`**, key **`latest`** |
| `FIREBASE_SERVICE_ACCOUNT_JSON` | `firebase-service-account-json:latest` |

---

## 4. AI Flags — Current (All Absent)

AI shadow enablement flags on revision `nonga-staging-00047-7xc`:

| Flag | Status on staging |
|------|-------------------|
| `NONGA_AI_PROVIDER` | **absent** |
| `NONGA_AI_MODE` | **absent** |
| `NONGA_AI_FIRST_ENABLED` | **absent** |
| `NONGA_AI_SHADOW_MODE_ENABLED` | **absent** |
| `NONGA_AI_USER_VISIBLE_ENABLED` | **absent** |
| `NONGA_AI_EMERGENCY_KILL_SWITCH` | **absent** |
| `NONGA_AI_BUDGET_DAILY_LIMIT` | **absent** |
| `NONGA_AI_BUDGET_MONTHLY_LIMIT` | **absent** |

**Gemini shadow ยังไม่เปิดจริง** — ถูกต้องสำหรับ pre-enable preflight

---

## 5. Secret Metadata (No Values)

Preflight checked Secret Manager **metadata only**.

| Item | Result |
|------|--------|
| **Resource** | `gemini-api-key` |
| **Status** | **exists** |
| **Created** | `2026-05-26T00:28:19Z` |
| **Replication** | `automatic` |
| **Cloud Run mapping** | env `GEMINI_API_KEY` → `gemini-api-key:latest` |

**Did not run** `gcloud secrets versions access` — **no secret values** in this record, repo, or CI output.

---

## 6. API Smoke (Staging)

Executed via GET to hosting URL (read-only, not AI provider):

| Endpoint | Expected | Actual | Notes |
|----------|----------|--------|-------|
| `GET /api/health` | 200 | **200** ✅ | `ok:true`, `dataBackend:firestore`, `imageBackend:firebase-storage`, **`publicSignupEnabled:false`** |

---

## 7. Budget Recommendation (Not Applied)

Per v6.0P — **ต้องมีตัวเลขก่อน enable; ไม่มี budget = ห้ามเปิด**

| Env var | Recommended staging cap | Applied on Cloud Run? |
|---------|------------------------|----------------------|
| `NONGA_AI_BUDGET_DAILY_LIMIT` | **`5`** (USD or token equivalent) | ❌ **absent** — not set in v6.0Q |
| `NONGA_AI_BUDGET_MONTHLY_LIMIT` | **`50`** (USD or token equivalent) | ❌ **absent** — not set in v6.0Q |

**v6.0Q ไม่ set env จริง** — operator applies caps only in future enablement command after approval.

---

## 8. Approval Gate (v6.0P)

### Required approval phrase (ลุง) — **not received in v6.0Q preflight**

```
อนุมัติให้เปิด Gemini shadow mode บน staging แบบไม่แสดงคำตอบ AI ให้ผู้ใช้ ตาม v6.0P
```

| Rule | Status |
|------|--------|
| Phrase received | ❌ **not yet** |
| Enablement allowed | ❌ **blocked** until phrase + budget caps confirmed |
| v6.0O phrase substitutes | ❌ no |

**ห้ามรัน enablement** จนกว่าได้ phrase + budget caps กำหนดแล้ว

---

## 9. Enablement Command — DO NOT RUN YET

> **⛔ DO NOT RUN YET** — staging `nonga-staging` only, project `nonga-ce93c`. Requires v6.0P approval phrase + ops sign-off.

```bash
# ⛔ DO NOT RUN YET — Gemini shadow, user-visible off
gcloud run services update nonga-staging \
  --project=nonga-ce93c \
  --region=asia-southeast1 \
  --update-env-vars="NONGA_AI_PROVIDER=gemini,NONGA_AI_MODE=high,NONGA_AI_FIRST_ENABLED=true,NONGA_AI_SHADOW_MODE_ENABLED=true,NONGA_AI_USER_VISIBLE_ENABLED=false,NONGA_AI_EMERGENCY_KILL_SWITCH=false,NONGA_AI_BUDGET_DAILY_LIMIT=5,NONGA_AI_BUDGET_MONTHLY_LIMIT=50"
```

| Target env | Value |
|------------|-------|
| `NONGA_AI_PROVIDER` | `gemini` |
| `NONGA_AI_MODE` | `high` |
| `NONGA_AI_FIRST_ENABLED` | `true` |
| `NONGA_AI_SHADOW_MODE_ENABLED` | `true` |
| `NONGA_AI_USER_VISIBLE_ENABLED` | **`false`** |
| `NONGA_AI_EMERGENCY_KILL_SWITCH` | `false` |
| `NONGA_AI_BUDGET_DAILY_LIMIT` | `5` |
| `NONGA_AI_BUDGET_MONTHLY_LIMIT` | `50` |

---

## 10. Rollback Command — DO NOT RUN YET

> **⛔ DO NOT RUN YET** — emergency rollback to legacy deterministic

```bash
# ⛔ DO NOT RUN YET
gcloud run services update nonga-staging \
  --project=nonga-ce93c \
  --region=asia-southeast1 \
  --update-env-vars="NONGA_AI_EMERGENCY_KILL_SWITCH=true,NONGA_AI_SHADOW_MODE_ENABLED=false,NONGA_AI_FIRST_ENABLED=false,NONGA_AI_MODE=off,NONGA_AI_USER_VISIBLE_ENABLED=false"
```

| Rollback lever | Value |
|----------------|-------|
| `NONGA_AI_EMERGENCY_KILL_SWITCH` | `true` |
| `NONGA_AI_SHADOW_MODE_ENABLED` | `false` |
| `NONGA_AI_FIRST_ENABLED` | `false` |
| `NONGA_AI_MODE` | `off` |
| `NONGA_AI_USER_VISIBLE_ENABLED` | `false` |

Fallback: **legacy deterministic** (`chatSearchOrchestrator` + templates)

---

## 11. AI Runtime Status & Readiness Gaps

### Runtime status (code at HEAD `eaee886`)

| Check | Status |
|-------|--------|
| `SALES_BRAIN_REAL_PROVIDER_NETWORK_ENABLED=false` | ✅ |
| `useChat.ts` — no Sales Brain imports | ✅ |
| `NONGA_AI_SHADOW_*` read in `src/` | ❌ **not yet** — no runtime env reader for shadow flags |
| User-visible Gemini | ❌ none |

### Readiness gaps

| Gap | Severity | Detail |
|-----|----------|--------|
| **v6.0P approval phrase** | **blocker** | **ยังไม่ได้รับ** from ลุง |
| **Budget env on Cloud Run** | **blocker** | `NONGA_AI_BUDGET_*` still **absent** — use 5/50 at enable time only |
| **Shadow env flags absent** | **expected** | correct pre-enable state |
| **Runtime shadow wiring** | **blocker for functional shadow** | `NONGA_AI_SHADOW_MODE_ENABLED` / `NONGA_AI_USER_VISIBLE_ENABLED` **not read in src** — requires separate slice wiring + deploy before shadow works |
| **Gemini secret** | ✅ pass | `gemini-api-key` exists |
| **Real stock import** | **blocked** | until shadow smoke + AI conversation smoke pass |

**Post-enable smoke (future):** health 200, legacy chat unchanged, user-visible off, no raw PII, no-go zones, budget/kill switch drill, no lead/reveal/outcome regression

---

## 12. Read-only Commands Executed

```bash
# Git
git status
git branch --show-current
git rev-parse HEAD
git rev-parse origin/feature/chat-image-attachment-v1

# GCP project
gcloud config get-value project

# Cloud Run describe (redacted)
gcloud run services describe nonga-staging \
  --region=asia-southeast1 \
  --project=nonga-ce93c \
  --format="yaml(status.latestReadyRevisionName,status.url,spec.template.spec.containers[0].env)"

# Secret metadata only — NO versions access
gcloud secrets describe gemini-api-key \
  --project=nonga-ce93c \
  --format="yaml(name,createTime,replication,labels)"

# API smoke (GET only — not AI provider)
curl -s -w "\nHTTP:%{http_code}\n" "https://nonga-ce93c.web.app/api/health"
```

### Not executed (forbidden)

- `gcloud run services update`
- `gcloud secrets versions access`
- `firebase deploy` / `gcloud run deploy`
- Paid Gemini API calls

---

## 13. Forbidden Actions (v6.0Q)

| Forbidden | v6.0Q status |
|-----------|--------------|
| Deploy Cloud Run / Hosting / Firestore rules | ❌ not done |
| `gcloud run services update` | ❌ not done |
| Open or modify env/secrets | ❌ not done |
| Access secret values / `secrets versions access` | ❌ not done |
| Call paid AI API | ❌ not done |
| Change user-visible runtime behavior | ❌ not done |
| Touch production | ❌ not done |
| Payment / invoice / settlement write changes | ❌ not done |
| Buyer lead / seller reveal / outcome changes | ❌ not done |
| Public signup enable | ❌ not done — staging `false` |

CI static test uses **readFileSync only** — no fetch, no gcloud, no Firebase, no AI API.

---

## 14. Relationship to v6.0P

| Slice | Role |
|-------|------|
| **v6.0P** | Pre-enable checklist + approval phrase definition |
| **v6.0Q** | **This doc** — live read-only preflight execution record |

Next: await v6.0P approval phrase → separate runtime wiring slice → enablement (DO NOT RUN YET) → post-enable shadow smoke → AI conversation smoke → stock import planning
