# v6.9A Phase 1.5 — Dark Variant Alignment Staging Visual Smoke Execution Record

**สถานะ:** **PASS** — visual smoke matrix **4/4 PASS**; critical App light + OS dark **PASS**  
**วันที่:** 2026-06-16  
**Git baseline (deployed):** `c43bc4244c265057e1d3e1c018072b4098dfe568` (`c43bc42`)  
**Branch:** `feature/chat-image-attachment-v1`  
**Commit message:** `fix(ui): align dark variant with app theme toggle`

> Staging hosting-only visual smoke. No secrets, tokens, API keys, raw prompts, or PII in this document.

---

## 1. Overall verdict

| Item | Result |
|------|--------|
| Visual smoke matrix | **4/4 PASS** |
| Critical target — App light + OS dark | **PASS** |
| Dark mode regression | **None observed** |
| Staging host | `https://nonga-ce93c.web.app` |
| Deploy type | Firebase Hosting only — UI/static assets |
| Cloud Run | **Unchanged** — `nonga-staging-00154-q7l` @ 100% |
| Production | **Not touched** |
| **v6.9 Controlled Production Pilot** | **NOT READY** |

---

## 2. Baseline

| Field | Value |
|-------|-------|
| Branch | `feature/chat-image-attachment-v1` |
| HEAD | `c43bc4244c265057e1d3e1c018072b4098dfe568` |
| Short hash | `c43bc42` |
| Staging project | `nonga-ce93c` |
| Staging URL | `https://nonga-ce93c.web.app` |
| Live CSS | `index-ButDouqC.css` (264,343 bytes) |
| Live JS | `index-Ddq10mfa.js` (3,886,443 bytes) |
| Hosting release | 2026-06-16 08:34:07 (local) / `2026-06-16T01:34:07Z` |
| Viewport | Mobile 390×844 (drawer + footer checks) |

---

## 3. Pre-deploy (Phase 1.5 hosting deploy — prior session)

| Check | Result |
|-------|--------|
| Branch | `feature/chat-image-attachment-v1` |
| HEAD | `c43bc42` (= origin) |
| Working tree | clean |
| `npm run lint` | **PASS** |
| `npm run test:v69a-light-mode-contrast-phase1-guard` | **PASS** (20/20) |
| `npm run test:v69a-phase15-dark-variant-alignment-guard` | **PASS** (9/9) |
| `npm run build:staging:hosting` | **PASS** |
| Deploy command | `firebase deploy --only hosting --project nonga-ce93c` |
| Cloud Run deploy | **NO** — hosting-only |

---

## 4. Asset verification (visual smoke rerun)

| Check | Result |
|-------|--------|
| Staging `index.html` references new CSS | **YES** — `index-ButDouqC.css` |
| Staging `index.html` references new JS | **YES** — `index-Ddq10mfa.js` |
| Browser loaded new CSS | **YES** — verified before smoke |
| Browser loaded new JS | **YES** — verified before smoke |
| Old assets loaded (`index-8qfUkJSm.css`, `index-B4N4UwpO.js`) | **NO** |
| Prior S2 FAIL (App light + OS dark) | **Invalid** — caused by stale browser session with pre-deploy hashed assets |
| Redeploy during visual smoke rerun | **NO** |
| Code change during visual smoke rerun | **NO** |
| Commit during visual smoke rerun | **NO** |

### Stale cache root cause (prior invalid S2)

- Hosting deploy correctly published new hashed assets; `index.html` uses `Cache-Control: no-cache`.
- Hashed assets use `Cache-Control: public, max-age=31536000, immutable`.
- A stale browser tab retained old `<link>` / `<script>` references from before Phase 1.5 deploy.
- Fresh navigate with cache-bust confirmed new assets; S2 then **PASS**.

---

## 5. Visual smoke matrix — 4/4 PASS

| # | Scenario | App | OS | Result | Notes |
|---|----------|-----|-----|--------|-------|
| S1 | App light + OS light | light | light | **PASS** | Footer brand ratio ~8.12; drawer brand ratio ~8.12; h5 readable |
| S2 | App light + OS dark | light | dark | **PASS** | Critical fix target — see §6 |
| S3 | App dark + OS light | dark | light | **PASS** | `.dark` activates dark variant; footer/drawer ratio ~19.54 |
| S4 | App dark + OS dark | dark | dark | **PASS** | No OS-driven light-shell bleed; dark shell readable |

---

## 6. Critical fix evidence — App light + OS dark (S2)

| Criterion | Result |
|-----------|--------|
| `htmlDark` false with OS dark | **YES** |
| `dark:text-white` applied on light shell | **NO** — `darkWhiteAppliesWhenLight=false` |
| Footer brand white-on-white | **NO** — dark slate text (`oklch(0.208…)`) on white footer, ratio ~8.12 |
| Footer h5 readable | **YES** — ratio ~19.3 |
| Footer description readable | **YES** — ratio ~8.49 |
| Mobile drawer brand white-on-white | **NO** — ratio ~8.12 |
| `.dark` class controls Tailwind `dark:` variant | **YES** — class-only `@custom-variant dark` |
| OS `prefers-color-scheme: dark` overrides app light mode | **NO** — Phase 1.5 fix verified on live staging |

---

## 7. Dark mode regression

| Check | Result |
|-------|--------|
| S3 App dark + OS light | **PASS** — `.dark` on `<html>` activates `dark:text-white` |
| S4 App dark + OS dark | **PASS** — no regression |
| Toggle light ↔ dark | **Works** — footer/drawer readable in both modes |
| Dark mode regression observed | **None** |

---

## 8. Additional route checks

| Route | Result | Notes |
|-------|--------|-------|
| `/home` | **PASS** | Loads; footer + mobile drawer tested in matrix |
| `/chat` | **PASS** | Loads; chat input present; no chat theme regression observed (Phase 1.5 out of scope) |
| `/onboarding` | **BLOCKED** | Pre-existing / out of scope — URL renders chat view, not onboarding |
| `/viral-captions` | **BLOCKED** | Pre-existing / out of scope — same routing issue |

---

## 9. Remaining issues / Phase 2 backlog

| Item | Status |
|------|--------|
| Trust panel contrast (light mode) | Minor — ratio ~2.47 (below 3:1); not white-on-white |
| `/onboarding` routing | Pre-existing — not in Phase 1.5 scope |
| `/viral-captions` routing | Pre-existing — not in Phase 1.5 scope |
| Broader light-mode contrast sweep | Phase 2 — dealer dashboard cluster, chat theme polish |
| v6.9 Controlled Production Pilot gates | Still pending — pilot **NOT READY** |

---

## 10. Safety confirmations

| Item | Result |
|------|--------|
| Production deploy | **NO** |
| Cloud Run deploy | **NO** |
| Code change during visual smoke rerun | **NO** |
| Commit during visual smoke rerun | **NO** |
| AI runtime / Gemini provider | **Not touched** |
| Guard / fallback logic | **Not touched** |
| Deploy config / env / secrets | **Not touched** |
| Firestore write | **Not touched** |
| Lead system / DealerLeads | **Not touched** |
| Payment / invoice | **Not touched** |
| Production config | **Not touched** |
| Dealer dashboard cluster | **Not touched** |
| Chat theme | **Not touched** |
| Routing `/onboarding` / `/viral-captions` | **Not touched** |
| **v6.9 Controlled Production Pilot** | **NOT READY** |

---

## 11. Rollback readiness

| Item | Detail |
|------|--------|
| Rollback scope | Firebase Hosting only |
| Method | Firebase Hosting release history — redeploy prior release or `firebase hosting:rollback` |
| Prior release | Available via Hosting console (pre-2026-06-16 08:34:07 release) |
| Cloud Run | **Unchanged** — `nonga-staging-00154-q7l` @ 100%; no rollback needed |
| Production | **Not touched** |

---

## 12. Phase 1.5 change summary (reference)

| File | Change |
|------|--------|
| `src/index.css` | `@custom-variant dark (&:where(.dark, .dark *));` — class-only dark variant |
| `src/store.ts` | `toggleDarkMode()` syncs `.dark` on `<html>`; bootstrap on load |
| `scripts/test-v69a-phase15-dark-variant-alignment-guard.mts` | Static + build spot-check guard |
| `package.json` | npm script for Phase 1.5 guard |

---

## 13. Comparison — Phase 1 vs Phase 1.5 staging smoke

| Item | Phase 1 (`ce861a9`) | Phase 1.5 (`c43bc42`) |
|------|---------------------|------------------------|
| App light + OS dark | **FAIL** — white-on-white footer/drawer | **PASS** |
| Root cause | Tailwind `dark:` followed OS preference | Class-only `dark:` aligned with app toggle |
| Visual smoke matrix | Not fully 4/4 at time of Phase 1 deploy | **4/4 PASS** |

---

*End of v6.9A Phase 1.5 staging visual smoke execution record.*
