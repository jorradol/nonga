# v22.22 - HOLD Fix: Real Staging Browser Chat Response Path

Date: 2026-07-09  
Branch: `feature/chat-image-attachment-v1`  
Execution type: `CONTROLLED STAGING CHAT RESPONSE STYLE HOLD FIX / REAL BROWSER PATH REGRESSION / THOR AUTO IMPORTED LISTINGS / NO PRODUCTION / NO PUBLIC / NO REAL LEAD / NO DEALER-FACING SEND`

## 1) Purpose

Fix the **actual owner-browser staging chat path** that still returned old UI/system inventory wording after v22.21 Hosting-only deploy, and remove default customer address “ลุง”.

## 2) Baseline before work

| Item | Value |
|------|--------|
| Branch | `feature/chat-image-attachment-v1` |
| HEAD | `336a5d0ad10bc2619ff21334346310937394ca77` |
| Origin | up to date |
| Working tree | untracked compiled `.js` mirrors only |
| Prior | HOLD — owner incognito still saw old text |

## 3) Root cause

Signed-in staging chat calls Cloud Run:

`POST /api/ai/chat-user-visible-orchestrate`

via `applyChatUserVisibleServerBridge` in `useChat.ts`. That bridge runs `tryOrchestrateChatReplyCore` **on Cloud Run** and **overwrites** the Hosting-bundled orchestrator text with `bridged.userVisibleText`.

v22.21 deployed **Firebase Hosting only**. Cloud Run remained `nonga-staging-00183-z2t` with the old templates, so owner-browser (signed-in) still got:

- `ค้นเจอ 1 คันที่ตรงสเปกครับ…`
- `น้องเอจัดการ์ดไว้ด้านล่างให้แล้ว`
- `กด 'ดูรายละเอียดในแชท'`
- routine `ปังปุริเย่!`

Hosting bundle already had the v22.21 copy; the browser path that matters for signed-in owner was Cloud Run.

Also: v22.21 wording used default “ลุง” — not acceptable for general buyers.

## 4) Fix

1. Neutral buyer-facing inventory copy (no default “ลุง”, no UI CTAs, no routine cheer)
2. Remove old openers from `thaiSalesCopyVariation.ts` as well
3. Deploy **Cloud Run staging** (required) + Hosting (so guest/local bundle matches)
4. Browser/API regression test `test:v22.22`

## 5) Boundary status

- staging only; no production; public signup false; no lead/dealer-facing send; no owner token/terminal
