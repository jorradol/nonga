# Nong A v5.1 Staging Candidate Summary

Date recorded: Tuesday May 26, 2026

This document records the current closed staging checkpoint for Nong A v5.1. It intentionally does not include passwords, secret values, Firebase service account JSON, API keys, tokens, or private user identifiers.

## Status

Nong A v5.1 is a **Staging Candidate / Closed Staging Ready** build.

This is **not** a Production release and is **not** a Public Beta. Public signup remains disabled, production has not been deployed, and durable Firestore/Storage backend migration has not been performed.

## Target Environment

- Hosting URL: `https://nonga-ce93c.web.app`
- Firebase project: `nonga-ce93c`
- Cloud Run service: `nonga-staging`
- Cloud Run region: `asia-southeast1`
- Data backend: `file`
- Image backend: `file`
- Public signup: disabled

Latest staged checkpoint commits:

- Draft validation fix: `7e3a0c07d1cf0492035aa481703fb7267cfc2b2a`
- Missing image checklist fix: `5b2f62e712990b917faf09e9700edcbfa6e03dc5`

Latest deployed staging artifacts at the time of this checkpoint:

- Cloud Run image: `asia-southeast1-docker.pkg.dev/nonga-ce93c/nonga-staging/nonga-staging:v5.0-cloudrun-staging-5b2f62e-missing-image-20260526-165947`
- Cloud Run revision: `nonga-staging-00005-ssc`
- Firebase Hosting: deployed to `https://nonga-ce93c.web.app`

## Health Check

`GET https://nonga-ce93c.web.app/api/health` was reported healthy after staging deploy:

```json
{
  "ok": true,
  "dataBackend": "file",
  "imageBackend": "file",
  "publicSignupEnabled": false
}
```

## Passed Staging Checks

- Member role can log in and is blocked from Dealer Portal and Admin.
- Dealer role can log in and access Dealer Portal.
- Dealer role is blocked from Admin.
- Admin role can log in and access Admin.
- Superadmin no longer appears as guest and can access admin-level routes.
- Dealer create/save Draft works.
- Drafts can be saved even when listing information is incomplete.
- Publish guard still blocks marketplace publish when required fields are missing.
- Chat general message flow works.
- Chat-to-Draft saves Drafts successfully.
- Missing checklist is complete for publish requirements, including examples such as mileage and image.
- New image upload works from both the web Dealer flow and chat flow.
- Newly uploaded staging images render successfully.
- `/api/health` remains `ok=true`, `dataBackend=file`, `imageBackend=file`, `publicSignupEnabled=false`.

## Current Limitations

- `NONGA_DATA_BACKEND=file` is still in use.
- `NONGA_IMAGE_BACKEND=file` is still in use.
- Historical `/storage/listings/**` image URLs that return `404` are a migration/durable-storage issue, not a current upload flow blocker.
- Cloud Run file backend is suitable only for closed staging and internal demo validation.
- File-backed Cloud Run data and uploads are ephemeral and instance-local.
- Migration write has not been run.
- Backend flags have not been changed to Firestore or Firebase Storage.
- Public signup has not been opened.
- Production has not been deployed.
- GitHub has not been pushed as part of this checkpoint.

## Explicitly Not Done

- No migration write.
- No switch to `NONGA_DATA_BACKEND=firestore`.
- No switch to `NONGA_IMAGE_BACKEND=firebase-storage`.
- No public signup enablement.
- No production deploy.
- No Firestore or Storage rules deploy.
- No Functions or App Hosting deploy.
- No GitHub push.

## Recommended Next Steps

1. Create a local git tag after review, for example `v5.1-staging-candidate`.
2. Continue treating new file-backed uploads as disposable closed staging data.
3. Plan the Firebase Storage and Firestore staging migration rehearsal separately.
4. Only after explicit approval, run migration dry-runs and then review any proposed migration write.
5. Keep public signup closed until there is a separate Public Beta decision.
