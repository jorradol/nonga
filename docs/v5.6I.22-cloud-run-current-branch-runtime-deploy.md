# v5.6I.22 — Cloud Run Current Branch Runtime Deploy (Recorded)

**สถานะ:** Execution record + static tests only — **ไม่ deploy อีก, ไม่เปิด settlement persistence**  
**Git baseline ขณะรัน:** `cebc7a607cae5fcdd22fd6807bee03e77d5aa4f3` (v5.6I.21)  
**วันที่บันทึก:** 2026-06-07  
**Target:** staging **`nonga-ce93c`** เท่านั้น  
**Branch:** `feature/chat-image-attachment-v1`  
**อ้างอิง:** v5.6I.21 rollback report, v5.6I.13 durable idempotency wiring, v5.6I.19 smoke harness

> เอกสารนี้บันทึกผล **deploy Cloud Run staging image จาก branch ล่าสุด** — runtime ใช้โค้ด v5.6I.13+ แล้ว แต่ **ยังไม่เปิด** Firestore settlement persistence

---

## สรุปผล (plain language)

| เรื่อง | ผล |
|--------|-----|
| Preflight | **PASS** — git clean, HEAD = origin @ `cebc7a6` |
| ก่อน deploy | rev `nonga-staging-00045-78f`, image `v5.6I.4-manual-settlement-adjustments-8bf49fd`, flags memory/false/false |
| Cloud Build | **SUCCESS** — `0d07e080-5a31-4b62-af58-5fac248730ba` |
| Cloud Run deploy | rev **`nonga-staging-00046-rh7`**, traffic **100%** |
| Settlement persistence | **ยังปิด** — memory/false/false |
| Firestore settlement docs | **0 docs** ทั้ง 4 collections |
| Hosting / rules / production | **ไม่แตะ** |

---

## 1. Preflight (passed)

| Check | Result |
|-------|--------|
| `git status` | clean |
| branch | `feature/chat-image-attachment-v1` |
| HEAD | `cebc7a607cae5fcdd22fd6807bee03e77d5aa4f3` |
| `origin/feature/chat-image-attachment-v1` | ตรงกับ HEAD |
| gcloud active project | `nonga-ce93c` |
| Cloud Run service | `nonga-staging` |
| region | `asia-southeast1` |

---

## 2. ก่อน deploy

| Field | Value |
|-------|-------|
| Revision | **`nonga-staging-00045-78f`** |
| Image tag | `v5.6I.4-manual-settlement-adjustments-8bf49fd` |

**Settlement flags (ก่อน deploy):**

| Env | Value |
|-----|-------|
| `NONGA_SETTLEMENT_DATA_BACKEND` | `memory` |
| `NONGA_SETTLEMENT_FIRESTORE_WRITES_ENABLED` | `false` |
| `NONGA_SUCCESS_FEE_RECORD_ENABLED` | `false` |

---

## 3. Tests / build ก่อน deploy (passed)

```bash
npm run test:v56i21-staging-persistence-enable-smoke-rollback-report
npm run test:v56i13-durable-idempotency-transaction-wiring
npm run lint
npm run build:staging:hosting
```

---

## 4. Deploy (executed — recorded only, do not re-run from CI)

### Cloud Build

```bash
# รันจริงแล้ว — อย่ารันซ้ำจาก CI
gcloud builds submit --project=nonga-ce93c \
  --config=cloudbuild.v53f.yaml \
  --substitutions=_IMAGE_URI=asia-southeast1-docker.pkg.dev/nonga-ce93c/nonga-staging/nonga-staging:v5.6I.22-current-branch-runtime-cebc7a6,...
```

| Field | Value |
|-------|-------|
| Cloud Build ID | **`0d07e080-5a31-4b62-af58-5fac248730ba`** |
| Status | **SUCCESS** |
| Duration | ~2m44s |

| Field | Value |
|-------|-------|
| Image tag | **`v5.6I.22-current-branch-runtime-cebc7a6`** |
| Image URI | `asia-southeast1-docker.pkg.dev/nonga-ce93c/nonga-staging/nonga-staging:v5.6I.22-current-branch-runtime-cebc7a6` |
| Image digest | **`sha256:30cd341c3320aaad428c935816064351f5d0e5a3bf61cf3aedf7f2cf8d015e03`** |

### Cloud Run deploy (image only — preserve env/secrets)

```bash
# รันจริงแล้ว — ไม่มี --set-env-vars / --update-env-vars
gcloud run deploy nonga-staging \
  --image asia-southeast1-docker.pkg.dev/nonga-ce93c/nonga-staging/nonga-staging:v5.6I.22-current-branch-runtime-cebc7a6 \
  --project=nonga-ce93c \
  --region=asia-southeast1 \
  --allow-unauthenticated \
  --service-account=nonga-staging-runner@nonga-ce93c.iam.gserviceaccount.com
```

| Field | Value |
|-------|-------|
| New revision | **`nonga-staging-00046-rh7`** |
| Traffic | **100%** |
| Previous revision | `nonga-staging-00045-78f` |

---

## 5. Health / API smoke หลัง deploy

Base: `https://nonga-ce93c.web.app`

| Endpoint | HTTP status |
|----------|-------------|
| `GET /api/health` | **200** |
| `GET /api/cars?limit=1` | **200** |
| `GET /api/admin/revenue/preview` (no auth) | **401** |
| `GET /api/my/revenue/preview` (no auth) | **401** |

---

## 6. Env flags หลัง deploy (settlement ยังปิด)

| Env | Value |
|-----|-------|
| `NONGA_SETTLEMENT_DATA_BACKEND` | **`memory`** |
| `NONGA_SETTLEMENT_FIRESTORE_WRITES_ENABLED` | **`false`** |
| `NONGA_SUCCESS_FEE_RECORD_ENABLED` | **`false`** |
| `VITE_NONGA_PUBLIC_SIGNUP_ENABLED` | `false` |

Inventory/image backends คงเดิม: `NONGA_DATA_BACKEND=firestore`, `NONGA_IMAGE_BACKEND=firebase-storage`

---

## 7. Firestore settlement collections (0 docs — ไม่มี durable writes)

| Collection | Doc count |
|------------|-----------|
| `settlementAdjustments` | **0** |
| `settlementAuditLogs` | **0** |
| `settlementIdempotencyRecords` | **0** |
| `successFeeRecords` | **0** |

ไม่มี Firestore durable settlement writes จาก deploy รอบนี้

---

## 8. Next step (do not run from v5.6I.22 doc)

1. Re-enable settlement flags ด้วย **quoted env** บน PowerShell (ตาม v5.6I.21 lesson)
2. รัน smoke **v5.6I.19** บน revision **`nonga-staging-00046-rh7`**
3. คาดหวัง S5/S6 pass + Firestore settlement docs > 0 (runtime มี v5.6I.13+ wiring)

**DO NOT RE-DEPLOY** จาก commit docs-only นี้

---

## Forbidden changes (v5.6I.22 slice — confirmed)

- ไม่ Hosting deploy
- ไม่ Firestore rules deploy
- ไม่ persistence enablement (firestore/true/true)
- ไม่ `gcloud run deploy` / `gcloud run services update` (ใน slice docs นี้)
- ไม่เปิด env flags เป็น firestore/true/true
- ไม่เขียน Firestore / migration / backfill
- ไม่แตะ production
- ไม่ payment / invoice / Gemini changes
- ไม่แก้ runtime code / secrets ในรอบ v5.6I.22 docs

---

## Files (v5.6I.22)

| File | เปลี่ยน |
|------|--------|
| `docs/v5.6I.22-cloud-run-current-branch-runtime-deploy.md` | **NEW** — record นี้ |
| `scripts/test-v56i22-cloud-run-current-branch-runtime-deploy.mts` | **NEW** — static validation |
| `package.json` | `test:v56i22-cloud-run-current-branch-runtime-deploy` |
