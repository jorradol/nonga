# v6.1H — Real Gemini Shadow Call Admin-only Staging Smoke / User-visible Off

**สถานะ:** Execution record — **Cloud Run deployed, admin real-provider flag enabled, automated health/guard smoke PASS, live admin SS-01 Gemini call requires Firebase admin session**  
**วันที่:** 2026-06-08  
**Git baseline (pre-slice):** `319fcc5f4c84eae425c6e4ba5bb065978573b0fc` (v6.1G)  
**Branch:** `feature/chat-image-attachment-v1`  
**Approval phrase:** อนุมัติให้ทดสอบ real Gemini shadow call เฉพาะ admin-only บน staging โดยไม่แสดงผลให้ผู้ใช้ทั่วไป ตาม v6.1H

> **v6.1H เปิด real Gemini path เฉพาะ admin shadow route SS-01** — `NONGA_AI_ADMIN_SHADOW_REAL_PROVIDER_ENABLED=true`, `NONGA_AI_USER_VISIBLE_ENABLED=false`, public chat ไม่เปลี่ยน

---

## 1. Preflight

| Check | Result |
|-------|--------|
| git status (pre-slice) | clean @ `319fcc5` |
| branch | `feature/chat-image-attachment-v1` |
| HEAD = origin (pre-slice) | ✅ `319fcc5` |
| project | `nonga-ce93c` |
| service | `nonga-staging` |
| region | `asia-southeast1` |
| previous revision | `nonga-staging-00051-fbd` |
| `publicSignupEnabled` | **`false`** |
| `NONGA_AI_USER_VISIBLE_ENABLED` (before) | **`false`** |
| budget daily / monthly | `5` / `50` |
| kill switch rollback | prepared — §8 |
| `GEMINI_API_KEY` ref | `gemini-api-key:latest` (metadata only) |

**Did not run** `gcloud secrets versions access` — no secret values in this record.

---

## 2. Tests / Build Before Deploy

| Command | Result |
|---------|--------|
| `npm run test:v61g-real-gemini-shadow-call-dry-run-readiness` | **PASS** |
| `npm run test:v61f-human-manual-smoke-checklist-admin-shadow-debug-ui` | **PASS** |
| `npm run test:v61e-admin-shadow-debug-ui-staging-hosting-deploy-record` | **PASS** |
| `npm run test:v61h-real-gemini-shadow-call-admin-only-staging-smoke` | **PASS** |
| `npm run test:v61b-server-side-shadow-evaluation-admin-debug-user-visible-off` | **PASS** |
| `npm run lint` | **PASS** |
| `npm run build:staging:hosting` | **PASS** |

---

## 3. Runtime Changes (v6.1H)

| File / area | Change |
|-------------|--------|
| `salesBrainRuntimeFlags.ts` | `NONGA_AI_ADMIN_SHADOW_REAL_PROVIDER_ENABLED` env constant |
| `salesBrainAdminShadowRealProvider.ts` | **new** — admin-only Gemini call (SS-01 only, redacted) |
| `salesBrainRealProvider.ts` | `buildAdminShadowProviderRequest`, `adminShadowRouteOnly` bypass |
| `salesBrainServerShadowSmoke.ts` | async handler, `providerNetwork` dynamic, legacy `userVisibleResponse` unchanged |
| `adminShadowSmokeApi.ts` | provider `mock \| gemini`, redacted provider metadata |
| Public chat path | **unchanged** — legacy orchestrator |
| Real stock import | **still blocked** |

Behavior:

| Flag | Route | Result |
|------|-------|--------|
| `NONGA_AI_ADMIN_SHADOW_REAL_PROVIDER_ENABLED=false` | admin SS-* | `providerNetwork:false`, mock |
| `NONGA_AI_ADMIN_SHADOW_REAL_PROVIDER_ENABLED=true` | admin SS-01 only | real Gemini allowed (staging) |
| any | admin SS-02..SS-08 | mock only |
| any | public `/chat` | legacy only |

---

## 4. Cloud Run Deploy

### Cloud Build

```bash
gcloud builds submit --project=nonga-ce93c \
  --config=cloudbuild.v53f.yaml \
  --substitutions=_IMAGE_URI=asia-southeast1-docker.pkg.dev/nonga-ce93c/nonga-staging/nonga-staging:v6.1H-admin-shadow-real-gemini-v61h,...
```

| Field | Value |
|-------|-------|
| **Cloud Build ID** | `54ee0847-590d-42ad-8756-4b812406da38` |
| **Status** | **SUCCESS** (~2m42s) |
| **Image tag** | `v6.1H-admin-shadow-real-gemini-v61h` |

### Cloud Run image deploy

```bash
gcloud run deploy nonga-staging \
  --image asia-southeast1-docker.pkg.dev/nonga-ce93c/nonga-staging/nonga-staging:v6.1H-admin-shadow-real-gemini-v61h \
  --project=nonga-ce93c --region=asia-southeast1 \
  --allow-unauthenticated \
  --service-account=nonga-staging-runner@nonga-ce93c.iam.gserviceaccount.com
```

| Field | Value |
|-------|-------|
| **Image revision** | `nonga-staging-00052-bp6` |
| **Deploy type** | image-only |

---

## 5. Env Flags After Enable

```bash
gcloud run services update nonga-staging \
  --project=nonga-ce93c --region=asia-southeast1 \
  --update-env-vars="NONGA_AI_ADMIN_SHADOW_REAL_PROVIDER_ENABLED=true,NONGA_AI_USER_VISIBLE_ENABLED=false"
```

| Flag | Value on `nonga-staging-00053-nvf` |
|------|-----------------------------------|
| `NONGA_AI_ADMIN_SHADOW_REAL_PROVIDER_ENABLED` | **`true`** |
| `NONGA_AI_USER_VISIBLE_ENABLED` | **`false`** |
| `NONGA_AI_PROVIDER` | `gemini` |
| `NONGA_AI_SHADOW_MODE_ENABLED` | `true` |
| `NONGA_AI_EMERGENCY_KILL_SWITCH` | `false` |
| `NONGA_AI_BUDGET_DAILY_LIMIT` | `5` |
| `NONGA_AI_BUDGET_MONTHLY_LIMIT` | `50` |
| `VITE_NONGA_PUBLIC_SIGNUP_ENABLED` | `false` |
| `GEMINI_API_KEY` | secret ref `gemini-api-key:latest` (unchanged) |

**Live revision:** `nonga-staging-00053-nvf` @ **100%**

---

## 6. Staging Smoke Results

### API health (automated)

| Endpoint | HTTP | Key fields |
|----------|------|------------|
| `GET /api/health` | **200** | `ok:true`, `publicSignupEnabled:false` |
| `GET /api/cars?limit=1` | **200** | `success:true` |
| `GET /api/admin/revenue/preview` (unauth) | **401** | auth required |

### Admin route guards (automated)

| Check | HTTP | Result |
|-------|------|--------|
| unauth `POST .../sales-brain-shadow-smoke` SS-01 | **401** | **PASS** |
| dealer Firebase token SS-01 | **SKIPPED** | staging requires real Firebase token (not dev stub) |
| admin SS-01 with real Gemini | **SKIPPED** | requires Firebase admin login session — use Admin UI `/admin/shadow-smoke` |

### Admin SS-01 real Gemini (manual — required to confirm paid call)

| Field | Expected when admin clicks SS-01 |
|-------|----------------------------------|
| `success` | `true` |
| `readOnly` | `true` |
| `userVisibleOff` | `true` |
| `providerNetwork` | **`true`** |
| `data.shadowDebugResult.provider` | `gemini` |
| `data.userVisibleResponse` | legacy reference unchanged |
| raw PII / secret dump | none |

**Automated agent did not invoke paid Gemini** — no Firebase admin ID token available in CI harness.

### Public chat

| Check | Result |
|-------|--------|
| `/chat` `"เริ่มใหม่"` legacy exact | **SKIPPED** — browser/manual (v6.1F baseline still valid; no user-visible path change in v6.1H code) |
| AI/Gemini to public user | ❌ **none** by design |

---

## 7. Budget / Cost Observation

| Item | Status |
|------|--------|
| Budget caps on revision | daily `5`, monthly `50` |
| Paid Gemini call observed in automated smoke | ❌ **none** — admin SS-01 not called without Firebase admin token |
| Cost anomaly | ❌ **none observed** |

---

## 8. Rollback Plan

```bash
gcloud run services update nonga-staging \
  --project=nonga-ce93c --region=asia-southeast1 \
  --update-env-vars="NONGA_AI_ADMIN_SHADOW_REAL_PROVIDER_ENABLED=false,NONGA_AI_USER_VISIBLE_ENABLED=false,NONGA_AI_EMERGENCY_KILL_SWITCH=true"
```

| Item | Status |
|------|--------|
| Rollback needed | ❌ **not yet** — health/guards passed; no PII leak / no public AI observed |
| Traffic rollback to `00051-fbd` | available if needed |

---

## 9. Compliance (v6.1H)

| Item | Status |
|------|--------|
| `NONGA_AI_USER_VISIBLE_ENABLED=true` | ❌ not set — remains **false** |
| AI response to public user | ❌ not changed |
| Production | ❌ not touched |
| Firestore rules deploy | ❌ not done |
| Hosting deploy | ❌ not done |
| Payment / invoice / settlement writes | ❌ not done |
| Buyer lead / seller reveal / outcome | ❌ not done |
| Public signup | ❌ not enabled |
| Real stock import | ❌ still blocked |
| `gcloud secrets versions access` | ❌ not done |
| Raw prompt / PII / secret in logs | ❌ redaction enforced in code |

---

## 10. Next Step (Manual)

1. Login as admin/superadmin on `https://nonga-ce93c.web.app/admin/shadow-smoke`
2. Click **SS-01** — confirm `providerNetwork:true`, `provider:gemini`, legacy reference unchanged
3. Confirm `/chat` `"เริ่มใหม่"` still legacy exact
4. If fail → run rollback §8 immediately
