# v7.0I — Authenticated Dealer Smoke Execution Template / Operator Runbook

**สถานะ:** **PARTIAL — AUTHENTICATED SMOKE EXECUTION TEMPLATE ONLY** — execution template / operator runbook สำหรับ authenticated dealer smoke จริงในอนาคต บันทึกแบบ **non-PII / count-only / status-only เท่านั้น** โดยยังคง **ไม่ login ไม่ใช้ credential ไม่อ่าน session/cookie/token ไม่แตะ lead จริง ไม่เขียน Firestore ไม่ import Thor และไม่เปิด user-visible Gemini**; **ยังไม่มี authenticated smoke PASS**  
**วันที่:** 2026-06-18  
**Git baseline:** `66a234c5014920dca54b397cc8ac813a9245d098` (`66a234c`)  
**Branch:** `feature/chat-image-attachment-v1`  
**Prep base:** v7.0A (operator non-login dry-run execution record), v7.0B (operator non-login dry-run result closure record), v7.0C (authenticated dealer smoke safe session prep packet), v7.0D (owner approval alignment for authenticated smoke), v7.0E (owner decision intake for authenticated smoke), v7.0F (owner decision closure for authenticated smoke), v7.0G (owner decision packet sync to v6.9Q), v7.0H (operator safe session readiness checklist closure), v6.7C-1-F (authenticated smoke runbook), v6.7C-1-G (operator authenticated smoke result), v6.9M (owner gate), v6.9N (ops SOP + lead monitor skeleton), v6.9O (incident / no-go response playbook), v6.9P (readiness index / gate matrix), v6.9Q (owner decision packet / manual approval form), v6.9R (evidence pack index / audit trail map)  
**Run mode:** no commit until owner approve; no deploy; no runtime code changes; no agent login; no operator login in this slice; no credential/session/cookie/token read; no Firestore write; no Thor import; no user-visible Gemini

> Authenticated dealer smoke **execution template / operator runbook** เท่านั้น เป็นแม่แบบ (template) สำหรับการรัน smoke จริงในอนาคต **ไม่ใช่** การรัน smoke จริงในรอบนี้ **ไม่มี credentials, passwords, tokens, session cookies, API keys, Authorization headers, buyer phone numbers, emails, real names, license plates, VINs, หรือ raw lead contact ในเอกสารนี้** **Not production-ready. Not pilot-ready. Not deploy-ready. This is NOT an authenticated smoke execution. There is NO authenticated smoke PASS yet. This is NOT GO FOR THOR RUNTIME IMPORT. Thor Auto real runtime import remains BLOCKED. User-visible Gemini remains BLOCKED. Public signup remains BLOCKED. Production deploy remains BLOCKED.**

---

## 1. Executive summary

v7.0I สร้าง **execution template / operator runbook** สำหรับ authenticated dealer smoke จริงในอนาคต โดยรวบรวม preconditions จาก v7.0A..v7.0H, ขั้นตอน execution แบบ step-by-step, dealer portal path checklist, lead queue path checklist, count/status-only result format, abort/no-go conditions และ evidence capture template — ทั้งหมดเป็น **template / shell ที่เริ่มต้นแบบ PENDING** เพื่อให้ operator นำไปใช้ในรอบ smoke execution slice ภายหลัง

slice นี้:

- รวบรวม **preconditions** ที่ปิดแล้วจาก v7.0A..v7.0H
- กำหนด **operator safe session boundary** + **credential/session prohibition** + **non-PII evidence rules**
- ให้ **authenticated smoke execution steps** (template) + **dealer portal path checklist** + **lead queue path checklist**
- กำหนด **count/status-only result format** + **abort/no-go conditions** + **evidence capture template**
- ให้ **result table AS-01..AS-09 = PENDING ทั้งหมด** (ยังไม่รัน)
- ระบุ **what this template allows** และ **what this template does not allow**
- ระบุ **missing items** ก่อน actual smoke execution, ก่อน Thor runtime import และก่อน Gemini user-visible pilot

slice นี้ **เป็นเอกสาร + static validation + npm script เท่านั้น** ไม่แตะ runtime / deploy / env / Firestore / Thor / Gemini และ **ไม่ใช่** การรัน authenticated smoke การมี template **ไม่ทำให้เกิด authenticated smoke PASS** และ **ไม่** unblock Thor/Gemini/public signup/production

---

## 2. Baseline

| รายการ | ค่า |
|--------|-----|
| Branch | `feature/chat-image-attachment-v1` |
| Origin HEAD ก่อนเริ่ม | `66a234c` |
| Git baseline | `66a234c5014920dca54b397cc8ac813a9245d098` |
| Working tree ก่อนเริ่ม | clean |
| Prep base | v7.0A, v7.0B, v7.0C, v7.0D, v7.0E, v7.0F, v7.0G, v7.0H, v6.7C-1-F, v6.7C-1-G, v6.9M, v6.9N, v6.9O, v6.9P, v6.9Q, v6.9R |
| Run mode | no commit until approve; no deploy; no runtime change; no login; no credential read; no Firestore; no Thor import; no user-visible Gemini |

**กฎ baseline:** เริ่มจาก origin HEAD `66a234c` บน working tree ที่ clean เท่านั้น และ scope จำกัดที่ docs + static validation script + package.json npm script (1 script)

---

## 3. Current gate status

| Gate / dependency | Phase | Verdict ปัจจุบัน | ความหมายต่อ v7.0I |
|-------------------|-------|------------------|--------------------|
| Operator non-login dry-run execution record | v7.0A | **PARTIAL — NON-LOGIN DRY-RUN EXECUTION ONLY** | execution record อ้างอิง |
| Operator non-login dry-run result closure | v7.0B | **PARTIAL — NON-LOGIN DRY-RUN RESULT CLOSED (UNDERSTANDING PASS ONLY)** | dry-run ปิดผลในระดับเข้าใจ |
| Authenticated dealer smoke safe session prep | v7.0C | **PARTIAL — AUTHENTICATED SMOKE SAFE SESSION PREP ONLY** | กรอบความปลอดภัยพร้อม |
| Owner approval alignment for authenticated smoke | v7.0D | **PARTIAL — OWNER APPROVAL ALIGNMENT ONLY** | alignment เงื่อนไขพร้อม |
| Owner decision intake for authenticated smoke | v7.0E | **PARTIAL — OWNER DECISION INTAKE ONLY** | ที่รับ decision |
| Owner decision closure for authenticated smoke | v7.0F | **PARTIAL — OWNER DECISION CLOSED (LIMITED AUTHENTICATED SMOKE PREP APPROVED)** | decision ปิดแล้ว (YES limited prep) |
| Owner decision packet sync to v6.9Q | v7.0G | **PARTIAL — OWNER DECISION SYNC TO v6.9Q ONLY** | decision sync เข้า v6.9Q path |
| Operator safe session readiness checklist closure | v7.0H | **PARTIAL — OPERATOR SAFE SESSION READINESS CLOSED ONLY** | readiness ปิดแล้ว (RC-01..RC-06 PASS) |
| Authenticated dealer portal smoke runbook | v6.7C-1-F | **PARTIAL — OPERATOR ACTION REQUIRED** | smoke จริงยังต้องให้ operator รัน |
| Operator authenticated smoke result | v6.7C-1-G | **PARTIAL — AWAITING OPERATOR RESULT** | ยังรอผล authenticated smoke จริง |
| Thor owner gate checklist | v6.9M | **PARTIAL — PREPARE ONLY** | owner ยังไม่ approve Thor — Thor import ยัง BLOCKED |
| Ops SOP + lead monitor skeleton | v6.9N | **PARTIAL — SOP SKELETON ONLY** | skeleton พร้อม รอ accept |
| Incident / no-go response playbook | v6.9O | **PARTIAL — INCIDENT PLAYBOOK ONLY** | playbook พร้อม รอ acknowledge |
| Readiness index / gate matrix | v6.9P | **PARTIAL — GATE MATRIX ONLY** | matrix พร้อมเป็น dashboard |
| Owner decision packet / manual approval form | v6.9Q | **PARTIAL — OWNER DECISION PACKET ONLY** | owner decision ทางการต้องบันทึกที่นี่ |
| Evidence pack index / audit trail map | v6.9R | **PARTIAL — EVIDENCE INDEX ONLY** | ดัชนีหลักฐานพร้อม |
| Thor Auto real runtime import | — | **BLOCKED** | ห้าม import จนกว่าจะครบ gate |
| Parent Thor aggregated inbox | — | **DEFER** | เลื่อนออกไป ยังไม่พิจารณา |
| User-visible AI / Gemini | — | **BLOCKED** | ยังไม่เปิดให้ผู้ใช้เห็น |
| Public signup | — | **BLOCKED** | ยังไม่เปิดสมัครสาธารณะ |
| Production deploy | — | **BLOCKED** | ยังไม่ deploy production |
| Controlled production pilot | v6.9 | **NOT READY** | ยังไม่ pilot-ready |

**สรุป gate:** การมี execution template **ไม่ได้** เปลี่ยนสถานะ gate อื่น ทุก gate ที่เหลือยัง **ไม่ผ่าน** และ Thor/Gemini/public signup/production ยัง **BLOCKED**

---

## 4. Why this is execution template only

| เหตุผล | คำอธิบาย |
|--------|----------|
| ยังไม่มี owner decision บันทึกใน v6.9Q | owner decision ทางการ (M-2) ยัง PENDING |
| ยังไม่มี operator safe session จริง | safe session (Option A/B per v6.7C-1-F §6) ยังไม่พร้อม |
| ป้องกันการรัน smoke ก่อนเงื่อนไขครบ | template ทำให้ขั้นตอนชัดเจน แต่ยังไม่ trigger execution |
| audit trail | บันทึก template แบบ status-only / PENDING เป็นหลักฐานก่อน execution |
| แยก template ออกจาก execution | template PASS **ไม่ใช่** smoke PASS — execution ต้องเกิดในรอบ slice ภายหลัง |

**สรุป:** v7.0I เป็น **execution template / operator runbook เท่านั้น** — ขั้นตอนทั้งหมดเป็นแม่แบบที่เริ่ม PENDING และ **ยังไม่ถูกรันจริงใน slice นี้**

---

## 5. Preconditions from v7.0A..v7.0H

> preconditions ที่ปิด/อ้างอิงแล้ว ก่อนนำ template ไปใช้รัน smoke จริง

| # | Precondition | ที่มา | สถานะ |
|---|--------------|-------|--------|
| P-1 | operator non-login dry-run execution record | v7.0A | **DONE** |
| P-2 | operator non-login dry-run result closure (understanding PASS) | v7.0B | **DONE** |
| P-3 | authenticated smoke safe session prep packet | v7.0C | **DONE** |
| P-4 | owner approval alignment | v7.0D | **DONE** |
| P-5 | owner decision intake | v7.0E | **DONE** |
| P-6 | owner decision closure (YES limited prep) | v7.0F | **DONE** |
| P-7 | owner decision sync to v6.9Q path | v7.0G | **DONE** |
| P-8 | operator safe session readiness checklist (RC-01..RC-06 PASS) | v7.0H | **DONE** |
| P-9 | owner decision บันทึกจริงใน v6.9Q form | v6.9Q | **PENDING** |
| P-10 | operator safe session available (Option A/B per v6.7C-1-F §6) | v6.7C-1-F | **MISSING** |

**กฎ:** P-1..P-8 ปิดแล้ว แต่ P-9, P-10 ยังไม่ครบ → template นี้ยังนำไปรัน smoke จริง **ไม่ได้** จนกว่าจะครบ และต้องมี explicit owner decision ใน v6.9Q ก่อน

---

## 6. Operator safe session boundary

| ขอบเขต | คำอธิบาย |
|--------|----------|
| operator login เองเท่านั้น | operator login ด้วยบัญชี dealer ของตนเองในรอบ execution เท่านั้น |
| agent ไม่ login | agent ไม่ login และไม่อ่าน credential ทุกกรณี |
| dealer scope เท่านั้น | session จำกัดเฉพาะ dealer ของตนเอง — ไม่ cross-dealer |
| logout หลังเสร็จ | logout / ปิด session หลัง smoke เสร็จทุกครั้ง |
| ไม่มี login ใน slice นี้ | login จริง **ยังไม่เกิด** ใน v7.0I — เป็น template เท่านั้น |

**กฎ:** safe session คือ session ที่ operator ควบคุมเองทั้งหมด — agent **ไม่** login, **ไม่** อ่าน credential/session/cookie/token ในทุกกรณี

---

## 7. Credential/session prohibition

| ข้อห้าม | สถานะ |
|---------|--------|
| ห้ามแชร์ password / token / session / cookie / credential | **PROHIBITED** |
| ห้ามส่ง credential/session/cookie/token กลับมาที่ repo/chat | **PROHIBITED** |
| ห้าม log Authorization header / API key | **PROHIBITED** |
| ห้ามบันทึก credential/session/cookie/token/password ลง evidence | **PROHIBITED** |

**กฎ:** credential/session/cookie/token/password **ห้ามบันทึกหรือส่งกลับทุกกรณี** — ถ้าหลุดเข้ามาในขั้นตอนใด ให้ถือเป็น abort condition (ดู §13)

---

## 8. Non-PII evidence rules

| กฎ | คำอธิบาย |
|----|----------|
| non-PII เท่านั้น | evidence ต้องเป็น non-PII / count-only / status-only เท่านั้น |
| count-only | บันทึกเป็น "จำนวน" หรือ label เท่านั้น — ไม่บันทึกค่าจริง |
| status-only | บันทึกเป็นสถานะ (accessible / not accessible / PASS / FAIL) เท่านั้น |
| ไม่บันทึก raw data | ไม่บันทึก raw data / un-redacted screenshot / raw lead row |
| ไม่มี PII | ไม่ใส่ phone / email / real name / license plate / VIN / raw contact |
| บันทึกลง v6.7C-1-G | ผลจริง (ภายหลัง) บันทึกลง v6.7C-1-G แบบ non-PII เท่านั้น |

**กฎ:** evidence ทุกชิ้นต้องเป็น status / count / label เท่านั้น — ถ้ามี lead/contact/plate/VIN/raw buyer data โผล่ ต้อง abort และ **ไม่บันทึกลง repo/chat**

---

## 9. Authenticated smoke execution steps (template)

> ขั้นตอน template สำหรับรอบ execution slice ภายหลัง — **ยังไม่รันใน v7.0I** ทุกขั้นตอนเริ่มต้นแบบ PENDING

| Step | Action (template) | Output (count/status-only) | สถานะ |
|------|-------------------|----------------------------|--------|
| ES-1 | ยืนยัน owner decision บันทึกใน v6.9Q + safe session พร้อม | precondition PASS/FAIL | **PENDING** |
| ES-2 | operator เตรียม safe session (Option A/B per v6.7C-1-F §6) | session prepared yes/no | **PENDING** |
| ES-3 | operator login ด้วยบัญชี dealer ของตนเอง (operator only) | login by operator yes/no | **PENDING** |
| ES-4 | เปิด dealer portal — ตรวจ accessible | portal accessible yes/no | **PENDING** |
| ES-5 | เปิด lead queue — ตรวจ accessible + นับจำนวน (count-only) | queue accessible + count | **PENDING** |
| ES-6 | ตรวจ no cross-dealer lead visibility | cross-dealer leak yes/no | **PENDING** |
| ES-7 | ตรวจ evidence ไม่มี PII / credential | non-PII evidence yes/no | **PENDING** |
| ES-8 | ตรวจ abort conditions ไม่ถูก trigger | abort triggered yes/no | **PENDING** |
| ES-9 | logout / ปิด session + บันทึกผล non-PII ลง v6.7C-1-G | result recorded yes/no | **PENDING** |

**กฎ:** ทุก step เป็น template — output ต้องเป็น count/status-only เท่านั้น และ slice นี้ **ไม่** รัน step ใด ๆ จริง

---

## 10. Dealer portal path checklist

> path checklist แบบ status-only — ใช้ตรวจ dealer portal ในรอบ execution ภายหลัง

| ID | Path check | Expected (status-only) | สถานะ |
|----|------------|------------------------|--------|
| DP-1 | dealer portal เปิดได้หลัง operator login | accessible | **PENDING** |
| DP-2 | dealer dashboard แสดงเฉพาะ dealer ของตนเอง | own-dealer scope only | **PENDING** |
| DP-3 | inventory / listing view เข้าถึงได้ | accessible | **PENDING** |
| DP-4 | ไม่มีข้อมูล dealer อื่นปรากฏ | no cross-dealer data | **PENDING** |
| DP-5 | ไม่มี credential/session/token แสดงในหน้า evidence | no credential exposed | **PENDING** |

**กฎ:** บันทึกเป็น status-only เท่านั้น — ไม่ capture raw screenshot ที่มี PII / credential

---

## 11. Lead queue path checklist

> path checklist แบบ count/status-only — ใช้ตรวจ lead queue ในรอบ execution ภายหลัง

| ID | Path check | Expected (count/status-only) | สถานะ |
|----|------------|------------------------------|--------|
| LQ-1 | lead queue เข้าถึงได้หลัง operator login | accessible | **PENDING** |
| LQ-2 | จำนวน lead ในคิว (count-only) | count เท่านั้น | **PENDING** |
| LQ-3 | lead แสดงเฉพาะ dealer ของตนเอง | own-dealer scope only | **PENDING** |
| LQ-4 | ไม่มี cross-dealer lead ปรากฏ | no cross-dealer lead | **PENDING** |
| LQ-5 | masking ของ buyer contact ทำงานถูกต้อง | masked status | **PENDING** |
| LQ-6 | ไม่บันทึก raw buyer contact / phone / email ลง evidence | non-PII only | **PENDING** |

**กฎ:** lead evidence เป็น count/status เท่านั้น — ถ้าพบ raw buyer contact / phone / email / name → abort (ดู §13)

---

## 12. Count/status-only result format

> รูปแบบผลที่อนุญาต — เพื่อกัน PII ในทุก evidence

| ประเภทผล | รูปแบบที่อนุญาต | ตัวอย่างรูปแบบ (ไม่ใช่ข้อมูลจริง) |
|----------|----------------|-------------------------------------|
| accessibility | yes / no | `portal accessible: yes` |
| count | จำนวนเต็มเท่านั้น | `lead queue count: <integer>` |
| status | PASS / FAIL / PENDING | `cross-dealer leak: no` |
| masking | masked / unmasked | `buyer contact: masked` |
| abort | triggered / not triggered | `abort: not triggered` |

**กฎ:** **ห้าม** บันทึกค่า raw (เบอร์โทร, อีเมล, ชื่อจริง, plate, VIN, token) — ใช้ count/status/label placeholder เท่านั้น เช่น `<integer>` หรือ `masked`

---

## 13. Abort/no-go conditions

| ID | Condition | Action | สถานะ |
|----|-----------|--------|--------|
| AB-1 | พบ buyer PII / raw contact / phone / email / name | ABORT + escalate (SEV-0) + ไม่บันทึก | **DEFINED** |
| AB-2 | พบ license plate / VIN | ABORT + escalate (SEV-0) + ไม่บันทึก | **DEFINED** |
| AB-3 | พบ credential / session / cookie / token หลุด | ABORT + escalate (SEV-0) + ไม่บันทึก | **DEFINED** |
| AB-4 | พบ cross-dealer lead leak | ABORT + escalate (SEV-0) | **DEFINED** |
| AB-5 | masking inconsistent / SOP violation | หยุด action + escalate (SEV-1) | **DEFINED** |
| AB-6 | owner approval ยังไม่บันทึกใน v6.9Q | NO-GO — ห้ามเริ่ม smoke | **DEFINED** |
| AB-7 | safe session ไม่พร้อม (Option A/B ไม่ครบ) | NO-GO — ห้ามเริ่ม smoke | **DEFINED** |

**กฎ:** abort conditions ใด ๆ ที่เกิด → หยุดทันที, **ไม่บันทึกข้อมูลที่ละเมิดลง repo/chat**, และรายงานตาม v6.9O incident playbook

---

## 14. Evidence capture template

> template สำหรับบันทึก evidence แบบ non-PII / count-only / status-only เท่านั้น (ยังไม่กรอกค่าจริงใน v7.0I)

| Evidence ID | สิ่งที่บันทึก | รูปแบบ | ค่า (รอ execution) |
|-------------|----------------|--------|---------------------|
| EV-1 | dealer portal accessible | status (yes/no) | **PENDING** |
| EV-2 | lead queue accessible | status (yes/no) | **PENDING** |
| EV-3 | lead queue count | count (integer) | **PENDING** |
| EV-4 | own-dealer scope only | status (yes/no) | **PENDING** |
| EV-5 | cross-dealer leak observed | status (yes/no) | **PENDING** |
| EV-6 | buyer contact masking | status (masked/unmasked) | **PENDING** |
| EV-7 | abort triggered | status (triggered/not) | **PENDING** |
| EV-8 | non-PII evidence confirmed | status (yes/no) | **PENDING** |

**กฎ:** ทุก field กรอกได้เฉพาะ count/status/label — **ห้าม** ใส่ raw PII / credential — ถ้า field ใดต้องใส่ค่าที่เป็น PII ให้ abort แทน

---

## 15. Result table template (AS-01..AS-09)

> result table เริ่มต้น **PENDING ทั้งหมด** — จะกรอกผลจริงในรอบ execution slice ภายหลัง (บันทึกลง v6.7C-1-G แบบ non-PII)

| ID | Result item | Result |
|----|-------------|--------|
| AS-01 | safe session prepared | **PENDING** |
| AS-02 | operator login performed by operator only | **PENDING** |
| AS-03 | no credential/session/token shared | **PENDING** |
| AS-04 | dealer portal accessible | **PENDING** |
| AS-05 | lead queue accessible count/status-only | **PENDING** |
| AS-06 | no cross-dealer lead visibility observed | **PENDING** |
| AS-07 | no PII captured in evidence | **PENDING** |
| AS-08 | abort conditions not triggered | **PENDING** |
| AS-09 | result ready for v6.7C-1-G | **PENDING** |

**สรุปตาราง:** AS-01..AS-09 = **PENDING ทั้งหมด** — v7.0I **ยังไม่ใช่** authenticated smoke execution และ **ยังไม่มี** authenticated smoke PASS

---

## 16. Relationship to v6.7C-1-G

| รายการ | สถานะ |
|--------|--------|
| v6.7C-1-G authenticated smoke result shell | **PARTIAL — AWAITING OPERATOR RESULT** |
| ผล authenticated smoke จริง | ยังไม่มี |
| ผลต่อ template | template นี้กำหนดรูปแบบผลที่จะบันทึกลง v6.7C-1-G แบบ non-PII |

**กฎ:** ผล authenticated smoke จริง (AS-01..AS-09) ต้องบันทึกลง v6.7C-1-G แบบ non-PII — v7.0I **ไม่** มี smoke PASS และ **ไม่** เขียนผลจริงลง v6.7C-1-G ใน slice นี้

---

## 17. What this template allows

| สิ่งที่ template อนุญาต | ขอบเขต |
|--------------------------|--------|
| ใช้เป็น operator runbook อ้างอิงขั้นตอน smoke execution ในอนาคต | reference only |
| กำหนดรูปแบบ count/status-only result + evidence | format only |
| กำหนด abort/no-go conditions ให้ชัดเจนก่อน execution | safety only |
| เตรียม result table AS-01..AS-09 (PENDING) ให้กรอกภายหลัง | template only |

**กฎ:** "allows" คือการใช้เป็นแม่แบบ/อ้างอิงเท่านั้น — การ login จริง + การรัน smoke จริงต้องเกิดในรอบ smoke execution slice ภายหลัง และต้องมี owner decision บันทึกใน v6.9Q ก่อน

---

## 18. What this template does not allow

| สิ่งที่ template ไม่อนุญาต | สถานะ |
|-----------------------------|--------|
| รัน authenticated smoke จริงใน slice นี้ | **NOT ALLOWED** |
| login จริง (operator หรือ agent) ใน slice นี้ | **NOT ALLOWED** |
| อ่าน/บันทึก credential/session/cookie/token/password | **NOT ALLOWED** |
| ใส่ PII / raw contact / phone / email / plate / VIN | **NOT ALLOWED** |
| runtime code change / deploy / env / Firestore write | **NOT ALLOWED** |
| Thor runtime import | **BLOCKED** |
| เปิด user-visible Gemini | **BLOCKED** |
| เปิด public signup | **BLOCKED** |
| production deploy | **BLOCKED** |
| claim pilot-ready / production-ready / GO | **NOT ALLOWED** |

**กฎ:** template นี้ **ไม่** unblock รายการข้างต้น — ทั้งหมดต้องมี explicit separate approval ภายหลังเท่านั้น

---

## 19. Missing items before actual smoke execution

| # | Missing item | สถานะ |
|---|--------------|--------|
| M-1 | owner decision = YES (limited prep) | **DONE (v7.0F)** |
| M-2 | owner decision บันทึกจริงใน v6.9Q | **PENDING** |
| M-3 | operator safe session available (Option A/B per v6.7C-1-F §6) | **MISSING** |
| M-4 | operator readiness checklist RC-01..RC-06 | **DONE (v7.0H — PASS)** |
| M-5 | abort / no-go conditions defined (this template / v7.0C / v6.9O) | **DONE (this doc)** |
| M-6 | execution template / operator runbook | **DONE (this doc)** |

> M-2, M-3 ยังไม่ครบ → authenticated smoke execution ยังไม่เกิด และ **ยังไม่มี authenticated smoke PASS**

---

## 20. Missing items before Thor runtime import

| # | Missing item | สถานะ |
|---|--------------|--------|
| T-1 | authenticated dealer smoke result PASS จาก v6.7C-1-G | **MISSING** |
| T-2 | owner approval (v6.9M / v6.9Q) | **REQUIRED — not given** |
| T-3 | explicit separate approval สำหรับ Thor runtime import | **REQUIRED — not given** |

> ตราบใดที่ T-1..T-3 ยังไม่ครบ → **Thor runtime import remains BLOCKED**

---

## 21. Missing items before Gemini user-visible pilot

| # | Missing item | สถานะ |
|---|--------------|--------|
| G-1 | authenticated dealer smoke result PASS จาก v6.7C-1-G | **MISSING** |
| G-2 | owner approval สำหรับ user-visible AI (v6.9M / v6.9Q) | **REQUIRED — not given** |
| G-3 | Gemini safety / abuse / cost guardrails review | **MISSING** |
| G-4 | controlled pilot scope + rollback plan | **MISSING** |
| G-5 | explicit separate approval เปิด user-visible Gemini | **REQUIRED — not given** |

> ตราบใดที่ G-1..G-5 ยังไม่ครบ → **user-visible Gemini remains BLOCKED**

---

## 22. Safety confirmations

| Item | Result |
|------|--------|
| Runtime code changes | **NO** |
| Commit during prep | **NO** |
| Deploy performed | **NO** |
| Cloud Run | **Not touched** |
| Production | **Not touched** |
| Env / secrets / deploy config | **Unchanged** |
| Firestore write from agent | **Not touched** |
| Real lead created/edited in Firestore | **NO** |
| Auth / session / routing changes | **Not changed** |
| Operator login in this slice | **NO** |
| Agent login / credential read | **NO** |
| Credential / session / cookie / token read | **NO** |
| Secret / token / session / cookie logged | **NO** |
| Buyer/seller PII / VIN / plate / raw contact in doc | **NO** |
| Thor data imported | **NO** |
| **Thor Auto runtime import unblocked** | **NO** — remains blocked |
| Parent Thor aggregated inbox | **DEFER** |
| Revenue pilot opened | **NO** |
| Owner GO for real revenue pilot | **NO** |
| User-visible AI / Gemini enabled | **NO** — remains BLOCKED |
| Public signup opened | **NO** — remains BLOCKED |
| Production deploy readiness | **BLOCKED** |
| Authenticated smoke executed | **NO** |
| Authenticated smoke PASS claimed | **NO** — no smoke PASS yet |
| Template scope | **TEMPLATE — execution template / operator runbook only** |
| Result table AS-01..AS-09 | **PENDING (all)** |
| Claims production-ready | **NO** |
| Claims pilot-ready | **NO** |
| Claims GO for Thor runtime import | **NO** |

---

## 23. Relation to v7.0A, v7.0B, v7.0C, v7.0F, v7.0G, v7.0H, v6.7C-1-G

| Phase | บทบาท | สถานะ |
|-------|--------|--------|
| v7.0A | operator non-login dry-run execution record | PARTIAL — NON-LOGIN DRY-RUN EXECUTION ONLY |
| v7.0B | operator non-login dry-run result closure record | PARTIAL — NON-LOGIN DRY-RUN RESULT CLOSED (UNDERSTANDING PASS ONLY) |
| v7.0C | authenticated dealer smoke safe session prep packet | PARTIAL — AUTHENTICATED SMOKE SAFE SESSION PREP ONLY |
| v7.0F | owner decision closure for authenticated smoke | PARTIAL — OWNER DECISION CLOSED (LIMITED AUTHENTICATED SMOKE PREP APPROVED) |
| v7.0G | owner decision packet sync to v6.9Q | PARTIAL — OWNER DECISION SYNC TO v6.9Q ONLY |
| v7.0H | operator safe session readiness checklist closure | PARTIAL — OPERATOR SAFE SESSION READINESS CLOSED ONLY |
| v6.7C-1-G | operator authenticated smoke result shell | PARTIAL — AWAITING OPERATOR RESULT |
| **v7.0I** | **authenticated dealer smoke execution template / operator runbook (เอกสารนี้)** | **PARTIAL — AUTHENTICATED SMOKE EXECUTION TEMPLATE ONLY** |

v7.0I เป็น **execution template / operator runbook** ที่ต่อจาก v7.0H โดยอ้างอิงทุก gate ข้างต้น และ **ไม่เปลี่ยนสถานะ gate อื่น** owner approval ทางการยังต้องบันทึกใน v6.9Q และผล smoke จริงยังต้องบันทึกใน v6.7C-1-G

---

## 24. This execution record (pending commit)

| File | Purpose |
|------|---------|
| `docs/v7.0I-authenticated-dealer-smoke-execution-template-operator-runbook.md` | This document (authenticated dealer smoke execution template / operator runbook) |
| `scripts/test-v70i-authenticated-dealer-smoke-execution-template-operator-runbook.mts` | Static validation |
| `package.json` | `test:v70i-authenticated-dealer-smoke-execution-template-operator-runbook` |

---

## 25. Next slices

1. **Owner** บันทึก decision (YES limited prep) จริงใน v6.9Q form — M-2
2. จัดเตรียม operator safe session (Option A/B per v6.7C-1-F §6) — M-3
3. **Operator** รัน authenticated dealer portal smoke จริงตาม template นี้ + v6.7C-1-F §6 (ยังไม่ใช่ slice นี้) — กรอก AS-01..AS-09
4. บันทึกผล non-PII ลง v6.7C-1-G (EP-01..07 / NC-01..06 / AS-01..AS-09)
5. อัปเดต readiness matrix (v6.9P) + evidence index (v6.9R) หลังได้ผลจริง
6. Thor runtime import + user-visible Gemini พิจารณา **ภายหลัง** เท่านั้น และยัง **BLOCKED** จนกว่าจะครบเงื่อนไข + explicit separate approval (T-3 / G-5)

---

## 26. Final verdict

**PARTIAL — AUTHENTICATED SMOKE EXECUTION TEMPLATE ONLY**

- execution template / operator runbook สำหรับ authenticated dealer smoke พร้อมแล้ว: execution steps + dealer portal checklist + lead queue checklist + count/status-only result format + abort/no-go conditions + evidence capture template + result table AS-01..AS-09 = **PENDING ทั้งหมด**
- v7.0I **ไม่ใช่** authenticated smoke execution และ **ยังไม่มี** authenticated smoke PASS
- template PASS **ยังไม่ใช่** pilot-ready / production-ready / GO
- login จริง **ยังไม่เกิด** ใน slice นี้
- credential/session/cookie/token/password **ห้ามบันทึกหรือส่งกลับทุกกรณี**
- evidence ต้องเป็น non-PII / count-only / status-only
- ถ้าพบ lead/contact/plate/VIN/raw buyer data → **abort** และไม่บันทึกลง repo/chat
- **ไม่ใช่** GO FOR THOR RUNTIME IMPORT
- **ไม่ใช่** production-ready, **ไม่ใช่** pilot-ready
- **ไม่ได้** login, **ไม่ได้** แก้ runtime, **ไม่ได้** deploy, **ไม่ได้** เปิด revenue pilot
- Thor runtime import ยัง **BLOCKED**
- parent Thor aggregated inbox ยัง **DEFER**
- user-visible Gemini / public signup ยัง **BLOCKED**
- production deploy ยัง **BLOCKED**
- รอ operator authenticated smoke result จาก v6.7C-1-G
- รอ owner approval ทางการบันทึกใน v6.9M / v6.9Q

---

*End of v7.0I authenticated dealer smoke execution template / operator runbook (authenticated smoke execution template only).*
