# v13.15K-A Secure Owner Auth Path Setup Plan (No Gemini Execution / No Secret Exposure)

## 1) Execution type

- auth path setup plan only: yes
- no Gemini execution: yes
- no provider network call: yes
- no one-run consumption: yes
- no runtime mutation/deploy/production/public activation: yes
- no buyer-facing release: yes
- no real lead/PII/phone/plate/VIN: yes
- no secret/API key/token exposure: yes
- no automatic retry/no second run: yes

## 2) Baseline local/origin

- branch: `feature/chat-image-attachment-v1`
- local `HEAD`: `6c3c3a5d7e3ce4635f79a7b575c5a4f0ce51a180`
- `origin/feature/chat-image-attachment-v1`: `6c3c3a5d7e3ce4635f79a7b575c5a4f0ce51a180`
- local = origin: yes

## 3) Auth path options found

### Option A (recommended): real Firebase owner login/session

- route `/api/ai/chat-user-visible-orchestrate` uses `getServerAuthContext(req)` and expects Bearer Firebase ID token path
- best fit for staging owner-only execution boundary

### Option B: Firebase ID token from owner session (local-only temporary)

- acceptable as operational form of Option A
- token must stay temporary in local operator session only

### Option C: `NONGA_DEV_FIREBASE_TOKEN_MAP` dev-mock

- optional for local/non-production mock flows
- not preferred for production-like staging owner retry validation

### Option D: admin helper tokens (`NONGA_ADMIN_API_TOKEN` / `VITE_NONGA_ADMIN_API_TOKEN`)

- available as helper/fallback tooling path in some utilities
- not the primary contract for this protected orchestrate route

## 4) Recommended secure owner auth path

Use **Option A/B**:

1. owner signs in through secure Firebase owner/admin session
2. operator uses authenticated session locally without revealing token value
3. verify boundary with authenticated request only after secure token path is ready

## 5) Owner action required

1. complete secure owner/admin Firebase login for staging session
2. confirm secure channel availability for authenticated operator probe
3. confirm runtime key presence (`GEMINI_API_KEY`) through secure infra console as presence-only

## 6) Operator action required

1. do not request or print raw token in chat/log
2. keep token in temporary local session only
3. run boundary checks:
   - `GET /api/health` -> `200`
   - unauth `POST /api/ai/chat-user-visible-orchestrate` -> `401`
   - authenticated probe only after secure auth path is ready
4. record only masked/presence status

## 7) Secret handling rules

- never paste raw token/API key/secret in chat
- never echo secret values to console
- never commit secrets
- never store secrets in docs/tests/repo files
- allowed status values only:
  - `present`
  - `missing`
  - `not required for this path`
  - `required but inaccessible`

## 8) Authenticated boundary check plan (without token echo)

1. run unauth boundary check first and confirm `401`
2. prepare authenticated session in local environment without printing token
3. execute authenticated probe and capture only:
   - HTTP status
   - high-level gate phase result
4. redact all credential-bearing headers from any logs/evidence

## 9) Staging runtime `GEMINI_API_KEY` confirmation plan (presence-only)

1. confirm from secure runtime/secret management surface
2. capture only presence state (`present/missing/inaccessible`)
3. do not copy value/prefix/suffix into logs/chat/docs

## 10) One-run consumed or not

- consumed: no
- run count remains: `0/1`

## 11) Gemini/provider call or not

- Gemini call: no
- provider network call: no

## 12) Final recommendation

READY FOR OWNER TO COMPLETE SECURE AUTH SETUP BEFORE ONE-RUN RETRY
