# v14.4E — Firebase Dispatch 401 Auth Diagnosis

Date: 2026-07-05  
Branch: `feature/chat-image-attachment-v1`

## Scope (This Round)

- Execution type: docs/tests/fixtures/static diagnosis + safe auth/header/token-flow inspection only.
- Agent execution boundary:
  - one-run execution by agent: no
  - retry by agent: no
  - second run by agent: no
  - provider/Gemini/runtime call by agent: no
  - provider network call by agent: no
  - live endpoint call by agent: no
  - deploy/runtime config mutation/production/public release by agent: no

## Interpreting the Observed `401` Result

Owner one-run `v14.3AJ` (already consumed) reported:

- `dispatchHttpStatus=401`
- `dispatchResponseBodyChars=55`
- `HOLD — dispatch response http-not-ok (401)`

From static server code, two 401 payloads are exactly 55 chars:

- `{"success":false,"message":"Missing Firebase ID token"}`
- `{"success":false,"message":"Invalid Firebase ID token"}`

This confirms provider/Gemini path was not reached in this run.

## Exact Source of HTTP 401 Handling (Wrapper)

File: `scripts/owner-local-user-visible-one-run-gate-v143ac.mts`

- `dispatchHttpStatus` is read from `response.status`.
- non-2xx HOLD is triggered by:
  - `if (!response.ok) { hold(\`dispatch response http-not-ok (${response.status})\`); }`
- response body is always read via `await response.text()` before status gate.
- patch in this round adds masked diagnostics before HOLD:
  - `dispatchResponseErrorCode=...`
  - `dispatchResponseErrorMessageClass=...`
  - `dispatchResponseBodyMaskedPreview=...`

No token value is logged. Body preview is redacted and truncated.

## Authorization Header Dispatch Path (Static)

Confirmed from wrapper code:

- token source:
  - `const tokenRaw = String(process.env.NONGA_OWNER_FIREBASE_ID_TOKEN ?? "")`
- token shape gate (fail-closed):
  - missing -> HOLD
  - invalid shape -> HOLD
  - `Bearer ...` string in env -> HOLD
- dispatch headers are assembled as:
  - `Content-Type: application/json`
  - `Authorization: Bearer <firebaseIdToken>`
- request body changes (prompt/run-id namespace) do not mutate header assembly path.

No branch was found that intentionally drops `Authorization` header after entering dispatch path.

## Firebase Token Acquisition / Freshness (Static)

### Confirmed from code

- wrapper reads token from same-CMD env only (`NONGA_OWNER_FIREBASE_ID_TOKEN`).
- wrapper does not fetch/refresh/reissue token itself.
- wrapper cannot verify token freshness, issuer, audience, or project binding locally.
- wrapper blocks `NONGA_ADMIN_API_TOKEN` usage for this route.

### Plausible but not confirmed from code

- stale/expired Firebase ID token.
- token minted from wrong Firebase project/audience/issuer.
- server runtime auth state changed between previous run and `v14.3AJ`.

### Ruled out by code

- header omission by prompt wording change alone.
- header omission by run-id namespace/lock patch alone.
- admin-token fallback silently passing for this route.

## Server Auth Middleware / Route Requirement (Static)

Files:

- `src/server/serverAuthContext.ts`
- `src/services/ai/salesBrainServerUserVisibleOrchestrationBridge.ts`
- `src/server/security/aiEndpointGuard.ts`

Findings:

- route `/api/ai/chat-user-visible-orchestrate` calls `getServerAuthContext(req)` directly.
- missing bearer token => `ServerAuthError(401, "Missing Firebase ID token")`.
- invalid/expired/unverifiable token => `ServerAuthError(401, "Invalid Firebase ID token")`.
- route catch returns `{ success:false, message: err.message }` with 401.
- allowlist mismatch is not expected to produce HTTP 401:
  - allowlist diagnostics (`allowlistMatch`) are carried in success payload diagnostics path.
- `aiEndpointGuard` returns only 403/429 classes (not 401), so observed 401 points to auth-context rejection path.

## Static Diff: `v14.3AI` vs `v14.3AJ` Auth Impact

Reviewed wrapper and related v14.4 commits.

Changes between those rounds affecting wrapper:

- dispatch approval phrase moved to `v14.3AJ` namespace.
- lock path resolved from parsed run-id (`v143aj`) to avoid namespace collision.
- synthetic prompt updated to follow-up-safe Thai wording.

No diff found that changes:

- token source env key
- `Authorization` header construction
- `Content-Type`
- fetch target path/origin
- server auth middleware behavior

Conclusion: recent patch set is unlikely to be direct root cause of missing/malformed auth header.

## One-Run Consumption State

Local lock marker confirms `v14.3AJ` consumed:

- `.nonga-owner-local-one-run-v143aj-user-visible-dispatch.lock.json`
- `{ "consumed": true, "reason": "user_visible_firebase_dispatch_entry_started", ... }`

So `v14.3AJ` must not be reused; next owner retest requires fresh run-id (example: `v14.3AK`).
