# v13.1 Admin Token Provisioning Approval Packet — No Secret Value / No Runtime Change Yet

## Status

APPROVAL PACKET ONLY — no secret value, no runtime change, no deploy, no endpoint call, no Gemini execution

## Baseline

- Repo: `https://github.com/jorradol/nonga.git`
- Branch: `feature/chat-image-attachment-v1`
- Latest accepted pushed commit before v13.1: `236ea5520ac4d76d9316db8ab8ef035c101676c4` (`236ea55`)
- Baseline verification requirements:
  - `git status`
  - `git branch --show-current`
  - `git rev-parse HEAD`
  - `git fetch origin`
  - `git rev-parse origin/feature/chat-image-attachment-v1`
- Baseline result for this approval-packet slice:
  - branch = `feature/chat-image-attachment-v1`
  - local HEAD = `236ea5520ac4d76d9316db8ab8ef035c101676c4`
  - origin HEAD = `236ea5520ac4d76d9316db8ab8ef035c101676c4`
  - local = origin: yes
  - working tree clean: yes

## v13.0 Source Status

v13.0 ACCEPTED / CLOSED / PUSHED with final recommendation:

- `READY FOR OWNER DECISION ON ADMIN TOKEN PROVISIONING / STAGING RUNTIME ENV SETUP`

Confirmed carry-forward status:

- plan for `NONGA_ADMIN_API_TOKEN` is already documented.
- same token is required across runtime/server env and operator/session env.
- Gemini API key must not be used as admin token.
- no real token created yet.
- no runtime config change yet.
- no deploy yet.
- no endpoint call yet.
- no Gemini execution yet.

## Owner Decision Required

Owner decision is required before moving to v13.2 controlled action:

1. approve creating or using a valid `NONGA_ADMIN_API_TOKEN`
2. approve setting the same token in staging runtime/server env
3. approve operator/session to use the same token for next recheck
4. approve staging-only scope
5. confirm Real Gemini execution is still not allowed until auth recheck passes and separate approval exists

## Admin Token Decision Options

- Option A — Generate a new dedicated Nong A admin token
- Option B — Use an existing Nong A admin token if owner/operator can locate it securely
- Option C — HOLD until secret ownership/runtime access is clarified

## Recommended Option

`Option A — Generate a new dedicated Nong A admin token`

Rationale:

- v12.15 did not find a usable prior admin token value in current operator/session context.
- Cloud Run metadata did not show `NONGA_ADMIN_API_TOKEN`.
- Secret Manager list did not show an explicit admin-token secret name.
- a dedicated admin-auth token is safer than misusing another credential type.

## Exact Approval Wording Needed From Owner

Use this wording in owner decision response (for v13.2 only):

FINAL APPROVAL FOR v13.2:  
I approve controlled staging-only NONGA_ADMIN_API_TOKEN provisioning.  
Use a dedicated admin token, do not expose the token, do not commit the token, do not use Gemini API key as admin token.  
Set the same token for staging runtime/server env and operator/session env.  
After provisioning, perform only the approved non-Gemini admin auth recheck.  
No Gemini execution, no smoke endpoint, no production, no public route, no buyer-facing AI, no real lead sending.

## Runtime/Server Env Setup — Planned Only

Planned-only target for next approved slice:

- staging runtime/server env key:
  - `NONGA_ADMIN_API_TOKEN=<same token>`
- staging-only scope, no production action.
- runtime config change is not executed in v13.1.
- deploy is not executed in v13.1.

## Operator/Session Env Setup — Planned Only

Planned-only operator alignment for next approved slice:

- operator/session env must hold `NONGA_ADMIN_API_TOKEN` with the same token used in staging runtime/server env.
- presence-only checks are allowed; value output is forbidden.
- no live auth recheck is executed in v13.1.

## Non-Secret Command Templates

Do not run this command in v13.1.  
This is a non-secret template only.  
Runtime env update requires explicit owner approval in v13.2.

```bash
# placeholder only — do not paste or print the real token in docs/logs
export NONGA_ADMIN_API_TOKEN="<ADMIN_TOKEN_FROM_SECURE_OPERATOR_SOURCE>"
```

```bash
# placeholder only — staging runtime env update requires explicit owner approval in v13.2
gcloud run services update <STAGING_SERVICE_NAME> \
  --region <REGION> \
  --set-env-vars NONGA_ADMIN_API_TOKEN="<ADMIN_TOKEN_FROM_SECURE_OPERATOR_SOURCE>"
```

## Secret Handling Rules

- no secret value in docs/logs/reports/commits
- no token print
- no token commit
- no token copy to unapproved channels
- use sanitized evidence only
- least privilege and owner-approved handling only

## What Must Not Be Used As Admin Token

- Gemini API key must not be used as `NONGA_ADMIN_API_TOKEN`.
- Firebase service account must not be used as `NONGA_ADMIN_API_TOKEN`.
- Google OAuth token must not be used as `NONGA_ADMIN_API_TOKEN`.
- any customer PII, phone, plate, VIN, or predictable value must not be used as `NONGA_ADMIN_API_TOKEN`.

## Future v13.2 Sequence If Approved

Planned sequence only; not executed in v13.1:

1. Confirm repo clean and local = origin
2. Confirm owner approval wording is present
3. Generate or retrieve dedicated admin token securely without printing it
4. Set same token in staging runtime/server env only
5. Export same token in operator/session env without printing it
6. Confirm token presence only, never value
7. Perform approved non-Gemini admin auth recheck only
8. Record sanitized result
9. Do not execute Gemini
10. Do not call smoke endpoint

## Stop Conditions

Stop and return HOLD immediately if any of these occurs:

- owner approval wording is missing or ambiguous
- runtime access/secret ownership is unclear
- any step would require secret exposure
- any step would require runtime change in v13.1
- any request attempts endpoint call, live auth recheck, or Gemini execution in v13.1

## Guardrail Confirmation

- no secret value
- no secret exposure
- no runtime change
- no deploy
- no endpoint call
- no live auth recheck
- no smoke endpoint
- no Gemini execution
- no `/api/gemini/*`
- no production
- no public route
- no buyer-facing AI
- no real lead
- no token/API key exposed

## Recommendation

`READY FOR OWNER APPROVAL TO PROCEED TO v13.2 CONTROLLED STAGING ADMIN TOKEN PROVISIONING`
