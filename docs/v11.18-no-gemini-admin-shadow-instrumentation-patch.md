# v11.18 No-Gemini Admin Shadow Instrumentation Patch

## 1. Status

PATCHED — no Gemini execution, no retry

## 2. Baseline

Baseline commit: be7400613a83383cd5a5f949e72fa507554ae993  
v11.17 finding: HOLD — review found unresolved stall risk

## 3. Problem Addressed

This patch is limited to instrumentation and timeout/lifecycle boundaries for admin-only manual smoke pathways, without running Gemini or calling smoke endpoints during validation.

- manual caller had no independent timeout
- provider timeout did not prove full request lifecycle completion
- diagnostics needed caller/server boundary clarity
- unresolved provider/network behavior still must not cause uncontrolled repeated attempts

## 4. Changes Made

- `src/services/ai/adminShadowSmokeApi.ts`
  - Added manual-caller timeout boundary with `AbortController` (caller-local timeout, bounded).
  - Added caller lifecycle events for start/timeout/completed/aborted (sanitized metadata only).
  - Added optional caller interface for timeout/signal/lifecycle callback; no retry logic added.
- `src/services/ai/salesBrainAdminShadowRealProvider.ts`
  - Extended provider caller interface to accept `AbortSignal` + timeout context.
  - Added timeout-driven abort signaling path in provider invocation boundary.
  - Kept single-attempt behavior (no auto retry).
- `src/services/ai/salesBrainAdminShadowDiagnostics.ts`
  - Expanded stage vocabulary to include request-handler start/return and manual-caller lifecycle stages.
- `src/services/ai/salesBrainServerShadowSmoke.ts`
  - Added handler lifecycle stage logs at request start and handler return (including validation-return paths).
  - Kept payload/logging sanitized; no secret/raw provider output/PII logging.
- `scripts/test-v1118-no-gemini-admin-shadow-instrumentation-patch.mts`
  - Added static validator for v11.18 guardrails and instrumentation presence.
- `package.json`
  - Added script: `test:v1118:no-gemini-admin-shadow-instrumentation-patch`.

## 5. Guardrail Confirmation

- no retry
- no Gemini execution
- no smoke endpoint call during validation
- no `/api/gemini/*`
- no deploy
- no production
- no public route activation
- no buyer-facing AI
- no real lead
- no PII/customer data
- no secret/token/API key exposure
- no automatic retry
- no public behavior change

## 6. Diagnostics Added

- `admin_shadow_request_handler_start`
- `admin_shadow_request_handler_return`
- `admin_shadow_manual_caller_start`
- `admin_shadow_manual_caller_timeout`
- `admin_shadow_manual_caller_completed`
- `admin_shadow_manual_caller_aborted`

Existing provider/gate/fallback diagnostics remain in place for boundary correlation.

## 7. What This Does Not Prove

- does not prove real Gemini success
- does not prove provider response completion
- does not prove transport/socket cleanup of Gemini SDK
- does not authorize retry
- does not authorize production or public AI

## 8. Recommendation

READY FOR STATIC REVIEW / OWNER DECISION
