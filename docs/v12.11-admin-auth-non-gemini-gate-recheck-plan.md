# v12.11 Admin Auth Non-Gemini Gate Recheck Plan — No Gemini Execution

Status: PREPARE ONLY — no Gemini execution, no smoke endpoint call

## Baseline

- Repo: `https://github.com/jorradol/nonga.git`
- Branch: `feature/chat-image-attachment-v1`
- Baseline commit: `c1f8ff0ce6276ef75b524ab4ef2a9f70fd1c18ce` (`c1f8ff0`)
- Baseline checks required before any future live recheck:
  - `git status`
  - `git rev-parse HEAD`
  - `git fetch origin`
  - `git rev-parse origin/feature/chat-image-attachment-v1`
- Precondition: local = origin and working tree clean, otherwise abort.

## Source Event: v12.8

v12.8 stopped before execution:

- env gate: FAIL
- admin auth non-Gemini gate: FAIL
- observed status: `401`
- endpoint called: no
- run count: `0/1`
- authorization consumed: no
- Gemini execution: no
- smoke endpoint call: no

## v12.9 Findings

Root-cause risk from v12.9:

- server token env: `NONGA_ADMIN_API_TOKEN`
- script/gate token env: `VITE_NONGA_ADMIN_API_TOKEN` / Firebase ID token path
- env name aligned: no
- token formatting risk: yes

## v12.10 Patch Summary

v12.10 completed admin auth/env alignment:

- server token env: `NONGA_ADMIN_API_TOKEN` primary
- script/gate env path: explicit server-first resolution
- auth mode alignment fixed: yes
- token validation added: yes
- sanitized header preview added: yes
- Firebase/admin token mode separated: yes
- legacy fallback clarified: yes
- v12.10 recommendation: `READY FOR ADMIN AUTH NON-GEMINI GATE RECHECK PLAN — no Gemini execution`

## Recheck Scope

v12.11 is prepare-only/recheck-plan-only:

- no live recheck in this version
- no retry
- no Gemini execution
- no smoke endpoint call
- no `POST /api/admin/sales-brain-shadow-smoke`
- no `/api/gemini/*`
- no deploy
- no production
- no public route activation
- no buyer-facing AI
- no real lead

## Non-Gemini Admin Auth Target

Selected safe target for next version live auth-gate recheck:

- method: `GET`
- path: `/api/admin/duplicates`
- reason:
  - admin path under `/api/admin/*`
  - protected by same `adminApiAuth` middleware chain (`app.use("/api/admin", adminApiAuth)`)
  - not the smoke route
  - no Gemini/provider call in handler
  - no lead send side effect

Excluded targets:

- `/api/admin/sales-brain-shadow-smoke` (forbidden in v12.11 plan target)
- `/api/gemini/*` (forbidden)

## Token Mode Requirement

Recheck must use admin API token mode as primary:

- primary mode: `Authorization: Bearer <NONGA_ADMIN_API_TOKEN>`
- fallback mode, if explicitly approved in recheck runbook: `VITE_NONGA_ADMIN_API_TOKEN`
- Firebase ID token mode is separate and must not be mixed silently
- legacy `x-api-token` path is legacy-only and not primary for this recheck

Token validation required before header construction:

- token present and not empty
- not literal env name (`$NONGA_ADMIN_API_TOKEN`, `${NONGA_ADMIN_API_TOKEN}`, `undefined`, `null`)
- no leading/trailing whitespace
- no newline
- not quoted literal
- not pre-prefixed with `Bearer `

## Expected Auth Boundary

For the next authorized live recheck version (not v12.11):

- no header request: expect `401`
- valid masked admin header request: expect `200` or expected success response from this non-Gemini admin endpoint

If valid-header result is not success, treat as gate fail and hold.

## Sanitized Command Template

No-header check:

```bash
curl --fail-with-body --silent --show-error \
  --request GET \
  --url "<VALIDATED_ADMIN_BASE_URL>/<SAFE_NON_GEMINI_ADMIN_AUTH_CHECK_PATH>"
```

Valid masked admin header check:

```bash
curl --fail-with-body --silent --show-error \
  --request GET \
  --url "<VALIDATED_ADMIN_BASE_URL>/<SAFE_NON_GEMINI_ADMIN_AUTH_CHECK_PATH>" \
  --header "Authorization: Bearer ***MASKED***"
```

Sanitization rules:

- response must be sanitized
- do not print token value
- do not commit secret/token/API key
- no retry option in command

## Abort / Fail-Fast Cases

Abort immediately if:

- local != origin
- working tree not clean
- target endpoint unclear
- target endpoint equals smoke route
- target endpoint may call Gemini/provider
- URL malformed
- token missing/empty/invalid format
- sanitized preview includes real token
- command includes retry behavior
- command includes `/api/gemini/*`
- any deploy/production/public/buyer-facing/real lead path appears
- non-Gemini gate cannot be proven

## Guardrail Confirmation

- no live recheck in v12.11
- no retry
- no Gemini execution
- no smoke endpoint call
- no `/api/gemini/*`
- no deploy
- no production
- no public route activation
- no buyer-facing AI
- no real lead
- no PII/customer data
- no raw provider output
- no automatic retry

## Recommendation

`READY FOR ADMIN AUTH NON-GEMINI LIVE RECHECK — no Gemini execution`
