# v5.6I.18 — Read-only Staging Preflight Execution (Recorded)

**สถานะ:** Execution record + static tests only — **ไม่เปลี่ยน runtime, ไม่ deploy, ไม่ update env**  
**Git baseline ขณะรัน:** `8fddd298a07390021c41e5636bba2a6079f79c1d` (v5.6I.17)  
**วันที่บันทึก:** 2026-06-06  
**Target:** staging **`nonga-ce93c`** เท่านั้น  
**Branch:** `feature/chat-image-attachment-v1`

> เอกสารนี้บันทึกผล **read-only preflight ที่รันจริงแล้ว** — ไม่ใช่คำสั่งให้รันซ้ำโดยอัตโนมัติจาก CI (test v56i18 เป็น static validation ของ record นี้)

---

## สรุปผล (plain language)

| เรื่อง | ผล |
|--------|-----|
| Git | clean, synced กับ origin ที่ `8fddd29` |
| Cloud Run staging | `nonga-ce93c` / `nonga-staging` / `asia-southeast1` |
| Settlement persistence flags | **ไม่มีบน Cloud Run** → default memory / writes off / success fee off |
| Staging API | health + cars OK; revenue endpoints ต้อง auth (401) |
| Firestore client direct | settlement collections **403 deny** (unauth) |
| Deploy / env update / Firestore write | **ไม่ได้ทำ** |

---

## 1. Git (read-only)

| Check | Result |
|-------|--------|
| `git status` | clean |
| branch | `feature/chat-image-attachment-v1` |
| HEAD | `8fddd298a07390021c41e5636bba2a6079f79c1d` |
| `origin/feature/chat-image-attachment-v1` | ตรงกับ HEAD |
| HEAD ≥ `8fddd29` | yes |

---

## 2. Cloud Run staging (read-only describe)

**คำสั่งที่รัน (read-only):**

```bash
gcloud config get-value project
gcloud run services describe nonga-staging \
  --project=nonga-ce93c \
  --region=asia-southeast1 \
  --format="yaml(metadata.name,metadata.labels,status.url,spec.template.spec.containers[0].env)"
```

| Field | Recorded value |
|-------|----------------|
| gcloud active project | `nonga-ce93c` |
| service | `nonga-staging` |
| region | `asia-southeast1` |
| service URL | `https://nonga-staging-dpf3rexexq-as.a.run.app` |
| APP_URL | `https://nonga-ce93c.web.app` |
| FIREBASE_PROJECT_ID | `nonga-ce93c` |

**Secrets (ref only — ไม่บันทึกค่า):**

- `GEMINI_API_KEY` ← `secretKeyRef: gemini-api-key:latest`
- `FIREBASE_SERVICE_ACCOUNT_JSON` ← `secretKeyRef: firebase-service-account-json:latest`

**หมายเหตุ:** มี `NONGA_DATA_BACKEND=firestore` และ `NONGA_IMAGE_BACKEND=firebase-storage` — เป็น inventory/image backend **ไม่ใช่** settlement persistence

---

## 3. Settlement env flags (absent on Cloud Run)

| Env | บน Cloud Run ขณะรัน | Default runtime |
|-----|---------------------|-----------------|
| `NONGA_SETTLEMENT_DATA_BACKEND` | **absent** | `memory` |
| `NONGA_SETTLEMENT_FIRESTORE_WRITES_ENABLED` | **absent** | off |
| `NONGA_SUCCESS_FEE_RECORD_ENABLED` | **absent** | off |

`isDurableSettlementPersistenceActive()` = **false**

---

## 4. Staging API (read-only GET)

Base: `https://nonga-ce93c.web.app`

| Endpoint | HTTP status |
|----------|-------------|
| `GET /api/health` | **200** |
| `GET /api/cars?limit=1` | **200** |
| `GET /api/admin/revenue/preview` (no auth) | **401** |
| `GET /api/my/revenue/preview` (no auth) | **401** |

---

## 5. Firestore direct unauth REST (read-only probe)

Project: `nonga-ce93c` — GET doc + LIST collection (ไม่สร้าง/แก้/ลบข้อมูล)

| Collection | GET (probe doc) | LIST (`pageSize=1`) |
|------------|-----------------|---------------------|
| `successFeeRecords` | **403** | **403** |
| `settlementAdjustments` | **403** | **403** |
| `settlementAuditLogs` | **403** | **403** |
| `settlementIdempotencyRecords` | **403** | **403** |

---

## Forbidden changes (confirmed not done)

- ไม่ deploy Cloud Run / Hosting / Firestore rules
- ไม่ `gcloud run deploy`
- ไม่ `gcloud run services update`
- ไม่เปิด env flags / secrets
- ไม่เปิด Firestore settlement persistence
- ไม่เขียน Firestore / migration / backfill
- ไม่แตะ production
- ไม่ payment / invoice / Gemini code changes

---

## Next step (not in v5.6I.18)

เมื่อลุงอนุมัติตาม v5.6I.16 / v5.6I.17 → ค่อยเปิด settlement flags บน staging ตามแผน (**DO NOT RUN YET**)

---

## Files (v5.6I.18)

| File | เปลี่ยน |
|------|--------|
| `docs/v5.6I.18-read-only-staging-preflight-execution.md` | **NEW** — record นี้ |
| `scripts/test-v56i18-read-only-staging-preflight-execution.mts` | **NEW** — static validation |
| `package.json` | `test:v56i18-read-only-staging-preflight-execution` |
