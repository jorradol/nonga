# v22.30 — Lead Capture Kill Switch Implementation

Date: 2026-07-09  
Branch: `feature/chat-image-attachment-v1`  
Execution type: `CONTROLLED LEAD CAPTURE KILL SWITCH IMPLEMENTATION / STAGING SAFETY GUARD ONLY / DEFAULT OFF / NO REAL LEAD / NO DEALER-FACING SEND / NO PRODUCTION / NO PUBLIC`

## 1) Purpose

Close the v22.29 HOLD: implement a dedicated global lead-capture kill switch that **defaults OFF** and blocks real lead creation even for authenticated invitees, until a later owner-approved ON step.

This step does **not** turn capture ON, create real leads, send to dealers, enable public signup, or deploy production.

## 2) Owner-readable summary

| Question | Answer |
|----------|--------|
| What switch was added? | Server env **`NONGA_LEAD_CAPTURE_ENABLED`** (boolean via exact `"true"` only) |
| Default state | **OFF** when unset, empty, `"false"`, or any value other than `"true"` |
| What happens when OFF? | `POST /api/buyer-leads` after auth returns **403** with safe Thai copy; **no lead row written**; no dealer notify |
| Authenticated users when OFF? | **Blocked** — same 403; modal submit disabled when health says OFF |
| Unauthenticated users? | Still **401** (auth checked before kill switch) |
| Can any real lead be created while OFF? | **No** (route + service double guard) |
| Can dealer send happen while OFF? | **No** create → nothing to reveal/send; auto-push still not implemented |
| Health signal | `GET /api/health` includes `leadCaptureEnabled: false` (boolean only; no env name) |
| What owner must approve before ON later? | Explicit staging-only enable of `NONGA_LEAD_CAPTURE_ENABLED=true`, cohort/listings, and monitoring — **separate packet** |

## 3) Technical summary

### Files changed

| File | Change |
|------|--------|
| `src/services/leads/leadCaptureFlags.ts` | **New** — `isLeadCaptureEnabled`, disabled message |
| `src/services/leads/leadCaptureClientFlags.ts` | **New** — client health read (fail closed) |
| `src/services/leads/buyerLeadService.ts` | Guard before create; optional `env` for tests |
| `src/server/buyerLeadRoutes.ts` | Auth → kill switch 403 → then create |
| `server.ts` | Health exposes `leadCaptureEnabled` |
| `src/services/leads/buyerLeadApi.ts` | Map 403 disabled copy |
| `src/services/leads/buyerLeadConsentModalCopy.ts` | Modal disabled hint |
| `src/services/leads/buyerLeadCaptureCopy.ts` | Chat staging hint |
| `src/services/leads/buyerLeadCaptureHandler.ts` | Async car-card + ready hints |
| `src/components/chat/BuyerLeadConsentModal.tsx` | Disable submit when OFF |
| `src/components/chat/BuyerLeadConsentModalHost.tsx` | Fetch health flag; fail closed |
| `src/hooks/chat/useChat.ts` | Await async car-card handler |
| Lead unit tests (`v56*`, multi-owner, etc.) | Opt-in `env: { NONGA_LEAD_CAPTURE_ENABLED: "true" }` for create paths only |

### Routes guarded

- `POST /api/buyer-leads` — primary
- `createConsentedBuyerLead` — defense in depth (no write if OFF)

### Env / default

- Name: `NONGA_LEAD_CAPTURE_ENABLED`
- Default when unset: **false**
- This deploy: **does not set** the env to true (image-only; unset → OFF)

### Frontend when OFF

- Modal shows amber hint; confirm button disabled / label 「ยังไม่เปิดส่งข้อมูลในรอบนี้」
- Chat may show soft staging hint; flow still explorable
- Backend remains authoritative if UI is bypassed

### Ownership guards preserved (v22.29)

- Recipient = `listing.ownerId` → `sellerId`
- Empty owner → 400
- No buyer recipient override
- Queue ACL unchanged; phone locked on create; no auto dealer push

## 4) Tests

```text
npx tsx scripts/test-v22.30-lead-capture-kill-switch-unit.mts
npm run test:v22.30
npx tsx scripts/test-v56c-buyer-consent-lead-capture.mts
npx tsx scripts/test-v56e1-buyer-purchase-profile-reuse.mts
npx tsx scripts/test-v56d-buyer-lead-queue.mts
npx tsx scripts/test-lead-multi-owner-isolation.mts
```

Unit proofs:

- unset / false → create **403**, **0** lead rows
- empty owner still **400** when ON
- ON path creates locked-phone lead in memory then cleaned
- process default remains OFF after tests

Authenticated create while OFF is proven at **service** layer (same function the route calls). Live staging does not use owner tokens; unauth remains **401**.

## 5) Staging verification evidence

| Field | Value |
|-------|--------|
| Cloud Run service | `nonga-staging` |
| Deploy type | staging **image-only** Cloud Run + Hosting (no env enable) |
| Cloud Build ID | `7ddef888-e810-42e5-9d83-63ef4b86963a` |
| Previous revision | `nonga-staging-00188-tm7` |
| New revision | **`nonga-staging-00189-9jl`** @ **100%** |
| Image tag | `v20260709-lead-kill-switch-44c3094` |
| Hosting asset | `assets/index-DxKBQ6yE.js` |
| `leadCaptureEnabled` | **false** (live health) |
| `publicSignupEnabled` | **false** |
| Unauth `POST /api/buyer-leads` | **401** |
| Marketplace count | **13** |
| `NONGA_LEAD_CAPTURE_ENABLED` on Cloud Run | **unset** (defaults OFF; not set to true) |
| Real lead created | **no** |
| Dealer-facing send | **no** |
| Capture turned ON | **no** |
| Production | untouched |

## 6) Safety report

| Check | Result |
|-------|--------|
| No real lead created | **PASS** |
| No dealer-facing send | **PASS** |
| Buyer-lead unauth 401 | **PASS** |
| Authenticated create while OFF blocked | **PASS** (unit/service) |
| `publicSignupEnabled:false` | **PASS** |
| Marketplace 13 | **PASS** |
| Production untouched | **PASS** |
| No secrets/PII in report | **PASS** |
| Kill switch default OFF | **PASS** |
| Kill switch not enabled this step | **PASS** |

## 7) Future ON checklist (not requesting approval now)

- [ ] Owner confirms kill switch OFF behavior on staging UI
- [ ] Separate approval to set `NONGA_LEAD_CAPTURE_ENABLED=true` on staging only
- [ ] Named accounts / listings / fake-vs-real phone policy
- [ ] Monitoring + cleanup owner
- [ ] Still forbid public signup, production, auto dealer push

## 8) Recommendation

NEED REVIEW — dedicated lead capture kill switch is implemented, defaults OFF, blocks authenticated lead creation (service + route), preserves unauth 401, no lead/dealer/public/production enablement occurred; owner may review UI wording before any later controlled ON approval.

### Explicit non-recommendations

- Not approval to turn capture ON
- Not approval to create real leads or contact dealers
- Not public / production ready
