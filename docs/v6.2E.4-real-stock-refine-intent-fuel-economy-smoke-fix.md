# v6.2E.4 — Real Stock Refine Intent Smoke Fix (Fuel Economy)

**สถานะ:** **FIX (STAGED)** — runtime refine copy + tests — **no deploy in this slice**  
**วันที่:** 2026-06-11  
**Git baseline:** `3b7c13921ea84aec2f4acae17ffc4848e435888e` (v6.2E.3 pushed)  
**Branch:** `feature/chat-image-attachment-v1`  
**Staging URL:** `https://a.nongbot.org`  
**อ้างอิง:** v6.2E.3 Image Migration, v6.2E Pilot Monitoring, v6.1L.2g Context Grounding

> **v6.2E.4 = minimal fix สำหรับ refine intent “เอาประหยัดน้ำมัน” หลัง compare/search** — grounded จาก recent carCards เท่านั้น  
> **Redacted diagnosis only** — no per-car / PII / raw URLs in git

---

## สารบัญ

1. [Issue Report](#1-issue-report)
2. [Diagnosis Summary](#2-diagnosis-summary)
3. [Root Cause](#3-root-cause)
4. [Fix Applied](#4-fix-applied)
5. [Expected Behavior After Fix](#5-expected-behavior-after-fix)
6. [Manual Smoke Plan](#6-manual-smoke-plan)
7. [Deploy Requirement](#7-deploy-requirement)
8. [Safety Confirmations](#8-safety-confirmations)
9. [Compliance (v6.2E.4)](#9-compliance-v62e4)

---

## 1. Issue Report

| Item | Detail |
|------|--------|
| Reporter | ลุง (allowlisted staging pilot) |
| Flow | งบ 4 แสน → เทียบคันที่ 1 กับ 2 → `เอาประหยัดน้ำมัน` |
| Symptom | ข้อความตอบแคบ — เน้นคันเดียว ไม่ชัดว่า refine จาก recent carCards |
| carCards | ยังแสดงหลายคัน แต่ copy ไม่สอดคล้อง |
| Images / sidebar / search / compare | ✅ ผ่านก่อนหน้า (v6.2E.3) |

---

## 2. Diagnosis Summary

| Check | Result |
|-------|--------|
| `detectBuyerRefinement("เอาประหยัดน้ำมัน")` | ✅ `fuel` |
| `isPilotBuyerFollowUpMessage` | ✅ true |
| Orchestrator refine routing | ✅ `tryContextualBuyerFollowUp` |
| Copy builder used | ❌ `buildFollowUpReplyCopy` — **single-car template** |
| Pilot copy builder exists | ✅ `buildBuyerRefinementPilotCopy` — แต่ orchestrator legacy ยังผิด |
| Server bridge override | ⚠️ ขึ้นกับ auth/API — orchestrator fix จำเป็นเป็น baseline |

---

## 3. Root Cause

```
User: เอาประหยัดน้ำมัน (after compare)
  → detectBuyerRefinement = fuel ✅
  → tryContextualBuyerFollowUp runs ✅
  → buildFollowUpReplyCopy(cards, message)
  → ❌ ignores refinement kind
  → ❌ uses cars[0] only
  → "จากข้อมูลที่มี {brand} {model} ปี {year} ตอนนี้:" (single car)
```

**Conclusion:** Intent classification ถูก — **reply copy path ผิด** สำหรับ fuel refine follow-up

---

## 4. Fix Applied

| File | Change |
|------|--------|
| `src/services/ai/chat/chatRefinementReplyCopy.ts` | **new** — grounded fuel/family/installment refine copy |
| `src/services/ai/chat/chatSearchReplyCopy.ts` | route `buildFollowUpReplyCopy` → refinement builder |
| `src/services/ai/salesBrainUserVisiblePilotBuyerCopy.ts` | pilot refine delegates to shared builder |

### Fuel refine copy rules (implemented)

| Rule | Status |
|------|--------|
| Compare/rank จาก recent carCards ทั้งชุด | ✅ |
| ใช้ fuel/body/description จากประกาศเท่านั้น | ✅ |
| ข้อมูลไม่พอ → `ข้อมูลประกาศยังไม่พอ` | ✅ |
| ห้ามแต่งอัตรากินน้ำมัน | ✅ |
| ห้ามฟันธง “ประหยัดแน่นอน” | ✅ |
| No-context safe copy | ✅ |
| โทนน้องเอ + disclaimer | ✅ |

---

## 5. Expected Behavior After Fix

| Step | Expected |
|------|----------|
| Search `งบ 4 แสน มีรถอะไรน่าเล่น` | carCards grounded |
| `เทียบคันที่ 1 กับ 2` | compare copy + 2 cards |
| `เอาประหยัดน้ำมัน` | เปิดด้วย “ถ้าเน้นประหยัดน้ำมัน น้องเอขอเทียบจากข้อมูลประกาศ…” |
| Multi-car listing | แสดงทุกคันใน context พร้อมเหตุผลจากประกาศ |
| Insufficient fuel data | มี `ข้อมูลประกาศยังไม่พอ` |
| No prior cards | safe no-context copy |

---

## 6. Manual Smoke Plan

| # | Prompt | Expected |
|---|--------|----------|
| SM-01 | งบ 4 แสน มีรถอะไรน่าเล่น | cards ≥ 1 |
| SM-02 | เทียบคันที่ 1 กับ 2 | compare grounded |
| SM-03 | เอาประหยัดน้ำมัน | multi-car refine copy — **not** single-car opener |
| SM-04 | เอาประหยัดน้ำมัน (new chat, no context) | no-context safe copy |
| SM-05 | Guest path | legacy orchestrator copy improved |

---

## 7. Deploy Requirement

| Component | Required |
|-----------|----------|
| **Firebase Hosting** | ✅ **yes** — frontend orchestrator + client path |
| **Cloud Run / backend** | ✅ **yes** — `/api/ai/chat-user-visible-orchestrate` pilot copy |
| Env update | ❌ none |
| Firestore writes | ❌ none |

> Deploy **Hosting + backend** together on staging after approval — both carry refine copy changes.

---

## 8. Safety Confirmations

| Item | Status |
|------|--------|
| Production | ❌ not touched |
| Env update | ❌ none |
| Firestore writes | ❌ none |
| Import / cleanup / hide | ❌ none |
| Public AI / real Gemini path | ❌ unchanged |
| AI vision provider | ❌ not used |
| Real car data / PII in repo | ❌ none |
| Invented fuel economy numbers | ❌ forbidden in copy |

---

## 9. Compliance (v6.2E.4)

| Item | Status |
|------|--------|
| Issue diagnosed §1–3 | ✅ |
| Minimal runtime fix §4 | ✅ |
| Tests added | ✅ `test:v62e4-real-stock-refine-intent-fuel-economy-smoke-fix` |
| Deploy noted §7 | ✅ hosting + backend |
| Staging only | ✅ |

CI: `npm run test:v62e4-real-stock-refine-intent-fuel-economy-smoke-fix`

---

## Redacted Summary (git-safe)

```text
v6.2E.4 Fuel Economy Refine Fix — STAGED
baseline: 3b7c139 | staging pilot allowlisted smoke
issue: เอาประหยัดน้ำมัน after compare → single-car copy mismatch
rootCause: buildFollowUpReplyCopy ignored refinement + cars[0] only
fix: chatRefinementReplyCopy + orchestrator/pilot shared refine builders
deploy: hosting + backend required on staging after approval
production: excluded | firestore/env/import: none
```

**No per-car details, UIDs, plates, or raw URLs in this repository.**
