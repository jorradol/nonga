# v22.35 — Controlled Live TEST Dealer Posting (Owner-Browser)

Date: 2026-07-09  
Branch: `feature/chat-image-attachment-v1`  
Execution type: `CONTROLLED LIVE TEST DEALER POSTING OWNER-BROWSER / STAGING ONLY / TEST DATA ONLY / LEAD CAPTURE OFF / NO REAL LEAD / NO DEALER-FACING SEND / NO PRODUCTION / NO PUBLIC`

## 1) Purpose

Owner-approved next step after **v22.34 PASS**: run a **controlled browser-based staging test** of the dealer posting workflow using **TEST DATA ONLY**, proving pending review, approval-gated marketplace visibility, safe public DTO, and cleanup — while lead capture stays OFF.

This step does **not**:

- approve real dealer posting
- turn lead capture ON (`NONGA_LEAD_CAPTURE_ENABLED` must stay unset/false)
- create real leads or dealer-facing sends
- deploy production / enable public signup
- ask owner for terminal, API call, token, cookie, header, env, or secret

## 2) Owner-browser checklist (simple, no terminal)

Use an existing invite/pilot account already allowed on staging. Do **not** paste tokens into chat.

### A) Dealer submit (TEST listing)

1. Open staging: `https://a.nongbot.org`
2. Sign in with the **invite dealer** test account (browser only).
3. Open **Dealer Portal → ยังไม่ลงขาย / Drafts** (`/dealer/drafts`).
4. Create a draft with clearly fake data only:

| Field | Use this |
|-------|----------|
| Title | `ทดสอบระบบประกาศ - ห้ามติดต่อจริง` (or `TEST OWNER PREVIEW - DO NOT CONTACT`) |
| Make/model | `TEST Toyota` / `Preview` |
| Year | e.g. `2021` |
| Price | e.g. `199000` |
| Mileage | e.g. `12000` |
| Province | public province only (e.g. กรุงเทพมหานคร) |
| Plate / VIN / phone / address | **leave empty / do not enter real values** |
| Images | safe placeholder / existing test image only — **no real customer photos** |

5. Click **「ลงขาย」**.
6. Confirm success toast roughly:  
   「ส่งประกาศเข้ารออนุมัติแล้ว — ยังไม่แสดงในตลาด…」
7. Open **รถในคลัง** (`/dealer/inventory`).
8. Confirm badge **「รออนุมัติ」** and guidance that it is **not** on marketplace yet.
9. Confirm you **cannot** open marketplace / self-unhide while pending.
10. Confirm lead queue is **not** offered for this pending listing.

### B) Marketplace before approval

1. Open marketplace (signed out or buyer view is fine): `https://a.nongbot.org/marketplace`
2. Confirm the TEST title does **not** appear.
3. Confirm marketplace still shows the existing Thor Auto cars (images OK).
4. Optional: ask operator to confirm API count still **13** before approval (no secrets needed).

### C) Owner/admin review

1. Sign in with the **owner/admin** allowed account (browser only).
2. Open Admin dashboard → **「คิวรออนุมัติประกาศเต็นท์」**  
   or go directly to `/admin/pending-listings`.
3. Confirm the TEST listing appears with useful fields:
   - title / brand / model / year
   - price
   - dealer/seller display name
   - image preview if available
   - pending status
4. Choose **one**:
   - **「อนุมัติขึ้นตลาด」** — temporary visibility check, then cleanup immediately  
   - **「พักไว้ก่อน」** — preferred if you only need to prove hold/non-public
5. No terminal / API / token paste.

### D) After approve (only if you approved)

1. Confirm TEST listing appears on marketplace **only after** approve.
2. Open the card; confirm buyers see display names only — not internal IDs / phone / VIN / full plate.
3. Immediately cleanup (next section). Do **not** leave it public.

### E) Cleanup (mandatory)

Preferred final state: **hidden / non-public** (or deleted if available later).

1. From admin pending/hold path, use **「พักไว้ก่อน」** if still pending, **or** hide the listing from dealer/admin inventory if already published.
2. Confirm TEST title is **gone** from marketplace.
3. Confirm marketplace count returns to **13** (or stays 13 if never approved).
4. Do **not** contact anyone; do **not** create leads; leave lead capture OFF.

### F) What owner must NOT do

- Do not turn lead capture ON  
- Do not create real leads / use “ให้ผู้ขายติดต่อกลับ” for real contact  
- Do not send dealer-facing messages  
- Do not deploy production / enable public signup  
- Do not paste tokens, cookies, headers, env, or secrets into chat  
- Do not use real customer PII or real customer images  

## 3) Operator execution note (this packet)

| Attempt | Result |
|---------|--------|
| Operator browser on staging | Opened `https://a.nongbot.org` — **not signed in** |
| `/dealer/drafts` | Gate: 「กรุณาเข้าสู่ระบบก่อนใช้งานส่วนนี้ครับ」 |
| `/admin/pending-listings` | Same auth gate — no admin session in operator browser |
| Live dealer submit / approve / hold | **Not executed** — requires owner’s existing signed-in invite dealer + admin sessions |
| Secrets / tokens requested | **None** |

Therefore this packet delivers:

1. Clear owner-browser instructions (section 2)  
2. Automated policy + staging **read-only** safety evidence  
3. Recommendation based on incomplete live signed-in click-through  

Owner (or operator with an already-signed-in allowed session) can complete section 2 and report back checklist ticks without terminal work.

## 4) Owner-readable summary

### Dealer browser submit result

| Item | Result |
|------|--------|
| Live dealer submit this packet | **Not completed** (no signed-in dealer session in operator browser) |
| Intended submit path | Dealer Portal Drafts → 「ลงขาย」 → `pending_review` |
| Policy / UI proof | **PASS** (v22.34 + v22.35 automated) |

### Pending review / 「รออนุมัติ」 result

| Item | Result |
|------|--------|
| Status after submit (policy) | `pending_review` |
| Badge | **「รออนุมัติ」** |
| Guidance | 「…ยังไม่แสดงในตลาด」 |
| Self-approve / self-unhide | Blocked (policy + UI) |
| Live dealer UI confirmation | **Pending owner browser** |

### Marketplace before approval

| Item | Result |
|------|--------|
| Live marketplace count | **13** |
| TEST title in marketplace | **0** (none present) |
| Thor Auto imports | **3**, images present |
| Public protected fields | **none** (`ownerId` / `dealerId` / vin / full plate absent) |
| `ownerPhone` nonempty | **0** |

### Owner/admin pending queue result

| Item | Result |
|------|--------|
| Browser route | `/admin/pending-listings` |
| Dashboard entry | 「คิวรออนุมัติประกาศเต็นท์」 |
| Approve / Hold buttons | Present in UI (v22.34 PASS) |
| Live queue click-through | **Pending owner browser** (auth required) |
| Terminal/token needed | **No** (Firebase session in browser) |

### Approve/hold result

| Item | Result |
|------|--------|
| Live approve | **Not executed** this packet |
| Live hold | **Not executed** this packet |
| Policy: approve → published | **PASS** (unit) |
| Policy: hold → hidden / non-public | **PASS** (unit + server route present) |

### Marketplace after approval / Public DTO

| Item | Result |
|------|--------|
| Live post-approve marketplace | **N/A** (approve not run live) |
| Public DTO redaction (unit + live corpus) | **PASS** |
| Safe display fields | `dealerDisplayName` / `sellerDisplayName` / `dealerSlug` / `sellerType` |

### Cleanup / final artifact status

| Field | Value |
|-------|--------|
| Live TEST listing id | **none created** |
| Final visibility/status | **n/a — no live write** |
| Appears in marketplace | **no** |
| Final marketplace count | **13** |
| Cleanup action | **not required** (no artifact) |

### What remains locked

- Lead capture ON  
- Real leads / dealer-facing send / auto Thor notify  
- Public signup / public dealer onboarding  
- Production  
- Uncontrolled real dealer posting  

### What owner must decide before real dealer posting

1. Complete the **owner-browser checklist** (section 2) once with invite dealer + admin sessions, including cleanup  
2. Confirm Thai wording on badge / approve confirm is acceptable  
3. Confirm hold is enough vs needing hard reject/delete  
4. Confirm public display names look right for buyers  
5. Separate packet required before any controlled staging lead-capture ON  

## 5) Technical summary

### Browser routes used / intended

| Route | Role |
|-------|------|
| `https://a.nongbot.org/` | Staging host |
| `/dealer/drafts` | Dealer submit |
| `/dealer/inventory` | Pending badge / self-unhide block |
| `/marketplace` | Pre/post visibility |
| `/admin` (dashboard) | Entry to pending queue |
| `/admin/pending-listings` | Approve / hold |

### API routes touched by UI

| Method | Route | Role |
|--------|-------|------|
| POST | `/api/dealer/drafts/:id/publish` | Submit → `pending_review` |
| GET | `/api/dealer/inventory` (dealer portal) | Dealer sees pending rows |
| PATCH | `/api/dealer/inventory/:id/visibility` | Self-approve blocked |
| GET | `/api/admin/listings/pending-review` | Admin queue |
| POST | `/api/admin/listings/:id/approve` | Publish |
| POST | `/api/admin/listings/:id/hold` | Hidden / non-public |
| GET | `/api/cars` | Public marketplace DTO |
| GET | `/api/health` | Kill-switch / signup flags |
| POST | `/api/buyer-leads` | Unauth must stay 401 |

Auth for dealer/admin UI: Firebase session headers via browser — **no token paste**.

### Test listing status transitions (policy)

```text
dealer submit → pending_review (not public)
admin approve → published (public, safe DTO only)
admin hold    → hidden (not public)
dealer self-approve → forbidden
cleanup       → hidden / non-public (mandatory after live TEST)
```

### Public DTO verification

| Check | Live staging (13 cars) | Unit TEST car |
|-------|------------------------|---------------|
| No `ownerId` | PASS | PASS |
| No `dealerId` | PASS | PASS |
| No vin / full plate | PASS | PASS |
| `ownerPhone` empty | PASS | PASS |
| Display names / sellerType | PASS | PASS |

### Files changed this step

| File | Change |
|------|--------|
| `docs/v22.35-controlled-live-test-dealer-posting-owner-browser.md` | This record + owner checklist |
| `scripts/test-v22.35-controlled-live-test-dealer-posting-owner-browser.mts` | Automated read-only + policy proofs |
| `package.json` | `test:v22.35` scripts |

No runtime gate / production / lead-capture flag changes.

### Tests / checks run

| Check | Result |
|-------|--------|
| `npm run test:v22.35-controlled-live-test-dealer-posting-owner-browser` | **PASS** (automated portion) |
| `npm run test:v22.34` | **PASS** |
| `npm run test:v22.33` | **PASS** |
| `npm run test:v22.32` | **PASS** |
| Staging health / cars / unauth lead | **PASS** (read-only) |
| Live signed-in submit → approve/hold → cleanup | **NOT RUN** (no owner session) |

### Staging evidence

| Field | Value |
|-------|--------|
| Host | `https://a.nongbot.org` |
| Cloud Run | **`nonga-staging-00191-jqq`** @ **100%** |
| Hosting | `assets/index-APBJgg5j.js` (v22.34 deploy; confirmed present or current staging index asset) |
| Health | `ok: true` |
| `leadCaptureEnabled` | **false** |
| `publicSignupEnabled` | **false** |
| Unauth `POST /api/buyer-leads` | **401** |
| Marketplace | **13** |
| Thor `car-import-*` | **3**, images present |
| Public `ownerId` / `dealerId` | **absent** |
| Live TEST listing written | **no** |
| Real lead / dealer send | **no** |
| Production | untouched |
| `NONGA_LEAD_CAPTURE_ENABLED` set true | **no** |

## 6) Safety report

| Check | Result |
|-------|--------|
| Lead capture remains OFF | **PASS** |
| `NONGA_LEAD_CAPTURE_ENABLED` not set true | **PASS** |
| No real lead created | **PASS** |
| No dealer-facing send | **PASS** |
| Buyer-lead unauth 401 | **PASS** |
| Authenticated create blocked while OFF | **PASS** (prior unit; unchanged) |
| `publicSignupEnabled:false` | **PASS** |
| Production untouched | **PASS** |
| Public not enabled | **PASS** |
| No secrets/PII in report | **PASS** |
| Public DTO redaction | **PASS** |
| No protected fields in public DTO | **PASS** |
| No internal `ownerId` / `dealerId` in public DTO | **PASS** |
| Cross-dealer access regression | **PASS** (unchanged gate; not reopened) |
| Marketplace initial/final count | **13 / 13** |
| Test listing cleanup/final status | **n/a — none created** |
| Thor Auto cards/images | **PASS** |
| Chat/card/image/persistence | **not touched** |

## 7) Risk assessment

### Ready

- Pending approval gate + public DTO + lead kill-switch remain correct  
- Dealer 「รออนุมัติ」 UI and admin approve/hold UI are browser-usable (v22.34)  
- Staging safety baseline unchanged (13 / lead OFF / unauth 401 / signup OFF)  
- Owner checklist is ready without terminal/secrets  

### Needs owner review

- **Complete live signed-in click-through** (section 2) and confirm cleanup  
- Optional wording tweaks on badge / approve confirm  
- Confirm hold vs hard reject for bad submissions  

### Must-fix before controlled real dealer posting

- Owner completes live TEST checklist with cleanup evidence  
- Accept UI wording and public display-name presentation  
- Keep lead capture OFF until a separate packet  

### Must-fix before controlled staging lead capture

- Separate kill-switch ON packet (not this step)  
- Manual reveal / no auto dealer send still required  

## 8) Recommendation

**HOLD**

Reason: operator could **not** complete the live signed-in dealer submit → pending → admin approve/hold → marketplace → cleanup path in-browser without an existing allowed Firebase session. Asking owner for tokens/terminal would violate hard boundaries.

What **did** pass safely:

- Staging remains healthy with lead capture OFF, signup OFF, marketplace **13**, unauth lead **401**, public DTO redaction intact, Thor images OK  
- Policy/UI proofs for pending, self-approve block, approve/hold, and public DTO remain **PASS**  
- No live TEST artifact was left public; production untouched; no real lead / dealer send  

**Unblock path (owner only, browser only):** run section 2 checklist, then report:

1. Dealer submit toast + 「รออนุมัติ」 seen  
2. Marketplace still 13 before approval  
3. Admin queue showed TEST row; approve **or** hold clicked  
4. If approved: briefly visible then cleaned; final marketplace **13** and TEST not public  
5. Lead capture still OFF  

After that evidence, recommendation can move to **PASS** (or **NEED REVIEW** if only wording/display concerns remain).

### Explicit non-recommendations

- Not approval for uncontrolled real dealer posting  
- Not approval to turn lead capture ON  
- Not approval to create real leads or contact dealers  
- Not approval for production / public signup  

## 9) Explicit non-actions taken

- Did not set `NONGA_LEAD_CAPTURE_ENABLED=true`  
- Did not create real leads  
- Did not send dealer-facing messages  
- Did not deploy production  
- Did not enable public signup  
- Did not ask owner for token/header/cookie/secret/env/terminal  
- Did not reopen passed import/image/duplicate/chat/lead-kill-switch/public-DTO/dealer-gate/admin-approval-UI work (no regression evidence)
