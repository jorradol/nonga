# v13.15E Pilot Path Runtime Diagnosis (No Gemini Execution / No Runtime Change)

## 1) Execution type

- diagnosis only: yes
- no Gemini execution: yes
- no provider network call: yes
- no runtime config change: yes
- no deploy: yes
- no production: yes
- no public route activation: yes
- no buyer-facing release: yes
- no real lead: yes
- no real PII: yes
- no phone/plate/VIN: yes
- no secret/API key/token exposure: yes
- no automatic retry: yes

## 2) Baseline local/origin ก่อนเริ่ม

- branch: `feature/chat-image-attachment-v1`
- baseline accepted from v13.15D context: `489db35`
- local HEAD at diagnosis start: `6334fe794942a5bc34e99ddb7004e42d0fb6d07f`
- origin HEAD at diagnosis start: `6334fe794942a5bc34e99ddb7004e42d0fb6d07f`
- local = origin: yes

## 3) Files inspected

- `src/services/ai/salesBrainServerUserVisibleOrchestrationBridge.ts`
- `src/services/ai/salesBrainUserVisibleRealProvider.ts`
- `src/services/ai/salesBrainUserVisibleChatPath.ts`
- `src/services/ai/salesBrainShadowChatPathNode.ts`
- `src/services/ai/salesBrainUserVisibleGate.ts`
- `src/services/ai/salesBrainRuntimeFlags.ts`
- `src/services/ai/chat/chatPilotSessionContext.ts`
- `src/services/ai/chat/chatPilotBuyerFollowUp.ts`
- `src/services/ai/salesBrainMock.ts`

## 4) Relevant code paths

1. Route entry:
   - `handleChatUserVisibleOrchestratePost()`
   - `orchestrateUserVisibleChatForTrustedAuth()`
2. Bridge composition:
   - `runUserVisibleOrchestrationBridge()`
   - `wireShadowChatPathWithPilot()`
   - `resolveUserVisibleChatResponse()`
3. Real-provider gate:
   - `maybeApplyUserVisibleRealProvider()`
   - hard gate: `if (!input.bridgeResult.payload.pilotPathActive) -> realProviderGateReason="pilot_path_inactive"`
4. Pilot follow-up/session context:
   - `isPilotBuyerFollowUpMessage()`
   - `sanitizePilotSessionContext()`
   - `resolvePilotOrchestrationHint()`

## 5) Exact condition chain ที่ทำให้ `pilotPathActive=false`

Runtime chain in the failing prequalification shape:

1. `evaluateUserVisibleGate()` passes (`blockedReason="user_visible_allowed"`)  
   -> user-visible gate is not the failing step.
2. `runUserVisibleOrchestrationBridge()` calls `wireShadowChatPathWithPilot()`.
3. `resolveUserVisibleChatResponse()` executes with mock adapter output.
4. For priming/search-like messages with no grounded session cards:
   - mock route returns fallback intent (`intent: "fallback.orchestrator"` or `intent: "unknown"`)
   - `output.fallback === true`
   - message is not treated as follow-up for pilot path branch
5. `buildPilotUserVisibleText()` takes:
   - `fallback === true` and
   - non-follow-up branch
   -> returns `{ text: legacy, pilotPathActive: false }`
6. `maybeApplyUserVisibleRealProvider()` receives `bridgeResult.payload.pilotPathActive=false`
   -> returns immediately with:
   - `realProviderNetwork=false`
   - `realProviderGateReason="pilot_path_inactive"`
   - no provider dispatch

This behavior is fail-closed and expected.

## 6) Missing runtime/session/orchestrated context

The failing runtime shape lacks one of the pilot activators below:

- **grounded follow-up context**: `pilotSessionContext.recentCarCards.length > 0`, or
- **follow-up-classified message** that can use pilot follow-up branch safely, or
- **orchestrated context that produces pilot-safe non-legacy copy path**

In the observed failing runtime shape, prequalification used a priming/search message with empty grounded card context, so the route stayed legacy/fallback and marked `pilotPathActive=false`.

## 7) Static path vs runtime path difference

Why static/in-memory could show pilot-active while runtime failed:

- Static diagnosis path used direct controlled input shape and can force/construct pilot-active branch without full runtime request/session shape.
- Runtime path depends on full bridge input (`userMessage`, sanitized `pilotSessionContext`, orchestrator output, and follow-up classification).
- Therefore:
  - static can prove pilot-active logic exists,
  - but runtime can still fail if request/session context is not pilot-activating.

## 8) Safety boundary confirmation

- no Gemini/provider call was made for diagnosis
- no runtime flags changed
- no deploy/public/production action
- no buyer-facing release
- no lead endpoint activity
- no real PII/phone/plate/VIN usage
- no secret values recorded

## 9) Proposed minimal fix / next retry preparation

For v13.15F preparation (no execution yet), keep patch minimal and diagnosis-scoped:

1. Add deterministic runtime prequalification harness that uses **exact bridge runtime call shape**.
2. Use two-step synthetic context:
   - Step A: seed synthetic session context (`pilotSessionContext.recentCarCards`) or equivalent safe in-memory context.
   - Step B: follow-up safe-zone message that clearly matches `isPilotBuyerFollowUpMessage()`.
3. Require prequalification assertion:
   - `bridgeResult.payload.pilotPathActive === true`
   - `realProviderNetwork === false` during prequal step.
4. Consume one-run only after above assertion passes.

## 10) Risk assessment

1. **Primary risk**: misreading gate order and toggling flags to force provider call  
   - mitigation: keep `pilotPathActive` diagnosis before provider stage; no runtime flag mutation.
2. **Context drift risk**: runtime prequal uses different message/session shape than static test  
   - mitigation: bind prequal harness to the exact runtime bridge input contract.
3. **Boundary risk**: accidental public/lead/PII path activation while diagnosing  
   - mitigation: static tests + docs only; no deploy, no route activation, no lead flow.

## 11) Test result

- `npm run test:v13.15E` -> pass

## 12) Files changed

- `docs/v13.15E-pilot-path-runtime-diagnosis.md`
- `scripts/test-v1315e-pilot-path-runtime-diagnosis.mts`
- `package.json`

## 13) Commit hash full/short

- full: (to be filled after commit)
- short: (to be filled after commit)

## 14) Push result

- (to be filled after push)

## 15) Local/origin sync result

- (to be filled after push verification)

## 16) Final recommendation

READY FOR OWNER APPROVAL TO PREPARE v13.15F PILOT-PATH RUNTIME CONTEXT PATCH
