# v7.1A — Lead Flow Escape + Intent Re-check Closure Record

**สถานะ:** **CLOSED — v7.1 LEAD FLOW ESCAPE + INTENT RE-CHECK COMPLETE (LOCAL-SAFE, COMMITTED + PUSHED)** — งานรอบ v7.1 จบแล้ว: commit + push ขึ้น origin สำเร็จ, test PASS, ไม่มี deploy, ไม่เปิด user-visible AI จริง, รักษาหลัก lead/PDPA เดิมครบ
**วันที่:** 2026-06-19
**Commit:** `6ab26fd4012639c19a56d71e5bb52ea25af9bf68` (`6ab26fd`)
**Branch:** `feature/chat-image-attachment-v1`
**Push range:** `c5bed06..6ab26fd`
**สถานะ git:** local = origin, working tree clean

> Closure record ของ v7.1 เท่านั้น — เป็นเอกสาร + static validation ไม่แตะ runtime/deploy เพิ่ม **ไม่มี production deploy, ไม่มี staging deploy, ไม่เปิด user-visible AI จริง, ไม่ส่ง lead จริงเอง, เบอร์ลูกค้ายังต้องกรอกเอง, ไม่เดา/ไม่ดึงเบอร์, ไม่แตะ queue display, ไม่เปิดเผยทะเบียนเต็มหรือเบอร์ใน public/buyer chat** เอกสารนี้ไม่มี secrets, tokens, PII, phone, plate, หรือ VIN

---

## 1. เป้าหมาย v7.1

**Lead Flow Escape + Intent Re-check** — ทำให้น้องเอไม่ติด lead flow แข็ง ๆ: เมื่อผู้ใช้อยู่ในขั้นตอนเก็บข้อมูล lead แล้วเปลี่ยนใจ ระบบต้องตรวจเจตนาล่าสุดของทุกข้อความก่อน แล้วพัก lead เดิมไว้เป็น draft (ไม่ส่ง) และกลับไปช่วยตามเจตนาใหม่ทันที

ครอบคลุมเจตนาใหม่: ค้นหารถรุ่นอื่น, เปลี่ยนงบ, เปลี่ยนพื้นที่, ขอเปรียบเทียบ, ถามไฟแนนซ์, ถามเรื่องติดแก๊ส, ขอรถประหยัดน้ำมัน และการลังเล/ขอเวลา ("ยังไม่ส่ง", "เดี๋ยวก่อน ยังไม่ให้เบอร์") โดยทุกกรณี **ไม่ส่ง lead และไม่บังคับกลับไปถามเบอร์ทันที**

---

## 2. สรุป commit / push

| รายการ | ค่า |
|--------|-----|
| Commit (full) | `6ab26fd4012639c19a56d71e5bb52ea25af9bf68` |
| Commit (short) | `6ab26fd` |
| Subject | `fix(leads): add v7.1 lead flow escape intent recheck` |
| Branch | `feature/chat-image-attachment-v1` |
| Baseline ก่อนหน้า | `c5bed06` |
| Push range | `c5bed06..6ab26fd` (1 commit) |
| Remote | `origin` |
| local HEAD = origin HEAD | `6ab26fd...` = `6ab26fd...` (sync ✅) |
| Working tree | clean |

---

## 3. ไฟล์ที่เปลี่ยน (6 ไฟล์, +470 / -3)

| File | ชนิด | บทบาท |
|------|------|-------|
| `src/services/leads/buyerLeadFlowEscape.ts` | ใหม่ | intent re-check (redirect/hold/none) + resolve เฉพาะเมื่อ session active — deterministic ไม่มี Gemini |
| `src/services/leads/buyerLeadCaptureFlow.ts` | แก้ | เพิ่ม stage `paused`, `pauseBuyerLeadCapture` (เก็บ draft ไม่ส่ง), `isPausedBuyerLeadCaptureSession`, paused = inert |
| `src/hooks/chat/useChat.ts` | แก้ | เรียก re-check ก่อนบล็อก lead capture: hold=พัก+ตอบนุ่ม, redirect=พัก+แจ้งแล้วไหลต่อไปช่วยเจตนาใหม่; guard บล็อกเดิมด้วย `kind === "none"` |
| `scripts/test-v71-lead-flow-escape-intent-recheck.mts` | ใหม่ | test harness พฤติกรรม v7.1 |
| `docs/v7.1-lead-flow-escape-intent-recheck.md` | ใหม่ | เอกสารงานหลัก v7.1 |
| `package.json` | แก้ | เพิ่ม npm script `test:v71-lead-flow-escape-intent-recheck` |

---

## 4. สรุปผล test

| รายการ | ผล |
|--------|-----|
| `npm run test:v71-lead-flow-escape-intent-recheck` | **PASS 34/34** |
| `npm run lint` (tsc --noEmit) | ผ่านในส่วน v7.1 ทั้งหมด |
| lint known existing issue | error เดิม 3 จุดใน `scripts/test-v69m-thor-owner-gate-controlled-revenue-pilot-go-no-go-checklist.mts` — **ไม่เกี่ยวกับ v7.1** และอยู่นอก scope รอบนี้ (ยืนยันด้วย `git stash` ว่ามีอยู่ก่อนงานนี้) |

ขอบเขต test ครอบคลุม: redirect 7 เคส (Yaris+งบ+พื้นที่, เปรียบเทียบ, ไม่ติดแก๊ส, พื้นที่+แทน, ผ่อนได้ไหม, ประหยัดน้ำมัน, เปลี่ยนงบ), hold 2 เคส, none 7 เคส (กรอก lead จริง/ปุ่ม action/ยืนยัน/ยกเลิก/ตัวเลขงบเปล่า), และยืนยัน paused = เก็บ draft / ไม่ส่ง / inert

---

## 5. ยืนยัน safety

| Item | Result |
|------|--------|
| Production deploy | **NO** |
| Staging deploy | **NO** |
| User-visible AI / Gemini จริง เปิด | **NO** |
| ส่ง lead จริงเองโดยไม่ยืนยัน | **NO** |
| เบอร์ลูกค้าต้องกรอกเอง (ใน modal) | **YES — คงเดิม** |
| เดาเบอร์ลูกค้า | **NO** |
| ดึงเบอร์แทนลูกค้า | **NO** |
| แตะ queue display (จำนวนคิวผู้สนใจ) | **NO** |
| เปิดเผยทะเบียนเต็มใน public/buyer chat | **NO** |
| เปิดเผยเบอร์ใน public/buyer chat | **NO** |
| เปลี่ยน architecture / data model / security / revenue logic | **NO** |
| import Thor runtime | **NO** |
| paused lead = draft ไม่ส่ง | **YES** |
| ข้อมูลที่มีประโยชน์ถูกล้างทิ้ง | **NO — เก็บ draft ไว้** |

---

## 6. สิ่งที่ยังไม่ทำและส่งต่อไป v7.2–v7.5

| # | รายการ | ส่งต่อไป |
|---|--------|----------|
| H-1 | auto-resume / memory ของ paused lead (จำบริบทความสนใจ แล้วชวนกลับมาส่งเมื่อพร้อม) | **v7.2 Conversational Lead Memory** |
| H-2 | lead preview แบบธรรมชาติ + ไม่แสดงจำนวนคิว (no queue count display) | **v7.3 Natural Lead Preview + No Queue Count Display** |
| H-3 | natural answer + car card narrative fusion | **v7.4 AI Answer Naturalness + Car Card Narrative Fusion** |
| H-4 | market context + used-car safety advice layer | **v7.5 Thai Used-car Market Context + Safety Advice Layer** |

หมายเหตุ known existing issue: `buildBuyerLeadSuccessReply` ปัจจุบันยังแสดงลำดับคิวของผู้สนใจรายนั้นเอง → เป็นงานของ v7.3 ยังไม่แตะในรอบนี้

---

## 7. สรุปการปิดรอบ

**v7.1 ปิดรอบแล้ว (CLOSED)** — Lead Flow Escape + Intent Re-check commit + push ขึ้น origin สำเร็จ, test PASS, รักษาหลักเดิมครบ

**แนะนำ:** งาน v7.2 Conversational Lead Memory ควรเริ่มใน **แชทใหม่เท่านั้น** เพื่อแยก scope ให้ชัด ไม่เริ่ม v7.2 ในแชทนี้

---

## 8. This closure record (pending commit)

| File | Purpose |
|------|---------|
| `docs/v7.1A-lead-flow-escape-intent-recheck-closure-record.md` | This document (v7.1 closure record) |
| `scripts/test-v71a-lead-flow-escape-intent-recheck-closure-record.mts` | Static validation |
| `package.json` | `test:v71a-lead-flow-escape-intent-recheck-closure-record` |

---

## 9. Final verdict

**CLOSED — v7.1 LEAD FLOW ESCAPE + INTENT RE-CHECK COMPLETE (LOCAL-SAFE, COMMITTED + PUSHED)**

- v7.1 committed `6ab26fd` + pushed (`c5bed06..6ab26fd`), local = origin, working tree clean
- test PASS 34/34; lint known existing issue ใน test-v69m นอก scope
- ไม่มี production/staging deploy, ไม่เปิด user-visible AI จริง, ไม่ส่ง lead จริงเอง
- เบอร์ลูกค้ายังกรอกเอง, ไม่เดา/ไม่ดึงเบอร์, ไม่แตะ queue display, ไม่เปิดเผยทะเบียนเต็ม/เบอร์ใน public/buyer chat
- handoff: H-1 → v7.2, H-2 → v7.3, H-3 → v7.4, H-4 → v7.5
- **v7.2 ให้เริ่มในแชทใหม่เท่านั้น**

---

*End of v7.1A lead flow escape + intent re-check closure record (v7.1 closed, local-safe, committed + pushed).*
