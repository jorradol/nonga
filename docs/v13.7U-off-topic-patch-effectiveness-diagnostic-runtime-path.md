# v13.7U Off-Topic Patch Effectiveness Diagnostic — Runtime Path / Deploy Gap Investigation

## Status

DIAGNOSED — patch reachable in active code path inside repo, but tested staging surface is likely not running latest commit bundle.

## Baseline

- repo: `https://github.com/jorradol/nonga.git`
- branch: `feature/chat-image-attachment-v1`
- baseline HEAD: `1217b2179c1bd28f8ce4594a38c1346839ed0830`
- local = origin before work: yes
- working tree clean before work: yes

## Owner Retest Finding

- owner retest after v13.7T: answer behavior looked unchanged
- observed symptom: patch effects not visible on tested surface

## Investigation Scope

- static runtime path trace from chat entry point to response source
- patch reachability trace for v13.7T changes
- staging runtime/deploy gap evidence review (no deploy performed)
- test meaningfulness review and active-path diagnostic tests

## Runtime / UI Entry Path Findings

Primary buyer chat path in code:

1. `useChat.sendMessage()` calls `tryOrchestrateChatReply(...)` first.
2. `tryOrchestrateChatReplyCore(...)` checks:
   - seller/listing intent (`isSellIntent` → `buildDraftPreviewCopy`)
   - buyer intent gate (`tryBuyerIntentGateReply`)
3. If deterministic reply exists and `skipGemini=true`, flow returns before `aiService.streamChat`.
4. `aiService.streamChat` (Gemini endpoint `/api/gemini/chat-stream`) is used only when orchestrator returns `null`.

Conclusion:

- deterministic off-topic guard is positioned before Gemini dispatch in the active chain.
- Real Gemini bypass is not expected for messages already caught by deterministic gate.

## v13.7T Patch Reachability

v13.7T patch locations:

- `chatBuyerIntentGate` (off-topic/service explanation branches)
- `sellIntentParser.buildDraftPreviewCopy` (richer listing draft)

Reachability via active handler (`tryOrchestrateChatReply`) was validated for owner-like inputs:

- research off-topic → detected and answered by `tryBuyerIntentGateReply` (reachable: yes)
- image request → detected and answered by `tryBuyerIntentGateReply` (reachable: yes)
- horoscope/car-color → detected and answered by `tryBuyerIntentGateReply` (reachable: yes)
- web/latest price → detected and answered by `tryBuyerIntentGateReply` (reachable: yes)
- Toyota Yaris listing request → handled by `isSellIntent` + `buildDraftPreviewCopy` (reachable: yes)
- normal car search query → not off-topic blocked; continues core search chain (reachable: yes)

## Deploy / Bundle Gap Findings

Evidence collected:

- repo latest at start: `1217b2179c1bd28f8ce4594a38c1346839ed0830` (v13.7T)
- commit times:
  - v13.7S: `2026-07-03T07:14:18+07:00`
  - v13.7T: `2026-07-03T08:05:17+07:00`
- staging Cloud Run latest ready revision: `nonga-staging-00158-lzd`
- revision creation time: `2026-07-02T23:10:35Z` (before v13.7S/v13.7T commits)

Interpretation:

- staging tested surface is likely still serving an older build/revision than v13.7T.
- this explains owner seeing unchanged behavior despite repo-side patch/tests passing.
- no deploy was performed in v13.7U (by requirement).

## Gemini Bypass Risk

- deterministic guard position: before provider dispatch (in `useChat` orchestration chain)
- if deterministic handler returns reply, Gemini path is not called
- bypass risk for these exact off-topic samples: low in current repo code
- practical risk remains if tested surface is old bundle (deploy gap), not logic gap

## Test Meaningfulness Review

- v13.7T tests validated component functions (`chatBuyerIntentGate`, `sellIntentParser`) and passed
- gap found: tests did not explicitly prove the same orchestrator entry chain used by UI
- v13.7U adds active-path diagnostic test on `tryOrchestrateChatReply` to close this gap

## Patch Applied If Any

- runtime behavior patch: no new runtime behavior patch required
- diagnostic patch added:
  - active-path reachability test script
  - diagnostic report document
  - package script entry

## Validation

Executed static/unit validations:

- `npm run test:v137u:off-topic-patch-effectiveness-diagnostic-runtime-path`
- `npm run test:v137t:off-topic-service-explanation-car-handoff-quality-patch`
- `npm run test:v137s:off-topic-boundary-role-recovery-patch`

Result: PASS

## Guardrail Confirmation

- no deploy: yes
- no runtime config change: yes
- no Gemini execution: yes
- no smoke endpoint call: yes
- no `/api/gemini/*` call: yes
- no production/public/buyer-facing activation: yes
- no lead sending: yes
- no token/API key exposure: yes

## Result

DIAGNOSIS COMPLETE — strongest root cause is deploy/bundle gap (repo patch exists and is reachable in active code path, but tested staging surface likely not updated to latest commit bundle).

## Recommendation

HOLD — patch exists but tested surface is not deployed to latest commit
