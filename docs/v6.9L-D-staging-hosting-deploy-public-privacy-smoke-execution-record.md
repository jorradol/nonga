# v6.9L-D — Staging Hosting Deploy + Public Privacy Smoke Execution Record

**สถานะ:** **PARTIAL** — staging Hosting deploy and live asset verification **PASS**; guest smoke **PASS**; live public API regression **PASS**; live staging inventory has **no records with `vin`/`licensePlate`** — synthetic sensitive-field proof relies on static tests (41/41 PASS)  
**วันที่:** 2026-06-17  
**Git baseline (deployed):** `3170c42cc1f2d9b4e4fd95290e994fd5cac723c3` (`3170c42`)  
**Patch commit deployed:** `0d03318ffb9c3d55c9bc9f335a31765711d30c03` (`0d03318`)  
**Record commit (prior docs):** `3170c42` — `docs: add v6.9L-C public listing sensitive field redaction patch execution record`  
**Branch:** `feature/chat-image-attachment-v1`  
**Deploy/smoke run:** no commit during deploy/smoke; no runtime code changes

> Staging hosting-only deploy + public privacy smoke for v6.9L-C public listing sensitive field redaction (`publicMarketplaceListingPrivacy.ts`). No secrets, tokens, API keys, Authorization headers, raw prompts, real VINs, license plates, phone numbers, emails, or private dealer data in this document. **Not production-ready. Not deploy-ready. Not pilot-ready. Production pilot still NOT READY. Thor Auto real runtime import remains blocked.**

---

## 1. Overall verdict

| Item | Result |
|------|--------|
| Overall result | **PARTIAL** |
| Staging Hosting deploy | **SUCCESS** |
| Live asset verification | **PASS** |
| Guest smoke (`/home`, guest `/profile`) | **PASS** |
| Live public API regression (`GET /api/cars`) | **PASS** |
| Live `vin` / `licensePlate` on staging records | **Not present** — cannot exercise synthetic VIN/plate on live data |
| Synthetic sensitive-field proof | **Static tests** — `test:v69l-c-public-listing-sensitive-field-redaction` **41/41 PASS** |
| `npm run lint` (pre-deploy) | **PASS** |
| `npm run test:v545b3-public-api-privacy` (pre-deploy) | **PASS** (26/26) |
| `npm run test:v69l-c-public-listing-sensitive-field-redaction-patch-execution-record` (pre-deploy) | **PASS** (52/52) |
| `npm run build:staging:hosting` | **PASS** |
| Staging URL | `https://nonga-ce93c.web.app` |
| Live JS asset | `/assets/index-CZrVhuOd.js` |
| Live CSS asset | `/assets/index-v8LQYv3A.css` (unchanged) |
| Cloud Run | **Not deployed / not touched** |
| Production | **Not touched** |
| Commit during deploy/smoke | **NO** |
| Runtime code changes during deploy/smoke | **NO** |
| **Thor Auto real runtime import** | **Still blocked** |
| **v6.9 Controlled Production Pilot** | **NOT READY** |

**Scope note:** PARTIAL applies because live staging inventory does not contain records with `vin` or `licensePlate`. Deploy, live bundle swap, guest regression, and live API absence checks passed. Synthetic sensitive-field removal is proven by static unit tests, not live inventory content.

---

## 2. Baseline verification

| Field | Value |
|-------|-------|
| Repo | `https://github.com/jorradol/nonga.git` |
| Branch | `feature/chat-image-attachment-v1` |
| Current HEAD (at deploy) | `3170c42cc1f2d9b4e4fd95290e994fd5cac723c3` (`3170c42`) |
| Patch commit deployed | `0d03318ffb9c3d55c9bc9f335a31765711d30c03` (`0d03318`) — `fix(privacy): redact sensitive vehicle fields from public listing DTO` |
| Prior execution record | `3170c42` — `docs: add v6.9L-C public listing sensitive field redaction patch execution record` |
| Local HEAD | `3170c42` (= `origin/feature/chat-image-attachment-v1` at deploy time) |
| Working tree (at deploy/smoke time) | clean |
| Working tree (at record time) | clean (docs/script pending separate commit) |
| Commit during deploy/smoke | **NO** |
| Runtime code changes during deploy/smoke | **NO** |
| Staging project | `nonga-ce93c` |
| Staging host | `https://nonga-ce93c.web.app` |

---

## 3. Pre-deploy verification

| Check | Result | Notes |
|-------|--------|-------|
| `git status` | **PASS** | Working tree clean |
| Local HEAD = origin HEAD | **PASS** | `3170c42` |
| `npm run test:v69l-c-public-listing-sensitive-field-redaction` | **PASS** | 41/41 |
| `npm run test:v545b3-public-api-privacy` | **PASS** | 26/26 |
| `npm run test:v69l-c-public-listing-sensitive-field-redaction-patch-execution-record` | **PASS** | 52/52 |
| `npm run lint` | **PASS** | `tsc --noEmit` |
| `npm run build:staging:hosting` | **PASS** | Staging Hosting build path |
| Env source | Cloud Build substitution | No local `.env.staging` on deploy machine |
| Firebase web config guard | **PASS** | `[vite-firebase-guard] PASS production Firebase web config` |
| Dist bundle verify | **PASS** | `PASS dist bundle verified` |

**Built / live assets:**

| Asset | Path |
|-------|------|
| JS bundle | `dist/assets/index-CZrVhuOd.js` |
| CSS bundle | `dist/assets/index-v8LQYv3A.css` |

**Known separate issue:** generic `npm run build` may **FAIL** due to Firebase production config guard / placeholder `apiKey` (documented in prior slices). This slice correctly used `build:staging:hosting` only — **not used as this deploy gate**.

---

## 4. Deploy result

| Item | Value |
|------|-------|
| Command | `npx -y firebase-tools@latest deploy --only hosting --project nonga-ce93c` |
| Result | **SUCCESS** |
| Scope | **Firebase Hosting only** — no functions, no Cloud Run |
| Project | `nonga-ce93c` |
| URL | `https://nonga-ce93c.web.app` |
| Files uploaded | **6 files** from `dist` (**2 new asset files** uploaded) |
| Exit code | **0** — Deploy complete |
| Production target | **Not selected / not touched** |
| Cloud Run deploy | **Not performed** |

**Live bundle references (verified post-deploy):**

| Asset | Before (v6.9J-C) | After (v6.9L-D) |
|-------|------------------|-----------------|
| JS | `/assets/index-CbN1x-wa.js` | `/assets/index-CZrVhuOd.js` |
| CSS | `/assets/index-v8LQYv3A.css` | `/assets/index-v8LQYv3A.css` (unchanged) |

**Background asset check:**

| Check | Result |
|-------|--------|
| Live `index.html` references new JS | **PASS** — `index-CZrVhuOd.js` |
| Live `index.html` references old JS | **NO** — `index-CbN1x-wa.js` not referenced |
| New JS HTTP | **200** |
| CSS HTTP | **200** |
| Old JS still on CDN | **200** — not referenced by live `index.html` |

---

## 5. Public privacy smoke (`GET /api/cars`)

Safe public request only — no credentials, tokens, or session cookies used.

| Check | Result |
|-------|--------|
| HTTP status | **200** |
| Listing count | **10** |
| `vin` on any record | **Absent** — PASS |
| `licensePlate` on any record | **Absent** — PASS |
| `plate` / `registration` | **Absent** |
| `wholesalePrice` / `wholesaleInternalPrice` / `internalPrice` / `cost` / `dealerCost` | **Absent** |
| `duplicateStatus` / `duplicateScore` / `duplicateMatches` | **Absent** |
| `imageMetadata` | **Absent** |
| Non-empty seller contact values (`ownerPhone`, etc.) | **0** — PASS |
| Contact key names in JSON | May appear as empty string `""` — **by design** (v6.9L-C clears contact fields, does not delete keys) |

**Live data limitation:** Staging inventory does not currently store `vin` or `licensePlate` on published records. Live API checks are **regression smoke** (absence + empty contacts), not full synthetic live proof of redaction when those fields exist in source data.

**Primary synthetic proof:** `npm run test:v69l-c-public-listing-sensitive-field-redaction` — **41/41 PASS** at pre-deploy.

---

## 6. Guest smoke

| Check | Result | Notes |
|-------|--------|-------|
| `GET /home` | **PASS** | HTTP 200; home content loads |
| Guest `/profile` RouteGuard | **PASS** | Login gate shown; no crash |
| App crash | **None observed** | |
| Chat / fallback regression | **Not observed** | Home chat CTAs render |

No safe operator session used. No signed-in smoke in this slice.

---

## 7. Reason for PARTIAL verdict

| Factor | Detail |
|--------|--------|
| Live data gap | Staging published listings do not contain `vin` or `licensePlate` fields |
| Synthetic proof path | Static unit tests (`41/41`) prove DTO redaction with synthetic fixtures |
| Live API role | Regression smoke only — confirms keys absent on current inventory |
| Not claimed | Full live proof that redaction strips VIN/plate when present in backend source |

---

## 8. Residual risks

| Risk | Level | Notes |
|------|-------|-------|
| Description sanitizer (pattern-based) | **MEDIUM** | Not verified on live records with embedded sensitive text |
| Live VIN/plate not exercised | **MEDIUM** | PARTIAL gap until test listing with synthetic fields on staging |
| Address in description | **MEDIUM** | Not stripped in v6.9L-C patch |
| `ownerId` remains public | **LOW–MEDIUM** | By current design |
| Unknown future JSON keys | **LOW** | Denylist approach — needs explicit review |
| **Thor Auto real runtime import** | **BLOCKED** | Requires owner gate after this record is committed |

---

## 9. Safety confirmations

| Item | Result |
|------|--------|
| Deploy scope | **Hosting only** (`nonga-ce93c`) |
| Cloud Run | **Not deployed / not touched** |
| Production deploy | **NO** |
| Production changes | **NO** |
| Env / secrets / deploy config | **Unchanged** |
| Auth / login / session / routing | **Not changed** |
| Firestore write | **Not touched** |
| Lead system / DealerLeads | **Not touched** |
| Payment / invoice / boost logic | **Not touched** |
| AI runtime / Gemini / guard / fallback | **Not touched** |
| Secret / token / session / PII logged | **NO** |
| Real VIN / plates / dealer PII in docs | **NO** |
| **Thor Auto runtime import unblocked** | **NO** — remains blocked |
| **v6.9 Controlled Production Pilot** | **NOT READY** |

---

## 10. Remaining gates

| Gate | Status |
|------|--------|
| Live staging listing with synthetic VIN/plate for end-to-end API proof | **Not done** — optional follow-up |
| Thor Auto real runtime import | **Blocked** — owner approval required separately |
| Description address stripping | **Deferred** |
| Signed-in / operator smoke (carried from prior slices) | **Still unresolved** — out of v6.9L-D scope |
| **Production pilot** | **NOT READY** |

---

## 11. Recommended next step

After this execution record is committed and pushed, request **separate owner approval** before:

1. Thor Auto runtime import / onboarding phase
2. Optional: staging test listing with synthetic VIN/plate for live API redaction proof
3. Production pilot planning (still **NOT READY**)

Do **not** unblock Thor Auto real runtime import from this record alone.

---

## 12. v6.9L-D deploy/smoke reference

| Field | Value |
|-------|-------|
| Deployed from HEAD | `3170c42cc1f2d9b4e4fd95290e994fd5cac723c3` |
| Privacy patch included | `0d03318ffb9c3d55c9bc9f335a31765711d30c03` |
| Staging project | `nonga-ce93c` |
| Live JS | `/assets/index-CZrVhuOd.js` |
| Prior live JS | `/assets/index-CbN1x-wa.js` |
| Deploy command | `npx -y firebase-tools@latest deploy --only hosting --project nonga-ce93c` |

---

## 13. This execution record (pending commit)

| File | Purpose |
|------|---------|
| `docs/v6.9L-D-staging-hosting-deploy-public-privacy-smoke-execution-record.md` | This document |
| `scripts/test-v69l-d-staging-hosting-deploy-public-privacy-smoke-execution-record.mts` | Static validation |
| `package.json` | `test:v69l-d-staging-hosting-deploy-public-privacy-smoke-execution-record` |

---

*End of v6.9L-D staging hosting deploy + public privacy smoke execution record.*
