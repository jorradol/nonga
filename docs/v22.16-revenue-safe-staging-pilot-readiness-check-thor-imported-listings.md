# v22.16 - Revenue-Safe Staging Pilot Readiness Check (Thor Imported Listings)

Date: 2026-07-09
Branch: `feature/chat-image-attachment-v1`
Execution type: `CONTROLLED STAGING REVENUE-SAFE PILOT READINESS CHECK / THOR AUTO IMPORTED LISTINGS ONLY / NO PRODUCTION / NO PUBLIC / NO REAL LEAD / NO DEALER-FACING SEND`

## 1) Purpose

Perform a controlled staging readiness review for a revenue-safe pilot using the imported Thor Auto staging listings only, after Step 3 PASS (`v22.14`) and readiness packet (`v22.15`).

This check does **not** start a revenue pilot run, create real leads, enable public signup, deploy production, or send dealer-facing messages.

## 2) Baseline

- Step 3 Thor Auto real CSV staging import core: **complete**
- Prior commit: `97fecea`
- Staging service: `nonga-staging`
- Staging URL: `https://a.nongbot.org`
- Owner-browser import + refresh persistence already confirmed in Step 3

## 3) Exact checks performed

### A) Operator staging API (no owner token)

1. `GET /api/health`
   - `ok: true`
   - `dataBackend: "firestore"`
   - `imageBackend: "file"`
   - `publicSignupEnabled: false`
2. `GET /api/cars` (two fetches)
   - total listings: **13**
   - Thor import listings (`car-import-*`, `dealerId: thor-auto`): **3**
   - brands/models observed (public-sale-safe): MAZDA CX-30 2022, Toyota Camry 2019, Honda CRV 2019
   - all 3 remain after second fetch
3. Public DTO privacy on Thor rows
   - `licensePlateMasked` present
   - `licensePlateFull` key absent
   - `vin` key absent
   - `licensePlate` key absent
   - `ownerPhone` empty when present
4. Image URL probe for Thor stored paths under `/storage/listings/car-import-...`
   - result: **404** from operator probe
   - interpretation: listing metadata persists in Firestore, but local file image backend is not durable across Cloud Run instances
5. Unauthenticated `POST /api/buyer-leads`
   - result: **401**
   - no real lead created in this check

### B) Owner-browser / staging UI (session-safe, no token request)

1. Marketplace list (`/marketplace`)
   - UI shows **13** cars including the 3 Thor imports (MAZDA CX-30, Toyota Camry, Honda CRV)
2. Listing detail (`/cars/car-import-1783556891631-p2`)
   - detail page loads listing data after short load
   - title/specs remain
   - text plate shown as masked form (`6ขธ****`)
   - cover/gallery image may appear when served from a warm instance that still has local files, but operator URL probe returns 404
3. No dealer-facing send action was triggered
4. No real lead creation action was triggered
5. No production/public enable action was performed

### C) Chat card retrieval (staging inventory + local search path)

Scripted readiness check against live staging `/api/cars` inventory:

- query `มี Mazda CX-30 ไหม` → retrieves Thor import card
- query `มี Toyota Camry ปี 2019 ไหม` → retrieves Thor import card
- query `มี Honda CRV ไหม` → retrieves Thor import card
- chat card payload does not include `licensePlateFull` / `vin` / non-empty `ownerPhone`

## 4) Check matrix

| Check | Result | Notes |
|------|--------|-------|
| marketplace list shows imported Thor cars | PASS | 3/3 visible in API + UI |
| listing detail keeps data after refresh | PASS | detail page shows Thor listing data |
| listing detail keeps images after refresh | NEED REVIEW | image URLs exist in listing, but `/storage/listings/...` often 404 under `imageBackend: file` |
| chat card retrieves imported persisted listings | PASS | 3/3 query hits |
| cover/gallery display when Drive access allowed | NEED REVIEW | import stored local paths; durable public image serving not reliable on current staging image backend |
| license plate remains masked (text/API) | PASS | masked field present; full plate key absent; detail text shows masked form |
| full plate not echoed in marketplace/chat public JSON | PASS | no full-plate/VIN echo in public DTO/chat cards |
| no phone/address/VIN/full plate in public listing API fields | PASS | sensitive keys absent/empty on Thor public DTO |
| photo content may still show physical plate | NEED REVIEW | source gallery photo can visually contain a plate even when text fields are masked; treat as image-privacy follow-up, not API field leak |
| no real lead created | PASS | unauthenticated lead create blocked (401); no lead action executed |
| no dealer-facing send triggered | PASS | not exercised |
| public signup remains disabled | PASS | health `publicSignupEnabled: false` |

## 5) Boundary confirmation

- staging only
- no production deploy
- no public enable
- no real lead creation
- no dealer-facing send
- no owner token/header/cookie/secret/env requested
- owner terminal not required
- secrets not written into docs/tests

## 6) Step status

- Step 1: `completed`
- Step 2: `completed`
- Step 3: `completed_thor_csv_staging_import_core`
- Step 3 completed?: `true`
- Step 4: `not_started`
- Revenue-safe staging pilot run: `not_started`
- This packet is readiness check only

## 7) Decision result

`NEED REVIEW — Thor Auto imported listings are visible and chat-retrievable on staging with privacy/lead boundaries intact, but durable cover/gallery image serving under the current file image backend remains unreliable and should be reviewed before revenue-safe pilot execution.`

## 8) Recommended next controlled action (not authorized here)

Separate controlled staging follow-up (fresh authorization required):

- review durable image serving for imported listings on staging (for example durable image backend / storage path consistency)
- keep production/public/real lead/dealer-facing blocked
- do not start revenue pilot run until image readiness is accepted or explicitly waived for a limited pilot
