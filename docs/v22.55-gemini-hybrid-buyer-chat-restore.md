# v22.55 — Existing Gemini Capability Audit + Controlled Staging Hybrid Restore

Date: 2026-07-10  
Branch: `feature/chat-image-attachment-v1`  
Execution type: `EXISTING GEMINI CAPABILITY AUDIT + CONTROLLED STAGING BUYER CHAT RESTORATION / HYBRID DETERMINISTIC INVENTORY GROUNDING + GUARDED GEMINI LANGUAGE REASONING / ACTIVE PILOT PRESERVED / NO TEST LEAD / NO DEALER SEND / NO PRODUCTION`

## Summary

Prior controlled Gemini path was **still present and Staging-flagged ON**, but Owner Corolla availability asks never entered the owner-controlled safe zone, so Buyer chat stayed on deterministic/template composition. Restored hybrid wiring on the **existing** `/api/ai/chat-user-visible-orchestrate` + `salesBrainUserVisibleRealProvider` path (no new AI system, legacy public Gemini remains OFF).

## Phase 1 — Audit findings

| Item | Result |
|------|--------|
| Prior PASS capability | v6.8D allowlist real Gemini + v6.8E.9 structured Thai + v14.2 Thai UX + v13.12 owner-only gates |
| Still present? | **Yes** — bridge, real provider, prompts, guards, fallback |
| Current Buyer runtime | Client `tryOrchestrateChatReply` (deterministic, `skipGemini: true`) → signed-in bridge → optional real Gemini |
| Why Owner scenario missed Gemini | Exact ask `มีรถ … ไหม` did **not** match `detectOwnerControlledGeminiUxZone` → `owner_controlled_zone_not_allowed` |
| Config drift? | Flags already ON Staging; **wiring/zone classifier gap**, not missing provider |
| Real Gemini audience | Owner-only: `admin`/`superadmin` + allowlist (v13.12) — unchanged |
| Legacy `/api/gemini/*` | Fail-closed unless `NONGA_AI_LEGACY_PUBLIC_GEMINI_ENABLED` |

## Hybrid authority (enforced)

**Deterministic:** inventory truth, exact filter/rank (v22.54), Lead slots/modal/consent/Pilot/auth/idempotency.  
**Gemini:** Thai wording/intent explanation over **grounded** cards only; discarded on timeout/unsafe/ungrounded model.  
**Lead-intent with name/contact:** blocked from provider (`deterministic_boundary_blocked`); name redacted in log/provider helper.

## Code changes

- `src/services/ai/salesBrainUserVisibleRealProvider.ts` — zone expand; Lead/PII boundary; exact-ask grounding narrow; ungrounded model guard; exact-ask prompt guidance
- `src/services/ai/salesBrainMock.ts` — name/id redaction
- `scripts/test-v22.55-gemini-hybrid-buyer-chat-restore.mts` + `package.json`

## Tests

| Suite | Result |
|-------|--------|
| `test:v22.55` | PASS (37+) |
| `test:v22.54` | PASS |
| `test:v13.12` | PASS |

## Staging deploy notes

- **Cloud Run required** (server path). Hosting unchanged unless client bundle changes (none in this slice).
- Pilot counter doc absent → aggregate created count **0** before work.
- No test Lead create; Production untouched.
