# v9.4 Admin-Only Runtime Proof Smoke Harness - No Real Gemini Yet

> **SMOKE HARNESS ONLY / NO REAL GEMINI / NO RUNTIME ACTIVATION**
> v9.4 เพิ่ม smoke harness/proof checker เพื่อยืนยันว่า safety guard ของ admin-only runtime proof ยังทำงานครบถ้วน

---

## 1. Purpose

ยืนยันว่า skeleton + provider wiring + kill switch + fallback + log redaction + quota/cost guard ทำงานตามที่ออกแบบ โดยยังไม่แตะ Gemini จริง

- default OFF
- kill switch protected
- provider wiring placeholder-only
- no real Gemini call
- no network/provider call
- no deploy
- no production
- no public route
- no buyer-facing AI
- no user-visible AI
- no real lead sending
- no real secret/API key in repo

## 2. Smoke Harness Coverage

smoke harness ตรวจประเด็นหลักต่อไปนี้:

- admin-only boundary remains (`/api/admin` + `adminApiAuth`)
- disabled by default
- kill switch wins over all flags
- provider remains placeholder-only (`effectiveProviderEnabled=false`)
- deterministic fallback remains source of truth
- logging redaction strips token/phone/VIN-like text
- quota/cost guard exists and blocks unsafe state
- buyer path cannot access runtime proof wiring/path

## 3. Non-Activation Confirmation

- no `GoogleGenAI`
- no `generateContent`
- no `fetch` network calls
- no real provider invocation
- no runtime activation
- no Gemini activation

## 4. Source of Truth

- deterministic flow remains source of truth
- deterministic fallback remains source of truth

