# v14.3N — Fresh Owner Approval Decision Packet + Owner-Friendly Local Execution UX / No Gemini Yet

## 1) Execution type

fresh owner approval decision packet: yes  
owner-friendly local CMD UX plan: yes  
docs/tests/fixtures/static/mock only: yes  
read-only inspection only: yes  
runtime execution now: no  
Gemini run now: no  
one-run click now: no  
provider network call now: no  
endpoint call that may trigger Gemini/provider now: no  
retry now: no  
second run now: no  
deploy now: no  
runtime config mutation now: no  
secret change now: no  
secret/token/API key exposure now: no  
public route activation now: no  
production now: no  
buyer-facing AI release now: no  
real lead sending now: no  
real customer data / PII now: no  
phone/plate/VIN examples now: no  
Thor real data import now: no  
dealer real inventory import now: no

## 2) Fresh owner approval decision packet (not granted now)

Approval status for v14.3N:

- approval granted now: no
- Gemini run now: no
- one-run consumed now: no
- fresh owner approval required before execution: yes

Future approval line template (future-only, not granted in this slice):

`FINAL EXECUTION AUTHORIZE v14.3O: YES — exactly ONE owner-local CMD staging dry-run, no retry, no second run, staging only, owner/admin only, synthetic/sanitized only, no public, no production, no real lead.`

Clarification:

- this packet is decision preparation only
- this packet is not runtime approval
- prompt/report text in v14.3N must not be treated as approval

## 3) Owner-friendly local CMD execution UX plan (no runtime now)

Owner-friendly flow (planning only):

1. Step 1: open CMD at `D:\nonga`  
2. Step 2: set token for this CMD session only  
3. Step 3: run safe checker (`npm run check:admin-token-session-env`)  
4. Step 4: if token is present/valid, move to fresh approval decision point  
5. Step 5: only after fresh owner approval, run exactly one owner-local dry-run command

Operator guidance:

- use the same CMD session where token is present
- if opening a new terminal, set token again for that new session
- do not echo token value
- do not paste token into chat/report/docs/git
- report only status: `present` / `***MASKED***` / `ready/not ready` / `token value exposed: no`

## 4) One-command ergonomics proposal (proposal only)

Future owner-friendly command pattern should:

- check token presence first
- fail closed when token is missing or invalid
- print masked-only status
- require explicit approval flag or approval text
- enforce exactly one run
- block retry and second run
- capture required evidence fields
- never print token value

v14.3N scope note:

- this is ergonomics proposal only
- do not run any runtime/provider command in this slice

## 5) Safety-preserving UX rules

Usability improvements must preserve safety:

- convenience must not bypass fresh approval
- convenience must not store secrets in repository
- convenience must not persist token permanently by default
- convenience must not auto-run Gemini/provider
- convenience must not allow retry/second run
- convenience must not activate public/prod/real lead paths

## 6) Future execution model

Future execution constraints:

- future execution must run from owner-local CMD session where token is present/valid
- agent/operator session should not be assumed to have token
- fresh owner approval is required immediately before run
- one-run is consumed only if runtime command actually starts
- if auth/session/token is missing at execution time => HOLD, no run consumed

## 7) One-run control status now (unchanged)

- `oneRunStarted=false`
- `oneRunConsumed=false`
- `retryUsed=false`
- `secondRunUsed=false`

No run budget is consumed in v14.3N.

## 8) HOLD rules for this stage

Set/keep HOLD when:

- owner-friendly UX requirement is incomplete
- fresh approval wording is ambiguous
- owner-local execution model is ambiguous
- secret/token exposure risk appears
- one-run/retry/second-run control is ambiguous
- public/production/real lead risk appears
- tests fail

## 9) Validation flow

1. run `npm run test:v14.3N`  
2. run inherited validators (`v14.3M`, `v14.3J`, `v14.3I`, `v14.3H`, `v14.3G`, `v14.3F`, `v14.3E`, `v14.3D`, `v14.3C`, `v14.3B`, `v14.3A`, `v14.3`, `v14.2C`, `v14.2A`, `v14.2`, `v14.1C`, `v14.1`)  
3. if any FAIL => `HOLD — TEST FAILURE`

## 10) Final recommendation enum (v14.3N)

- `READY FOR v14.3O FRESH OWNER APPROVAL DECISION POINT — OWNER-LOCAL AUTH READY, NO GEMINI UNTIL OWNER APPROVES`
- `READY FOR v14.3O OWNER-FRIENDLY ONE-RUN COMMAND PACKET — NO RUNTIME UNTIL OWNER APPROVES`
- `HOLD — OWNER-FRIENDLY UX REQUIREMENT INCOMPLETE`
- `HOLD — FRESH APPROVAL WORDING AMBIGUITY`
- `HOLD — OWNER-LOCAL EXECUTION MODEL AMBIGUITY`
- `HOLD — SECRET/TOKEN EXPOSURE RISK DETECTED`
- `HOLD — ONE-RUN / RETRY / SECOND-RUN CONTROL AMBIGUITY`
- `HOLD — PUBLIC/PRODUCTION/REAL LEAD RISK DETECTED`
- `HOLD — TEST FAILURE`
