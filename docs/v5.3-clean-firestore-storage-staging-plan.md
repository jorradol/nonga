# V5.3C Clean Firestore/Storage Staging Rehearsal Plan

Date recorded: Tuesday May 26, 2026

This runbook replaces the earlier "migrate old file backend data" direction for V5.3. The new direction is to keep old noisy/test file data out of Firestore and Firebase Storage, switch staging to clean durable backends only after readiness fixes, and create fresh test data through the real web UI.

This document does not include secrets, passwords, tokens, API keys, service account JSON, or private user identifiers.

## Decision

Do not migrate old file-backend data into Firestore or Firebase Storage.

Reasons:

- The current file backend contains a large amount of staging/test/noisy data.
- Prior dry-run found missing dealer IDs, missing local image files, and many orphan image folders.
- Importing old noisy data would make Firestore/Storage harder to validate and clean up.
- The better staging rehearsal is a clean durable backend with new data created through actual user flows.

The V5.3B migration mapping/reporting tooling remains useful for audits, exports, and future cleanup, but it is not the next path for staging write rehearsal.

## Current State

- Current staging URL: `https://nonga-ce93c.web.app`
- Current staging service: `nonga-staging`
- Current Firebase project: `nonga-ce93c`
- Current data backend: `NONGA_DATA_BACKEND=file`
- Current image backend: `NONGA_IMAGE_BACKEND=file`
- Public signup: disabled
- Production: untouched
- GitHub branch: `feature/chat-image-attachment-v1`

The v5.1/v5.2 core staging flows passed on file/file:

- Role auth for member, dealer, admin, and superadmin.
- Dealer create/save Draft.
- Publish guard.
- Chat-to-Draft.
- New image upload.
- Marketplace display.
- `/api/health`.

## Target State

Clean staging durable backend:

- `NONGA_DATA_BACKEND=firestore`
- `NONGA_IMAGE_BACKEND=firebase-storage`
- `VITE_NONGA_PUBLIC_SIGNUP_ENABLED=false`
- No old file-backend records imported.
- Fresh staging test records created through the real UI.
- Data and images remain available after Cloud Run redeploy/restart.

Production remains untouched.

## Required Env Flags

Required for staging backend switch rehearsal:

- `NODE_ENV=production`
- `APP_URL=https://nonga-ce93c.web.app`
- `FIREBASE_PROJECT_ID=nonga-ce93c`
- `FIREBASE_STORAGE_BUCKET` for the staging Firebase project
- `NONGA_DATA_BACKEND=firestore`
- `NONGA_IMAGE_BACKEND=firebase-storage`
- `VITE_NONGA_PUBLIC_SIGNUP_ENABLED=false`

Required secret refs:

- `GEMINI_API_KEY`
- `FIREBASE_SERVICE_ACCOUNT_JSON`

Do not print secret values. Do not add secret values to repo files.

## Readiness Audit

### Ready

- Repository layer exists:
  - `src/server/repositories/inventoryRepository.ts`
  - `FileInventoryRepository`
  - `FirestoreInventoryRepository`
  - `createInventoryRepository()`
  - `resolveInventoryDataBackend()`
- Image repository layer exists:
  - `src/server/repositories/imageStorageRepository.ts`
  - `FileImageStorageRepository`
  - `FirebaseStorageImageRepository`
  - `createImageStorageRepository()`
  - `resolveImageStorageBackend()`
- Upload helpers already use the image repository:
  - `src/server/pasteUploadedImageStorage.ts`
  - `src/server/listingImageUploadBody.ts`
- Existing repository tests cover the Firestore/Storage-ready layers:
  - `npm run test:v50-firestore-repository`
  - `npm run test:v50-image-storage-repository`

### Not Yet Ready For Switch

Several core API paths still call file-backed modules directly instead of the inventory repository:

- `src/server/dealerPortalRoutes.ts`
  - Draft create/list/update/delete still uses `dealerDraftInventory`.
  - Dealer inventory list/edit/delete still uses `marketplaceInventory`.
  - Draft publish calls `publishDealerDraftToMarketplace()`, which is currently file-backed.
  - Dealer import commit calls `processSmartInventoryImport()`, which is currently file-backed.
- `src/server/publishDraftListing.ts`
  - Reads draft from file backend.
  - Copies local image files with `migrateListingImagesToCarId()`.
  - Writes published listing through `addMarketplaceCar()`.
  - Deletes draft through `removeDealerDraft()`.
- `src/server/ownerListingRoutes.ts`
  - Owner listing edit/visibility/delete still uses `marketplaceInventory`.
- `src/server/ownerListingAccess.ts`
  - Owner listing scope lookup still uses file-backed marketplace access helpers.
- `src/server/inventoryImportCommit.ts`
  - Bulk/import commit still uses file-backed marketplace/draft modules.
  - Remote image download still uses local file image helpers.
- `server.ts`
  - `GET /api/cars` and `POST /api/cars` still use file-backed marketplace functions.
  - `/storage/listings` still serves local file images.

Conclusion: image uploads are closer to Firebase Storage readiness than listing/draft data routes. A clean switch should not be deployed until the core dealer listing/draft/publish routes are wired through the repository layer or explicitly tested behind a narrow staging-only route path.

## V5.3D Code Readiness Update

V5.3D adds the minimum repository wiring needed before requesting a clean staging backend switch:

- Dealer Portal draft create/list/update/delete is routed through `createInventoryRepository()`.
- Dealer Portal inventory list/update/visibility/delete is routed through `createInventoryRepository()`.
- Dealer Portal draft publish is routed through `InventoryRepository.publishDraft()`.
- `POST /api/cars` and `GET /api/cars` use the inventory repository for create and public marketplace reads.
- `publishDraftListing.ts` no longer assumes local file image copies when `NONGA_IMAGE_BACKEND=firebase-storage`; Storage URLs are preserved instead.
- Published listings can preserve draft `imageMetadata` for Firebase Storage-backed images.
- `registerDealerPortalRoutes()` accepts an injected repository so tests can cover Firestore mode without touching real Firebase.
- Focused readiness test: `npm run test:v53-clean-storage-readiness`.

V5.3D intentionally keeps these areas out of scope:

- Owner listing routes still need a full repository-backed pass before owner/member editing is part of the clean switch rehearsal.
- Admin import and dealer import commit still use file-backed bulk import modules and local image download helpers.
- Duplicate review/rescan services still use current file-backed duplicate corpus.
- Historical migration remains out of scope; old file-backed records should not be imported into clean staging.

Clean staging can be requested only for the minimal dealer flow after reviewing these remaining exclusions.

## Safe Staging-Only Switch Plan

Phase 0: no write, no deploy

1. Keep staging on file/file.
2. Confirm health:
   - `ok=true`
   - `dataBackend=file`
   - `imageBackend=file`
   - `publicSignupEnabled=false`
3. Confirm GitHub branch is pushed and working tree is clean.
4. Confirm Firestore and Storage rules are ready for emulator/staging validation.

Phase 1: code readiness fix, still no deploy

1. Dealer draft/listing operations through `createInventoryRepository()` are required.
2. Draft publish through `InventoryRepository.publishDraft()` is required.
3. Publish from draft must not call local `migrateListingImagesToCarId()` when Storage is active.
4. Owner listing edit/visibility/delete remains out of scope unless a separate repository-backed pass is completed.
5. Admin import/bulk import remains out of scope unless a separate repository-backed pass is completed.
6. Focused tests for Firestore-backed draft create/list/update/publish and Storage URL preservation are required.

Phase 2: staging deploy only after approval

1. Deploy Cloud Run staging only with:
   - `NONGA_DATA_BACKEND=firestore`
   - `NONGA_IMAGE_BACKEND=firebase-storage`
   - public signup still false
2. Do not deploy production.
3. Do not run migration.
4. Do not import old file data.

Phase 3: create fresh data through UI

1. Login as the staging dealer.
2. Create a new draft from Dealer create listing.
3. Upload at least one new image.
4. Save draft.
5. Publish listing.
6. Edit listing fields.
7. Upload/replace image after publish if supported.
8. View listing in Marketplace.
9. Confirm member/admin/superadmin role boundaries still hold.

Phase 4: durability verification

1. Record the created staging listing/draft IDs and Storage URLs in a private test note, not in committed docs if they contain private data.
2. Redeploy/restart Cloud Run staging with the same firestore/firebase-storage flags.
3. Confirm the listing still appears in Dealer Portal and Marketplace.
4. Confirm images still render from Firebase Storage URLs.
5. Confirm `/api/health` reports:
   - `dataBackend=firestore`
   - `imageBackend=firebase-storage`
   - `publicSignupEnabled=false`

## Firestore/Storage Clean Data Checklist

Before switch:

- Confirm old file data will not be migrated.
- Confirm `dealerListings` and `dealerDrafts` are empty or contain only intentionally created staging test records.
- Confirm old noisy file records are not copied.
- Confirm Storage paths are empty or contain only intentional staging test images:
  - `listing-images/{dealerId}/{listingId}/...`
  - `draft-images/{dealerId}/{draftId}/...`
- Confirm test dealer membership exists and is active for the target staging dealer.
- Confirm admin/superadmin test profiles are active.

After switch:

- Create one draft through UI.
- Create one published listing through UI.
- Verify Firestore doc `dealerId` is the expected staging dealer.
- Verify Storage object paths include the expected staging dealer ID.
- Verify no unrelated dealer data is visible.
- Verify no test writes happen to production.

## Manual Web UI Smoke Checklist

Run on `https://nonga-ce93c.web.app` after staging switch approval and deploy:

- Member login:
  - Can browse Marketplace.
  - Cannot access Dealer Portal.
  - Cannot access Admin.
- Dealer login:
  - Can open Dealer Portal.
  - Can create draft.
  - Can upload image.
  - Can save incomplete draft.
  - Can complete required fields.
  - Can publish listing.
  - Can edit listing.
  - Can view listing in Marketplace.
  - Cannot access another dealer scope.
- Admin login:
  - Can access admin pages intended for staging ops.
  - Member/dealer remain blocked from admin.
- Superadmin login:
  - Does not fall back to guest.
  - Can access admin-level routes.
- Chat-to-Draft:
  - Can create a fresh draft.
  - Missing checklist still includes publish requirements.
  - Uploaded image persists through Storage.
- Health:
  - `ok=true`
  - `dataBackend=firestore`
  - `imageBackend=firebase-storage`
  - `publicSignupEnabled=false`

## Rollback Plan

Rollback target:

- `NONGA_DATA_BACKEND=file`
- `NONGA_IMAGE_BACKEND=file`
- public signup remains false

Rollback steps:

1. Redeploy Cloud Run staging with file/file flags and previous known-good image if needed.
2. Do not delete Firestore/Storage test data until it has been reviewed.
3. Keep Firebase Hosting unchanged unless frontend assets were separately changed.
4. Confirm health returns:
   - `dataBackend=file`
   - `imageBackend=file`
   - `publicSignupEnabled=false`
5. Re-run dealer draft/publish/image smoke on file/file if rollback is used.

## No-Production Guards

For V5.3C:

- No production deploy.
- No migration `--write`.
- No `--confirm-staging`.
- No public signup enablement.
- No production backend flag changes.
- No Firestore/Storage rules deploy without explicit approval.
- No old file data import.
- No secrets in docs, logs, code, or committed files.

## Success Criteria

V5.3C planning is complete when:

- This runbook is committed.
- Readiness gaps are documented.
- Verification passes on the current file/file baseline.
- No deploy or backend flag change has happened.

Clean switch is ready to request approval only after:

- Dealer draft/listing/publish routes are repository-backed or explicitly narrowed to a safe test path.
- Image upload paths are verified with `NONGA_IMAGE_BACKEND=firebase-storage`.
- Firestore/Storage rules are validated for dealer isolation.
- A rollback path to file/file is rehearsed or operationally clear.

## Recommended Next Prompt

```text
Proceed with V5.3C code readiness fix only. Do not deploy. Do not change staging flags. Wire the minimal dealer draft/listing/publish paths through createInventoryRepository() for Firestore readiness, keep image upload through createImageStorageRepository(), add focused tests, and report deploy scope separately before any staging switch.
```
