# v22.19 - Staging Thor Duplicate Cleanup Plan (13 → 16)

Date: 2026-07-09  
Scope: **staging only** — do not touch production  
Execution type: controlled operator cleanup after upsert fix deploy

## 1) Incident summary

Owner re-ran Thor Auto CSV Confirm Import in the browser after seller image consent + Firebase Storage deploy.

| Metric | Value |
|--------|-------|
| Marketplace count before | 13 |
| Marketplace count after | 16 |
| Suspected new duplicates | **+3** Thor listings |
| Symptom on newest 3 | no images in marketplace |
| Intended outcome (missed) | update existing 3 Thor rows with durable Firebase Storage image URLs |

## 2) Why automatic delete is blocked

Matching for the already-created duplicates may be ambiguous until operator inspects:

- New rows have **new** `car-import-{timestamp}-*` ids
- Fingerprints may collide with both the original Thor rows and each other
- Image fields on duplicates may be placeholder / empty / wrong id paths

**Rule:** do not delete if matching is ambiguous. Prefer merge into the **oldest** original Thor listing, then remove the duplicate only when clearly identified.

## 3) Identification procedure (staging, operator-controlled)

1. List Thor Auto dealer listings from staging Firestore `dealerListings` where `dealerId == "thor-auto"` (or equivalent admin inventory view).
2. Group by fingerprint: `brand + model + year + color + mileage + price` (and plate/VIN if privately present).
3. For each group with count ≥ 2:
   - **Canonical keeper** = oldest `createdAt` (prefer pre-reimport ids).
   - **Duplicate candidates** = newer `car-import-*` rows created at the re-import timestamp window.
4. Confirm each duplicate pair with at least one of:
   - exact full plate match (private field), or
   - exact VIN, or
   - exact fingerprint unique within dealer after excluding other groups
5. If more than one plausible keeper → **HOLD** that group; mark NEED REVIEW; do not delete.

**Do not log or paste full plate / VIN / phone / address into tickets or public docs.** Refer by listing id + brand/model/year only in audit notes.

### 3A) Staging pairs identified (2026-07-09 public marketplace read — no PII)

Matching is **clear** (same brand/model/year/price; no ambiguity). Prefer merge durable images into oldest, then hide newest.

| Vehicle (public) | Canonical (older, ephemeral images) | Duplicate (newer, durable Firebase images HTTP 200) |
|------------------|-------------------------------------|-----------------------------------------------------|
| Honda CRV 2019 @ 161392 | `car-import-1783556891631-p0` | `car-import-1783565788477-p0` |
| Toyota Camry 2019 @ 120384 | `car-import-1783556891631-p1` | `car-import-1783565788477-p1` |
| MAZDA CX-30 2022 @ 87421 | `car-import-1783556891631-p2` | `car-import-1783565788477-p2` |

Expected marketplace count after hide/remove of the 3 duplicates: **13** (assuming no other unrelated adds).

## 4) Preferred merge then remove

For each clearly identified pair `(canonicalId, duplicateId)`:

1. Copy any **working** durable Firebase Storage image URLs from duplicate → canonical **only if** canonical still has ephemeral `/storage/listings/...` or empty/placeholder images.
2. Merge any newer valid CSV fields (price/mileage/description) into canonical using blank-safe rules (same as import upsert).
3. Set duplicate metadata on duplicate: `duplicateStatus: merged`, `duplicateCanonicalId: canonicalId`, `listingStatus: hidden` (or delete only after hide verified).
4. Prefer **hide + merge metadata** first; hard-delete only after marketplace count and chat retrieval look correct.
5. Record audit line (below).

After upsert fix is deployed, a **future** owner re-import should update canonical rows in place (count stable). Cleanup of the existing +3 should still be done once so count returns toward 13 (or the true unique Thor set).

## 5) Audit note template (fill after cleanup)

| Field | Value |
|-------|-------|
| Staging project | `nonga-ce93c` (staging) |
| Operator | _(name)_ |
| Date | _(ISO)_ |
| Canonical ids kept | `car-import-…` (oldest) |
| Duplicate ids hidden/removed | `car-import-…` (newest re-import window) |
| Match basis | fingerprint / plate / VIN _(no raw PII in this doc)_ |
| Images merged? | yes/no — durable URL present on keeper? |
| Marketplace count after | _(expected ~13 if only +3 were dups)_ |
| Production touched? | **no** |
| Real lead created? | **no** |
| Dealer-facing send? | **no** |

### Audit log (executed actions)

_Identification recorded in v22.19; merge/hide script prepared but **not executed** in this agent session (no Application Default Credentials for Firestore write)._

| When | Action | Why |
|------|--------|-----|
| 2026-07-09 | Identified 3 clear Thor duplicate pairs (ids above); durable images live on newer batch (HTTP 200) | Owner reported 13→16; public API confirms 6 Thor import rows = 3+3 |
| 2026-07-09 | Added `scripts/run-v22.19-staging-thor-duplicate-cleanup.mts` | Operator-controlled staging merge: copy durable `images[]` → older canonical, hide newer with `duplicateStatus:merged` |
| — | Operator runs cleanup script with staging ADC | Required before owner re-import |
| — | Confirm marketplace count ≈13 | Cleanup complete gate |

## 6) Owner-safe path (no terminal / no token)

1. Operator deploys upsert fix to **staging only**.
2. Operator (not owner) identifies and merges/hides the 3 duplicates per this plan.
3. Only then ask owner to re-run Confirm Import once in the browser to verify:
   - count does not increase
   - correct cars update
   - images show
   - chat sees updated listings
   - no duplicate Thor cars remain

## 7) Boundaries

- staging only
- no production
- no public enable
- no real lead
- no dealer-facing send
- no secrets in repo/report/log
- do not ask owner for token/env/terminal work
