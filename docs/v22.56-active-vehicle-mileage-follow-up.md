# v22.56 — Active Vehicle Conversation Context + Mileage Follow-up Bugfix

Date: 2026-07-10  
Branch: `feature/chat-image-attachment-v1`  
Execution type: `CONTROLLED STAGING ACTIVE-VEHICLE CONVERSATION CONTEXT + MILEAGE FOLLOW-UP BUGFIX / EXISTING GUARDED GEMINI HYBRID / OWNER-ALLOWLIST PATH / NO TEST LEAD / NO DEALER SEND / NO PRODUCTION`

## Summary

Owner-browser v22.55 found one remaining defect: after an exact Corolla 2020 search and family-use follow-up, asking `ไมล์ 88,000 เยอะไปไหม` returned “ช่วยกดดูรายละเอียดรถ…” even though the grounded Corolla (mileage 88,000) was already the active conversation vehicle.

## Root cause

1. Mileage evaluation was **not** classified as a pilot follow-up (unlike family/`ครอบครัว`), so the server bridge re-ran the client orchestrator.
2. `/ไมล์/` matched bare **facts Q&A** (`specField`) which required an explicit pronoun / card click for session fallback → `BUYER_ASK_SELECT_CAR_FIRST`.
3. Gemini zone regex required `ไมล์` immediately followed by `เยอะ|…`, so `ไมล์ 88,000 เยอะไปไหม` never opened `car_fit_reason`.
4. Exact single-result search saved card context but did **not** set `lastSelectedCarId` without a card click.

## Fix (reuse existing mechanisms)

| Area | Change |
|------|--------|
| Pilot follow-up | `isPilotBuyerMileageFollowUp` → same session-first path as family |
| Gemini zone | Allow digits/punctuation between `ไมล์` and judgment words |
| Facts / orchestrator | Grounded mileage evaluation before select-car trap; mileage fact match; ambiguity clarification |
| Active vehicle | Exact single-result search + single-card assistant message sets `lastSelectedCarId` |
| Deterministic fallback | `buildMileageEvaluationReply` / pilot copy with annual estimate + condition caveats |
| Advisor | Grounded mileageGeneral uses listing mileage when a target car exists |

No new AI architecture, no Lead create, Pilot controls unchanged, Production untouched.

## Tests

| Suite | Result |
|-------|--------|
| `test:v22.56` | PASS (43) |
| `test:v22.55` | PASS |
| `test:v22.54` | PASS |

## Staging deploy

| Item | Value |
|------|--------|
| Image | `…/nonga-staging:v22.56-active-vehicle-def8799` |
| Cloud Build | `04bba775-13d1-46ec-a112-63c3f6cd39e8` SUCCESS |
| Cloud Run revision | **`nonga-staging-00203-d66`** @ 100% |
| Prior revision | `nonga-staging-00202-lt5` |
| Hosting asset | **`assets/index-Nw6XxJ4n.js`** |
| Prior Hosting | `assets/index-DIreM_mc.js` |
| Commit | `def8799` |
| Health | ok; Capture ON; Pilot active max 3; Signup OFF |
| Orchestrate unauth | 401 |
| Marketplace | 15; Corolla 2020 mileage 88,000 images 5/5; Corolla 2021 images 5/5 |
| Pilot count | 0 (counter doc absent) before/after |
| Production | Untouched |

## Final recommendation

**NEED REVIEW** — code/tests/Staging deploy complete; Owner-browser quality recheck of the mileage follow-up is still required for PASS.

Signed-in Admin/Owner allowlist on Staging, same chat thread:

1. `มีรถ โตโยต้า Corolla 2020 ไหม` → Corolla 2020 grounded
2. `คันนี้เหมาะกับใช้ครอบครัวไหม` → useful family guidance (no reselect)
3. `ไมล์ 88,000 เยอะไปไหม` → grounded mileage judgment; **must not** ask to กดดูรายละเอียดรถก่อน
4. `เทียบกับ Corolla 2021 ให้หน่อย` → compare retains base + 2021

Do not submit Lead. Do not request Owner token/terminal work from the agent.
