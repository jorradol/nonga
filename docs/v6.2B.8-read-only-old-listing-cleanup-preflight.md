# v6.2B.8 — Read-only Old Listing Cleanup Preflight

**สถานะ:** **READ-ONLY PREFLIGHT (REDACTED RECORD)** — docs/tests/package only — **ยังไม่ cleanup จริง**, **ยังไม่ import**, **ยังไม่ deploy**  
**วันที่เอกสาร:** 2026-06-09  
**Git baseline:** `14907c6dbc546f6876f2055b6d9b6c6f72394d66` (v6.2B.7 pushed)  
**Branch:** `feature/chat-image-attachment-v1`  
**อ้างอิง:** v6.2B.7 Cleanup/Archive Plan, v6.2B.6 Redacted GO Record, G-11

> **v6.2B.8 = read-only preflight record แบบ redacted summary เท่านั้น** — query/count จริงทำบน staging **นอก repo**; git เก็บ **counts + status เท่านั้น** ไม่เก็บข้อมูลรายคัน  
> **Existing old staging listings ยังไม่ถูกลบ/ซ่อน** — ไม่มี Firestore writes ใน slice นี้

---

## สารบัญ

1. [Preflight Metadata](#1-preflight-metadata)
2. [Old Listing Count Summary (Redacted)](#2-old-listing-count-summary-redacted)
3. [Candidate Criteria Review](#3-candidate-criteria-review)
4. [Safety Risk Summary](#4-safety-risk-summary)
5. [Cleanup Readiness Recommendation](#5-cleanup-readiness-recommendation)
6. [Import Gate](#6-import-gate)
7. [Forbidden in Repo (Preflight Rules)](#7-forbidden-in-repo-preflight-rules)
8. [Compliance (v6.2B.8)](#8-compliance-v62b8)

---

## 1. Preflight Metadata

| Field | Value |
|-------|-------|
| project | **staging only** (`nonga-ce93c`) — production **excluded** |
| branch | `feature/chat-image-attachment-v1` |
| head / origin (baseline) | `14907c6dbc546f6876f2055b6d9b6c6f72394d66` |
| working tree | **clean / synced** (at doc authoring) |
| preflight mode | **read-only only** |
| Firestore writes | ❌ **none** |
| cleanup executed | ❌ **none** |
| import executed | ❌ **none** |
| deploy / env update | ❌ **none** |
| recordType | `redacted-preflight-summary` |

**read-only only** — no cleanup / import / deploy in v6.2B.8

---

## 2. Old Listing Count Summary (Redacted)

Counts below are **git-safe placeholders** — actual read-only query results live in **ops secure channel** only.

| Metric | Git record | Notes |
|--------|------------|-------|
| totalVisiblePublished | `N_total` | marketplace-visible published count (ops query) |
| candidateOldTest | `N_candidate` | matches v6.2B.7 criteria §3 |
| unknownNeedsReview | `N_review` | ambiguous — manual ops review |
| protectedNewRealBatch | **0** | real pilot batch **not imported yet** — must stay **0** |
| per-car details in git | ❌ **forbidden** | no make/model/price/plate in repo |

### Git template default (before ops read-only query)

| Metric | Placeholder |
|--------|-------------|
| totalVisiblePublished | `PENDING` |
| candidateOldTest | `PENDING` |
| unknownNeedsReview | `PENDING` |
| protectedNewRealBatch | **0** |

**ห้ามบันทึกข้อมูลรายคัน** — counts only in git

---

## 3. Candidate Criteria Review

Criteria from v6.2B.7 — used for **counting only**, not exposing per-car data in git:

| Criterion | Used for count | Per-car in git |
|-----------|----------------|----------------|
| `createdBefore` | ✅ cutoff filter | ❌ forbidden |
| `sourceTag` | ✅ mock/test/legacy tags | ❌ forbidden |
| `pilotBatchId` | ✅ exclude future real batch | ❌ forbidden |
| `ownerUid` | ✅ masked ref in ops | ❌ **no raw UID in git** |
| `status` | ✅ published/hidden/draft | ❌ forbidden |
| `test/demo marker` | ✅ explicit test flag | ❌ forbidden |

**criteria ใช้เพื่อ count เท่านั้น ไม่ expose รายคัน**

Read-only preflight on staging: aggregate queries + criteria filters — results as counts in ops channel

---

## 4. Safety Risk Summary

Reference checks — **count/status only**, no mutation:

| Risk area | Git record | Rule |
|-----------|------------|------|
| lead references | `count_or_none` / `PENDING` | **no mutation** |
| reveal references | `count_or_none` / `PENDING` | **no mutation** |
| outcome references | `count_or_none` / `PENDING` | **no mutation** |
| payment references | `count_or_none` / `PENDING` | **no mutation** |
| settlement references | `count_or_none` / `PENDING` | **no mutation** |

| Risk status | Meaning |
|-------------|---------|
| **none** | no blocking references on candidate set |
| **present** | references exist — **FIX REQUIRED** before cleanup |
| **PENDING** | ops read-only check not yet recorded |

**ห้าม mutate reference ใด ๆ** — read-only preflight only

If reference risk **present** → mark **FIX REQUIRED** before cleanup approval

---

## 5. Cleanup Readiness Recommendation

| Value | Meaning |
|-------|---------|
| **READY FOR CLEANUP APPROVAL** | counts OK, no reference risk, criteria clear — may request cleanup phrase |
| **FIX REQUIRED** | reference risk or ambiguous candidates — resolve before cleanup |
| **NO-GO** | unsafe to proceed — stop |
| **PENDING** | read-only preflight not yet executed on staging (git template default) |

**Git template default:** `PENDING`

### Cleanup approval (required before real cleanup)

> **อนุมัติให้ cleanup/archive old staging listings บน staging เท่านั้น แบบ controlled rollback ได้ ตาม v6.2B.7**

| Item | Status |
|------|--------|
| v6.2B.8 preflight | read-only record only |
| Cleanup execution | ❌ **not in v6.2B.8** |
| Cleanup approval phrase | **required separately** — not granted here |

---

## 6. Import Gate

| Item | Status |
|------|--------|
| Real stock import | ❌ **still blocked** — G-11 |
| Import before cleanup preflight | ❌ **wrong order** — preflight → cleanup approval → cleanup → import approval |
| v6.2B.6 GO record | ✅ ops data package GO — **not import approval** |

### Import approval (unchanged — separate, after cleanup path)

> **อนุมัติให้ import real stock แบบ controlled pilot บน staging เท่านั้น ตาม v6.2B ชุดเล็ก 5–10 คัน**

| Requirement | Status |
|-------------|--------|
| Cleanup preflight (v6.2B.8) | planning/record slice |
| Cleanup execution + approval | **before** import |
| Import approval phrase | **required separately** — not granted in v6.2B.8 |
| Import execution record | required after import approval |

**import ต้องรอ cleanup preflight และ approval แยก**

---

## 7. Forbidden in Repo (Preflight Rules)

| Forbidden in git | Rule |
|----------------|------|
| Per-car listing details | ❌ |
| make/model/price | ❌ |
| Full plate (e.g. `กข 1234`) | ❌ |
| Raw image URL | ❌ |
| Phone / LINE / address | ❌ |
| Raw UID | ❌ |
| Prompt dump / secret / env dump | ❌ |
| Firestore write commands | ❌ |
| Cleanup/delete/hide execution | ❌ |

---

## 8. Compliance (v6.2B.8)

| Item | Status |
|------|--------|
| Preflight metadata §1 | ✅ |
| Redacted count summary §2 | ✅ |
| Candidate criteria §3 | ✅ |
| Safety risk summary §4 | ✅ |
| Cleanup recommendation §5 | ✅ |
| Import gate §6 | ✅ |
| Read-only only | ✅ |
| Firestore writes | ❌ none |
| Cleanup/delete/hide executed | ❌ none |
| Hard delete | ❌ none |
| Real stock import | ❌ blocked |
| Deploy / env / Firestore rules deploy | ❌ none |
| Secret access | ❌ not done |
| Payment / lead / reveal / outcome mutation | ❌ none |
| Public signup / public AI | ❌ not enabled |
| Real Gemini user-visible path | ❌ not enabled |
| Image processing / AI vision API | ❌ not in v6.2B.8 |

CI: `npm run test:v62b8-read-only-old-listing-cleanup-preflight` — readFileSync only.

---

## Redacted Preflight Summary (for report — git-safe only)

```text
v6.2B.8 Read-only Old Listing Cleanup Preflight — REDACTED
project: staging only | read-only only
head/origin: 14907c6dbc546f6876f2055b6d9b6c6f72394d66
totalVisiblePublished: PENDING (ops) | candidateOldTest: PENDING | unknownNeedsReview: PENDING
protectedNewRealBatch: 0 (not imported yet)
reference risk: PENDING (lead/reveal/outcome/payment/settlement — count only)
recommendation: PENDING
cleanup approval phrase required (v6.2B.7) | import still BLOCKED
no per-car data / PII / raw URLs in repo
no Firestore writes / no cleanup executed
```

**Actual read-only query counts live in ops secure channel only.**

---

## Next Steps

1. ✅ v6.2B.8 read-only preflight record (this doc)
2. Ops runs read-only aggregate queries on staging (outside git)
3. Ops records counts + recommendation in secure channel
4. READY → request cleanup approval phrase §5
5. Cleanup execution record (future slice) — after cleanup approval
6. Import approval phrase + import execution — **after** cleanup pass
