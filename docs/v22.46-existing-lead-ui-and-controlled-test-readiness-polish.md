# v22.46 — Existing Lead UI and Controlled-Test Readiness Polish

Date: 2026-07-10  
Branch: `feature/chat-image-attachment-v1`  
Execution type: `EXISTING LEAD UI AND CONTROLLED-TEST READINESS POLISH / REUSE EXISTING LEAD SYSTEM / LEAD CAPTURE OFF / NO LEAD WRITE / NO DEALER SEND / STAGING ONLY`

## 1) Executive summary

Polished the **existing** chat Buyer Lead OFF-state so buyers are not asked for phone/name/consent while Lead Capture is unavailable. Backend Kill Switch remains authoritative. No new Lead system. No Lead write. No Dealer send. Marketplace **15** and Thor Auto Corollas preserved.

**Recommendation: NEED REVIEW** — UI OFF polish is safe; a future synthetic test still needs Owner choice on **memory durability** and **cleanup** (Options C vs D) before execution.

---

## 2) Baseline (before changes)

| Check | Result |
|-------|--------|
| Repo | `D:/nonga` |
| Branch | `feature/chat-image-attachment-v1` |
| Local / origin HEAD | `4c7e3bc` |
| Working tree | Unrelated untracked `.js` tree preserved |
| Cloud Run | `nonga-staging-00192-6th` @ 100% |
| Hosting | `assets/index-CHSJ9MTc.js` |
| `leadCaptureEnabled` | **false** |
| `publicSignupEnabled` | **false** |
| Unauth buyer-lead | **401** |
| Marketplace | **15** |
| Corolla 2020/2021 | Public, `Thor Auto`, images 5/5 |
| Public DTO protected fields | **0** |
| Dealer notification | **None** |
| Production | Untouched |

---

## 3) Existing CTA/modal behavior (before)

| Item | Before |
|------|--------|
| CTA location | Chat car card 「ให้ผู้ขายติดต่อกลับ」 |
| While OFF | CTA started capture draft and asked for name/method/budget; modal still showed phone field + disabled submit |
| Login | Only at submit (after PII collection path) |
| Risk | Buyer could enter PII into a flow that cannot submit |

---

## 4) UI changes made

| Change | Detail |
|--------|--------|
| Car-card CTA while OFF | Short-circuits: Thai unavailable reply only; **no draft started**; **no PII asked** |
| Chat start/continue while OFF | Same unavailable reply; clears any in-progress draft |
| Consent modal while OFF | Unavailable panel only — **no phone input**, no consent form, submit disabled |
| Submit path | Client fail-closed + backend kill switch still blocks |
| Thai copy | Clear: not open, no lead saved, no dealer send; invite browse/ask Nong A |
| Legacy | Comments on `InquireModal` / `DealerLeads` — not activated |

Supported path unchanged: **chat CTA → `BuyerLeadConsentModal` → `POST /api/buyer-leads`**.

---

## 5) OFF-state source of truth

| Layer | Role |
|-------|------|
| `/api/health` → `leadCaptureEnabled` | Client display (fail-closed; only boolean `true`) |
| `NONGA_LEAD_CAPTURE_ENABLED` | Server authority (exact `"true"`) |
| Route + `createConsentedBuyerLead` | Dual guard; no write while OFF |
| Query / localStorage / DTO | **Cannot** enable capture |

Stale UI still fails safely at API.

---

## 6) Legacy duplicate-risk review

| Path | Active in Buyer Lead flow? |
|------|----------------------------|
| Chat CTA + consent modal + `/api/buyer-leads` | **Yes** (supported) |
| `InquireModal` | **No** — mock `setTimeout` only |
| `DealerLeads` analytics CRM | **No** — not buyer-lead engine |

Static tests assert InquireModal does not call `/api/buyer-leads`.

---

## 7) Consent and PII findings (future ON-state)

| Field | When ON |
|-------|---------|
| Chat | Name/nickname, purchase method, budget/offer, contact window |
| Modal | Thai phone (required) + consent |
| Free text | Forbidden-doc patterns blocked |
| Dealer private contact on buyer UI | Not exposed via public DTO |
| Errors | Thai; no env/secret echo |

No broad ON-state contract change in v22.46. OFF path no longer requests phone/name.

---

## 8) Memory backend findings

| Topic | Finding |
|-------|---------|
| Default | `NONGA_LEAD_DATA_BACKEND` unset → **memory** in-process Map |
| Survives restart / new revision | **No** |
| Multi-instance | Lead may 404 on another pod |
| Firestore repo | Exists (`buyerLeadRepositoryFirestore`) — **not enabled** this task |
| Controlled single synthetic test | **Possible** if same instance + immediate verify/cleanup |
| Real dealer pilot / production | **Not sufficient** — needs durable backend (Option D) |

No Firestore writes in v22.46.

---

## 9) Cleanup readiness

| Capability | Status |
|------------|--------|
| Public hard-delete API | **None** (by design) |
| `resetBuyerLeadRepositoryForTests` | Memory test reset only |
| Seller skip → withdrawn | Soft lifecycle; not hard delete |
| Future synthetic cleanup | Prefer **memory backend** + process-local lead + skip/withdrawn **or** revision recycle after OFF; document lead id only in operator log (redacted in Owner report) |

**Classification:** Future synthetic test is **ready only with Owner awareness** of memory ephemerality + agreed cleanup. Prefer Option D before treating results as durable proof.

---

## 10) Future v22.47 controlled-test draft (DO NOT EXECUTE)

Exact approval phrase (draft only — **not approved**):

`FINAL AUTHORIZE V22.47 EXACTLY ONE SYNTHETIC CONTROLLED STAGING LEAD TEST USING THE EXISTING LEAD SYSTEM / TEMPORARY LEAD CAPTURE ON / SYNTHETIC DATA ONLY / NO DEALER-FACING SEND / CLEANUP REQUIRED / RETURN LEAD CAPTURE OFF`

Outline:

1. One existing approved Thor Auto Corolla (`nonga-dealer`).  
2. Synthetic buyer data only — no real customer phone/PII.  
3. Temporarily `NONGA_LEAD_CAPTURE_ENABLED=true` on staging only.  
4. Decide memory vs firestore **before** write (Owner Option C vs D).  
5. Existing chat → modal → `POST /api/buyer-leads` only.  
6. No dealer-facing send / email / SMS / LINE / webhook.  
7. Optional masked queue verify only if separately authorized.  
8. Cleanup synthetic lead; return capture OFF; prove writes blocked.  
9. Marketplace remains 15; listings/images unchanged.  
10. Rollback if any step fails.

---

## 11) Owner decision options

| Option | Meaning |
|--------|---------|
| **Option A** | Keep Lead OFF — use marketplace/chat without submission |
| **Option B** | Further UI wording polish only; Lead remains OFF |
| **Option C** | Fresh approval for exactly one synthetic controlled staging Lead test (existing system) |
| **Option D** | Prepare durable Lead storage before any controlled Lead test |
| **Option E** | Review Dealer in-app queue/reveal separately after persistence proven |

None selected by this packet.

---

## 12) Risks

1. Memory backend not durable for real pilot.  
2. No hard-delete API — cleanup must be planned.  
3. CTA label still 「ให้ผู้ขายติดต่อกลับ」 — reply clarifies unavailable (acceptable).  
4. InquireModal still present on marketplace detail as mock — documented/not wired to real API.

---

## 13) Safety checks

| Check | Result |
|-------|--------|
| Lead Capture OFF | **PASS** |
| No Lead write | **PASS** |
| No Dealer send | **PASS** |
| Marketplace 15 | **PASS** |
| Thor Auto display / images | **PASS** |
| Public DTO safe | **PASS** |
| Unauth 401 | **PASS** |
| Kill switch exact `"true"` | **PASS** (unchanged) |

---

## 14) Tests

| Check | Result |
|-------|--------|
| `npm run test:v22.39`–`v22.45` | **PASS** |
| `npm run test:v22.30-lead-capture-kill-switch-unit` | **PASS** |
| `npm run test:v22.46` | **PASS** |

## 14b) Staging Hosting deploy (UI only)

| Check | Result |
|-------|--------|
| Build | `npm run build:staging:hosting` **PASS** |
| Deploy | Firebase Hosting only — **no** Cloud Run change |
| New asset | `assets/index-Bsnqa-MN.js` |
| Cloud Run | Still `nonga-staging-00192-6th` |
| Lead Capture | **OFF** |
| Marketplace | **15** |
| Unauth buyer-lead | **401** |

---

## 15) Recommendation

**NEED REVIEW**

OFF-state UI is safely polished and does not collect unnecessary PII. Existing Lead system remains the only active path. Backend Kill Switch unchanged and authoritative. No Lead created; no Dealer send.

Before **Option C** execution, Owner should confirm cleanup + whether **Option D** (durable storage) is required first.

### Explicit non-actions

- Did not set `NONGA_LEAD_CAPTURE_ENABLED=true`  
- Did not create leads / queue writes / notifications  
- Did not create a second Lead system  
- Did not activate InquireModal / DealerLeads as real Lead  
- Did not ask Owner for secrets / terminal work  
- Did not execute v22.47  
