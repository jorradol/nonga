# v6.9I-B — Operator Manual Authenticated Profile Light Mode Smoke Execution Record

**สถานะ:** **PARTIAL** — operator/manual login **unavailable** in this session; authenticated `/profile` internals **not verified**; guest `/profile` route behavior **PASS**; new guest P1 on RouteGuard auth gate contrast  
**วันที่:** 2026-06-17  
**Git baseline (smoke):** `700ece6b4518cb9b545f70b7ae1f9620d066d2eb` (`700ece6`)  
**Prior staging deploy record:** v6.9H-A (`0ae72ff` header/drawer contrast patch live on staging Hosting)  
**Branch:** `feature/chat-image-attachment-v1`  
**Smoke run:** no commit during smoke; no deploy; no runtime code changes

> Smoke-only verification on live staging for operator-assisted authenticated profile light-mode readability. No credentials, tokens, session cookies, API keys, Authorization headers, raw prompts, avatar URLs, or PII in this document. **Not production-ready. Production pilot still NOT READY.**

---

## 1. Overall verdict

| Item | Result |
|------|--------|
| Overall result | **PARTIAL** |
| Operator/manual login | **Unavailable** — no safe password/token/session for agent |
| Login performed | **NO** |
| Authenticated `/profile` internals | **Not verified** |
| Signed-in header/profile dropdown | **Not verified** |
| Signed-in mobile chrome | **Not verified** |
| Guest `/profile` route behavior | **PASS** — stays `/profile`, no chat fallback, no crash |
| Guest header CTA (post v6.9H-A) | **PASS** |
| Guest mobile drawer (390×844) | **PASS** |
| Deploy performed | **NO** (staging already live from v6.9H-A) |
| Commit during smoke | **NO** |
| Runtime code changes | **NO** |
| Cloud Run | **Not touched** |
| Production | **Not touched** |
| **v6.9 Controlled Production Pilot** | **NOT READY** |

---

## 2. Baseline verification

| Field | Value |
|-------|-------|
| Repo | `https://github.com/jorradol/nonga.git` |
| Branch | `feature/chat-image-attachment-v1` |
| HEAD | `700ece6b4518cb9b545f70b7ae1f9620d066d2eb` (`700ece6`) |
| Local HEAD | `700ece6` (= `origin/feature/chat-image-attachment-v1`) |
| Working tree (at smoke time) | clean |
| Working tree (at record time) | clean (docs/script pending separate commit) |
| Staging project | `nonga-ce93c` |
| Staging host | `https://nonga-ce93c.web.app` |
| Deploy needed | **NO** — v6.9H-A Hosting deploy already live |

**Live staging assets (confirmed match with v6.9H-A):**

| Asset | Path |
|-------|------|
| JS bundle | `/assets/index-CbnVCCnG.js` |
| CSS bundle | `/assets/index-v8LQYv3A.css` |

---

## 3. Login availability

| Check | Result |
|-------|--------|
| Operator/manual login available | **Unavailable** in this smoke session |
| Pre-authenticated browser session | **None** — fresh browser, auth state guest |
| Password env vars | **Absent** — `NONGA_STAGING_MEMBER_PASSWORD`, `NONGA_TEST_MEMBER_PASSWORD` |
| `NONGA_TEST_MEMBER_EMAIL` in local `.env` | **Present** (len=19; value **not logged**) |
| `NONGA_TEST_MEMBER_UID` in local `.env` | **Present** (len=28; value **not logged**) |
| Login surface on staging | Email/Password + OAuth (Google / FB / LINE) at `/login` |
| Login performed | **NO** |
| Credentials / tokens / session values logged | **NO** |

**Conclusion:** Staging member test account metadata exists locally, but no safe credentials or pre-authenticated session were available for operator-assisted login in this run.

---

## 4. Authenticated `/profile` smoke result

| Check | Result |
|-------|--------|
| Status | **PARTIAL / Not verified** |
| Auth state | **Guest** — gate: "กรุณาเข้าสู่ระบบก่อนใช้งานส่วนนี้ครับ" |
| Route | Stays on `/profile` — **no chat fallback** |
| Visible app crash | **None** observed |
| SettingsCard | **Not verified** |
| Profile name / email / inputs | **Not verified** |
| AI tone cards | **Not verified** |
| Bordered panels (saved/system/history/dealer) | **Not verified** |
| Section headings / tab labels | **Not verified** |
| Signed-in profile dropdown | **Not verified** |
| Intentional writes (Save / tone / avatar / settings) | **None** |
| Fake pass for profile internals | **NO** |

---

## 5. Guest findings

| Check | Result |
|-------|--------|
| Guest `/profile` auth gate shell | **Visible** in light mode |
| Route stayed `/profile` | **Yes** |
| No chat fallback | **Confirmed** |
| Guest header CTA "เข้าสู่ระบบ AI 🪄" | **PASS** — white on orange gradient (v6.9H-A fix holds) |
| Desktop inactive nav links | **Readable** in light mode |
| Mobile drawer (390×844) | **PASS** — opens/closes; guest login CTA visible |
| Horizontal overflow (mobile) | **None** |
| White-on-white on tested guest surfaces | **None observed** |

**New P1 finding (guest RouteGuard shell):**

| ID | Severity | Area | Finding |
|----|----------|------|---------|
| I-B-02 | **P1** | Guest auth gate | `RouteGuard.tsx` `AccessDenied` uses hardcoded `text-slate-200` on light background at `/profile` — low contrast visually in light mode |

Observed class on gate message element: `text-sm font-semibold text-slate-200` (dark-mode text token on light page background).

---

## 6. Signed-in chrome (not verified)

| Check | Result |
|-------|--------|
| Header profile label / avatar | **Not testable** — guest session |
| Profile dropdown opens | **Not testable** |
| Dropdown menu items readable | **Not testable** |
| Mobile signed-in header/drawer | **Not testable** |

---

## 7. Regression sanity findings (guest only — not signed-in pass)

Light-mode guest sanity checks only. **These do not count as signed-in regression pass.**

| Route | Guest sanity | Notes |
|-------|--------------|-------|
| `/home` | **PASS** | Trust panel heading readable |
| `/car-vision` | **PASS** | H1 + body readable; v6.9F-A regression OK |
| `/viral-captions` | **PASS** | Usage widget + hook chips readable |

Signed-in regression on these routes remains **Defer** until authenticated session available.

---

## 8. Issue classification

| ID | Severity | Area | Finding |
|----|----------|------|---------|
| I-B-01 | **Blocker (slice)** | Auth | No safe operator session/password → authenticated profile **unverified** |
| I-B-02 | **P1** | Guest RouteGuard | Auth gate message `text-slate-200` on light background at `/profile` |
| I-B-03 | **Defer** | Profile UI | SettingsCard, inputs, tone cards, bordered panels, signed-in dropdown — pending login |
| I-B-04 | **P2** | Theme | Cold load defaults dark; light mode requires toggle (known) |
| I-B-05 | **Defer** | Signed-in regression | `/home`, `/car-vision`, `/viral-captions` signed-in spot-check pending auth |
| Owner backlog | **Defer** | Superadmin avatar | Owner reports superadmin profile icon/avatar has no image — defer to v6.9J audit |

**Owner backlog (do not patch in v6.9I-B):**

- **Owner backlog:** Superadmin profile icon/avatar is missing or has no image.
- **Recommended follow-up:** v6.9J Superadmin Profile Icon / Default Avatar Fallback Audit — audit-only first.
- **Do not mix into v6.9I-B:** current slice is smoke/record only; authenticated profile still not verified.
- **Potential areas to inspect later (v6.9J):**
  - signed-in header avatar/profile label
  - UserProfileView avatar section
  - AvatarSelector
  - role/user seed data for superadmin/admin
  - default avatar fallback logic
  - profile photoURL/displayName fields
- **Safety:** no real PII or avatar URL logged in docs.

**No P0** white-on-white blockers confirmed on tested **guest** surfaces.

**P0 cannot be ruled out** on authenticated profile internals until smoke completes with safe operator session.

---

## 9. Why PARTIAL (not PASS / not FAIL)

**Not PASS because:**

- Authenticated `/profile` UI **not verified**
- SettingsCard / profile panels / AI tone cards **not tested**
- Signed-in profile dropdown and mobile signed-in chrome **not tested**
- Signed-in regression routes **not tested**

**Not FAIL because:**

- No route crash or severe unreadable contrast on tested guest surfaces
- Guest `/profile` route behavior **correct** (stays `/profile`, no chat fallback)
- Guest header/drawer remain **stable** after v6.9H-A
- No intentional Firestore/profile writes performed

---

## 10. Firestore / write safety

| Item | Result |
|------|--------|
| Login performed | **NO** |
| `users/{uid}.lastLogin` side effect | **None** — no login |
| Profile save / AI tone change | **NO** |
| Avatar upload | **NO** |
| Settings persistence | **NO** |
| Firestore / leads / DealerLeads intentionally modified | **NO** |

---

## 11. Recommended next gates

| Gate | Notes |
|------|-------|
| v6.9I-C | RouteGuard guest gate contrast patch — class-only, separate approval |
| v6.9J | Superadmin Profile Icon / Default Avatar Fallback Audit — audit-only, separate approval |
| Authenticated profile smoke retry | Safe operator session / manual login only — no credentials in chat/repo/report |

---

## 12. Safety confirmations

| Item | Result |
|------|--------|
| Runtime code changes | **NO** |
| Commit during smoke | **NO** |
| Deploy | **NO** |
| Cloud Run | **Not touched** |
| Production | **Not touched** |
| Env / secrets / deploy config | **Unchanged** |
| Credentials / tokens / session logged | **NO** |
| AI runtime / Gemini provider | **Not touched** |
| Guard / fallback logic | **Not touched** |
| Firestore write | **Not touched** |
| Lead system / DealerLeads | **Not touched** |
| Payment / invoice / boost logic | **Not touched** |
| Full prompt / raw Gemini output in docs | **NO** |
| **v6.9 Controlled Production Pilot** | **NOT READY** |

---

## 13. Commands run (reference)

| Command | Result |
|---------|--------|
| `git status` / `git rev-parse HEAD` | Clean; `700ece6` |
| Live asset check (`index.html`) | `index-CbnVCCnG.js`, `index-v8LQYv3A.css` |
| Browser smoke on `https://nonga-ce93c.web.app` | See sections 4–7 |

---

*End of v6.9I-B operator manual authenticated profile light mode smoke execution record.*
