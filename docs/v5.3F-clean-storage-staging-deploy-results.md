# V5.3F Clean Firestore/Storage Staging Deploy Results

Date: 2026-05-26

## Summary

V5.3F was approved for a staging-only clean Firestore/Storage switch, but the app deploy was stopped at the rules readiness stage because Firebase Storage has not been set up for project `nonga-ce93c`.

No Cloud Run backend switch was deployed. No Firebase Hosting deploy was run. No migration write was run.

## Preflight

- Git branch: `feature/chat-image-attachment-v1`
- Starting remote commit: `b55374715791eefac017b5b16cae6d9c20f2c819`
- Rules config commit pushed before deploy attempt: `adb08b015e5aed8689d25ca4b94b28055d03f03b`
- Google Cloud project: `nonga-ce93c`
- Cloud Run service: `nonga-staging`
- Region: `asia-southeast1`
- Cloud Run URL observed: `https://nonga-staging-dpf3rexexq-as.a.run.app`
- Artifact Registry repo exists: `asia-southeast1-docker.pkg.dev/nonga-ce93c/nonga-staging`
- Required Secret Manager secret names exist:
  - `gemini-api-key`
  - `firebase-service-account-json`
- Firestore database exists: `(default)`, native mode, `asia-southeast1`

## Rules Deploy Status

Firestore rules:

- Status: deployed successfully to `nonga-ce93c`
- Command scope: `--only firestore:rules --project nonga-ce93c`
- Result: `firestore.rules` compiled and was released.

Storage rules:

- Status: not deployed
- Config was added and pushed safely:
  - `firebase.json` now has a `storage.rules` deploy target.
  - `storage.rules` matches `storage.rules.draft`.
  - `npm run test:v50-security-rules` verifies this match.
- Deploy attempt was blocked by Firebase CLI:
  - Firebase Storage has not been set up on project `nonga-ce93c`.
  - The project currently has Cloud Build/Run buckets, but no Firebase Storage default bucket.

Required next action before retrying V5.3F:

1. Open Firebase Console for `nonga-ce93c`.
2. Go to Storage.
3. Click Get Started.
4. Create the default Firebase Storage bucket in the staging location.
5. Confirm the bucket name in the Storage tab.
6. Retry Storage rules deploy with `--project nonga-ce93c`.
7. Use the confirmed bucket as `FIREBASE_STORAGE_BUCKET` for Cloud Run.

## App Deploy Status

Cloud Run:

- Status: skipped
- Reason: clean Storage mode is not ready until Firebase Storage default bucket exists and Storage rules can be deployed.
- No `NONGA_DATA_BACKEND=firestore` switch was deployed.
- No `NONGA_IMAGE_BACKEND=firebase-storage` switch was deployed.

Firebase Hosting:

- Status: skipped
- Reason: Hosting should only deploy after Cloud Run health passes with Firestore/Storage flags.

Docker image:

- Status: not built or pushed in this blocked run.
- Intended tag format remains:
  - `v5.3-clean-storage-<commit>-YYYYMMDD-HHMM`

## Health Results

No new Cloud Run revision was deployed, so no post-switch `/api/health` result exists.

Expected health after retry:

- `ok=true`
- `dataBackend=firestore`
- `imageBackend=firebase-storage`
- `publicSignupEnabled=false`

## Verification Run

Passed before/around the blocked deploy:

- `npm run lint`
- `npm run test:v50-security-rules`

Previous V5.3E verification remains the latest full local suite:

- `npm run build`
- `npm run test:v53-clean-storage-readiness`
- `npm run test:v50-firestore-repository`
- `npm run test:v50-image-storage-repository`
- `npm run test:dealer-draft-publish-guard`
- `npm run test:dealer-image-resize-upload`
- `npm run test:dealer-draft-edit-publish`
- `npm run test:v49-final-smoke`

## Manual Web UI Smoke

Status: not run.

Reason: Cloud Run/Hosting were not deployed with Firestore/Storage flags.

Keep this checklist for the retry after Storage is provisioned:

1. Login as dealer.
2. Create a new incomplete draft.
3. Upload a new image.
4. Save draft.
5. Reload page and confirm draft remains.
6. Edit draft to complete required fields.
7. Publish draft.
8. Open marketplace and confirm listing appears.
9. Open detail and confirm the Storage image displays.
10. Edit listing.
11. Change visibility.
12. Delete or hide listing.
13. Restart/redeploy Cloud Run and confirm data/image durability.
14. Run short member/dealer/admin role smoke.
15. Confirm public signup remains closed.

## Rollback

Rollback remains ready because no app switch was deployed:

- Current staging app remains on its prior backend flags.
- If a later retry deploys the switch and fails, roll back Cloud Run to the previous healthy revision or redeploy with:
  - `NONGA_DATA_BACKEND=file`
  - `NONGA_IMAGE_BACKEND=file`
  - `VITE_NONGA_PUBLIC_SIGNUP_ENABLED=false`

## Safety Confirmation

- No migration `--write`.
- No `--confirm-staging`.
- No old file data migration.
- No production project touched.
- No public signup enablement.
- No secret, API key, or service account value committed.
- No Firestore/Storage data cleanup was performed.
