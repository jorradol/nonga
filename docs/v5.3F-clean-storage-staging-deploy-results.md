# V5.3F Clean Firestore/Storage Staging Deploy Results

Date: 2026-05-26

## Summary

V5.3F was approved for a staging-only clean Firestore/Storage switch. The first attempt was stopped at the rules readiness stage because Firebase Storage had not been set up for project `nonga-ce93c`.

After the Firebase Storage default bucket was created, the retry completed the staging switch to Firestore/Storage.

No migration write was run. No old file-backed data was migrated.

## Preflight

- Git branch: `feature/chat-image-attachment-v1`
- Starting remote commit: `b55374715791eefac017b5b16cae6d9c20f2c819`
- Rules config commit pushed before deploy attempt: `adb08b015e5aed8689d25ca4b94b28055d03f03b`
- Blocker result commit: `71f4d3798329b309af4f58422c5d11213058b28d`
- Cloud Build config commit: `6a599c7`
- Google Cloud project: `nonga-ce93c`
- Cloud Run service: `nonga-staging`
- Region: `asia-southeast1`
- Cloud Run URL observed: `https://nonga-staging-dpf3rexexq-as.a.run.app`
- Cloud Run deploy URL also returned: `https://nonga-staging-265743710806.asia-southeast1.run.app`
- Artifact Registry repo exists: `asia-southeast1-docker.pkg.dev/nonga-ce93c/nonga-staging`
- Required Secret Manager secret names exist:
  - `gemini-api-key`
  - `firebase-service-account-json`
- Firestore database exists: `(default)`, native mode, `asia-southeast1`
- Firebase Storage bucket: `nonga-ce93c.firebasestorage.app`

## Rules Deploy Status

Firestore rules:

- Status: deployed successfully to `nonga-ce93c`
- Command scope: `--only firestore:rules --project nonga-ce93c`
- Result: `firestore.rules` compiled and was released.

Storage rules:

- Status: deployed successfully on retry
- Config was added and pushed safely:
  - `firebase.json` now has a `storage.rules` deploy target.
  - `storage.rules` matches `storage.rules.draft`.
  - `npm run test:v50-security-rules` verifies this match.
- First deploy attempt was blocked by Firebase CLI:
  - Firebase Storage has not been set up on project `nonga-ce93c`.
  - The project currently has Cloud Build/Run buckets, but no Firebase Storage default bucket.
- Retry deploy after bucket creation:
  - Command scope: `--only storage --project nonga-ce93c`
  - Result: `storage.rules` compiled, uploaded, and was released to Firebase Storage.

Completed prerequisite before retrying V5.3F:

1. Open Firebase Console for `nonga-ce93c`.
2. Go to Storage.
3. Click Get Started.
4. Create the default Firebase Storage bucket in the staging location.
5. Confirm the bucket name in the Storage tab.
6. Bucket confirmed: `nonga-ce93c.firebasestorage.app`

## App Deploy Status

Cloud Run:

- Status: deployed successfully
- Image tag: `v5.3-clean-storage-6a599c7-20260526-2044`
- Image URI: `asia-southeast1-docker.pkg.dev/nonga-ce93c/nonga-staging/nonga-staging:v5.3-clean-storage-6a599c7-20260526-2044`
- Cloud Build ID: `aa4c7f34-7be6-4cb6-8310-c6c5a3214de7`
- Cloud Run revision: `nonga-staging-00006-8qp`
- Traffic: 100 percent to the new revision
- Runtime flags:
  - `NODE_ENV=production`
  - `APP_URL=https://nonga-ce93c.web.app`
  - `NONGA_DATA_BACKEND=firestore`
  - `NONGA_IMAGE_BACKEND=firebase-storage`
  - `VITE_NONGA_PUBLIC_SIGNUP_ENABLED=false`
  - `FIREBASE_PROJECT_ID=nonga-ce93c`
  - `FIREBASE_STORAGE_BUCKET=nonga-ce93c.firebasestorage.app`

Firebase Hosting:

- Status: deployed successfully after Cloud Run health passed
- Hosting URL: `https://nonga-ce93c.web.app`

Docker image:

- Status: built and pushed through Cloud Build because local Docker CLI is not installed.
- Build config: `cloudbuild.v53f.yaml`
- Public Vite build args were sourced from Firebase web config without printing API key values.

## Health Results

Cloud Run status URL:

- `ok=true`
- `dataBackend=firestore`
- `imageBackend=firebase-storage`
- `publicSignupEnabled=false`

Cloud Run deploy URL:

- `ok=true`
- `dataBackend=firestore`
- `imageBackend=firebase-storage`
- `publicSignupEnabled=false`

Firebase Hosting URL:

- `ok=true`
- `dataBackend=firestore`
- `imageBackend=firebase-storage`
- `publicSignupEnabled=false`

Marketplace read smoke:

- `GET https://nonga-ce93c.web.app/api/cars`
- `success=true`
- `count=0`
- This is expected for clean Firestore staging before creating fresh web UI test data.

## Verification Run

Passed before/around the blocked deploy:

- `npm run lint`
- `npm run test:v50-security-rules`
- `npm run test:v53-clean-storage-readiness`

Previous V5.3E verification remains the latest full local suite:

- `npm run build`
- `npm run test:v53-clean-storage-readiness`
- `npm run test:v50-firestore-repository`
- `npm run test:v50-image-storage-repository`
- `npm run test:dealer-draft-publish-guard`
- `npm run test:dealer-image-resize-upload`
- `npm run test:dealer-draft-edit-publish`
- `npm run test:v49-final-smoke`

`npm run test:v49-final-smoke` was not re-run against live staging after the switch because it is a dev-token/API-heavy smoke that creates synthetic data. For this clean staging switch, live data validation should use the real web UI and real closed-staging login roles.

## Manual Web UI Smoke

Status: pending manual execution by the staging tester.

Reason: the deployed app now uses production Firebase auth. The next validation should use real closed-staging dealer/member/admin accounts and create fresh test data through the web UI.

Checklist:

1. Login as dealer.
2. Create a new incomplete draft.
3. Upload a new image.
4. Save draft.
5. Reload page and confirm draft remains.
6. Edit draft to complete required fields.
7. Publish draft.
8. Open marketplace and confirm listing appears.
9. Open detail and confirm the Storage image displays.
10. Edit listing.
11. Change visibility.
12. Delete or hide listing.
13. Restart/redeploy Cloud Run and confirm data/image durability.
14. Run short member/dealer/admin role smoke.
15. Confirm public signup remains closed.

Durability smoke status: pending. It should be run only after a draft/listing/image is created through the real web UI.

## Rollback

Rollback remains ready:

- Roll back Cloud Run to the previous healthy revision, or redeploy with:
  - `NONGA_DATA_BACKEND=file`
  - `NONGA_IMAGE_BACKEND=file`
  - `VITE_NONGA_PUBLIC_SIGNUP_ENABLED=false`
- If Hosting release causes a regression, roll back the Firebase Hosting release.

## Safety Confirmation

- No migration `--write`.
- No `--confirm-staging`.
- No old file data migration.
- No production project touched.
- No public signup enablement.
- No secret, API key, or service account value committed.
- No Firestore/Storage data cleanup was performed.
