# v6.1K.2 — Chat Shadow Real Provider Flag Pilot

**สถานะ:** Execution record — **env-only Cloud Run update, `NONGA_AI_CHAT_SHADOW_REAL_PROVIDER_ENABLED=true`, CP-02 admin-only Gemini sink pilot PASS, user-visible off, public chat legacy unchanged**  
**วันที่:** 2026-06-08  
**Git commit (baseline):** `e3b8278bca3d4d387a262835934c0b1dcc58273b` (v6.1K.1 execution record)  
**Branch:** `feature/chat-image-attachment-v1`  
**อ้างอิง:** v6.1K.1 (chat shadow sink staging deploy), v6.1K (chat-path shadow sink module), v6.1J (admin full matrix)

> **v6.1K.2 เปิด chat shadow real provider flag บน staging เท่านั้น** — env-only revision, **ไม่มี image deploy ใหม่**, **`NONGA_AI_USER_VISIBLE_ENABLED=false`**, CP-02 เรียก Gemini จริงแบบ admin-only / sink-only / user-visible off

---

## สารบัญ

1. [Vision & Execution Summary](#1-vision--execution-summary)
2. [Preflight (Before Env Update)](#2-preflight-before-env-update)
3. [Env Update (Executed)](#3-env-update-executed)
4. [Env Flags Before / After](#4-env-flags-before--after)
5. [Post-update API Smoke](#5-post-update-api-smoke)
6. [Public Chat (Unchanged)](#6-public-chat-unchanged)
7. [Image Unchanged](#7-image-unchanged)
8. [Rollback Plan](#8-rollback-plan)
9. [Compliance (v6.1K.2)](#9-compliance-v61k2)
10. [Next Steps](#10-next-steps)

---

## 1. Vision & Execution Summary

### เป้าหมาย v6.1K.2

เปิด `NONGA_AI_CHAT_SHADOW_REAL_PROVIDER_ENABLED=true` บน Cloud Run staging เพื่อให้ CP-02 ของ route `/api/admin/chat-shadow-sink` เรียก Gemini จริงแบบ:

- **admin-only** — Firebase admin token required
- **sink-only** — ไม่ส่ง AI response ให้ public user
- **user-visible off** — `NONGA_AI_USER_VISIBLE_ENABLED=false` คงเดิม

### Execution outcome

| Area | Result |
|------|--------|
| Preflight | ✅ HEAD = origin `e3b8278`, git clean |
| Env update | ✅ rev **`nonga-staging-00059-lvx`**, traffic **100%** |
| Image deploy | ❌ **none** — env-only revision |
| `NONGA_AI_USER_VISIBLE_ENABLED` | ✅ **`false`** — unchanged |
| `NONGA_AI_CHAT_SHADOW_REAL_PROVIDER_ENABLED` | ✅ **`true`** — only env change |
| Post-update API smoke | ✅ health, auth guards, CP-01..CP-05, SS-01 — **PASS** |
| CP-02 real Gemini | ✅ `providerNetwork:true`, `provider:gemini`, `real_provider_call_ok` |
| CP-01/03/04/05 real provider | ❌ **none** — `providerNetwork:false` |
| Public chat `"เริ่มใหม่"` | ✅ exact legacy |
| Budget/kill switch guardrail | ✅ CP-03 scenario-local — shadow off |
| Rollback | ❌ **not needed** — smoke passed |
| Firebase Hosting deploy | ❌ **SKIPPED** |
| Firestore rules deploy | ❌ **SKIPPED** |

---

## 2. Preflight (Before Env Update)

| Check | Value |
|-------|-------|
| **git status** | clean |
| **branch** | `feature/chat-image-attachment-v1` |
| **HEAD** | `e3b8278bca3d4d387a262835934c0b1dcc58273b` |
| **origin/feature/chat-image-attachment-v1** | `e3b8278bca3d4d387a262835934c0b1dcc58273b` |
| **Firebase project** | `nonga-ce93c` |
| **Cloud Run service** | `nonga-staging` |
| **Region** | `asia-southeast1` |
| **Current revision (before update)** | `nonga-staging-00058-xwh` @ **100%** |
| **Current image** | `v6.1K.1-chat-path-shadow-sink` |
| **Image digest** | `sha256:0cf221536c2fea79215cd26aa793214e8ce3f9f42b9a747f8ef3bae46900f84e` |
| **Public signup** | **`false`** |
| **`NONGA_AI_USER_VISIBLE_ENABLED`** | **`false`** |
| **`NONGA_AI_CHAT_SHADOW_REAL_PROVIDER_ENABLED`** | **absent** (default off) |
| **`NONGA_AI_ADMIN_SHADOW_REAL_PROVIDER_ENABLED`** | **`true`** |
| **`NONGA_AI_EMERGENCY_KILL_SWITCH`** | **`false`** |
| **`NONGA_AI_BUDGET_DAILY_LIMIT`** | **`5`** |
| **`NONGA_AI_BUDGET_MONTHLY_LIMIT`** | **`50`** |

### Preflight smoke (before flag)

| Check | Result |
|-------|--------|
| `GET /api/health` | **200** — `ok:true`, `publicSignupEnabled:false` |
| `POST /api/admin/chat-shadow-sink` unauth | **401** |
| Dealer token | **403** |
| Admin CP-02 (flag off) | **200** — `providerNetwork:false` |
| Public `/chat` `"เริ่มใหม่"` | exact legacy |

**Did not run** `gcloud secrets versions access` — no secret values in this record.

---

## 3. Env Update (Executed)

```bash
gcloud run services update nonga-staging \
  --project=nonga-ce93c \
  --region=asia-southeast1 \
  --update-env-vars=NONGA_AI_CHAT_SHADOW_REAL_PROVIDER_ENABLED=true
```

| Field | Value |
|-------|-------|
| **Update type** | **env-only** — no new image deploy |
| **Previous revision** | `nonga-staging-00058-xwh` |
| **New revision** | **`nonga-staging-00059-lvx`** @ **100%** |
| **Image** | **unchanged** — `v6.1K.1-chat-path-shadow-sink` |
| **Image digest** | `sha256:0cf221536c2fea79215cd26aa793214e8ce3f9f42b9a747f8ef3bae46900f84e` |
| **Env changed** | **only** `NONGA_AI_CHAT_SHADOW_REAL_PROVIDER_ENABLED=true` |
| **Service URL** | `https://nonga-staging-265743710806.asia-southeast1.run.app` |
| **Smoke base (Hosting rewrite)** | `https://a.nongbot.org` |

**Did not run** `gcloud builds submit` or `gcloud run services update --image=...` in this slice.

---

## 4. Env Flags Before / After

### Before update (revision `nonga-staging-00058-xwh`)

| Flag | Value |
|------|-------|
| `NONGA_AI_USER_VISIBLE_ENABLED` | **`false`** |
| `NONGA_AI_CHAT_SHADOW_REAL_PROVIDER_ENABLED` | **absent** (default off) |
| `NONGA_AI_ADMIN_SHADOW_REAL_PROVIDER_ENABLED` | **`true`** |
| `NONGA_AI_EMERGENCY_KILL_SWITCH` | **`false`** |
| `NONGA_AI_BUDGET_DAILY_LIMIT` | **`5`** |
| `NONGA_AI_BUDGET_MONTHLY_LIMIT` | **`50`** |

### After update (revision `nonga-staging-00059-lvx`)

| Flag | Value |
|------|-------|
| `NONGA_AI_USER_VISIBLE_ENABLED` | **`false`** (unchanged) |
| `NONGA_AI_CHAT_SHADOW_REAL_PROVIDER_ENABLED` | **`true`** |
| `NONGA_AI_ADMIN_SHADOW_REAL_PROVIDER_ENABLED` | **`true`** (unchanged) |
| `NONGA_AI_EMERGENCY_KILL_SWITCH` | **`false`** (unchanged) |
| `NONGA_AI_BUDGET_DAILY_LIMIT` | **`5`** (unchanged) |
| `NONGA_AI_BUDGET_MONTHLY_LIMIT` | **`50`** (unchanged) |

**Did not set** `NONGA_AI_USER_VISIBLE_ENABLED=true`.

---

## 5. Post-update API Smoke

Smoke base: **`https://a.nongbot.org`** (Firebase Hosting → Cloud Run rewrite)

### Health

| Endpoint | HTTP | Key fields |
|----------|------|------------|
| `GET /api/health` | **200** | `ok:true`, `publicSignupEnabled:false` |

### Chat shadow sink route

```
POST /api/admin/chat-shadow-sink
Content-Type: application/json
Authorization: Bearer <Firebase ID token>
{ "scenarioId": "CP-02" }
```

| Case | HTTP | Result |
|------|------|--------|
| Unauth `{"scenarioId":"CP-02"}` | **401** | **PASS** |
| Dealer Firebase token `{"scenarioId":"CP-02"}` | **403** | **PASS** |
| Admin `CP-01` | **200** | **PASS** — `providerNetwork:false`, no real provider |
| Admin `CP-02` | **200** | **PASS** — see CP-02 detail below |
| Admin `CP-03` | **200** | **PASS** — `providerNetwork:false`, kill switch scenario |
| Admin `CP-04` | **200** | **PASS** — `providerNetwork:false`, user-visible blocked |
| Admin `CP-05` | **200** | **PASS** — `providerNetwork:false`, budget guard |

### CP-02 real provider detail

| Field | Value |
|-------|-------|
| `sinkOnly` | **`true`** |
| `userVisibleOff` | **`true`** |
| `readOnly` | **`true`** |
| `providerNetwork` | **`true`** |
| `provider` | **`gemini`** |
| gate/reason | **`real_provider_call_ok`** |
| AI response to public user | ❌ **none** — sink-only admin payload |

### CP-01 / CP-03 / CP-04 / CP-05 — no real provider

| Scenario | `providerNetwork` | Real provider call |
|----------|-------------------|-------------------|
| CP-01 | `false` | ❌ none — mock only |
| CP-03 | `false` | ❌ none — kill switch scenario |
| CP-04 | `false` | ❌ none — user-visible blocked |
| CP-05 | `false` | ❌ none — budget guard |

### Admin SS-01 regression

| Case | HTTP | Key fields |
|------|------|------------|
| Admin `POST /api/admin/sales-brain-shadow-smoke` `{"caseId":"SS-01"}` | **200** | `success:true`, `userVisibleOff:true` |

### Budget / kill switch guardrail

| Check | Result |
|-------|--------|
| CP-03 kill switch scenario | **PASS** — `providerNetwork:false`, shadow off |
| Budget limits unchanged | daily **5** / monthly **50** |
| `NONGA_AI_EMERGENCY_KILL_SWITCH` | **`false`** |

### Safety checks on responses

| Check | Result |
|-------|--------|
| Raw phone / email in response | ❌ **none observed** |
| Secret / env dump in response | ❌ **none** — no `GEMINI_API_KEY` or `FIREBASE_SERVICE_ACCOUNT_JSON` values |
| AI response shown to public user | ❌ **none** — admin-only sink payload |

**Did not run** `gcloud secrets versions access` during smoke.

---

## 6. Public Chat (Unchanged)

Safe chat smoke via primary Hosting URL (API → Cloud Run; **client bundle unchanged** — no Hosting deploy in v6.1K.2):

| Check | Result |
|-------|--------|
| **URL** | `https://a.nongbot.org/chat` |
| Message | `"เริ่มใหม่"` |
| User-visible response | **legacy 100%** — exact: `ยกเลิกข้อมูลเดิมแล้วครับ พิมพ์ข้อมูลรถคันใหม่ที่ต้องการลงขายได้เลยครับ` |
| AI/Gemini replacing legacy | ❌ **none** |
| Raw PII in UI | ❌ **not observed** |

**No paid chat-path user-visible real provider call** — client `wireShadowChatPath` flags-only unchanged; CP-02 Gemini call is admin sink route only.

---

## 7. Image Unchanged

| Field | Value |
|-------|-------|
| **Image tag** | `v6.1K.1-chat-path-shadow-sink` |
| **Image digest** | `sha256:0cf221536c2fea79215cd26aa793214e8ce3f9f42b9a747f8ef3bae46900f84e` |
| **Deploy type** | **env-only revision** — no `gcloud builds submit`, no `--image=` update |
| **Previous revision** | `nonga-staging-00058-xwh` |
| **New revision** | `nonga-staging-00059-lvx` |

---

## 8. Rollback Plan (Prepared — Not Run)

Rollback **not needed** — health, route guards, CP-02 real provider smoke, CP-01/03/04/05 no-provider guard, SS-01 regression, budget/kill switch guardrail, and public chat smoke passed.

```bash
# Prepared only — do not run unless smoke fails
gcloud run services update nonga-staging \
  --project=nonga-ce93c \
  --region=asia-southeast1 \
  --update-env-vars=NONGA_AI_CHAT_SHADOW_REAL_PROVIDER_ENABLED=false
```

This reverts CP-02 to `providerNetwork:false` (flag default off) without changing image or `NONGA_AI_USER_VISIBLE_ENABLED`.

---

## 9. Compliance (v6.1K.2)

| Check | Status |
|-------|--------|
| `NONGA_AI_USER_VISIBLE_ENABLED=true` | ❌ not set — remains **false** |
| Public AI response | ❌ not changed — legacy chat only |
| Production deploy | ❌ not done |
| Firebase Hosting deploy | ❌ not done |
| Firestore rules deploy | ❌ not done |
| Image deploy (`--image=`) | ❌ not done — env-only |
| `gcloud secrets versions access` | ❌ not done |
| Payment / invoice / settlement write | ❌ not touched |
| Buyer lead / seller reveal / outcome | ❌ not touched |
| Public signup | ❌ not opened |
| Real stock import | ❌ still blocked |
| Custom prompt / public debug endpoint | ❌ not touched |
| v6.1L user-visible approval phrase | ❌ not in scope |

---

## 10. Next Steps

1. Monitor CP-02 sink-only Gemini calls — budget daily **5** / monthly **50**
2. Optional: extend CP-02 conversation matrix — separate slice, report before change
3. **v6.1L** — controlled user-visible pilot requires separate approval gate
4. Do **not** enable `NONGA_AI_USER_VISIBLE_ENABLED=true` without explicit v6.1L approval
5. Rollback command prepared above if CP-02 smoke regresses
