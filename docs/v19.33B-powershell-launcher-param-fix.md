# v19.33B - PowerShell Launcher Param-Block Fix

Date: 2026-07-07
Branch: `feature/chat-image-attachment-v1`
Execution type: `FAST PATCH / OWNER POWERSHELL LAUNCHER PARAM-BLOCK FIX ONLY / NO ONE-RUN`
Latest status before this task: `PASS — v19.33A PowerShell launcher syntax fixed`
Latest commit before this task: `7a1175a`

## 1) Owner error inspected

- reported owner error:
- `param : The term 'param' is not recognized as the name of a cmdlet...`
- source location:
- `scripts/run-v19-step1-owner-token.ps1:3 char:1`

## 2) Root cause

- top-level `param(...)` block was present while the script had a statement before it.
- in PowerShell script files, `param(...)` must be the first executable statement.
- this made `param` parse/execute as a normal command token in owner environment.

## 3) Launcher simplification summary

- removed top-level `param(...)` entirely (minimal script path).
- approval path is resolved from repo root via `$PSScriptRoot` + `Set-Location`.
- owner command remains one-line and unchanged:
- `powershell -ExecutionPolicy Bypass -File scripts/run-v19-step1-owner-token.ps1`
- added safe dry mode:
- `NONGA_V19_LAUNCHER_PARSE_ONLY=1` to skip prompt/runtime path.
- token handling stays masked-only and in-memory only.
- env clear best-effort kept in `finally`.

## 4) Boundary confirmation

- no one-run
- no retry
- no second-run
- no re-arm
- no lock reset/delete/mutation
- no deploy/public/production
- no real dealer/real lead/real customer data
- no token/secret/API key/platform auth credential/Authorization header exposure
- no PII/phone/plate/VIN exposure

## 5) Parse check and tests

- PowerShell parse-only check for launcher: PASS
- `npm run test:v19.33` => PASS
- `npm run test:v19.33A` => PASS
- `npm run test:v19.33B` => PASS

## 6) Final decision

- `PASS — owner PowerShell launcher simplified and ready for manual owner run`

## 7) Current 5-step status

- Step 1 still not completed until launcher one-run PASS
- Step 2 not started
- Step 3 not started
- Step 4 not started
- Step 5 not started
