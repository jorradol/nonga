# v13.15J Owner Credential Action Guide (Secure Auth Path Setup / No Gemini Execution)

## 1) Execution type

- owner credential action guide/readiness only: yes
- no Gemini execution: yes
- no provider network call: yes
- no one-run consumption: yes
- no runtime mutation/deploy/production/public activation: yes
- no buyer-facing release: yes
- no real lead/PII/phone/plate/VIN: yes
- no secret/API key/token exposure: yes
- no automatic retry/no second run: yes

## 2) Baseline local/origin before guide

- branch: `feature/chat-image-attachment-v1`
- local `HEAD`: `6c0a8dfe97ce400658a4bfad8c66d46456b4845c`
- `origin/feature/chat-image-attachment-v1`: `6c0a8dfe97ce400658a4bfad8c66d46456b4845c`
- local = origin: yes

## 3) Owner credential action guide summary

Goal of this guide: prepare secure authenticated operator path for next one-run retry without exposing any secret/token.

This guide explicitly separates:

1. protected route auth gate requirements
2. provider key presence requirements
3. safe operator workflow for presence-only confirmations

## 4) Firebase authenticated owner/operator path instruction

### Step A: Owner login path (secure)

1. owner performs normal secure login to staging app as admin/superadmin account
2. operator obtains authenticated request headers only through approved local auth helper/session flow
3. never paste raw Firebase ID token in chat/docs/repo/log

### Step B: Local session usage (temporary only)

1. keep token only in temporary local operator session memory/environment
2. do not save token in source files, `.env` committed files, docs, test fixtures
3. clear session variable after verification

## 5) Secret handling rule

Hard rules:

- do not paste raw token/API key in chat
- do not `echo` secret values to console
- do not commit secrets
- do not copy secrets into docs/scripts/package
- report only:
  - `present`
  - `missing`
  - `not required for this path`
  - `required but inaccessible from current operator session`

## 6) Authenticated boundary check plan

### Required checks

1. `GET /api/health` -> must return `200`
2. unauthenticated `POST /api/ai/chat-user-visible-orchestrate` -> must return `401`
3. authenticated owner/operator `POST /api/ai/chat-user-visible-orchestrate`:
   - expected to pass auth boundary first
   - may still stop at later safety/provider gates, which is acceptable for readiness confirmation

If secure authenticated token path is unavailable, stop and hold (no bypass).

## 7) Staging runtime `GEMINI_API_KEY` presence confirmation plan

Use secure infra channel only (Cloud Run/Firebase secret wiring view) and confirm presence-only:

- `GEMINI_API_KEY`: present/missing (masked only)
- no value preview
- no prefix/suffix display
- no copy into shell output, docs, or chat

## 8) `NONGA_DEV_FIREBASE_TOKEN_MAP` requirement

- not required for production-like owner-authenticated staging run
- optional only for local/non-production dev-mock rehearsal

## 9) `NONGA_ADMIN_API_TOKEN` / `VITE_NONGA_ADMIN_API_TOKEN` requirement

- for `/api/ai/chat-user-visible-orchestrate` primary secure path: not required (Firebase auth path is primary)
- can be used as helper/fallback mode only where explicitly approved
- do not treat as replacement for secure owner-authenticated Firebase session when route contract requires Firebase context

## 10) Should next round execute one-run retry?

- execute next one-run retry only when both are true:
  1. secure authenticated owner/operator path is available
  2. staging runtime `GEMINI_API_KEY` presence is confirmed via masked/presence-only method

Otherwise keep HOLD.

## 11) Carry-forward status from v13.15I

- `pilotPathActive=true`: yes
- `fallbackToLegacy=false`: yes
- one-run authorization consumed: no
- run count remains `0/1`

## 12) Safety checklist before v13.15K

1. no secret exposed in any channel
2. no runtime mutation unless explicitly approved
3. no Gemini dispatch during credential confirmation phase
4. no lead/public/production impact

## 13) Final recommendation

READY FOR OWNER TO PERFORM SECURE CREDENTIAL ACTION BEFORE v13.15K ONE-RUN RETRY
