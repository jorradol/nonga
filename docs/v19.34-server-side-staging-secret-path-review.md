# v19.34 - Server-Side Staging Secret Path Review

Date: 2026-07-07
Branch: `feature/chat-image-attachment-v1`
Execution type: `STATIC / READ-ONLY / SERVER-SIDE STAGING SECRET PATH DESIGN + READINESS REVIEW ONLY / NO EXECUTION`
Latest status before this task: `PASS — v19.33B owner PowerShell launcher simplified`
Latest commit before this task: `6a923f7`

## 1) Boundary status

- no one-run
- no retry
- no second-run
- no additional re-arm
- no lock reset/delete/mutation
- no deploy
- no public
- no production
- no real dealer action
- no real lead
- no real customer data
- no Thor real import
- no dealer real inventory import
- no token/secret/API key/platform auth credential/Authorization header exposure
- no PII/phone/plate/VIN exposure

## 2) Branch / HEAD / origin / working tree before work

- branch confirmed exact: `feature/chat-image-attachment-v1`
- local HEAD: `6a923f7`
- origin HEAD: `6a923f7`
- working tree before work: clean

## 3) v19.29-v19.33B baseline confirmation

- chain confirmed from v19.29 through v19.33B.
- owner-local token path produced repeated operational friction:
- v19.29 token missing in execution session
- v19.30 same-session diagnosis
- v19.31 same-terminal token missing
- v19.32 interactive prompt unavailable in non-interactive terminal
- v19.33/v19.33A/v19.33B launcher path still required owner terminal UX handling
- one-run attempts remain `0` in this chain.
- Step 1 still not completed.
- Step 2-5 not started.

## 4) Lock state read-only inspection

- lock file: `.nonga-owner-local-one-run-v143u.lock.json`
- `consumed=false`
- `rearmAttemptCount=1`
- `rearmedAtUtc=present`

## 5) Confirmation no re-arm / reset / delete / mutation

- no re-arm command executed in v19.34
- no lock reset/delete/mutation performed in v19.34

## 6) Server-side secret path findings (static/read-only)

- existing server runtime already reads admin/dealer token from server env:
- `src/server/apiAuth.ts` -> `process.env.NONGA_ADMIN_API_TOKEN`, `process.env.NONGA_DEALER_API_TOKEN`, `NONGA_DEALER_TOKEN_MAP`
- owner scope resolution also supports server-side admin token and Firebase-auth context:
- `src/server/ownerListingAccess.ts`
- admin/dealer routes are already guarded server-side:
- `server.ts` uses `app.use("/api/dealer", dealerApiAuth)` and `app.use("/api/admin", adminApiAuth)`
- Firebase Admin verification path already exists for trusted auth context:
- `src/server/serverAuthContext.ts`
- staging API traffic is already routed to Cloud Run service:
- `firebase.json` rewrite `/api/**` -> `run.serviceId = "nonga-staging"`
- no repo evidence requires owner terminal token for real runtime if server env secret is properly configured.

## 7) Recommended path (pivot decision)

- owner-local manual token flow should be deprecated for future real usage.
- move token ownership to server-side staging env/secret only.
- owner should use existing admin/owner auth (Firebase/admin guard), not terminal token copy/paste.
- staging-only controlled Step 1 continuation should trigger via existing admin-protected server path or server-side operator script.
- no new public route required.
- future production/public must use platform-managed secret/env or Secret Manager, never manual owner terminal token.

## 8) Minimal implementation plan (budget/time optimized)

- keep existing `/api/admin` auth guard and server auth context; avoid architecture rewrite.
- configure `NONGA_ADMIN_API_TOKEN` (and related admin credentials) in staging runtime environment only.
- remove dependency on owner-local token launcher for real staging continuation.
- reuse existing owner/admin protected route surface; do not add public route.
- add a small readiness checklist + validation script for staging env presence (masked-only checks).
- keep scope strictly Step 1 staging-only until owner approves next step.

## 9) Budget/time constraint handling

- this pivot is the fastest cost-saving path:
- stop owner-local terminal token loop and use single source of truth on server/runtime.
- no additional token-loop diagnosis should be started unless this server-side staging secret path fails.

## 10) Artifacts created/updated

- added: `docs/v19.34-server-side-staging-secret-path-review.md`
- added: `docs/examples/v19.34-server-side-staging-secret-path-review.example.md`
- added: `scripts/test-v1934-server-side-staging-secret-path-review.mts`
- updated: `package.json` (`test:v19.34`)

## 11) Static checks

- `npm run test:v19.33` => PASS
- `npm run test:v19.33A` => PASS
- `npm run test:v19.33B` => PASS
- `npm run test:v19.34` => PASS

## 12) Final decision

- `PASS — server-side staging secret path review closed, no execution`

## 13) Current 5-step status

- Step 1 still not completed until controlled owner-only staging trial PASS
- Step 2 not started
- Step 3 not started
- Step 4 not started
- Step 5 not started
