# v13.3 Admin-Only Real Gemini Proof Plan — One-Run Approval Packet / No Execution Yet

## Status

APPROVAL PACKET ONLY — no Gemini execution, no smoke endpoint call, no deploy, no production, no buyer-facing AI, no real lead

## Baseline

- Repo: `https://github.com/jorradol/nonga.git`
- Branch: `feature/chat-image-attachment-v1`
- Latest accepted pushed commit before v13.3: `674dd67a96b0db363b0bce0eaef81405522dda6a` (`674dd67`)
- Baseline checks:
  - branch = `feature/chat-image-attachment-v1`
  - local HEAD = origin HEAD
  - working tree clean

## v13.2 Source Status

v13.2 ACCEPTED / CLOSED / PUSHED with final recommendation:

- `READY FOR v13.3 ADMIN-ONLY REAL GEMINI PROOF PLAN`

Confirmed carry-forward status:

- `NONGA_ADMIN_API_TOKEN` provisioned for staging and operator/session usage.
- non-Gemini admin auth recheck passed.
- `GET /api/admin/duplicates` without header = `401`.
- `GET /api/admin/duplicates` with valid admin token = `200`.
- no token exposure, no Gemini execution, no smoke endpoint call, no real lead sending.

## Objective

Goal for future v13.4 execution slice:

- prove that staging admin-only path can invoke the Real Gemini provider under kill-switch, admin guard, and shadow-only boundary.
- keep execution strictly admin-only and controlled one-run.

Boundary reminder:

- This is not buyer-facing AI.
- This is not public route activation.
- This is not production.
- This is not real lead sending.

## Owner Approval Required For v13.4

v13.4 execution is allowed only with fresh owner approval wording:

FINAL APPROVAL FOR v13.4:  
I approve exactly one admin-only Real Gemini proof run on staging.  
Use only the approved admin smoke endpoint and sanitized test payload.  
Do not expose secrets.  
Do not use real customer data, phone, plate, VIN, or PII.  
Do not send real leads.  
Do not enable buyer-facing AI.  
Do not activate public route.  
Do not touch production.  
Do not retry automatically.  
Capture only sanitized evidence.  
Stop immediately after one run and report PASS / NEED REVIEW / HOLD.

## Admin-Only Real Gemini Proof Boundary

- staging only
- admin-only
- real provider proof only
- one run only
- sanitized payload only
- sanitized evidence only
- no buyer-facing AI
- no real lead sending
- no public route
- no production

## One-Run Limit

- v13.4 may perform exactly one authorized Real Gemini admin-only proof run.
- no automatic retry.
- no second run without fresh owner approval.

## Approved Endpoint For Future v13.4 Only

- `POST /api/admin/sales-brain-shadow-smoke`

Important:

- Do not call this endpoint in v13.3.
- this endpoint is listed only for future v13.4 approval planning.

## Input Payload Rules

Sanitized test payload template for v13.4 planning only:

```json
{
  "caseId": "SS-01",
  "mode": "admin-only-real-provider-proof",
  "locale": "th-TH",
  "buyerText": "ทดสอบแอดมินเท่านั้น สนใจรถครอบครัวมือสองงบประหยัด ไม่ใช่ข้อมูลลูกค้าจริง",
  "constraints": {
    "budgetMax": 300000,
    "location": "staging-test-only",
    "noRealCustomerData": true
  }
}
```

Mandatory data restrictions:

- No real name.
- No real phone.
- No plate.
- No VIN.
- No real buyer data.
- No real lead sending.

## Secret Handling Rules

- Do not print `NONGA_ADMIN_API_TOKEN`.
- Do not print `GEMINI_API_KEY`.
- Do not print any Authorization header.
- Do not commit secrets.
- Do not store raw request headers.
- Do not store raw provider response if it may contain sensitive data.

## Sanitized Evidence Rules

v13.3 planning evidence allows only:

- run count: 0/1 in v13.3 planning only
- planned endpoint path
- planned caseId
- expected status class
- provider called: planned only
- Gemini execution: no in v13.3
- smoke endpoint call: no in v13.3
- secret exposed: no
- real lead sent: no

## Expected Pass Criteria

Expected pass criteria for v13.4:

- exactly one authorized call made
- endpoint returns expected success status
- evidence confirms real provider path was used, sanitized only
- no secret exposure
- no real customer data
- no real lead sent
- no buyer-facing AI enabled
- no production/public route touched

## Expected Fail / Hold Criteria

- HOLD if `NONGA_ADMIN_API_TOKEN` missing
- HOLD if Gemini provider env/config missing
- HOLD if admin auth fails
- HOLD if smoke endpoint unavailable
- HOLD if any secret exposure risk appears
- NEED REVIEW if endpoint returns non-success after one run
- NEED REVIEW if provider dispatch cannot be confirmed

## Stop Conditions

Stop immediately and return HOLD if any occurs:

- missing fresh owner approval wording for v13.4
- preconditions fail for admin auth or provider prerequisites
- any secret exposure risk detected
- any attempt to perform extra run or automatic retry
- any attempt to touch production/public/buyer-facing path

## Guardrail Confirmation

- no Gemini execution
- no smoke endpoint call
- no `/api/gemini/*`
- no deploy
- no production
- no public route activation
- no buyer-facing AI
- no real lead
- no PII/customer data
- no token/API key exposed
- no runtime change

## Future v13.4 Sequence If Approved

1. Confirm repo clean and local = origin
2. Confirm fresh owner approval wording for v13.4
3. Confirm `NONGA_ADMIN_API_TOKEN` present without printing value
4. Confirm required Gemini provider env/config presence without printing secret value
5. Confirm admin auth gate using approved non-Gemini route only if necessary
6. Prepare exactly one sanitized POST request to admin smoke endpoint
7. Execute one run only
8. Capture sanitized status and evidence
9. Do not retry
10. Do not send lead
11. Do not enable buyer-facing AI
12. Record docs/test/package only
13. Commit/push sanitized execution record

## Recommendation

`READY FOR OWNER APPROVAL TO PROCEED TO v13.4 ONE-RUN ADMIN-ONLY REAL GEMINI PROOF`
