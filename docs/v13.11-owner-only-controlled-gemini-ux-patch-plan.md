# v13.11 Owner-Only Controlled Gemini UX Patch Plan

## 1) Execution type

prepare-only: yes  
docs/test/package only: yes  
code path audit / patch plan: yes  
no runtime behavior change  
no deploy  
no runtime config change  
no Gemini execution  
no smoke endpoint  
no production  
no public route  
no buyer-facing release  
no real lead  
no real PII

Scope note: this document is a static audit + patch design packet for next round only. It does not change runtime path.

## 2) Baseline and accepted context

- latest commit: `80810d2`
- v13.10 accepted
- owner manual trial PASS
- same-chat context switching PASS
- controlled Gemini UX gate PASS

## 3) Code path audit targets

### 3.1 Chat response handling

- Primary deterministic orchestrator: `src/services/ai/chat/chatSearchOrchestrator.ts` via `tryOrchestrateChatReplyCore()` and `tryOrchestrateChatReply()`.
- Entry in chat hook: `src/hooks/chat/useChat.ts` (orchestrator call + server bridge call).
- Server user-visible bridge: `src/services/ai/salesBrainServerUserVisibleOrchestrationBridge.ts` at `runUserVisibleOrchestrationBridge()`.
- Finding: current buyer UX baseline is deterministic-first (`skipGemini: true` on guarded/template flows).

### 3.2 Deterministic guardrail / off-topic guard

- Guard before search/advisor: `src/services/ai/chat/chatBuyerIntentGate.ts` via `tryBuyerIntentGateReply()`.
- Off-topic role recovery path exists and returns deterministic Thai copy (`skipGemini: true`) for poem/song/horoscope/research/image/web-price/general off-topic.
- Finance and safety wording deterministic templates: `src/services/ai/chat/chatBuyerFinanceCalculator.ts`, `src/services/ai/chat/chatBuyerAdvisorTemplates.ts`, `src/services/ai/chat/chatUsedCarSafetyAdvice.ts`.
- Finding: hard off-topic recovery and finance safety boundaries are already explicit and deterministic.

### 3.3 Gemini/provider dispatch path

- User-visible gate eval: `src/services/ai/salesBrainUserVisibleGate.ts` (`evaluateUserVisibleGate()` default-deny).
- Runtime flags + kill switch + production default off: `src/services/ai/salesBrainRuntimeFlags.ts`.
- Node pilot resolution: `src/services/ai/salesBrainShadowChatPathNode.ts` (`wireShadowChatPathWithPilot()`).
- Real provider eligibility + post-check pipeline: `src/services/ai/salesBrainUserVisibleRealProvider.ts` (`evaluateUserVisibleRealProviderEligibility()`, `maybeApplyUserVisibleRealProvider()`).
- Provider payload redaction/no-go patterns: `src/services/ai/salesBrainRealProvider.ts`.
- Finding: for user-visible real-provider path, gate/eligibility checks happen before provider invocation; output safety post-check exists before showing text.

### 3.4 In-chat memory / context switching

- Chat session scoping and active session handoff: `src/stores/chat/chatStore.ts`.
- Session-scoped follow-up context: `src/services/ai/chat/chatPilotSessionContext.ts`.
- Interest memory without contact/consent fields: `src/services/leads/conversationalLeadMemory.ts`.
- Finding: same-chat context switching remains session-scoped; memory module explicitly excludes contact/consent payload.

### 3.5 Lead confirmation boundary

- Deterministic lead capture state machine: `src/services/leads/buyerLeadCaptureFlow.ts`.
- Lead capture turn handler + modal submission gate: `src/services/leads/buyerLeadCaptureHandler.ts`.
- Input validation + consent version: `src/services/leads/buyerLeadValidation.ts`.
- API submit boundary: `src/services/leads/buyerLeadApi.ts`.
- Finding: lead send is explicit and gated (ready_for_modal + phone validate + signed-in + consent-confirmed). No implicit lead send in chat wording path.

### 3.6 PII / phone / plate / VIN handling

- Phone normalize/validate: `buyerLeadValidation.ts`.
- Sensitive-document filter in lead collection path: `buyerLeadValidation.ts`, `buyerLeadCaptureHandler.ts`.
- Provider redaction + no-go payload assertion: `salesBrainRealProvider.ts`.
- Seller draft parser includes `licensePlate` field in parsing stage: `src/services/ai/chat/sellIntentParser.ts` (deterministic parsing, not provider dispatch).
- Finding: buyer PII boundaries are deterministic; provider payload has redaction guard. Plate-like fields remain local parse context in seller draft flow.

### 3.7 Seller / fun / finance paths

- Seller listing draft parse/preview deterministic: `sellIntentParser.ts`.
- Seller listing wording helper deterministic: `listingDescriptionHelper.ts`.
- Finance explanation deterministic calculator/advisor: `chatBuyerFinanceCalculator.ts` + `chatBuyerAdvisorTemplates.ts`.
- Fun funnel entry (lucky-color/horoscope style entertainment with disclaimer) is currently handled in off-topic recovery branch: `chatBuyerIntentGate.ts`.
- Finding: seller/fun/finance paths are deterministic and can be selectively phrasing-polished later without touching lead boundary logic.

## 4) Proposed controlled Gemini patch zones (v13.12 candidate only)

Owner-only controlled candidate zones for wording/UX polish:

- natural Thai car search explanation
- car fit reason framing (not safety decisions)
- budget/location explanation wording
- compare car type explanation wording
- seller listing draft tone polish
- safe market context wording polishing
- friendly follow-up question wording
- lucky color / fun car match entertainment phrasing with explicit disclaimer
- same-chat context switching wording when follow-up has weak context

All candidate zones above are text-shaping only. They must not write lead data or bypass deterministic guard outcomes.

## 5) Must-remain deterministic zones

The following zones must remain deterministic in v13.12 as hard boundary:

- lead confirmation boundary
- phone/PII handling
- plate/VIN handling
- secret/API key/token requests
- production/public activation requests
- payment/deposit safety boundary
- legal/financial guarantee avoidance
- admin-only actions
- hard off-topic recovery

## 6) Hybrid zones (Gemini phrasing allowed, safety decision deterministic)

Hybrid candidate zones:

- finance/installment explanation phrasing
- deposit/transfer warning phrasing
- PDPA explanation phrasing
- seller listing generation phrasing with strict safety limits
- lead pre-confirmation preview phrasing using synthetic data only
- same-chat context switching response shaping

Hybrid rule: deterministic policy engine decides allowed intent and safety result first, then optional wording layer may rephrase within bounded template contract.

## 7) Required guard position

Required position for future patch:

- deterministic safety guard must run before provider dispatch on risky intents
- Gemini output must pass post-check before display (if future patch enables Gemini wording path)
- owner-only gate default must be off
- kill switch must restore deterministic response path immediately

Audit result against current code path: pre-dispatch eligibility checks and post-output safety checks are already present in server user-visible real-provider flow.

## 8) Owner-only activation design (proposal for v13.12, not active now)

Design proposal:

- env flag or admin/owner-only flag as top-level gate
- allowlist/session gating (trusted auth UID only)
- staging-only path
- budget cap guard required
- no automatic retries
- deterministic fallback on any gate miss or unsafe output

## 9) Rollback / kill switch plan

- keep deterministic orchestrator as canonical fallback path
- if owner-only Gemini UX path is disabled by flag or kill switch, response returns deterministic copy immediately
- if output safety check fails, suppress Gemini text and fallback deterministic without touching lead capture flow
- keep lead submission path independent from Gemini UX wording path to prevent accidental lead send behavior change

## 10) Risk table

| Risk | Current baseline signal | v13.12 mitigation requirement |
| --- | --- | --- |
| PII leak | Redaction + validation already exists | Keep provider input/output redaction and no-contact policy checks hard-fail |
| lead sent unintentionally | Lead flow is explicit modal + consent boundary | Keep lead API call path deterministic-only and separate from wording path |
| Gemini overclaim | Output safety guards exist | Keep strict post-check denylist and fallback deterministic |
| live-web claim | Off-topic and web-price guard exists | Keep no live-web claim policy deterministic |
| finance/legal guarantee | Deterministic finance disclaimer templates exist | Keep guarantee phrase blocker before output display |
| context drift | Session-scoped context modules exist | Keep same-session context gating and no cross-session carryover |
| off-topic role loss | Off-topic recovery deterministic | Keep hard recovery deterministic before any provider call |
| budget/cost risk | Runtime flags include budget limits | Keep budget caps mandatory for owner-only path |
| production/public accidental exposure | Production default off + gate checks | Keep production fail-closed and owner-only default off |

## 11) Final recommendation

READY FOR OWNER APPROVAL TO EXECUTE v13.12 OWNER-ONLY CONTROLLED GEMINI UX PATCH
