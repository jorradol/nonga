# v13.15B Owner-Only Gemini UX One-Run Dispatch Retry Record

## 1) Execution type

owner-only Gemini UX dispatch retry: yes  
staging only: yes  
one-run only: yes  
synthetic data only: yes  
safe-zone only: yes  
production: no  
public route activation: no  
buyer-facing release: no  
real lead sending: no  
real PII: no  
real phone/plate/VIN: no  
automatic Gemini retry: no  
secret exposure: no  
post-run rollback: yes

## 2) Fresh owner authorization

- fresh owner authorization for exactly one v13.15B owner-only Gemini UX dispatch retry: required
- authorization present: yes (explicit v13.15B owner instruction in this run)
- authorization consumed: yes
- run count allowed: `1`
- run count executed: `1/1`
- second run attempted: no

## 3) Baseline

- repo: `https://github.com/jorradol/nonga.git`
- branch: `feature/chat-image-attachment-v1`
- baseline commit before execution: `edc90e1`
- local/origin sync before execution:
  - local `HEAD`: `edc90e1709b365c72a4bbb00e26367b39b7e94a6`
  - origin branch head: `edc90e1709b365c72a4bbb00e26367b39b7e94a6`

## 4) Root cause carried from v13.15A

Carried diagnosis from v13.15A:

- previous blocker: `provider_gate_reason=ai_first_disabled`
- root cause: `NONGA_AI_FIRST_ENABLED` not truthy
- source path:
  - `evaluateUserVisibleGate()`
  - `checkUserVisiblePrerequisites()`
  - `src/services/ai/salesBrainUserVisibleGate.ts`
- classification: expected fail-closed behavior, not bug

## 5) Runtime/config masked evidence (before / active / after rollback)

### 5.1 Before temporary activation

- service: `nonga-staging`
- region: `asia-southeast1`
- project: `nonga-ce93c`
- revision: `nonga-staging-00161-nsw`
- image: `v13.14-owner-only-bb9603f`
- `NONGA_AI_PROVIDER`: present (length masked)
- `NONGA_AI_MODE`: present (length masked)
- `NONGA_AI_FIRST_ENABLED`: present (length masked, value not printed)
- `NONGA_AI_BUDGET_DAILY_LIMIT`: present (length masked)
- `NONGA_AI_BUDGET_MONTHLY_LIMIT`: present (length masked)
- `NONGA_AI_USER_VISIBLE_ENABLED`: missing
- `NONGA_AI_USER_VISIBLE_ALLOWLIST_UIDS`: missing
- `NONGA_AI_OWNER_ONLY_CONTROLLED_UX_ENABLED`: missing
- `NONGA_AI_USER_VISIBLE_REAL_PROVIDER_ENABLED`: missing
- `GEMINI_API_KEY`: present (masked metadata only; value never printed)

### 5.2 Active temporary v13.15B window

Temporary staging flags set for one-run window:

- `NONGA_AI_FIRST_ENABLED=true`
- `NONGA_AI_USER_VISIBLE_ENABLED=true`
- `NONGA_AI_USER_VISIBLE_REAL_PROVIDER_ENABLED=true`
- `NONGA_AI_OWNER_ONLY_CONTROLLED_UX_ENABLED=true`
- `NONGA_AI_USER_VISIBLE_ALLOWLIST_UIDS=<synthetic-owner-uid-only>`

Active masked evidence:

- revision: `nonga-staging-00162-6k5`
- image unchanged: `v13.14-owner-only-bb9603f`
- required temporary flags: present (length masked)

### 5.3 After immediate rollback

Rollback command removed temporary flags right after one-run.

Post-rollback masked evidence:

- revision: `nonga-staging-00163-cqc`
- image unchanged: `v13.14-owner-only-bb9603f`
- `NONGA_AI_FIRST_ENABLED`: missing
- `NONGA_AI_USER_VISIBLE_ENABLED`: missing
- `NONGA_AI_USER_VISIBLE_REAL_PROVIDER_ENABLED`: missing
- `NONGA_AI_OWNER_ONLY_CONTROLLED_UX_ENABLED`: missing
- `NONGA_AI_USER_VISIBLE_ALLOWLIST_UIDS`: missing
- core provider/mode/budget flags remain present (masked)

## 6) Preflight tests

Required preflight checks executed before one-run:

- `git status`
- `git fetch origin`
- `git rev-parse HEAD`
- `git rev-parse origin/feature/chat-image-attachment-v1`
- `npm run test:v13.12` -> pass
- `npm run test:v13.13` -> pass
- `npm run test:v13.14` -> pass
- `npm run test:v13.15` -> pass
- `npm run test:v13.15A` -> pass
- `GET /api/health` -> `200`
- unauthenticated `POST /api/ai/chat-user-visible-orchestrate` -> `401`

No smoke endpoint was called for provider dispatch in this round.

## 7) Trial scenario

Synthetic owner-only safe-zone prompt used:

`ลุงกำลังมองหา Toyota Yaris ปี 2020 งบไม่แรง ใช้ในเมืองแถวรังสิต-คูคต ช่วยอธิบายแบบเป็นธรรมชาติว่ารถแนวนี้เหมาะกับใคร และควรถามอะไรต่อดี`

Scenario constraints applied:

- synthetic owner account only
- safe-zone wording intent only
- no lead request
- no real PII
- no real phone
- no real plate/VIN

## 8) Execution result

One-run execution was performed exactly once:

- trial API envelope success: `true`
- run count before execution: `0`
- run count after execution: `1`
- second run attempted: no

Observed runtime result:

- `provider_network=false`
- `provider_gate_reason=pilot_path_inactive`
- `skip_gemini=true`
- `fallback_to_legacy=true`
- `pilot_path_active=false`

## 9) Provider dispatch evidence

Provider dispatch did not occur in v13.15B one-run due to gate:

- gate reason: `pilot_path_inactive`
- provider class target remains Gemini-capable path
- actual dispatch signal: not observed (`provider_network=false`)

Interpretation:

- v13.15A root-cause gate (`ai_first_disabled`) was addressed by setting AI-first true in temporary window
- new blocking point in this one-run is pilot path inactivity, which still fail-closed before dispatch

## 10) Owner-only / AI-first / user-visible gate evidence

- owner-only flag active during trial window: yes (masked)
- user-visible enabled flag active during trial window: yes (masked)
- real-provider user-visible flag active during trial window: yes (masked)
- allowlist configured to synthetic UID only: yes (masked)
- AI-first flag active during trial window: yes (masked)
- authenticated synthetic owner UID used: yes
- owner role provisioning for synthetic user: admin (provision success true)

## 11) Safe-zone / deterministic boundary evidence

- prompt is safe-zone wording case: yes
- deterministic boundary block triggered: no
- no lead / no PII / no phone / no plate / no VIN in trial prompt: confirmed

## 12) Post-check / fallback evidence

- post path returned deterministic legacy fallback
- fallback used: yes
- reason chain includes gate stop before provider dispatch (`pilot_path_inactive`)
- unsafe-output retry path was not used

## 13) No retry evidence

- one request only for execution
- automatic retry used: no
- second run attempted: no

## 14) No lead / no PII / no public / no production evidence

- no lead submit endpoint call
- no real lead sent
- no real PII used
- no real phone used
- no real plate/VIN used
- no public route activation
- no buyer-facing release
- no production deploy

## 15) Rollback posture

Immediate rollback completed after one-run:

- removed temporary flags:
  - `NONGA_AI_FIRST_ENABLED`
  - `NONGA_AI_USER_VISIBLE_ENABLED`
  - `NONGA_AI_USER_VISIBLE_REAL_PROVIDER_ENABLED`
  - `NONGA_AI_OWNER_ONLY_CONTROLLED_UX_ENABLED`
  - `NONGA_AI_USER_VISIBLE_ALLOWLIST_UIDS`
- owner-only and user-visible gates returned to missing/default-off posture
- public remains closed
- buyer-facing release remains closed
- deterministic fallback remains available
- no production impact
- no lead flow impact

## 16) Risk review

1. **Dispatch goal not met in one-run window**  
   - observed blocker: `pilot_path_inactive`
   - impact: one-run consumed without provider dispatch

2. **Boundary controls remained intact**  
   - no evidence of public/buyer-facing/prod exposure
   - no lead and no PII exposure

3. **Rollback safety succeeded**  
   - temporary flags removed immediately and verified

## 17) Final recommendation

HOLD — v13.15B owner-only Gemini UX dispatch retry incomplete
