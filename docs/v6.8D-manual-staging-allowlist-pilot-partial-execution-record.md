# v6.8D — Manual Staging Allowlist Pilot Partial Execution Record

**สถานะ:** Execution record — **PARTIAL PASS — negative gates OK; positive real Gemini user-visible FAIL; rollback DONE**  
**วันที่:** 2026-06-15  
**Git baseline (implementation):** `79438b028a60f94b16b2e9b364371aedf9a226af` (v6.8D)  
**Branch:** `feature/chat-image-attachment-v1`  
**Checklist:** `docs/v6.8D-staging-real-gemini-user-visible-allowlist-pilot-checklist.md`  
**Follow-up:** v6.8D.1 — diagnose & fix user-visible real Gemini invoke failure

> **Staging-only manual pilot.** No secrets, tokens, or API key values in this document.

---

## 1. Execution summary

| Item | Result |
|------|--------|
| Staging host | `https://nonga-ce93c.web.app` → Cloud Run `nonga-staging` |
| v6.8D image deployed | **YES** — tag `v6.8D-real-user-visible-allowlist-79438b0` |
| Allowlisted UID (masked) | **`X7hg...Zw2`** (operator-approved existing staging allowlist) |
| Positive real Gemini user-visible | **FAIL** — `real_provider_call_failed` |
| Negative gates (guest / non-allowlisted / kill switch / real flag off) | **PASS** |
| Fallback mock/legacy | **PASS** |
| Secret leak in responses | **None observed** |
| Rollback flags | **DONE** — revision `nonga-staging-00088-6c2` |

---

## 2. Staging revision timeline

| Revision | Action |
|----------|--------|
| `nonga-staging-00084-fhc` | Deploy v6.8D image |
| `nonga-staging-00085-wv9` | Pilot env ON (`USER_VISIBLE_ENABLED=true`, `USER_VISIBLE_REAL_PROVIDER=true`, kill switch off) |
| `nonga-staging-00086-zch` | Kill switch drill (`EMERGENCY_KILL_SWITCH=true`) |
| `nonga-staging-00087-dnn` | Real provider flag OFF drill |
| `nonga-staging-00088-6c2` | **Final rollback** — safe flags restored |

---

## 3. Pilot env (no secret values)

| Flag | Pilot window value |
|------|-------------------|
| `NONGA_AI_USER_VISIBLE_ENABLED` | `true` |
| `NONGA_AI_USER_VISIBLE_REAL_PROVIDER_ENABLED` | `true` |
| `NONGA_AI_USER_VISIBLE_ALLOWLIST_UIDS` | 1 UID (masked) |
| `NONGA_AI_EMERGENCY_KILL_SWITCH` | `false` (pilot only) |
| Shadow real provider flags | `false` |
| Budget caps | daily `5` / monthly `50` |
| `GEMINI_API_KEY` | Secret Manager mount only |

---

## 4. Positive smoke — FAIL

**Route:** `POST /api/ai/chat-user-visible-orchestrate` · allowlisted buyer token

| Scenario | pilotPathActive | realProviderNetwork | realProviderGateReason |
|----------|-----------------|---------------------|------------------------|
| Budget search (`งบ 4 แสน มีรถอะไรน่าเล่น`) | `true` | **`false`** | **`real_provider_call_failed`** |
| Finance (`ผ่อนประมาณเท่าไหร่ได้ไหม`) | `true` | **`false`** | **`real_provider_call_failed`** |
| Follow-up highlights (with session cards) | `false` | `false` | `pilot_path_inactive` |

**Observations:**
- Mock pilot fallback text returned — no raw error shown to user
- No API key / token in JSON response
- Admin shadow SS-01 on same staging stack previously succeeded with `gemini-3.5-flash`

---

## 5. Negative smoke — PASS

| Check | Result |
|-------|--------|
| Guest (no Bearer) | HTTP **401** |
| Non-allowlisted signed-in | `uid_not_allowlisted`, no real provider |
| Kill switch ON | `emergency_kill_switch`, fallback |
| Real provider flag OFF | `real_provider_flag_off`, fallback |

---

## 6. Root cause hypothesis (pre-v6.8D.1 code review)

| # | Hypothesis | Evidence |
|---|------------|----------|
| 1 | **Finance/installment preflight guard** in shared `buildProviderRequest` rejects messages containing `ผ่อน` when `listingContext.listingId` absent — throws before Gemini call | Reproduced offline for finance message |
| 2 | **User-visible SDK call shape differed from admin shadow** — user-visible used `config.systemInstruction` + split contents; admin shadow uses single merged `contents` text (known-good on staging) | Code diff vs `salesBrainAdminShadowRealProvider.ts` |
| 3 | **Orchestrated car-card count not forwarded** to real provider when client omits `pilotSessionContext` — finance/search paths lack listing context for preflight | Handler only passed session-based `pilotOrchestration` |
| 4 | **Insufficient redacted server logging** — Cloud Run logs did not surface error class/message for operator triage | Empty jsonPayload in log pull during pilot |

**Next action:** v6.8D.1 patch — align SDK call shape, inject pilot listing context for preflight, add redacted failure logs, rerun manual staging allowlist pilot.

---

## 7. Rollback — DONE

Final revision **`nonga-staging-00088-6c2`**:

| Flag | Value |
|------|-------|
| `NONGA_AI_EMERGENCY_KILL_SWITCH` | **`true`** |
| `NONGA_AI_USER_VISIBLE_ENABLED` | **`false`** |
| `NONGA_AI_USER_VISIBLE_REAL_PROVIDER_ENABLED` | **`false`** |
| Admin/chat shadow real provider | **`false`** |

---

## 8. Compliance

| Rule | Status |
|------|--------|
| Staging only | ✅ |
| Production untouched | ✅ |
| No secrets in git doc | ✅ |
| No Firestore writes for smoke | ✅ |
| No lead system changes | ✅ |
| Rollback completed | ✅ |

---

## 9. v6.8E readiness

**Not ready** until v6.8D manual pilot re-run passes with `realProviderNetwork=true` for allowlisted buyer on staging.
