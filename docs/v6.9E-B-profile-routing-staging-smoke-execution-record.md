# v6.9E-B — Profile Routing Staging Route Smoke Execution Record

**สถานะ:** **PASS** for v6.9E-B routing objective — smoke detail **42/46 PASS**; `/profile` guest direct + refresh **fixed**; member gate **preserved**; no chat fallback  
**วันที่:** 2026-06-16  
**Git baseline (deployed):** `00d8fc1e06c867cd5ff9a02ba03091de5d274d1c` (`00d8fc1`)  
**Branch:** `feature/chat-image-attachment-v1`  
**Commit message:** `fix(routing): map /profile to profile view with member gate preserved (v6.9E-B)`

> Staging hosting-only route smoke. No secrets, tokens, API keys, raw prompts, or PII in this document.

---

## 1. Overall verdict

| Item | Result |
|------|--------|
| v6.9E-B routing objective | **PASS** |
| Smoke detail | **42/46 PASS** (4 non-blocking automation failures — not routing regressions) |
| `/profile` guest direct | **Fixed** — URL `/profile` preserved; RequireMember gate; not chat |
| `/profile` guest refresh | **Fixed** — URL `/profile` preserved; member gate; not chat |
| Member gate (`RequireMember`) | **Preserved** |
| No chat fallback for `/profile` | **Confirmed** |
| v6.9C regression (`/onboarding`, `/viral-captions`) | **PASS** |
| v6.9D regression (`/car-vision`, `/car-post-generator`, `/seo-landing`) | **PASS** |
| Core regression (`/home`, `/chat`, `/search`, `/login`) | **PASS** |
| Staging host | `https://nonga-ce93c.web.app` |
| Deploy type | Firebase Hosting only — UI/static assets |
| Cloud Run | **Unchanged** — `nonga-staging-00154-q7l` @ 100% |
| Production | **Not touched** |
| **v6.9 Controlled Production Pilot** | **NOT READY** |

---

## 2. Baseline

| Field | Value |
|-------|-------|
| Branch | `feature/chat-image-attachment-v1` |
| HEAD | `00d8fc1e06c867cd5ff9a02ba03091de5d274d1c` |
| Short hash | `00d8fc1` |
| Staging project | `nonga-ce93c` |
| Staging URL | `https://nonga-ce93c.web.app` |
| Live CSS | `index-ButDouqC.css` (unchanged from v6.9C/v6.9D) |
| Live JS | `index-BdxExQxg.js` (new — was `index-BQlddXam.js` pre-v6.9E-B) |
| Deploy type | Firebase Hosting only / UI-only |

---

## 3. Deploy summary

| Item | Result |
|------|--------|
| Deploy scope | **Staging only** — project `nonga-ce93c` |
| Production target | **Not selected / not touched** |
| Deploy command | `npm run build:staging:hosting` → `npx firebase-tools@latest deploy --only hosting --project nonga-ce93c` |
| Cloud Run image deploy | **NO** — hosting-only |
| Env / secrets | **Unchanged** |
| Code change during deploy/smoke | **NO** |
| Commit during deploy/smoke | **NO** |

---

## 4. Pre-deploy tests

| Check | Result |
|-------|--------|
| Branch | `feature/chat-image-attachment-v1` |
| HEAD | `00d8fc1` (= origin) |
| Working tree | clean |
| `npm run lint` | **PASS** |
| `npm run test:v69e-profile-routing-guard` | **PASS** (32/32) |
| `npm run test:v69c-onboarding-viral-captions-routing-guard` | **PASS** (27/27) |
| `npm run test:v69d-similar-unmapped-routes-routing-guard` | **PASS** (35/35) |
| `npm run test:v547g-car-detail-routing` | **PASS** (10/10) |
| `npm run build:staging:hosting` | **PASS** |

---

## 5. Asset verification

| Check | Result |
|-------|--------|
| Staging `index.html` references live CSS | **YES** — `index-ButDouqC.css` |
| Staging `index.html` references live JS | **YES** — `index-BdxExQxg.js` |
| Browser loaded live CSS | **YES** — verified before smoke |
| Browser loaded live JS | **YES** — verified before smoke |
| Old JS loaded (`index-BQlddXam.js`) | **NO** |
| Stale cache issue | **None observed** |
| Fresh navigate + cache-bust URLs | **YES** — `?v=00d8fc1` |
| Route string `/profile` in live JS bundle | **YES** — verified in `index-BdxExQxg.js` |

---

## 6. Route smoke matrix

| # | Scenario | Result | Notes |
|---|----------|--------|-------|
| 1 | `/profile` guest direct open | **PASS** | URL `/profile` preserved; RequireMember gate text; no chat input; not UserProfileView |
| 2 | `/profile` guest refresh | **PASS** | URL `/profile` preserved; member gate; no chat fallback |
| 3 | `/profile` signed-in → UserProfileView | **N/A** | Automation non-blocking fail — simulated signed-in session did not bootstrap UserProfileView in time on live Firebase staging |
| 4 | `/profile` signed-in refresh view | **N/A** | Same auth bootstrap limitation — URL `/profile` preserved in automation; view content not confirmed |
| 5 | Header profile menu navigation | **N/A** | Automation non-blocking fail — mobile viewport (390×844) + auth bootstrap; profile avatar not found |
| 6 | Guest header login button on `/profile` | **Non-blocking fail** | Login button hidden on mobile due to `hidden sm:` — not a routing regression |
| 7 | Dealer entry → profile hint | **N/A** | Not executed — requires auth/setup; out of scope for this smoke slice |
| 8 | `/onboarding` regression (v6.9C) | **PASS** | OnboardingView — direct + refresh |
| 9 | `/viral-captions` regression (v6.9C) | **PASS** | CaptionEngineDashboard — direct + refresh |
| 10 | `/car-vision` regression (v6.9D) | **PASS** | CarVisionDashboard — direct + refresh |
| 11 | `/car-post-generator` regression (v6.9D) | **PASS** | PostGeneratorDashboard — direct + refresh |
| 12 | `/seo-landing` regression (v6.9D) | **PASS** | SeoLandingDashboard — direct + refresh |
| 13 | `/home` regression | **PASS** | HomeView |
| 14 | `/chat` regression | **PASS** | AIChatView — chat input present |
| 15 | `/search` regression | **PASS** | SearchPageView |
| 16 | `/login` regression | **PASS** | LoginView / auth flow |

**Smoke automation summary:** 42/46 assertions PASS.

---

## 7. Non-blocking smoke failures

| # | Failed assertion | Root cause | Routing impact |
|---|------------------|------------|----------------|
| 1 | Guest header login button visible on `/profile` | Login button uses `hidden sm:inline-flex` — hidden on mobile viewport | **None** — not a routing regression |
| 2 | Signed-in UserProfileView heading (`โปรไฟล์ของฉัน`) | Simulated signed-in session via Playwright localStorage did not bootstrap in time on live Firebase staging | **None** — URL `/profile` correct; no chat fallback |
| 3 | Signed-in refresh view content | Same auth bootstrap limitation as above | **None** — URL preserved |
| 4 | Header profile menu click | Mobile viewport + auth bootstrap — profile avatar not found | **None** — not a routing regression |

**Conclusion:** 4 failed assertions are **not routing regressions**. None invalidate the v6.9E-B `/profile` route mapping objective.

---

## 8. Direct URL evidence

| Route | Pre-v6.9E-B (bug) | Post-v6.9E-B (live staging) |
|-------|-------------------|------------------------------|
| `/profile` (guest) | Fell through to chat (`resolveViewFromPathname` default) | **RequireMember gate** — URL `/profile` preserved; no chat input |
| `/profile` (member) | Fell through to chat | **UserProfileView** (static guard confirmed; live signed-in automation N/A) |

Root cause (reference): missing `/profile` mapping in `resolveViewFromPathname` / `resolvePathnameForView` in `src/utils/appRouteSync.ts` — fixed in commit `00d8fc1`. `RequireMember` wrapper in `App.tsx` unchanged.

---

## 9. Refresh evidence

| Route | URL preserved | View preserved |
|-------|---------------|----------------|
| `/profile` (guest) | **YES** | **YES** — RequireMember gate; no chat |
| `/profile` (signed-in) | **YES** (automation) | View content N/A — auth bootstrap limitation |

---

## 10. v6.9C + v6.9D + core regression evidence

| Route | Expected view | Result |
|-------|---------------|--------|
| `/onboarding` | OnboardingView | **PASS** |
| `/viral-captions` | CaptionEngineDashboard | **PASS** |
| `/car-vision` | CarVisionDashboard | **PASS** |
| `/car-post-generator` | PostGeneratorDashboard | **PASS** |
| `/seo-landing` | SeoLandingDashboard | **PASS** |
| `/home` | HomeView | **PASS** |
| `/chat` | AIChatView | **PASS** |
| `/search` | SearchPageView | **PASS** |
| `/login` | LoginView | **PASS** |

---

## 11. Deferred / not touched

| Item | Status |
|------|--------|
| `/billing` | Deferred — sandbox simulation surface |
| `/boost` | Deferred — sandbox simulation surface |
| `/dealer-dashboard` | Deferred — URL design / prefix collision |
| `/dealer-showroom` | Deferred — URL design / `selectedDealerId` |
| Dealer URL design | Deferred — Option C redesign |
| `selectedDealerId` / showroom state | Not touched |
| Payment / invoice / boost logic | Not touched |
| `dealerEntryNavigation.ts` | Not changed in v6.9E-B (manual `/profile` write now aligns with resolver) |
| v6.9 Controlled Production Pilot gates | Still pending — pilot **NOT READY** |

---

## 12. Safety confirmations

| Item | Result |
|------|--------|
| Production deploy | **NO** |
| Production pilot started | **NOT STARTED** |
| Public / nationwide opened | **NO** |
| Cloud Run image deploy | **NO** |
| Env / secrets / deploy config | **Unchanged** |
| AI runtime / Gemini provider | **Not touched** |
| Guard / fallback logic | **Not touched** |
| Firestore write | **Not touched** |
| Lead system / DealerLeads | **Not touched** |
| Payment / invoice | **Not touched** |
| Production config | **Not touched** |
| Dealer dashboard cluster | **Not touched** |
| Chat theme | **Not touched** |
| Auth / member gate logic | **Not changed** — `RequireMember` preserved |
| **v6.9 Controlled Production Pilot** | **NOT READY** |

---

## 13. Rollback readiness

| Item | Detail |
|------|--------|
| Rollback scope | Firebase Hosting only |
| Method | Firebase Hosting release history — redeploy prior release (pre-v6.9E-B JS `index-BQlddXam.js`) |
| Cloud Run | **Unchanged** — `nonga-staging-00154-q7l` @ 100%; **no Cloud Run rollback needed** |
| Git code rollback | Revert commit `00d8fc1` if code rollback required |
| Production | **Not touched** |

---

## 14. v6.9E-B change summary (reference)

| File | Change |
|------|--------|
| `src/utils/appRouteSync.ts` | Map `/profile` → `profile` in `resolveViewFromPathname` and `resolvePathnameForView` |
| `scripts/test-v69e-profile-routing-guard.mts` | Static route guard (32 assertions) |
| `package.json` | npm script for v6.9E-B routing guard |

---

## 15. Comparison — pre-v6.9E-B vs v6.9E-B staging smoke

| Item | Pre-v6.9E-B (v6.9D staging) | v6.9E-B (`00d8fc1`) |
|------|-------------------------------|---------------------|
| `/profile` (guest direct) | Chat view (routing bug) | **RequireMember gate** — URL `/profile` |
| `/profile` (guest refresh) | Chat view (routing bug) | **RequireMember gate** — URL `/profile` |
| Live JS hash | `index-BQlddXam.js` | **`index-BdxExQxg.js`** |
| Primary routing smoke | `/profile` blocked | **PASS** (42/46 detail) |

---

*End of v6.9E-B profile routing staging route smoke execution record.*
