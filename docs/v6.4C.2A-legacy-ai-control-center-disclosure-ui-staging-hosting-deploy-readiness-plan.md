# v6.4C.2A — Legacy AI Control Center Disclosure UI Staging Hosting Deploy Readiness Plan

**สถานะ:** **PLANNING** — deploy readiness plan only — ยังไม่ deploy, ยังไม่ env update จริง  
**วันที่:** 2026-06-12  
**Git baseline:** `676baada30f02cf7dd181ca6d635032773618d21` (v6.4C.2 pushed)  
**Branch:** `feature/chat-image-attachment-v1`  
**Current staging live bundle:** `index-DMz8UVjA.js` (v6.4C.A — unchanged until deploy)  
**Staging URL:** `https://a.nongbot.org`  
**อ้างอิง:** v6.4C.2 Disclosure UI, v6.4C.1 Audit, v6.4C.A Execution Record, v6.4C Read-Only Panel

> **v6.4C.2A (this slice) = deploy readiness plan เท่านั้น** — **ไม่ใช่การ deploy จริง** — staging hosting deploy → **approval แยก**  
> **Firebase Hosting only** @ `nonga-ce93c` — **no Cloud Run / no Firestore / no production / no AI enable**

---

## สารบัญ

0. [Baseline & Working Tree](#0-baseline--working-tree)
1. [Goal](#1-goal)
2. [Non-goals](#2-non-goals)
3. [Deploy Scope](#3-deploy-scope)
4. [Untracked Deploy Plan Doc Policy](#4-untracked-deploy-plan-doc-policy)
5. [Env Update Plan](#5-env-update-plan)
6. [UI Contract (v6.4C.2 live)](#6-ui-contract-v642-live)
7. [Preflight Gates](#7-preflight-gates)
8. [Build & Bundle Hash](#8-build--bundle-hash)
9. [Deploy Execution Steps](#9-deploy-execution-steps)
10. [Automated Post-Deploy Smoke](#10-automated-post-deploy-smoke)
11. [Manual Smoke Checklist (ลุง)](#11-manual-smoke-checklist-ลุง)
12. [Rollback Plan](#12-rollback-plan)
13. [Execution Record Plan](#13-execution-record-plan)
14. [Forbidden Actions](#14-forbidden-actions)
15. [Compliance](#15-compliance)

---

## 0. Baseline & Working Tree

| Field | Value |
|-------|-------|
| Branch | `feature/chat-image-attachment-v1` |
| HEAD / origin | `676baada30f02cf7dd181ca6d635032773618d21` (`676baad`) |
| Last commit message | `fix(v6.4C.2): disclose legacy AI control center as demo and not connected` |
| Staging live JS | `index-DMz8UVjA.js` |
| Staging live CSS | `index-CtTXHG2V.css` (v6.4C.A deploy session) |
| Cloud Run staging (expected unchanged) | `nonga-staging-00076-hmh` |
| Production API (expected unchanged) | `nonga-api-00003-fg4` |
| AI / Gemini | **not enabled** |
| Production | **untouched** |

### Git status at plan time (expected)

```text
## feature/chat-image-attachment-v1...origin/feature/chat-image-attachment-v1
?? docs/v6.4C.A-admin-read-only-ai-control-status-panel-staging-hosting-deploy-readiness-plan.md
```

| Item | Status |
|------|--------|
| Working tree clean? | ⚠️ **ไม่ clean** — มี **untracked deploy plan doc v6.4C.A** เท่านั้น |
| v6.4C.2A plan doc (this file) | ยังไม่ commit จนกว่า approval แยก |
| Staged files | **none** |

---

## 1. Goal

Deploy **v6.4C.2 Legacy AI Control Center disclosure UI remediation** ไปยัง staging hosting เพื่อให้ลุง/admin เห็นบน live staging:

| Goal ID | Description |
|---------|-------------|
| G-01 | Deploy = **Firebase Hosting only** @ `nonga-ce93c` |
| G-02 | Primary smoke URL = `https://a.nongbot.org` |
| G-03 | Legacy `AIControlCenter` แสดง **safety banner** + **Demo / Not connected** |
| G-04 | **ไม่มี** misleading `LIVE SYNC` / `Gemini 3.5 Flash Core Speed` บน live |
| G-05 | Analytics = **mock/placeholder/no live data source** labels ชัด |
| G-06 | v6.4C `AiControlStatusPanel` ยังอยู่ด้านบน — **OFF / read-only** ไม่ regression |
| G-07 | ไม่ regression v6.3B.6 golden seller / preview (replicate v6.4C.A build env) |
| G-08 | Manual smoke + execution record หลัง deploy |

---

## 2. Non-goals

| Item | v6.4C.2A plan status |
|------|----------------------|
| Firebase Hosting deploy (this slice) | ❌ **plan only** — separate approval |
| Cloud Run deploy / env update | ❌ **forbidden** |
| Firestore rules deploy / writes | ❌ **forbidden** |
| Production touch | ❌ **forbidden** |
| AI / Gemini / shadow real provider enable | ❌ **forbidden** |
| `NONGA_AI_CHAT_SHADOW_REAL_PROVIDER_ENABLED=true` | ❌ **forbidden** |
| New public route / public debug endpoint | ❌ **forbidden** |
| Enable/activate/start real Gemini button | ❌ **forbidden** |
| New form / write action beyond legacy CRUD | ❌ **forbidden** |
| Disable legacy CRUD (write-protect) | ❌ **not in deploy slice** — v6.4C.2 kept CRUD + warnings |
| DB persist / backend write ใหม่ | ❌ **forbidden** |
| lead/payment/reveal/outcome mutation | ❌ **forbidden** |
| import/cleanup/hide / public signup | ❌ **forbidden** |
| gcloud secrets access | ❌ **forbidden** |
| Commit untracked v6.4C.A deploy plan doc | ❌ **forbidden in deploy slice** |
| full UID / PII / secret ใน repo/report | ❌ **forbidden** |

---

## 3. Deploy Scope

| Field | Value |
|-------|-------|
| **Deploy target** | Firebase Hosting **only** |
| **Project** | `nonga-ce93c` |
| **Hosting site** | `nonga-ce93c` |
| **Primary URL** | `https://a.nongbot.org` |
| **Alt URL** | `https://nonga-ce93c.web.app` |
| **Deploy command** | `npx -y firebase-tools@latest deploy --only hosting --project nonga-ce93c` |
| **Build command** | `npm run build:staging:hosting` (env per §5) |
| **Code commit (expected)** | `676baada30f02cf7dd181ca6d635032773618d21` |
| **Cloud Run `nonga-staging`** | ❌ **must not change** — expect `nonga-staging-00076-hmh` |
| **Production `nonga-api`** | ❌ **must not touch** — expect `nonga-api-00003-fg4` |
| **Firestore rules deploy** | ❌ **must not run** |
| **Deploy-session Firestore writes** | ❌ **none** (legacy CRUD UI unchanged — admin browser actions same as pre-deploy) |

### What changes on staging after deploy

| Layer | Change |
|-------|--------|
| Hosting JS/CSS bundle | New hash replaces `index-DMz8UVjA.js` |
| Admin `ai-control` tab — legacy section | Disclosure banners, badge/copy remediation (v6.4C.2) |
| Admin `ai-control` tab — v6.4C panel | **Unchanged behavior** — still read-only OFF |
| Backend / AI runtime | **No change** |
| Public routes | **No new routes** |

---

## 4. Untracked Deploy Plan Doc Policy

| File | Policy |
|------|--------|
| `docs/v6.4C.A-admin-read-only-ai-control-status-panel-staging-hosting-deploy-readiness-plan.md` | **Untracked** — **ห้าม stage/commit** ใน v6.4C.2A deploy slice |
| `docs/v6.4C.2A-...-readiness-plan.md` (this plan) | Commit แยกเมื่อ approval — **ไม่ใน deploy execution slice** |
| Deploy execution record (post-deploy) | `docs/v6.4C.2A-staging-hosting-deploy-execution-record.md` — after manual smoke |

**Deploy session checklist:** ก่อน `git add` / deploy — ยืนยัน `git status` ไม่มี staging ของ v6.4C.A plan doc

---

## 5. Env Update Plan

v6.4C.2 disclosure = **UI copy/banners only** — **ไม่ต้องเปิด env ใหม่**

### Deploy session env policy

| Policy | Rule |
|--------|------|
| v6.4C.2 disclosure | **No new Vite env keys** |
| Staging parity | **Replicate v6.4C.A live build env** — ไม่ regression golden seller / preview |
| AI/shadow flags | **Do not change** |
| `VITE_NONGA_PUBLIC_SIGNUP_ENABLED` | คง `false` |
| Cloud Run env | **Do not touch** |
| gcloud secrets | **Do not access** |

### Keys to replicate from v6.4C.A live (deploy session only — private UIDs)

| Key | Deploy session value | Notes |
|-----|----------------------|-------|
| `VITE_NONGA_BUYER_FRIENDLY_COPY_PREVIEW_ENABLED` | `true` | same as live `index-DMz8UVjA.js` |
| `VITE_NONGA_BUYER_FRIENDLY_COPY_PREVIEW_ALLOWLIST_UIDS` | approved tester UIDs (comma-separated) | **ห้าม commit** — report masked/count only |
| `VITE_NONGA_PUBLIC_SIGNUP_ENABLED` | `false` | unchanged |
| `VITE_FIREBASE_*` | via `build-staging-hosting.mts` default | unchanged flow |

### Masked allowlist reference (record only — same as v6.4C.A)

| Field | Value |
|-------|-------|
| Allowlist count | **4 UIDs** |
| Masked examples | `UUvg…hK2`, `h0xj…SC3`, `IjEx…l23`, `IcoN…0x1` |

### Keys that must NOT change / must NOT enable

| Key | Rule |
|-----|------|
| `NONGA_AI_CHAT_SHADOW_REAL_PROVIDER_ENABLED` | ห้าม `true` (Cloud Run — ไม่แตะ) |
| `NONGA_AI_*` runtime flags | ห้าม env update บน Cloud Run |
| New AI control / disclosure env | **ไม่เพิ่ม** |

---

## 6. UI Contract (v6.4C.2 live)

```text
Admin Dashboard → tab "ai-control" (existing)
  ├─ AiControlStatusPanel (v6.4C — read-only OFF — unchanged authority)
  ├─ SmartSalesAiControlPreview (superadmin — read-only)
  └─ AIControlCenter (v6.4C.2 — legacy disclosure remediation)
       ├─ legacy-ai-control-safety-banner
       ├─ Legacy admin config + Demo / Not connected badges
       ├─ legacy-ai-control-write-warning
       └─ analytics tab — mock/placeholder labels
```

### Legacy section must display (post-deploy)

| Element | Expected |
|---------|------------|
| Safety banner | AI provider **OFF**, Real Gemini **not enabled**, mock metrics, no secrets/UID |
| Header badges | `Legacy admin config`, `Demo / Not connected` |
| Write warning | Legacy config persistence — not real provider runtime |
| Analytics tab | Mock analytics banner, Placeholder badges, no live data source |
| Fine-tuning block | Future-ready + not connected to real Gemini |

### Legacy section must NOT display (post-deploy)

| Element | Rule |
|---------|------|
| `LIVE SYNC` | ❌ **absent** |
| `Gemini 3.5 Flash Core Speed` | ❌ **absent** |
| `Enterprise Co-Pilot Core` (misleading live tone) | ❌ **replaced** by Legacy admin config |
| Enable/activate/start real Gemini button | ❌ **absent** |
| New write actions beyond existing legacy CRUD | ❌ **absent** |

### v6.4C panel regression (must remain)

| Check | Expected |
|-------|----------|
| `AiControlStatusPanel` visible first | ✅ |
| Provider OFF / Real Gemini not enabled | ✅ |
| Read-only — no enable button in panel | ✅ |

---

## 7. Preflight Gates

รันก่อน deploy จริง (หลัง approval แยก):

```bash
npm run test:v64c2-legacy-ai-control-center-disclosure-ui-remediation
npm run test:v64c1-legacy-ai-control-center-disclosure-safety-audit
npm run test:v64c-admin-read-only-ai-control-status-panel-readiness
npm run test:v64b-ai-control-model-types-static-readiness
npm run lint
npm run build:staging:hosting
```

| Step | Check |
|------|-------|
| PF-01 | `git status` — **no staged v6.4C.A plan doc**; HEAD = `676baad` (or approved commit) |
| PF-02 | All preflight npm scripts **PASS** |
| PF-03 | Set deploy session env (§5) — replicate v6.4C.A preview flag + allowlist |
| PF-04 | `npm run build:staging:hosting` **PASS** |
| PF-05 | Record `NEW_BUNDLE_JS` / `NEW_BUNDLE_CSS` from `dist/assets/` (§8) |
| PF-06 | Grep bundle markers (§8) — v6.4C.2 disclosure + v6.4C regression |
| PF-07 | Project = `nonga-ce93c` only |
| PF-08 | Deploy = `--only hosting` only |
| PF-09 | No Cloud Run / Firestore rules / production commands |
| PF-10 | No gcloud secrets access |

### Regression preflight (recommended)

```bash
npm run test:v63b6-golden-seller-voice-refinement
npm run test:v63b5-in-chat-curated-sales-copy-weave
```

---

## 8. Build & Bundle Hash

### Current live (pre-deploy)

| Asset | Hash |
|-------|------|
| **Live JS** | `index-DMz8UVjA.js` |
| **Live CSS** | `index-CtTXHG2V.css` |

### Expected after build (deploy session)

| Asset | Expected |
|-------|----------|
| **New JS** | `dist/assets/index-<HASH>.js` — **record at build time** |
| **New CSS** | `dist/assets/index-<HASH>.css` — may change (v6.4C.2 added Tailwind classes) |

### Local reference (@ `676baad`, default build env — **not** deploy session)

| Asset | Reference (not deployed) |
|-------|--------------------------|
| JS | `index-3rqJr71o.js` (local preflight @ default env) |
| CSS | `index-D9QT0jB9.css` |

> **Deploy session ต้องใช้ env เดียวกับ v6.4C.A live** — hash บันทึกใน execution record หลัง build

### Bundle markers — v6.4C.2 disclosure (grep new JS)

| Marker | Required |
|--------|----------|
| `legacy-ai-control-center` | ✅ |
| `legacy-ai-control-safety-banner` | ✅ |
| `Legacy admin config` | ✅ |
| `Demo / Not connected` | ✅ |
| `Legacy config persistence` | ✅ |
| `legacy-ai-control-analytics-mock-banner` | ✅ |
| `Mock AI Analytics` | ✅ |
| `no live data source` | ✅ |
| `Placeholder` | ✅ |
| `Not connected to real Gemini` | ✅ |
| `Gemini runtime not enabled` | ✅ |

### Bundle markers — must be ABSENT in new JS

| Marker | Rule |
|--------|------|
| `LIVE SYNC` | ❌ absent |
| `Gemini 3.5 Flash Core Speed` | ❌ absent |
| `Enterprise Co-Pilot Core` | ❌ absent (replaced) |
| `enable real gemini` / `activate real gemini` / `start real gemini` | ❌ absent |
| `generateContent` | ❌ absent |

### Bundle markers — v6.4C + regression (must remain)

| Marker | Required |
|--------|----------|
| `ai-control-status-panel` | ✅ |
| `AI provider is OFF` | ✅ |
| `Real Gemini is not enabled` | ✅ |
| `Read-only readiness` | ✅ |
| `DETERMINISTIC_ONLY` | ✅ |
| `คันนี้จุดที่น่าดูคือ` or golden seller hooks | ✅ (if preview env replicated) |
| `featureWeave` | ✅ |
| `บทเกณฑ์คัดสรรของน้องเอ` | ✅ |

---

## 9. Deploy Execution Steps

```text
Step 0   Confirm git status — untracked v6.4C.A plan doc only (§4)
Step 1   Preflight (§7) — all PASS
Step 2   Confirm git HEAD @ 676baad (or approved commit)
Step 3   Set deploy session env (§5) — replicate v6.4C.A preview flag + allowlist (private)
Step 4   npm run build:staging:hosting
Step 5   Record NEW_BUNDLE_JS / NEW_BUNDLE_CSS (§8)
Step 6   Grep bundle markers (§8) — disclosure + regression
Step 7   npx -y firebase-tools@latest deploy --only hosting --project nonga-ce93c
Step 8   Automated post-deploy smoke (§10)
Step 9   Manual browser smoke — ลุง (§11)
Step 10  Write execution record (§13) — redacted
Step 11  Rollback plan on standby (§12)
```

---

## 10. Automated Post-Deploy Smoke

Smoke base: **`https://a.nongbot.org`**

| Check ID | Check | Expected | Record |
|----------|-------|----------|--------|
| AD-01 | `GET /` HTTP status | 200 | pass/fail |
| AD-02 | Live HTML references `NEW_BUNDLE_JS` | yes | hash |
| AD-03 | Previous `index-DMz8UVjA.js` absent in HTML | yes | pass/fail |
| AD-04 | `GET /assets/<NEW_BUNDLE_JS>` HTTP 200 | yes | pass/fail |
| AD-05 | Legacy safety banner testid in live JS | `legacy-ai-control-safety-banner` | pass/fail |
| AD-06 | Demo / Not connected copy in live JS | yes | pass/fail |
| AD-07 | Legacy admin config copy in live JS | yes | pass/fail |
| AD-08 | Mock analytics banner in live JS | `legacy-ai-control-analytics-mock-banner` | pass/fail |
| AD-09 | `LIVE SYNC` absent in live JS | yes | pass/fail |
| AD-10 | `Gemini 3.5 Flash Core Speed` absent in live JS | yes | pass/fail |
| AD-11 | v6.4C panel testid in live JS | `ai-control-status-panel` | pass/fail |
| AD-12 | Provider OFF copy in live JS | `AI provider is OFF` | pass/fail |
| AD-13 | Cloud Run staging revision | `nonga-staging-00076-hmh` unchanged | pass/fail |
| AD-14 | Production API revision | `nonga-api-00003-fg4` unchanged | pass/fail |
| AD-15 | Golden seller regression marker | present if env replicated | pass/fail |

---

## 11. Manual Smoke Checklist (ลุง)

**When:** หลัง staging hosting deploy (future approval)  
**Where:** `https://a.nongbot.org`  
**Path:** Admin Dashboard → tab **`ai-control`**  
**Expected bundle:** `NEW_BUNDLE_JS` (record from deploy session)  
**Sessions:** Admin role minimum; Superadmin optional for SmartSales preview

### MS-4C2A-A — v6.4C panel regression (top)

| ID | Check | Pass |
|----|-------|------|
| MS-A01 | เข้า Admin → tab `ai-control` | ☐ |
| MS-A02 | **AI Control Status Panel** ยังอยู่ **ด้านบน** | ☐ |
| MS-A03 | Panel ยัง **read-only** / Provider **OFF** | ☐ |
| MS-A04 | **Real Gemini is not enabled** ยังเห็นใน panel | ☐ |

### MS-4C2A-B — Legacy disclosure (v6.4C.2 primary)

| ID | Check | Pass |
|----|-------|------|
| MS-B01 | Legacy section มี **safety banner** (`legacy-ai-control-safety-banner`) | ☐ |
| MS-B02 | Banner ระบุ AI provider **OFF** + Real Gemini **not enabled** | ☐ |
| MS-B03 | **ไม่เห็น** `LIVE SYNC` แบบชวนเข้าใจผิด | ☐ |
| MS-B04 | เห็น **`Demo / Not connected`** | ☐ |
| MS-B05 | เห็น **`Legacy admin config`** | ☐ |
| MS-B06 | เห็น **write warning** — legacy config persistence, not real provider | ☐ |
| MS-B07 | Analytics tab — **mock/placeholder/no live data** labels | ☐ |
| MS-B08 | **ไม่เห็น** `Gemini 3.5 Flash Core Speed` | ☐ |
| MS-B09 | Fine-tuning / memory / deals metrics มี disclosure mock/future/not connected | ☐ |
| MS-B10 | **ไม่มี** enable/activate/start real Gemini button | ☐ |
| MS-B11 | **ไม่มี** form/write action **ใหม่** เกิน legacy เดิม | ☐ |
| MS-B12 | **ไม่มี** secret / full UID / raw prompt / env dump | ☐ |

### MS-4C2A-C — Scope / infra

| ID | Check | Pass |
|----|-------|------|
| MS-C01 | Live bundle = `NEW_BUNDLE_JS` (DevTools) | ☐ |
| MS-C02 | Cloud Run / Firestore / production **unchanged** | ☐ |
| MS-C03 | Real Gemini ยัง **ไม่เปิด** (runtime) | ☐ |
| MS-C04 | ไม่มี public route / debug endpoint ใหม่ | ☐ |
| MS-C05 | Spot check golden seller weave ไม่ regress (optional) | ☐ |

**Verdict:** PASS / FAIL — record in execution doc (redacted)

---

## 12. Rollback Plan

| Trigger | Action | Scope |
|---------|--------|-------|
| Disclosure regression / scope concern | Redeploy v6.4C.A bundle | **hosting only** |
| Golden seller / preview regression | Rebuild with v6.4C.A env → redeploy | **hosting only** |
| Any production concern | **Stop** — do not deploy production | — |

### Rollback target

| Item | Value |
|------|-------|
| Previous live JS | `index-DMz8UVjA.js` |
| Previous live CSS | `index-CtTXHG2V.css` |
| Previous commit (reference) | `cafe966` (v6.4C.A execution record) or rebuild from `676baad` parent deploy artifact |

```bash
# Rebuild from commit before v6.4C.2 OR restore dist from v6.4C.A deploy session
# Set v6.4C.A env (§5) then:
npm run build:staging:hosting
npx -y firebase-tools@latest deploy --only hosting --project nonga-ce93c
# Target live bundle: index-DMz8UVjA.js
```

### Rollback must NOT touch

- Cloud Run env / revision deploy
- Firestore rules deploy / writes (beyond existing legacy admin UI behavior)
- Production API
- AI/Gemini/shadow enable flags
- gcloud secrets

---

## 13. Execution Record Plan

**File (after deploy + manual smoke):**  
`docs/v6.4C.2A-staging-hosting-deploy-execution-record.md`

| Section | Content rule |
|---------|--------------|
| Metadata | project, branch, commit `676baad`, date, hosting-only |
| Working tree | note untracked v6.4C.A plan doc if still present |
| Preflight | §7 gate results |
| Build | NEW_BUNDLE_JS hash; preview flag yes/no; allowlist **count only** |
| Deploy | `--only hosting`; Cloud Run revision **unchanged** |
| Automated smoke | §10 AD-01..AD-15 |
| Manual smoke | §11 MS-4C2A-A/B/C verdict |
| Safety | §14 checklist |
| Rollback | §12 reference |

**Optional follow-up test script (after record written):**  
`scripts/test-v64c2a-staging-hosting-deploy-execution-record.mts`  
`npm run test:v64c2a-staging-hosting-deploy-execution-record`

**Forbidden in execution record:** full UID, plate, phone, LINE, per-car PII, secrets, raw image URLs, raw prompt dumps

---

## 14. Forbidden Actions

| Forbidden | v6.4C.2A deploy |
|-----------|-----------------|
| Deploy (this slice) | ❌ plan only |
| Stage/commit v6.4C.A untracked plan doc | ❌ |
| Cloud Run / Firestore rules / production | ❌ |
| AI/Gemini/shadow enable | ❌ |
| env update beyond §5 | ❌ |
| gcloud secrets | ❌ |
| public route / debug endpoint | ❌ |
| enable button / new write in panel | ❌ |
| DB persist / lead/payment/reveal/outcome | ❌ |
| PII/secrets/full UID in repo/report | ❌ |

---

## 15. Compliance

| Guardrail | Plan status |
|-----------|-------------|
| Hosting only @ nonga-ce93c / a.nongbot.org | ✅ |
| Baseline + git status documented | ✅ |
| Untracked v6.4C.A plan doc policy | ✅ |
| Preflight tests listed | ✅ |
| Build + bundle hash policy | ✅ |
| v6.4C.2 disclosure markers | ✅ |
| v6.4C regression markers | ✅ |
| Automated + manual smoke | ✅ |
| Rollback hosting-only | ✅ |
| Execution record template | ✅ |
| No deploy in this slice | ✅ |

### Suggested commit message (plan doc only — after approval)

```
docs(v6.4C.2A): add staging hosting deploy readiness plan for legacy disclosure UI
```

---

## Readiness Summary

```text
v6.4C.2A Legacy Disclosure UI Staging Deploy — PLAN ONLY (no deploy)

baseline: 676baad @ feature/chat-image-attachment-v1
live: index-DMz8UVjA.js (v6.4C.A)
change: v6.4C.2 disclosure banners/copy on hosting bundle
untracked: v6.4C.A deploy plan doc — do not stage in deploy slice
deploy: awaiting separate approval
AI/Gemini: not enabled
production: NO-GO
```
