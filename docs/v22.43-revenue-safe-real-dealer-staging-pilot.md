# v22.43 — Revenue-Safe Real Dealer Staging Pilot

Date: 2026-07-10  
Branch: `feature/chat-image-attachment-v1`  
Execution type: `REVENUE-SAFE REAL DEALER STAGING PILOT EXECUTION / EXISTING nonga-dealer FIRST / BATCH 1-3 REAL LISTINGS / OWNER-APPROVAL GATED / LEAD CAPTURE OFF / NO REAL LEAD / NO DEALER-FACING SEND / NO PRODUCTION / NO PUBLIC`

## 1) Owner approval statement

Owner explicitly approved starting the **Revenue-safe Real Dealer Staging Pilot** with:

- existing **`nonga-dealer`** first  
- batch **1–3** real listings in **staging only**  
- lead capture / dealer-facing send / public signup / production remain **OFF**

This packet executes that approved pilot only.

## 2) Preflight baseline

| Check | Result |
|-------|--------|
| Cloud Run | **`nonga-staging-00192-6th`** @ 100% |
| Hosting | **`assets/index-CHSJ9MTc.js`** |
| Health | `ok: true`, firestore, firebase-storage |
| `leadCaptureEnabled` | **false** |
| `publicSignupEnabled` | **false** |
| Unauth buyer-lead | **401** |
| Marketplace before pilot | **13** |
| TEST titles public | **0** |
| Public protected fields | **0** |
| Thor Auto listings/images | **13/13** with images |
| `nonga-dealer` | **active** (`role=dealer`, status active) |
| Production | Untouched |
| Dealer-send enabled | **No** |

## 3) Chosen input path

| Decision | Value |
|----------|--------|
| Path | Existing **Dealer Portal drafts** already on `nonga-dealer` → 「ลงขาย」 via `POST /api/dealer/drafts/:id/publish` |
| Why | Proven in v22.39–v22.42; no new import system; no new dealer; approval gate preserved |
| Not used | Admin Draft Inventory; Thor demo login; new dealer provisioning |
| Batch selected | **2** of max 3 existing publish-ready drafts (Corolla 2020 + Corolla 2021) |

## 4) Listings submitted (safe public form)

| # | Title (public) | Listing id (redacted) | Images | Status after submit |
|---|----------------|------------------------|--------|---------------------|
| 1 | Toyota Corolla 2020 | `car-178364…4039` | 5 | `pending_review` |
| 2 | Toyota Corolla 2021 | `car-178364…4475` | 5 | `pending_review` |

Public-sale-safe fields used: brand, model, year, price, mileage, images, sale title.  
**Not** included in public DTO/report: VIN, full plate, phone, address, ownerId, dealerId, customer PII.

Batch size: **2** (within 1–3). No extra retry listings.

## 5) Pending-review / 「รออนุมัติ」 evidence

| Check | Result |
|-------|--------|
| Publish HTTP | **200** / **200** |
| `listingStatus` after publish | **`pending_review`** both |
| Success message (live API) | 「ส่งรายการเรียบร้อยแล้ว รถของคุณอยู่ระหว่างรอผู้ดูแลอนุมัติ ก่อนแสดงในตลาด」 |
| Raw admin-token error | **Not returned** |
| Marketplace while pending | **13** (unchanged) |
| Admin pending queue | **2** pilot rows present before approve |

## 6) Owner/admin approve/hold evidence

| Listing | Action | Result |
|---------|--------|--------|
| Toyota Corolla 2021 | **Approve** | `published` |
| Toyota Corolla 2020 | **Approve** | `published` |
| Hold used? | No (both public-sale-safe: brand/model/year/price/images present) | |
| Dealer self-approve | Still blocked by policy (`assertCanPublishDealerListingToMarketplace` / route gate) | |

Approve message: 「อนุมัติประกาศแล้ว — แสดงในตลาดได้」

## 7) Marketplace + public DTO + images

| Check | Result |
|-------|--------|
| Marketplace before | **13** |
| Marketplace while pending | **13** |
| Marketplace after approve | **15** (= 13 + 2) |
| Pilot titles public | **2** |
| Images on public cards | **5** each |
| Public DTO `ownerId` / `dealerId` / `vin` / `licensePlateFull` | **Absent** |
| `sellerType` | `dealer` |
| `dealerSlug` | `nonga-dealer` (allowed public slug) |
| `sellerDisplayName` | `Unknown Owner` (see follow-up) |
| `dealerDisplayName` | `null` (see follow-up) |

### Soft follow-up (non-blocking for gate/DTO)

Dealer profile display fields for `nonga-dealer` are thin (`Unknown Owner` / null `dealerDisplayName`). Safe for privacy, but owner may want to fill showroom/owner display name before treating the pilot as customer-facing polish.

## 8) Rollback / cleanup path (documented; not executed on approved keepers)

| Action | Route / UI |
|--------|------------|
| Hold / hide from marketplace | Admin `/admin/pending-listings` 「พักไว้ก่อน」 or `POST /api/admin/listings/:id/hold` → `hidden` |
| Hide after publish | Dealer inventory visibility hide (dealer-scoped) or admin hold |
| Delete dealer-owned listing | `DELETE /api/dealer/inventory/:id` (dealer session; use carefully) |
| Expected count after full rollback of both pilot cars | **13** |

**This packet did not cleanup the two approved pilot listings** (owner did not request cleanup). They remain marketplace-visible as the staging pilot batch.

## 9) Final safety checks

| Check | Result |
|-------|--------|
| `leadCaptureEnabled:false` | **PASS** |
| `publicSignupEnabled:false` | **PASS** |
| Unauth buyer-lead 401 | **PASS** |
| No real lead created | **PASS** |
| No dealer-facing send / auto notification | **PASS** |
| No production deploy | **PASS** |
| Public signup not enabled | **PASS** |
| No new dealer | **PASS** |
| Batch ≤ 3 | **PASS** (2) |
| Listing statuses clear | **PASS** (both `published` after approve) |
| Marketplace expected | **PASS** (15) |
| Thor Auto preserved | **PASS** (original 13 remain; +2 pilot) |
| Public DTO safe | **PASS** |
| Secrets/PII in report | **PASS** (redacted ids only) |

## 10) Tests

| Check | Result |
|-------|--------|
| `npm run test:v22.39` | **PASS** |
| `npm run test:v22.40` | **PASS** |
| `npm run test:v22.41` | **PASS** |
| `npm run test:v22.42` | **PASS** |
| `npm run test:v22.43` | **PASS** |

## 11) Recommendation

**PASS**

Two real public-sale-safe listings were submitted through the approved Dealer Portal draft publish path on existing **`nonga-dealer`**, entered **`pending_review`** with canonical waiting-for-approval copy, stayed off marketplace until owner/admin approve, then became public with safe DTO and images. Marketplace **13 → 15**. Lead capture, dealer-facing send, public signup, and production remain **OFF**. Rollback/hold path is documented; approved keepers left live per owner intent.

### Owner next (optional)

1. Fill `nonga-dealer` showroom/display name so public cards are not 「Unknown Owner」.  
2. Decide whether to keep both Corollas live on staging or hold/hide one/both.  
3. Fresh approval required before lead capture ON, dealer-facing send, public signup, or production.

### Explicit non-actions

- Did not create a new dealer  
- Did not exceed 1–3 listings  
- Did not include VIN/full plate/phone/address/PII in public DTO/report  
- Did not ask for token/secret/env/terminal  
- Did not set `NONGA_LEAD_CAPTURE_ENABLED=true`  
- Did not create real leads / dealer-facing send  
- Did not deploy production / enable public signup  
