# v12.5 Client Command URL Construction Review — No Retry
Status: REVIEW ONLY — no retry, no Gemini execution

## Baseline

- Repo: `https://github.com/jorradol/nonga.git`
- Branch: `feature/chat-image-attachment-v1`
- Local HEAD at review start: `ea50f50ff22addf5477005b6af797c9a573b34da`
- `origin/feature/chat-image-attachment-v1` at review start: `ea50f50ff22addf5477005b6af797c9a573b34da`
- local = origin: yes
- working tree clean: yes

## Source Event

Reviewed source event (owner-provided sanitized):

- execution decision: `NEED REVIEW`
- curl exit code: `3`
- client error: `URL rejected: Port number was not a decimal number between 0 and 65535`
- HTTP status captured: `400000`
- requestDispatched: `unknown`
- confirmed real provider call: `unknown`
- confirmed provider output captured: `no`
- post_run_health_status: `200`
- authorization consumed: `yes` (conservative hold)
- retry allowed: `no`

## Interpretation

- v12.4 is not Gemini PASS.
- v12.4 is not confirmed provider timeout.
- request dispatch could not be confirmed.
- confirmed real provider call: unknown.
- provider output captured: no.
- health after run remained `200`.
- authorization consumed in that one-run attempt remains accepted as conservative hold.
- no retry is allowed without fresh `FINAL EXECUTION AUTHORIZE`.

## URL Construction Review

Static review scope covered:

- `scripts/test-v61h-real-gemini-shadow-call-admin-only-staging-smoke.mts`
- `src/services/ai/adminShadowSmokeApi.ts`
- `src/utils/safeApiFetch.ts`
- `src/services/ai/salesBrainServerShadowSmoke.ts`
- `src/services/ai/salesBrainAdminShadowDiagnostics.ts`
- `docs/v12.3-new-authorized-one-run-plan-prepare-only.md`
- `docs/v12.1-no-gemini-runtime-diagnostics-capture-patch.md`

Findings:

1. Client runtime code for admin shadow smoke uses relative route `"/api/admin/sales-brain-shadow-smoke"` via `safeApiFetch` and does not construct a curl command.
2. No in-repo client smoke caller path was found that concatenates host/port into a manual curl URL for v12.4.
3. Therefore, the failing URL assembly likely happened in external/manual client command construction (outside these static modules).
4. The specific error message is strongly consistent with malformed `host:port` interpolation (empty/non-decimal/contaminated port token).
5. Existing docs in v12.3 define endpoint path and policy but do not enforce a strict, validated shell-safe URL template.

URL/base URL component assessment for v12.4 event:

- endpoint path: known (`/api/admin/sales-brain-shadow-smoke`)
- base URL source for failed curl: unknown from repo evidence
- host env used by failed curl: unknown
- port env used by failed curl: unknown
- malformed port possibility: yes (high likelihood from curl error text)

## Curl Exit Code 3 Analysis

- `curl exit code: 3` indicates URL format rejection on client side before a valid HTTP transaction.
- The reported message pinpoints URL parsing failure at port parsing stage.
- This supports a client command failure boundary, not a confirmed server/provider runtime failure boundary.

## HTTP Status Capture Ambiguity

- `HTTP status captured: 400000` is not a valid HTTP status code.
- Plausible parsing/reporting artifact: a client-side formatter concatenated/derived `400` with curl `%{http_code}` fallback `000`, yielding `400000`.
- In reviewed runtime code, admin snapshot status is a numeric field (`httpStatus`) and server-side valid values are `200` or `400`; no in-repo logic emits `400000`.
- Result: status capture is ambiguous and should not be used as provider/server success/failure proof.

## Request Dispatch Assessment

- request dispatch could not be confirmed from the source event.
- v12.4 event remains insufficient to assert that request reached server route handler.
- Client command failure boundary remains the most supported interpretation.

## Provider Call Assessment

- confirmed real provider call: unknown.
- confirmed provider output captured: no.
- no evidence supports provider success, provider timeout completion, or provider output capture for this event.

## Guardrail Confirmation

- no retry
- no Gemini execution
- no smoke endpoint call
- no `/api/gemini/*`
- no deploy
- no production
- no public route activation
- no buyer-facing AI
- no real lead
- no PII/customer data exposure
- no secret/token/API key exposed
- no automatic retry
- no runtime behavior change

## Recommendation

HOLD — fix client command construction before any new authorized one-run

Sanitized client command template for next authorized run preparation (template only, no execution in v12.5):

```text
BASE_URL="<staging-origin-without-trailing-slash>"
CASE_ID="SS-01"
ADMIN_TOKEN="<masked-or-env-ref-only>"

POST_URL="${BASE_URL}/api/admin/sales-brain-shadow-smoke"

# Pre-validate URL and port token before any execution attempt:
# - BASE_URL must be absolute http(s) URL
# - if explicit port exists, it must be decimal 0..65535
# - no whitespace/newline/control chars in BASE_URL
# - quote POST_URL as one shell argument
```

Recommendation line:

HOLD — fix client command construction before any new authorized one-run
