# v9.0 - Limited Admin-Only Runtime Proof GO Boundary Record

> **LIMITED GO ONLY / ADMIN-ONLY RUNTIME PROOF PREPARATION ONLY / DOCS-ONLY / STATIC VALIDATION ONLY**
> Owner decision หลัง v8.9 คือ Limited GO เพื่อเตรียมขอบเขต runtime proof แบบ admin-only เท่านั้น
> ไม่ใช่ production GO, ไม่ใช่ buyer-facing GO, ไม่ใช่ user-visible GO, ไม่ใช่ Gemini activation GO และไม่ใช่ real lead GO

---

## 1. Owner Limited GO Decision

เอกสารนี้บันทึกการตัดสินใจของ owner ว่าเลือก **Limited GO** หลัง v8.9 โดย Limited GO ในรอบนี้หมายถึงการอนุญาตเฉพาะงานเตรียม boundary สำหรับ admin-only runtime proof เท่านั้น

- Limited GO = GO เพื่อเตรียม admin-only runtime proof boundary
- not a production GO
- not a buyer-facing GO
- not a user-visible GO
- not a Gemini activation GO
- not a real lead GO
- deterministic flow remains source of truth

## 2. Allowed Scope

สิ่งที่อนุญาตเฉพาะในรอบ v9.0 นี้:

- docs-only record
- static validation
- boundary definition
- preparation checklist
- future runtime-proof prerequisites
- admin-only proof planning
- kill switch / fallback / logging redaction / quota cap / admin-only boundary documentation

## 3. Forbidden Scope

สิ่งที่ห้ามทำในรอบนี้โดยเด็ดขาด:

- no src changes
- no runtime activation
- no Gemini activation
- no buyer-facing AI
- no user-visible AI
- no production deploy
- no deploy
- no real lead sending
- no real secret/API key in repo
- no public route
- no change to deterministic flow
- no change to real lead flow
- no change to dealer, buyer, or admin runtime behavior

## 4. Runtime Proof Prerequisites (Future Only)

ก่อนเข้าสู่ runtime proof จริงในอนาคต ต้องมีอย่างน้อย:

- admin-only route boundary confirmed
- environment variable names documented without real values
- kill switch default OFF
- fallback deterministic response confirmed
- logging redaction checklist
- quota cap checklist
- no PII leak checklist
- no buyer-visible path checklist
- rollback/disable instruction
- explicit owner approval for runtime-touching work

## 5. Source of Truth

- deterministic flow remains source of truth
- Gemini/admin-only runtime proof, if approved in the future, is proof path only
- buyer path must remain deterministic until explicit future approval

## 6. Explicit Non-Activation and Non-Production Confirmation

เอกสาร v9.0 นี้เป็น boundary record เท่านั้น และยืนยันว่า:

- no runtime activation
- no Gemini activation
- no production deploy
- no deploy
- no buyer-facing AI
- no user-visible AI
- no real lead sending
- no real secret/API key in repo
- no public route
- deterministic flow remains source of truth

## 7. Owner Approval Gate for Runtime-Touching Work

งานที่แตะ runtime จริงในอนาคตต้องผ่านการอนุมัติใหม่แบบ explicit จาก owner ก่อนเริ่มเสมอ

- current decision: Limited GO for admin-only runtime proof preparation only
- runtime-touching status: not approved in v9.0
- production status: not approved
- buyer-facing status: not approved
- user-visible status: not approved
