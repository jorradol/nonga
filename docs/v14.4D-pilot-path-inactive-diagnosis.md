# v14.4D — Pilot Path Inactive Diagnosis

Date: 2026-07-05  
Branch: `feature/chat-image-attachment-v1`

## Scope (This Round)

- Execution type: docs/tests/fixtures/static diagnosis + safe request/response/gate inspection only.
- Agent execution boundary:
  - one-run execution by agent: no
  - retry by agent: no
  - second run by agent: no
  - provider/Gemini/runtime call by agent: no
  - provider network call by agent: no
  - deploy/runtime config mutation/production/public release by agent: no

## Exact Source of `pilot_path_inactive`

- file: `src/services/ai/salesBrainUserVisibleRealProvider.ts`
- function: `maybeApplyUserVisibleRealProvider()`
- condition:
  - `if (!input.bridgeResult.payload.pilotPathActive) { ... realProviderGateReason: "pilot_path_inactive" }`
- inputs checked:
  - `input.bridgeResult.payload.pilotPathActive`
  - eligibility must pass earlier (`evaluateUserVisibleRealProviderEligibility(...)`)
- blocked reason:
  - bridge result entered real-provider stage with `pilotPathActive=false`, so provider dispatch is blocked fail-closed

## Why `pilotPathActive=false` Happened

Static evidence from in-memory bridge path (`runUserVisibleOrchestrationBridge`):

- inactive prompt used in wrapper before this patch:
  - `ช่วยอธิบายแบบสั้นและสุภาพว่ารถ 2 คันนี้ต่างกันอย่างไรในภาพรวม โดยไม่ขอข้อมูลติดต่อครับ`
- follow-up classifier result:
  - `isPilotBuyerFollowUpMessage(...) === false`
- bridge result with same synthetic pilot context:
  - `pilotPathActive=false`
  - `fallbackToLegacy=true`
- then real-provider gate result:
  - `realProviderGateReason=pilot_path_inactive`
  - `realProviderNetwork=false`

So root cause is prompt classification/routing into fallback legacy branch, not missing token/auth schema extraction.

## Request Payload Analysis (Wrapper)

Wrapper request body already includes:

- `userMessage`
- `attachedImageCount`
- `pilotSessionContext.recentCarCards`
- `pilotSessionContext.lastSearchBudgetMax`

No extra owner/admin marker is required in body for this route. Auth role/UID comes from Firebase token on server side.

Patch applied:

- switched wrapper synthetic dispatch prompt to a follow-up-safe prompt proven to activate pilot path in static in-memory checks:
  - `ลูกค้าทดลองถามแบบไม่มีข้อมูลจริง: ช่วยสรุปจุดเด่นของคันที่ 1 และ 2 แบบสุภาพสำหรับครอบครัวหน่อยครับ`

## Response Extractor Analysis

Wrapper extractor paths are correct and conservative:

- `success.data.pilotPathActive`
- `success.data.fallbackToLegacy`
- `success.data.skipGemini`
- `success.data.realProviderNetwork` (fallback `data.providerNetwork`)
- `success.data.realProviderGateReason` (fallback `data.gateReason`)
- `success.data.userVisibleGateDiagnostic.allowlistMatch` (fallback `data.allowlistMatch`)
- `success.data.userVisibleRuntimeDiagnostic.userVisibleEnabled` (fallback `data.userVisibleEnabled`)
- guard metadata from runtime diagnostic with safe fallback paths

No fake field synthesis is present.

## Allowlist / Auth (Masked-Only)

- allowlist logic:
  - `src/services/ai/salesBrainUserVisibleGate.ts`
  - env key: `NONGA_AI_USER_VISIBLE_ALLOWLIST_UIDS`
  - check: `uidAllowlisted = allowlist.includes(uid)`
- auth model:
  - `src/server/serverAuthContext.ts`
  - uses `Authorization: Bearer <Firebase ID token>`
  - UID resolved by Firebase Admin verify path
- bridge/runtime diagnostics only expose masked UID and masked allowlist entries

## Staging Runtime Version / Gate State

From repo-only evidence, `dispatchHttpStatus=200` + schema parse success + new diagnostics appearing indicate deployed route includes expected diagnostics shape.

However, repository alone cannot prove exact deployed runtime revision hash at request time.  
Result classification:

- staging runtime deploy hash certainty: unavailable from static repo-only evidence
- gate behavior certainty: confirmed for observed response (`pilot_path_inactive`)

## One-Run Approval Update

`v14.3AI` one-run namespace is consumed and must not be retried.

Wrapper dispatch approval phrase updated to `v14.3AJ`:

```bat
echo FINAL EXECUTION AUTHORIZE v14.3AJ USER-VISIBLE FIREBASE DISPATCH SAME-CMD EXACTLY-ONE-RUN> v14.3AJ-user-visible-dispatch-local-approval.txt
npm run owner-local-one-run:v14.3AC:user-visible -- --execute-approved --dispatch-approved --approval-file v14.3AJ-user-visible-dispatch-local-approval.txt
```
