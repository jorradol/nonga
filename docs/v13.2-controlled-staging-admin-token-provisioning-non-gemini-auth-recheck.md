# v13.2 Controlled Staging Admin Token Provisioning — Non-Gemini Auth Recheck Only

## Status

COMPLETED — controlled staging-only provisioning and non-Gemini admin auth recheck only

- no token value
- no token print
- token value exposed: no
- secret value exposed: no
- no Gemini execution
- no smoke endpoint call

## Owner Approval

Reviewed and applied scope from owner approval for v13.2:

- staging-only `NONGA_ADMIN_API_TOKEN` provisioning
- same token across runtime/server env and operator/session env
- non-Gemini admin auth recheck only
- no Gemini execution, no smoke endpoint, no production, no public route activation, no buyer-facing AI, no real lead sending

## Baseline

- Repo: `https://github.com/jorradol/nonga.git`
- Branch: `feature/chat-image-attachment-v1`
- Baseline commit before v13.2: `766dcd21923199456793e3e2ff2fbcc838ef447a` (`766dcd2`)
- Baseline checks:
  - branch = `feature/chat-image-attachment-v1`
  - local = origin: yes
  - working tree clean: yes

## Scope

Executed scope only:

- repo baseline check
- dedicated admin token source gate without value output
- operator/session env setup for `NONGA_ADMIN_API_TOKEN`
- staging runtime/server env setup for `NONGA_ADMIN_API_TOKEN`
- token presence check without reporting length/value
- approved non-Gemini admin auth recheck target only: `GET /api/admin/duplicates`
- sanitized evidence recording

## Token Handling Confirmation

- dedicated `NONGA_ADMIN_API_TOKEN` source available: yes
- Gemini API key used as admin token: no
- Firebase service account used as admin token: no
- Google OAuth token used as admin token: no
- no token value in chat/report/log/doc/commit
- no token print
- no token echo
- token value exposed: no
- secret value exposed: no
- same token used for runtime/server env and operator/session env: yes

## Operator/Session Env Setup

- operator/session env target: `NONGA_ADMIN_API_TOKEN`
- presence check result: `NONGA_ADMIN_API_TOKEN present: yes`
- value exposure: no
- length reporting: not reported
- value reporting: `[REDACTED]` only

## Staging Runtime/Server Env Setup

- staging runtime/server env target: `NONGA_ADMIN_API_TOKEN`
- target service: `nonga-staging`
- target region: `asia-southeast1`
- staging runtime/server env updated: yes
- production touched: no
- public route activation: no
- secret value exposed: no

## Non-Gemini Admin Auth Recheck

- endpoint: `GET /api/admin/duplicates`
- without admin token/header status: `401`
- with valid admin token/header status: `200`
- expected 401/200 met: yes
- response body captured: no
- network calls count: 2
- automatic retry used: no
- no `/api/gemini/*`
- no Gemini execution
- no smoke endpoint call
- no lead sending

## Sanitized Evidence

- `NONGA_ADMIN_API_TOKEN present: yes`
- `NONGA_ADMIN_API_TOKEN length: not reported`
- `NONGA_ADMIN_API_TOKEN value: [REDACTED]`
- `header preview: Authorization: Bearer [REDACTED]`
- `token value exposed: no`
- `secret value exposed: no`

## Guardrail Confirmation

- no token value exposed
- no token print
- no secret committed
- no Gemini execution
- no smoke endpoint call
- no `/api/gemini/*`
- no production
- no public route activation
- no buyer-facing AI
- no real lead sending
- no PII/customer data

## Result

PASS — controlled staging provisioning and approved non-Gemini admin auth recheck completed under guardrails.

## Recommendation

`READY FOR v13.3 ADMIN-ONLY REAL GEMINI PROOF PLAN`
