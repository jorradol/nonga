# v14.3AB — Provider Path Mapping Decision Packet After v14.3AA

## 1) Execution type

v14.3AB target type: provider path mapping decision packet (docs/tests/fixtures/static architecture review)  
one-run execution by agent: no  
retry by agent: no  
second run by agent: no  
provider/Gemini/runtime call by agent: no  
provider network call by agent: no  
deploy by agent: no  
runtime config mutation by agent: no

## 2) Boundary status

- no production: `yes`
- no public route activation: `yes`
- no buyer-facing AI release: `yes`
- no real lead sending: `yes`
- no real customer data / PII: `yes`
- no phone / plate / VIN: `yes`
- no token / secret / API key exposure: `yes`
- no Thor real data import: `yes`
- no dealer real inventory import: `yes`

## 3) Branch / HEAD / origin / working tree before work

- branch: `feature/chat-image-attachment-v1`
- local HEAD: `94afaedc98aec58c5977c17d3778aa8616782d2f`
- origin HEAD: `94afaedc98aec58c5977c17d3778aa8616782d2f`
- local equals origin: `yes`
- working tree clean: `yes`

## 4) Previous v14.3AA final decision

`NEED REVIEW — EXISTING PROVIDER PATH NOT SAFELY MAPPED`

## 5) Gate 0 repo sanity result

Executed:

```bash
git branch --show-current
git rev-parse HEAD
git fetch origin
git rev-parse origin/feature/chat-image-attachment-v1
git status --short
```

Result:

- branch check: pass
- local/origin sync: pass
- working tree clean: pass
- starting HEAD exact match: pass (`94afaedc98aec58c5977c17d3778aa8616782d2f`)
- Gate 0 decision: `PASS`

## 6) Files/routes inspected (read-only)

- `scripts/owner-local-one-run-gate-v143u.mts`
- `docs/v14.3AA-controlled-runtime-adapter-preflight-diagnosis.md`
- `docs/examples/v14.3AA-controlled-runtime-adapter-preflight.synthetic.json`
- `scripts/test-v143AA-controlled-runtime-adapter-preflight.mts`
- `src/services/ai/salesBrainServerShadowSmoke.ts`
- `src/services/ai/salesBrainServerUserVisibleOrchestrationBridge.ts`
- `src/services/ai/salesBrainUserVisibleGate.ts`
- `src/services/ai/salesBrainUserVisibleRealProvider.ts`
- `src/server/serverAuthContext.ts`
- `src/server/apiAuth.ts`
- `src/services/ai/chat/chatUserVisibleOrchestrateClient.ts`
- `server.ts`
- `package.json`

Markers confirmed in inspected source:

- `sales-brain-shadow-smoke`
- `chat-user-visible-orchestrate`
- `NONGA_ADMIN_API_TOKEN`
- `getServerAuthContext`
- `pilotPathActive`
- `fallbackToLegacy`
- `skipGemini`
- `providerNetwork` / `realProviderNetwork`
- `real_provider_call_ok`
- `guardPolicyVersion`
- `leadPiiCueGuardActive`
- `phoneEchoGuardActive`
- `safeConfirmationStepWordingActive`
- `allowlistMatch`
- `userVisibleEnabled`

## 7) Option mapping table (A vs B)

| Mapping dimension | Option A — admin-shadow proof path | Option B — user-visible orchestrate path |
|---|---|---|
| Endpoint model | `/api/admin/sales-brain-shadow-smoke` | `/api/ai/chat-user-visible-orchestrate` |
| Auth model | `NONGA_ADMIN_API_TOKEN` via `adminApiAuth` | Firebase ID token via `getServerAuthContext` |
| Owner-local wrapper compatibility (`owner-local-one-run-gate-v143u`) | High (same token family) | Low (wrapper does not mint/pass Firebase ID token) |
| Same-CMD feasibility | High with existing token gate | Low unless auth model changes |
| Fresh approval compatibility | High (existing pattern) | Medium (requires new auth ceremony wording) |
| Exactly-one-run compatibility | High (current one-run lock semantics align) | Medium (needs separate auth/session handling to avoid ambiguity) |
| Reuse proven runtime path | High for admin-shadow provider proof path | High for user-visible Thai UX path once auth is valid |
| Thai UX / guardrail evidence (`guardPolicyVersion`, `leadPiiCueGuardActive`, etc.) | Limited for buyer-visible quality proof | Strong; these markers are native to this route payload |
| Public/prod/real lead/PII risk | Lower if kept admin-only synthetic (`SS-01..SS-08`) | Higher if mapping is wrong; must stay fully authenticated and non-public |
| Token exposure risk | Low with existing masked admin-token checks | Medium if ad-hoc token bridge attempted incorrectly |
| Retry/second-run risk | Low with existing lock behavior | Medium unless run mode and auth path are isolated cleanly |
| Patch needed for v14.3AC if chosen | Minimal controlled bridge to admin-shadow provider route only | Auth-model redesign (wrapper to Firebase context) or separate owner command |
| Must not touch | No public routes, no runtime mutation, no deploy | Same; additionally no manual auth bypass |
| Expected success evidence | `realProviderGateReason=real_provider_call_ok` on admin-shadow diagnostics | user-visible runtime markers + Thai UX guard markers + real provider gate reason |
| Expected HOLD evidence if not passed | token/approval/one-run gate hold | missing Firebase auth context / gate block / unsafe mapping hold |

## 8) Option A admin-shadow mapping summary

- Route exists and is explicitly admin-gated: `server.ts` mounts `/api/admin` with `adminApiAuth`, and `registerSalesBrainAdminShadowSmokeRoutes`.
- Path is synthetic/admin-readonly oriented and already surfaces `real_provider_call_ok` and `providerNetwork` diagnostics under controlled gates.
- Current wrapper (`scripts/owner-local-one-run-gate-v143u.mts`) validates `NONGA_ADMIN_API_TOKEN` and is structurally closest to this path.
- This option is safer for auth continuity, but it will not by itself prove buyer-facing Thai UX quality on user-visible route.

## 9) Option B user-visible orchestrate mapping summary

- Route exists and is explicitly Firebase-auth bound: `handleChatUserVisibleOrchestratePost` calls `getServerAuthContext(req)` and rejects missing/invalid Firebase token.
- Path naturally exposes user-visible diagnostics (`pilotPathActive`, `fallbackToLegacy`, `skipGemini`, `allowlistMatch`, `guardPolicyVersion`, Thai UX/guard markers).
- This option is strongest for proving Thai UX + user-visible guard behavior.
- Current owner-local wrapper is not auth-compatible with this route because it only checks admin token model.

## 10) Auth model mismatch analysis

Mismatch is real and safety-relevant:

- Wrapper gate auth contract: `NONGA_ADMIN_API_TOKEN` (admin token model).
- User-visible route auth contract: Firebase ID token + server-verified identity (`getServerAuthContext` / `verifyFirebaseIdToken`).
- A direct bridge from current wrapper to user-visible route without explicit auth-model redesign would be unsafe and ambiguous.

Bridge feasibility without manual endpoint guess:

- Safe bridge **does not currently exist** in v14.3AB source.
- To use Option B safely, either:
  - replace wrapper auth model to Firebase ID token flow, or
  - introduce a separate owner command dedicated to Firebase-authenticated user-visible proof path.
- Any attempt to force Option B through admin-token wrapper would violate fail-closed mapping intent.

## 11) Provider path recommendation

`NEED OWNER DECISION — AUTH MODEL TRADEOFF NOT SAFE TO CHOOSE AUTOMATICALLY`

Two-owner-choice question (max 2 options):

1. Choose `Option A (admin-shadow)` for auth-compatible controlled provider-path proof first.
2. Choose `Option B (user-visible orchestrate)` and approve explicit auth-model change (Firebase ID token path) before one-run.

## 12) What v14.3AB patch did (and did not do)

Patch type in this round: `docs/tests/static decision packet only`

- no runtime/provider dispatch enablement
- no manual endpoint guess
- no runtime config mutation
- no deploy/public/prod change
- no buyer-facing activation
- no real lead/data flow

## 13) No-run / no-retry / no-second-run confirmation

- one-run command executed by agent: no
- retry attempted by agent: no
- second run attempted by agent: no

## 14) Runtime/provider/Gemini status during agent work

- runtime adapter execution by agent: `not_run`
- provider call by agent: `not_run`
- Gemini call by agent: `not_run`
- provider network by agent: `not_run`

## 15) Token/secret exposure confirmation

- no raw token/API key/secret printed
- no auth header value exposed
- masked/status-only token handling preserved

## 16) Expected v14.3AC patch scope per owner choice

If owner selects Option A:

- Keep current admin-token wrapper model.
- Patch only controlled provider-path mapping from wrapper to admin-shadow proof path (fail-closed).
- Keep user-visible path untouched in this step.

If owner selects Option B:

- Do not reuse admin-token wrapper as-is.
- Patch wrapper auth model to Firebase ID token flow **or** create separate command dedicated to user-visible orchestrate proof.
- Keep fail-closed default when Firebase context is missing/invalid.

## 17) Final decision

`READY FOR OWNER DECISION — CHOOSE ADMIN-SHADOW OR USER-VISIBLE PROVIDER PATH`

## 18) Exact next owner action

Choose one path explicitly:

- `Option A`: proceed v14.3AC with admin-shadow controlled provider-path patch.
- `Option B`: approve Firebase-auth model change/new command first, then proceed v14.3AC for user-visible orchestrate path.
