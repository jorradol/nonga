# v14.3D — Controlled Owner Approval Packet / No Gemini Yet

## 1) Execution type

controlled owner approval packet: yes  
code/docs/tests static/mock only: yes  
read-only inspection only: yes  
runtime execution: no  
Gemini run: no  
one-run click: no  
retry: no  
second run: no  
provider network call: no  
endpoint call that may trigger provider: no  
production deploy: no  
staging deploy: no  
public route activation: no  
buyer-facing AI release: no  
real lead sending: no  
real customer data / PII: no  
phone/plate/VIN examples: no  
secret/token/API key exposure: no  
Thor real data import: no  
dealer real inventory import: no  
runtime config mutation: no  
secret value change: no

## 2) Packet purpose and current status

v14.3D provides an owner-readable approval packet template for a future owner-only staging one-run decision.

This packet is a decision envelope only. It is not execution permission in this round.

Current approval state must remain:

- `approval status now: not granted`
- `future fresh owner approval required: yes`

## 3) Owner approval statement template (future use only)

Template text for a future explicit approval event:

`I, owner, authorize exactly ONE owner-only staging pilot dry-run. Gemini/provider call may occur only for this approved run. No retry. No second run. No public/prod/real lead. Synthetic/sanitized only.`

Required template metadata:

- owner name / signer
- approval date-time (ISO)
- approved run count (`exactly 1`)
- environment (`staging only`)
- audience (`owner/admin only`)
- synthetic scope confirmation (`synthetic/sanitized only`)
- explicit denials (`no retry`, `no second run`, `no public route`, `no production`, `no real lead`)
- expiry window / one-time token semantics (if used in future runtime phase)

This template is a draft artifact only in v14.3D. It is not granted authorization now.

## 4) One-run scope checklist (must all be true)

- staging only
- owner/admin only
- allowlist required
- synthetic/sanitized inventory only
- no real buyer data
- no real phone capture
- no lead delivery
- no public route
- no production
- one run only
- no retry
- no second run
- runSessionLocked required

If any checklist item is unclear or false, outcome must be HOLD.

## 5) Pre-run GO/HOLD checklist

Set HOLD immediately when any condition occurs:

- route/auth/gate ambiguity
- public/production exposure risk
- real lead risk
- PII/secret risk
- marker missing
- visibility field missing
- approval wording ambiguous
- run count not explicit
- runSessionLocked missing
- tests fail

Pre-run GO requires all controls visible and owner wording unambiguous.

## 6) Post-run evidence checklist (future one-run must return)

Required evidence keys:

- `httpStatus`
- `auth`
- `runtimeMode`
- `userVisibleEnabled`
- `pilotPathActive`
- `fallbackToLegacy`
- `skipGemini`
- `providerNetwork`
- `gateReason`
- `guardPolicyVersion`
- `thaiUxTuningSliceId`
- `thaiUxTuningActive`
- `leadPiiCueGuardActive`
- `phoneEchoGuardActive`
- `safeConfirmationStepWordingActive`
- `sanitizedUserVisibleText`
- `missingUserVisibleText`
- `answerFieldSource`
- `answerCharCount`
- `runSessionLocked`
- `oneRunConsumed`
- `noRetry`
- `noSecondRun`

Evidence interpretation requirements:

- guard/Thai UX markers must match approved baseline policy
- missing/empty answer evidence must force HOLD pending owner review
- lock and one-run counters must prove no retry/second-run behavior
- evidence packet must remain sanitized and free of PII/secrets

## 7) Anti-misleading wording (mandatory explicit lines)

At v14.3D packet time, these statements must remain exactly true:

- Gemini run now: no
- one-run approval granted now: no
- pilot active now: no
- public active now: no
- production active now: no
- real lead path active now: no
- retry allowed: no
- second run allowed: no

Any wording that implies already-approved execution is forbidden in this slice.

## 8) Owner-readable summary (plain decision framing)

Owner summary in simple terms:

- v14.3D is an approval packet draft, not a run.
- It tells what must be checked before a future one-run can be approved.
- It defines strict boundaries: staging-only, owner-only, synthetic-only, no public/prod/real lead.
- It requires one-run-only controls: explicit count, no retry, no second run, run lock.
- It defines required post-run evidence so owner can decide GO/HOLD safely.

Short owner-facing sentence:

`This is an approval envelope only, not execution. Approval is not granted yet. Fresh explicit owner approval is required before any future one-run.`

## 9) Read-only inspection anchors for packet completeness

- `docs/v14.3C-controlled-staging-pilot-preflight-visibility-check-no-gemini.md`
- `docs/v14.3B-staging-only-pilot-gate-preflight-no-gemini.md`
- `docs/v14.2B-controlled-staging-deploy-thai-ux-evidence-surface-visibility-check.md`
- `docs/v14.1C-owner-manual-gemini-quality-retest-pass-closure.md`
- `src/components/admin/ownerOneRunEvidence.ts`
- `src/components/admin/OwnerFirebaseTokenHelperPanel.tsx`

This v14.3D packet verifies visibility and approval wording only through repository inspection.

## 10) Stop/HOLD governance for v14.3D packet

Stop and mark HOLD when:

- owner approval statement template is incomplete
- one-run checklist misses any required boundary
- pre-run GO/HOLD rules are incomplete
- post-run evidence checklist is incomplete
- anti-misleading lines are missing or ambiguous
- any sensitive pattern appears (secret/token/email/phone/plate/VIN)
- static validator fails

No progression to runtime planning is allowed while HOLD remains open.

## 11) Operator static validation flow

1. run `npm run test:v14.3D`  
2. run inherited validators (`v14.3C`, `v14.3B`, `v14.3A`, `v14.3`, `v14.2C`, `v14.2A`, `v14.2`, `v14.1C`, `v14.1`)  
3. if any FAIL appears, mark `HOLD — TEST FAILURE` and stop  
4. if all PASS, prepare next owner-approval drafting step only (still no runtime)

## 12) Final recommendation enum (v14.3D)

- `READY FOR v14.3E OWNER-ONLY PILOT DRY-RUN PLAN — NO RUNTIME EXECUTION`
- `READY FOR v14.3E FRESH OWNER APPROVAL REQUEST DRAFT — NO GEMINI YET`
- `HOLD — OWNER APPROVAL PACKET INCOMPLETE`
- `HOLD — APPROVAL WORDING AMBIGUITY`
- `HOLD — ONE-RUN / RETRY / SECOND-RUN CONTROL AMBIGUITY`
- `HOLD — ROUTE/AUTH/GUARD/EVIDENCE REQUIREMENT INCOMPLETE`
- `HOLD — PUBLIC/PRODUCTION/REAL LEAD RISK DETECTED`
- `HOLD — SECRET/PII RISK DETECTED`
- `HOLD — TEST FAILURE`
