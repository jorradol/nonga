# v22.62 — Q4 Text-Only Named-Compare Resolution Fix

Date: 2026-07-10  
Branch: `feature/chat-image-attachment-v1`  
Execution type: `CONTROLLED STAGING Q4 TEXT-ONLY NAMED-COMPARE RESOLUTION FIX / EXISTING CANONICAL COMPARISON PAIR + DETERMINISTIC COPY + GUARDED GEMINI PATH / PRESERVE OWNER-PASSED Q1–Q3 AND Q4 CARDS-IMAGES / NO REDESIGN / NO TEST LEAD / NO PILOT EXPANSION / NO PRODUCTION`

## Owner-browser failure (reproduced)

Q1–Q3 PASS. Q4 cards/images PASS (Corolla 2020 + 2021). Q4 **text FAIL**:

> น้องเอยังไม่เห็นชุดรถล่าสุดให้เทียบในแชทนี้ครับ ลองพิมพ์งบหรือเงื่อนไขรถที่อยากได้ก่อน เช่น “งบ 4 แสน มีรถอะไรน่าเล่น” แล้วน้องเอจะคัดรถมาให้ จากนั้นค่อยพิมพ์ “เทียบคันที่ 1 กับ 2” ได้ครับ

## Proven root cause

1. Orchestrator correctly resolved the inventory-backed canonical pair and attached two cards + `buildCompareReplyCopy` text.
2. Server bridge wired the pilot user-visible path with those two orchestrated cards as `pilotOrchestration`.
3. `buildPilotBuyerUserVisibleCopy` intentionally returned `null` for named inventory compare (defer to orchestrator legacy).
4. `buildPilotUserVisibleText` treated `null` + follow-up as **no session context** and replaced the good legacy pair text with `buildPilotFollowUpNoContextCopy()`.
5. Cards remained from the orchestrator; only the final user-visible text was overwritten.

Not a card/inventory/image defect. Not a missing Corolla 2021. Not a need for budget search or `เทียบคันที่ 1 กับ 2`.

## Fix (smallest)

| File | Change |
|------|--------|
| `salesBrainUserVisibleChatPath.ts` | `preferNamedCompareLegacyText` — when named compare defers, keep orchestrator pair text; never emit no-context / numbered-slot CTA over a valid pair answer |
| `salesBrainUserVisiblePilotBuyerCopy.ts` | Named inventory compare always returns `null` early (defer); never emit “อยากให้เทียบกับรุ่นหรือปีไหน” for an already-named target |

Reuses existing `inventoryBackedCompare` + `buildCompareReplyCopy`. No new pair store, search path, or Gemini pipeline. Q1–Q3 / card-image paths untouched.

## Tests

| Suite | Result |
|-------|--------|
| `test:v22.62` | **52 PASS** (stateful Q1→Q4 + bridge final text + generic fallback rejection) |
| `test:v22.61` | 39 PASS |
| `test:v22.58` | 25 PASS |
| `test:v22.57` | 57 PASS |
| `test:v22.54` | PASS |

## Staging deploy

| Item | Value |
|------|--------|
| Prior Cloud Run | `nonga-staging-00208-h4b` |
| Prior Hosting | `assets/index-BKfG57f1.js` (v22.61) |
| Prior image | `…/nonga-staging:v22.61-q1q4-restore-cc6a8f2` |
| New image / revision | *(filled after deploy)* |
| Health before | Capture ON; Pilot active max 3; Signup OFF |
| Marketplace before | 15; Corolla 2020 399k/88k ×5; Corolla 2021 429k/58k ×5; DTO safe |
| Pilot / buyerLeads | empty (unchanged; no Lead create) |
| Production | Untouched |

## Final recommendation

**NEED REVIEW** — automated proof complete; Owner one fresh Q1–Q4 chat must confirm Q4 text matches the already-correct two cards before PASS.
