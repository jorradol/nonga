# v22.34 — Dealer Pending Review UI + Owner/Admin Approval UI Polish

Date: 2026-07-09  
Branch: `feature/chat-image-attachment-v1`  
Execution type: `CONTROLLED DEALER PENDING REVIEW UI + OWNER/ADMIN APPROVAL UI POLISH / STAGING ONLY / TEST DATA ONLY / LEAD CAPTURE OFF / NO REAL LEAD / NO DEALER-FACING SEND / NO PRODUCTION / NO PUBLIC`

## 1) Purpose

Follow-up to **v22.33 NEED REVIEW**. Make dealer pending status and owner/admin approval **browser-usable** so owner does not need terminal, API calls, or token paste.

This step does **not**:

- enable real dealer posting / public onboarding
- turn lead capture ON
- create real leads or live TEST marketplace listings
- send dealer-facing messages
- deploy production / enable public signup

## 2) Owner-readable summary

### Dealer pending badge / wording

| After submit | What dealer sees |
|--------------|------------------|
| Status badge | **「รออนุมัติ」** (violet) |
| Guidance | 「ประกาศนี้ส่งให้ผู้ดูแลตรวจสอบแล้ว ยังไม่แสดงในตลาด」 |
| Drafts success toast | 「ส่งประกาศเข้ารออนุมัติแล้ว — ยังไม่แสดงในตลาด…」 |
| Inventory page title | 「รถในคลังของเต็นท์」 (not “already on market”) |
| Open marketplace button | Disabled while pending |
| Self “show on market” | Hidden / blocked while pending |
| Lead queue section | Only on **published** listings |

Dealer cannot self-approve. Cross-dealer inventory remains scoped.

### Owner/admin pending queue

| Item | Result |
|------|--------|
| Browser route | `/admin/pending-listings` |
| Entry | Admin dashboard → 「คิวรออนุมัติประกาศเต็นท์」 |
| Queue shows | Title, brand/model/year, price, showroom/owner display name, image preview, pending badge, privacy reminder |
| Approve | 「อนุมัติขึ้นตลาด」 → `POST /api/admin/listings/:id/approve` |
| Hold | 「พักไว้ก่อน」 → `POST /api/admin/listings/:id/hold` → `hidden` (non-public) |
| Reject (hard delete) | **Future work** — hold covers “not public for now” |
| Auth | Firebase session headers — **no token paste / no terminal** |

### Marketplace / public DTO

| Before approval | After approval |
|-----------------|----------------|
| Not in marketplace | Visible when `published` |
| Buyer sees nothing for that listing | Safe public fields only |

Public DTO still hides `ownerId`, `dealerId`, phone, VIN, full plate, address/PII; uses `dealerDisplayName` / `sellerDisplayName` / `dealerSlug` / `sellerType`.

### Test listing / cleanup

- **No live TEST listing created** this step (UI + unit proofs only).
- Marketplace remains **13**.
- Final artifact status: **none on staging**.

### What remains locked

Lead capture ON · real leads · dealer send · public signup · production · uncontrolled real dealer posting.

### Remaining owner decisions

1. Run a later **owner-browser live TEST listing** (with cleanup) now that UI is ready  
2. Whether hard **reject/delete** is needed beyond hold  
3. Wording tweaks on pending badge / approve confirm copy  

## 3) Technical summary

### UI routes / components

| Piece | Path |
|-------|------|
| Dealer inventory polish | `src/components/dealer-portal/DealerInventoryPage.tsx` |
| Dealer drafts success copy | `src/components/dealer-portal/DealerDraftsPage.tsx` |
| Status badge + guidance | `src/components/shared/ListingStatusBadge.tsx` |
| Admin pending queue | `src/components/admin/AdminPendingListingsView.tsx` |
| Admin dashboard link | `src/components/admin/AdminDashboardView.tsx` |
| App route wiring | `src/App.tsx`, `src/utils/appRouteSync.ts`, `src/store.ts` |
| Admin client API | `src/services/admin/adminListingReviewApi.ts` |

### API routes used

| Method | Route | Role |
|--------|-------|------|
| GET | `/api/admin/listings/pending-review` | List pending |
| POST | `/api/admin/listings/:id/approve` | Publish |
| POST | `/api/admin/listings/:id/hold` | **New** — set `hidden` |

Auth: existing `adminApiAuth` + Firebase session from browser.

### Status transitions

```text
dealer submit → pending_review (not public)
admin approve → published (public)
admin hold    → hidden (not public)
dealer self-approve → still forbidden (403)
```

### Files changed

- UI/components/routes listed above  
- `server.ts` — hold endpoint  
- `scripts/test-v22.34-dealer-pending-review-admin-approval-ui.mts`  
- `package.json` — `test:v22.34`  
- this doc  

### Tests run

| Test | Result |
|------|--------|
| `npm run test:v22.34-dealer-pending-review-admin-approval-ui` | **PASS** |
| `npm run test:v22.33-controlled-dealer-posting-owner-preview` | **PASS** |
| `npm run test:v22.32-dealer-posting-approval-gate-public-dto-cleanup` | **PASS** |

### Staging verification evidence

| Field | Value |
|-------|--------|
| Host | `https://a.nongbot.org` |
| Cloud Build ID | `7f0a4351-6c28-46ba-b23a-9d9022e5e4d0` |
| Previous revision | `nonga-staging-00190-77d` |
| New revision | **`nonga-staging-00191-jqq`** @ **100%** |
| Image tag | `v20260709-pending-ui-613a95a` |
| Hosting asset | `assets/index-APBJgg5j.js` |
| Health | `ok: true` |
| `leadCaptureEnabled` | **false** |
| `publicSignupEnabled` | **false** |
| Unauth lead | **401** |
| Marketplace | **13** |
| Thor imports | **3**; public `ownerId`/`dealerId` absent |
| Live TEST listing | **none** |
| Production | untouched |

## 4) Safety report

| Check | Result |
|-------|--------|
| Lead capture OFF | **PASS** |
| No real lead | **PASS** |
| No dealer-facing send | **PASS** |
| Unauth lead 401 | **PASS** (live) |
| `publicSignupEnabled:false` | **PASS** |
| Production untouched | **PASS** |
| No secrets/PII in report | **PASS** |
| Public DTO redaction | **PASS** (unit) |
| Cross-dealer isolation | **PASS** (unchanged gate) |
| Marketplace final count | **13** |
| Test artifact | **none live** |

## 5) Risk assessment

### Ready

- Dealer clearly sees 「รออนุมัติ」 + guidance  
- Owner/admin can review/approve/hold in browser  
- Self-approve still blocked  
- Public DTO still safe  
- Lead capture OFF  

### Needs owner review

- Confirm Thai wording on badge / approve confirm  
- Confirm hold is enough vs hard reject  
- Optional: owner-browser live TEST listing next  

### Must-fix before controlled live TEST dealer posting

- Owner signed-in as invite dealer + admin accounts (browser only)  
- TEST title convention + cleanup owner  
- Keep lead capture OFF  

### Must-fix before real dealer posting

- Owner accepts UI wording  
- Cleanup / hold SOP for bad submissions  

### Must-fix before controlled staging lead capture

- Separate kill-switch ON packet  

## 6) Recommendation

**PASS**

Dealer pending UI and owner/admin approval UI are browser-usable; approval remains gated; dealer self-approve and cross-dealer access remain blocked; public DTO remains safe; lead capture stays OFF; no real lead/dealer send/public/prod action; no live test artifact left.

Owner may still optionally review wording before a live TEST browser posting packet — that does not block this UI polish PASS.

### Explicit non-recommendations

- Not approval for uncontrolled real dealer posting  
- Not approval to turn lead capture ON  
- Not approval to create real leads or contact dealers  

## 7) Next-step proposal (only)

`CONTROLLED LIVE TEST DEALER POSTING OWNER-BROWSER / STAGING ONLY / TEST DATA ONLY / LEAD CAPTURE OFF / NO REAL LEAD / NO DEALER-FACING SEND / NO PRODUCTION / NO PUBLIC`

With mandatory cleanup of any TEST listing.
