# v20.1A - Step 2 Auth Context Owner-Browser Pivot Packet

Date: 2026-07-07
Branch: `feature/chat-image-attachment-v1`
Execution type: `STEP 2 AUTH CONTEXT PIVOT TO OWNER-BROWSER RUN PACKET ONLY / NO EXECUTION / NO DEPLOY / NO ONE-RUN`
Expected HEAD before work: `70e4c72`

## 1) Continuity and current status

- v19.39 final decision: `PASS — controlled owner-only staging trial completed, Step 1 completed`
- v19.40 final decision: `PASS — Step 2 limited private pilot readiness packet closed, no execution`
- v20.0 final decision: `PASS — v20.0 authenticated allowlist context readiness packet closed, no execution`
- v20.1 final decision: `HOLD — missing required authenticated owner token context because NONGA_OWNER_FIREBASE_ID_TOKEN present=false`
- current 5-step status before this packet:
  - Step 1 completed
  - Step 2 not started
  - Step 3 not started
  - Step 4 not started
  - Step 5 not started

## 2) Explicit v20.1A boundary

- no execution
- no deploy
- no one-run
- no retry
- no second-run
- no re-arm
- staging-only intent remains
- private allowlist-only intent remains

## 3) Architecture decision (v20.1A)

1. `NONGA_ADMIN_API_TOKEN` remains a server-side secret managed by Secret Manager binding.
2. NONGA_OWNER_FIREBASE_ID_TOKEN is not a permanent server-side secret.
3. Real/private pilot auth context should come from authenticated browser/client login session.
4. Server verifies Firebase token and resolves trusted identity from request auth context.
5. Server enforces allowlist UID gate via `NONGA_AI_USER_VISIBLE_ALLOWLIST_UIDS`.
6. v20.2 should prefer owner-browser authenticated staging path as primary execution path.

## 4) Static/read-only findings: owner-browser route availability

### 4.1 Browser flow exists now

- route target is already implemented: `POST /api/ai/chat-user-visible-orchestrate`
- browser client helper exists: `src/services/ai/chat/chatUserVisibleOrchestrateClient.ts`
- helper obtains Firebase auth header via `getFirebaseAuthHeaders()` from current signed-in browser user
- helper calls the exact route with Firebase Bearer auth from browser session (token value never logged in packet)
- chat runtime uses this bridge at `src/hooks/chat/useChat.ts` when signed-in and pilot conditions are met
- chat UI entry is available at `/` or `/chat` via `AIChatView`

### 4.2 Safest owner-browser run path for v20.2 (execution planning only)

- page/path: `/` or `/chat` (chat view)
- required owner state:
  - owner account is logged in on staging browser
  - Firebase auth session is active in browser
  - request carries authenticated Firebase Bearer header automatically
- allowlist requirement:
  - owner UID must be present in `NONGA_AI_USER_VISIBLE_ALLOWLIST_UIDS`
  - otherwise gate fail-closed reasons may include: `allowlist_empty` or `uid_not_allowlisted`
- safe synthetic message for v20.2:
  - `อยากดูรถมือสองงบไม่เกิน 300,000 บาท ใช้ขับในเมือง ขอประหยัดน้ำมันและดูแลง่าย`
- masked evidence to capture in v20.2:
  - route reached
  - auth accepted
  - allowlist accepted
  - `userVisibleGateDiagnostic` (masked)
  - `userVisibleRuntimeDiagnostic` (safe status only)
  - provider decision classification only (no sensitive dump)
  - no real lead / no real dealer action / no real customer data / no PII

## 5) Server verification path (confirmed)

- trusted auth entrypoint: `getServerAuthContext(req)` in `src/server/serverAuthContext.ts`
- token verification: `verifyFirebaseIdToken(token)` in `src/server/serverAuthContext.ts`
- token source: request `Authorization` bearer header (server-side extraction)
- allowlist gate:
  - `evaluateUserVisibleGate(...)`
  - env key `NONGA_AI_USER_VISIBLE_ALLOWLIST_UIDS`
  - gate diagnostics are redacted/masked in response payload

## 6) Owner-friendly guidance

- do not paste Firebase token in chat
- do not store Firebase ID token as a permanent server secret
- use owner browser login/session as auth context for real/private pilot where possible
- if browser route is available (it is), v20.2 should run exactly one owner-browser controlled staging run
- if a blocker appears later, report exact blocker and minimum safe next step without exposing token/header

## 7) v20.2 evidence checklist (token/header-safe)

v20.2 should capture only masked-safe evidence:

- route reached (`POST /api/ai/chat-user-visible-orchestrate`)
- auth passed (`getServerAuthContext` resolved)
- allowlist passed
- diagnostics masked (`userVisibleGateDiagnostic`, `userVisibleRuntimeDiagnostic`)
- safe response returned (shape summary only)
- provider/Gemini decision classified as one of:
  - `provider_call_blocked_or_not_reached`
  - `fallback_or_guarded_path_used`
  - `provider_path_reached_under_approved_gate`
- no real lead
- no real dealer action
- no real customer data
- no PII/phone/plate/VIN
- no token/secret/header exposure

## 8) Future owner approval phrase draft for v20.2 (draft only)

`FINAL AUTHORIZE v20.2 STEP 2 OWNER-BROWSER AUTHENTICATED CONTROLLED STAGING RUN / OWNER LOGGED-IN BROWSER SESSION ONLY / ROUTE POST /api/ai/chat-user-visible-orchestrate ONLY / EXACTLY ONE RUN / NO RETRY / NO SECOND-RUN / NO RE-ARM / STAGING ONLY / PRIVATE ALLOWLIST ONLY / NO PUBLIC / NO PRODUCTION / NO REAL DEALER / NO REAL LEAD / NO REAL CUSTOMER DATA / NO PII PHONE PLATE VIN / NO TOKEN VALUE IN CHAT OR REPO`

This is draft wording only for future v20.2.
It is not approval for v20.1A execution.

## 9) Final decision boundary for v20.1A

- packet preparation completed only
- no runtime route execution
- no one-run
- no deploy
