# v22.50 — Controlled Staging Lead Repository Switch-Only Validation (Option C2)

Date: 2026-07-10  
Branch: `feature/chat-image-attachment-v1`  
Execution type: `CONTROLLED STAGING LEAD REPOSITORY SWITCH-ONLY VALIDATION / OPTION C2 / EXISTING FIRESTORE REPOSITORY / LEAD CAPTURE OFF / NO LEAD WRITE / NO DEALER SEND / NO PRODUCTION`

## 1) Executive summary

Owner Option **C2** executed: Staging Cloud Run now initializes the **existing** Firestore Lead repository (`NONGA_LEAD_DATA_BACKEND=firestore`) while **Lead Capture remains OFF**. Health reports `leadDataBackend: firestore`. **Zero** Buyer Lead / contact-log documents were created. Marketplace remained **15**. No Dealer send. Production untouched.

**Final recommendation: PASS**  
**D2 remains unapproved.**

---

## 2) Owner authorization scope

Authorized:

- Staging only  
- Repository switch to existing Firestore Lead repository  
- Lead Capture OFF at all times  
- Health/config validation, regression, rollback readiness  

Not authorized / not done: D2, capture ON, any Lead create, queue write, Dealer notification, public signup, production, new architecture.

---

## 3) Preflight baseline

| Check | Result |
|-------|--------|
| Repo / branch | `D:/nonga` · `feature/chat-image-attachment-v1` |
| HEAD before | `9ae079d` (local = origin) |
| Cloud Run before | `nonga-staging-00194-72z` @ 100% |
| Image before | `…:v20260710-dealer-pending-copy-f2865db` |
| Hosting | `assets/index-Bsnqa-MN.js` (unchanged) |
| Lead backend | memory (env unset) |
| `leadCaptureEnabled` | **false** |
| Signup | **false** |
| Unauth buyer-lead | **401** |
| Marketplace | **15** |
| Corollas | Public · `Thor Auto` · 5/5 |
| Public DTO protected hits | **0** |
| `buyerLeads` / `leadContactLogs` | Empty (masked list) |
| Production | Untouched |

---

## 4) Firestore server-side readiness

| Item | Result |
|------|--------|
| Staging project | Same as inventory (`FIREBASE_PROJECT_ID` already set on service) |
| Admin access | Existing `FIREBASE_SERVICE_ACCOUNT_JSON` secret + runner SA |
| Client rules | Deny-all on Lead collections (unchanged) |
| Frontend direct Lead access | None introduced |
| Fail-closed | Requested Firestore without Admin creds throws (no silent memory after firestore selected) |
| New Owner secret | **Not required** |
| Ambiguity | **None** — Staging project already used for inventory Firestore |

---

## 5) Repository selector and health

| Item | Result |
|------|--------|
| Selector | Existing `NONGA_LEAD_DATA_BACKEND=firestore` |
| Health field | `leadDataBackend` from `getActiveBuyerLeadDataBackend()` |
| Secrets in health | None (no project id / credentials / collection names) |
| Kill Switch | Independent; capture OFF blocks create before repository write |

---

## 6) Deployment details

| Item | Result |
|------|--------|
| Cloud Build | `b6d53706-7a7c-483f-9cae-c96aa0c111ab` SUCCESS |
| Image tag | `v20260710-lead-backend-firestore-9ae079d` |
| Cloud Run revision | **`nonga-staging-00195-bbs`** @ **100%** |
| Prior safe revision | `nonga-staging-00194-72z` |
| Env change | Lead backend → Firestore only |
| Capture env | **Absent** (OFF) |
| Signup | Remains false |
| Hosting deploy | **None** (existing asset retained) |
| Production | Untouched |

---

## 7) Active backend proof

| Probe | Result |
|-------|--------|
| `GET /api/health` | `ok: true` |
| `leadDataBackend` | **`firestore`** |
| `leadCaptureEnabled` | **`false`** |
| `publicSignupEnabled` | **`false`** |
| Silent fallback to memory | **No** (health reports firestore) |

---

## 8) Lead Capture OFF / no-write proof

| Check | Result |
|-------|--------|
| Unauth `POST /api/buyer-leads` | **401** |
| Route kill switch before create | Present (auth → OFF → 403; no create) |
| Service kill switch | Blocks before repository write |
| `buyerLeads` after deploy | Still empty |
| `leadContactLogs` after deploy | Still empty |
| Public interest (Corolla 2020) | **0** |
| Cleanup needed | **No** (nothing written) |

---

## 9) Marketplace / DTO / UI

| Check | Result |
|-------|--------|
| Marketplace count | **15** |
| Corolla 2020/2021 | Public · `Thor Auto` · 5/5 |
| Protected DTO fields | **0** |
| Hosting asset | `assets/index-Bsnqa-MN.js` (OFF-state UI from v22.46) |
| Notification path | None on Lead create path; capture OFF |

---

## 10) Multi-instance readiness

| Item | Result |
|------|--------|
| Scaling | max **20** (min unset / 0) — **not** pinned to 1 |
| Memory pin required? | **No** for Firestore Lead coherence (architecture + Emulator evidence) |
| Live multi-instance Lead R/W | Awaits D2 (not proven this packet) |

---

## 11) Rollback plan (ready; not executed — C2 PASS)

1. `gcloud run services update-traffic nonga-staging --to-revisions=nonga-staging-00194-72z=100` **or** redeploy prior image and remove `NONGA_LEAD_DATA_BACKEND`.  
2. Keep capture OFF.  
3. Verify health (memory or prior), unauth 401, marketplace 15, DTO safe.  
4. No Lead cleanup required for this packet.

Immediate rollback triggers (none occurred): init failure, wrong backend field, capture ON, any Lead write, marketplace/DTO regression, notification activation.

---

## 12) Final safe state

| Item | State |
|------|-------|
| Lead backend | **Firestore** (Staging) |
| Lead Capture | **OFF** |
| Public Signup | **OFF** |
| Lead records created by C2 | **0** |
| Dealer notification | Disabled / unused |
| Production | Untouched |
| D2 | **Not approved** |

---

## 13) Remaining risks

- Live durable create/queue path still unproven (D2).  
- Duplicate guard remains partial (sufficient for one controlled test only).  
- Hosting bundle not rebuilt this packet (OFF UI already live).  

---

## 14) Owner decision options (not approved)

| Option | Meaning |
|--------|---------|
| A | Keep Staging on Firestore + capture OFF |
| B | Return Staging to memory + capture OFF |
| D2 | Fresh approval for exactly one synthetic durable Lead |
| E | More hardening before D2 |

---

## 15) Final recommendation

**PASS**

Staging successfully uses the existing Firestore Lead repository with capture OFF and zero Lead writes. Prefer Owner Option **A** (keep Firestore OFF) until a fresh D2 authorization.
