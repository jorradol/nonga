# v6.9J-C — Staging Hosting Deploy + Header Avatar Fallback Smoke Execution Record

**สถานะ:** **PARTIAL** — staging Hosting deploy and live asset verification **PASS**; guest smoke **PASS**; signed-in avatar smoke **not verified** (no safe operator session); authenticated `/profile` full contrast smoke **not verified**  
**วันที่:** 2026-06-17  
**Git baseline (deployed):** `339814837f516b177cb50c034a80f9f0b04d9766` (`3398148`)  
**Patch commit deployed:** `e8e513333d38bb5324e42ef389b34d5ebfba23cf` (`e8e5133`)  
**Record commit (docs only):** `339814837f516b177cb50c034a80f9f0b04d9766` (`3398148`)  
**Branch:** `feature/chat-image-attachment-v1`  
**Deploy/smoke run:** no commit during deploy/smoke; no runtime code changes

> Staging hosting-only deploy + guest smoke for v6.9J-B header avatar fallback patch (`ProfileAvatar`, `profilePhotoUrl.ts`, `Header.tsx`). No secrets, tokens, API keys, Authorization headers, raw prompts, avatar URLs, or PII in this document. **Not production-ready. Not deploy-ready. Production pilot still NOT READY.**

---

## 1. Overall verdict

| Item | Result |
|------|--------|
| Overall result | **PARTIAL** |
| Staging Hosting deploy | **SUCCESS** |
| Live asset verification | **PASS** |
| Guest smoke (`/home`, guest `/profile`) | **PASS** |
| Signed-in header/mobile avatar fallback (live) | **NOT VERIFIED** — no safe operator session |
| Authenticated `/profile` full contrast smoke | **NOT VERIFIED** — separate unresolved gate |
| `npm run lint` (pre-deploy) | **PASS** |
| `npm run test:v69j-b-header-avatar-fallback-patch-execution-record` (pre-deploy) | **PASS** |
| `npm run build:staging:hosting` | **PASS** |
| Staging URL | `https://nonga-ce93c.web.app` |
| Live JS asset | `/assets/index-CbN1x-wa.js` |
| Live CSS asset | `/assets/index-v8LQYv3A.css` (unchanged) |
| Cloud Run | **Not touched** |
| Production | **Not touched** |
| Commit during deploy/smoke | **NO** |
| Runtime code changes during deploy/smoke | **NO** |
| **v6.9 Controlled Production Pilot** | **NOT READY** |

**Scope note:** PARTIAL applies because signed-in avatar fallback was not verified on live staging without a safe operator session. Deploy, live bundle swap, and guest regression checks passed.

---

## 2. Baseline verification

| Field | Value |
|-------|-------|
| Repo | `https://github.com/jorradol/nonga.git` |
| Branch | `feature/chat-image-attachment-v1` |
| Current HEAD | `339814837f516b177cb50c034a80f9f0b04d9766` (`3398148`) |
| Patch commit deployed | `e8e513333d38bb5324e42ef389b34d5ebfba23cf` (`e8e5133`) — `fix(ui): add header profile avatar fallback for missing photoURL` |
| Record commit (prior docs) | `3398148` — `docs: add v6.9J-B header avatar fallback patch execution record` |
| Local HEAD | `3398148` (= `origin/feature/chat-image-attachment-v1`) |
| Working tree (at deploy/smoke time) | clean |
| Working tree (at record time) | clean (docs/script pending separate commit) |
| Commit during deploy/smoke | **NO** |
| Runtime code changes during deploy/smoke | **NO** |
| Staging project | `nonga-ce93c` |
| Staging host | `https://nonga-ce93c.web.app` |

---

## 3. Build result

| Check | Result | Notes |
|-------|--------|-------|
| `npm run lint` | **PASS** | `tsc --noEmit` pre-deploy |
| `npm run test:v69j-b-header-avatar-fallback-patch-execution-record` | **PASS** | Pre-deploy doc guard |
| `npm run build:staging:hosting` | **PASS** | Correct staging Hosting build path |
| Env source | Cloud Build substitution | No local `.env.staging` on deploy machine |
| Firebase web config guard | **PASS** | `[vite-firebase-guard] PASS production Firebase web config` |
| Dist bundle verify | **PASS** | `PASS dist bundle verified` |

**Built / live assets:**

| Asset | Path |
|-------|------|
| JS bundle | `dist/assets/index-CbN1x-wa.js` |
| CSS bundle | `dist/assets/index-v8LQYv3A.css` |

**Known separate issue:** generic `npm run build` remains **FAIL** due to Firebase production config guard / placeholder `apiKey` (documented in prior slices). This slice correctly used `build:staging:hosting` only — **not used as this deploy gate**.

---

## 4. Deploy result

| Item | Value |
|------|-------|
| Command | `npx -y firebase-tools@latest deploy --only hosting --project nonga-ce93c` |
| Result | **SUCCESS** |
| Scope | **Firebase Hosting only** — no functions, no Cloud Run |
| Project | `nonga-ce93c` |
| URL | `https://nonga-ce93c.web.app` |
| Files uploaded | **6 files** from `dist` (**2 new asset files** uploaded) |
| Exit code | **0** — Deploy complete |
| Production target | **Not selected / not touched** |

**Live bundle references (verified post-deploy):**

| Asset | Before (v6.9I-C) | After (v6.9J-C) |
|-------|------------------|-----------------|
| JS | `/assets/index-0kj8LJJl.js` | `/assets/index-CbN1x-wa.js` |
| CSS | `/assets/index-v8LQYv3A.css` | `/assets/index-v8LQYv3A.css` (unchanged) |

---

## 5. Live asset verification (background check)

Background HTTP verification against live staging assets:

| Asset | HTTP | Approx. size | Notes |
|-------|------|--------------|-------|
| `/assets/index-CbN1x-wa.js` | **200** | ~3.9 MB | **Active** bundle referenced by live `index.html` |
| `/assets/index-v8LQYv3A.css` | **200** | ~266 KB | Unchanged CSS bundle |
| `/assets/index-0kj8LJJl.js` | **200** | (prior bundle) | Still reachable on CDN; **no longer referenced** by live `index.html` |

**Interpretation:** Live `index.html` on staging now points to `/assets/index-CbN1x-wa.js`, confirming the new Hosting bundle that includes the v6.9J-B header avatar fallback patch is being served.

---

## 6. Cloud Run confirmation

| Item | Result |
|------|--------|
| Cloud Run deploy | **Not performed** |
| Cloud Run service/revision | **Not touched** |
| Deploy type | **Hosting-only deploy** |

No Cloud Run deploy, revision update, or service config change in this slice.

---

## 7. Guest smoke result

| Route / check | Result | Notes |
|---------------|--------|-------|
| `/home` | **PASS** | Header, nav, login button render; no crash |
| Guest header regression | **PASS** | Shows login CTA; no blank/broken guest chrome |
| `/profile` (guest) | **PASS** | Stays on `/profile`; RouteGuard message visible; no chat fallback |
| App crash | **None** observed | |

---

## 8. Signed-in avatar smoke result

| Check | Result |
|-------|--------|
| Safe operator session available | **NO** |
| Desktop signed-in header avatar fallback | **NOT VERIFIED** |
| Mobile drawer signed-in avatar fallback | **NOT VERIFIED** |
| Login performed during smoke | **NO** |
| Credentials/tokens/session logged | **NO** |

Per v6.9I-B policy: no passwords requested; no authenticated session used; mark **PARTIAL** until safe operator smoke completes.

---

## 9. Authenticated `/profile` gate (separate)

| Item | Status |
|------|--------|
| SettingsCard / profile inputs / AI tone cards | **NOT VERIFIED** |
| Signed-in profile dropdown internals | **NOT VERIFIED** |
| Full authenticated profile contrast smoke | **Separate unresolved gate** |

Do not treat this deploy/smoke slice as authenticated profile sign-off.

---

## 10. Safety confirmations

| Item | Result |
|------|--------|
| Cloud Run | **Not touched** |
| Production deploy | **NO** |
| Production changes | **NO** |
| Env / secrets / deploy config | **Unchanged** |
| Auth / login / session / routing | **Not changed** |
| Firestore write | **Not touched** |
| Lead system / DealerLeads | **Not touched** |
| Payment / invoice / boost logic | **Not touched** |
| AI runtime / Gemini / guard / fallback | **Not touched** |
| Secret / token / session / PII logged | **NO** |
| Full prompt / raw Gemini output in docs | **NO** |
| **v6.9 Controlled Production Pilot** | **NOT READY** |

---

## 11. Remaining gates

1. Signed-in header/mobile avatar fallback smoke with safe operator session only.
2. Authenticated `/profile` full contrast smoke (deferred from v6.9I-B).
3. Optional v6.9J-C seed/backfill — not required if UI fallback remains sufficient.

---

## 12. Recommended next step

After this execution record is committed and pushed, request separate approval for signed-in avatar smoke when a safe operator session is available. Do not claim production or pilot readiness.

---

## 13. This execution record (pending commit)

| File | Purpose |
|------|---------|
| `docs/v6.9J-C-staging-hosting-deploy-header-avatar-fallback-smoke-execution-record.md` | This document |
| `scripts/test-v69j-c-staging-hosting-deploy-header-avatar-fallback-smoke-execution-record.mts` | Static validation |
| `package.json` | `test:v69j-c-staging-hosting-deploy-header-avatar-fallback-smoke-execution-record` |

---

*End of v6.9J-C staging hosting deploy + header avatar fallback smoke execution record.*
