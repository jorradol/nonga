# v6.1A — Staging Shadow Integration Smoke / User-visible Still Off

**สถานะ:** Execution record (docs/tests only) — **staging shadow integration smoke บันทึกแล้ว, smoke-only slice, ไม่ deploy, ไม่ gcloud env update, ไม่เปิด NONGA_AI_USER_VISIBLE_ENABLED, user-visible response ยัง legacy 100%**  
**วันที่:** 2026-06-07  
**Git baseline ขณะ smoke:** `5a72d87d85b16727f231cc61cf7d29ebb45fd5b6` (v6.0Z record)  
**Branch:** `feature/chat-image-attachment-v1`  
**อ้างอิง:** v6.0Z (local server shadow smoke harness), v6.0Y (server-side shadow smoke readiness), v6.0X (hosting shadow chat client bundle deploy), v6.0W (Cloud Run shadow chat path deploy), v6.0V (shadow chat path wiring), v6.0U (Gemini shadow env enable)

> **v6.1A บันทึกผล staging shadow integration smoke จริง** — read-only smoke ผ่าน primary URL และ Cloud Run direct เพื่อยืนยันว่า shadow/chat wiring + runtime flags + primary URL bundle พร้อมสำหรับขั้นถัดไป โดย **user-visible response ยัง legacy 100%**, **ไม่มี shadow AI replacement แสดงให้ผู้ใช้**, และ **ไม่มี paid Gemini call จาก shadow chat path**

---

## สารบัญ

1. [Vision & Execution Summary](#1-vision--execution-summary)
2. [Preflight (Before Smoke)](#2-preflight-before-smoke)
3. [Tests / Build Before Smoke](#3-tests--build-before-smoke)
4. [Bundle / Revision Notes](#4-bundle--revision-notes)
5. [API Smoke](#5-api-smoke)
6. [Chat Smoke (Primary URL)](#6-chat-smoke-primary-url)
7. [Chat Smoke (Cloud Run Direct — Optional)](#7-chat-smoke-cloud-run-direct--optional)
8. [Shadow / Log / Network Safety](#8-shadow--log--network-safety)
9. [Git Status After Smoke](#9-git-status-after-smoke)
10. [Rollback Plan (Not Needed)](#10-rollback-plan-not-needed)
11. [Forbidden Changes (v6.1A)](#11-forbidden-changes-v61a)
12. [Next Steps](#12-next-steps)

---

## 1. Vision & Execution Summary

### เป้าหมาย v6.1A

รัน staging shadow integration smoke แบบปลอดภัย (read-only) เพื่อยืนยัน:

- shadow/chat wiring + runtime flags + primary URL bundle พร้อมระดับหนึ่ง
- **ไม่เปิด** `NONGA_AI_USER_VISIBLE_ENABLED=true`
- **ไม่เรียก** paid Gemini API จาก shadow chat path
- **ไม่ใช้** ข้อมูลจริง / สต๊อกจริง / PII จริง
- **ไม่ deploy** Cloud Run / Hosting / Firestore rules ใน slice นี้

### Execution outcome

| Area | Result |
|------|--------|
| Preflight | ✅ git clean, HEAD = origin @ `5a72d87` |
| Local tests (v60z / v60y / v60v) | ✅ PASS |
| lint + build:staging:hosting | ✅ PASS |
| Primary URL bundle | ✅ `index-NVEc0A-_.js` |
| Cloud Run revision | ✅ `nonga-staging-00050-rqs` @ **100%** |
| API smoke (primary + Cloud Run direct) | ✅ health/cars 200, revenue 401, signup false |
| Chat smoke (primary URL) | ✅ legacy 100%, no shadow AI replacement |
| Chat smoke (Cloud Run direct, optional) | ✅ case 1 legacy match |
| Rollback | ❌ **not needed** — no regression user-visible AI |
| User-visible AI | ❌ unchanged — legacy only |
| Paid Gemini from shadow chat path | ❌ not observed |
| Real stock import | ❌ still blocked |

---

## 2. Preflight (Before Smoke)

Recorded at smoke execution time:

| Check | Value |
|-------|-------|
| **git status** | clean (nothing to commit, working tree clean) |
| **branch** | `feature/chat-image-attachment-v1` |
| **HEAD** | `5a72d87d85b16727f231cc61cf7d29ebb45fd5b6` |
| **origin/feature/chat-image-attachment-v1** | `5a72d87d85b16727f231cc61cf7d29ebb45fd5b6` |
| **HEAD = origin** | ✅ yes |
| **Firebase project** | `nonga-ce93c` |
| **Cloud Run service** | `nonga-staging` |
| **Cloud Run revision** | `nonga-staging-00050-rqs` @ **100%** |
| **Region** | `asia-southeast1` |
| **Primary URL bundle** | `index-NVEc0A-_.js` |
| **Public signup** | **`false`** (`publicSignupEnabled:false`) |

### NONGA_AI_* flags (read-only revision describe — no secret value access)

| Flag | Value |
|------|-------|
| `NONGA_AI_PROVIDER` | `gemini` |
| `NONGA_AI_MODE` | `high` |
| `NONGA_AI_FIRST_ENABLED` | `true` |
| `NONGA_AI_SHADOW_MODE_ENABLED` | `true` |
| `NONGA_AI_USER_VISIBLE_ENABLED` | **`false`** |
| `NONGA_AI_EMERGENCY_KILL_SWITCH` | `false` |
| `NONGA_AI_BUDGET_DAILY_LIMIT` | `5` |
| `NONGA_AI_BUDGET_MONTHLY_LIMIT` | `50` |
| `GEMINI_API_KEY` | secret ref **`gemini-api-key`** only — **did not access secret value** |

**Did not run** `gcloud secrets versions access` — no secret values in this record.

---

## 3. Tests / Build Before Smoke

All passed before staging smoke:

| Command | Result |
|---------|--------|
| `npm run test:v60z-local-server-shadow-smoke-harness-no-provider-network` | **PASS** |
| `npm run test:v60y-server-side-shadow-evaluation-smoke-readiness` | **PASS** |
| `npm run test:v60v-shadow-chat-path-wiring-user-visible-legacy` | **PASS** |
| `npm run lint` | **PASS** |
| `npm run build:staging:hosting` | **PASS** |

Local build output bundle: **`index-NVEc0A-_.js`** — matches live primary URL bundle.

---

## 4. Bundle / Revision Notes

| Surface | Bundle / Revision |
|---------|-------------------|
| **Primary Hosting** (`nonga-ce93c.web.app`) | **`index-NVEc0A-_.js`** |
| **Cloud Run direct static** (`nonga-staging-dpf3rexexq-as.a.run.app`) | `index-D1gO2JxF.js` |
| **Cloud Run revision** | `nonga-staging-00050-rqs` @ **100%** |

**Bundle difference is expected:** v6.1A did **not** deploy Cloud Run — primary Hosting has the v6.0X client bundle with v6.0V shadow chat path wiring; Cloud Run direct serves the older bundle baked into the container image.

---

## 5. API Smoke

### Primary URL — `https://nonga-ce93c.web.app`

Read-only GET smoke (API rewrites → Cloud Run):

| Endpoint | HTTP | Key fields |
|----------|------|------------|
| `GET /api/health` | **200** | `ok:true`, `publicSignupEnabled:false` |
| `GET /api/cars?limit=1` | **200** | `success:true`, `count:17` |
| `GET /api/admin/revenue/preview` (unauth) | **401** | auth required |
| `GET /api/my/revenue/preview` (unauth) | **401** | auth required |

### Cloud Run direct — `https://nonga-staging-dpf3rexexq-as.a.run.app`

| Endpoint | HTTP | Key fields |
|----------|------|------------|
| `GET /api/health` | **200** | `publicSignupEnabled:false` |
| `GET /api/cars?limit=1` | **200** | `success:true` |

**No paid Gemini API call from shadow chat path** during smoke.

---

## 6. Chat Smoke (Primary URL)

Safe chat smoke via `https://nonga-ce93c.web.app/chat` — synthetic/safe messages only, no real PII.

### Case 1 — Start over

| Check | Result |
|-------|--------|
| Message | `"เริ่มใหม่"` |
| User-visible response | **exact legacy:** `"ยกเลิกข้อมูลเดิมแล้วครับ พิมพ์ข้อมูลรถคันใหม่ที่ต้องการลงขายได้เลยครับ"` |
| AI/Gemini shadow replacement | ❌ **none** |
| Raw PII in UI | ❌ **not observed** |

### Case 2 — Buyer generic search

| Check | Result |
|-------|--------|
| Message | `"งบ 4 แสน มีรถอะไรน่าเล่น"` |
| Behavior | Legacy buyer search orchestrator — normal operation |
| User-visible output | Staging market listings (e.g. Toyota Corolla / Altis / Camry) with cards + CTA |
| Shadow AI answer replacement | ❌ **none** |

### Case 3 — Dealer/seller bulk import intent

| Check | Result |
|-------|--------|
| Message | `"มีไฟล์รถหลายคันอยากเอาเข้าระบบ"` |
| User-visible response | Legacy clarifying: `"ได้ครับคุณพี่ ขอรายละเอียดเพิ่มนิดนึงก่อนนะครับ"` + asks for งบประมาณ ยี่ห้อ/รุ่น หรือประเภทรถ |
| Assessment | Legacy acceptable (no dealer bulk-import routing yet) |
| Shadow AI output replacement | ❌ **none** |

---

## 7. Chat Smoke (Cloud Run Direct — Optional)

Optional repeat via `https://nonga-staging-dpf3rexexq-as.a.run.app/chat`:

| Check | Result |
|-------|--------|
| Message | `"เริ่มใหม่"` |
| User-visible response | **exact legacy** — same text as primary URL |
| Shadow AI replacement | ❌ **none** |

---

## 8. Shadow / Log / Network Safety

| Check | Result |
|-------|--------|
| AI response shown to user (shadow replacement) | ❌ **none** — legacy 100% |
| Browser direct provider network (`generativelanguage`, OpenAI, etc.) | **0 requests** |
| Raw PII in inspected UI/log | ❌ **not observed** |
| Secret value access | ❌ **not done** |
| Shadow sales-brain user-visible wiring shown | ❌ **not shown** (v6.0V flags-only client path) |

### Legacy memory sidebar note

During chat smoke, browser network log observed an **internal** call to `/api/gemini/analyze-memory`. This is the **existing legacy memory sidebar feature** — **not** shadow chat replacement. The browser does **not** fetch directly to an AI provider; any server-side Gemini usage for memory extraction is separate from the shadow sales-brain user-visible path verified in v6.1A.

**No paid Gemini API call from shadow chat path** was observed during this smoke slice.

---

## 9. Git Status After Smoke

```
On branch feature/chat-image-attachment-v1
Your branch is up to date with 'origin/feature/chat-image-attachment-v1'.
nothing to commit, working tree clean
```

- **`dist/` ไม่ tracked ใน git** — local build only
- Smoke slice ไม่สร้าง deploy artifacts ใน git

---

## 10. Rollback Plan (Not Needed)

Rollback **not needed** — API + chat smoke passed, no regression user-visible AI.

Prepared conceptually (for separate approved slice only):

- Set kill switch `true` / shadow `false`, or
- Rollback Cloud Run/Hosting via ops approval

**No rollback was run in v6.1A.**

---

## 11. Forbidden Changes (v6.1A)

| Forbidden | v6.1A status |
|-----------|--------------|
| Cloud Run deploy | ❌ not done |
| Firebase Hosting deploy | ❌ not done |
| Firestore rules deploy | ❌ not done |
| `gcloud run services update` for env | ❌ not done |
| `NONGA_AI_*` env changes | ❌ not done |
| `NONGA_AI_USER_VISIBLE_ENABLED=true` | ❌ not done — remains **false** |
| Secrets create/update / `secrets versions access` | ❌ not done |
| Paid Gemini API from shadow chat path / fetch to AI provider from shadow path | ❌ not done |
| User-visible AI / AI response shown to user | ❌ not done |
| User-visible runtime behavior change | ❌ not done |
| Production | ❌ not touched |
| Payment / invoice / settlement write logic changes | ❌ not done |
| Buyer lead / seller reveal / outcome changes | ❌ not done |
| Public signup enable | ❌ not done — staging `false` |
| Real stock import / real customer data | ❌ not done — still blocked |

CI static test uses **readFileSync only** — no fetch, no gcloud, no Firebase CLI, no AI API.

---

## 12. Next Steps

1. **v6.1B+** — server-side shadow evaluation live wiring smoke (separate approved slice)
2. Do **not** enable `NONGA_AI_USER_VISIBLE_ENABLED=true` without explicit approval
3. Notify ลุง → plan real stock import (still blocked until further smoke passes)
