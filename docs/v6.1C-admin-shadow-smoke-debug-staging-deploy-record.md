# v6.1C — Admin Shadow Smoke Debug Staging Deploy Record

**สถานะ:** Execution record (docs/tests only) — **Cloud Run staging image-only deploy บันทึกแล้ว, ไม่ deploy ซ้ำ, ไม่ gcloud env update, ไม่เปิด NONGA_AI_USER_VISIBLE_ENABLED, admin-only shadow smoke route live บน staging, user-visible response ยัง legacy 100%**  
**วันที่:** 2026-06-07  
**Git baseline ขณะ deploy:** `0883faf9a1282120f548c9440c4bead5eaf2f533` (v6.1B)  
**Branch:** `feature/chat-image-attachment-v1`  
**อ้างอิง:** v6.1B (admin-gated server-side shadow debug wiring), v6.1A (staging shadow integration smoke), v6.0Z (local server shadow harness), v6.0W (Cloud Run shadow chat path deploy)

> **v6.1C บันทึกผล deploy จริง** — นำโค้ด v6.1B admin-only server-side shadow smoke debug route ขึ้น staging revision `nonga-staging-00051-fbd` โดย **env/secrets ไม่เปลี่ยน**, **NONGA_AI_USER_VISIBLE_ENABLED=false**, **mock provider only / no paid Gemini call**, และ **user-visible chat ยัง legacy 100%**

---

## สารบัญ

1. [Vision & Execution Summary](#1-vision--execution-summary)
2. [Preflight (Before Deploy)](#2-preflight-before-deploy)
3. [Tests / Build Before Deploy](#3-tests--build-before-deploy)
4. [Cloud Build (Executed)](#4-cloud-build-executed)
5. [Cloud Run Deploy (Image-only)](#5-cloud-run-deploy-image-only)
6. [Env Flags After Deploy](#6-env-flags-after-deploy)
7. [Post-deploy API Smoke](#7-post-deploy-api-smoke)
8. [Admin Shadow Route Smoke](#8-admin-shadow-route-smoke)
9. [Chat Smoke (Primary URL)](#9-chat-smoke-primary-url)
10. [Hosting vs Cloud Run Note](#10-hosting-vs-cloud-run-note)
11. [Rollback Plan (Prepared — Not Run)](#11-rollback-plan-prepared--not-run)
12. [Git Status After Deploy](#12-git-status-after-deploy)
13. [Forbidden Changes (v6.1C)](#13-forbidden-changes-v61c)
14. [Next Steps](#14-next-steps)

---

## 1. Vision & Execution Summary

### เป้าหมาย v6.1C

Deploy Cloud Run staging **image-only** จาก branch ที่มี v6.1B admin-only server-side shadow smoke debug route โดย:

- **Preserve** env/secrets เดิม — ไม่ `--set-env-vars` / `--update-env-vars`
- **ไม่เปิด** `NONGA_AI_USER_VISIBLE_ENABLED=true`
- **ไม่** Hosting / Firestore rules deploy
- Post-deploy read-only API smoke + admin-gated shadow route smoke + safe chat smoke

### Execution outcome

| Area | Result |
|------|--------|
| Preflight | ✅ git clean, HEAD = origin @ `0883faf` |
| Before deploy | rev `nonga-staging-00050-rqs`, image `v6.0W-shadow-chat-path-legacy-visible-d4cecd6` |
| Cloud Build | ✅ **SUCCESS** — `fb226e64-cc7a-4fe7-96bb-8cdf29e37164` |
| Cloud Run deploy | ✅ rev **`nonga-staging-00051-fbd`**, traffic **100%** |
| Env flags after deploy | ✅ unchanged — shadow on, user-visible off |
| Post-deploy API smoke | ✅ health/cars 200, revenue 401, signup false |
| Admin shadow route smoke | ✅ 401/403/200/400 + SS-05..SS-08 safe cases |
| Chat smoke (primary URL) | ✅ legacy response exact, no AI/Gemini shown |
| Rollback | ❌ **not needed** — smoke passed |
| User-visible AI | ❌ unchanged — legacy only |
| Paid Gemini / provider network | ❌ not called — mock provider only |
| Real stock import | ❌ still blocked |

---

## 2. Preflight (Before Deploy)

Recorded at deploy execution time:

| Check | Value |
|-------|-------|
| **git status** | clean (nothing to commit, working tree clean) |
| **branch** | `feature/chat-image-attachment-v1` |
| **HEAD** | `0883faf9a1282120f548c9440c4bead5eaf2f533` |
| **origin/feature/chat-image-attachment-v1** | `0883faf9a1282120f548c9440c4bead5eaf2f533` |
| **HEAD = origin** | ✅ yes |
| **gcloud active project** | `nonga-ce93c` |
| **Cloud Run service** | `nonga-staging` |
| **Region** | `asia-southeast1` |
| **Previous revision** | `nonga-staging-00050-rqs` @ **100%** |
| **Previous image** | `v6.0W-shadow-chat-path-legacy-visible-d4cecd6` |
| **Hosting primary bundle** | `index-NVEc0A-_.js` (**unchanged** — no Hosting deploy in v6.1C) |
| **Public signup** | **`false`** (`publicSignupEnabled:false`) |
| **GEMINI_API_KEY ref** | `gemini-api-key:latest` (metadata only — **no value accessed**) |

### NONGA_AI_* flags before deploy (unchanged)

| Flag | Value |
|------|-------|
| `NONGA_AI_PROVIDER` | `gemini` |
| `NONGA_AI_MODE` | `high` |
| `NONGA_AI_FIRST_ENABLED` | `true` |
| `NONGA_AI_SHADOW_MODE_ENABLED` | `true` |
| `NONGA_AI_USER_VISIBLE_ENABLED` | **`false`** |
| `NONGA_AI_EMERGENCY_KILL_SWITCH` | `false` |
| `NONGA_AI_BUDGET_DAILY_LIMIT` | `5` |
| `NONGA_AI_BUDGET_MONTHLY_LIMIT` | `50` |

**Did not run** `gcloud secrets versions access` — no secret values in this record.

---

## 3. Tests / Build Before Deploy

All passed before Cloud Build + deploy:

| Command | Result |
|---------|--------|
| `npm run test:v61b-server-side-shadow-evaluation-admin-debug-user-visible-off` | **PASS** |
| `npm run test:v61a-staging-shadow-integration-smoke-user-visible-still-off` | **PASS** |
| `npm run test:v60z-local-server-shadow-smoke-harness-no-provider-network` | **PASS** |
| `npm run lint` | **PASS** |
| `npm run build:staging:hosting` | **PASS** (local Vite only — **not deployed to Hosting**) |

---

## 4. Cloud Build (Executed)

```bash
# Executed — do not re-run from CI without ops approval
gcloud builds submit --project=nonga-ce93c \
  --config=cloudbuild.v53f.yaml \
  --substitutions=_IMAGE_URI=asia-southeast1-docker.pkg.dev/nonga-ce93c/nonga-staging/nonga-staging:v6.1C-admin-shadow-smoke-debug-0883faf,...
```

| Field | Value |
|-------|-------|
| **Cloud Build ID** | `fb226e64-cc7a-4fe7-96bb-8cdf29e37164` |
| **Status** | **SUCCESS** (~2m48s) |
| **Image tag** | `v6.1C-admin-shadow-smoke-debug-0883faf` |
| **Image URI** | `asia-southeast1-docker.pkg.dev/nonga-ce93c/nonga-staging/nonga-staging:v6.1C-admin-shadow-smoke-debug-0883faf` |
| **Image digest** | `sha256:70cb1e4ab85f15eadc5dae41f631bd2997bb589ab8e8af8ab2528bd84a9bce15` |

**Did not run** `gcloud secrets versions access` — no secret values in this record.

---

## 5. Cloud Run Deploy (Image-only)

```bash
# Executed — image-only, no --set-env-vars / --update-env-vars
gcloud run deploy nonga-staging \
  --image asia-southeast1-docker.pkg.dev/nonga-ce93c/nonga-staging/nonga-staging:v6.1C-admin-shadow-smoke-debug-0883faf \
  --project=nonga-ce93c \
  --region=asia-southeast1 \
  --allow-unauthenticated \
  --service-account=nonga-staging-runner@nonga-ce93c.iam.gserviceaccount.com
```

| Field | Value |
|-------|-------|
| **Deploy type** | **image-only** |
| **Previous revision** | `nonga-staging-00050-rqs` |
| **New revision** | **`nonga-staging-00051-fbd`** |
| **Traffic** | **100%** → new revision |
| **Env/secrets** | **preserved** — no `--set-env-vars`, no `--update-env-vars` |
| **Service account** | `nonga-staging-runner@nonga-ce93c.iam.gserviceaccount.com` |
| **Service URL** | `https://nonga-staging-265743710806.asia-southeast1.run.app` |
| **API smoke base** | `https://nonga-ce93c.web.app` |

---

## 6. Env Flags After Deploy

On revision `nonga-staging-00051-fbd` — **unchanged from pre-deploy**:

| Flag | Value after deploy |
|------|---------------------|
| `NONGA_AI_PROVIDER` | `gemini` |
| `NONGA_AI_MODE` | `high` |
| `NONGA_AI_FIRST_ENABLED` | `true` |
| `NONGA_AI_SHADOW_MODE_ENABLED` | `true` |
| `NONGA_AI_USER_VISIBLE_ENABLED` | **`false`** |
| `NONGA_AI_EMERGENCY_KILL_SWITCH` | `false` |
| `NONGA_AI_BUDGET_DAILY_LIMIT` | `5` |
| `NONGA_AI_BUDGET_MONTHLY_LIMIT` | `50` |
| `VITE_NONGA_PUBLIC_SIGNUP_ENABLED` | `false` |

Secret refs (names only — **no values**):

| Env var | Ref |
|---------|-----|
| `GEMINI_API_KEY` | `gemini-api-key:latest` (unchanged) |
| `FIREBASE_SERVICE_ACCOUNT_JSON` | `firebase-service-account-json:latest` (unchanged) |

---

## 7. Post-deploy API Smoke

Read-only GET smoke via `https://nonga-ce93c.web.app` (API proxy → Cloud Run):

| Endpoint | HTTP | Key fields |
|----------|------|------------|
| `GET /api/health` | **200** | `ok:true`, `publicSignupEnabled:false` |
| `GET /api/cars?limit=1` | **200** | `success:true`, marketplace count=**17** |
| `GET /api/admin/revenue/preview` (unauth) | **401** | auth required |
| `GET /api/my/revenue/preview` (unauth) | **401** | auth required |

**No paid AI API call** during deploy or smoke. **User-visible AI response** unchanged — legacy orchestrator/template only.

---

## 8. Admin Shadow Route Smoke

Route deployed in v6.1B, live on staging after v6.1C deploy:

```
POST /api/admin/sales-brain-shadow-smoke
Content-Type: application/json
Authorization: Bearer <Firebase ID token>
{ "caseId": "SS-01" }
```

Post-deploy smoke via `https://nonga-ce93c.web.app` (admin auth via existing Firebase test accounts — **no new data created**):

| Case | HTTP | Result |
|------|------|--------|
| Unauth `{"caseId":"SS-01"}` | **401** | **PASS** |
| Dealer Firebase token `{"caseId":"SS-01"}` | **403** | **PASS** |
| Admin Firebase token `{"caseId":"SS-01"}` | **200** | **PASS** |
| Admin `{"caseId":"UNKNOWN"}` | **400** | **PASS** |
| Admin `{}` (missing caseId) | **400** | **PASS** |
| Admin SS-05 (kill switch) | **200** safe | **PASS** |
| Admin SS-06 (budget missing) | **200** safe | **PASS** |
| Admin SS-07 (user-visible blocked) | **200** safe | **PASS** |
| Admin SS-08 (production off) | **200** safe | **PASS** |

### Admin SS-01 response (redacted — key fields only)

| Field | Value |
|-------|-------|
| `success` | **`true`** |
| `readOnly` | **`true`** |
| `userVisibleOff` | **`true`** |
| `providerNetwork` | **`false`** |
| `data.shadowDebugResult.provider` | **`"mock"`** |

Safety checks on SS-01 response body:

| Check | Result |
|-------|--------|
| Paid Gemini API call | ❌ **none** — mock provider only |
| Raw phone / email in response | ❌ **none observed** |
| Secret / env dump in response | ❌ **none** — no `GEMINI_API_KEY` or `FIREBASE_SERVICE_ACCOUNT_JSON` values |
| AI response shown to end user | ❌ **none** — admin-only debug payload, chat path unchanged |

**Did not run** `gcloud secrets versions access` during smoke.

---

## 9. Chat Smoke (Primary URL)

Safe chat smoke via primary Hosting URL (API → Cloud Run, client bundle unchanged):

| Check | Result |
|-------|--------|
| **URL** | `https://nonga-ce93c.web.app/chat` |
| หน้า chat โหลดได้ | ✅ |
| ส่งข้อความปลอดภัย `"เริ่มใหม่"` | ✅ |
| User-visible response | **legacy 100%** — exact text: `"ยกเลิกข้อมูลเดิมแล้วครับ พิมพ์ข้อมูลรถคันใหม่ที่ต้องการลงขายได้เลยครับ"` |
| AI/Gemini response แทน legacy | ❌ **none** |
| Hosting primary bundle | `index-NVEc0A-_.js` — **unchanged** (no Hosting deploy in v6.1C) |
| Raw PII ใน UI | ❌ **not observed** |

**No paid AI API call** during chat smoke. Shadow path runs flags-only — **legacy text returned unchanged**.

---

## 10. Hosting vs Cloud Run Note

| Surface | Status |
|---------|--------|
| **Cloud Run revision** | ✅ `nonga-staging-00051-fbd` @ 100% — includes v6.1B admin shadow route |
| **Firebase Hosting `nonga-ce93c.web.app`** | ❌ **Not redeployed** in v6.1C |
| **Hosting primary bundle** | `index-NVEc0A-_.js` — unchanged |
| **API routes via web.app** | Proxy to Cloud Run — admin route reachable via web.app |
| **Chat UI on web.app** | Client bundle unchanged — legacy user-visible behavior preserved |

---

## 11. Rollback Plan (Prepared — Not Run)

Rollback **not needed** — health, route guards, admin shadow smoke, and chat smoke passed.

If health/smoke fails in future — **prepared but not executed** in v6.1C:

```bash
# Prepared — NOT RUN in v6.1C (smoke passed)
gcloud run services update-traffic nonga-staging \
  --project=nonga-ce93c \
  --region=asia-southeast1 \
  --to-revisions=nonga-staging-00050-rqs=100
```

**Do not** modify env/secrets to fix issues — rollback revision first.

---

## 12. Git Status After Deploy

| Check | Value |
|-------|-------|
| **branch** | `feature/chat-image-attachment-v1` |
| **HEAD** | up to date with `origin/feature/chat-image-attachment-v1` |
| **working tree** | clean |

**No docs auto-commit** in deploy slice — this record committed separately.

---

## 13. Forbidden Changes (v6.1C)

| Forbidden | v6.1C status |
|-----------|--------------|
| Further Cloud Run deploy | ❌ not done after initial v6.1C deploy |
| `NONGA_AI_USER_VISIBLE_ENABLED=true` | ❌ not done — remains **false** |
| `gcloud run services update` for env | ❌ not done |
| `NONGA_AI_*` env changes | ❌ not done |
| Secrets create/update / `secrets versions access` | ❌ not done |
| Paid AI API / fetch to AI provider | ❌ not done |
| User-visible AI / AI response shown to user | ❌ not done |
| Production | ❌ not touched |
| Firestore rules deploy | ❌ not done |
| Hosting deploy | ❌ not done |
| Payment / invoice / settlement write logic changes | ❌ not done |
| Buyer lead / seller reveal / outcome changes | ❌ not done |
| Public signup enable | ❌ not done — staging `false` |
| Real stock import | ❌ not done — still blocked |
| Docs auto-commit in deploy slice | ❌ not done — separate docs slice |

CI static test uses **readFileSync only** — no fetch, no gcloud, no Firebase, no AI API.

---

## 14. Next Steps

1. Monitor admin shadow route usage — admin/superadmin only, synthetic cases only
2. Future slice: optional expanded staging shadow integration with additional safe cases
3. Do **not** enable `NONGA_AI_USER_VISIBLE_ENABLED=true` without explicit approval
4. Real stock import remains blocked until further approved slices
