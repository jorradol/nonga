# v13.7 One-Run Admin-Only Real Gemini Proof Approval Packet

## Status

APPROVAL PACKET ONLY — no Gemini execution in this packet

## v13.6 Source Status

- source packet: `docs/v13.6-controlled-staging-gemini-shadow-runtime-config-setup-record.md`
- v13.6 result: runtime config readiness captured with sanitized evidence
- v13.6 recommendation: READY FOR v13.7 ONE-RUN ADMIN-ONLY REAL GEMINI PROOF APPROVAL

## Owner Approval Required

Exact owner wording required:

FINAL APPROVAL FOR v13.7:  
I approve exactly one admin-only Real Gemini proof run on staging.  
Use only POST /api/admin/sales-brain-shadow-smoke with sanitized SS-01 payload.  
Do not expose secrets.  
Do not use real customer data, phone, plate, VIN, or PII.  
Do not send real leads.  
Do not enable buyer-facing AI.  
Do not activate public route.  
Do not touch production.  
Do not retry automatically.  
Capture only sanitized evidence.  
Stop immediately after one run and report PASS / NEED REVIEW / HOLD.

## One-Run Limit

- exactly one run only (`1/1`)
- no automatic retry
- no second attempt in same packet
- stop immediately after one run and classify PASS / NEED REVIEW / HOLD

## Approved Endpoint

- POST /api/admin/sales-brain-shadow-smoke
- staging service boundary only: `nonga-staging` (`asia-southeast1`)
- admin-only route only
- `/api/gemini/*` direct call remains disallowed

## Approved Sanitized Payload

- case id locked: `SS-01`
- payload must be sanitized and synthetic
- no real customer data
- no phone
- no plate
- no VIN
- no PII
- no real lead sending fields

## Secret Handling Rules

- do not print `GEMINI_API_KEY`
- do not print `NONGA_ADMIN_API_TOKEN`
- do not print Authorization header values
- no secret values in logs/docs/commits
- use secret references/runtime source only

## Evidence Rules

- capture sanitized request/response evidence only
- include dispatch outcome and guardrail confirmations
- redact any sensitive diagnostic detail
- include explicit one-run counter (`1/1`) and stop confirmation

## Pass / Need Review / Hold Criteria

- PASS: endpoint called once, provider path executed, sanitized output returned, all guardrails preserved
- NEED REVIEW: endpoint called once but provider result inconclusive/partial/unstable, guardrails still preserved
- HOLD: missing prerequisite, secret/runtime risk, or any guardrail violation

## Guardrails

- no buyer-facing AI enablement
- no public route activation
- no production change
- no real lead sending
- no PII/customer data
- no automatic retry
- stop after one run

## Recommendation

READY FOR OWNER APPROVAL TO PROCEED TO v13.7 ONE-RUN ADMIN-ONLY REAL GEMINI PROOF
