# v7.5.1A — Staging Deploy Closure Record (รอบรวม v7.1–v7.5.1)

**สถานะ:** **CLOSED — v7.1–v7.5.1 ON STAGING (HOSTING-ONLY, LOCAL-SAFE)** — GitHub และ staging อยู่ที่ `fdceaf9`, deploy แบบ Hosting-only, pre-deploy tests PASS, live smoke v7.5.1 payment-safety ผ่าน, ไม่มี Cloud Run / production deploy, ไม่แตะ env/secrets, ไม่เปิด public signup / user-visible AI จริง / Gemini จริง / real lead sending
**วันที่:** 2026-06-19
**Commit ปิดรอบล่าสุด:** `fdceaf9ace432b8c9c101a166d485ff9e5704efc` (`fdceaf9`)
**Branch:** `feature/chat-image-attachment-v1`
**สถานะ git:** local = origin, working tree clean

> Closure record ของรอบ staging deploy v7.1–v7.5.1 เท่านั้น — เป็นเอกสาร + static validation **ไม่แตะ runtime logic / ไม่ deploy เพิ่ม** เอกสารนี้ไม่มี secrets, tokens, PII, phone, plate หรือ VIN

---

## 1. ขอบเขต closure record นี้

บันทึกปิดรอบการนำ v7.1–v7.5.1 ขึ้น **staging (Hosting-only)** หลังผ่าน pre-deploy tests และ live smoke โดยรวมงานทั้งสายเป็นชุดเดียว:

- v7.1 Lead Flow Escape + Intent Re-check
- v7.2 Conversational Lead Memory
- v7.3 Natural Lead Preview + No Queue Count Display
- v7.4 AI Answer Naturalness + Car Card Narrative Fusion
- v7.5 Thai Used-car Market Context + Safety Advice Layer
- v7.5.1 Payment-Safety Coverage Fix (staging smoke patch)

---

## 2. Commit / branch ที่ปิดรอบ

| รายการ | ค่า |
|--------|-----|
| Commit ปิดรอบล่าสุด (full) | `fdceaf9ace432b8c9c101a166d485ff9e5704efc` |
| Commit ปิดรอบล่าสุด (short) | `fdceaf9` |
| Branch | `feature/chat-image-attachment-v1` |
| GitHub (origin HEAD) | `fdceaf9` |
| Staging (live) | `fdceaf9` |
| local = origin | sync ✅ |
| Working tree | clean |

ลำดับ commit สำคัญในรอบนี้: `1188ae1` (v7.2) → `1499ea0` (v7.3) → `45f3446` (v7.4) → `b0ca96e` (v7.5) → `fdceaf9` (v7.5.1)
Staging ถูก deploy 2 ครั้ง: ครั้งแรกที่ `b0ca96e` (v7.5), แล้ว re-deploy ที่ `fdceaf9` (v7.5.1)

---

## 3. ชนิดการ deploy

| รายการ | ค่า |
|--------|-----|
| Deploy type | **Hosting-only** |
| Firebase project | `nonga-ce93c` |
| Hosting site | `nonga-ce93c` |
| Hosting URL | https://nonga-ce93c.web.app |
| Deploy command | `firebase deploy --only hosting --project nonga-ce93c` |
| Live JS asset (parity check) | `index-D15T-l5t.js` (ตรงกับ local build ที่ `fdceaf9`) |

---

## 4. ยืนยัน scope / guardrails ของ deploy

| Item | Result |
|------|--------|
| Cloud Run / backend image deploy | **NO** |
| Production deploy | **NO** |
| env / secrets change | **NO** |
| Public signup เปิด | **NO** |
| User-visible AI จริง เปิด | **NO** |
| Gemini จริง เปิด | **NO** |
| Real lead sending เปิด | **NO** |
| import Thor runtime จริง | **NO** |
| แตะ runtime logic / lead payload / consent / final confirmation | **NO** |
| แตะ dealer isolation / privacy / data model | **NO** |
| force push | **NO** |

เหตุผลที่ไม่ต้อง deploy backend: โค้ด AI/chat/lead-draft ของ v7.1–v7.5.1 รันฝั่ง client (อยู่ในบันเดิล SPA → Hosting); server (`server.ts`) ไม่ได้ import orchestrator/advisor/safety/memory และโมดูลที่ server ใช้ร่วม (`buyerLeadQueuePolicy.publicQueuePayloadHasNoOtherBuyerPii`) ไม่ถูกแก้ในรอบนี้ → พฤติกรรม Cloud Run ไม่เปลี่ยน

---

## 5. สรุปผล pre-deploy tests / regression / tsc

| รายการ | ผล |
|--------|-----|
| `npm run test:v71-lead-flow-escape-intent-recheck` | PASS 34 |
| `npm run test:v72-conversational-lead-memory` | PASS 36 |
| `npm run test:v73-natural-lead-preview-no-queue-count` | PASS 29 |
| `npm run test:v74-ai-answer-naturalness-card-narrative-fusion` | PASS 40 |
| `npm run test:v75-thai-used-car-market-context-safety-advice` | **PASS 72** (รวมเคส v7.5.1) |
| regression: v548c / v548d | PASS 66 / 34 |
| regression: v546 finance / v547 help | PASS 25 / 48 |
| regression: v549c / v61l2f | PASS 23 / 49 |
| รวม | **ALL PASS: True** |
| build:staging:hosting + `verifyDistBundle` | PASS (project=nonga-ce93c, signup=false) |
| tsc filter เฉพาะไฟล์ v7.5/v7.5.1 | clean |

---

## 6. สรุปผล smoke บน live staging

### 6.1 v7.5.1 Payment-Safety Coverage (โฟกัสรอบนี้)

| เคส | ผล |
|-----|-----|
| `ต้องโอนมัดจำก่อนดูรถไหม` → คืนคำแนะนำความปลอดภัย (ไม่ใช่ "ไม่มีรถ") | **PASS — แก้แล้ว** |
| `มีรถ SUV ให้นัดดูรถไหม` → คืนรถ + safety nudge 1 บรรทัด | PASS |
| `มีรถ Yaris ไหม` (control) → ไม่มี safety nudge | PASS |

### 6.2 รวม v7.1–v7.5 (เท่าที่ทำได้แบบ read-only)

| เคส | ผล |
|-----|-----|
| หน้าเว็บโหลดได้ / ไม่มี public signup (invite-only) | PASS |
| chat search ได้การ์ดรถ + fitReason ต่อใบ | PASS |
| ไม่มี buyer-facing queue count | PASS |
| market note weave เมื่อมีงบ/ไฟแนนซ์ | PASS |
| คำตอบ deterministic / ไม่มี console error | PASS |

> หมายเหตุ: lead preview + กรอกเบอร์เอง + final confirmation ไม่ได้เปิด modal เต็มในรอบ smoke (กติกาห้ามกรอกเบอร์/ส่ง lead จริง) — flow ส่วนนี้ไม่ถูกแก้ใน v7.5/v7.5.1

---

## 7. Baseline issues เดิม (ไม่ใช่ regression จากรอบนี้)

| # | รายการ | สถานะ |
|---|--------|-------|
| B-1 | `scripts/test-v69m-thor-owner-gate-controlled-revenue-pilot-go-no-go-checklist.mts` ทำ `tsc --noEmit` exit 2 | known existing issue, นอก scope |
| B-2 | `scripts/test-nonga-chat-car-cards.mts` FAIL (sessionStorage ไม่มีใน Node + test เก่า expect legacy routing) | known existing issue, นอก scope |
| B-3 | staging inventory เป็นข้อมูลจริง บาง search อาจตอบ "ไม่มีรถ" | พฤติกรรมปกติของข้อมูลจริง |

ทั้งหมด reproduce ได้ก่อนรอบ v7.5.1 → ไม่ใช่ regression

---

## 8. Rollback path

- **Hosting rollback ทำได้**: `firebase hosting:rollback` หรือ Firebase Console → Hosting → Release history → เลือก release ก่อนหน้า (`b0ca96e` หรือก่อนหน้า) → Rollback
- **ไม่แตะ backend / data**: ไม่มี Cloud Run deploy, ไม่แก้ Firestore/Storage rules, ไม่มี data migration → ไม่มีอะไรต้องย้อนฝั่ง backend/data
- Git rollback (ถ้าจำเป็น): สร้าง revert commit แล้ว build/deploy ใหม่ — **ไม่ force push**

---

## 9. Next recommended step

**แนะนำให้ลุงเด่นทดลองใช้งาน staging แบบผู้ใช้งานจริง** (เปิด chat ค้นหารถ, ถามคำแนะนำ market/safety, ลองเส้นทาง lead จนถึง modal โดยไม่ส่งจริง) เพื่อเก็บ feedback คุณภาพคำตอบและ UX **ก่อน** จะพิจารณาขั้นถัดไป เช่น production deploy หรือการเปิด Gemini จริง / user-visible AI ซึ่งต้องขออนุมัติแยกและมี readiness plan เฉพาะ

ยังไม่แนะนำให้เปิด production / Gemini จริง / public signup ในขั้นนี้

---

## 10. This closure record (pending commit)

| File | Purpose |
|------|---------|
| `docs/v7.5.1A-staging-deploy-closure-record.md` | This document (staging deploy closure record v7.1–v7.5.1) |
| `scripts/test-v751a-staging-deploy-closure-record.mts` | Static validation |
| `package.json` | `test:v751a-staging-deploy-closure-record` |

---

## 11. Final verdict

**CLOSED — v7.1–v7.5.1 ON STAGING (HOSTING-ONLY, LOCAL-SAFE)**

- GitHub + staging อยู่ที่ `fdceaf9`, local = origin, working tree clean
- Hosting-only deploy (`firebase deploy --only hosting --project nonga-ce93c`), live asset parity ✅
- pre-deploy ALL PASS, v7.5 PASS 72, build + verifyDistBundle PASS
- live smoke v7.5.1 payment-safety ผ่าน (ประเด็น smoke เดิมจาก `b0ca96e` แก้แล้ว)
- ไม่มี Cloud Run / production deploy, ไม่แตะ env/secrets, ไม่เปิด public signup / user-visible AI จริง / Gemini จริง / real lead sending
- baseline B-1/B-2/B-3 เป็น known issue เดิม ไม่ใช่ regression
- next: ลุงเด่นทดลอง staging แบบผู้ใช้จริง ก่อนคิดเรื่อง production / Gemini จริง

---

*End of v7.5.1A staging deploy closure record (v7.1–v7.5.1 on staging, hosting-only, local-safe).*
