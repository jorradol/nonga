# v22.3 - Thor Auto Dry-Run Mock Template Validation

Date: 2026-07-07
Branch: `feature/chat-image-attachment-v1`
Execution type: `STATIC LOCAL DOCS/TESTS/FIXTURES + THOR AUTO MOCK TEMPLATE VALIDATION ONLY / MOCK DATA ONLY / NO REAL INVENTORY IMPORT / NO DEALER-FACING SEND / NO REAL LEAD`

## Baseline confirmation from v22.2

PASS — v22.2 Thor Auto data shape review prepared with mock/sanitized template only, no real dealer action performed

## Owner v22.3 approval phrase (recorded exactly)

`FINAL AUTHORIZE v22.3 THOR AUTO DRY-RUN MOCK TEMPLATE VALIDATION / MOCK DATA ONLY / STAGING ONLY / NO PUBLIC / NO PRODUCTION / NO REAL LEAD / NO REAL CUSTOMER DATA / NO PII PHONE PLATE VIN / NO TOKEN HEADER COOKIE SECRET SHARING / NO REAL INVENTORY IMPORT YET / NO DEALER-FACING SEND / NO RETRY / NO SECOND-RUN / DO NOT MOVE TO STEP 4`

This phrase authorizes v22.3 mock template validation only. It does not authorize real dry-run execution.

## 1) Scope in v22.3 (mock template validation only)

- mock template validation only
- mock data only
- no real dealer data
- no real inventory import
- no dealer-facing send
- no real lead creation/send
- do not move to Step 4
- do not mark Step 3 completed

## 2) Required fields list

- `dealer_code` (placeholder only, e.g. `THOR_MOCK`)
- `car_id` (placeholder only)
- `make`
- `model`
- `year`
- `transmission`
- `fuel_type`
- `mileage_range`
- `sanitized_price`
- `location_area`
- `public_description`
- `availability_status`

## 3) Optional fields list

- `trim`
- `body_type`
- `image_placeholder_url`
- `safety_notes`

## 4) Forbidden fields list

- real customer name
- real customer phone
- real buyer lead info
- real dealer staff phone
- real dealer private notes
- license plate
- VIN
- full registration data
- precise internal cost
- bank/account/payment details
- token/header/cookie/secret/env
- Authorization header
- real credential
- any real PII

## 5) Field-level validation rules

- `dealer_code` must be placeholder pattern: `THOR_MOCK` or `THOR_MOCK_*`
- `car_id` must be placeholder ID, not production identifier
- `year` must be integer in safe range `1990..2035`
- `mileage_range` must be range string, not exact identity-linked record
- `sanitized_price` must be sanitized range/value (e.g. `4xx,xxx THB`)
- `image_placeholder_url` if provided must be placeholder/mock URL
- no unknown sensitive key may be present

## 6) Redaction rules

- phone-related values -> `REDACTED_PHONE`
- plate-related values -> `REDACTED_PLATE`
- VIN-related values -> `REDACTED_VIN`
- personal identifiers -> `REDACTED_PII`
- internal dealer note content -> `REDACTED_INTERNAL_NOTE`
- credentials/token/header/cookie/secret/env -> never included

## 7) Owner-friendly explanation (Thai business terms)

เอกสารนี้พิสูจน์เฉพาะว่า "แบบฟอร์มข้อมูลรถตัวอย่าง" ปลอดภัยพอสำหรับวางแผนการทดสอบในอนาคต โดยยังไม่แตะข้อมูลจริงของดีลเลอร์และลูกค้า ไม่ได้พิสูจน์ผลการนำเข้าข้อมูลจริง และยังไม่ใช่อนุมัติให้เริ่ม dry-run จริง

## 8) Owner-friendly checklist

1. ยืนยันว่าไฟล์นี้ใช้ `mock data only`
2. ยืนยันว่าไม่มีข้อมูลลูกค้าจริง/ดีลเลอร์จริง/PII
3. ยืนยันว่าไม่มีการ import inventory จริงและไม่มีการส่งอะไรให้ dealer
4. ยืนยันว่าไม่มีการสร้างหรือส่ง lead จริง
5. ตรวจ PASS/NEED REVIEW/HOLD ก่อนขออนุมัติรอบถัดไป

Owner should not be asked to paste token/secret/env/header/cookie or choose terminal/session.

## 9) Technical checklist for น้องซี

- validate fixture exists and parses as JSON
- validate required fields present
- validate optional fields are restricted to allowlist
- validate forbidden keys are absent
- validate placeholder-only patterns for `dealer_code` and `car_id`
- validate no path enables real inventory import/mutation
- validate no path enables dealer-facing send or real lead create/send
- validate Step 3 remains validation-only and Step 4 remains locked

## 10) PASS / NEED REVIEW / HOLD criteria

- PASS:
  - v22.2 baseline pass confirmed
  - exact v22.3 owner phrase recorded
  - mock fixture passes required/optional/forbidden checks
  - Step status remains safe with no execution start
- NEED REVIEW:
  - uncertain field rule or ambiguous placeholder values
  - unclear owner wording that may cause real-data submission
- HOLD:
  - validator failure or missing required section
  - any sign of real-data inclusion, dealer contact/send, or lead action

## 11) Future approval phrase for v22.4 documented only

`FINAL AUTHORIZE v22.4 THOR AUTO FUTURE DRY-RUN APPROVAL PACKET / OWNER DECISION ONLY / NO NEW RUN / NO REAL INVENTORY IMPORT YET / NO DEALER-FACING SEND / NO REAL LEAD / NO REAL CUSTOMER DATA / NO PII PHONE PLATE VIN / NO TOKEN HEADER COOKIE SECRET SHARING / NO PUBLIC / NO PRODUCTION / NO RETRY / NO SECOND-RUN / DO NOT MOVE TO STEP 4`

In v22.3, this phrase is documented only, not treated as approval, and no v22.4 work is performed.

## Step status record in v22.3

- Step 1: `completed`
- Step 2: `completed`
- Step 3: `mock_template_validation_only`
- Step 3 execution started?: `false`
- Step 3 completed?: `false`
- Step 4: `not_started`
- Step 5: `not_started`

## Boundary confirmation in v22.3

- no real dealer action
- no dealer-facing send
- no real inventory import
- no real inventory mutation
- no real lead creation/send
- no real customer data
- no real dealer data
- mock data only
- no token/header/cookie/secret/env handling
- no PII/phone/plate/VIN
- no deploy/public/production
- no approval to import real inventory
- no approval to contact dealer
- no approval to create/send real lead
- no Step 4 movement

ยังไม่ public, ยังไม่ production, ยังไม่ real dealer, ยังไม่ real lead และยังไม่แตะของจริง
