# v6.7C-1 — Dealer Inventory Real BuyerLead Queue Wiring Patch Execution Record

**สถานะ:** **PASS** — patch commit + push **DONE**; lead queue tests **PASS**; `npm run lint` **PASS**; static doc validation **PASS**; **no deploy**; staging dealer-inventory smoke **not verified**  
**วันที่:** 2026-06-17  
**Git baseline (synced):** `d2ac583ca1221f55bd76102c3d8d78973f42ff57` (`d2ac583`)  
**Previous HEAD:** `2235ce0129c4963b2c7d4a3a6fba8dde396526b5` (`2235ce0`)  
**Push range:** `2235ce0..d2ac583`  
**Branch:** `feature/chat-image-attachment-v1`  
**Patch commit message:** `feat(dealer): wire real BuyerLead queue into Dealer Portal inventory`

> UI-only wiring patch record following v6.7A lead multi-owner isolation audit. No secrets, tokens, API keys, Authorization headers, raw prompts, buyer phone numbers, emails, or private lead data in this document. **Not production-ready. Not deploy-ready. Not pilot-ready. Production pilot still NOT READY. Thor Auto runtime import remains blocked.**

---

## 1. Overall verdict

| Item | Result |
|------|--------|
| v6.7C-1 objective (real per-listing BuyerLead queue on Dealer Portal inventory) | **DONE** |
| Patch commit on `origin/feature/chat-image-attachment-v1` | **DONE** (`d2ac583`) |
| `npm run test:lead-multi-owner-isolation` | **PASS** |
| `npm run test:v56d-buyer-lead-queue` | **PASS** |
| `npm run test:v56e-seller-masked-lead-queue` | **PASS** |
| `npm run test:v56e3-seller-queue-smoke-ux` | **PASS** |
| `npm run lint` | **PASS** |
| Static execution-record validation | **PASS** (`test:v67c1-dealer-inventory-real-buyerlead-queue-wiring-patch-execution-record`) |
| Staging Hosting deploy | **NO** |
| Staging dealer-inventory smoke (live) | **NOT PERFORMED** |
| Thor Auto real runtime import | **Still blocked** until deploy + smoke pass |
| Cloud Run | **Not touched** |
| Production | **Not touched** |
| **v6.9 Controlled Production Pilot** | **NOT READY** |

**Scope note:** PASS applies only to the v6.7C-1 patch commit + push + static verification slice. This is **not** a staging deploy sign-off or live dealer-inventory smoke sign-off.

---

## 2. Baseline verification

| Field | Value |
|-------|-------|
| Repo | `https://github.com/jorradol/nonga.git` |
| Branch | `feature/chat-image-attachment-v1` |
| Previous HEAD | `2235ce0129c4963b2c7d4a3a6fba8dde396526b5` (`2235ce0`) |
| Current synced HEAD | `d2ac583ca1221f55bd76102c3d8d78973f42ff57` (`d2ac583`) |
| Push range | `2235ce0..d2ac583` |
| Local HEAD | `d2ac583` (= `origin/feature/chat-image-attachment-v1` after push) |
| Working tree (at record time) | clean (docs/script pending separate commit) |
| Deploy performed | **NO** |

---

## 3. Context from v6.7A audit

| Finding | Notes |
|---------|-------|
| Primary gap | Dealer Portal inventory showed published listings but no real BuyerLead queue |
| Secondary gap | `DealerLeads.tsx` sandbox CRM looked like a real lead system but used demo/mock data |
| Existing real path | `MyListingsView` → `ListingLeadQueueSection` → `SellerMaskedLeadQueuePanel` + scoped APIs already worked |
| Parent Thor group | No aggregated parent-group lead view API exists (documented GAP in v6.7A harness) |
| Proposed fix (v6.7C) | Wire existing per-listing queue into Dealer Portal; gate/relabel sandbox CRM |

---

## 4. Patch scope summary

| # | File | Role |
|---|------|------|
| 1 | `src/components/dealer-portal/DealerInventoryPage.tsx` | Render `ListingLeadQueueSection` under each published (non-hidden) inventory card |
| 2 | `src/components/dealer/DealerSidebar.tsx` | Hide sandbox CRM nav item for non-sandbox/non-dev users; relabel badge as Demo when visible |
| 3 | `src/components/DealerDashboardView.tsx` | Redirect away from sandbox `leads` tab when sandbox navigation is hidden |
| 4 | `src/components/dealer/DealerLeads.tsx` | Demo CRM banner + CTA to Dealer Portal inventory and My Listings real queue path |

**Diff stat (patch commit):** `4 files changed, 76 insertions(+), 2 deletions(-)`

Out of scope (deferred): aggregated dealer inbox, parent Thor group lead aggregation, backend/API route changes, full CRM rewrite, staging deploy, live dealer-inventory smoke.

---

## 5. Behavior before / after

**Before:**

- Dealer Portal inventory had no real lead queue
- `DealerLeads` CRM looked like a real lead system but was sandbox/demo

**After:**

- Dealer Portal inventory surfaces real per-listing `ListingLeadQueueSection` for published listings
- Real queue path uses existing `SellerMaskedLeadQueuePanel` / scoped APIs
- Sandbox `DealerLeads` is hidden for non-sandbox/non-dev users and relabeled/bannered as demo when visible

| Surface | Before | After |
|---------|--------|-------|
| Dealer Portal → รถที่ลงขายแล้ว | Published inventory cards only — no real lead queue | `ListingLeadQueueSection` shown under each non-hidden published listing |
| Real queue data path | Available via My Listings only | Also available per listing in Dealer Portal inventory |
| Queue panel / APIs | Existing `SellerMaskedLeadQueuePanel` + scoped listing APIs | **Unchanged** — reused, not rewritten |
| Dealer dashboard CRM nav | Always visible as "ลูกค้าติดต่อขอซื้อ CRM" with "ใหม่" badge | Hidden for real (non-sandbox, non-dev) users; when visible, relabeled **Demo CRM — ไม่ใช่ลีดจริง** |
| DealerLeads tab content | Looked like real CRM | Demo banner explains sandbox nature; CTAs route to Dealer Portal inventory and My Listings |
| Hidden listings | N/A | Queue section omitted when `listingStatus === "hidden"` |

### 5.1 Sandbox navigation gating (UI-only)

Sandbox CRM navigation remains available when:

- `isSimulatedState` is true, **or**
- `import.meta.env.DEV` is true

Real dealers in production-like sessions are directed to the real per-listing queue path instead of the demo CRM.

---

## 6. Paths wired (reuse only — no backend changes)

| Path | Role |
|------|------|
| `DealerInventoryPage` → `ListingLeadQueueSection` | Per-listing queue UI on Dealer Portal inventory |
| `ListingLeadQueueSection` → `SellerMaskedLeadQueuePanel` | Existing masked seller queue panel |
| Scoped listing lead APIs (existing) | Queue fetch, reveal, skip — unchanged |
| `MyListingsView` → `ListingLeadQueueSection` | Pre-existing real queue path — unchanged |
| `DealerLeads.tsx` | Demo-only; banner + navigation CTAs to real paths |

No new API routes. No Firestore writes. No lead engine rewrite.

---

## 7. Verification summary

| Check | Result | Notes |
|-------|--------|-------|
| `npm run test:lead-multi-owner-isolation` | **PASS** | v6.7A harness — owner isolation + documented parent-group GAP |
| `npm run test:v56d-buyer-lead-queue` | **PASS** | Buyer queue ordering, reveal, supersede |
| `npm run test:v56e-seller-masked-lead-queue` | **PASS** | Seller masked queue, skip, feedback |
| `npm run test:v56e3-seller-queue-smoke-ux` | **PASS** | UX hardening, layout, scoped guards |
| `npm run lint` | **PASS** | `tsc --noEmit` at patch time |
| `npm run test:v67c1-dealer-inventory-real-buyerlead-queue-wiring-patch-execution-record` | **PASS** | Static doc validation (this record) |
| Staging Hosting deploy | **NOT RUN** | Separate approval required |
| Live staging dealer-inventory smoke | **NOT RUN** | Requires deploy + authenticated dealer session check |

---

## 8. Push result

| Item | Value |
|------|-------|
| Remote | `origin` (`https://github.com/jorradol/nonga.git`) |
| Branch | `feature/chat-image-attachment-v1` |
| Push range | `2235ce0..d2ac583` |
| Result | **SUCCESS** |
| Local HEAD after push | `d2ac583ca1221f55bd76102c3d8d78973f42ff57` |
| Origin HEAD after push | `d2ac583ca1221f55bd76102c3d8d78973f42ff57` |

---

## 9. Residual risks

| Risk | Level | Notes |
|------|-------|-------|
| No aggregated dealer inbox | **HIGH (UX)** | Dealers must open each listing to see its queue |
| Parent Thor group aggregated lead view | **HIGH (UX)** | Still missing — documented GAP from v6.7A |
| Staging dealer-inventory smoke not verified | **MEDIUM** | Live bundle behavior unconfirmed until deploy + manual smoke |
| Per-listing wiring only | **MEDIUM** | Not a full CRM rewrite; sandbox CRM still exists for dev/sim |
| Sandbox CRM confusion in dev mode | **LOW** | Demo banner + nav relabel reduce risk when sandbox nav is visible |

---

## 10. Remaining gates

| Gate | Status |
|------|--------|
| This execution record commit/push | **Pending** — separate approval |
| Staging Hosting deploy for `d2ac583` | **Not yet done** |
| Staging dealer-inventory smoke | **Not performed** — verify queue appears under published cards on live bundle |
| Safe dealer/authenticated session smoke | **Not performed** — if required before broader rollout |
| Thor owner gate before runtime import | **Blocked** — Thor Auto real runtime import remains blocked |
| **Production pilot** | **NOT READY** |

---

## 11. Safety confirmations

| Item | Result |
|------|--------|
| Cloud Run | **Not touched** |
| Production deploy | **NO** |
| Production changes | **NO** |
| Staging deploy | **NO** |
| Env / secrets / deploy config | **Unchanged** (patch commit) |
| Backend / API route changes | **NO** |
| Firestore write | **Not touched** |
| Payment / invoice / boost logic | **Not touched** |
| AI runtime / Gemini / guard / fallback | **Not touched** |
| Secret / token / session / PII logged | **NO** |
| Buyer phone / email / private lead data in docs/tests | **NO** |
| Thor Auto real runtime import | **Still blocked** |
| **v6.9 Controlled Production Pilot** | **NOT READY** |

---

## 12. Recommended next step

After this execution record is committed and pushed, request **separate approval** for:

1. **Staging Hosting deploy** of `d2ac583` (Hosting only; Cloud Run unchanged)
2. **Dealer inventory smoke** — confirm `ListingLeadQueueSection` renders under published cards on live Dealer Portal inventory
3. **Safe authenticated dealer session smoke** — if required before broader dealer rollout
4. **Thor runtime import gate review** — only after deploy + smoke pass

Do not start Thor Auto real runtime import until deploy + smoke complete.

---

## 13. v6.7C-1 patch commit reference

| Field | Value |
|-------|-------|
| Commit | `d2ac583ca1221f55bd76102c3d8d78973f42ff57` |
| Parent | `2235ce0129c4963b2c7d4a3a6fba8dde396526b5` |
| Message | `feat(dealer): wire real BuyerLead queue into Dealer Portal inventory` |
| Body | Surface the existing per-listing masked BuyerLead queue on published Dealer Portal inventory cards and gate the legacy sandbox DealerLeads CRM so real dealers are directed to the real lead path. |
| Pushed to | `origin/feature/chat-image-attachment-v1` |

---

## 14. This execution record (pending commit)

| File | Purpose |
|------|---------|
| `docs/v6.7C-1-dealer-inventory-real-buyerlead-queue-wiring-patch-execution-record.md` | This document |
| `scripts/test-v67c1-dealer-inventory-real-buyerlead-queue-wiring-patch-execution-record.mts` | Static validation |
| `package.json` | `test:v67c1-dealer-inventory-real-buyerlead-queue-wiring-patch-execution-record` |

---

*End of v6.7C-1 dealer inventory real BuyerLead queue wiring patch execution record.*
