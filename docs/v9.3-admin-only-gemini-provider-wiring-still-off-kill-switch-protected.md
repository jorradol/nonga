# v9.3 Admin-Only Gemini Provider Wiring - Still OFF / Kill-Switch Protected

> **PLACEHOLDER WIRING ONLY / STILL OFF / NO REAL GEMINI CALL**
> v9.3 เพิ่มโครง provider wiring สำหรับ admin-only runtime proof ในอนาคต แต่ยังปิดทั้งหมด

---

## 1. Purpose

เตรียมโครง provider wiring เพื่อรองรับงาน runtime proof แบบ admin-only ในอนาคต โดยยังไม่เปิดใช้งานจริง

- default OFF
- kill switch protected
- no real Gemini call
- no network/provider call
- no deploy
- no production
- no public route
- no buyer-facing AI
- no user-visible AI
- no real lead sending
- no real secret/API key in repo

## 2. Provider Wiring (Placeholder Only)

- เพิ่ม adapter interface และ disabled adapter (`gemini-placeholder`)
- ต่อ env/config gate สำหรับ provider request แบบ placeholder เท่านั้น
- สถานะ effective provider ต้องเป็น OFF เสมอใน v9.3
- แม้ config ครบ ก็ยัง block ด้วย `provider_wiring_placeholder_off`

## 3. Kill Switch and Guard Order

ลำดับการบล็อก:

1) kill switch (`AI_RUNTIME_PROOF_KILL_SWITCH`) ชนะทุก flag
2) runtime proof default OFF
3) admin-only boundary required
4) provider flag off
5) quota guard missing
6) cost guard missing
7) logging redaction required
8) provider wiring placeholder off

## 4. Fallback and Source of Truth

- deterministic flow remains source of truth
- deterministic fallback remains source of truth
- ถ้า env/config ไม่ครบหรือไม่ผ่าน guard ให้ fallback deterministic เสมอ

## 5. Logging Redaction and Quota/Cost Placeholder Guards

- logging diagnostics ต้อง redacted (ไม่เก็บ raw prompt/PII/secret)
- มี quota guard placeholder (`AI_RUNTIME_PROOF_QUOTA_LIMIT`)
- มี cost guard placeholder (`AI_RUNTIME_PROOF_MAX_COST_USD`)
- ไม่มีการส่ง request ไป provider จริงใน v9.3

## 6. Boundaries and Non-Activation Confirmation

- route ยังอยู่ใต้ `/api/admin` และผ่าน `adminApiAuth`
- buyer path ไม่เข้าถึง provider wiring
- no Gemini activation
- no runtime activation
- no deploy / no production

