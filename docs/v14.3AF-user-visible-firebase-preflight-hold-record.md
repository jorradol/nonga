# v14.3AF — User-Visible Firebase Preflight HOLD Record + Controlled Provider Dispatch Readiness

## 1) Execution type

v14.3AF type: docs/tests/fixtures/static diagnosis + controlled provider-dispatch readiness prep only  
one-run execution by agent: no  
retry by agent: no  
second run by agent: no  
provider/Gemini/runtime call by agent: no  
provider network call by agent: no  
deploy by agent: no  
runtime config mutation by agent: no

## 2) Boundary status

- no production: `yes`
- no public route activation: `yes`
- no buyer-facing public release: `yes`
- no real lead sending: `yes`
- no real customer data / PII: `yes`
- no phone / plate / VIN: `yes`
- no token / secret / API key exposure: `yes`
- no Thor real data import: `yes`
- no dealer real inventory import: `yes`

## 3) Branch / HEAD / origin / working tree before work

- branch: `feature/chat-image-attachment-v1`
- local HEAD: `6e0256a92267f06b541faa62283a0c81408d0572`
- origin HEAD: `6e0256a92267f06b541faa62283a0c81408d0572`
- local equals origin: `yes`
- working tree clean: `yes`

## 4) Previous owner-local Firebase preflight result

Owner sanitized result:

```text
owner-local-user-visible-one-run-gate-v143ac
firebaseToken: ***MASKED***
executeApproved=true
authModel=firebase-id-token
targetPath=/api/ai/chat-user-visible-orchestrate
runtimeAdapter=entered
runtimeAdapterPolicy=user-visible-firebase-auth-preflight
providerCall=not_run
Gemini/runtime=not_run
providerNetwork=not_run
deploy=not_run
runtimeMutation=not_run
tokenExposure=masked-only
HOLD — user-visible Firebase auth adapter reached controlled preflight
```

State interpretation:

- ownerLocalCommandStarted=true
- firebaseTokenShape=valid
- approvalTextMatched=true
- provider/network/runtime execution remained not_run
- one-run used in this preflight round; retry/second-run forbidden

## 5) Gate 0 repo sanity result

Executed:

```bash
git branch --show-current
git rev-parse HEAD
git fetch origin
git rev-parse origin/feature/chat-image-attachment-v1
git status --short
```

Result:

- branch check: pass
- local/origin sync: pass
- working tree clean: pass
- starting HEAD exact match: pass (`6e0256a92267f06b541faa62283a0c81408d0572`)
- Gate 0 decision: `PASS`

## 6) Exact preflight HOLD source

Exact fail-closed HOLD source:

- file: `scripts/owner-local-user-visible-one-run-gate-v143ac.mts`
- function: `runControlledRuntimeAdapterPreflight()`
- stop line: emits `HOLD — user-visible Firebase auth adapter reached controlled preflight`
- branch used: `--execute-approved` path after token/approval/lock gates

Conclusion: preflight stop is **intentional fail-closed**, not accidental crash.

## 7) Root cause summary

- v14.3AC wrapper was designed as preflight-only gate packet.
- after all auth/approval/lock checks pass, flow always enters preflight hold and exits.
- no provider dispatch implementation existed in wrapper before v14.3AF.

## 8) Dispatch readiness decision

v14.3AF decision: **Level 2 — controlled dispatch patch prepared (not executed by agent)**

Prepared behavior:

- preflight mode remains unchanged (`--execute-approved` only -> intentional HOLD)
- dispatch-ready mode added for next round (`--execute-approved --dispatch-approved`)
- dispatch mode is still fail-closed and requires fresh approval text for v14.3AG

## 9) Provider path mapping summary

Mapped fixed existing user-visible server route:

- fixed path: `/api/ai/chat-user-visible-orchestrate`
- server registration: `registerSalesBrainUserVisibleOrchestrationBridgeRoutes()` in `server.ts`
- route handler auth source: `getServerAuthContext()` in `src/server/serverAuthContext.ts`
- auth header requirement: `Authorization` with Firebase ID token

No manual endpoint guess in wrapper patch:

- target path fixed constant
- target origin fixed constant (staging-only posture)
- no CLI flag/env override for endpoint path

## 10) Lock/consume behavior analysis

Preflight mode (existing):

- lock file: `.nonga-owner-local-one-run-v143ac-user-visible.lock.json`
- consume occurs after token + admin-contamination + approval checks
- consume happens before intentional preflight HOLD

Dispatch-ready mode (new for v14.3AG):

- lock file: `.nonga-owner-local-one-run-v143ag-user-visible-dispatch.lock.json`
- consume occurs after same gates + v14.3AG approval phrase check
- retry/second-run blocked independently for dispatch phase

## 11) Controlled dispatch patch summary (prepared only)

Updated wrapper now supports a guarded dispatch branch:

- requires `executeApproved=true`
- requires `dispatchApproved=true` in addition
- requires exact fresh approval text:
  - `FINAL EXECUTION AUTHORIZE v14.3AG USER-VISIBLE FIREBASE DISPATCH SAME-CMD EXACTLY-ONE-RUN`
- requires valid-shape `NONGA_OWNER_FIREBASE_ID_TOKEN`
- blocks admin token contamination
- exact command policy retained (unknown args blocked)
- staging-only posture retained
- target path fixed to `/api/ai/chat-user-visible-orchestrate`
- no manual endpoint override
- token never logged
- synthetic/sanitized payload only (no lead intent)
- response evidence extraction is status-only and fail-closed

Dispatch success evidence required by wrapper:

```text
providerCall=run
Gemini/runtime=run
providerNetwork=true
fallbackToLegacy=false
skipGemini=false
gateReason=real_provider_call_ok
pilotPathActive=true
allowlistMatch=true
userVisibleEnabled=true
guardPolicyVersion=present
leadPiiCueGuardActive=true
phoneEchoGuardActive=true
safeConfirmationStepWordingActive=true
tokenExposure=masked-only
```

If any required evidence is missing -> wrapper holds fail-closed.

## 12) No-run / no-retry / no-second-run confirmation

- one-run command executed by agent: no
- retry attempted by agent: no
- second run attempted by agent: no

## 13) Runtime/provider/Gemini status during agent work

- runtime adapter execution by agent: `not_run`
- provider call by agent: `not_run`
- Gemini call by agent: `not_run`
- provider network by agent: `not_run`

## 14) Token/secret exposure confirmation

- no raw token/API key/secret printed by agent
- wrapper/checker remain masked-only for token fields
- docs/fixtures/tests contain no raw token value

## 15) Final decision

`READY FOR v14.3AG FRESH OWNER APPROVAL + USER-VISIBLE FIREBASE DISPATCH ONE-RUN`
