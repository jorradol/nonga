# v6.2B.13A — Real Stock Package Validation / Normalization Pre-import

**สถานะ:** **VALIDATION RECORD (REDACTED AGGREGATES)** — pre-import only — **ยังไม่ import รถจริง**, **ยังไม่ Firestore writes**  
**วันที่เอกสาร:** 2026-06-09  
**Git baseline:** `8a409c3bc8027df1521f7b8bb41ec6c642932ccd` (v6.2B.12 pushed)  
**Branch:** `feature/chat-image-attachment-v1`  
**อ้างอิง:** v6.2B.1 Template, v6.2B.6 Redacted GO Record, v6.2B.12 Cleanup Execution, G-11

> **v6.2B.13A = validate/normalize plan จาก source package นอก repo เท่านั้น** — **ไม่ใช่ import step** — git เก็บ **aggregate counts + pass/fix/no-go summary** ไม่เก็บข้อมูลรายคัน

---

## สารบัญ

1. [Execution Metadata](#1-execution-metadata)
2. [Source Package (Outside Repo)](#2-source-package-outside-repo)
3. [Structure Validation](#3-structure-validation)
4. [Row Count & Pilot Selection](#4-row-count--pilot-selection)
5. [Required Fields Coverage (Redacted)](#5-required-fields-coverage-redacted)
6. [Optional / Pre-import Gaps](#6-optional--pre-import-gaps)
7. [PII & Privacy / Plate Handling](#7-pii--privacy--plate-handling)
8. [Image Links (Redacted)](#8-image-links-redacted)
9. [Normalization Plan (Redacted)](#9-normalization-plan-redacted)
10. [UID Mapping Status](#10-uid-mapping-status)
11. [Recommendation](#11-recommendation)
12. [Import Gate (Still Blocked)](#12-import-gate-still-blocked)
13. [Forbidden in Repo (Record Rules)](#13-forbidden-in-repo-record-rules)
14. [Compliance (v6.2B.13A)](#14-compliance-v62b13a)

---

## 1. Execution Metadata

| Field | Value |
|-------|-------|
| project | **staging only** (`nonga-ce93c`) — production **excluded** |
| branch | `feature/chat-image-attachment-v1` |
| head / origin (baseline) | `8a409c3bc8027df1521f7b8bb41ec6c642932ccd` |
| execution date | 2026-06-09 |
| slice type | validation / normalization pre-import |
| real stock import | ❌ **not executed** |
| Firestore writes | ❌ **none** |
| deploy / env update | ❌ **none** |
| recordType | `redacted-validation-pre-import-record` |

---

## 2. Source Package (Outside Repo)

| Field | Value |
|-------|-------|
| packageLocation | **outside repo** — secure ops path (not in git) |
| fileNamePattern | `NongA_Car_Stock_EX.xlsx` |
| fileReadable | ✅ **yes** |
| fileSizeBytes | **23371** (aggregate only) |
| format | Excel (`.xlsx`) |
| real data in git | ❌ **forbidden** — none committed |

**Source package อ่านได้จาก secure ops path ที่ ops ให้ — path เต็มไม่บันทึกใน git**

---

## 3. Structure Validation

| Check | Result |
|-------|--------|
| File exists / readable | ✅ **pass** |
| Sheet name | `Car_Database_Template` |
| Template title rows | 2 (instruction rows — skip) |
| Header row index | row **4** (1-based Excel) / index **3** (0-based) |
| Header column count | **12** |
| Data rows detected | **18** |
| Empty trailing rows | ignored |

### Header structure (template labels only — no cell values)

| Col | Template label (abbreviated) |
|-----|------------------------------|
| 0 | License Plate / Province (combined source column) |
| 1 | Make |
| 2 | Model / Trim |
| 3 | Year |
| 4 | Gear |
| 5 | Color |
| 6 | Mileage |
| 7 | Front-store price (public max price) |
| 8 | Dealer/wholesale price (internal — exclude public) |
| 9 | Selling points & condition notes |
| 10 | Cover image link (Google Drive) |
| 11 | Gallery image links (Google Drive) |

---

## 4. Row Count & Pilot Selection

| Metric | Count |
|--------|------:|
| **rowCountTotal** | **18** |
| rows with all core required fields | **18** |
| rows with selling points present | **17** |
| rows missing selling points | **1** |
| rows with cover + gallery links | **18** |
| **pilotEligible (all required incl. selling + cover)** | **17** |
| **selectedPilotCount (recommended)** | **10** |

### Pilot batch selection rule (recommended)

1. Exclude rows missing **selling points** (count **1**) until fixed or explicitly skipped  
2. From remaining **17** eligible rows, select **10** for controlled pilot (within 5–10 policy max uses **10**)  
3. Selection method: **deterministic first-N eligible** or ops-prioritized subset — **no per-car IDs in git**  
4. Do not import all **18** — batch cap **10** for pilot

---

## 5. Required Fields Coverage (Redacted)

Aggregate pass/missing counts only — **no per-car values in git**

| Field | Present | Missing | Status |
|-------|--------:|--------:|--------|
| make | **18** | **0** | ✅ pass |
| model/trim | **18** | **0** | ✅ pass |
| year | **18** | **0** | ✅ pass (numeric sanity **18/18** ok) |
| price (front-store col) | **18** | **0** | ✅ pass (numeric sanity **18/18** ok) |
| mileage | **18** | **0** | ✅ pass (numeric sanity **18/18** ok) |
| gear | **18** | **0** | ✅ pass |
| color | **18** | **0** | ✅ pass |
| selling points | **17** | **1** | ⚠️ **fix required** (1 row) |
| cover image link | **18** | **0** | ✅ pass |
| gallery image links | **18** | **0** | ✅ pass |

**requiredFieldsPass summary:** **9/10 field groups pass** — selling points **fix required** for **1** row before import

---

## 6. Optional / Pre-import Gaps

Fields not in source file — must be supplied at manifest generation (outside repo / ops channel):

| Field | In source file | Status | Action before import |
|-------|----------------|--------|----------------------|
| sellerDisplayName (non-PII) | ❌ absent | ⚠️ fix required | add per seller/dealer at manifest build |
| ownerUid / sellerUid | ❌ absent | ⚠️ fix required | bind from **secure ops channel** (not git) |
| listingStatus | ❌ absent | ⚠️ fix required | set `published` per v6.2B plan at import |
| pilotBatchId | ❌ absent | ⚠️ fix required | assign batch id at manifest build |
| sourceTag | ❌ absent | optional | assign if cleanup cohort rules need tag |
| fuel | ❌ absent (no column) | ⚠️ fix required | infer from model/trim rules or add column |
| province (standalone) | combined in col 0 | ⚠️ normalize | split/mask — see §7 |

---

## 7. PII & Privacy / Plate Handling

| Check | Result |
|-------|--------|
| phone column in source | ❌ **none** — ✅ pass |
| LINE column in source | ❌ **none** — ✅ pass |
| address column in source | ❌ **none** — ✅ pass |
| document fields | ❌ **none detected** — ✅ pass |
| combined plate/province column (col 0) | ✅ **present 18/18** |
| col 0 contains digit + Thai script (aggregate) | **18/18** — treat as **plate-sensitive** |
| import full plate as public field | ❌ **forbidden** |

### plateFieldHandling (required)

| Rule | Action |
|------|--------|
| Source col 0 | **internal-only / ops validation** — may contain plate or plate+province text |
| Public listing payload | **exclude full plate** — do **not** map to public `licensePlate` |
| Normalization options | (a) **exclude** plate from public record; (b) **mask** if internal dedup needed; province extract only if non-PII |
| Wholesale price col 8 | **exclude from public** marketplace fields |
| Git / docs / reports | ❌ **no plate examples**, no full plate strings |

**plateFieldHandling status:** ⚠️ **exclude/mask/internal-only required** — confirmed before import manifest build

---

## 8. Image Links (Redacted)

Aggregate accessibility check performed **outside repo** — **no raw URLs in git**

| Metric | Count |
|--------|------:|
| rows with cover link | **18** |
| rows with gallery links | **18** |
| cover links checked | **18** |
| cover accessible (HTTP ok) | **18** |
| cover failed | **0** |
| gallery links total (split) | **55** |
| gallery accessible | **55** |
| gallery failed | **0** |
| host type (aggregate) | Google Drive **73** links — no other host |
| gallery links per row (min/max/avg) | **2** / **5** / **3.1** |

| Check | Status |
|-------|--------|
| imageLinksStatus | ✅ **pass** |
| raw image URL in git | ❌ **none** |
| image download at import | use existing import pipeline — **not in this slice** |
| AI vision / image processing runtime | ❌ **not run** |

**Note:** Drive links responded at validation time — import-time download still required via existing storage pipeline.

---

## 9. Normalization Plan (Redacted)

Pre-import manifest build steps (execution outside repo):

1. **Parse** sheet `Car_Database_Template` — skip rows 1–3 (title/instructions), header row 4, data from row 5  
2. **Select pilot batch** — **10** cars from **17** eligible (exclude **1** missing selling points unless fixed)  
3. **Map columns** → v6.2B.1 manifest schema (make/model/year/price/mileage/gear/color/highlights/photos)  
4. **Col 0** → internal-only; extract province if possible; **never** public full plate  
5. **Col 8** → drop from public payload (wholesale/internal price)  
6. **Col 9** → `highlights` / description (sanitized — no PII)  
7. **Col 10–11** → cover + gallery URLs — stay **outside repo** until import downloads to storage  
8. **Add at manifest time:** `pilotBatchId`, `listingStatus=published`, `sellerDisplayName` (non-PII), `ownerUid` from ops channel, `fuel` inference/default  
9. **Privacy gate** re-check per v6.2B.5/6 before v6.2B.13 import execution  
10. **Output** sanitized manifest JSON — **secure ops storage only**, not git

---

## 10. UID Mapping Status

| Check | Result |
|-------|--------|
| ownerUid in source Excel | ❌ **absent** (expected) |
| uid mapping in git | ❌ **forbidden** |
| uid mapping from secure ops channel | ⚠️ **required before import** |
| uidMappingStatus | ⚠️ **fix required** — ops must supply UID map outside repo |

---

## 11. Recommendation

### Summary table (for report)

| Item | Value |
|------|-------|
| **rowCountTotal** | **18** |
| **selectedPilotCount** | **10** |
| **requiredFieldsPass** | **9/10 groups pass** — selling points **1 missing** |
| **plateFieldHandling** | **exclude/mask/internal-only required** |
| **imageLinksStatus** | ✅ **pass** |
| **uidMappingStatus** | ⚠️ **fix required** |
| **recommendation** | ⚠️ **FIX REQUIRED** |

### FIX REQUIRED — blockers before v6.2B.13 import

| # | Item | Count / action |
|---|------|----------------|
| FR-01 | Selling points missing | **1** row — fix or exclude from pilot |
| FR-02 | ownerUid mapping | supply from secure ops channel |
| FR-03 | sellerDisplayName | add non-PII display names at manifest |
| FR-04 | listingStatus + pilotBatchId | assign at manifest build |
| FR-05 | fuel field | add column or inference rules |
| FR-06 | plate col 0 | enforce exclude/mask — no public full plate |
| FR-07 | wholesale price col 8 | exclude from public payload |

### Ready when

- Above fixes applied in **manifest build outside repo**  
- Pilot batch = **10** cars selected from eligible set  
- Re-run validation or proceed to **v6.2B.13 import execution** with explicit approval

**Not NO-GO:** structure, core fields, and image links are strong — gaps are manifest/metadata/normalization items.

---

## 12. Import Gate (Still Blocked)

| Item | Status |
|------|--------|
| v6.2B.13A validation | ✅ **complete** (this doc) |
| Real stock import (v6.2B.13) | ❌ **still blocked** — separate execution slice |
| Firestore writes | ❌ **none in v6.2B.13A** |
| Approval phrase for import | required separately for v6.2B.13 execute |

> **อนุมัติให้ import real stock แบบ controlled pilot บน staging เท่านั้น ตาม v6.2B ชุดเล็ก 5–10 คัน**

v6.2B.13A validation **does not execute import** — phrase applies to future v6.2B.13 slice only.

---

## 13. Forbidden in Repo (Record Rules)

| Forbidden in git | Rule |
|------------------|------|
| Per-car make/model/price | ❌ |
| Full plate / plate examples | ❌ |
| Raw image URL / Drive links | ❌ |
| Phone / LINE / address / UID | ❌ |
| Prompt / secret / env dump | ❌ |
| Source file path with secrets | ❌ — path pattern only |
| Import commands executed | ❌ |

---

## 14. Compliance (v6.2B.13A)

| Item | Status |
|------|--------|
| Read source package outside repo | ✅ |
| Validate structure / counts | ✅ |
| Redacted aggregate report | ✅ |
| Real stock import | ❌ not done |
| Firestore writes | ❌ none |
| Real data in repo | ❌ none |
| Production | ❌ not touched |
| Deploy / env / Firestore rules | ❌ none |
| Secret access | ❌ not done |
| Lead/reveal/outcome/payment mutation | ❌ none |
| Public signup / public AI / real Gemini | ❌ not enabled |
| Image processing / AI vision API | ❌ not run |

CI: `npm run test:v62b13a-real-stock-package-validation-normalization-pre-import` — readFileSync only.

---

## Redacted Validation Summary (for report — git-safe only)

```text
v6.2B.13A Real Stock Package Validation / Normalization Pre-import
source: outside repo Excel template package (readable)
sheet: Car_Database_Template | rowCountTotal: 18 | selectedPilotCount: 10
requiredFields: 9/10 pass — selling points missing: 1 row
plateFieldHandling: exclude/mask/internal-only required (col 0 present 18/18)
imageLinksStatus: pass (cover 18/18, gallery 55/55 accessible)
uidMappingStatus: fix required (ops channel)
recommendation: FIX REQUIRED
import: NOT executed | Firestore writes: none | real data in repo: none
next: fix gaps + build manifest outside repo → v6.2B.13 import execution
```

**Per-car details remain in secure ops storage only — never in this repository.**
