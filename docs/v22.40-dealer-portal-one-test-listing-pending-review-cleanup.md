# v22.40 — Dealer Portal One TEST Listing → Pending Review → Hold → Cleanup

Date: 2026-07-10  
Branch: `feature/chat-image-attachment-v1`  
Execution type: `CONTROLLED DEALER PORTAL TEST LISTING FLOW / CORRECT DEALER PATH ONLY / EXISTING nonga-dealer ONLY / ONE TEST LISTING ONLY / PENDING REVIEW VERIFY / ADMIN HOLD PREFERRED / CLEANUP REQUIRED / NO NEW DEALER / NO REAL LEAD / LEAD CAPTURE OFF / NO DEALER-FACING SEND / NO PRODUCTION / NO PUBLIC`

## 1) Purpose

Owner-approved follow-up after **v22.39 PASS**: run **exactly one** controlled TEST listing through the **correct Dealer Portal path** (`Dealer Portal → ยังไม่ลงขาย`), verify `pending_review`, verify admin pending queue, prefer hold, then **mandatory cleanup** so marketplace returns to **13**.

Hard boundaries observed: staging only; existing `nonga-dealer` only; no new dealer; no real PII/VIN/full plate/phone/address; lead capture OFF; no real lead; no dealer-facing send; no production; no public enable; no owner token/secret asked in chat.

## 2) Preflight state

| Check | Result |
|-------|--------|
| Host | `https://a.nongbot.org` + Hosting `https://nonga-ce93c.web.app` |
| Cloud Run revision | **`nonga-staging-00191-jqq`** (same as v22.34 era; **not** yet a post-v22.39 Cloud Run roll) |
| Hosting asset | **`assets/index-APBJgg5j.js`** |
| Health | `ok: true`, `dataBackend: firestore`, `imageBackend: firebase-storage` |
| `leadCaptureEnabled` | **false** |
| `publicSignupEnabled` | **false** |
| Unauth `POST /api/buyer-leads` | **401** |
| Marketplace count | **13** |
| TEST titles public | **0** |
| Public protected fields (`ownerId` / `dealerId` / `vin` / `licensePlateFull`) | **0** |
| Thor Auto listings/images | **13/13** with images; dealer display names Thor Auto / Thor Auto (locale) |
| Existing dealer `nonga-dealer` | **active** (`role=dealer`, membership `status=active`) |
| New dealer created? | **No** |
| Path note | Did **not** use admin Draft Inventory / คลังรถรอแก้ไข |

### Pre-existing dealer inventory note (not created this packet)

Dealer inventory already had **one** earlier `pending_review` row (Mitsubishi Pajero import). This packet **did not** approve/hold that row and did not count it as the v22.40 TEST listing.

## 3) Correct Dealer Portal path used

| Item | Value |
|------|--------|
| Intended UI path | Dealer Portal → **ยังไม่ลงขาย** (`/dealer/drafts`) → 「ลงขาย」 |
| Existing dealer | **`nonga-dealer`** (display: Nong A Staging Dealer) |
| Auth method | Operator-local Firebase Admin custom-token mint for existing `NONGA_TEST_DEALER_UID` (no owner password asked; no secrets in report) |
| Create | `POST /api/dealer/drafts/new` (same draft create surface used by dealer sell/draft flows that land in **ยังไม่ลงขาย**) |
| Publish | `POST /api/dealer/drafts/:id/publish` (same endpoint as `DealerDraftsPage` 「ลงขาย」) |
| Admin Draft Inventory used? | **No** |

### Browser UI limitation

Firebase custom-token auth via CDP did **not** persist into the app’s React Firebase session after SPA navigation, so a full interactive toast screenshot on `/dealer/drafts` was **not** captured. Evidence below is from the **Dealer Portal publish API** used by that page, plus admin hold APIs used by `/admin/pending-listings`.

## 4) TEST listing (redacted)

| Field | Safe value |
|-------|------------|
| Title | `TEST-v22.40-nonga-dealer-delete-me` |
| Draft id | `draft-178364…7161` |
| Listing id | `car-178364…6523` |
| Dummy vehicle | Toyota Yaris 2020 · fake price/mileage · dummy description |
| VIN / full plate / phone / address | **Not used** |
| Count of TEST listings created | **1** |

## 5) Dealer pending_review evidence

| Check | Result |
|-------|--------|
| Publish HTTP | **200** |
| `listingStatus` after publish | **`pending_review`** |
| Marketplace after publish | **13** (unchanged) |
| TEST title public after publish | **0** |
| Raw `Missing admin API token for server mode` on this publish path | **Not returned** |

## 6) Dealer “รออนุมัติ” / success copy evidence

| Surface | Evidence |
|---------|----------|
| Live staging API publish message | 「ส่งประกาศเข้ารออนุมัติแล้ว — ยังไม่แสดงในตลาดจนกว่าเจ้าของระบบจะอนุมัติ」 |
| Repo canonical (v22.39) dealer copy | 「ส่งรายการเรียบร้อยแล้ว รถของคุณอยู่ระหว่างรอผู้ดูแลอนุมัติ ก่อนแสดงในตลาด」 |
| Hosting bundle (`index-APBJgg5j.js`) contains new v22.39 success copy? | **No** |
| Hosting bundle still contains raw admin-token error string? | **Yes** (pre-v22.39 Hosting asset) |
| Live Cloud Run revision includes v22.39? | **No evidence** — still `nonga-staging-00191-jqq` |

Interpretation: pending/wait semantics on the **live** publish response are correct (waiting, not live marketplace). The **exact v22.39 Thai string** is present in repo/tests but **not yet on staging Hosting/Cloud Run**.

## 7) Admin pending queue + hold

| Check | Result |
|-------|--------|
| Admin pending queue includes TEST title | **Yes** (found before hold) |
| Preferred action | **Hold** 「พักไว้ก่อน」 via `POST /api/admin/listings/:id/hold` |
| Approved? | **No** |
| Status after hold | **`hidden`** |
| Marketplace after hold | **13** |
| Hold message | 「พักประกาศแล้ว — ยังไม่แสดงในตลาด」 |

## 8) Cleanup evidence

| Check | Result |
|-------|--------|
| Action | Hold already applied; cleanup reconfirm → **already-hidden** |
| TEST title public | **0** |
| Marketplace final | **13** |
| Extra dealers created | **0** |
| Extra TEST listings created | **0** (exactly one; now hidden) |
| Thor Auto listings/images preserved | **Yes** (13 with images) |
| Residual artifact | Hidden TEST row remains in dealer inventory (non-public). Safe for PASS boundaries; optional later archive/delete if owner wants zero residual. |

## 9) Final safety checks

| Check | Result |
|-------|--------|
| `leadCaptureEnabled:false` | **PASS** |
| `publicSignupEnabled:false` | **PASS** |
| Unauth buyer-lead 401 | **PASS** |
| No real lead created | **PASS** |
| No dealer-facing send | **PASS** |
| No production deploy | **PASS** |
| Public not enabled | **PASS** |
| No new dealer | **PASS** |
| Exactly one TEST listing created | **PASS** |
| TEST cleaned up / safely hidden | **PASS** (hidden; not public) |
| Marketplace returned to 13 | **PASS** |
| Thor Auto preserved | **PASS** |
| Public DTO safe | **PASS** |
| Raw admin-token error not dealer-visible on this publish path | **PASS** (API path) |
| Staging Hosting already free of raw token string / new copy live | **FAIL / deferred** — Hosting still pre-v22.39 |
| Secrets/PII in report | **PASS** (redacted ids only) |

## 10) Tests

| Check | Result |
|-------|--------|
| `npm run test:v22.40` | **PASS** |

## 11) Recommendation

**NEED REVIEW**

### Why not PASS

1. Staging Cloud Run remains **`nonga-staging-00191-jqq`** and Hosting remains **`index-APBJgg5j.js`** — **v22.39 Hosting/Cloud Run roll is not live**, so the canonical dealer success string from v22.39 did not appear on staging.  
2. Full interactive Dealer Portal toast / 「รออนุมัติ」 badge screenshot on `/dealer/drafts` was not captured (app React auth session did not adopt CDP custom-token).  
3. Owner should decide whether to **deploy v22.39 to staging Hosting + Cloud Run**, then optionally re-check UI copy once before calling the UX portion PASS.

### Why not HOLD

- Exactly one TEST was created via **Dealer Portal draft publish API** (ยังไม่ลงขาย publish endpoint).  
- Approval gate worked (`pending_review`, marketplace stayed 13).  
- Admin hold worked; cleanup left listing non-public; marketplace **13**.  
- Safety boundaries intact; no new dealer; no lead; no production; no public enable.  
- Waiting-for-approval semantics appeared on live API (older wording), not “already live”.

### Owner next decision (browser / deploy only — no secrets in chat)

1. Deploy **v22.39** (and this packet docs/tests if desired) to staging Hosting + `nonga-staging`.  
2. Optionally open Dealer Portal → ยังไม่ลงขาย while signed in as `nonga-dealer` to confirm toast text matches:  
   「ส่งรายการเรียบร้อยแล้ว รถของคุณอยู่ระหว่างรอผู้ดูแลอนุมัติ ก่อนแสดงในตลาด」  
3. Optional: archive/delete the already-hidden TEST residual if a zero leftover inventory row is preferred.

## 12) Explicit non-actions

- Did not use admin Draft Inventory for dealer submit proof  
- Did not ask owner for token/header/cookie/secret/env/password  
- Did not ask owner to use terminal  
- Did not create a new dealer  
- Did not create more than one TEST listing  
- Did not use real customer PII / VIN / full plate / phone / address  
- Did not set `NONGA_LEAD_CAPTURE_ENABLED=true`  
- Did not create real leads / dealer-facing send  
- Did not deploy production / enable public signup  
