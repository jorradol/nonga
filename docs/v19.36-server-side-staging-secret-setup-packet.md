# v19.36 - Server-Side Staging Secret Setup Packet

Date: 2026-07-07
Branch: `feature/chat-image-attachment-v1`
Execution type: `SERVER-SIDE STAGING SECRET SETUP PREPARATION / OWNER APPROVAL REQUIRED / NO DEPLOY / NO ONE-RUN`
Latest status before this task: `PASS — v19.35 server-side staging secret readiness packet`
Latest commit before this task: `0f6463c`

## 1) Purpose

- prepare safe setup steps for `NONGA_ADMIN_API_TOKEN` as server-side staging env/secret only.
- scope is preparation-only packet and command templates with placeholders.
- no real secret value in chat/repo/docs/report.
- no token in chat/repo/docs/report.

## 2) Boundary status

- no secret read
- no secret value exposure
- no deploy
- no one-run
- no retry
- no second-run
- no additional re-arm
- no lock reset/delete/mutation
- no public
- no production
- no real dealer action
- no real lead
- no real customer data
- no Thor real import
- no dealer real inventory import
- no token/secret/API key/platform auth credential/Authorization header exposure
- no PII/phone/plate/VIN exposure

## 3) Why this replaces owner-local token loop

- owner-local terminal token loop is deprecated for future runtime because it is fragile and costly.
- server-side staging env/secret is the correct single source of truth for runtime auth.
- owner budget/time constraint is best served by minimizing manual token handling and repeated loops.

## 4) Fresh owner approval requirement (mandatory before real setup)

- real secret setup in v19.36 is blocked unless owner provides exact wording below:

`FINAL AUTHORIZE v19.36 SERVER-SIDE STAGING SECRET SETUP FOR NONGA_ADMIN_API_TOKEN ONLY / STAGING ONLY / NO DEPLOY / NO ONE-RUN / NO PUBLIC / NO PRODUCTION / NO REAL DEALER / NO REAL LEAD / NO REAL CUSTOMER DATA / NO TOKEN VALUE IN CHAT OR REPO`

- if this exact approval is absent, v19.36 remains preparation-only.

## 5) Staging secret setup method findings (static)

- expected Cloud Run staging service name: `nonga-staging`
- expected region from repo config: `asia-southeast1`
- expected env/secret name: `NONGA_ADMIN_API_TOKEN`
- repo config evidence:
  - `firebase.json` routes `/api/**` and `/storage/listings/**` to Cloud Run `nonga-staging` region `asia-southeast1`
  - `src/server/apiAuth.ts` reads `process.env.NONGA_ADMIN_API_TOKEN`
  - `server.ts` guards `/api/admin` and `/api/dealer` server-side
- recommended setup location:
  - Cloud Run staging service env/secret
  - platform secret/env integration
  - Secret Manager reference

## 6) Command templates (placeholders only / no execution in this packet)

- template placeholders:
  - `<PROJECT_ID>`
  - `<REGION>`
  - `<SERVICE_NAME>`
  - `<SECRET_VALUE_ENTERED_OUTSIDE_CHAT>`
  - `<SECRET_VERSION>`

```bash
# Template A: Secret Manager create/update (placeholder only)
echo "<SECRET_VALUE_ENTERED_OUTSIDE_CHAT>" | gcloud secrets create NONGA_ADMIN_API_TOKEN --project="<PROJECT_ID>" --data-file=-
```

```bash
# Template B: Add new secret version (placeholder only)
echo "<SECRET_VALUE_ENTERED_OUTSIDE_CHAT>" | gcloud secrets versions add NONGA_ADMIN_API_TOKEN --project="<PROJECT_ID>" --data-file=-
```

```bash
# Template C: Bind secret to Cloud Run staging service env (placeholder only)
gcloud run services update "<SERVICE_NAME>" \
  --project="<PROJECT_ID>" \
  --region="<REGION>" \
  --set-secrets="NONGA_ADMIN_API_TOKEN=NONGA_ADMIN_API_TOKEN:<SECRET_VERSION>"
```

```bash
# Template D: Masked-only verification (presence/config shape only)
gcloud run services describe "<SERVICE_NAME>" \
  --project="<PROJECT_ID>" \
  --region="<REGION>" \
  --format="yaml(spec.template.spec.containers)"
```

- command templates above are for owner-approved future execution only.
- do not run any template without exact fresh owner approval wording.
- do not print or copy any token value into terminal logs, chat, repo, or docs.

## 7) Masked-only verification approach

- verify only that `NONGA_ADMIN_API_TOKEN` binding exists in staging service config.
- verify secret name/reference shape only, never secret value.
- record pass/fail as masked-only evidence.

## 8) Rollback/removal note

- if rollback is required, remove `NONGA_ADMIN_API_TOKEN` binding from staging service env/secret reference and disable/delete staging secret version according to platform policy.
- rollback actions remain staging-only and must still require owner approval if real runtime mutation is involved.

## 9) v19.36 execution guard

- no deploy/no one-run in v19.36 unless separately and explicitly approved by owner.
- no runtime config mutation in this packet because fresh owner approval wording is not embedded as an approved execution action.

## 10) Baseline continuity

- v19.34 PASS server-side staging secret path review confirmed.
- v19.35 PASS server-side staging secret readiness packet confirmed.
- owner-local token loop deprecated for future runtime.
- one-run attempts remain `0`.

## 11) Current 5-step status

- Step 1 still not completed until controlled owner-only staging trial PASS
- Step 2 not started
- Step 3 not started
- Step 4 not started
- Step 5 not started
