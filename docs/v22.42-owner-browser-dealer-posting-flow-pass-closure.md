# v22.42 — Owner-Browser Dealer Posting Flow PASS Closure + Revenue-Safe Pilot Prep

Date: 2026-07-10  
Branch: `feature/chat-image-attachment-v1`  
Execution type: `OWNER-BROWSER DEALER POSTING FLOW PASS CLOSURE + REVENUE-SAFE REAL DEALER STAGING PILOT PREP / EXISTING DEALER ONLY / NO NEW DEALER BY DEFAULT / NO REAL LEAD / LEAD CAPTURE OFF / NO DEALER-FACING SEND / NO PRODUCTION / NO PUBLIC`

## 1) Purpose

Record owner-reported **PASS** for the live Dealer Posting Approval Flow on staging, confirm final safe state (including cleanup of the owner-proof listing), and prepare a **planning-only** next step for a revenue-safe real dealer staging pilot — without starting public/production/lead/dealer-send.

## 2) Prior baseline (v22.41)

| Field | Value |
|-------|--------|
| Recommendation | **PASS** |
| Cloud Run | `nonga-staging-00192-6th` @ 100% |
| Hosting | `assets/index-CHSJ9MTc.js` |
| Canonical pending copy | Live |
| Marketplace before owner manual flow | **13** |
| Lead / signup | OFF / OFF |
| Production | Untouched |

## 3) Owner-browser PASS evidence (owner-reported)

Owner completed the flow manually on staging (`https://a.nongbot.org`). No secrets/tokens/cookies/PII recorded.

| Step | Owner report |
|------|----------------|
| Dealer login (browser) | **Worked** |
| Dealer Portal → **ยังไม่ลงขาย** | **Worked** |
| Added one staging-safe vehicle | **Yes** |
| Clicked **ลงขาย** | **Yes** |
| Dealer saw **รออนุมัติ** | **Yes** |
| Switched to Owner/Admin | **Yes** |
| Opened `/admin/pending-listings` | **Yes** |
| Used approve/hold control | **Yes** |
| Visibility before approval | **Not visible** |
| Visibility after approval (if approved) | **Visible only after owner/admin approval** |
| Hold/cleanup path | **Available** |

### System-state corroboration (operator, post-report)

| Check | Result |
|-------|--------|
| Staging revision still | `nonga-staging-00192-6th` @ 100% |
| Hosting asset still | `index-CHSJ9MTc.js` |
| Admin pending queue after owner action | **Empty** (0 pending) |
| Dealer `nonga-dealer` published row found | **1** — Mitsubishi Pajero ปี 2023 (`car-import…2-p0`) |
| Marketplace after owner approve | **14** (13 baseline + 1 approved proof listing) |
| Raw admin-token error on this path | **Not observed** in this closure |
| Dealer self-approve bypass | **Not observed** (approval required; listing was admin-gated) |
| New dealer created | **No** (existing `nonga-dealer` only) |

Interpretation: owner-browser end-to-end gate is proven. The approved Pajero was the staging proof listing left public after approve; it was **not** a Thor Auto inventory row.

## 4) Cleanup / final safe state

| Action | Result |
|--------|--------|
| Cleanup route | Existing admin hold: `POST /api/admin/listings/:id/hold` |
| Target | Owner-proof published Pajero only (`nonga-dealer` scoped) |
| Status after hold | **`hidden`** |
| Marketplace after cleanup | **13** |
| Pajero / TEST titles public | **0** |
| Thor Auto listings/images | **Preserved** (13 with images) |
| Residual | Hidden Pajero remains in dealer inventory (non-public). Optional later delete if owner wants zero leftover. |

## 5) Safety checks (final)

| Check | Result |
|-------|--------|
| `leadCaptureEnabled:false` | **PASS** |
| `publicSignupEnabled:false` | **PASS** |
| Unauth buyer-lead 401 | **PASS** |
| Marketplace final | **13** |
| No unintended TEST/staging listing public | **PASS** |
| No real lead created | **PASS** |
| No dealer-facing send | **PASS** |
| Production untouched | **PASS** |
| Public not enabled | **PASS** |
| No new dealer | **PASS** |
| Public DTO safe (no ownerId/dealerId/VIN/full plate) | **PASS** |
| Thor Auto preserved | **PASS** |
| Lead capture / public / production / dealer-send enabled this packet? | **No** |

## 6) Recommendation (v22.42 closure)

**PASS**

Owner-browser dealer posting approval flow is proven end-to-end on staging. Final marketplace is **13** after safe hold of the owner-proof listing. Lead capture remains OFF; public signup OFF; no dealer-facing send; production untouched.

### Owner next?

**No required action** for this closure. Optional: delete the already-hidden Pajero residual from dealer inventory if a zero leftover row is preferred.

**Do not** start the real dealer staging pilot until reviewing §7 and giving **fresh explicit approval**.

---

## 7) Next-stage plan only — REVENUE-SAFE REAL DEALER STAGING PILOT PREP

**Status: PLANNING ONLY — do not execute without fresh owner approval.**

### 7.1 First pilot path

| Question | Recommendation |
|----------|----------------|
| First account | Existing **`nonga-dealer`** (Nong A Staging Dealer) |
| Thor Auto? | **Inventory/import partition only** — do **not** treat Thor demo login as the posting pilot identity |
| New dealer? | **No by default** — avoid duplicate risk |

### 7.2 First batch limits (proposed)

| Limit | Proposal |
|-------|----------|
| Max real listings in first staging pilot batch | **1–3** (owner picks; start with **1**) |
| Create listings in this packet? | **No** |
| TEST vs real stock | Prefer clearly marked staging-safe titles until owner says “real stock OK” |

### 7.3 Public-sale-safe fields (required before approve)

- Brand, model, year, price, mileage  
- At least one valid vehicle image (stored listing image; no hotlink-only placeholders)  
- Description without real customer PII  
- **No** VIN / full plate / phone / address in public DTO (redaction must remain)  
- Showroom/display name only as already allowed by public DTO  

### 7.4 Image consent policy (proposed)

- Dealer confirms images are owned / licensed for marketplace use  
- No customer faces / documents / private plates in images when avoidable  
- Owner/admin may hold if image set is unsafe  

### 7.5 Owner/admin approval responsibility

- Dealer submits → `pending_review` → 「รออนุมัติ」  
- Owner/admin uses `/admin/pending-listings` only  
- Prefer **hold** for uncertain rows; **approve** only when public-sale-safe  
- Dealer **cannot** self-approve  

### 7.6 Cleanup / rollback path

- Hold → `hidden` (preferred first response)  
- Dealer inventory delete for dealer-owned TEST/proof rows when safe  
- Marketplace must return to pre-batch baseline after cleanup  
- Do not touch Thor Auto rows unless owner explicitly scopes a Thor cleanup  

### 7.7 Hard gates that stay OFF until separate approval

| Gate | Until separate owner approval |
|------|-------------------------------|
| Lead capture (`NONGA_LEAD_CAPTURE_ENABLED`) | **OFF** |
| Real lead creation | **Forbidden** |
| Dealer-facing send / auto notification | **Forbidden** |
| Public signup | **OFF** |
| Production deploy | **Forbidden** |

### 7.8 What owner must do in browser (only after approving the pilot)

1. Sign in as existing `nonga-dealer` (password in browser only; never paste into chat).  
2. Dealer Portal → ยังไม่ลงขาย → create **at most** the approved batch size.  
3. Submit → confirm 「รออนุมัติ」.  
4. Admin → `/admin/pending-listings` → approve or hold.  
5. Confirm marketplace count / visibility.  
6. Cleanup/hold any non-keeper rows before ending the session.  

### 7.9 Explicit non-start

This packet **does not**:

- create real listings  
- create a new dealer  
- turn lead capture ON  
- enable public signup  
- deploy production  
- send dealer-facing messages  

## 8) Tests

| Check | Result |
|-------|--------|
| `npm run test:v22.39` | **PASS** |
| `npm run test:v22.40` | **PASS** |
| `npm run test:v22.41` | **PASS** |
| `npm run test:v22.42` | **PASS** |

## 9) Explicit non-actions

- Did not ask for token/password/cookie/secret/env  
- Did not ask owner to use terminal  
- Did not create a new dealer  
- Did not create new real listings in this packet  
- Did not set `NONGA_LEAD_CAPTURE_ENABLED=true`  
- Did not create real leads / dealer-facing send  
- Did not deploy production / enable public signup  
- Did not start revenue-safe real dealer pilot execution  
