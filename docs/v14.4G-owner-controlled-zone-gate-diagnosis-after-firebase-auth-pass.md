# v14.4G — Owner Controlled Zone Gate Diagnosis After Firebase Auth Pass

Date: 2026-07-05  
Branch: `feature/chat-image-attachment-v1`

## Scope (This Round)

- Execution type: static/read-only diagnosis + safe docs/tests/wrapper context patch only.
- Agent execution boundary:
  - one-run execution by agent: no
  - retry by agent: no
  - second run by agent: no
  - live endpoint call by agent: no
  - provider/Gemini/runtime call by agent: no
  - deploy/runtime config mutation/production/public release by agent: no

## Owner v14.3AK Result Interpretation

Observed sanitized owner result confirms:

- Firebase auth dispatch reached route: `dispatchHttpStatus=200`
- response schema parsed (`success.data{...}`)
- fail occurred at guarded runtime evidence gate in wrapper, not at auth transport

Observed missing evidence summary:

- `dispatchEvidenceGateReason=owner_controlled_zone_not_allowed`
- `dispatchEvidenceMissingFields=skipGemini!=false,providerNetwork!=true,gateReason!=real_provider_call_ok`

## Exact Source of `owner_controlled_zone_not_allowed`

File: `src/services/ai/salesBrainUserVisibleRealProvider.ts`  
Function: `maybeApplyUserVisibleRealProvider()`

Gate branch:

- `const ownerControlledZone = detectOwnerControlledGeminiUxZone(input.userMessage);`
- `if (!ownerControlledZone) { ... realProviderGateReason: "owner_controlled_zone_not_allowed" }`

Pass condition:

- `detectOwnerControlledGeminiUxZone(...)` must return a non-null allowed zone (`compare_car_types`, `car_fit_reason`, etc.)

## Route / Evidence Payload Trace

Route: `/api/ai/chat-user-visible-orchestrate`  
Handler: `handleChatUserVisibleOrchestratePost()` in `src/services/ai/salesBrainServerUserVisibleOrchestrationBridge.ts`

Payload flow:

1. Build base payload via `buildRedactedPayload(...)`
2. Call `maybeApplyUserVisibleRealProvider(...)`
3. Attach `userVisibleGateDiagnostic` and `userVisibleRuntimeDiagnostic`
4. Return `res.json({ success: true, data: { ... } })`

Where guarded fields come from:

- `skipGemini`: initial bridge payload (`buildRedactedPayload`) and only flips to `false` on real provider success branch
- `realProviderNetwork` / `providerNetwork`: set in `maybeApplyUserVisibleRealProvider(...)`
- `realProviderGateReason` / `gateReason`: set in `maybeApplyUserVisibleRealProvider(...)`
- `pilotPathActive`, `fallbackToLegacy`: from bridge wiring path (`wireShadowChatPathWithPilot`)

## Root Cause

Wrapper request context mismatch:

- Wrapper synthetic message before this patch:
  - `ลูกค้าทดลองถามแบบไม่มีข้อมูลจริง: ช่วยสรุปจุดเด่นของคันที่ 1 และ 2 แบบสุภาพสำหรับครอบครัวหน่อยครับ`
- This message can still activate pilot follow-up path, but does not match any owner-controlled zone regex in `detectOwnerControlledGeminiUxZone(...)`.
- Result: real provider call is blocked before Gemini call:
  - `realProviderNetwork=false`
  - `realProviderGateReason=owner_controlled_zone_not_allowed`
  - `skipGemini` remains prior bridge value (typically `true` in this wrapper route shape)

## Safe Patch Applied (No Guard Weakening)

Patched wrapper synthetic dispatch message to a zone-allowed compare phrase:

- `ลูกค้าทดลองถามแบบไม่มีข้อมูลจริง: ช่วยเปรียบเทียบคันที่ 1 กับ 2 แบบสุภาพสำหรับครอบครัวหน่อยครับ`

Why this is safe:

- does not bypass owner-controlled gate
- does not force provider flags
- does not fake runtime evidence
- only aligns wrapper synthetic context with documented allowed owner zone

## v14.4F Change Impact Check

`v14.4F` wrapper change in commit `ffb9d2fcdf296a7907f1be5f5621a71c78ba7d4a` changed only dispatch approval phrase:

- from `v14.3AJ` to `v14.3AK`
- no owner-controlled-zone prompt/context update in that commit

## `v14.3AK` Consumption Status

Repository-local lock file exists and is consumed:

- `.nonga-owner-local-one-run-v143ak-user-visible-dispatch.lock.json`
- `consumed=true`
- `reason=user_visible_firebase_dispatch_entry_started`
- `v14.3AK` must not be retried or reused in any follow-up step

If a later owner-approved one-run is prepared after this patch is committed/pushed and repo is clean,
the next run namespace must advance to `v14.3AL` (new one-run id, no retry).

## Decision For This Round

`NEED REVIEW — OWNER CONTROLLED ZONE CONTEXT MISSING FROM WRAPPER`

Patch is now prepared to address that static context issue without reducing any safety gates.
