# v13.15F Pilot-Path Runtime Context Patch (Prepare Only / Static-Test Only)

## 1) Execution type

- prepare-only runtime-context patch: yes
- static-test-only: yes
- no Gemini execution: yes
- no provider network call: yes
- no runtime config change: yes
- no deploy: yes
- no production: yes
- no public route activation: yes
- no buyer-facing release: yes
- no real lead sending: yes
- no real PII: yes
- no phone/plate/VIN: yes
- no secret/API key/token exposure: yes
- no automatic retry: yes
- no second run: yes

## 2) Baseline local/origin ก่อนเริ่ม

- branch: `feature/chat-image-attachment-v1`
- local HEAD before patch: `8136523ba63d67838b92578e3772f30d511d1f6c`
- origin HEAD before patch: `8136523ba63d67838b92578e3772f30d511d1f6c`
- local = origin before patch: yes

## 3) Files changed

- `docs/v13.15F-pilot-path-runtime-context-prequalification-record.md`
- `scripts/test-v1315f-pilot-path-runtime-context-prequalification.mts`
- `package.json`

## 4) Root cause addressed

From v13.15E, runtime `pilotPathActive=false` occurred when request/session/orchestration shape lacked pilot activators (especially grounded `pilotSessionContext.recentCarCards` and follow-up-classified message path).

v13.15F addresses this by adding a runtime-shaped synthetic prequalification harness that:

1. pre-seeds synthetic card context through sanitization path
2. uses a synthetic safe-zone follow-up prompt that matches `isPilotBuyerFollowUpMessage()`
3. executes server bridge path without provider dispatch
4. asserts `pilotPathActive=true` for the runtime-shaped prequalification step

## 5) Runtime-shaped prequalification harness

Harness path used in test:

1. `sanitizePilotSessionContext()` on synthetic raw context
2. `isPilotBuyerFollowUpMessage()` on synthetic safe-zone follow-up prompt
3. `runUserVisibleOrchestrationBridge()` with:
   - staging environment
   - synthetic allowlisted UID
   - sanitized synthetic `pilotSessionContext`
4. assertion target:
   - `payload.pilotPathActive === true`
   - `payload.fallbackToLegacy === false`

No provider call is needed for this prequalification proof.

## 6) Synthetic `recentCarCards` evidence

Synthetic card payload is explicitly sanitized in test:

- brand/model/year/price only from synthetic values
- no real seller/customer identity fields
- no phone/email/VIN/plate in cards
- long description is truncated by sanitizer boundary
- card shape passed via `sanitizePilotSessionContext()` before bridge call

## 7) Synthetic follow-up safe-zone prompt evidence

Synthetic prompt used for prequalification:

`ช่วยอธิบายแบบเป็นธรรมชาติว่ารถแนวนี้เหมาะกับใคร และควรถามอะไรต่อดี`

Evidence asserted:

- recognized by `isPilotBuyerFollowUpMessage() === true`
- safe-zone wording objective only
- no legal/finance guarantee claim requirement
- no request for contact/lead data

## 8) `pilotPathActive=true` evidence

Runtime-shaped bridge result with sanitized context + safe follow-up prompt:

- `payload.pilotPathActive === true`
- `payload.fallbackToLegacy === false`
- prequalification success is achieved without provider dispatch

## 9) Provider/Gemini call count evidence

Test injects a Gemini caller trap (`setUserVisibleGeminiCallerForTests`) and checks:

- Gemini call count = `0`
- provider network call count = `0`

Additionally, missing-context branch is exercised and gated at `pilot_path_inactive` before provider stage.

## 10) No lead / no PII / no phone-plate-VIN evidence

Test assertions enforce:

- no lead submit behavior in harness
- prompt/cards are synthetic only
- no phone/plate/VIN patterns in synthetic context
- no secret/API key/token literal leakage in doc

## 11) No public / no production / no buyer-facing release evidence

- all checks run in local static test context only
- no route activation/deploy command
- no production/public toggle mutation
- no buyer-facing release action

## 12) Runtime config mutation evidence

Harness captures env snapshot before/after and verifies unchanged:

- no runtime flag mutation in test process map
- no external runtime config write path

## 13) Fallback behavior when context missing

Test includes missing-context priming branch and confirms:

- `pilotPathActive === false`
- `fallbackToLegacy === true`
- `realProviderGateReason === "pilot_path_inactive"` on gate evaluation path
- fallback remains deterministic and safe

## 14) Test results

- `npm run test:v13.15D` -> pass
- `npm run test:v13.15E` -> pass
- `npm run test:v13.15F` -> pass

## 15) Commit hash full/short

- full: (filled after commit)
- short: (filled after commit)

## 16) Push result

- (filled after push)

## 17) Local/origin sync result

- (filled after push verification)

## 18) Final recommendation

READY FOR OWNER APPROVAL TO EXECUTE v13.15G OWNER-ONLY GEMINI UX ONE-RUN WITH PILOT-PATH CONTEXT
