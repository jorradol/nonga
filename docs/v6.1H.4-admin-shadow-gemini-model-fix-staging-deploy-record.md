# v6.1H.4 — Admin Shadow Gemini Model Fix Staging Deploy Record

**สถานะ:** Execution record — **Cloud Run deployed, manual admin SS-01 real Gemini PASS, Hosting skipped, user-visible chat unchanged**  
**วันที่:** 2026-06-08  
**Git baseline (pre-slice):** `23b6d7a` (v6.1H.3)  
**Git commit (slice):** `f878972d10981b9162447c32497d463011f8215d`  
**Branch:** `feature/chat-image-attachment-v1`  
**อ้างอิง:** v6.1H (admin-only real Gemini path), v6.1H.3 (error diagnostics + `gemini_model_not_found`), v6.1G (dry-run readiness), v6.1F (manual smoke checklist)

> **v6.1H.4 แก้ model selection** — เปลี่ยน admin shadow real provider จาก `gemini-2.0-flash` (404 NOT_FOUND) เป็น `gemini-3.5-flash` (align กับ `server.ts` routes ที่ใช้งานได้) โดย **`NONGA_AI_USER_VISIBLE_ENABLED=false`**, **public chat ไม่เปลี่ยน**, **SS-02..SS-08 ยัง mock only**

---

## สารบัญ

1. [Vision & Execution Summary](#1-vision--execution-summary)
2. [Root Cause](#2-root-cause)
3. [Preflight (Before Deploy)](#3-preflight-before-deploy)
4. [Tests / Build Before Deploy](#4-tests--build-before-deploy)
5. [Runtime Changes (v6.1H.4)](#5-runtime-changes-v61h4)
6. [Cloud Build (Executed)](#6-cloud-build-executed)
7. [Cloud Run Deploy (Image-only)](#7-cloud-run-deploy-image-only)
8. [Env Flags (Unchanged)](#8-env-flags-unchanged)
9. [Post-deploy API Smoke](#9-post-deploy-api-smoke)
10. [Manual Admin SS-01 Smoke (PASS)](#10-manual-admin-ss-01-smoke-pass)
11. [Public Chat (Unchanged)](#11-public-chat-unchanged)
12. [Budget / Cost Observation](#12-budget--cost-observation)
13. [Rollback Plan](#13-rollback-plan)
14. [Compliance (v6.1H.4)](#14-compliance-v61h4)
15. [Next Steps](#15-next-steps)

---

## 1. Vision & Execution Summary

### เป้าหมาย v6.1H.4

แก้ admin-only real Gemini shadow call บน staging ให้เรียก model ที่มีอยู่จริงใน project `nonga-ce93c` โดย:

- เปลี่ยนเฉพาะ **admin shadow SS-01 real provider path**
- คง **`NONGA_AI_USER_VISIBLE_ENABLED=false`**
- คง **SS-02..SS-08 mock only** และ **public `/chat` legacy**
- **ไม่** `--update-env-vars` (ยกเว้นจำเป็นต้องเพิ่ม model env — ไม่จำเป็นใน slice นี้)
- **ไม่** Hosting deploy (UI ไม่เปลี่ยน — `geminiModel` มาจาก API)

### Execution outcome

| Area | Result |
|------|--------|
| Root cause confirmed | ✅ `gemini-2.0-flash` → 404 NOT_FOUND |
| Model fix | ✅ `gemini-3.5-flash` (align `server.ts`) |
| Preflight | ✅ branch `feature/chat-image-attachment-v1`, baseline `23b6d7a` |
| Tests / build | ✅ v61h, v61b, v61g, lint, build:staging:hosting — **PASS** |
| Cloud Build | ✅ **SUCCESS** — `02b188bc-3bad-4b2d-b6b5-e4130503d362` |
| Cloud Run deploy | ✅ rev **`nonga-staging-00057-bq2`**, traffic **100%** |
| Env flags | ✅ unchanged — no `--update-env-vars` |
| Hosting deploy | ❌ **SKIPPED** — UI bundle unchanged |
| Post-deploy API smoke | ✅ health 200, `publicSignupEnabled:false`, unauth admin 401 |
| Manual admin SS-01 | ✅ **PASS** — `providerNetwork:true`, `provider:gemini`, `real_provider_call_ok` |
| Public chat | ✅ unchanged by design — legacy path only |
| Rollback | ❌ **not needed** — manual smoke passed |
| User-visible AI | ❌ unchanged — `NONGA_AI_USER_VISIBLE_ENABLED=false` |
| Real stock import | ❌ still blocked |

---

## 2. Root Cause

| Item | Detail |
|------|--------|
| Symptom (v6.1H.3 manual) | `fallbackReason:gemini_model_not_found`, `geminiHttpStatus:404`, `geminiErrorCode:NOT_FOUND` |
| Configured model | `gemini-2.0-flash` |
| Gates passed | `flagEnabled:true`, `geminiKeyPresent:true`, `shadowAllowed:true`, `caseAllowed:true` |
| Real provider path | ✅ reached — `realProviderGateReason:real_provider_call_failed` (model lookup only) |
| Working reference | `server.ts` Gemini routes use **`gemini-3.5-flash`** successfully |
| Conclusion | Model name retired/unavailable on Generative Language API for this project — not auth/key/API enable issue |

---

## 3. Preflight (Before Deploy)

| Check | Value |
|-------|-------|
| **git status** | clean @ `f878972` (after slice commit) |
| **branch** | `feature/chat-image-attachment-v1` |
| **baseline (pre-slice)** | `23b6d7a` (v6.1H.3) |
| **HEAD (slice)** | `f878972d10981b9162447c32497d463011f8215d` |
| **Firebase project** | `nonga-ce93c` |
| **Cloud Run service** | `nonga-staging` |
| **Region** | `asia-southeast1` |
| **Previous revision** | `nonga-staging-00056-k48` @ **100%** |
| **Previous image** | `v6.1H.3-admin-shadow-gemini-error-diagnostics` |
| **Public signup** | **`false`** |
| **`NONGA_AI_USER_VISIBLE_ENABLED`** | **`false`** |
| **`NONGA_AI_ADMIN_SHADOW_REAL_PROVIDER_ENABLED`** | **`true`** |
| **Budget daily / monthly** | `5` / `50` |
| **`GEMINI_API_KEY` ref** | `gemini-api-key:latest` (metadata only) |

**Did not run** `gcloud secrets versions access` — no secret values in this record.

---

## 4. Tests / Build Before Deploy

| Command | Result |
|---------|--------|
| `npm run test:v61h-real-gemini-shadow-call-admin-only-staging-smoke` | **PASS** |
| `npm run test:v61b-server-side-shadow-evaluation-admin-debug-user-visible-off` | **PASS** |
| `npm run test:v61g-real-gemini-shadow-call-dry-run-readiness` | **PASS** |
| `npm run lint` | **PASS** |
| `npm run build:staging:hosting` | **PASS** |

---

## 5. Runtime Changes (v6.1H.4)

| File | Change |
|------|--------|
| `salesBrainAdminShadowRealProvider.ts` | `ADMIN_SHADOW_GEMINI_MODEL` → **`gemini-3.5-flash`** |
| `salesBrainAdminShadowDiagnostics.ts` | `ADMIN_SHADOW_SMOKE_SLICE_ID` → **`v6.1H.4`** |
| `scripts/test-v61h-real-gemini-shadow-call-admin-only-staging-smoke.mts` | model assertions + 404 regression |

**Unchanged:**

| Area | Status |
|------|--------|
| Public chat path | legacy orchestrator only |
| SS-02..SS-08 | mock provider only |
| `userVisibleResponse` | legacy reference only |
| Admin route auth | admin/superadmin only |
| Output redaction | enforced |

---

## 6. Cloud Build (Executed)

```bash
gcloud builds submit --project=nonga-ce93c \
  --config=cloudbuild.v53f.yaml \
  --substitutions=_IMAGE_URI=asia-southeast1-docker.pkg.dev/nonga-ce93c/nonga-staging/nonga-staging:v6.1H.4-admin-shadow-gemini-model-fix,...
```

| Field | Value |
|-------|-------|
| **Cloud Build ID** | `02b188bc-3bad-4b2d-b6b5-e4130503d362` |
| **Status** | **SUCCESS** (~2m11s) |
| **Image tag** | `v6.1H.4-admin-shadow-gemini-model-fix` |

---

## 7. Cloud Run Deploy (Image-only)

```bash
gcloud run services update nonga-staging \
  --project=nonga-ce93c --region=asia-southeast1 \
  --image=asia-southeast1-docker.pkg.dev/nonga-ce93c/nonga-staging/nonga-staging:v6.1H.4-admin-shadow-gemini-model-fix
```

| Field | Value |
|-------|-------|
| **Previous revision** | `nonga-staging-00056-k48` |
| **New revision** | **`nonga-staging-00057-bq2`** @ **100%** |
| **Deploy type** | image-only — **no env update** |
| **Service URL** | `https://nonga-staging-265743710806.asia-southeast1.run.app` |
| **Primary URL (Hosting rewrite)** | `https://a.nongbot.org` / `https://nonga-ce93c.web.app` |

---

## 8. Env Flags (Unchanged)

| Flag | Value on `nonga-staging-00057-bq2` |
|------|-----------------------------------|
| `NONGA_AI_ADMIN_SHADOW_REAL_PROVIDER_ENABLED` | **`true`** |
| `NONGA_AI_USER_VISIBLE_ENABLED` | **`false`** |
| `NONGA_AI_PROVIDER` | `gemini` |
| `NONGA_AI_SHADOW_MODE_ENABLED` | `true` |
| `NONGA_AI_EMERGENCY_KILL_SWITCH` | `false` |
| `NONGA_AI_BUDGET_DAILY_LIMIT` | `5` |
| `NONGA_AI_BUDGET_MONTHLY_LIMIT` | `50` |
| `VITE_NONGA_PUBLIC_SIGNUP_ENABLED` | `false` |
| `GEMINI_API_KEY` | secret ref `gemini-api-key:latest` (unchanged) |

**Did not run** `gcloud run services update --update-env-vars` in this slice.

---

## 9. Post-deploy API Smoke

| Endpoint | HTTP | Key fields |
|----------|------|------------|
| `GET https://a.nongbot.org/api/health` | **200** | `ok:true`, `publicSignupEnabled:false` |
| `POST /api/admin/sales-brain-shadow-smoke` (unauth) | **401** | auth required |

---

## 10. Manual Admin SS-01 Smoke (PASS)

**URL:** `https://a.nongbot.org/admin/shadow-smoke`  
**Actor:** admin/superadmin (ลุง) — manual screenshot confirmed **PASS**

| Field | Observed |
|-------|----------|
| `success` | **`true`** |
| `readOnly` | **`true`** |
| `userVisibleOff` | **`true`** |
| `providerNetwork` | **`true`** |
| `provider` | **`gemini`** |
| `realProviderGateReason` | **`real_provider_call_ok`** |
| `sliceId` | **`v6.1H.4`** |
| `geminiKeyPresent` | **`true`** |
| `geminiModel` | **`gemini-3.5-flash`** |
| `caseId` | **`SS-01`** |

**Conclusion:** Admin-only real Gemini shadow call on staging **verified** — first successful paid provider call in shadow pilot.

---

## 11. Public Chat (Unchanged)

| Check | Result |
|-------|--------|
| `/chat` `"เริ่มใหม่"` legacy exact | **unchanged by design** — no user-visible path change in v6.1H.4 |
| AI/Gemini to public user | ❌ **none** — `NONGA_AI_USER_VISIBLE_ENABLED=false` |

Expected legacy response (unchanged):

> ยกเลิกข้อมูลเดิมแล้วครับ พิมพ์ข้อมูลรถคันใหม่ที่ต้องการลงขายได้เลยครับ

---

## 12. Budget / Cost Observation

| Item | Status |
|------|--------|
| Budget caps | daily `5`, monthly `50` |
| Paid Gemini call | ✅ **one SS-01 admin call observed** (manual smoke) |
| Cost anomaly | ❌ **none observed** |
| Provider output to user | ❌ **none** — redacted admin debug only |

---

## 13. Rollback Plan

```bash
gcloud run services update nonga-staging \
  --project=nonga-ce93c --region=asia-southeast1 \
  --image=asia-southeast1-docker.pkg.dev/nonga-ce93c/nonga-staging/nonga-staging:v6.1H.3-admin-shadow-gemini-error-diagnostics
```

Emergency env rollback (if needed):

```bash
gcloud run services update nonga-staging \
  --project=nonga-ce93c --region=asia-southeast1 \
  --update-env-vars="NONGA_AI_ADMIN_SHADOW_REAL_PROVIDER_ENABLED=false,NONGA_AI_USER_VISIBLE_ENABLED=false,NONGA_AI_EMERGENCY_KILL_SWITCH=true"
```

| Item | Status |
|------|--------|
| Rollback needed | ❌ **not needed** — manual SS-01 PASS |

---

## 14. Compliance (v6.1H.4)

| Item | Status |
|------|--------|
| `NONGA_AI_USER_VISIBLE_ENABLED=true` | ❌ not set — remains **false** |
| AI response to public user | ❌ not changed |
| Production | ❌ not touched |
| Firestore rules deploy | ❌ not done |
| Hosting deploy | ❌ skipped (UI unchanged) |
| `gcloud run services update --update-env-vars` | ❌ not done |
| Payment / invoice / settlement writes | ❌ not done |
| Buyer lead / seller reveal / outcome | ❌ not done |
| Public signup | ❌ not enabled |
| Real stock import | ❌ still blocked |
| `gcloud secrets versions access` | ❌ not done |
| Raw prompt / PII / secret in logs | ❌ redaction enforced |

---

## 15. Next Steps

1. ✅ v6.1H.4 manual admin SS-01 PASS — **real Gemini admin shadow verified**
2. Proceed to **v6.1I** — staging AI conversation smoke readiness (docs/tests) — see `docs/v6.1I-staging-ai-conversation-smoke-readiness.md`
3. Do **not** enable `NONGA_AI_USER_VISIBLE_ENABLED=true` without separate approval gate (v6.1I §6)
4. Real stock import **still blocked** until conversation smoke gates pass
