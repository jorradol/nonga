# v13.13 Owner-Only Staging Config / Deploy Plan

## 1) Execution type

prepare-only: yes  
docs/test/package only: yes  
staging config/deploy plan: yes  
no runtime behavior change: yes  
no deploy: yes  
no runtime config change: yes  
no Gemini execution: yes  
no smoke endpoint call: yes  
no production: yes  
no public route activation: yes  
no buyer-facing release: yes  
no real lead sending: yes  
no real PII: yes  
no automatic Gemini retry: yes

## 2) Baseline

- latest commit: `676c2d4`
- v13.12 accepted
- owner-only controlled Gemini UX patch exists
- default-off exists
- guard-before-dispatch exists
- post-check/fallback exists
- no automatic retry exists

## 3) Required staging env/config checklist for future v13.14

Checklist only (no runtime config is changed in v13.13):

- Verify owner-only UX gate flag presence: `NONGA_AI_OWNER_ONLY_CONTROLLED_UX_ENABLED` (default false behavior confirmed before any future activation).
- Verify owner/admin allowlist or session-gate policy for controlled path (owner/admin only).
- Verify existing Gemini/admin provider gate flags align with owner-only path requirements (no buyer-facing/public widening).
- Explicit checkpoint label: Gemini/admin provider flags.
- Verify staging-only environment guard remains enforced.
- Verify budget/cost cap flags are present and non-zero for controlled route.
- Verify no-retry behavior remains enforced (single attempt only, deterministic fallback on fail).
- Verify deterministic fallback route remains available and active when gate or post-check fails.
- Explicit checkpoint label: fallback deterministic confirmation.

Security note: no secret value, API key, token, or real credential is included in this document.

## 4) Future deploy plan (plan-only, sanitized placeholders)

### 4.1 Pre-deploy checks

- Confirm git HEAD equals approved release commit.
- Confirm working tree clean and test suite approved for v13.14 run.
- Confirm explicit owner approval record exists for activation step.
- Confirm staging-only target and no production target in command plan.

### 4.2 Hosting/Cloud Run scope decision

- Decide controlled deploy surface: hosting only / Cloud Run only / coordinated both.
- Keep public/buyer routes unchanged; activate only owner-controlled path gates.

### 4.3 Config verification (before deploy command)

- Validate runtime flags in masked form (status only, no secret values).
- Validate owner/admin gate diagnostics expected for staging.
- Validate no-retry and deterministic fallback policy remains true.

### 4.4 Deploy command placeholders (sanitized)

- Deploy command placeholder (sanitized): `[DEPLOY_COMMAND_PLACEHOLDER_SANITIZED_STAGING_ONLY]`
- Config apply placeholder (sanitized): `[CONFIG_APPLY_PLACEHOLDER_SANITIZED]`
- Verification command placeholder (sanitized): `[POST_DEPLOY_VERIFY_PLACEHOLDER_SANITIZED]`

No deploy command is executed in v13.13.

### 4.5 Post-deploy verification plan

- Check owner-only gate passes for owner/admin synthetic session.
- Check non-owner is blocked.
- Check safe-zone wording candidate route behavior.
- Check deterministic fallback on blocked/unsafe output.

### 4.6 Rollback command placeholders (sanitized)

- Rollback placeholder (sanitized): `[ROLLBACK_COMMAND_PLACEHOLDER_SANITIZED]`
- Flag reset placeholder (sanitized): `[FLAG_RESET_PLACEHOLDER_SANITIZED]`

## 5) Future owner-only smoke plan (synthetic only)

- owner role only
- non-owner blocked
- default-off blocked
- safe-zone wording candidate
- unsafe-zone blocked
- PII/phone/plate/VIN blocked
- lead confirmation deterministic
- secret request blocked
- production/public request blocked
- post-check fallback
- no retry evidence

No smoke endpoint call is executed in v13.13.

## 6) Evidence collection plan

For v13.14 evidence packet, collect:

- git HEAD
- runtime flag masked status
- owner-only gate result
- safe-zone result
- boundary block result
- fallback result
- no retry proof
- no lead proof
- no PII proof
- no public/production proof

## 7) Rollback plan

Rollback strategy (future execution round only):

- Disable owner-only flag.
- Force deterministic fallback path.
- Do not alter lead flow behavior.
- Do not alter public route behavior.
- Do not alter production route behavior.
- No secret deletion is required.

## 8) Risk table

| Risk | Impact | Mitigation |
| --- | --- | --- |
| wrong config | owner path misbehavior | pre-deploy checklist + masked config verification |
| owner gate bypass | unauthorized access | strict owner/admin allowlist/session gate checks |
| public accidental exposure | buyer-facing leakage | keep public route unchanged + staging-only scope |
| Gemini execution outside approval | uncontrolled provider usage | explicit owner approval gate + runbook controls |
| secret exposure | credential leak | no secret values in docs/logs, masked-only evidence |
| PII leak | privacy violation | deterministic boundary block + redaction checks |
| lead sent unintentionally | lead integrity break | keep lead confirmation deterministic-only |
| retry/cost overrun | uncontrolled spend | enforce no retry + budget cap checks |
| unsafe output | user trust/safety risk | post-check + deterministic fallback |
| rollback failure | prolonged unsafe state | explicit rollback placeholders + flag-off-first recovery |

## 9) GO/HOLD criteria for v13.14

Proceed to v13.14 only when all are true:

- plan complete
- static test pass
- scope docs/test/package only
- no deploy happened
- no runtime config changed
- no Gemini execution happened
- owner approves explicitly

Else hold execution until criteria are fully met.

## 10) Final recommendation

READY FOR OWNER APPROVAL TO EXECUTE v13.14 OWNER-ONLY STAGING CONFIG + CONTROLLED DEPLOY
