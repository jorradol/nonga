# v8.2 — Gate B-C Evidence Collection Checklist Packet

> **CHECKLIST-PACKET-ONLY / DOCS-ONLY / STATIC VALIDATION.**
> เอกสารนี้เป็น packet สำหรับลุงเด่นใช้ตรวจความพร้อม Gate B/C ทีละข้อก่อน owner sign-off เท่านั้น
> รอบ v8.2 นี้ **ไม่เปิด Gemini จริง**, **ไม่ใส่ secret/API key จริง**, **ไม่ deploy**, **ไม่เปิด user-visible AI**, **ไม่เปิด buyer-facing AI**, **ไม่เปิด admin-only shadow จริง**, **ไม่เปิด public signup**, **ไม่ส่ง lead จริง**, **ไม่แก้ runtime**, **ไม่แตะ `src/`**

---

## 1. Executive Summary (สำหรับลุงเด่น)

v8.2 จัดทำ "Gate B-C Evidence Collection Checklist Packet" เพื่อให้ลุงเด่นใช้ตรวจความพร้อมและหลักฐานประกอบการตัดสินใจแบบ GO / HOLD / NO-GO ก่อนอนุมัติรอบถัดไปในอนาคต

สาระสำคัญของรอบนี้:

- checklist แยก Gate B และ Gate C แบบตรวจทีละข้อ
- แต่ละข้อมี `evidence required`, `acceptable evidence`, `missing evidence`, `blocker condition`, `owner review status`
- ยืนยันขอบเขตความปลอดภัย: PDPA/privacy, lead/dealer isolation, deterministic authority, logging/redaction
- ระบุ known blockers ที่ถ้ายังไม่ครบ ห้ามเปิด admin-only Gemini shadow จริง
- เพิ่ม static validation เพื่อยืนยันว่า v8.2 ยังเป็น docs-only/checklist-only/no activation

## 2. Current Baseline ก่อนเริ่ม v8.2

- branch: `feature/chat-image-attachment-v1`
- HEAD (short): `34aae52`
- HEAD (full): `34aae52735dc1d46add35c8939525d172a2d196b`
- latest commit: `docs(ai): add v8.1A gate b-c evidence collection dry-run plan closure record`
- staging runtime: `92d0dac`
- deploy status: no deploy หลัง v8.1A
- production: ไม่ถูกแตะ
- env/secrets/API key: ไม่เปลี่ยน
- secret จริงใน repo: ไม่มี
- Gemini จริง: ยังปิด
- user-visible AI จริง: ยังปิด
- admin-only shadow จริง: ยังไม่ได้เปิด
- buyer-facing AI: ยังไม่ได้เปิด
- public signup: ยังปิด
- real lead sending: ยังปิด
- runtime/`src/`: ไม่มีการเปลี่ยนแปลง
- deterministic flow: ยังเป็น source of truth

## 3. Decision Purpose

- v8.2 คือ checklist packet สำหรับตรวจ readiness Gate B/C
- v8.2 ไม่ใช่การเปิด admin-only shadow จริง
- v8.2 ไม่ใช่การเปิด Gemini จริง
- v8.2 ไม่ใช่ activation milestone

## 4. Gate B Checklist — Secret Readiness / Boundary / Control

> Owner review status ที่ใช้ในทุกข้อ: `PASS` / `HOLD` / `FAIL` / `N/A`

### 4.1 Gate B Itemized Checklist

| Item | Evidence Required | Acceptable Evidence | Missing Evidence | Blocker Condition | Owner Review Status |
| --- | --- | --- | --- | --- | --- |
| B1. Secret readiness inventory | รายการ secret placeholder, owner, rotation window, audit cadence | sanitized inventory sheet (ไม่มีค่าจริง) + owner initials | ยังไม่มี secret inventory ที่ลงนาม | ไม่มี inventory ที่ traceable ต่อ owner | N/A |
| B2. No secret in repo proof | ผล static scan และยืนยันไม่เพิ่ม env/secrets/API key | validator output + git diff summary | ยังไม่แนบ output scan รอบล่าสุด | พบ pattern ที่เหมือน key/token จริงในเอกสาร/สคริปต์ | N/A |
| B3. Secret storage boundary | หลักฐานว่า secret อยู่ใน storage boundary แยกจาก repo | boundary diagram แบบ sanitized + policy excerpt | ยังไม่มี policy excerpt ที่ยืนยัน boundary | secret ถูกเก็บใน code repo หรือช่องทางที่ไม่อนุมัติ | N/A |
| B4. Access boundary | role matrix owner/admin/operator และ deny สำหรับ buyer/public | sanitized RBAC matrix + reviewer sign-off | ยังไม่มี matrix ฉบับอนุมัติ | buyer/public เข้าถึงขั้นเตรียม secret ได้ | N/A |
| B5. Failure/rollback readiness | playbook สำหรับ missing/invalid secret และ unauthorized access | rollback checklist + stop condition note | ยังไม่มี rollback rehearsal evidence | fail case แล้วไม่สามารถ revert deterministic-only ทันที | N/A |
| B6. Budget/quota readiness | daily cap, per-run cap, timeout cap, emergency stop, spending limit | sanitized cap sheet + owner-approved budget limit | ยังไม่มี owner-approved spending limit หลักฐานจริง | cap/limit ไม่ชัดเจนหรือ owner ไม่อนุมัติ | N/A |
| B7. Kill switch readiness | switch concept + verification evidence + stop condition | switch policy sheet + dry-run verification log (sanitized) | ยังไม่มี runtime proof ของ kill switch | kill switch verification ไม่ผ่านหรือพิสูจน์หยุด flow ไม่ได้ | N/A |

### 4.2 Gate B Detail Notes

- budget/quota ต้องระบุชัดเจนทั้ง `daily cap`, `per-run cap`, `timeout cap`, `emergency stop`, `owner-approved spending limit`
- kill switch ต้องมี stop condition ชัดเจน: ถ้า switch ไม่ผ่าน ห้ามเดินหน้ารอบเปิด shadow จริง

## 5. Gate C Checklist — Admin-only Dry-run Readiness / Exposure Guard

### 5.1 Gate C Itemized Checklist

| Item | Evidence Required | Acceptable Evidence | Missing Evidence | Blocker Condition | Owner Review Status |
| --- | --- | --- | --- | --- | --- |
| C1. Admin-only dry-run readiness | หลักฐานว่า dry-run scope จำกัดเฉพาะ admin/operator allowlist | allowlist policy (sanitized) + test matrix | ยังไม่มี allowlist proof จาก runtime | admin scope ไม่ชัดเจนหรือข้าม role boundary | N/A |
| C2. Admin-only boundary proof | หลักฐาน deny buyer/public ทุกทางที่เกี่ยวกับ shadow path | boundary test checklist + deny evidence | ยังไม่มี deny evidence ที่ตรวจครบ scenario | มี path ให้ buyer/public เห็น output จาก shadow | N/A |
| C3. No buyer-facing exposure proof | หลักฐานว่า buyer/public ยังได้ deterministic only | negative-path scenario records (sanitized) | ยังไม่มีหลักฐานครบทุก scenario | buyer-facing surface เห็น AI output หรือ metadata ภายใน | N/A |
| C4. Deterministic source-of-truth proof | search/lead/consent/queue/inventory ยัง deterministic authority | architecture assertion checklist + owner/dealer flow map | ยังไม่มีเอกสารยืนยัน authority ล่าสุด | AI สามารถ override deterministic authority ได้ | N/A |
| C5. Fallback deterministic proof | หลักฐาน fallback สำหรับ AI fail/timeout/blocked/unsafe/hallucination/policy uncertainty | fallback matrix + expected deterministic outcomes | ยังไม่มี matrix runtime evidence ครบกรณี | fallback ไม่ชัดเจนหรือไม่ deterministic-safe | N/A |
| C6. Logging/redaction proof | allowed sanitized logs และ forbidden logs ครบ | redaction guideline + sample sanitized logs | ยังไม่มีหลักฐาน enforce log-redaction runtime | มีการบันทึก phone/full plate/VIN/internal price/wholesale/raw owner/raw lead | N/A |

## 6. PDPA / Privacy Checklist

| Item | Evidence Required | Acceptable Evidence | Missing Evidence | Blocker Condition | Owner Review Status |
| --- | --- | --- | --- | --- | --- |
| P1. No PII sent to AI | policy ระบุ no PII-to-AI | sanitized policy + transport field map | ยังไม่มี runtime enforcement proof | พบ PII ถูกส่งไป AI | N/A |
| P2. No phone auto-fill | หลักฐานว่าไม่มี auto-fill เบอร์โทรฝั่ง buyer | UX guard checklist + test case | ยังไม่มี test evidence ล่าสุด | ระบบกรอกเบอร์แทน buyer | N/A |
| P3. Phone capture only at final confirmation | flow ยืนยันว่าลูกค้ากรอกเองเฉพาะ final confirmation | step-by-step flow proof (sanitized) | ยังไม่มี flow evidence รอบล่าสุด | เก็บเบอร์ก่อน final confirmation หรือ auto-infer | N/A |
| P4. Consent preview retained | หลักฐานว่า consent preview ยังอยู่ | deterministic flow checklist + UI capture (sanitized) | ยังไม่มี capture ล่าสุด | consent preview ถูกข้าม/ถูกถอด | N/A |
| P5. Final confirmation retained | หลักฐานว่า final confirmation ยังอยู่ | deterministic flow checklist + UI capture (sanitized) | ยังไม่มี capture ล่าสุด | final confirmation ถูก bypass | N/A |

## 7. Lead / Dealer Isolation Checklist

| Item | Evidence Required | Acceptable Evidence | Missing Evidence | Blocker Condition | Owner Review Status |
| --- | --- | --- | --- | --- | --- |
| L1. No real lead sending in this phase | policy และ validation ยืนยัน no real lead sending | docs + validator output | ยังไม่มี owner sign-off สำหรับ phase ถัดไป | มีการส่ง lead จริงใน phase นี้ | N/A |
| L2. Owner/dealer routing unchanged | routing map เทียบ baseline | deterministic routing checklist | ยังไม่มี routing proof ล่าสุด | routing เปลี่ยนโดยไม่อนุมัติ | N/A |
| L3. Dealer isolation untouched | หลักฐาน no cross-dealer exposure | isolation guard checklist | ยังไม่มี isolation runtime proof ล่าสุด | ข้อมูลรั่วข้าม dealer/owner boundary | N/A |
| L4. Queue count not user-facing | หลักฐานว่า queue count ไม่แสดง user-facing | UI negative proof + checklist | ยังไม่มี user-facing negative proof ล่าสุด | user-facing เห็นจำนวนคิวผู้สนใจ | N/A |

## 8. Deterministic Authority Checklist

| Authority Domain | Required Evidence | Acceptable Evidence | Missing Evidence | Blocker Condition | Owner Review Status |
| --- | --- | --- | --- | --- | --- |
| Search | deterministic search remains primary authority | flow map + scenario proof | ยังไม่มี runtime scenario snapshot ล่าสุด | AI override deterministic search | N/A |
| Lead capture | deterministic consent/lead chain unchanged | consent-flow checklist | ยังไม่มี final confirmation proof ล่าสุด | AI bypass consent/final confirmation | N/A |
| Queue handling | deterministic queue logic unchanged | queue policy checklist | ยังไม่มี queue policy runtime cross-check | AI injects/changes queue state | N/A |
| Inventory truth | inventory authority remains deterministic source | inventory boundary note + scenario proof | ยังไม่มี shadow hallucination runtime proof | AI แต่งข้อมูลรถนอก inventory | N/A |

## 9. Shadow Scenario Checklist (Admin-only Future Dry-run Reference)

| Scenario | Required Evidence | Acceptable Evidence | Missing Evidence | Blocker Condition | Owner Review Status |
| --- | --- | --- | --- | --- | --- |
| S1. ค้นหารถ | deterministic result precedence + safe fallback | scenario log (sanitized) | ยังไม่มี runtime dry-run evidence | AI override deterministic result | N/A |
| S2. เปรียบเทียบรถ | no fabricated specs + inventory grounding | structured scenario record | ยังไม่มี grounding proof | AI แต่งสเปกนอก inventory | N/A |
| S3. ถามผ่อน | safe explanatory response without hidden pricing leakage | sanitized response sample | ยังไม่มี redaction-safe response evidence | เผย internal/wholesale price | N/A |
| S4. ขอผู้ขายติดต่อกลับ | consent preview + final confirmation chain intact | flow checklist + scenario record | ยังไม่มี end-to-end flow evidence | ข้าม consent/final confirmation | N/A |
| S5. เปลี่ยนใจค้นหาต่อ | fallback deterministic search continuity | scenario note | ยังไม่มี continuity proof | flow ค้างหรือหลุด deterministic authority | N/A |
| S6. ถามเรื่องโอนเงิน/มัดจำ/ความปลอดภัย | policy-safe response + escalation-safe fallback | safety scenario record | ยังไม่มี policy uncertainty proof | ตอบเสี่ยงผิดนโยบาย/ผิดความปลอดภัย | N/A |
| S7. Prompt injection | injection rejection + deterministic-safe fallback | attack simulation note (sanitized) | ยังไม่มี injection defense runtime proof | prompt injection บังคับให้หลุด boundary | N/A |

## 10. Logging / Redaction Checklist

### 10.1 Allowed Sanitized Logs

- `trace_id`, `request_id` ที่ไม่โยงกลับ PII โดยตรง
- status/reason code แบบ aggregate
- timestamp และ feature-state แบบไม่เผยข้อมูลลูกค้า

### 10.2 Forbidden Logs

ห้ามบันทึกหรือเผยแพร่ข้อมูลต่อไปนี้:

- phone number
- full plate number
- full VIN
- internal price
- wholesale price
- raw owner data
- raw lead data

### 10.3 Sanitized Evidence Format (ตัวอย่าง)

```
record_id=evc-xxxx
flow=deterministic_fallback
reason=policy_uncertainty
decision=hold
pii_present=false
```

## 11. Evidence Status Table (Current Packet Snapshot)

| Item | Required Evidence | Acceptable Evidence | Current Status | Blocker (Yes/No) | Owner Decision |
| --- | --- | --- | --- | --- | --- |
| Gate B secret readiness | secret inventory + owner sign | sanitized inventory | Missing runtime proof | Yes | HOLD |
| Gate B no secret in repo | static scan + diff review | validator output | Checklist ready, runtime N/A | No | HOLD |
| Gate B storage boundary | boundary policy + diagram | sanitized boundary docs | Missing proof package | Yes | HOLD |
| Gate B access boundary | role matrix + deny proof | RBAC checklist | Missing runtime deny evidence | Yes | HOLD |
| Gate B rollback readiness | rollback playbook | documented stop/rollback flow | Missing rehearsal evidence | Yes | HOLD |
| Gate B budget/quota readiness | caps + owner budget sign | cap sheet + owner sign-off | Missing owner-approved evidence | Yes | HOLD |
| Gate B kill switch readiness | switch verify + stop condition | policy + verify record | Missing runtime switch proof | Yes | HOLD |
| Gate C admin-only readiness | allowlist + boundary matrix | admin scope checklist | Missing runtime allowlist proof | Yes | HOLD |
| Gate C no buyer exposure | negative buyer/public proof | sanitized scenario records | Missing full scenario evidence | Yes | HOLD |
| Gate C deterministic authority | authority mapping proof | deterministic checklist | Missing latest runtime proof | Yes | HOLD |
| Gate C fallback deterministic | fallback matrix (6 cases) | scenario matrix | Missing runtime fallback proof | Yes | HOLD |
| Gate C logging/redaction | allow/forbid log contract | redaction evidence pack | Missing runtime redaction proof | Yes | HOLD |
| PDPA/privacy | no PII-to-AI + consent/final confirmation | policy + flow proof | Missing runtime privacy proof | Yes | HOLD |
| lead/dealer isolation | no real lead + dealer isolation proof | isolation checklist | Missing runtime isolation proof | Yes | HOLD |
| shadow scenarios | scenario-by-scenario evidence | sanitized scenario packet | Missing execution evidence | Yes | HOLD |

## 12. Owner Decision Table

| Decision | Criteria | Action |
| --- | --- | --- |
| GO | หลักฐานครบทุก critical gate และไม่มี privacy/security/lead/dealer/fallback/kill-switch blocker | อนุมัติไปขั้นเตรียม execution ในรอบถัดไป (ไม่ใช่เปิดอัตโนมัติ) |
| HOLD | หลักฐานยังไม่ครบ แต่มีแผนแก้ชัดเจนและไม่มี violation ร้ายแรง | ให้เติมหลักฐานตาม checklist ก่อน review ใหม่ |
| NO-GO | มี blocker ด้าน privacy/security/lead/dealer isolation/fallback/kill switch หรือพบ violation ที่รับไม่ได้ | ห้ามเดินหน้ารอบเปิด admin-only shadow จริง |

## 13. Explicit Non-goals (v8.2)

- ไม่เปิด Gemini จริงใน v8.2
- ไม่เปิด admin-only shadow จริง
- ไม่เปิด buyer-facing AI
- ไม่ deploy
- ไม่ใส่ secret
- ไม่ส่ง lead จริง
- ไม่แก้ runtime

## 14. Stop / Rollback Conditions สำหรับรอบอนาคต

- secret wiring proof ไม่ครบ -> stop
- budget/quota cap proof ไม่ครบ -> stop
- kill switch runtime proof ไม่ผ่าน -> stop
- admin-only boundary runtime proof ไม่ครบ -> stop
- fallback/logging-redaction runtime proof ไม่ครบ -> stop
- หากพบความเสี่ยง PDPA/PII หรือ lead/dealer isolation breach -> rollback เป็น deterministic-only ทันที

## 15. Known Blockers (Current)

- owner sign-off ยังไม่มี
- secret wiring ยังไม่พิสูจน์จริง
- budget/quota cap ยังไม่พิสูจน์จริง
- kill switch runtime proof ยังไม่มี
- admin-only boundary runtime proof ยังไม่มี
- fallback/logging-redaction runtime proof ยังไม่มี

## 16. Recommended Next Version

ตัวเลือกแนะนำ:

- `v8.2A` closure record, หรือ
- `v8.3` owner sign-off readiness review packet

เงื่อนไขร่วม:

- ต้องไม่ใช่ Gemini activation อัตโนมัติ
- ต้องยังคง owner approval ก่อน execution จริง

## 17. Static Validation Requirement (v8.2)

ต้องมี static validator ที่ยืนยันว่า:

- v8.2 เป็น checklist-packet-only / docs-only
- ไม่มี activation command
- ไม่มีการเปลี่ยน env/secrets/API key
- ไม่มีการเปิด Gemini/user-visible AI/admin-only shadow/buyer-facing AI/public signup/real lead sending
- ไม่มี `src/` หรือ runtime change ในรอบเอกสารนี้

---

## v8.2 Checklist Packet Snapshot

- [x] มี Executive summary สำหรับลุงเด่น
- [x] ระบุ current baseline ตาม HEAD `34aae52` และ full hash ที่กำหนด
- [x] ระบุ decision purpose ว่าเป็น checklist packet ไม่ใช่ activation
- [x] แตก Gate B checklist พร้อม evidence required/acceptable/missing/blocker/owner status
- [x] แตก Gate C checklist พร้อม evidence required/acceptable/missing/blocker/owner status
- [x] ระบุ budget/quota/kill-switch readiness checklist
- [x] ระบุ deterministic authority + fallback deterministic checklist
- [x] ระบุ logging/redaction + sanitized evidence format
- [x] ระบุ PDPA/privacy + lead/dealer isolation checklist
- [x] ระบุ shadow scenario checklist ครบ 7 สถานการณ์
- [x] มี evidence status table และ owner decision table
- [x] ระบุ explicit non-goals + stop/rollback conditions + known blockers
- [x] ระบุ recommended next version ที่ไม่ใช่ auto activation
- [x] ระบุ static validation requirement ว่าเป็น docs-only/checklist-only/no activation

## Packet Verdict

> **v8.2 CHECKLIST PACKET READY (DOCS-ONLY / NO ACTIVATION).**
>
> เอกสารนี้เป็น checklist packet สำหรับเก็บและประเมินหลักฐานก่อน owner sign-off เท่านั้น
> ยังไม่ใช่การเปิด Gemini จริง, ไม่ใช่การเปิด admin-only shadow จริง, ไม่ใช่ buyer-facing AI, ไม่ deploy, ไม่ใส่ secret, ไม่ส่ง lead จริง, และไม่แตะ runtime
