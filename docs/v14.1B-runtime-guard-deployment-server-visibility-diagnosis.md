# v14.1B Runtime Guard Deployment / Server Visibility Diagnosis

## 1) Execution type

runtime guard deployment/server visibility diagnosis only: yes  
staging scope only: yes  
read-only runtime metadata inspection: yes  
safe diagnostics marker patch (static): yes  
Gemini run: no  
one-run click: no  
retry: no  
second run: no  
provider network call: no  
production deploy: no  
public route activation: no  
buyer-facing AI release: no  
real lead path execution: no  
real customer data / PII: no  
phone/plate/VIN exposure: no  
secret/token/API key exposure: no

## 2) Why v14.1A stayed HOLD

v14.1A confirmed frontend hosting deploy, but v14.1 guard symbols were not visible in served frontend JS bundle.  
That result is expected when guards are implemented in server runtime code instead of browser bundle code.

v14.1B therefore diagnoses runtime path and deployment state without triggering one-run or Gemini.

## 3) One-run runtime path diagnosis (read-only)

### 3.1 Frontend button path

`src/components/admin/OwnerFirebaseTokenHelperPanel.tsx` uses:

- `OWNER_GEMINI_ONE_RUN_ROUTE = "/api/ai/chat-user-visible-orchestrate"`
- `fetch(..., { method: "POST", body: { userMessage, pilotSessionContext } })`

No direct Gemini SDK call exists in browser code for this path.

### 3.2 Client bridge

`src/services/ai/chat/chatUserVisibleOrchestrateClient.ts` confirms:

- `CHAT_USER_VISIBLE_ORCHESTRATE_ROUTE = "/api/ai/chat-user-visible-orchestrate"`
- browser sends authenticated request to server bridge route only.

### 3.3 Server bridge and guard execution location

`src/services/ai/salesBrainServerUserVisibleOrchestrationBridge.ts` handles:

- `POST /api/ai/chat-user-visible-orchestrate`
- calls `maybeApplyUserVisibleRealProvider(...)`

`maybeApplyUserVisibleRealProvider` is defined in:

- `src/services/ai/salesBrainUserVisibleRealProvider.ts`

v14.1 guards are implemented and executed in this server file:

- `USER_VISIBLE_LEAD_PII_CUE_OUTPUT_PATTERNS`
- `USER_VISIBLE_PHONE_ECHO_OUTPUT_PATTERNS`
- `assertNoLeadOrPiiCueLanguage(...)`
- `assertNoPhoneEchoInChatText(...)`
- guard checks bound inside `evaluateRealProviderOutputSafety(...)` fail-closed path

Conclusion: v14.1 guard is server/backend runtime logic, not frontend-only logic.

## 4) Staging deploy target / runtime identity (read-only metadata)

From `firebase.json` hosting rewrites:

- `/api/**` -> Cloud Run service `nonga-staging`
- region: `asia-southeast1`
- project/site: `nonga-ce93c`

From Cloud Run service/revision metadata inspection (read-only):

- service: `nonga-staging`
- region: `asia-southeast1`
- project: `nonga-ce93c`
- latest ready revision: `nonga-staging-00173-nm8`
- runtime artifact type: container image (Cloud Run image digest), not frontend bundle
- image digest: `sha256:a9fca1b882fc2b9ef098fa5dd8fd85626e55fd5c073aac0963d7dd75a6cfe20e`
- image tag resolved from Artifact Registry digest: `v13.15n-w-runtime-evidence-f939fd2`

Inference:

- current staging runtime image tracks commit short-hash `f939fd2` tag lineage
- v14.1 repo commit `9716083...` and v14.1A commit `18f0f8a...` are not included in that deployed runtime image lineage
- therefore runtime currently serving one-run endpoint is not yet updated to v14.1 code

## 5) Runtime/version visibility findings before patch

Existing safe runtime diagnostics already existed in API payload:

- runtime mode/provider status
- allowlist match diagnostics (masked)
- pilot context diagnostics

Gap found:

- no explicit guard policy version marker proving v14.1 lead/PII guard activation by name

## 6) v14.1B safe marker patch (server diagnostics only)

Added static runtime-safe guard policy marker fields with no secret and no PII:

- `AI_USER_VISIBLE_GUARD_POLICY_VERSION = "v14.1-lead-pii-cue-guard"`
- `AI_USER_VISIBLE_GUARD_POLICY_MARKERS.leadPiiCueGuard = true`
- `AI_USER_VISIBLE_GUARD_POLICY_MARKERS.phoneEchoGuard = true`
- `AI_USER_VISIBLE_GUARD_POLICY_MARKERS.safeConfirmationStepWording = true`

Exposed in `userVisibleRuntimeDiagnostic` response object:

- `guardPolicyVersion`
- `leadPiiCueGuardActive`
- `phoneEchoGuardActive`
- `safeConfirmationStepWordingActive`

Scope:

- owner/admin diagnostics surface only (existing authenticated orchestration path)
- no new public endpoint
- no provider call added
- no token/secret values returned

## 7) How to verify v14.1 guard active without one-run click

After controlled staging runtime deploy of this patch set:

1. Read runtime metadata for `nonga-staging` revision/image digest (Cloud Run describe).  
2. Call only safe diagnostics path already used in owner flow (no Gemini invocation required for metadata visibility validation policy).  
3. Confirm `userVisibleRuntimeDiagnostic.guardPolicyVersion === "v14.1-lead-pii-cue-guard"`.  
4. Confirm all guard marker booleans are `true`.

No owner one-run click is needed for this visibility confirmation step.

## 8) Boundary reminder

v14.1B does not execute runtime quality run:

- Gemini run: no
- one-run click: no
- retry: no
- second run: no
- provider network call: no

Before any next owner manual quality retest:

`READY FOR OWNER MANUAL GEMINI QUALITY RETEST — FRESH OWNER APPROVAL REQUIRED`

## 9) Final recommendation enum (v14.1B)

- `READY FOR OWNER MANUAL GEMINI QUALITY RETEST — FRESH APPROVAL REQUIRED`
- `READY WITH STAGING RUNTIME DEPLOY NEEDED — NO RETEST YET`
- `HOLD — RUNTIME GUARD VISIBILITY NOT CONFIRMED`
- `HOLD — RUNTIME DEPLOY COMMAND / SCOPE NOT SAFE TO EXECUTE WITHOUT OWNER REVIEW`
- `HOLD — GUARD POLICY MARKER MISSING`
- `HOLD — STAGING RUNTIME NOT UPDATED`
- `HOLD — GUARDRAIL GAP DETECTED`
- `HOLD — SECRET/PII/REAL LEAD RISK DETECTED`

