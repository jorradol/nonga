# v14.3AE — Correct Firebase ID Token Acquisition Prep After Safe Read-Only Status Check

## 1) Execution type

v14.3AE type: docs/tests/fixtures/static + owner-local Firebase ID token acquisition prep only  
one-run execution by agent: no  
retry by agent: no  
second run by agent: no  
provider/Gemini/runtime call by agent: no  
provider network call by agent: no  
deploy by agent: no  
runtime config mutation by agent: no

No retry.  
No second run.

## 2) Boundary status

- no production: `yes`
- no public route activation: `yes`
- no buyer-facing AI release: `yes`
- no real lead sending: `yes`
- no real customer data / PII: `yes`
- no phone / plate / VIN: `yes`
- no token / secret / API key exposure: `yes`
- no Thor real data import: `yes`
- no dealer real inventory import: `yes`

## 3) Branch / HEAD / origin / working tree before work

- branch: `feature/chat-image-attachment-v1`
- local HEAD: `c0f9b5b78792b8b6a9eaad5757bbf6822b15ebc7`
- origin HEAD: `c0f9b5b78792b8b6a9eaad5757bbf6822b15ebc7`
- local equals origin: `yes`
- working tree clean: `yes`

## 4) Previous read-only status check result (owner)

Observed sanitized status:

```text
firebaseToken: missing
firebaseTokenShape=missing
adminTokenPresent=no
approvalFileExists=yes
approvalTextMatch=match
approvalFileAgeMinutes=25
lockFileExists=no
lockConsumed=false
providerCall=not_run
Gemini/runtime=not_run
providerNetwork=not_run
runtimeMutation=not_run
tokenExposure=masked-only
```

Interpretation:

- lock was not consumed
- no evidence of provider/Gemini/runtime execution
- still no valid owner Firebase ID token in session env
- one-run remains forbidden in v14.3AE

## 5) Gate 0 repo sanity result

Executed:

```bash
git branch --show-current
git rev-parse HEAD
git fetch origin
git rev-parse origin/feature/chat-image-attachment-v1
git status --short
```

Result:

- branch check: pass
- local/origin sync: pass
- working tree clean: pass
- starting HEAD exact match: pass (`c0f9b5b78792b8b6a9eaad5757bbf6822b15ebc7`)
- Gate 0 decision: `PASS`

## 6) Firebase auth/token source inspection

Inspected paths:

- `src/server/serverAuthContext.ts`
- `src/services/auth/authService.ts`
- `src/services/auth/firebaseAuthHeaders.ts`
- `src/lib/firebase/index.ts`
- `src/lib/firebase/firebaseConfigGuard.ts`
- `src/components/admin/OwnerFirebaseTokenHelperPanel.tsx`
- `src/services/ai/salesBrainServerUserVisibleOrchestrationBridge.ts`
- `src/services/ai/salesBrainUserVisibleGate.ts`
- `scripts/check-owner-firebase-token-session-env.mts`
- `scripts/owner-local-user-visible-one-run-gate-v143ac.mts`
- `docs/v14.3AC-user-visible-firebase-auth-wrapper-prep.md`
- `docs/v14.3AD-lost-owner-output-wrong-token-incident-record.md`

Key findings:

- server user-visible route requires Firebase Bearer token (`getServerAuthContext` in `serverAuthContext.ts`)
- client helper exists for signed-in Firebase token extraction: `getCurrentUserIdToken(forceRefresh)` in `firebaseAuthHeaders.ts`
- owner-safe UI helper already exists: `OwnerFirebaseTokenHelperPanel` copy button uses `getCurrentUserIdToken(true)` then writes token to clipboard only, not to UI log
- v14.3AC checker enforces JWT-like shape and masked-only output for `NONGA_OWNER_FIREBASE_ID_TOKEN`
- v14.3AC wrapper blocks admin token mixing and blocks retry/second-run with lock

## 7) Recommended token acquisition method

Recommended path for v14.3AE: **A (existing helper), with B as controlled fallback**

Primary:

- use existing owner helper panel (`OwnerFirebaseTokenHelperPanel`) while owner is signed in with real Firebase session
- helper action: `Copy Firebase ID token (force refresh)` -> internally `getCurrentUserIdToken(true)`
- token is copied to clipboard only; token is not rendered in panel text

Fallback (only if helper panel unavailable by gate/flag):

- owner uses browser DevTools console in signed-in session and manually runs:
  - `await window.nongaOwnerDebug?.getCurrentUserIdToken?.(true)` only if explicit safe helper is exposed
  - or equivalent `getIdToken(true)` path from authenticated Firebase user object in current app context
- do not persist token in files, localStorage, screenshots, or chat logs

Disallowed acquisition paths:

- random token scraping from localStorage/sessionStorage
- using `NONGA_ADMIN_API_TOKEN` as substitute for Firebase ID token
- printing raw token in logs/console output intended for sharing
- writing token into repo files or committing token content

## 8) Helper status

- existing helper found: `src/components/admin/OwnerFirebaseTokenHelperPanel.tsx`
- added helper in v14.3AE: no
- helper needed in this round: no

## 9) Safety controls for token exposure

- checker output is masked-only (`token: ***MASKED***`)
- wrapper output is masked-only (`firebaseToken: ***MASKED***`)
- no raw token is stored in docs/fixtures/tests
- no token persistence in repo artifacts
- no provider/Gemini calls in this preparation round

## 10) No-run / no-retry / no-second-run confirmation

- one-run command executed by agent: no
- retry attempted by agent: no
- second run attempted by agent: no
- owner one-run instruction in v14.3AE: forbidden

## 11) Runtime/provider/Gemini status during agent work

- runtime adapter execution by agent: `not_run`
- provider call by agent: `not_run`
- Gemini call by agent: `not_run`
- provider network by agent: `not_run`

## 12) Exact next owner action (token check only)

```bat
cd /d D:\nonga
set NONGA_OWNER_FIREBASE_ID_TOKEN=<paste-firebase-id-token-here>
npm run check:owner-firebase-token-session-env
```

If and only if checker result is `format: valid-shape`, continue in next round with fresh approval preparation.  
v14.3AE does not authorize one-run execution.

## 13) Final decision

`READY FOR OWNER FIREBASE TOKEN ACQUISITION — NO ONE-RUN`
