# v10.4 Admin-Only Real Gemini Gated Execution Planning Packet

> PLANNING PACKET ONLY / DOCS-ONLY / DO NOT RUN
> v10.4 is a gated execution planning packet for a future owner-approved admin-only real Gemini manual smoke. This version does not execute Gemini, does not deploy, does not change runtime, does not open a public route, does not enable buyer-facing AI, and does not send a real lead.

---

## 1. Purpose

v10.4 is the final gated execution planning packet before requesting approval for a future admin-only real Gemini manual smoke run.

- still no real Gemini execution in v10.4
- still no production
- still no deploy
- still no runtime/provider wiring change
- still no public route
- still no buyer-facing AI
- still no real lead
- deterministic flow remains source of truth

## 2. Relationship to v10.0-v10.3

Continuity chain:

- v10.0 = manual smoke path prep
- v10.1 = operator runbook
- v10.2 = manual smoke preflight checklist/gate
- v10.3 = admin-only real Gemini manual smoke execution packet
- v10.4 = gated execution planning before requesting owner approval for any real run

## 3. Execution Gate Model

### Gate 1: Repository gate

All repository checks must pass before any future execution request:

- correct branch
- expected HEAD
- working tree clean
- local = origin
- no out-of-scope file changes

### Gate 2: Safety gate

Hard safety boundary for v10.4 and future run-request review:

- no production
- no deploy
- no public route
- no buyer-facing AI
- no real lead
- no real customer data
- no PII / phone / VIN / plate
- no secret in repo

### Gate 3: Runtime-proof gate

Future run must remain tightly constrained:

- admin-only only
- synthetic prompt and synthetic test data only
- `GEMINI_API_KEY` stays environment secret only (never committed)
- kill switch verified
- deterministic fallback verified
- log redaction verified
- quota/cost cap acknowledged
- no normal test hitting Gemini

### Gate 4: Owner approval gate

No future real run is allowed without explicit owner approval:

- owner explicit approval is required in a separate approval step
- approval must explicitly allow only one admin-only real Gemini manual smoke scope
- approval does not allow deploy, production, buyer-facing AI, public route, or real lead sending

## 4. Command Boundary (Planning Only)

v10.4 provides planning placeholders only.

```bash
# DO NOT RUN in v10.4.
# Placeholder only for a future gated admin-only Gemini smoke command.
# Real command must be added or confirmed only after owner explicit approval.
npm run <future-admin-only-real-gemini-manual-smoke-command>
```

Boundary rules:

- do not add runnable Gemini command in this version
- do not add npm script that executes Gemini
- do not let normal tests invoke Gemini
- do not read or print real secret values in static validation

## 5. Approval Wording Template

### English template

I explicitly approve one admin-only real Gemini manual smoke run for Nong A / NongBot.
This approval does not allow production, deploy, public route, buyer-facing AI, or real lead sending.
Use synthetic data only. Do not store or print secrets. Abort if any guardrail fails.

### Thai template

ผม/ฉันอนุมัติแบบชัดเจนเฉพาะการรัน admin-only real Gemini manual smoke จำนวน 1 ครั้งสำหรับ Nong A / NongBot เท่านั้น
การอนุมัตินี้ไม่ครอบคลุม production, deploy, public route, buyer-facing AI หรือการส่ง real lead
ให้ใช้เฉพาะข้อมูลสังเคราะห์ (synthetic) ห้ามเก็บหรือแสดง secret และให้ abort ทันทีเมื่อ guardrail ใดไม่ผ่าน

## 6. Cost / Quota Plan

Future manual smoke must stay low-cost and bounded:

- very low run count (single-run intent)
- strict prompt count limit
- synthetic data only
- abort on abnormal cost or quota usage
- no loop
- no large batch
- no normal test hitting Gemini

## 7. Evidence Requirements (Future Approved Run)

If a future owner-approved run occurs, collect sanitized evidence only:

- date/time
- operator
- branch
- HEAD
- approval reference
- command used
- synthetic data confirmation
- env secret source confirmation without value
- pass/fail/abort result
- quota/cost observation
- redaction check
- fallback check
- no buyer/public/lead confirmation
- sanitized logs only

## 8. Abort Criteria

Abort immediately if any condition is detected:

- secret appears in output/log
- real PII appears
- buyer/public path is involved
- lead sending path is involved
- cost/quota anomaly appears
- response violates guardrail
- deterministic fallback behavior is abnormal
- environment source is unclear
- branch/HEAD mismatch
- working tree is not clean

## 9. Next Step After v10.4

Next step is not deploy and not production.

Candidate next packet:

- `v10.5 Admin-Only Real Gemini Manual Smoke Approval Request`

That next step must request separate owner explicit approval before any real execution.
