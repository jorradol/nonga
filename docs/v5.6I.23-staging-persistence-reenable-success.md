# v5.6I.23 — Staging Persistence Re-enable Success (Recorded)

**สถานะ:** Execution record + static tests only — **ไม่ deploy อีก, ไม่ update env อีก, ไม่เขียน Firestore จาก CI**  
**Git baseline ขณะรัน:** `e088e40fffe61921d7b8d232257f5f1634fff5d7` (v5.6I.22)  
**วันที่บันทึก:** 2026-06-07  
**Target:** staging **`nonga-ce93c`** เท่านั้น  
**Branch:** `feature/chat-image-attachment-v1`  
**อ้างอิง:** v5.6I.22 current-branch runtime deploy, v5.6I.21 rollback lesson, v5.6I.19 smoke harness, v5.6I.20 approval packet

> เอกสารนี้บันทึกผล **re-enable Firestore settlement persistence บน updated staging runtime สำเร็จ** — test v56i23 เป็น static validation ของ record นี้ (**ไม่ fetch staging / ไม่ gcloud**)

---

## สรุปผล (plain language)

| เรื่อง | ผล |
|--------|-----|
| Preflight | **PASS** — git clean, HEAD = origin @ `e088e40` |
| ก่อนเปิด | rev `nonga-staging-00046-rh7`, image `v5.6I.22-current-branch-runtime-cebc7a6`, flags memory/false/false |
| Re-enable | **PASS** — quoted env ถูกต้อง → rev **`nonga-staging-00047-7xc`**, traffic **100%** |
| Smoke v5.6I.19 | **PASS 23/23** — S5 duplicate + S6 conflict + durable Firestore writes |
| Rollback | **ไม่มี** — smoke ผ่านครบ |
| Production / Hosting / rules / image deploy | **ไม่แตะ** |

---

## 1. Preflight (passed)

| Check | Result |
|-------|--------|
| `git status` | clean |
| branch | `feature/chat-image-attachment-v1` |
| HEAD | `e088e40fffe61921d7b8d232257f5f1634fff5d7` |
| `origin/feature/chat-image-attachment-v1` | ตรงกับ HEAD |
| gcloud active project | `nonga-ce93c` |
| Cloud Run service | `nonga-staging` |
| region | `asia-southeast1` |

---

## 2. ก่อนเปิด — rev 00046-rh7, flags memory/off

| Field | Value |
|-------|-------|
| Revision | **`nonga-staging-00046-rh7`** |
| Image tag | **`v5.6I.22-current-branch-runtime-cebc7a6`** |
| Traffic | 100% |

**Settlement flags (ก่อนเปิด):**

| Env | Value |
|-----|-------|
| `NONGA_SETTLEMENT_DATA_BACKEND` | **`memory`** |
| `NONGA_SETTLEMENT_FIRESTORE_WRITES_ENABLED` | **`false`** |
| `NONGA_SUCCESS_FEE_RECORD_ENABLED` | **`false`** |

---

## 3. Re-enable command (executed — recorded only, do not re-run from CI)

```bash
# รันจริงแล้ว — ใช้ quote ครอบ update-env-vars บน PowerShell
gcloud run services update nonga-staging \
  --project=nonga-ce93c \
  --region=asia-southeast1 \
  --update-env-vars="NONGA_SETTLEMENT_DATA_BACKEND=firestore,NONGA_SETTLEMENT_FIRESTORE_WRITES_ENABLED=true,NONGA_SUCCESS_FEE_RECORD_ENABLED=true"
```

| Env | Value |
|-----|-------|
| `NONGA_SETTLEMENT_DATA_BACKEND` | `firestore` |
| `NONGA_SETTLEMENT_FIRESTORE_WRITES_ENABLED` | `true` |
| `NONGA_SUCCESS_FEE_RECORD_ENABLED` | `true` |

---

## 4. หลังเปิด — rev 00047-7xc

| Field | Value |
|-------|-------|
| New revision | **`nonga-staging-00047-7xc`** |
| Traffic | **100%** |
| Image | ไม่เปลี่ยน — ยังเป็น `v5.6I.22-current-branch-runtime-cebc7a6` |
| Previous revision | `nonga-staging-00046-rh7` |

**Settlement flags (หลังเปิด):**

| Env | Value |
|-----|-------|
| `NONGA_SETTLEMENT_DATA_BACKEND` | **`firestore`** |
| `NONGA_SETTLEMENT_FIRESTORE_WRITES_ENABLED` | **`true`** |
| `NONGA_SUCCESS_FEE_RECORD_ENABLED` | **`true`** |

---

## 5. Health หลัง re-enable

Base: `https://nonga-ce93c.web.app`

| Endpoint | HTTP status |
|----------|-------------|
| `GET /api/health` | **200** — `ok: true` |

---

## 6. Smoke result (v5.6I.19 — rev 00047-7xc)

**สรุป: PASS 23/23**

### PASS

| Check | ผล |
|-------|-----|
| S1 health | **200** |
| S2 admin revenue preview | **200**, 3 rows |
| S3 seller revenue preview | **200**, 3 rows |
| S4 admin manual adjustment | **200**, `outcome: processed` |
| **S5 duplicate requestId replay** | **200**, `outcome: duplicate` — **ไม่สร้าง adjustment/audit/idempotency ซ้ำ** |
| **S6 conflict fingerprint** | **409** |
| S7 seller scope | seller เห็นยอดของตัวเองเท่านั้น |
| S8–S11 Firestore client deny | **403** ทั้ง 4 collections |
| S12 PII safety | **ไม่มี** buyer phone ใน admin/seller payload |
| S13 buyer lead | cars **200**; submit **401** (expected — staging ต้อง login) |
| S14 seller queue | **200** |
| S15 outcome endpoint | queue API reachable |
| S16 cancel prereq | seller auth OK |

### FAIL

ไม่มี — smoke **PASS 23/23**

**Test listing (smoke):** `car-1780410414291` — ไม่มี buyer PII ใน API responses

---

## 7. Firestore durable docs count (metadata only — ไม่เปิด PII)

| Collection | Doc count |
|------------|-----------|
| `settlementAdjustments` | **1** |
| `settlementAuditLogs` | **4** |
| `settlementIdempotencyRecords` | **4** |
| `successFeeRecords` | **0** — smoke ยังไม่มี success-fee event |

---

## 8. Firestore client direct deny (unauth — 403)

| Collection | GET | LIST |
|------------|-----|------|
| `successFeeRecords` | **403** | **403** |
| `settlementAdjustments` | **403** | **403** |
| `settlementAuditLogs` | **403** | **403** |
| `settlementIdempotencyRecords` | **403** | **403** |

---

## 9. Rollback

**ไม่มี rollback** — smoke ผ่านครบ

Rollback command สำรอง (ยังไม่รัน):

```bash
# ใช้เมื่อ smoke fail — kill-switch กลับ memory (DO NOT RUN จาก commit docs-only นี้)
gcloud run services update nonga-staging \
  --project=nonga-ce93c \
  --region=asia-southeast1 \
  --update-env-vars="NONGA_SETTLEMENT_DATA_BACKEND=memory,NONGA_SETTLEMENT_FIRESTORE_WRITES_ENABLED=false,NONGA_SUCCESS_FEE_RECORD_ENABLED=false"
```

---

## 10. เปรียบเทียบ v5.6I.21 vs v5.6I.23

| เรื่อง | v5.6I.21 (rev 00044, image เก่า) | v5.6I.23 (rev 00047, image v5.6I.22) |
|--------|----------------------------------|--------------------------------------|
| S5 duplicate requestId | **FAIL** | **PASS** — `outcome: duplicate` |
| S6 conflict fingerprint | **FAIL** (200) | **PASS** — **409** |
| Durable Firestore writes | **FAIL** (0 docs) | **PASS** (1/4/4 docs) |
| Rollback | รันแล้ว | ไม่ต้อง rollback |

Root cause v5.6I.21: image เก่าไม่มี v5.6I.13+ durable wiring — แก้ด้วย v5.6I.22 deploy ก่อน re-enable

---

## Forbidden changes (v5.6I.23 slice — confirmed)

- ไม่ deploy Cloud Run / Hosting / Firestore rules
- ไม่ `gcloud run deploy`
- ไม่ `gcloud run services update` (ใน slice docs นี้)
- ไม่เปิด env flags อีก
- ไม่แก้ secrets
- ไม่ migration / backfill
- ไม่ลบหรือแก้ Firestore records จาก smoke
- ไม่แตะ production
- ไม่ payment / invoice / Gemini changes
- ไม่แก้ runtime code ในรอบ v5.6I.23 docs

---

## Files (v5.6I.23)

| File | เปลี่ยน |
|------|--------|
| `docs/v5.6I.23-staging-persistence-reenable-success.md` | **NEW** — record นี้ |
| `scripts/test-v56i23-staging-persistence-reenable-success.mts` | **NEW** — static validation |
| `package.json` | `test:v56i23-staging-persistence-reenable-success` |
