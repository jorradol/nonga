# v6.2B.9 — Read-only Old Listing Aggregate Query Execution Record

**สถานะ:** **READ-ONLY EXECUTION RECORD (REDACTED COUNTS)** — docs/tests/package only — **ยังไม่ cleanup จริง**, **ยังไม่ import**, **ยังไม่ deploy**  
**วันที่เอกสาร:** 2026-06-09  
**Git baseline:** `fbe37179553a8236b24698aa39a8bdd628758379` (v6.2B.8 pushed)  
**Branch:** `feature/chat-image-attachment-v1`  
**อ้างอิง:** v6.2B.8 Read-only Preflight, v6.2B.7 Cleanup/Archive Plan, G-11

> **v6.2B.9 = read-only aggregate query execution record แบบ redacted counts เท่านั้น** — query จริงทำบน staging ผ่าน **read-only GET** — git เก็บ **counts + criteria + status เท่านั้น** ไม่เก็บข้อมูลรายคัน  
> **Existing old staging listings ยังไม่ถูกลบ/ซ่อน** — ไม่มี Firestore writes ใน slice นี้

---

## สารบัญ

1. [Preflight Metadata](#1-preflight-metadata)
2. [Aggregate Query Summary (Redacted)](#2-aggregate-query-summary-redacted)
3. [Query Criteria Applied (Count-only)](#3-query-criteria-applied-count-only)
4. [Reference Risk Summary](#4-reference-risk-summary)
5. [Cleanup Readiness Recommendation](#5-cleanup-readiness-recommendation)
6. [Import Gate](#6-import-gate)
7. [Forbidden in Repo (Execution Rules)](#7-forbidden-in-repo-execution-rules)
8. [Compliance (v6.2B.9)](#8-compliance-v62b9)

---

## 1. Preflight Metadata

| Field | Value |
|-------|-------|
| project | **staging only** (`nonga-ce93c`) — production **excluded** |
| branch | `feature/chat-image-attachment-v1` |
| head / origin (baseline) | `fbe37179553a8236b24698aa39a8bdd628758379` |
| working tree | **clean / synced** (at doc authoring) |
| execution mode | **read-only aggregate query only** |
| query method | **GET** marketplace listings API on staging (published inventory DTO) |
| Firestore writes | ❌ **none** |
| cleanup executed | ❌ **none** |
| import executed | ❌ **none** |
| deploy / env update | ❌ **none** |
| recordType | `redacted-aggregate-execution-record` |

**read-only only** — no cleanup / import / deploy in v6.2B.9

---

## 2. Aggregate Query Summary (Redacted)

Counts below are **git-safe redacted aggregates** from read-only staging query — **no per-car data in repo**.

| Metric | Count | Notes |
|--------|------:|-------|
| totalVisiblePublished | **17** | `listingStatus=published` and not sold — visible marketplace cohort |
| candidateOldTest | **17** | pre-real-import published cohort — matches v6.2B.7 §3 aggregate bucket |
| unknownNeedsReview | **0** | no ambiguous partial subset at aggregate level |
| protectedNewRealBatch | **0** | real pilot batch **not imported yet** — must stay **0** |
| distinctOwnerCount | **2** | aggregate owner cardinality only — **no raw UID in git** |
| per-car details in git | ❌ **forbidden** | no make/model/price/plate in repo |

### Execution timestamp

| Field | Value |
|-------|-------|
| queryExecutedAt | 2026-06-09 (read-only GET on staging) |
| fetchedRecordCount | 17 |
| httpStatus | 200 |

**ห้ามบันทึกข้อมูลรายคัน** — counts only in git

---

## 3. Query Criteria Applied (Count-only)

Criteria from v6.2B.7 / v6.2B.8 — applied for **aggregate classification only**:

| Criterion | Applied value (git-safe) | Used for count | Per-car in git |
|-----------|--------------------------|----------------|----------------|
| `createdBefore` | execution cutoff **2026-06-09** (all fetched records predate real-import phase) | ✅ | ❌ forbidden |
| `sourceTag` | field **absent** on all records in public query surface (0 tagged via API) | ✅ | ❌ forbidden |
| `pilotBatchId` | field **absent** on all records — no real pilot batch assigned | ✅ | ❌ forbidden |
| `ownerUid` | aggregate **distinctOwnerCount=2** only | ✅ | ❌ **no raw UID in git** |
| `status` | `listingStatus=published` for entire visible cohort (17/17) | ✅ | ❌ forbidden |
| `test/demo marker` | **0** explicit markers in public title/description fields | ✅ | ❌ forbidden |

### Classification rule (aggregate)

```
visible published cohort
  AND createdBefore execution cutoff
  AND pilotBatchId absent (pre-real-import)
  AND protectedNewRealBatch excluded (0)
→ candidateOldTest bucket
```

**criteria ใช้เพื่อ count เท่านั้น ไม่ expose รายคัน**

---

## 4. Reference Risk Summary

Reference checks for lead/reveal/outcome/payment/settlement — **not available via unauthenticated public marketplace GET**:

| Risk area | Git record | Rule |
|-----------|------------|------|
| lead references | **needs-review** | authenticated ops count required |
| reveal references | **needs-review** | authenticated ops count required |
| outcome references | **needs-review** | authenticated ops count required |
| payment references | **needs-review** | authenticated ops count required |
| settlement references | **needs-review** | authenticated ops count required |

| referenceRisk | **needs-review** |
|---------------|------------------|
| Meaning | aggregate listing counts complete; reference linkage counts **not verified** in this read-only public query |

**ห้าม mutate reference ใด ๆ** — read-only execution only

---

## 5. Cleanup Readiness Recommendation

| Value | Meaning |
|-------|---------|
| **READY FOR CLEANUP APPROVAL** | counts OK, no reference risk, criteria clear — may request cleanup phrase |
| **FIX REQUIRED** | reference risk unverified or ambiguous candidates — resolve before cleanup |
| **NO-GO** | unsafe to proceed — stop |

**This execution record:** **FIX REQUIRED**

| Reason | Detail |
|--------|--------|
| Aggregate counts | ✅ complete — 17 candidate / 0 protected / 0 unknown at aggregate level |
| Reference risk | ⚠️ **needs-review** — run authenticated ops reference count before cleanup approval |
| Cleanup execution | ❌ **not in v6.2B.9** |

### Cleanup approval (required before real cleanup)

> **อนุมัติให้ cleanup/archive old staging listings บน staging เท่านั้น แบบ controlled rollback ได้ ตาม v6.2B.7**

| Item | Status |
|------|--------|
| v6.2B.9 aggregate query | ✅ read-only execution record |
| Reference count verification | ⚠️ **pending** (ops authenticated query) |
| Cleanup execution | ❌ **not in v6.2B.9** |
| Cleanup approval phrase | **required separately** — not granted here |

---

## 6. Import Gate

| Item | Status |
|------|--------|
| Real stock import | ❌ **still blocked** — G-11 |
| Import before cleanup path | ❌ **wrong order** — aggregate query → reference verify → cleanup approval → cleanup → import approval |
| v6.2B.6 GO record | ✅ ops data package GO — **not import approval** |

### Import approval (unchanged — separate, after cleanup path)

> **อนุมัติให้ import real stock แบบ controlled pilot บน staging เท่านั้น ตาม v6.2B ชุดเล็ก 5–10 คัน**

| Requirement | Status |
|-------------|--------|
| Aggregate query (v6.2B.9) | ✅ this record |
| Cleanup execution + approval | **before** import |
| Import approval phrase | **required separately** — not granted in v6.2B.9 |
| Import execution record | required after import approval |

**import ต้องรอ cleanup path และ approval แยก**

---

## 7. Forbidden in Repo (Execution Rules)

| Forbidden in git | Rule |
|------------------|------|
| Per-car listing details | ❌ |
| make/model/price | ❌ |
| Full plate (e.g. `กข 1234`) | ❌ |
| Raw image URL | ❌ |
| Phone / LINE / address | ❌ |
| Raw UID | ❌ |
| Prompt dump / secret / env dump | ❌ |
| Firestore write commands | ❌ |
| Cleanup/delete/hide execution | ❌ |

---

## 8. Compliance (v6.2B.9)

| Item | Status |
|------|--------|
| Preflight metadata §1 | ✅ |
| Redacted aggregate summary §2 | ✅ |
| Query criteria §3 | ✅ |
| Reference risk summary §4 | ✅ |
| Cleanup recommendation §5 | ✅ |
| Import gate §6 | ✅ |
| Read-only only | ✅ |
| Firestore writes | ❌ none |
| Cleanup/delete/hide executed | ❌ none |
| Hard delete | ❌ none |
| Real stock import | ❌ blocked |
| Deploy / env / Firestore rules deploy | ❌ none |
| Secret access | ❌ not done |
| Payment / lead / reveal / outcome mutation | ❌ none |
| Public signup / public AI | ❌ not enabled |
| Real Gemini user-visible path | ❌ not enabled |
| Image processing / AI vision API | ❌ not in v6.2B.9 |

CI: `npm run test:v62b9-read-only-old-listing-aggregate-query-execution-record` — readFileSync only.

---

## Redacted Execution Summary (for report — git-safe only)

```text
v6.2B.9 Read-only Old Listing Aggregate Query Execution — REDACTED
project: staging only | read-only GET aggregate query
head/origin: fbe37179553a8236b24698aa39a8bdd628758379
totalVisiblePublished: 17 | candidateOldTest: 17 | unknownNeedsReview: 0
protectedNewRealBatch: 0 (not imported yet)
referenceRisk: needs-review (lead/reveal/outcome/payment/settlement — ops count pending)
recommendation: FIX REQUIRED (reference verification before cleanup approval)
cleanup approval phrase required (v6.2B.7) | import still BLOCKED
no per-car data / PII / raw URLs in repo
no Firestore writes / no cleanup executed
```

---

## Next Steps

1. ✅ v6.2B.9 read-only aggregate query execution record (this doc)
2. Ops runs authenticated reference count queries on staging (outside git)
3. Reference counts clear → update recommendation to READY FOR CLEANUP APPROVAL
4. Request cleanup approval phrase §5
5. Cleanup execution record (future slice) — after cleanup approval
6. Import approval phrase + import execution — **after** cleanup pass
