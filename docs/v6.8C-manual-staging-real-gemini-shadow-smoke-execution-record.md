# v6.8C — Manual Staging Real Gemini Shadow Smoke Execution Record

**สถานะ:** Execution record — **manual operator smoke completed on staging; kill switch drill PARTIAL; rollback DONE**  
**วันที่:** 2026-06-15  
**Git baseline (implementation packet):** `2cf4860b721092da00b75a640f947c9e61c8f7be` (v6.8C)  
**Branch:** `feature/chat-image-attachment-v1`  
**Checklist:** `docs/v6.8C-staging-real-gemini-shadow-smoke-operator-checklist.md`  
**Follow-up patch:** v6.8C.1 — admin SS-01 global kill switch alignment

> **Manual paid Gemini API calls were operator-approved on staging only.** No secrets, tokens, or API key values are recorded in this document.

---

## 1. Execution summary

| Item | Result |
|------|--------|
| Staging host | `https://nonga-ce93c.web.app` (Firebase Hosting → Cloud Run `nonga-staging`) |
| SS-01 admin shadow real Gemini | **PASS** |
| CP-02 chat shadow sink real Gemini | **PASS** (optional) |
| User-visible real Gemini | **Not opened** — confirmed |
| Kill switch drill | **PARTIAL** — see §5 |
| Rollback flags | **DONE** — revision `nonga-staging-00080-xdg` |
| Real stock / Firestore writes for smoke | **None** (read-only admin diagnostics) |

---

## 2. Staging revisions (operator timeline)

| Revision | Action |
|----------|--------|
| `nonga-staging-00077-j6w` | Pre-smoke — real provider flags `false` |
| `nonga-staging-00078-8n6` | Enabled `NONGA_AI_ADMIN_SHADOW_REAL_PROVIDER_ENABLED=true`, `NONGA_AI_CHAT_SHADOW_REAL_PROVIDER_ENABLED=true` — **smoke window** |
| `nonga-staging-00079-lbc` | `NONGA_AI_EMERGENCY_KILL_SWITCH=true` — **kill switch drill** |
| `nonga-staging-00080-xdg` | **Rollback** — kill switch `true`, real provider flags `false`, `USER_VISIBLE=false` |

---

## 3. Env / flags confirmed (no secret values)

| Flag / mount | Pre-smoke / smoke |
|--------------|-------------------|
| `GEMINI_API_KEY` | Mounted from Secret Manager resource `gemini-api-key:latest` |
| `NONGA_AI_PROVIDER` | `gemini` |
| `NONGA_AI_MODE` | `high` |
| `NONGA_AI_FIRST_ENABLED` | `true` |
| `NONGA_AI_SHADOW_MODE_ENABLED` | `true` |
| `NONGA_AI_USER_VISIBLE_ENABLED` | **`false`** (entire execution) |
| `NONGA_AI_EMERGENCY_KILL_SWITCH` | `false` during smoke; `true` after drill/rollback |
| `NONGA_AI_BUDGET_DAILY_LIMIT` / `MONTHLY` | `5` / `50` |
| `NONGA_AI_ADMIN_SHADOW_REAL_PROVIDER_ENABLED` | `true` during smoke; `false` after rollback |
| `NONGA_AI_CHAT_SHADOW_REAL_PROVIDER_ENABLED` | `true` during smoke; `false` after rollback |

Auth: Firebase admin ID token (staging test admin). Guest token not used.

---

## 4. SS-01 result — PASS

**Route:** `POST /api/admin/sales-brain-shadow-smoke` · `{ "caseId": "SS-01" }`

| Field | Observed |
|-------|----------|
| HTTP | `200` |
| `userVisibleOff` | **`true`** |
| `readOnly` | `true` |
| `providerNetwork` | **`true`** |
| `realProviderGateReason` | `real_provider_call_ok` |
| `adminShadowDiag.geminiKeyPresent` | `true` |
| `adminShadowDiag.geminiModel` | `gemini-3.5-flash` |
| `data.shadowDebugResult.provider` | `gemini` |
| `data.shadowDebugResult.routedVia` | `admin.shadow.real_provider` |
| `data.userVisibleResponse` | Legacy orchestrator label — not raw Gemini user copy |

---

## 5. CP-02 result — PASS (optional)

**Route:** `POST /api/admin/chat-shadow-sink` · `{ "scenarioId": "CP-02" }`

| Field | Observed |
|-------|----------|
| HTTP | `200` |
| `userVisibleOff` | **`true`** |
| `sinkOnly` | **`true`** |
| `providerNetwork` | **`true`** |
| `realProviderGateReason` | `real_provider_call_ok` |
| `data.shadowDebugResult.routedVia` | `chat.shadow.real_provider` |

---

## 6. User-visible orchestrate — no real Gemini

During smoke, `POST /api/ai/chat-user-visible-orchestrate` (admin token):

| Field | Observed |
|-------|----------|
| `pilotPathActive` | `false` |
| `fallbackToLegacy` | `true` |
| `skipGemini` | `true` |

`NONGA_AI_USER_VISIBLE_ENABLED` remained `false` throughout.

---

## 7. Kill switch drill — PARTIAL

After `NONGA_AI_EMERGENCY_KILL_SWITCH=true` on revision `00079-lbc`:

| Check | Result | Notes |
|-------|--------|-------|
| SS-01 | **`providerNetwork: true`** still | **Gap:** admin SS-01 handler did not check global kill switch before invoke (fixed in **v6.8C.1**) |
| CP-02 path | Expected blocked via existing check | Not re-run separately in drill window |
| Legacy `/api/gemini/chat` | `success: true`, no `isMock` | **Gap:** staging Cloud Run image predates v6.8B `legacyGeminiSafety` — env-only update without image deploy |
| User-visible orchestrate | `fallbackToLegacy: true` | No real Gemini output |

---

## 8. Cost / usage observation (short)

- Primary paid calls: SS-01 + CP-02 during smoke window (~2 main Gemini invocations)
- Additional SS-01 call during kill switch drill (unintended — pre-v6.8C.1 gap)
- Model: `gemini-3.5-flash`, synthetic/redacted admin prompts
- Budget env caps present; no consumption tracker in runtime

---

## 9. Rollback — DONE

Final revision `nonga-staging-00080-xdg`:

| Flag | Value |
|------|--------|
| `NONGA_AI_EMERGENCY_KILL_SWITCH` | `true` |
| `NONGA_AI_ADMIN_SHADOW_REAL_PROVIDER_ENABLED` | `false` |
| `NONGA_AI_CHAT_SHADOW_REAL_PROVIDER_ENABLED` | `false` |
| `NONGA_AI_USER_VISIBLE_ENABLED` | `false` |

---

## 10. Required follow-up (post v6.8C)

1. **Deploy latest staging Cloud Run image** (includes v6.8B `9cb467d` + v6.8C.1) — env-only flag updates are insufficient without image deploy
2. **Rerun kill switch drill** on staging after deploy:
   - SS-01 → `providerNetwork: false`, `realProviderGateReason: emergency_kill_switch`
   - Legacy `/api/gemini/chat` → mock/fail-closed (`isMock: true` or no paid call)
   - User-visible orchestrate → legacy/mock only
3. **v6.8D** — real Gemini user-visible allowlist pilot (separate approval)

---

## 11. Compliance

| Rule | Status |
|------|--------|
| Production deploy | ❌ not done |
| User-visible real Gemini | ❌ not enabled |
| Secrets in repo / this doc | ❌ none |
| Firestore writes for smoke | ❌ none |
| Lead system changes | ❌ none |

---

## 12. Tests referenced (offline)

```bash
npm run test:v68c-staging-real-gemini-shadow-smoke-readiness
npm run test:v68b-gemini-safety-alignment
npm run test:v68c1-admin-shadow-kill-switch-patch
```

Manual paid API smoke: **operator execution only** — not automated from CI.
