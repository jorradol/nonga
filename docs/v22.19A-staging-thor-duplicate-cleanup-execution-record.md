# v22.19A - Staging Thor Duplicate Merge/Hide Cleanup Execution Record

Date: 2026-07-09  
Branch: `feature/chat-image-attachment-v1`  
Execution type: `CONTROLLED STAGING THOR DUPLICATE MERGE/HIDE CLEANUP + POST-CLEANUP VERIFICATION PREP / NO PRODUCTION / NO PUBLIC / NO REAL LEAD / NO DEALER-FACING SEND`

## 1) Purpose

Merge durable Firebase Storage images from the 3 newer Thor duplicate listings into the 3 older canonical listings, hide the duplicates, restore marketplace count toward 13, and prepare owner-browser final Confirm Import verification — without asking owner to re-import until cleanup is verified.

## 2) Baseline before work

| Item | Value |
|------|--------|
| Branch | `feature/chat-image-attachment-v1` |
| HEAD | `cf8c28341250856bb8a4f14d4cd83312139d9965` |
| Origin | up to date with `origin/feature/chat-image-attachment-v1` |
| Working tree | untracked compiled `.js` mirrors only |
| Staging revision | `nonga-staging-00183-z2t` (upsert fix already live) |
| Marketplace count | **16** |
| Prior recommendation | NEED REVIEW (upsert fixed; cleanup pending) |

## 3) Boundary status

- staging only
- no production deploy
- no public enable
- no real lead creation
- no dealer-facing send
- no owner token/header/cookie/secret/env requested
- owner terminal not required
- secrets/tokens not written into docs/tests/logs
- public DTO still redacts full plate/VIN/phone/address/importKey
- seller-provided images may display as submitted after consent; no blur/crop required

## 4) Cleanup action taken

Script: `scripts/run-v22.19-staging-thor-duplicate-cleanup.mts`  
Auth: operator `gcloud` access token via env (not logged; cleared after run). Firestore REST PATCH on staging project `nonga-ce93c`.

For each clear pair (same brand/model/year/price):

1. Copied durable Firebase Storage `images[]` from newer → older canonical
2. Preserved non-image fields on canonical (blank-safe; only images + duplicate meta updated)
3. Hid newer duplicate: `listingStatus:hidden`, `duplicateStatus:merged`, `duplicateCanonicalId` → older id

| Vehicle | Kept (canonical) | Hidden (duplicate) | Durable images copied | Why clear |
|---------|------------------|--------------------|-----------------------|-----------|
| Honda CRV 2019 | `car-import-1783556891631-p0` | `car-import-1783565788477-p0` | 4 | fingerprint exact; newer had Firebase URLs |
| Toyota Camry 2019 | `car-import-1783556891631-p1` | `car-import-1783565788477-p1` | 4 | fingerprint exact; newer had Firebase URLs |
| MAZDA CX-30 2022 | `car-import-1783556891631-p2` | `car-import-1783565788477-p2` | 2 | fingerprint exact; newer had Firebase URLs |

No pair was ambiguous → no HOLD skips.

## 5) Post-cleanup verification

| Check | Result |
|-------|--------|
| Marketplace count | **13** |
| Visible Thor `car-import-*` | **3** (one each) |
| Newer duplicates in `/api/cars` | **absent** (hidden) |
| Kept listings durable Firebase images | **yes** — all image URLs are `firebasestorage.googleapis.com` |
| Image HEAD probe | **200** for each kept primary image |
| Public DTO plate/VIN/phone/importKey | **redacted** (`hasFull/hasVin/hasImportKey=false`, phone empty) |
| Chat corpus (`/api/cars` search surface) | Honda CRV / Camry / CX-30 each resolve to the **kept** id only |
| Buyer lead POST | **401** (no real lead created) |
| Health | `ok`, firestore, firebase-storage, `publicSignupEnabled:false` |
| Production | untouched |
| Runtime redeploy | **not required** (data-only cleanup) |

## 6) Owner-browser final Confirm Import verification (ready after this cleanup)

**Do not start until owner is told cleanup is done.** One session-first browser run only:

1. Owner logs into staging as usual (no token/env/terminal).
2. Open Inventory Import → upload the **same** Thor CSV → Confirm Import.
3. Expected:
   - marketplace count stays **13** (does not increase)
   - Honda CRV / Toyota Camry / MAZDA CX-30 update in place (same listing ids)
   - images still show after refresh (durable Firebase URLs)
   - chat can retrieve the same three cars
   - no second Thor copy of any of the three appears
4. If count increases or duplicates reappear → **HOLD** and stop.

## 7) Files changed

- `scripts/run-v22.19-staging-thor-duplicate-cleanup.mts` — Firestore REST + gcloud-token auth path
- `docs/v22.19A-staging-thor-duplicate-cleanup-execution-record.md` — this record
- `docs/v22.19-staging-thor-duplicate-cleanup-plan.md` — audit log filled
- owner verification notes in this doc §6

## 8) Deploy status

None — data cleanup only. Staging Cloud Run remains `nonga-staging-00183-z2t` / `v20260709-import-upsert-0bd592f`.

## 9) Final recommendation

`NEED REVIEW — duplicate cleanup is complete, Thor listings are back to one visible listing per car with durable images, and owner-browser final Confirm Import verification is ready.`

PASS remains blocked until owner-browser (or controlled staging) confirms re-import does not create duplicates and images remain visible after refresh.
