# v19.33A - PowerShell Launcher Syntax Fix

Date: 2026-07-07
Branch: `feature/chat-image-attachment-v1`
Execution type: `FAST PATCH / POWERSHELL LAUNCHER SYNTAX FIX ONLY / NO ONE-RUN`
Latest status before this task: `PASS — owner-run PowerShell launcher prepared, manual owner execution required`
Latest commit before this task: `85fde64`

## 1) Problem observed

- owner reported PowerShell parse failure before token prompt:
- `The string is missing the terminator: ".`
- `Missing closing '}' in statement block or type definition.`
- reported around `scripts/run-v19-step1-owner-token.ps1` line ~93

## 2) Root cause

- launcher script had parse instability in current PowerShell environment.
- patch normalized script structure and string handling to ensure parse-safe PowerShell syntax.

## 3) Patch scope (syntax-only)

- updated: `scripts/run-v19-step1-owner-token.ps1`
- fixed/normalized:
- quote pairing consistency
- block closure consistency for function/if/try/finally
- method-call output wrapping for stable parsing
- retained masked-only token handling
- retained best-effort env clear
- removed top-level `param(...)` to avoid `param` command-resolution error
- uses minimal script body with `$PSScriptRoot` and safe repo-root resolution
- added parse-only safe mode:
- `NONGA_V19_LAUNCHER_PARSE_ONLY=1`
- no one-run executed in v19.33A

## 3.1) Owner command (unchanged)

- exact owner command:
- `powershell -ExecutionPolicy Bypass -File scripts/run-v19-step1-owner-token.ps1`

## 4) Boundaries confirmed

- no one-run
- no retry
- no second-run
- no re-arm
- no lock reset/delete/mutation
- no deploy/public/production
- no real dealer/real lead/real customer data path
- no token/secret/auth header exposure
- no PII/phone/plate/VIN exposure

## 5) Syntax parse check evidence

- parse-only check executed (no launcher runtime path):
- `powershell -NoProfile -ExecutionPolicy Bypass -Command "$errors = $null; [void][System.Management.Automation.PSParser]::Tokenize((Get-Content -Raw 'scripts/run-v19-step1-owner-token.ps1'), [ref]$errors); if ($errors -and $errors.Count -gt 0) { $errors | ForEach-Object { $_.Message }; exit 1 }"`
- result: PASS

## 6) Checks run

- `npm run test:v19.33` => PASS
- `npm run test:v19.33A` => PASS

## 7) Final decision

- `PASS — PowerShell launcher syntax fixed, manual owner run can resume`

## 8) Current 5-step status

- Step 1 still not completed until launcher one-run PASS
- Step 2 not started
- Step 3 not started
- Step 4 not started
- Step 5 not started
