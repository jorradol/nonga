# v14.2A Owner-Only Quality Evidence Surface / Thai UX Tuning Marker

## 1) Execution type

owner-only quality evidence surface patch: yes  
code/docs/static validation only: yes  
Gemini run: no  
one-run click: no  
retry: no  
second run: no  
provider network call: no  
production deploy: no  
public route activation: no  
buyer-facing AI release: no  
real lead sending: no  
real customer data / PII: no  
phone/plate/VIN exposure: no  
secret/token/API key exposure: no

## 2) Purpose

v14.2 tuned Thai UX quality guidance in prompt contract.  
v14.2A adds explicit owner/admin evidence markers so runtime diagnostics can prove that Thai UX tuning slice is active before asking for a fresh owner approval to run the next owner-only Gemini quality retest.

This slice is evidence-only and does not execute provider traffic.

## 3) Evidence marker contract (static/sanitized only)

Added/confirmed marker fields:

- `thaiUxTuningSliceId = "v14.2"`
- `thaiUxTuningActive = true`
- `targetAnswerLengthGuidance = "4-7-sentences"`
- `safeConfirmationStepWordingActive = true`
- `leadPiiCueGuardActive = true`
- `phoneEchoGuardActive = true`

All fields are static/sanitized status fields only.

Not exposed:

- full prompt text
- raw token/secret/api key
- full UID/email
- phone number, plate, VIN
- real customer data

## 4) Surface wiring covered in v14.2A

Runtime/provider source marker export:

- `src/services/ai/salesBrainUserVisibleRealProvider.ts`

Server diagnostic object:

- `src/services/ai/salesBrainServerUserVisibleOrchestrationBridge.ts`
- `userVisibleRuntimeDiagnostic` now includes Thai UX marker fields plus existing v14.1 guard markers

Client contract/type:

- `src/services/ai/chat/chatUserVisibleOrchestrateClient.ts`

Owner/admin evidence helper parser:

- `src/components/admin/ownerOneRunEvidence.ts`

Owner evidence UI (owner-only helper panel):

- `src/components/admin/OwnerFirebaseTokenHelperPanel.tsx`

## 5) Guardrails preserved (must remain true)

v14.2A keeps v14.1/v14.2 guard behavior active:

- no lead/PII cue collection in chat
- no phone echo in chat
- safe confirmation-step wording only
- no finance guarantee language
- no hallucinated vehicle facts beyond listing context
- no secret/token/PII in evidence surface

## 6) Boundary statement

This is not a retest and not a runtime execution slice.

- no Gemini run
- no one-run click
- no retry/second run
- no provider network call
- no deploy/public activation

## 7) Next step gate

After markers are visible and validation passes, request fresh owner approval first, then proceed with controlled owner-only Gemini quality retest.

## 8) Final recommendation enum (v14.2A)

- `READY FOR CONTROLLED STAGING DEPLOY OF v14.2A EVIDENCE SURFACE — NO RETEST YET`
- `READY FOR OWNER MANUAL QUALITY RETEST — FRESH APPROVAL REQUIRED`
- `HOLD — EVIDENCE SURFACE INCOMPLETE`
- `HOLD — GUARDRAIL REGRESSION DETECTED`
- `HOLD — TEST FAILURE`
- `HOLD — SECRET/PII/REAL LEAD RISK DETECTED`
