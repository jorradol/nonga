# v20.0 - Step 2 Authenticated Allowlist Context Readiness Packet

Date: 2026-07-07
Branch: `feature/chat-image-attachment-v1`
Execution type: `STEP 2 AUTHENTICATED ALLOWLIST CONTEXT READINESS PACKET ONLY / NO EXECUTION / NO DEPLOY / NO ONE-RUN`
Expected HEAD before work: `d8319b8`

## 1) Continuity and status

- v19.39 final decision: `PASS — controlled owner-only staging trial completed, Step 1 completed`
- v19.40 final decision: `PASS — Step 2 limited private pilot readiness packet closed, no execution`
- v19.41 final decision: `HOLD — missing authenticated trusted/allowlisted run context (owner Firebase token absent), therefore exact one-run cannot be executed safely under v19.41 constraints`
- v19.41 run attempt count: `0`
- current 5-step status:
  - Step 1 completed
  - Step 2 not started
  - Step 3 not started
  - Step 4 not started
  - Step 5 not started

## 2) What exact context is missing

Missing context for Step 2 controlled run is:

- authenticated Bearer Firebase ID token in owner-controlled terminal session (not in chat/repo/docs)
- trusted auth resolution for `getServerAuthContext(req)` on server side
- allowlisted UID alignment for user-visible gate (`NONGA_AI_USER_VISIBLE_ALLOWLIST_UIDS`)

Without this context, route execution must fail-closed and Step 2 must not start.

## 3) Static/read-only findings

### 3.1 Trusted auth expectation on Step 2 route

- route target for Step 2 remains: `POST /api/ai/chat-user-visible-orchestrate`
- route handler resolves trusted auth only from server auth context:
  - `getServerAuthContext(req)` from `src/server/serverAuthContext.ts`
- missing Bearer token path returns auth failure (`Missing Firebase ID token`)

### 3.2 Firebase token consumption and verification path

- Bearer token is extracted from HTTP auth header (Bearer scheme) in server auth context
- token is verified by:
  - `verifyFirebaseIdToken(token)` (Firebase Admin or dev-mock map)
- token handling remains server-side and must never be printed into chat/docs/repo

### 3.3 Allowlist gate evaluation

- allowlist env key: `NONGA_AI_USER_VISIBLE_ALLOWLIST_UIDS`
- gate logic uses UID + allowlist + runtime prerequisites
- expected blocked reasons if context is missing/misaligned:
  - `guest_uid_missing`
  - `allowlist_empty`
  - `uid_not_allowlisted`
- masked diagnostics already exist (`requestUidMasked`, `allowlistMasked`, `allowlistMatch`)

### 3.4 Safe owner-owned token holding mechanism

Recommended mechanism for v20.1 pre-run:

- keep token only in owner terminal session env var:
  - `NONGA_OWNER_FIREBASE_ID_TOKEN`
- do not place token in:
  - chat
  - docs
  - git-tracked files
  - `.env` committed files
  - logs/reports
- token lifetime should remain one-session and owner-controlled only

## 4) Owner-safe action guide (no token exposure)

1. Owner signs in locally and obtains Firebase ID token by owner-controlled method outside chat
2. Owner places token into same terminal session env var only (`NONGA_OWNER_FIREBASE_ID_TOKEN`)
3. owner must not paste token in chat
4. token must not be committed
5. token must not be logged
6. token must be masked in all reports
7. Owner keeps execution terminal private and short-lived
8. Owner clears session variable after controlled run window closes

## 5) Masked-only verification plan (no value exposure)

Allowed masked checks before v20.1:

- token present: yes/no
- token length nonzero: yes/no
- JWT-like shape check: yes/no (three dot-separated base64url parts)
- no newline: yes/no
- no leading/trailing whitespace: yes/no
- token print policy: always `***MASKED***`

Forbidden:

- printing raw token
- printing Authorization header value
- storing token in docs/examples/repo

## 6) Private allowlist assurance plan

Before v20.1 execution:

- confirm trusted auth context returns UID (masked in evidence)
- confirm UID membership against `NONGA_AI_USER_VISIBLE_ALLOWLIST_UIDS`
- capture only masked diagnostics:
  - gate blocked reason
  - request UID masked
  - allowlist masked
  - allowlist match boolean/count

No raw UID/token dump is allowed.

## 7) v20.1 pre-run checks (must all pass)

- path/repo/branch/HEAD/working tree exact match
- v19.39 PASS continuity confirmed
- v19.40 PASS continuity confirmed
- v19.41 HOLD continuity confirmed with blocker unchanged
- owner terminal session has authenticated token context present and shape-valid (masked checks only)
- target route fixed exactly to `POST /api/ai/chat-user-visible-orchestrate`
- staging only + private allowlist only
- no prior v20.1 run, no retry, no second-run, no re-arm plan
- no public/production/real dealer/real lead/real customer data/PII

If any check fails: HOLD and do not execute.

## 8) Fail-closed policy when context absent

Fail-closed response for v20.1 pre-run must be:

- do not execute route
- do not attempt fallback live run
- do not retry
- return HOLD reason precisely (for example: authenticated token context absent)

## 9) Future v20.1 owner approval phrase (draft only, not for v20.0 execution)

`FINAL AUTHORIZE v20.1 STEP 2 LIMITED PRIVATE PILOT CONTROLLED STAGING RUN / AUTHENTICATED OWNER ALLOWLIST CONTEXT PRESENT / ROUTE POST /api/ai/chat-user-visible-orchestrate ONLY / EXACTLY ONE RUN / NO RETRY / NO SECOND-RUN / NO RE-ARM / STAGING ONLY / PRIVATE ALLOWLIST ONLY / NO PUBLIC / NO PRODUCTION / NO REAL DEALER / NO REAL LEAD / NO REAL CUSTOMER DATA / NO PII PHONE PLATE VIN / NO TOKEN VALUE IN CHAT OR REPO`

This phrase is future-only draft wording for v20.1.
It is not approval for v20.0.

## 10) Explicit v20.0 boundary

- v20.0 is readiness packet only
- v20.0 does not execute Step 2 route
- v20.0 does not deploy
- v20.0 does not run one-run
