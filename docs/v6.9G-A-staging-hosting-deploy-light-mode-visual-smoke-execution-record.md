# v6.9G-A — Staging Hosting Deploy + Light Mode Visual Smoke Execution Record

**สถานะ:** **PARTIAL** — staging Hosting deploy **PASS**; public route smoke **PASS**; `/profile` auth-gated; mobile drawer not tested; generic `npm run build` guard issue **unresolved**  
**วันที่:** 2026-06-17  
**Git baseline (deployed):** `a9943ac2198764e3b6eecca5ce617a1928366df2` (`a9943ac`)  
**Runtime patch deployed:** `ed63cb3d550a2cd78a3ff723271c3fb2621ac562` (`ed63cb3`) — v6.9F-A light mode contrast quick wins  
**Branch:** `feature/chat-image-attachment-v1`  
**Deploy/smoke run:** no commit during deploy/smoke; no runtime code changes

> Staging hosting-only deploy + light-mode visual smoke for v6.9F-A targets. No secrets, tokens, API keys, Authorization headers, raw prompts, or PII in this document. **Not production-ready. Production pilot still NOT READY.**

---

## 1. Overall verdict

| Item | Result |
|------|--------|
| Overall result | **PARTIAL** |
| `npm run build:staging:hosting` | **PASS** |
| Firebase Hosting deploy only | **PASS** |
| Staging URL | `https://nonga-ce93c.web.app` |
| Cloud Run | **Not touched** |
| Production | **Not touched** |
| Generic `npm run build` | **FAIL** (known unresolved Firebase production config guard / placeholder `apiKey`) |
| Commit during deploy/smoke | **NO** |
| Runtime code changes during deploy/smoke | **NO** |
| **v6.9 Controlled Production Pilot** | **NOT READY** |

---

## 2. Baseline verification

| Field | Value |
|-------|-------|
| Repo | `https://github.com/jorradol/nonga.git` |
| Branch | `feature/chat-image-attachment-v1` |
| HEAD | `a9943ac2198764e3b6eecca5ce617a1928366df2` (`a9943ac`) |
| Runtime patch (v6.9F-A) | `ed63cb3d550a2cd78a3ff723271c3fb2621ac562` (`ed63cb3`) |
| Local HEAD | `a9943ac` (= `origin/feature/chat-image-attachment-v1`) |
| Working tree (at deploy/smoke time) | clean |
| Working tree (at record time) | clean (docs/script pending separate commit) |
| Commit during deploy/smoke | **NO** |
| Runtime code changes during deploy/smoke | **NO** |
| Staging project | `nonga-ce93c` |
| Staging host | `https://nonga-ce93c.web.app` |

---

## 3. Build result

| Check | Result | Notes |
|-------|--------|-------|
| `npm run build:staging:hosting` | **PASS** | Correct staging Hosting build path (not generic `npm run build`) |
| Env source | Cloud Build substitution | No local `.env.staging` on deploy machine |
| Firebase guard | **PASS** | `[vite-firebase-guard] PASS production Firebase web config` |
| Dist bundle verify | **PASS** | `PASS dist bundle verified` |
| Built assets | See below | Local build matches live staging bundle |

**Built / live assets:**

| Asset | Path |
|-------|------|
| JS bundle | `dist/assets/index-CZUJav8l.js` |
| CSS bundle | `dist/assets/index-v8LQYv3A.css` |

**Known separate issue:** generic `npm run build` remains **FAIL** due to Firebase production config guard / placeholder `apiKey` (documented in v6.9G investigation). This slice correctly used `build:staging:hosting` only — not a blocker for this Hosting deploy.

---

## 4. Deploy result

| Item | Value |
|------|--------|
| Command | `npx -y firebase-tools@latest deploy --only hosting --project nonga-ce93c` |
| Scope | **Firebase Hosting only** — no functions, no Cloud Run |
| Project | `nonga-ce93c` |
| URL | `https://nonga-ce93c.web.app` |
| Files uploaded | **6 files** from `dist` (**3 new/changed** assets) |
| Exit code | **0** — Deploy complete |
| Production target | **Not selected / not touched** |

**Live bundle references (verified post-deploy):**

- `/assets/index-CZUJav8l.js`
- `/assets/index-v8LQYv3A.css`

---

## 5. Cloud Run confirmation

Read-only checks before and after deploy — **unchanged**:

| Service | Region | Revision | Status |
|---------|--------|----------|--------|
| `nonga-staging` | asia-southeast1 | `nonga-staging-00154-q7l` | **Unchanged** |
| `nonga-api` | asia-southeast1 | `nonga-api-00003-fg4` | **Unchanged** |

No Cloud Run deploy, revision update, or service config change in this slice.

---

## 6. Smoke results table

Light-mode testing: forced light mode per route by removing `html.dark` after navigation (app defaults to dark on cold load; theme not persisted to localStorage).

| Route | HTTP | Status | Notes |
|-------|------|--------|-------|
| `/profile` | 200 | **PARTIAL** | Auth-gated — gate shell readable; full profile UI not verified without login |
| `/car-vision` | 200 | **PASS** | Route correct; H1 and body readable in light mode; no chat fallback |
| `/viral-captions` | 200 | **PASS** | Usage widget + hook chips readable; form labels readable |
| `/home` | 200 | **PASS** | Trust panel present; no regression vs v6.9B |
| `/login` | 200 | **PASS** | Form labels and CTA readable |
| `/search` | 200 | **PASS** | Hero + filter chips readable |
| `/onboarding` | 200 | **PASS** | Personality picker loads |
| `/seo-landing` | 200 | **PASS** | SEO builder UI; no regression |
| `/car-post-generator` | 200 | **PASS** | Dark form island intentional |

**Console/runtime:** No "Application error" overlays observed. Full console log capture not instrumented.

**Mobile drawer:** **Not tested** (viewport resize not in scope).

---

## 7. Light-mode visual findings

| Area | v6.9G-A staging result |
|------|------------------------|
| **CarVisionDashboard** (`/car-vision`) | **P0 resolved** — light mode headings/body readable on staging |
| **CaptionEngineDashboard** (`/viral-captions`) | **P0 resolved** — usage widget + hook chips readable |
| **Header** (tested routes) | **Improved** — nav/search readable in light mode |
| **UserProfileView** (`/profile`) | **Not fully verified** — auth gate blocks profile internals |
| **SettingsCard** | **Not verified** — inside auth-gated profile |
| `/home` trust panel (v6.9B) | **No regression** |
| `/search`, `/login`, `/seo-landing` | **OK** |
| `/car-post-generator` dark island | **Intentional** — acceptable |

**Deferred (out of v6.9F-A scope):** dealer/admin contrast (v6.9F-B), authenticated profile deep contrast, theme persistence on cold load.

---

## 8. Auth-gated limitations

| Route | Limitation |
|-------|------------|
| `/profile` | Requires staging test account or authenticated smoke later — **no fake pass** for profile internals |
| Other routes | Publicly accessible |

No credentials used during smoke. Mobile drawer not tested.

---

## 9. Why PARTIAL (not PASS / not FAIL)

**Not PASS because:**

- `/profile` full UI not verified (auth-gated)
- Mobile drawer not tested
- Generic `npm run build` Firebase production config guard / placeholder `apiKey` issue remains **unresolved**

**Not FAIL because:**

- Staging Hosting deploy **succeeded**
- All smoke routes HTTP **200**
- Public target routes (`/car-vision`, `/viral-captions`, regression routes) **PASS**
- No Cloud Run or production impact

---

## 10. Safety confirmations

| Item | Result |
|------|--------|
| Runtime code changes (deploy/smoke) | **NO** |
| Commit during deploy/smoke | **NO** |
| Cloud Run | **Not touched** |
| Production deploy | **NO** |
| Production changes | **NO** |
| Env / secrets / deploy config | **Unchanged** |
| AI runtime / Gemini provider | **Not touched** |
| Guard / fallback logic | **Not touched** |
| Firestore write | **Not touched** |
| Lead system / DealerLeads | **Not touched** |
| Payment / invoice / boost logic | **Not touched** |
| Secret / token / API key logged | **NO** |
| Full prompt / raw Gemini output in docs | **NO** |
| **v6.9 Controlled Production Pilot** | **NOT READY** |

---

## 11. Next recommended gates

| Gate | Notes |
|------|-------|
| Authenticated `/profile` smoke | Staging test account if available |
| Mobile drawer smoke | Light-mode nav drawer + profile label |
| v6.9F-B dealer/admin contrast | After approval |
| Build gate docs clarification | Optional — distinguish `npm run build` vs `build:staging:hosting` |

---

## 12. Commands run (reference)

| Command | Result |
|---------|--------|
| `git status` / `git rev-parse HEAD` | Clean; `a9943ac` |
| `npm run build:staging:hosting` | **PASS** |
| `npx -y firebase-tools@latest deploy --only hosting --project nonga-ce93c` | **PASS** |
| `gcloud run services describe` (read-only) | Revisions unchanged |
| HTTP HEAD all 9 smoke routes | All **200** |
| Browser smoke on `https://nonga-ce93c.web.app` | See §6 |

---

*End of v6.9G-A staging hosting deploy + light mode visual smoke execution record.*
