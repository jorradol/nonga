# v22.53 — Controlled Real Lead Pilot Activation

Date: 2026-07-10  
Branch: `feature/chat-image-attachment-v1`  
Execution type: `CONTROLLED REAL LEAD PILOT ACTIVATION / STAGING ONLY / EXISTING FIRESTORE LEAD SYSTEM / THOR AUTO / TWO APPROVED COROLLAS / MAX 3 REAL LEADS / MAX 7 DAYS / NO AUTOMATIC NOTIFICATION / NO PRODUCTION`

## 1) Executive summary

Server-side Pilot guard added (listing allowlist, Thor/`nonga-dealer` ownership, max 3 created, 7-day window, durable counter). Staging Capture turned **ON** with Firestore backend. Public Signup remains **OFF**. No Lead created during activation. No automatic notification. Real Lead Pilot is **ACTIVE** within Owner limits.

**Final recommendation: PILOT ACTIVE**

---

## 2) Owner authorization scope

Authorized: Staging Capture ON · Thor Auto · Corolla 2020/2021 only · max 3 real Leads · max 7 days · manual queue · emergency OFF.

Not authorized: Production · Public Signup · notifications · new Dealer/Listing · legacy Lead UI · auto-extend · Owner secrets.

---

## 3) Pilot configuration (server env — no secrets / no PII)

| Key | Value |
|-----|--------|
| `NONGA_LEAD_DATA_BACKEND` | `firestore` |
| `NONGA_LEAD_CAPTURE_ENABLED` | `true` |
| `NONGA_LEAD_PILOT_LISTING_IDS` | two approved Corolla listing ids (server-only; not printed in public docs) |
| `NONGA_LEAD_PILOT_DEALER_IDS` | `nonga-dealer` |
| `NONGA_LEAD_PILOT_MAX_CREATED` | `3` |
| `NONGA_LEAD_PILOT_STARTED_AT` | activation UTC |
| `NONGA_LEAD_PILOT_EXPIRES_AT` | start + 7 days |
| `NONGA_LEAD_PILOT_COUNTER_ID` | `controlled-real-lead-v2253` |
| `VITE_NONGA_PUBLIC_SIGNUP_ENABLED` | `false` |
| `NONGA_LEAD_PILOT_TEST_RELAX_MAX` | **unset** on Staging |

Duplicate replay does not increment the Pilot counter. Fourth create is blocked server-side even if Capture remains ON.

---

## 4) Emergency Capture OFF

```text
gcloud run services update nonga-staging \
  --project=nonga-ce93c \
  --region=asia-southeast1 \
  --update-env-vars=NONGA_LEAD_CAPTURE_ENABLED=false
```

Or remove `NONGA_LEAD_CAPTURE_ENABLED`. Keep Firestore backend. Do not delete real Leads without separate Owner deletion authorization.

---

## 5) Automatic stop conditions

Turn Capture OFF immediately if: count reaches 3 · window expires · Health/backend inconsistent · duplicate real Leads · wrong Dealer access · recipient override · PII leak · notification triggered · Signup ON · marketplace/Thor regression · Production touched · Owner stop request.

Preserve legitimate Lead records; no automatic hard-delete of real Leads.

---

## 6) Buyer / Dealer handling

- Buyer: existing Chat consent modal + auth + `POST /api/buyer-leads` when Capture ON
- Dealer: existing masked Seller queue only; manual review; no auto notify; no export
- Reports: aggregate/masked only — no Buyer PII in repo/logs/chat

---

## 8) Activation result

| Item | Result |
|------|--------|
| Cloud Run revision | **`nonga-staging-00201-tm4`** @ 100% |
| Image | `v20260710-lead-pilot-v2253-88bd4b9` |
| Prior Capture-OFF revision | `nonga-staging-00199-7b9` |
| `leadDataBackend` | firestore |
| `dataBackend` / `imageBackend` | firestore / firebase-storage |
| `leadCaptureEnabled` | **true** |
| `leadPilotConfigured` / `leadPilotActive` | **true** / **true** |
| `leadPilotMaxCreated` | **3** |
| `leadPilotExpiresAt` | `2026-07-17T05:59:34.799Z` |
| `publicSignupEnabled` | **false** |
| Initial Pilot Lead count | **0** (interest 0/0; no create during activation) |
| Hosting | Unchanged `assets/index-Bsnqa-MN.js` |
| Production | Untouched |

Note: first deploy used `--env-vars-file` which replaced non-secret env; immediately restored prior Staging env via `--update-env-vars` while keeping Capture ON + Pilot config. Final Health consistent.

## 9) Tests

| Suite | Result |
|-------|--------|
| `test:v22.53-static` | PASS |
| `test:v22.52-static` regression | PASS |

## 10) Final recommendation

**PILOT ACTIVE**
