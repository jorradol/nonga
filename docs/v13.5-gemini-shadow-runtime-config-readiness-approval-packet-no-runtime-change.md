# v13.5 Gemini Shadow Runtime Config Readiness Approval Packet — No Runtime Change / No Gemini Execution

## Status

APPROVAL PACKET ONLY — no runtime change, no deploy, no Gemini execution, no smoke endpoint call

## Baseline

- Repo: `https://github.com/jorradol/nonga.git`
- Branch: `feature/chat-image-attachment-v1`
- Latest accepted pushed commit before v13.5: `e77e3fa32fac511051d513db170a8fba79470d0f` (`e77e3fa`)
- Baseline checks:
  - branch = `feature/chat-image-attachment-v1`
  - local HEAD = origin HEAD
  - working tree clean

## v13.4 Source Status

v13.4 SAFE HOLD / CLOSED / PUSHED with final recommendation:

- `HOLD — prerequisites missing or secret/runtime risk detected`

## Current Blocker

v13.4 stopped before dispatch because pre-execution gate did not pass:

- Gemini provider config present: no
- admin-only smoke enabled/config present: no
- manual smoke enabled/config present: no
- real provider shadow enabled/config present: no

Safety confirmation:

- This was a safe HOLD before dispatch.
- Real Gemini run count remained 0/1.
- No smoke endpoint call was made.
- No Gemini execution occurred.

## Runtime Config Required For Future v13.6

Based on static repo inspection (no runtime change in v13.5), staging runtime/server must have:

- provider secret presence:
  - `GEMINI_API_KEY` (provider secret only)
- admin token remains present:
  - `NONGA_ADMIN_API_TOKEN`
- runtime provider prerequisite flags:
  - `NONGA_AI_PROVIDER=gemini`
  - `NONGA_AI_MODE=high` (or approved non-off mode)
  - `NONGA_AI_FIRST_ENABLED=true`
  - `NONGA_AI_SHADOW_MODE_ENABLED=true`
  - `NONGA_AI_BUDGET_DAILY_LIMIT` present
  - `NONGA_AI_BUDGET_MONTHLY_LIMIT` present
- admin-only shadow proof gates:
  - `NONGA_AI_ADMIN_SHADOW_MANUAL_SMOKE_ENABLED=true`
  - `NONGA_AI_ADMIN_SHADOW_MANUAL_SMOKE_CASE_ID=SS-01`
  - `NONGA_AI_ADMIN_SHADOW_REAL_PROVIDER_ENABLED=true`

## Secret / Non-Secret Config Separation

Secret config:

- `GEMINI_API_KEY`
- `NONGA_ADMIN_API_TOKEN`

Non-secret boolean/runtime flags:

- `NONGA_AI_ADMIN_SHADOW_MANUAL_SMOKE_ENABLED`
- `NONGA_AI_ADMIN_SHADOW_REAL_PROVIDER_ENABLED`
- `NONGA_AI_SHADOW_MODE_ENABLED`
- `NONGA_AI_FIRST_ENABLED`
- `NONGA_AI_PROVIDER`
- `NONGA_AI_MODE`
- `NONGA_AI_BUDGET_DAILY_LIMIT`
- `NONGA_AI_BUDGET_MONTHLY_LIMIT`
- `NONGA_AI_ADMIN_SHADOW_MANUAL_SMOKE_CASE_ID`

## Gemini API Key Handling Rules

- Gemini API key is provider secret only.
- Gemini API key must not be used as `NONGA_ADMIN_API_TOKEN`.
- Gemini API key must not be used as NONGA_ADMIN_API_TOKEN.
- Do not print `GEMINI_API_KEY`.
- Do not commit `GEMINI_API_KEY`.
- Do not place `GEMINI_API_KEY` in docs, logs, reports, screenshots, or command history.

## Admin-Only Smoke Enablement Rules

- Enablement is for admin-only shadow proof boundary only.
- Enabling admin-only smoke config must not enable buyer-facing AI.
- Enabling admin-only smoke config must not activate public AI routes.
- Enabling admin-only smoke config must not send real leads.

## Manual Smoke Enablement Rules

- Manual smoke must remain case-bound to sanitized test case (`SS-01`) only.
- Manual smoke enablement must not allow unrestricted repeated runs.
- Manual smoke enablement must remain staging-only and admin-only.

## Real Provider Shadow Enablement Rules

- Real provider shadow flag is only for controlled admin proof path.
- Real provider shadow enablement must not expose provider secret values.
- Real provider shadow enablement must not toggle buyer-facing/public AI behavior.

## Staging-Only Boundary

- staging only
- no production
- no public route
- no buyer-facing AI
- no real leads

## Owner Approval Required For v13.6

v13.6 runtime config setup is allowed only with fresh owner approval.

## Exact Approval Wording Needed From Owner

FINAL APPROVAL FOR v13.6:  
I approve staging-only Gemini shadow runtime config setup for admin-only proof.  
Set only the required staging runtime/server env values for Gemini provider presence and admin-only shadow smoke enablement.  
Do not expose secrets.  
Do not use Gemini API key as NONGA_ADMIN_API_TOKEN.  
Do not enable buyer-facing AI.  
Do not activate public route.  
Do not send real leads.  
Do not touch production.  
Do not execute Gemini in v13.6 unless explicitly included as a separate one-run approval.  
Capture only sanitized evidence and stop if prerequisites are unclear.

## Non-Secret Runtime Config Templates

Do not run this command in v13.5.  
This is a non-secret template only.  
Use repo-confirmed env names only.  
Runtime config update requires explicit owner approval in v13.6.

```bash
# Placeholder only — do not run in v13.5
# Runtime config update requires fresh owner approval in v13.6
gcloud run services update nonga-staging \
  --region asia-southeast1 \
  --set-env-vars \
  GEMINI_API_KEY="<REDACTED_PROVIDER_SECRET>",\
  NONGA_AI_ADMIN_SHADOW_MANUAL_SMOKE_ENABLED="true",\
  NONGA_AI_ADMIN_SHADOW_MANUAL_SMOKE_CASE_ID="SS-01",\
  NONGA_AI_ADMIN_SHADOW_REAL_PROVIDER_ENABLED="true",\
  NONGA_AI_PROVIDER="gemini",\
  NONGA_AI_MODE="high",\
  NONGA_AI_FIRST_ENABLED="true",\
  NONGA_AI_SHADOW_MODE_ENABLED="true"
```

## Stop Conditions

Stop with HOLD immediately if any occurs:

- runtime/secret ownership unclear
- repo-confirmed env names become unclear
- any action would expose secret
- any step would require v13.5 runtime change/deploy/endpoint call
- any request would touch production/public/buyer-facing path

## Future v13.6 Sequence If Approved

1. Confirm repo clean and local = origin
2. Confirm fresh owner approval wording for v13.6
3. Static-confirm exact required env variable names from repo/docs
4. Confirm `NONGA_ADMIN_API_TOKEN` remains present without printing value
5. Confirm Gemini provider secret source exists without printing value
6. Set only required staging runtime/server env values
7. Do not touch production
8. Do not enable buyer-facing AI
9. Do not call smoke endpoint unless fresh one-run Gemini execution approval is included
10. Capture sanitized runtime config readiness evidence
11. Record docs/test/package only
12. Commit/push sanitized readiness record

## Guardrail Confirmation

- no runtime change
- no deploy
- no Gemini execution
- no smoke endpoint call
- no `/api/gemini/*`
- no production
- no public route
- no buyer-facing AI
- no real leads
- no token/API key exposed

## Recommendation

`READY FOR OWNER APPROVAL TO PROCEED TO v13.6 STAGING GEMINI SHADOW RUNTIME CONFIG SETUP`
