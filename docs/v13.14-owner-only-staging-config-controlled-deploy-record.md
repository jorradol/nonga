# v13.14 Owner-Only Staging Config + Controlled Deploy Record

## 1) Execution type

controlled staging deploy: yes  
owner-only staging config preparation: yes  
staging only: yes  
production: no  
public route activation: no  
buyer-facing release: no  
real lead sending: no  
real PII: no  
Gemini execution: no  
smoke endpoint call: no  
automatic Gemini retry: no  
secret exposure: no

## 2) Baseline

- branch: `feature/chat-image-attachment-v1`
- baseline commit before execution: `bb9603f`
- local/origin baseline check: synced before deploy actions
- pre-check tests passed before deploy:
  - `npm run test:v13.12`
  - `npm run test:v13.13`
- v13.12 patch presence confirmed:
  - owner-only controlled UX path exists
  - guard-before-dispatch exists
  - post-check + deterministic fallback exists
  - no automatic retry path exists

## 3) Deploy/config decision

- Decision: **Cloud Run staging-only deploy** (server runtime path required for v13.12 owner-only controlled UX patch).
- Hosting-only deploy was not selected because the relevant patch path is server-side (`salesBrain*` runtime route path).
- Runtime config decision: **no staging runtime config change applied in v13.14**.
  - Reason: owner-only UX gate remains safe by default-off when flag is missing.
  - Reason: avoiding unnecessary runtime mutation reduces activation risk.

## 4) Runtime config masked evidence

### 4.1 Before deploy (masked status)

- revision: `nonga-staging-00158-lzd`
- service URL: staging Cloud Run URL (masked reference kept)
- `NONGA_AI_OWNER_ONLY_CONTROLLED_UX_ENABLED`: missing
- `NONGA_AI_USER_VISIBLE_REAL_PROVIDER_ENABLED`: missing
- `NONGA_AI_USER_VISIBLE_ENABLED`: missing
- `NONGA_AI_PROVIDER`: present (length masked only)
- `NONGA_AI_MODE`: present (length masked only)
- `NONGA_AI_BUDGET_DAILY_LIMIT`: present (length masked only)
- `NONGA_AI_BUDGET_MONTHLY_LIMIT`: present (length masked only)

### 4.2 After deploy (masked status)

- revision: `nonga-staging-00159-8dw`
- image: `asia-southeast1-docker.pkg.dev/nonga-ce93c/nonga-staging/nonga-staging:v13.14-owner-only-bb9603f`
- `NONGA_AI_OWNER_ONLY_CONTROLLED_UX_ENABLED`: missing
- `NONGA_AI_USER_VISIBLE_REAL_PROVIDER_ENABLED`: missing
- `NONGA_AI_USER_VISIBLE_ENABLED`: missing
- `NONGA_AI_PROVIDER`: present (length masked only)
- `NONGA_AI_MODE`: present (length masked only)
- `NONGA_AI_BUDGET_DAILY_LIMIT`: present (length masked only)
- `NONGA_AI_BUDGET_MONTHLY_LIMIT`: present (length masked only)

Config mutation result:

- runtime config changed in staging: no
- owner-only controlled UX remained default-off posture: yes

## 5) Deploy scope

- scope: staging Cloud Run service `nonga-staging` only
- project: `nonga-ce93c`
- region: `asia-southeast1`
- build path: `cloudbuild.v53f.yaml`
- built image tag: `v13.14-owner-only-bb9603f`
- traffic result: latest revision serving 100% on staging service

## 6) Post-deploy non-Gemini verification

Only non-Gemini/non-smoke/non-lead checks were performed:

- `GET /api/health` -> `200`
- `GET /api/cars?limit=1` -> `200`
- unauthenticated `POST /api/ai/chat-user-visible-orchestrate` -> `401` (auth boundary block before owner route usage)

Additional non-provider stability notes:

- staging revision rolled forward successfully and served traffic without auth/health regression.
- verification set intentionally excluded any provider-dispatch route and any lead/PII mutation route.
- no customer-facing activation step was included in verification checklist.

## 7) Owner-only gate readiness evidence

- owner-controlled env flag remained missing (default-off behavior preserved).
- unauthenticated request to orchestrate route rejected (`401`) showing boundary block on non-owner/non-auth access.
- local static gate behavior already validated by `test:v13.12` (owner-only eligibility logic + deterministic boundaries + no retry).

## 8) No Gemini execution evidence

- no Gemini/provider dispatch endpoint was called in this round.
- no admin shadow smoke/Gemini smoke command executed.
- verification set restricted to health/data/auth-boundary checks only.

## 9) No smoke endpoint evidence

- no smoke endpoint invocation command was executed.
- no `/api/admin/...smoke...` dispatch command was run.

## 10) No lead evidence

- no lead submission endpoint was called.
- no lead flow mutation path was invoked.
- no runtime code change to lead sending flow in this round.

## 11) No PII evidence

- no real phone values used.
- no real plate/VIN values used.
- no real customer PII used in checks.
- evidence kept as status codes and masked presence/length only.

## 12) No public/production evidence

- deployment target was staging Cloud Run service only.
- production deployment was not executed.
- public route activation was not executed.
- buyer-facing release activation was not executed.

## 13) Rollback readiness

Rollback-ready actions (if needed in next round):

- disable owner-only controlled UX flag on staging (if later enabled).
- keep deterministic fallback path as active safe baseline.
- deploy previous known-good staging image revision if rollback required.
- no lead flow change rollback needed (lead flow unchanged).
- no production rollback needed (production untouched).
- no secret deletion required.

Rollback execution order (future-only):

1) freeze owner-only activation attempt on staging,  
2) switch gate posture to deterministic-safe baseline,  
3) re-point staging traffic to previous known-good revision if required,  
4) rerun non-Gemini verification (`/api/health`, `/api/cars`, auth-boundary block),  
5) publish rollback evidence packet with masked runtime flag status.

## 14) Risk review

| Risk | Current status | Mitigation in this execution |
| --- | --- | --- |
| wrong staging target | controlled | explicit project/region/service scoped to staging only |
| owner gate bypass | controlled | default-off gate posture + auth boundary check (401) |
| public accidental exposure | controlled | no public route activation, no buyer-facing release |
| unintended Gemini execution | controlled | no provider/smoke dispatch commands executed |
| secret exposure | controlled | masked evidence only; no secret/token value recorded |
| PII leak | controlled | no real PII input used in verification |
| real lead sent unintentionally | controlled | no lead endpoints invoked |
| retry/cost overrun | controlled | v13.12 no-retry test revalidated pre-deploy |
| unsafe output | controlled | provider path not executed in this round |
| rollback gap | controlled | rollback plan captured and scoped to staging only |

## 15) Final recommendation

READY FOR OWNER APPROVAL TO EXECUTE v13.15 OWNER-ONLY CONTROLLED GEMINI UX TRIAL
