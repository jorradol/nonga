# v13.0 Admin Token Provisioning Plan — No Secret Exposure / No Deploy

## Status

PREPARE ONLY — no secret exposure, no deploy, no Gemini execution

## Baseline

- Repo: `https://github.com/jorradol/nonga.git`
- Branch: `feature/chat-image-attachment-v1`
- Latest accepted pushed commit before v13.0: `7f1ccd7f63520dc9e97e25943afbf0799d83f41f` (`7f1ccd7`)
- Baseline verification requirements:
  - `git status`
  - `git branch --show-current`
  - `git rev-parse HEAD`
  - `git fetch origin`
  - `git rev-parse origin/feature/chat-image-attachment-v1`
- Baseline result for this prepare-only slice:
  - branch = `feature/chat-image-attachment-v1`
  - local HEAD = `7f1ccd7f63520dc9e97e25943afbf0799d83f41f`
  - origin HEAD = `7f1ccd7f63520dc9e97e25943afbf0799d83f41f`
  - local = origin: yes
  - working tree clean: yes

## v12.15 Source Status

v12.15 CLOSED / PUSHED / ACCEPTED with final recommendation:

- `HOLD — secret access required from owner/operator`

Confirmed source context carried forward:

- `NONGA_ADMIN_API_TOKEN` missing in current operator/session env.
- operator/admin secret value not available in session.
- Cloud Run env metadata did not show `NONGA_ADMIN_API_TOKEN`.
- Secret Manager list only showed `firebase-service-account-json`, `gemini-api-key`.

## Current Blocker

- `NONGA_ADMIN_API_TOKEN` still has no usable secret value shared across runtime and operator session.
- The blocker is secret provisioning ownership/access, not URL/port/smoke route, and not main admin auth logic.
- Runtime/server and operator/session are not yet aligned on the same admin token.

## Admin Token Requirement

The admin token must be the same admin token in both places:

- runtime/server env:
  - `NONGA_ADMIN_API_TOKEN=<same admin token>`
- operator/session env:
  - `export NONGA_ADMIN_API_TOKEN="<same admin token>"`

Critical rule:

- Setting token only in operator/session is not sufficient.
- Runtime/server must also know and use the same admin token.

## Why Gemini API Key Must Not Be Used

Gemini API key must not be used as admin auth token:

- Gemini API key is provider/AI credential scope only.
- `NONGA_ADMIN_API_TOKEN` is a separate Nong A admin authentication token.
- Do not use Gemini API key as a substitute for `NONGA_ADMIN_API_TOKEN`.
- Do not use Firebase service account or Google OAuth token as replacement.

## Token Generation Guidance

Generate a strong token with high entropy in a secure owner/operator environment:

- Generate a high-entropy admin token in a secure operator environment.
- Do not print, commit, paste, or store the token in repo.
- Do not include token value in docs, logs, reports, commit messages, or terminal history shared to others.
- Store only in approved runtime secret/env configuration and operator session env.
- Keep secret handling least-privilege and owner-approved.

## Runtime/Server Env Setup Plan

Staging runtime env setup plan only (no change executed in v13.0):

- target env key in runtime/server env: `NONGA_ADMIN_API_TOKEN=<same admin token>`
- apply to staging runtime env setup first; no production change in this slice.
- any runtime config change and deploy needed to propagate env must be owner approval gated.
- this v13.0 document is prepare-only and does not perform runtime config update.

## Operator/Session Env Setup Plan

Before future live auth recheck, operator session must set the same token:

- operator/session env must export `NONGA_ADMIN_API_TOKEN` with exactly the same admin token value as runtime.
- session verification must check presence only without printing token value.
- do not run endpoint calls in v13.0; this is environment preparation guidance only.

## Secret Handling Rules

- no secret exposure
- no token print
- no token commit
- no Gemini API key used as admin token
- no real customer data / PII
- no VIN / plate / phone จริง
- use redacted evidence only when reporting status

## Approval Required Before Runtime Config Change

Owner approval is required before any runtime action:

- owner approval required for staging runtime env setup change.
- owner approval required for any runtime config update.
- owner approval required for any deploy step.
- without explicit owner approval, status remains prepare-only.

## Future Recheck Sequence

Future sequence after owner approval (not executed in v13.0):

1. owner/operator provision same admin token to staging runtime/server env
2. operator exports same token in session
3. confirm env presence without printing token value
4. perform non-Gemini admin auth recheck only when approved
5. only after auth passes, prepare separate approval for admin-only Real Gemini proof

## Guardrail Confirmation

- no secret exposure
- no deploy
- no runtime config update
- no live auth recheck
- no endpoint call
- no smoke endpoint call
- no Gemini execution
- no `/api/gemini/*`
- no production
- no public route activation
- no buyer-facing AI
- no real lead
- no token/API key exposed

## Recommendation

`READY FOR OWNER DECISION ON ADMIN TOKEN PROVISIONING / STAGING RUNTIME ENV SETUP`
