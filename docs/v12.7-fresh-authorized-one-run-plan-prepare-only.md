# v12.7 Fresh Authorized One-Run Plan — Prepare Only
Status: PREPARE ONLY — no execution, no retry, no Gemini execution

## Baseline

- Repo: `https://github.com/jorradol/nonga.git`
- Branch: `feature/chat-image-attachment-v1`
- Latest accepted pushed commit: `ce3952a837462ace424e52184c09fd64234d9197` (`ce3952a`)
- Commit message: `docs(ai): patch v12.6 client command template safety`
- Required pre-check commands:
  - `git status`
  - `git rev-parse HEAD`
  - `git fetch origin`
  - `git rev-parse origin/feature/chat-image-attachment-v1`
- Required pre-check state:
  - branch correct
  - local = origin
  - working tree clean

## Source Event

v12.4 source event (sanitized):

- curl exit code: `3`
- client error: `URL rejected: Port number was not a decimal number between 0 and 65535`
- HTTP status captured: `400000`
- request dispatch: unknown
- provider call: unknown
- provider output captured: no
- authorization consumed: yes (conservative hold)
- retry allowed: no

Boundary retained:

- request dispatch unknown is unresolved evidence.
- provider call unknown is unresolved evidence.
- client error vs HTTP status must be separated in diagnostics.
- `400000` is treated as non-HTTP artifact path, not a valid HTTP status.

## v12.6 Readiness Summary

v12.6 closed with:

`READY FOR FRESH AUTHORIZED ONE-RUN PLAN — after owner approval only`

v12.6 readiness basis (prepare artifacts only):

- strict URL validator helper added
- port validation added
- malformed URL fail-fast before curl
- sanitized curl template with masked token
- client error vs HTTP status separation improved
- `400000` handled as non-HTTP artifact path
- request dispatch classification improved
- no retry logic
- no Gemini execution
- no smoke endpoint call
- no network calls in validation scripts

## Fresh Authorization Requirement

Authorization from v12.4 is already consumed conservatively and must not be reused.

Execution is not allowed in v12.7. v12.7 is prepare-only. v12.8 is the earliest candidate for execution, and only if owner sends a fresh exact authorization line:

```text
FINAL EXECUTION AUTHORIZE v12.8 ADMIN SS-01 ONE-RUN
```

Strict interpretation:

- do not interpret "โอเค", "ไปต่อ", "จัดเลย" as authorization
- no inferred authorization from context
- authorization must be fresh owner message and exact wording match
- no use of old authorization

## Pre-Execution Gates

Before any v12.8 execution attempt, all gates must PASS.

1) Repo gate

- branch must be `feature/chat-image-attachment-v1`
- local = origin
- working tree clean

2) Health gate

- staging/admin health must return `200`

3) Env gate

- legacy Gemini public/user-facing disabled
- admin shadow real provider enabled according to intended gate
- manual smoke enabled according to intended gate
- no production/public/buyer-facing activation

4) Admin auth non-Gemini gate

- no header must return `401`
- valid masked admin header must pass non-Gemini diagnostic/admin check only
- never expose token

5) Client command safety gate

- final URL parse must pass
- protocol must be `http` or `https`
- hostname must be non-empty
- port if present must be decimal in range `0–65535`
- path must be exactly `/api/admin/sales-brain-shadow-smoke`
- sanitized command preview must mask token
- malformed URL must fail-fast before curl

6) Diagnostics contract gate

- diagnostic fields ready for capture
- request dispatch classification ready
- provider status fields ready
- client error vs HTTP status clearly separated

## Client Command Safety Requirements

Command safety for v12.8 candidate execution must continue to use v12.6 helper/template principles:

- URL parse must pass before command assembly
- strict port check with decimal range `0–65535`
- reject empty/non-decimal/out-of-range port
- endpoint path locked to `/api/admin/sales-brain-shadow-smoke`
- sanitized preview uses masked Authorization value only
- one command only
- no retry
- no automatic retry
- timeout/report behavior must separate client error vs HTTP status

## Sanitized Command Template

Sanitized template only (no real token):

```bash
curl --fail-with-body --silent --show-error \
  --request POST \
  --url "<VALIDATED_ADMIN_BASE_URL>/api/admin/sales-brain-shadow-smoke" \
  --header "Authorization: Bearer ***MASKED***" \
  --header "Content-Type: application/json" \
  --data '{"caseId":"SS-01"}'
```

Template constraints:

- URL must pass v12.6 validator before use
- token must be masked in any preview/output
- no retry option
- one command only
- no automatic retry
- no smoke endpoint call in v12.7 prepare phase
- no `/api/gemini/*` in v12.7 prepare phase

## Abort / Fail-Fast Cases

Abort before execution if any condition is true:

- local != origin
- working tree not clean
- health not `200`
- env gate fails
- admin auth gate fails
- URL malformed
- port missing/empty/non-decimal/out of range
- sanitized preview contains secret/token
- command contains retry option
- endpoint is not `/api/admin/sales-brain-shadow-smoke`
- any `/api/gemini/*` usage
- production/public/buyer-facing activation detected
- real lead/PII/customer data involved
- owner authorization wording mismatch

## One-Run Boundary

Execution boundary for next phase (v12.8 candidate only):

- one run only
- no retry
- no second command
- no old authorization reuse
- immediate abort on any gate fail or guardrail violation

## Diagnostics Capture Plan

Capture sanitized diagnostics fields when and only when v12.8 execution is owner-authorized:

- `curl_exit_code`
- `http_status` (or non-http/client-error marker)
- `request_dispatched` classification
- `provider_call_status`
- `provider_output_captured` (must remain sanitized/no raw output)
- timeout flags and boundary cause
- endpoint path confirmation
- guardrail check summary

Capture rules:

- separate client error vs HTTP status
- preserve request dispatch unknown when evidence is insufficient
- preserve provider call unknown when evidence is insufficient
- never output raw provider output
- never output secret/token/API key

## Result Interpretation Plan

For future v12.8 execution candidate:

PASS only when all hold:

- request dispatch confirmed
- admin-only smoke endpoint responded
- diagnostics captured
- no secret/PII/raw provider output leaked
- provider status clearly classified
- health remains OK

NEED REVIEW when one or more hold:

- client-side curl error
- HTTP status ambiguous
- request dispatch unknown
- diagnostics incomplete
- provider call unknown
- status parsing unclear

FAIL when one or more hold:

- secret/PII leaked
- wrong route called
- public/buyer-facing/production activated
- uncontrolled retry occurred
- health degraded materially
- guardrail violated

## Guardrail Confirmation

- no execution
- no retry
- no Gemini execution
- no smoke endpoint call
- no `/api/gemini/*`
- no deploy
- no production
- no public route activation
- no buyer-facing AI
- no real lead
- no PII/customer data
- no secret/token/API key exposure
- no raw provider output
- no automatic retry
- no use of old authorization
- no runtime behavior change

## Owner Decision Required

Owner must provide fresh exact wording to unlock v12.8 execution candidate:

`FINAL EXECUTION AUTHORIZE v12.8 ADMIN SS-01 ONE-RUN`

If wording is absent or different:

`HOLD — one-run plan still incomplete`

## Recommendation

READY FOR OWNER DECISION ON FRESH FINAL EXECUTION AUTHORIZE FOR v12.8 ONE-RUN
