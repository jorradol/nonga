# v9.6 Admin-Only Gemini Runtime Proof Prep - Env-Safe / No Buyer Exposure

> **PREP ONLY / ADMIN-ONLY BOUNDARY ONLY / NO REAL GEMINI CALL IN NORMAL FLOW**
> เอกสารนี้ยืนยันความพร้อมเชิง guard ก่อนขั้น proof จริงรอบถัดไป โดยยังไม่เปิด buyer-facing AI และยังไม่เปิด runtime/provider จริง

---

## 1. Scope of v9.6

- env-safe boundary preparation for admin-only runtime proof
- readiness checks for environment-only secret policy
- static validation and test hardening for kill switch, fallback, redaction, quota/cost guard
- no deploy, no production, no public route changes

## 2. Safety Boundary (Must Hold)

- no real API key/secret in repo
- no buyer-facing/user-visible AI enablement
- no real lead sending
- no public route to runtime proof
- no uncontrolled real Gemini call
- no normal test depends on real secret
- deterministic flow remains source of truth

## 3. Runtime-Proof Guard Readiness

- disabled by default (`AI_RUNTIME_PROOF_ENABLED` default OFF)
- admin-only boundary required (`/api/admin/*` only)
- kill switch wins all flags
- provider secret must come from environment only (`GEMINI_API_KEY`) and must not be placeholder
- fallback deterministic remains source of truth
- logging redaction covers prompt/phone/VIN/token/secret-like text
- quota/cost cap guard must be ready before any runtime-proof enablement
- provider wiring remains placeholder-only (no normal real Gemini call)

## 4. Explicit Non-GO in v9.6

- NOT GO for production
- NOT GO for buyer-facing AI
- NOT GO for public route
- NOT GO for real lead sending
- NOT GO for uncontrolled runtime/provider activation

## 5. Evidence Checklist Before Next Runtime-Touching Approval

- env-safe/secret-safe validation PASS
- no-secret-pattern scan PASS
- kill switch precedence PASS
- admin-only boundary PASS
- no buyer/public path access PASS
- deterministic fallback PASS
- redaction guard PASS
- quota/cost guard PASS
- explicit owner approval required before any real provider call

