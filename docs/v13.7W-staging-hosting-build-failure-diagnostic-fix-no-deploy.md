# v13.7W Staging Hosting Build Failure Diagnostic / Fix — No Deploy

## Status

COMPLETED — build blocker diagnosed and fixed; staging hosting build now passes (no deploy performed)

## Owner Approval

FINAL APPROVAL FOR v13.7W:  
I approve diagnosing and fixing the staging hosting build failure only.  
Run build/test commands needed to identify the blocker.  
Apply the smallest safe patch required for npm run build:staging:hosting to pass.  
Do not deploy.  
Do not change runtime config.  
Do not execute Gemini.  
Do not call the smoke endpoint.  
Do not call /api/gemini/*.  
Do not send real leads.  
Do not touch production.  
Do not activate public route.  
Do not expose secrets.  
Capture sanitized build evidence and stop with a report.

## Baseline

- repo: `https://github.com/jorradol/nonga.git`
- branch: `feature/chat-image-attachment-v1`
- local HEAD before work: `6977a2a678bbf3585e13f9780a26a91ce35959ee`
- origin HEAD before work: `6977a2a678bbf3585e13f9780a26a91ce35959ee`
- local = origin: yes
- working tree clean before work: yes

## Source Status

- v13.7V recommendation: `HOLD — predeploy validation failed`
- failing command from v13.7V: `npm run build:staging:hosting`
- deploy status from v13.7V: not performed
- deploy target from v13.7V: staging hosting site `nonga-ce93c` with existing rewrite to `nonga-staging`

## Build Failure Reproduction

- command: `npm run build:staging:hosting`
- exit code: `1` (reproduced at v13.7W start)
- failing phase: Vite production bundle stage in staging hosting build flow
- sanitized error summary:
  - category: browser bundle import incompatibility
  - symbol: `createHash`
  - import: `node:crypto`
  - failing modules: `src/services/ai/salesBrainMock.ts`, then `src/services/ai/salesBrainRealProvider.ts`
- secret/token/API key exposed: no

## Root Cause

- category: **B. Vite/build import error** (+ **C. import mismatch for browser build path**)
- file/function/script:
  - `src/services/ai/salesBrainMock.ts` (`computeParamsHash`)
  - `src/services/ai/salesBrainRealProvider.ts` (`computeRequestIdHash`)
  - `src/services/ai/mockAiProvider.ts` (`deterministicMockHash`)
  - script surfaced it: `scripts/build-staging-hosting.mts` (via Vite prod build)
- explanation:
  - these modules imported `createHash` from `node:crypto`
  - staging hosting build targets browser bundle; `node:crypto` is externalized and `createHash` is unavailable
  - bundler fails before deploy gate
- introduced by: existing code path included in browser build graph
- confidence: high

## Patch Scope

- smallest safe patch only to unblock build
- replaced `node:crypto` hashing usage with deterministic local `stableHash16` helper in affected modules
- no runtime config/deploy/provider/lead behavior changes

## Patch Applied

- updated files:
  - `src/services/ai/salesBrainMock.ts`
  - `src/services/ai/salesBrainRealProvider.ts`
  - `src/services/ai/mockAiProvider.ts`
- patch detail:
  - remove `import { createHash } from "node:crypto"`
  - use deterministic non-Node hash helper for 16-char IDs
- behavior impact:
  - maintains deterministic hash semantics for log/request IDs
  - preserves off-topic/service explanation logic (v13.7S/T/U)
  - no core car flow functional regression observed in required tests

## Validation

Commands run:

- `npm run build:staging:hosting`
- `npm run test:v137v:controlled-staging-deploy-owner-retest-surface-refresh`
- `npm run test:v137u:off-topic-patch-effectiveness-diagnostic-runtime-path`
- `npm run test:v137t:off-topic-service-explanation-car-handoff-quality-patch`
- `npm run test:v137s:off-topic-boundary-role-recovery-patch`

Result:

- all listed commands: PASS
- `npm run build:staging:hosting`: PASS

## Deploy Guardrail

- deploy performed: no
- firebase deploy: no
- gcloud run deploy/update: no
- production touched: no
- public route changed: no
- runtime config changed: no
- no deploy
- no runtime config change
- no production
- no public route activation

## Gemini / Smoke / Lead Guardrail

- Gemini execution: no
- smoke endpoint call: no
- `/api/gemini/*` call: no
- real lead sent: no
- buyer-facing AI activation change: no
- no Gemini execution
- no smoke endpoint call
- no /api/gemini/*
- no buyer-facing AI
- no real lead

## Sanitized Evidence

- reproduced failure and captured sanitized root cause at source-file/import level
- after patch, staging hosting build completed successfully with Vite production output
- regression tests for v13.7S/v13.7T/v13.7U/v13.7V records passed
- no secrets or tokens printed in report

## Result

PASS — staging hosting build blocker resolved without deploy or runtime config change

## Recommendation

READY FOR v13.7X CONTROLLED STAGING DEPLOY / OWNER RETEST SURFACE REFRESH
