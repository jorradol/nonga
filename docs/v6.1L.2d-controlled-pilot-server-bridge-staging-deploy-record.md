# v6.1L.2d — Controlled Pilot Server Bridge Staging Deploy Record

**สถานะ:** Execution record — **Cloud Run image-only + Firebase Hosting deployed (v6.1L.2c bridge live), env unchanged, user-visible off**  
**วันที่:** 2026-06-09  
**Git commit (slice):** `78a4b9402ae30ac741696a5c7bce676b3811e3a8` (v6.1L.2c controlled pilot server bridge)  
**Branch:** `feature/chat-image-attachment-v1`  
**อ้างอิง:** v6.1L.2c (server orchestration bridge), v6.1L.2b (pilot path readiness), v6.1L.1a (previous image baseline)

> **v6.1L.2d deploy bridge code ขึ้น staging (Cloud Run + Hosting)** — **ไม่** env update, **`NONGA_AI_USER_VISIBLE_ENABLED=false`**, ไม่ใส่ allowlist UID จริง, public chat legacy unchanged

---

## สารบัญ

1. [Vision & Execution Summary](#1-vision--execution-summary)
2. [Preflight (Before Deploy)](#2-preflight-before-deploy)
3. [Tests / Build Before Deploy](#3-tests--build-before-deploy)
4. [Cloud Build (Executed)](#4-cloud-build-executed)
5. [Cloud Run Deploy (Image-only)](#5-cloud-run-deploy-image-only)
6. [Firebase Hosting Deploy (Executed)](#6-firebase-hosting-deploy-executed)
7. [Post-deploy Smoke](#7-post-deploy-smoke)
8. [Final Env Summary](#8-final-env-summary)
9. [Rollback Plan](#9-rollback-plan)
10. [Compliance (v6.1L.2d)](#10-compliance-v61l2d)
11. [Next Steps](#11-next-steps)

---

## 1. Vision & Execution Summary

### เป้าหมาย v6.1L.2d

Deploy commit `78a4b94` (v6.1L.2c) ไป staging:

- **Cloud Run** — route `POST /api/ai/chat-user-visible-orchestrate` (auth-trusted UID)
- **Firebase Hosting** — client `useChat` bridge (`applyChatUserVisibleServerBridge`)
- **ไม่** env update — user-visible AI ยังปิด

### Execution outcome

| Area | Result |
|------|--------|
| Preflight | ✅ HEAD = `78a4b94`, git clean |
| Pre-deploy tests / build | ✅ all PASS |
| Cloud Build | ✅ **SUCCESS** — `58cd2a20-1b69-46d8-88f4-5ae9859bf10c` |
| Cloud Run image deploy | ✅ rev **`nonga-staging-00068-2qr`** @ **100%** — **no env update** |
| Firebase Hosting deploy | ✅ **SUCCESS** — live `index-BZNOnRfe.js` |
| Post-deploy smoke | ✅ health, auth guards, bridge 401, legacy chat — **PASS** |
| Env update | ❌ **none** |
| User-visible AI | ❌ **off** — legacy chat only |
| Allowlist UIDs in env | ❌ **absent** |

---

## 2. Preflight (Before Deploy)

| Check | Value |
|-------|-------|
| **git status** | clean |
| **branch** | `feature/chat-image-attachment-v1` |
| **HEAD** | `78a4b9402ae30ac741696a5c7bce676b3811e3a8` |
| **Firebase project** | `nonga-ce93c` |
| **Cloud Run service** | `nonga-staging` |
| **Region** | `asia-southeast1` |
| **Previous revision** | `nonga-staging-00067-f6v` @ **100%** |
| **Previous image** | `v6.1L.1-user-visible-allowlist-gate` |
| **Public signup** | **`false`** |

**Did not run** `gcloud secrets versions access` — no secret values in this record.

---

## 3. Tests / Build Before Deploy

| Command | Result |
|---------|--------|
| `npm run test:v61l2c-controlled-pilot-server-orchestration-bridge` | **PASS** |
| `npm run test:v61l2b-user-visible-path-allowlisted-only` | **PASS** |
| `npm run test:v61l2-controlled-pilot-preflight-command-review` | **PASS** |
| `npm run test:v61l1-user-visible-allowlist-gate` | **PASS** |
| `npm run test:v60v-shadow-chat-path-wiring-user-visible-legacy` | **PASS** |
| `npm run test:v61k4-chat-shadow-global-kill-switch-fix` | **PASS** |
| `npm run lint` | **PASS** |
| `npm run build:staging:hosting` | **PASS** (local Vite — deployed to Hosting in this slice) |

---

## 4. Cloud Build (Executed)

```bash
# PowerShell — quoted substitutions required
gcloud builds submit --project=nonga-ce93c \
  --config=cloudbuild.v53f.yaml \
  --substitutions="_IMAGE_URI=asia-southeast1-docker.pkg.dev/nonga-ce93c/nonga-staging/nonga-staging:v6.1L.2d-controlled-pilot-server-bridge,..."
```

| Field | Value |
|-------|-------|
| **Cloud Build ID** | `58cd2a20-1b69-46d8-88f4-5ae9859bf10c` |
| **Status** | **SUCCESS** (~2m 46s) |
| **Image tag** | `v6.1L.2d-controlled-pilot-server-bridge` |
| **Image URI** | `asia-southeast1-docker.pkg.dev/nonga-ce93c/nonga-staging/nonga-staging:v6.1L.2d-controlled-pilot-server-bridge` |
| **Image digest** | `sha256:7ea9644dc092e9299a9fadb9510eb44c8845b6b5b7b916536b1f18437d586c80` |

**Did not run** `gcloud secrets versions access` during build.

---

## 5. Cloud Run Deploy (Image-only)

```bash
# Executed — image-only, no --set-env-vars / --update-env-vars
gcloud run services update nonga-staging \
  --project=nonga-ce93c \
  --region=asia-southeast1 \
  --image=asia-southeast1-docker.pkg.dev/nonga-ce93c/nonga-staging/nonga-staging:v6.1L.2d-controlled-pilot-server-bridge
```

| Field | Value |
|-------|-------|
| **Deploy type** | **image-only** |
| **Previous revision** | `nonga-staging-00067-f6v` |
| **New revision** | **`nonga-staging-00068-2qr`** @ **100%** |
| **Env/secrets** | **preserved** — no env update |
| **Service URL** | `https://nonga-staging-265743710806.asia-southeast1.run.app` |
| **Smoke base (Hosting rewrite)** | `https://a.nongbot.org` |

---

## 6. Firebase Hosting Deploy (Executed)

```bash
npx -y firebase-tools@latest deploy --only hosting --project nonga-ce93c
```

| Field | Value |
|-------|-------|
| **Result** | **SUCCESS** |
| **Deploy scope** | `--only hosting` (no Firestore rules) |
| **Hosting URL** | `https://nonga-ce93c.web.app` |
| **Primary smoke URL** | `https://a.nongbot.org` |
| **Live JS** | `index-BZNOnRfe.js` |
| **Live CSS** | `index-BzbRjXCL.css` |
| **Bridge bundle markers** | `/api/ai/chat-user-visible-orchestrate`, `applyChatUserVisibleServerBridge` (minified in bundle) |

---

## 7. Post-deploy Smoke

Smoke base: **`https://a.nongbot.org`** — revision `nonga-staging-00068-2qr`

### Health & auth

| Check | HTTP / Result |
|-------|----------------|
| `GET /api/health` | **200** — `ok:true`, `publicSignupEnabled:false` — **PASS** |
| `POST /api/ai/chat-user-visible-orchestrate` (no auth) | **401** — `Missing Firebase ID token` — **PASS** |
| `POST /api/ai/chat-user-visible-orchestrate` (invalid Bearer + `firebaseUid` in body) | **401** — fake UID rejected — **PASS** |
| `GET /api/admin/revenue/preview` (unauth) | **401** — **PASS** |

### Public chat (guest — legacy)

| Check | Result |
|-------|--------|
| Message | `"เริ่มใหม่"` |
| User-visible response | **exact legacy:** `ยกเลิกข้อมูลเดิมแล้วครับ พิมพ์ข้อมูลรถคันใหม่ที่ต้องการลงขายได้เลยครับ` — **PASS** |
| User-visible AI replacement | ❌ **none** — **PASS** |
| Provider call from public chat | ❌ **none observed** — **PASS** |

### Safety

| Check | Result |
|-------|--------|
| Raw PII in smoke responses | ❌ **none observed** |
| Raw UID in API responses | ❌ **none** |
| Secret / env dump | ❌ **none** |
| payment / lead / reveal / outcome mutation | ❌ **none** |

**Note:** Signed-in non-allowlisted users receive legacy text because `NONGA_AI_USER_VISIBLE_ENABLED=false` and allowlist absent — env enable is a **separate slice** with approval phrase.

---

## 8. Final Env Summary

Revision `nonga-staging-00068-2qr` — **env preserved** (no update in this slice)

| Key | Value |
|-----|-------|
| `NONGA_AI_USER_VISIBLE_ENABLED` | **`false`** |
| `NONGA_AI_CHAT_SHADOW_REAL_PROVIDER_ENABLED` | **`false`** |
| `NONGA_AI_EMERGENCY_KILL_SWITCH` | **`false`** |
| `NONGA_AI_USER_VISIBLE_ALLOWLIST_UIDS` | **absent** |
| `NONGA_AI_SHADOW_MODE_ENABLED` | `true` |
| `NONGA_AI_PROVIDER` | `gemini` |
| `NONGA_AI_MODE` | `high` |
| `NONGA_AI_BUDGET_DAILY_LIMIT` / `MONTHLY` | `5` / `50` |
| `VITE_NONGA_PUBLIC_SIGNUP_ENABLED` | **`false`** |
| `GEMINI_API_KEY` | secret ref `gemini-api-key:latest` (metadata only) |
| `FIREBASE_SERVICE_ACCOUNT_JSON` | secret ref (metadata only) |

**Did not dump** secret values.

---

## 9. Rollback Plan (Prepared — Not Executed)

### Code rollback (v6.1L.2d → v6.1L.1a image)

```bash
gcloud run services update nonga-staging \
  --project=nonga-ce93c \
  --region=asia-southeast1 \
  --image=asia-southeast1-docker.pkg.dev/nonga-ce93c/nonga-staging/nonga-staging:v6.1L.1-user-visible-allowlist-gate
```

Target revision: `nonga-staging-00067-f6v` (previous known good)

### Deeper rollback (if needed)

```bash
gcloud run services update nonga-staging \
  --project=nonga-ce93c \
  --region=asia-southeast1 \
  --image=asia-southeast1-docker.pkg.dev/nonga-ce93c/nonga-staging/nonga-staging:v6.1K.5-chat-shadow-kill-switch-fix
```

Hosting rollback: redeploy prior Hosting bundle from git history if client bridge must be removed.

**Rollback not needed** — smoke passed.

---

## 10. Compliance (v6.1L.2d)

| Constraint | Status |
|------------|--------|
| Staging only `nonga-ce93c` | ✅ |
| Cloud Run + Hosting deploy | ✅ (code-only) |
| Image-only Cloud Run (no `--update-env-vars`) | ✅ |
| Env update | ❌ **not done** |
| `NONGA_AI_USER_VISIBLE_ENABLED=true` | ❌ **not set** |
| `NONGA_AI_CHAT_SHADOW_REAL_PROVIDER_ENABLED=true` | ❌ **not set** |
| Allowlist real UIDs | ❌ **not set** |
| Public user-visible AI | ❌ **none** (env off) |
| `gcloud secrets versions access` | ❌ **not run** |
| Firestore rules deploy | ❌ **not done** |
| Production | ❌ **not done** |
| payment / settlement / lead / reveal / outcome | ❌ **not touched** |
| public signup | ❌ unchanged `false` |
| real stock import | ❌ still blocked |
| public debug endpoint | ❌ **none** |
| custom public prompt route | ❌ **none** |

---

## 11. Next Steps

**Env enable is the next slice — separate from this deploy — and requires explicit approval phrase:**

> อนุมัติให้เปิด user-visible AI แบบ controlled pilot บน staging เท่านั้น ตาม v6.1L

1. **v6.1L.3 (or approved env slice)** — env update only after approval phrase: `NONGA_AI_USER_VISIBLE_ENABLED=true`, `NONGA_AI_USER_VISIBLE_ALLOWLIST_UIDS=<ALLOWLIST_TESTER_UID>`, kill switch off, chat shadow real provider **still false**
2. Post-env smoke — allowlisted signed-in → pilot text via server bridge; guest/non-allowlisted → legacy + `"เริ่มใหม่"` exact
3. **Do not** combine env enable with rollback deploy without explicit ops approval

---

## Related commits

| Slice | Commit |
|-------|--------|
| v6.1L.2c bridge code | `78a4b9402ae30ac741696a5c7bce676b3811e3a8` |
| v6.1L.2b pilot path | `e16384d8f049a145b3bcc5a82355a88c8ff60116` |
| v6.1L.1a previous image | `e3e139ed6912f4cc42df095240079d09e60f1a6c` |
