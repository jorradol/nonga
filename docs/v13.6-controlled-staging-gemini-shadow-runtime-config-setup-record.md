# v13.6 Controlled Staging Gemini Shadow Runtime Config Setup Record

## Status

COMPLETED — staging runtime config setup only (no Gemini execution in v13.6)

## Owner Approval

Owner approval used for this packet:

FINAL APPROVAL FOR v13.6:  
I approve staging-only Gemini shadow runtime config setup for admin-only proof.  
Set only the required staging runtime/server env values for Gemini provider presence and admin-only shadow smoke enablement.  
Do not expose secrets.  
Do not use Gemini API key as NONGA_ADMIN_API_TOKEN.  
Do not enable buyer-facing AI.  
Do not activate public route.  
Do not send real leads.  
Do not touch production.  
Do not execute Gemini in v13.6.  
Do not call the smoke endpoint in v13.6.  
Capture only sanitized evidence and stop if prerequisites are unclear.

## Baseline

- repo: `https://github.com/jorradol/nonga.git`
- branch: `feature/chat-image-attachment-v1`
- local HEAD before work: `45ee46ca0303a3744282ac9893722714978ac3a7`
- origin HEAD before work: `45ee46ca0303a3744282ac9893722714978ac3a7`
- local = origin: yes
- working tree clean before work: yes

## Source Status

- v13.5 status: ACCEPTED / CLOSED / PUSHED
- v13.5 recommendation: READY FOR OWNER APPROVAL TO PROCEED TO v13.6 STAGING GEMINI SHADOW RUNTIME CONFIG SETUP
- v13.4 unresolved execution prerequisites were addressed in this packet by staging runtime config setup only

## Static Config Confirmation

Static-confirmed from repo/docs references:

- `GEMINI_API_KEY`
- `NONGA_AI_PROVIDER`
- `NONGA_AI_MODE`
- `NONGA_AI_FIRST_ENABLED`
- `NONGA_AI_SHADOW_MODE_ENABLED`
- `NONGA_AI_BUDGET_DAILY_LIMIT`
- `NONGA_AI_BUDGET_MONTHLY_LIMIT`
- `NONGA_AI_ADMIN_SHADOW_MANUAL_SMOKE_ENABLED`
- `NONGA_AI_ADMIN_SHADOW_MANUAL_SMOKE_CASE_ID`
- `NONGA_AI_ADMIN_SHADOW_REAL_PROVIDER_ENABLED`
- `NONGA_ADMIN_API_TOKEN`

Unresolved env names: none

## Staging Runtime Config Setup

Target runtime boundary:

- service: `nonga-staging`
- region: `asia-southeast1`
- production touched: no

Applied / confirmed staging runtime config (sanitized):

- `GEMINI_API_KEY` present: yes
- `GEMINI_API_KEY` source: `gemini-api-key` (secret reference metadata only)
- `NONGA_ADMIN_API_TOKEN` present: yes
- `NONGA_AI_PROVIDER=gemini`
- `NONGA_AI_MODE=shadow`
- `NONGA_AI_FIRST_ENABLED=false`
- `NONGA_AI_SHADOW_MODE_ENABLED=true`
- `NONGA_AI_ADMIN_SHADOW_REAL_PROVIDER_ENABLED=true`
- `NONGA_AI_ADMIN_SHADOW_MANUAL_SMOKE_ENABLED=true`
- `NONGA_AI_ADMIN_SHADOW_MANUAL_SMOKE_CASE_ID=SS-01`
- `NONGA_AI_BUDGET_DAILY_LIMIT=1` (safe low staging limit)
- `NONGA_AI_BUDGET_MONTHLY_LIMIT=10` (safe low staging limit)

## Secret Handling Confirmation

- secret value exposed: no
- `NONGA_ADMIN_API_TOKEN` value printed: no
- `GEMINI_API_KEY` value printed: no
- no Authorization header printed
- no secret committed to docs/scripts
- Gemini API key used as `NONGA_ADMIN_API_TOKEN`: no

## Buyer-Facing / Public / Production Guardrails

- buyer-facing AI enabled: no
- public route activated: no
- production touched: no
- deploy frontend/hosting performed: no
- real lead sent: no

## Gemini Execution Guardrail

- Gemini execution: no
- `/api/gemini/*` called: no

## Smoke Endpoint Guardrail

- smoke endpoint call: no
- POST /api/admin/sales-brain-shadow-smoke called: no

## Sanitized Evidence

- `git status --short --branch` before work indicated clean tracking branch
- baseline branch/head checks matched expected baseline and origin
- runtime update executed on `nonga-staging` in `asia-southeast1` only
- Cloud Run sanitized verification confirmed:
  - `GEMINI_API_KEY present: yes`
  - `NONGA_ADMIN_API_TOKEN present: yes`
  - runtime env keys above set as required
- token/API key required for validation tests: no
- network calls from tests: no (static file checks only)

## Result

PASS — staging runtime config updated for admin-only Gemini shadow smoke readiness with hard-stop guardrails preserved

## Recommendation

READY FOR v13.7 ONE-RUN ADMIN-ONLY REAL GEMINI PROOF APPROVAL
