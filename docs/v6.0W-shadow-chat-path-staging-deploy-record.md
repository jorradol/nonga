# v6.0W — Shadow Chat Path Staging Deploy Record

**สถานะ:** Execution record (docs/tests only) — **Cloud Run staging image-only deploy บันทึกแล้ว, ไม่ deploy ซ้ำ, ไม่ gcloud env update, ไม่เปิด NONGA_AI_USER_VISIBLE_ENABLED, ไม่เรียก paid AI API, user-visible response ยัง legacy 100%**  
**วันที่:** 2026-06-07  
**Git baseline ขณะ deploy:** `d4cecd6855c7aff1b500c80529c54bb077e96842` (v6.0V)  
**Branch:** `feature/chat-image-attachment-v1`  
**อ้างอิง:** v6.0V (shadow chat path wiring), v6.0U (Gemini shadow env enable), v6.0T (shadow runtime reader deploy), v6.0R (runtime flag reader)

> **v6.0W บันทึกผล deploy จริง** — นำโค้ด v6.0V shadow chat path wiring ขึ้น staging revision `nonga-staging-00050-rqs` โดย **env/secrets ไม่เปลี่ยน**, **NONGA_AI_USER_VISIBLE_ENABLED=false**, และ **user-visible response ยัง legacy 100%**

---

## สารบัญ

1. [Vision & Execution Summary](#1-vision--execution-summary)
2. [Preflight (Before Deploy)](#2-preflight-before-deploy)
3. [Tests / Build Before Deploy](#3-tests--build-before-deploy)
4. [Cloud Build (Executed)](#4-cloud-build-executed)
5. [Cloud Run Deploy (Image-only)](#5-cloud-run-deploy-image-only)
6. [Env Flags After Deploy](#6-env-flags-after-deploy)
7. [Post-deploy API Smoke](#7-post-deploy-api-smoke)
8. [Chat Smoke (Cloud Run Direct URL)](#8-chat-smoke-cloud-run-direct-url)
9. [Hosting vs Cloud Run Note](#9-hosting-vs-cloud-run-note)
10. [Rollback Plan (Prepared — Not Run)](#10-rollback-plan-prepared--not-run)
11. [Forbidden Changes (v6.0W)](#11-forbidden-changes-v60w)
12. [Next Steps](#12-next-steps)

---

## 1. Vision & Execution Summary

### เป้าหมาย v6.0W

Deploy Cloud Run staging **image-only** จาก branch ที่มี v6.0V shadow chat path wiring โดย:

- **Preserve** env/secrets เดิม — ไม่ `--set-env-vars` / `--update-env-vars`
- **ไม่เปิด** `NONGA_AI_USER_VISIBLE_ENABLED=true`
- **ไม่** Hosting / Firestore rules deploy
- Post-deploy read-only smoke + safe chat smoke

### Execution outcome

| Area | Result |
|------|--------|
| Preflight | ✅ git clean, HEAD = origin @ `d4cecd6` |
| Before deploy | rev `nonga-staging-00049-nsd`, image `v6.0T-shadow-runtime-reader-db29970` |
| Cloud Build | ✅ **SUCCESS** — `2a8f8699-1356-4fe4-aa0a-1ec6278cc50d` |
| Cloud Run deploy | ✅ rev **`nonga-staging-00050-rqs`**, traffic **100%** |
| Env flags after deploy | ✅ unchanged from v6.0U (shadow on, user-visible off) |
| Post-deploy API smoke | ✅ health/cars 200, revenue 401, signup false |
| Chat smoke | ✅ legacy response 100%, no AI/Gemini shown to user |
| Rollback | ❌ **not needed** — smoke passed |
| User-visible AI | ❌ unchanged — legacy only |
| Paid AI API | ❌ not called |
| Real stock import | ❌ still blocked |

---

## 2. Preflight (Before Deploy)

Recorded at deploy execution time:

| Check | Value |
|-------|-------|
| **git status** | clean (nothing to commit, working tree clean) |
| **branch** | `feature/chat-image-attachment-v1` |
| **HEAD** | `d4cecd6855c7aff1b500c80529c54bb077e96842` |
| **origin/feature/chat-image-attachment-v1** | `d4cecd6855c7aff1b500c80529c54bb077e96842` |
| **HEAD = origin** | ✅ yes |
| **gcloud active project** | `nonga-ce93c` |
| **Cloud Run service** | `nonga-staging` |
| **Region** | `asia-southeast1` |
| **Previous revision** | `nonga-staging-00049-nsd` |
| **Previous image** | `v6.0T-shadow-runtime-reader-db29970` |
| **Public signup** | **`false`** (`VITE_NONGA_PUBLIC_SIGNUP_ENABLED=false`) |
| **GEMINI_API_KEY ref** | `gemini-api-key:latest` (metadata only — **no value accessed**) |

### NONGA_AI_* flags before deploy (unchanged from v6.0U)

| Flag | Value |
|------|-------|
| `NONGA_AI_PROVIDER` | `gemini` |
| `NONGA_AI_MODE` | `high` |
| `NONGA_AI_FIRST_ENABLED` | `true` |
| `NONGA_AI_SHADOW_MODE_ENABLED` | `true` |
| `NONGA_AI_USER_VISIBLE_ENABLED` | **`false`** |
| `NONGA_AI_EMERGENCY_KILL_SWITCH` | `false` |
| `NONGA_AI_BUDGET_DAILY_LIMIT` | `5` |
| `NONGA_AI_BUDGET_MONTHLY_LIMIT` | `50` |

---

## 3. Tests / Build Before Deploy

All passed before Cloud Build + deploy:

| Command | Result |
|---------|--------|
| `npm run test:v60v-shadow-chat-path-wiring-user-visible-legacy` | **PASS** |
| `npm run test:v60r-runtime-shadow-flag-reader-user-visible-off` | **PASS** |
| `npm run test:v60u-gemini-shadow-env-enable-staging-record` | **PASS** |
| `npm run lint` | **PASS** |
| `npm run build:staging:hosting` | **PASS** (local Vite only — **not deployed to Hosting**) |

---

## 4. Cloud Build (Executed)

```bash
# Executed — do not re-run from CI without ops approval
gcloud builds submit --project=nonga-ce93c \
  --config=cloudbuild.v53f.yaml \
  --substitutions=_IMAGE_URI=asia-southeast1-docker.pkg.dev/nonga-ce93c/nonga-staging/nonga-staging:v6.0W-shadow-chat-path-legacy-visible-d4cecd6,...
```

| Field | Value |
|-------|-------|
| **Cloud Build ID** | `2a8f8699-1356-4fe4-aa0a-1ec6278cc50d` |
| **Status** | **SUCCESS** (~2m46s) |
| **Image tag** | `v6.0W-shadow-chat-path-legacy-visible-d4cecd6` |
| **Image URI** | `asia-southeast1-docker.pkg.dev/nonga-ce93c/nonga-staging/nonga-staging:v6.0W-shadow-chat-path-legacy-visible-d4cecd6` |
| **Image digest** | `sha256:887a7c7d7ec346c96d0e8d6d871a57d05e89ccd4c9613f743e98954d7a845639` |

**Did not run** `gcloud secrets versions access` — no secret values in this record.

---

## 5. Cloud Run Deploy (Image-only)

```bash
# Executed — image-only, no --set-env-vars / --update-env-vars
gcloud run deploy nonga-staging \
  --image asia-southeast1-docker.pkg.dev/nonga-ce93c/nonga-staging/nonga-staging:v6.0W-shadow-chat-path-legacy-visible-d4cecd6 \
  --project=nonga-ce93c \
  --region=asia-southeast1 \
  --allow-unauthenticated \
  --service-account=nonga-staging-runner@nonga-ce93c.iam.gserviceaccount.com
```

| Field | Value |
|-------|-------|
| **Deploy type** | **image-only** |
| **Previous revision** | `nonga-staging-00049-nsd` |
| **New revision** | **`nonga-staging-00050-rqs`** |
| **Traffic** | **100%** → new revision |
| **Env/secrets** | **preserved** — no `--set-env-vars`, no `--update-env-vars` |
| **Service account** | `nonga-staging-runner@nonga-ce93c.iam.gserviceaccount.com` |
| **Service URL** | `https://nonga-staging-265743710806.asia-southeast1.run.app` |
| **API smoke base** | `https://nonga-ce93c.web.app` |

---

## 6. Env Flags After Deploy

On revision `nonga-staging-00050-rqs` — **unchanged from v6.0U**:

| Flag | Value after deploy |
|------|---------------------|
| `NONGA_AI_PROVIDER` | `gemini` |
| `NONGA_AI_MODE` | `high` |
| `NONGA_AI_FIRST_ENABLED` | `true` |
| `NONGA_AI_SHADOW_MODE_ENABLED` | `true` |
| `NONGA_AI_USER_VISIBLE_ENABLED` | **`false`** |
| `NONGA_AI_EMERGENCY_KILL_SWITCH` | `false` |
| `NONGA_AI_BUDGET_DAILY_LIMIT` | `5` |
| `NONGA_AI_BUDGET_MONTHLY_LIMIT` | `50` |
| `VITE_NONGA_PUBLIC_SIGNUP_ENABLED` | `false` |

Secret refs (names only — **no values**):

| Env var | Ref |
|---------|-----|
| `GEMINI_API_KEY` | `gemini-api-key:latest` (unchanged) |
| `FIREBASE_SERVICE_ACCOUNT_JSON` | `firebase-service-account-json:latest` (unchanged) |

Settlement env **unchanged** from pre-deploy:

| Env | Value |
|-----|-------|
| `NONGA_SETTLEMENT_DATA_BACKEND` | `firestore` |
| `NONGA_SETTLEMENT_FIRESTORE_WRITES_ENABLED` | `true` |
| `NONGA_SUCCESS_FEE_RECORD_ENABLED` | `true` |

---

## 7. Post-deploy API Smoke

Read-only GET smoke via `https://nonga-ce93c.web.app` (API proxy → Cloud Run):

| Endpoint | HTTP | Key fields |
|----------|------|------------|
| `GET /api/health` | **200** | `ok:true`, `publicSignupEnabled:false` |
| `GET /api/cars?limit=1` | **200** | `success:true` |
| `GET /api/admin/revenue/preview` (unauth) | **401** | auth required |
| `GET /api/my/revenue/preview` (unauth) | **401** | auth required |

**No paid AI API call** during deploy or smoke. **User-visible AI response** unchanged — legacy orchestrator/template only.

---

## 8. Chat Smoke (Cloud Run Direct URL)

Safe chat smoke via Cloud Run direct URL (not Hosting CDN):

| Check | Result |
|-------|--------|
| **URL** | `https://nonga-staging-265743710806.asia-southeast1.run.app/chat` |
| หน้า chat โหลดได้ | ✅ |
| ส่งข้อความปลอดภัย `"เริ่มใหม่"` | ✅ |
| User-visible response | **legacy 100%** — `"ยกเลิกข้อมูลเดิมแล้วครับ พิมพ์ข้อมูลรถคันใหม่ที่ต้องการลงขายได้เลยครับ"` |
| AI/Gemini response แสดงต่อผู้ใช้ | ❌ **none** |
| JS bundle | `index-D1gO2JxF.js` — contains `flagsOnly`, `NONGA_AI_*` keys, `userVisibleEnabled:false`, `wireShadowChatPath` (minified) |
| Raw PII ใน UI | ❌ **not observed** |

**No paid AI API call** during chat smoke. Shadow path wiring runs flags-only debug pass — **legacy text returned unchanged**.

---

## 9. Hosting vs Cloud Run Note

| Surface | Status |
|---------|--------|
| **Cloud Run direct URL** | ✅ Has v6.0V client wiring (`wireShadowChatPath` in deployed bundle) |
| **Firebase Hosting `nonga-ce93c.web.app`** | ❌ **Not redeployed** in v6.0W |
| **Users on web.app** | May receive **older client bundle** for chat UI |
| **API routes via web.app** | Already proxy to Cloud Run |
| **Future slice** | Hosting deploy required for wiring on primary URL |

---

## 10. Rollback Plan (Prepared — Not Run)

Rollback **not needed** — smoke passed.

If health/smoke fails in future — **prepared but not executed** in v6.0W:

```bash
# Prepared — NOT RUN in v6.0W (smoke passed)
gcloud run services update-traffic nonga-staging \
  --project=nonga-ce93c \
  --region=asia-southeast1 \
  --to-revisions=nonga-staging-00049-nsd=100
```

**Do not** modify env/secrets to fix issues — rollback revision first.

---

## 11. Forbidden Changes (v6.0W)

| Forbidden | v6.0W status |
|-----------|--------------|
| `NONGA_AI_USER_VISIBLE_ENABLED=true` | ❌ not done — remains **false** |
| `gcloud run services update` for env | ❌ not done |
| Secrets create/update / `secrets versions access` | ❌ not done |
| Paid AI API / fetch to AI provider | ❌ not done |
| User-visible AI / AI response shown to user | ❌ not done |
| Production | ❌ not touched |
| Firestore rules deploy | ❌ not done |
| Hosting deploy | ❌ not done |
| Payment / invoice / settlement write logic changes | ❌ not done |
| Buyer lead / seller reveal / outcome changes | ❌ not done |
| Public signup enable | ❌ not done — staging `false` |
| Real stock import | ❌ not done — still blocked |

CI static test uses **readFileSync only** — no fetch, no gcloud, no Firebase, no AI API.

---

## 12. Next Steps

1. Post-deploy **shadow smoke** + **AI conversation smoke** (flags-only, no user-visible AI)
2. Future slice: **Hosting deploy** for v6.0V client wiring on primary `web.app` URL
3. Notify ลุง → plan real stock import (still blocked until shadow + conversation smoke passes)
4. Do **not** enable `NONGA_AI_USER_VISIBLE_ENABLED=true` without explicit approval
