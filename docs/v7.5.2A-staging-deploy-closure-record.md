# v7.5.2A — Staging Deploy Closure Record (v7.5.2 Lead Capture Search Escape)

**สถานะ:** **CLOSED — v7.5.2 ON STAGING (HOSTING-ONLY, LOCAL-SAFE)** — GitHub และ staging อยู่ที่ `92d0dac`, deploy แบบ Hosting-only, pre-deploy tests PASS, logic smoke + live-bundle verification ผ่าน, ไม่มี Cloud Run / production deploy, ไม่แตะ env/secrets, ไม่เปิด public signup / user-visible AI จริง / Gemini จริง / real lead sending, ไม่แสดง buyer-facing queue count
**วันที่:** 2026-06-29
**Commit runtime ปิดรอบ:** `92d0dac74c254e617dcf0bfad5dd9f214228d755` (`92d0dac`)
**Branch:** `feature/chat-image-attachment-v1`
**สถานะ git:** local = origin, working tree clean

> Closure record ของรอบ staging deploy v7.5.2 เท่านั้น — เป็นเอกสาร + static validation **ไม่แตะ runtime logic / ไม่แตะ `src/` / ไม่ deploy เพิ่ม** เอกสารนี้ไม่มี secrets, tokens, PII, phone, plate หรือ VIN

---

## 1. ขอบเขต closure record นี้

บันทึกปิดรอบการนำ **v7.5.2 Lead Capture Search Escape Patch** ขึ้น **staging (Hosting-only)** หลังผ่าน pre-deploy tests, build verification, live-bundle parity และ logic smoke ที่จำลอง repro ของลุงเด่น

งานที่ปิดรอบ: v7.5.2 — เมื่อผู้ใช้อยู่ใน lead capture flow แล้วพิมพ์ค้นหารถใหม่ (เช่น "ราคาถูกกว่า 400,000", "รุ่นอื่น") deterministic escape gate จะ detect เป็น search intent ใหม่ → พัก lead draft เดิม (ไม่ส่ง) แทนการถาม lead prompt ซ้ำ

ฐานก่อนหน้า (ปิดไปแล้ว): v7.1 → v7.2 → v7.3 → v7.4 → v7.5 → v7.5.1 (closure record v7.5.1A ที่ `672cbb4`)

---

## 2. Commit / branch ที่ปิดรอบ

| รายการ | ค่า |
|--------|-----|
| Commit runtime ปิดรอบ (full) | `92d0dac74c254e617dcf0bfad5dd9f214228d755` |
| Commit runtime ปิดรอบ (short) | `92d0dac` |
| Branch | `feature/chat-image-attachment-v1` |
| GitHub (origin HEAD) | `92d0dac` |
| Staging (live) | `92d0dac` |
| local = origin | sync ✅ |
| Working tree | clean |

ลำดับ commit: `672cbb4` (v7.5.1A closure record) → `92d0dac` (v7.5.2 patch, push range `672cbb4..92d0dac`)
Staging runtime เลื่อนจาก `fdceaf9` (v7.5.1) → `92d0dac` (v7.5.2) ด้วย Hosting-only re-deploy

> closure record v7.5.2A นี้เป็นเอกสารล้วน — เมื่อ commit จะได้ commit hash ใหม่บน GitHub แต่ **staging ไม่ต้อง re-deploy** ตาม commit ของเอกสาร เพราะไม่แตะ runtime/`src/`

---

## 3. ชนิดการ deploy

| รายการ | ค่า |
|--------|-----|
| Deploy type | **Hosting-only** |
| Firebase project | `nonga-ce93c` |
| Hosting site | `nonga-ce93c` |
| Hosting URL | https://nonga-ce93c.web.app |
| Deploy command | `npx firebase-tools@latest deploy --only hosting:nonga-ce93c --project nonga-ce93c` |
| Build command | `npm run build:staging:hosting` (vite only) |
| Live JS asset (parity check) | `index-BZhZFqJ5.js` (3,916,186 bytes, ตรงกับ local build ที่ `92d0dac`) |
| Live CSS asset | `index-B_zTzHdb.css` (266,002 bytes) |

**Live-bundle verification:** ดึง `https://nonga-ce93c.web.app/assets/index-BZhZFqJ5.js` ได้ HTTP 200, `Content-Length` ตรงกับ local build เป๊ะ และพบ string literal ของ v7.5.2 ใน live bundle: escape ack `"พักคันก่อนหน้า"`, copy `"ยังไม่ส่งข้อมูลให้ผู้ขาย"`, signal `"ถูกกว่า"`, `"รุ่นอื่น"`, redirect ack `"เงื่อนไขใหม่"` → ยืนยันว่า logic v7.5.2 live จริงบน staging

---

## 4. ยืนยัน scope / guardrails ของ deploy

| Item | Result |
|------|--------|
| Cloud Run / backend image deploy | **NO** |
| Production deploy | **NO** |
| env / secrets change | **NO** |
| Public signup เปิด | **NO** |
| User-visible AI จริง เปิด | **NO** |
| Gemini จริง เปิด | **NO** |
| Real lead sending เปิด | **NO** |
| Buyer-facing queue count แสดง | **NO** |
| แตะ runtime logic / lead payload / consent / final confirmation | **NO** |
| แตะ dealer isolation / privacy / data model | **NO** |
| force push | **NO** |

เหตุผลที่ไม่ต้อง deploy backend: v7.5.2 แก้เฉพาะ `src/services/leads/buyerLeadFlowEscape.ts` (escape gate) และ `src/services/ai/chat/buyerSearchIntentParser.ts` (budget parser) ซึ่งเป็น client-side deterministic logic ที่ bundle เข้า SPA → Hosting ; `server.ts` / Cloud Run service `nonga-staging` ไม่ถูกแก้ → พฤติกรรม backend ไม่เปลี่ยน

---

## 5. สรุปผล pre-deploy tests / regression / lint

| รายการ | ผล |
|--------|-----|
| `npm run test:v752-lead-capture-search-escape` | **PASS 37/37** |
| `npm run test:v71-lead-flow-escape-intent-recheck` | PASS 34 |
| `npm run test:v72-conversational-lead-memory` | PASS 35 |
| `npm run test:v73-natural-lead-preview-no-queue-count` | PASS 28 |
| `npm run test:v74-ai-answer-naturalness-card-narrative-fusion` | PASS |
| `npm run test:v75-thai-used-car-market-context-safety-advice` | PASS 64 (รวมเคส v7.5.1 payment-safety) |
| `npm run test:v548a-buyer-search-intent-parser` | PASS |
| รวม | **ALL PASS: True** |
| build:staging:hosting + `verifyDistBundle` | PASS (project=nonga-ce93c, signup=false) |
| `npm run lint` (tsc) | เหลือเฉพาะ known issue v69m (TS2352 ×3) |

---

## 6. สรุปผล smoke บน staging (รอบนี้: logic smoke + live-bundle verification)

repro ของลุงเด่น พิสูจน์ผ่าน deterministic harness (`test:v752-lead-capture-search-escape`) ที่จำลอง flow end-to-end + การยืนยันว่า bundle เดียวกัน live จริง:

| เคส repro | ผล |
|-----|-----|
| ค้นหารถไม่เกิน 5 แสน → เข้า search (parser budget = 500,000) | PASS |
| กด "ให้ผู้ขายติดต่อกลับ" → เข้า lead capture | PASS |
| พิมพ์ "ช่วยหารถที่ราคาถูกกว่า 400,000 มานำเสนอเพิ่มให้ด้วยครับ" → escape redirect (budget = 400,000) | PASS |
| ไม่ถาม lead prompt ซ้ำ (paused session inert) | PASS |
| กลับไปค้นหารถต่อ (ack "เงื่อนไขใหม่", lead turn ไม่ถูกบังคับ) | PASS |
| ไม่ส่ง lead (no createdLeadId, paused draft ส่งไม่ได้) | PASS |
| ไม่สร้าง consent / phone / createdLeadId เอง | PASS |
| ไม่แสดง buyer-facing queue count (QUEUE_NUMBER_RE guard) | PASS |
| return path: กด "ให้ผู้ขายติดต่อกลับ" อีกครั้ง → preview + final confirmation เดิม (ต้องมี phone + consent) | PASS |

เคสเสริม PASS: "ขอดูคันอื่นที่ถูกกว่านี้", "มีรถไม่เกิน 3 แสนไหม", "ขอ SUV ราคาไม่เกิน 5 แสนแทน", "เปลี่ยนเป็นรถเกียร์ออโต้", "หา Yaris รุ่นอื่นให้ดูหน่อย", "ขอเปรียบเทียบกับคันอื่นก่อน" → escape ; "ยังไม่ส่งข้อมูล ขอหารถต่อก่อน" → hold

> ข้อมูลที่ใช้ smoke เป็น data สมมติทั้งหมด (`car-v752-yaris`, เบอร์ทดสอบ) — ไม่มีการใช้ข้อมูลลูกค้าจริง/เบอร์จริง

---

## 7. ข้อจำกัดของ smoke รอบนี้ (manual UI smoke ยังเป็นขั้นเพิ่มเติม)

- smoke รอบนี้เป็น **logic smoke + live-bundle verification** เท่านั้น
- **agent ยังไม่ได้คลิก/พิมพ์บน UI จริงที่ https://nonga-ce93c.web.app**
- **manual UI smoke โดยลุงเด่น** (เปิด URL จริงแล้วพิมพ์ตาม repro) ยังเป็น **ขั้นตรวจ UX เพิ่มเติมหลัง deploy** ที่แนะนำให้ทำอีกชั้นเพื่อยืนยันประสบการณ์ผู้ใช้จริง

---

## 8. Baseline issues เดิม (ไม่ใช่ regression จาก v7.5.2)

| # | รายการ | สถานะ |
|---|--------|-------|
| B-1 | `scripts/test-v69m-thor-owner-gate-controlled-revenue-pilot-go-no-go-checklist.mts` ทำ `tsc --noEmit` exit 2 (TS2352 ×3) | known existing issue, นอก scope |
| B-2 | staging inventory เป็นข้อมูลจริง บาง search อาจตอบ "ไม่มีรถ" | พฤติกรรมปกติของข้อมูลจริง |

ทั้งหมด reproduce ได้ก่อนรอบ v7.5.2 และไฟล์ v69m ไม่อยู่ใน 5 ไฟล์ scope v7.5.2 → ไม่ใช่ regression

---

## 9. Rollback path

- **Hosting rollback ทำได้**: `npx firebase-tools@latest hosting:rollback --project nonga-ce93c` หรือ Firebase Console → Hosting → Release history → เลือก release ก่อนหน้า (`fdceaf9`) → Rollback → staging กลับ runtime `fdceaf9` ทันที
- **ไม่แตะ backend / data**: ไม่มี Cloud Run deploy, ไม่แก้ Firestore/Storage rules, ไม่มี data migration → ไม่มีอะไรต้องย้อนฝั่ง backend/data
- Git rollback (ถ้าจำเป็น): สร้าง revert commit แล้ว build/deploy ใหม่ — **ไม่ force push**

---

## 10. Next recommended step

**แนะนำให้ลุงเด่นทดลองใช้งาน staging แบบผู้ใช้งานจริง** (เปิด chat, ค้นหารถ, กด "ให้ผู้ขายติดต่อกลับ", แล้วพิมพ์ขอหารถถูกกว่า/รุ่นอื่นกลางคัน เพื่อยืนยันว่าไม่ถาม lead ซ้ำและกลับไป search ต่อ) เพื่อเก็บ feedback UX **ก่อน** พิจารณาขั้นถัดไป เช่น production deploy หรือการเปิด Gemini จริง / user-visible AI ซึ่งต้องขออนุมัติแยกและมี readiness plan เฉพาะ

ยังไม่แนะนำให้เปิด production / Gemini จริง / public signup ในขั้นนี้

---

## 11. This closure record (pending commit)

| File | Purpose |
|------|---------|
| `docs/v7.5.2A-staging-deploy-closure-record.md` | This document (staging deploy closure record v7.5.2) |
| `scripts/test-v752a-staging-deploy-closure-record.mts` | Static validation |
| `package.json` | `test:v752a-staging-deploy-closure-record` |

---

## 12. Final verdict

**CLOSED — v7.5.2 ON STAGING (HOSTING-ONLY, LOCAL-SAFE)**

- GitHub + staging อยู่ที่ `92d0dac`, local = origin, working tree clean
- Hosting-only deploy (`npx firebase-tools@latest deploy --only hosting:nonga-ce93c --project nonga-ce93c`), live asset parity ✅ (`index-BZhZFqJ5.js`)
- pre-deploy ALL PASS, v7.5.2 PASS 37/37, build + verifyDistBundle PASS
- logic smoke + live-bundle verification ผ่าน (repro ของลุงเด่นแก้แล้ว)
- staging runtime เลื่อน `fdceaf9` → `92d0dac`
- ไม่มี Cloud Run / production deploy, ไม่แตะ env/secrets, ไม่เปิด public signup / user-visible AI จริง / Gemini จริง / real lead sending, ไม่แสดง buyer-facing queue count
- baseline B-1/B-2 เป็น known issue เดิม ไม่ใช่ regression
- ข้อจำกัด: manual UI smoke โดยลุงเด่น ยังเป็นขั้นตรวจ UX เพิ่มเติมหลัง deploy
- next: ลุงเด่นทดลอง staging แบบผู้ใช้จริง ก่อนคิดเรื่อง production / Gemini จริง

---

*End of v7.5.2A staging deploy closure record (v7.5.2 on staging, hosting-only, local-safe).*
