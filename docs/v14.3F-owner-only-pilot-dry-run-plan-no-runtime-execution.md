# v14.3F — Owner-Only Pilot Dry-Run Plan / No Runtime Execution

## 1) Execution type

owner-only pilot dry-run plan: yes  
code/docs/tests static/mock only: yes  
read-only inspection only: yes  
runtime execution: no  
Gemini run: no  
one-run click: no  
retry: no  
second run: no  
provider network call: no  
endpoint call that may trigger provider: no  
production deploy: no  
staging deploy: no  
public route activation: no  
buyer-facing AI release: no  
real lead sending: no  
real customer data / PII: no  
phone/plate/VIN examples: no  
secret/token/API key exposure: no  
Thor real data import: no  
dealer real inventory import: no  
runtime config mutation: no  
secret value change: no

## 2) Authorization input received (for future runtime window)

Recorded owner authorization input:

`FINAL EXECUTION AUTHORIZE v14.3F: YES — exactly ONE owner-only staging dry-run, no retry, no second run, staging only, owner/admin only, synthetic/sanitized only, no public, no production, no real lead.`

Plan interpretation in this slice:

- authorization text is recorded as the governing instruction set
- this slice still performs no runtime execution
- one-run allowance remains reserved for a future explicit execution step only

## 3) Non-execution lock in this slice

For v14.3F planning slice, mandatory lock:

- `runtime action now: no`
- `one-run consumed now: no`
- `retry consumed now: no`
- `second run consumed now: no`

This plan is preparation-only and cannot consume run budget.

## 4) Owner-only dry-run plan scope (future run contract)

Future execution contract remains strict:

- staging only
- owner/admin only
- allowlist only
- synthetic/sanitized only
- no real buyer data
- no phone capture
- no lead delivery
- no public route
- no production
- exactly one run only
- no retry
- no second run
- runSessionLocked required

Any boundary mismatch => HOLD.

## 5) Pre-run readiness checklist (must be PASS before execution step)

- owner/admin auth path verified
- allowlist enforcement visible
- route target remains staging-only
- required markers visible (`guardPolicyVersion`, Thai UX markers, safety markers)
- run lock semantics visible (`runSessionLocked`, `noRetry`, `noSecondRun`)
- approval wording remains unambiguous
- no public/prod/real-lead risk wording
- no secret/PII risk
- static validation chain passes

If any item fails, set HOLD and block execution.

## 6) Dry-run sequence plan (future one-run, not executed now)

Planned sequence:

1. pre-run boundary confirmation (staging/owner/allowlist/synthetic)  
2. single authorized one-run trigger (1/1 only)  
3. immediate lock confirmation (`runSessionLocked=true`, `oneRunConsumed=true`)  
4. collect sanitized evidence fields only  
5. terminate flow with no retry and no second run  
6. produce owner-readable PASS/HOLD report

This sequence is a procedural plan only in v14.3F and is not executed in this slice.

## 7) Planned post-run evidence minimum set

Future one-run report must include at minimum:

- `httpStatus`
- `auth`
- `runtimeMode`
- `userVisibleEnabled`
- `pilotPathActive`
- `fallbackToLegacy`
- `skipGemini`
- `providerNetwork`
- `gateReason`
- `guardPolicyVersion`
- `thaiUxTuningSliceId`
- `thaiUxTuningActive`
- `targetAnswerLengthGuidance`
- `leadPiiCueGuardActive`
- `phoneEchoGuardActive`
- `safeConfirmationStepWordingActive`
- `sanitizedUserVisibleText`
- `missingUserVisibleText`
- `missingUserVisibleTextReason`
- `answerFieldSource`
- `answerCharCount`
- `runSessionLocked`
- `oneRunConsumed`
- `noRetry`
- `noSecondRun`
- `boundary confirmation`

Evidence gaps or ambiguity => HOLD.

## 8) Risk and HOLD policy for future execution step

Set HOLD immediately when:

- route/auth/guard ambiguity appears
- marker is missing or inconsistent
- approval wording is ambiguous
- one-run count is not explicit
- run lock is missing
- retry/second-run path appears available
- public/production/real-lead exposure risk appears
- secret/PII risk appears
- post-run evidence incomplete
- tests fail

No progression to runtime step is allowed while HOLD remains open.

## 9) Anti-misleading statements (must remain true in v14.3F plan slice)

- Gemini run now: no
- one-run approval granted now: yes (recorded for future step only)
- one-run executed now: no
- pilot active now: no
- public active now: no
- production active now: no
- real lead path active now: no
- retry allowed: no
- second run allowed: no

## 10) Read-only inspection anchors

- `docs/v14.3E-fresh-owner-approval-request-draft-no-gemini.md`
- `docs/v14.3D-controlled-owner-approval-packet-no-gemini.md`
- `docs/v14.3C-controlled-staging-pilot-preflight-visibility-check-no-gemini.md`
- `docs/v14.3B-staging-only-pilot-gate-preflight-no-gemini.md`
- `docs/v14.2C-owner-manual-gemini-quality-retest-pass-closure.md`
- `src/components/admin/ownerOneRunEvidence.ts`
- `src/components/admin/OwnerFirebaseTokenHelperPanel.tsx`

## 11) Validation flow (static/mock)

1. run `npm run test:v14.3F`  
2. run inherited validators (`v14.3E`, `v14.3D`, `v14.3C`, `v14.3B`, `v14.3A`, `v14.3`, `v14.2C`, `v14.2A`, `v14.2`, `v14.1C`, `v14.1`)  
3. if any FAIL appears => `HOLD — TEST FAILURE`  
4. if all PASS => ready for controlled execution handoff step (outside this plan slice)

## 12) Final recommendation enum (v14.3F plan)

- `READY FOR v14.3G OWNER-ONLY ONE-RUN EXECUTION RECORD — STRICT SINGLE RUN`
- `READY FOR v14.3G CONTROLLED EXECUTION HANDOFF — NO RETRY / NO SECOND RUN`
- `HOLD — DRY-RUN PLAN INCOMPLETE`
- `HOLD — APPROVAL/BOUNDARY WORDING AMBIGUITY`
- `HOLD — ONE-RUN LOCK/COUNT CONTROL INCOMPLETE`
- `HOLD — POST-RUN EVIDENCE PLAN INCOMPLETE`
- `HOLD — PUBLIC/PRODUCTION/REAL LEAD RISK DETECTED`
- `HOLD — SECRET/PII RISK DETECTED`
- `HOLD — TEST FAILURE`
