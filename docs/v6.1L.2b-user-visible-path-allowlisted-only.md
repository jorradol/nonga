# v6.1L.2b — User-visible path (allowlisted only)

**สถานะ:** Implementation record (code/tests only) — **Node/server pilot path readiness; browser `/chat` ยังไม่เห็น pilot UX จริง**

**ยังไม่ deploy, ยังไม่ env update, ยังไม่เปิด flags บน staging**

**Base commit:** `44531a1e5b3605df6fdf26d25f1aa7c09ece6827` (v6.1L.2 preflight closed)

## Browser vs Node/server (สำคัญ)

| Path | Pilot UX |
|------|----------|
| **Browser public `/chat`** (`wireShadowChatPath` ใน client bundle) | ❌ **ยังไม่เห็น pilot** — flags-only debug; `userVisibleText` = legacy เพราะ `NONGA_AI_*` อยู่บน Cloud Run ไม่ได้อยู่ใน Vite bundle |
| **Node/server** (`resolveUserVisibleChatResponse`, `wireShadowChatPathWithPilot`, `evaluateSalesBrainShadowRuntime`) | ✅ pilot readiness เมื่อ allowlist gate + env ครบ (mock only) |

**Next slice ก่อน pilot execution จริง:** ต้องตัดสินใจ browser vs server orchestration (เช่น server-side reply path หรือ env-safe client wiring) — slice นี้ยังไม่ทำ

## สิ่งที่ทำใน slice นี้

| รายการ | สถานะ |
|--------|--------|
| Lift `SALES_BRAIN_V60R_USER_VISIBLE_BLOCKED` | ✅ `false` — allowlist gate เป็นตัวควบคุม |
| `SALES_BRAIN_V60V_LEGACY_USER_VISIBLE_ONLY` | ✅ `false` — pilot path เมื่อ gate ผ่าน |
| `salesBrainUserVisibleChatPath.ts` | ✅ mock provider only, side-effect-free |
| Wire `resolveUserVisibleChatResponse` + `wireShadowChatPathWithPilot` (Node) | ✅ |
| Browser `wireShadowChatPath` | ✅ flags-only; `userVisibleText` stays legacy |
| Legacy fallback | ✅ gate fail, mock fallback/no_go, provider error |
| Redacted diagnostics | ✅ ไม่มี raw UID / prompt / PII |

## สิ่งที่ยังไม่ทำ

| รายการ | สถานะ |
|--------|--------|
| Cloud Run / Hosting deploy | ❌ |
| Env update บน staging | ❌ |
| `NONGA_AI_USER_VISIBLE_ENABLED=true` | ❌ |
| `NONGA_AI_CHAT_SHADOW_REAL_PROVIDER_ENABLED=true` | ❌ |
| Real allowlist UID | ❌ |
| Real Gemini ใน public chat path | ❌ |
| Production | ❌ |

## Guardrail stack (ต้องครบทุกชั้น)

1. `NONGA_AI_USER_VISIBLE_ENABLED=true`
2. `NONGA_AI_USER_VISIBLE_ALLOWLIST_UIDS` ไม่ว่าง + UID ตรง
3. Signed-in Firebase UID (ไม่ guest)
4. `NONGA_AI_EMERGENCY_KILL_SWITCH=false`
5. Provider/mode/ai-first/budget prerequisites
6. `SALES_BRAIN_V60R_USER_VISIBLE_BLOCKED=false` (lifted ใน slice นี้)
7. Mock provider only — ไม่มี network ใน pilot path

## Legacy preservation

- Guest / non-allowlisted / empty allowlist / flag off / kill switch → legacy 100%
- Public `/chat` **"เริ่มใหม่"** สำหรับ guest/non-allowlisted → exact:
  `ยกเลิกข้อมูลเดิมแล้วครับ พิมพ์ข้อมูลรถคันใหม่ที่ต้องการลงขายได้เลยครับ`
- Allowlisted + pilot: mock `fallback:true` สำหรับ "เริ่มใหม่" → ยังคง legacy text

## Tests

```bash
npm run test:v61l2b-user-visible-path-allowlisted-only
npm run test:v61l1-user-visible-allowlist-gate
npm run test:v60v-shadow-chat-path-wiring-user-visible-legacy
npm run test:v61l2-controlled-pilot-preflight-command-review
npm run test:v61k4-chat-shadow-global-kill-switch-fix
npm run lint
npm run build:staging:hosting
```

## Rollback (code)

Revert slice → `SALES_BRAIN_V60R_USER_VISIBLE_BLOCKED=true` และ `SALES_BRAIN_V60V_LEGACY_USER_VISIBLE_ONLY=true`
