# v12.13 Admin Token Session Env Setup Check — No Gemini Execution

Status: SETUP CHECK ONLY — no live auth recheck, no Gemini execution, no smoke endpoint call

## Baseline

- Repo: `https://github.com/jorradol/nonga.git`
- Branch: `feature/chat-image-attachment-v1`
- Latest accepted pushed commit before v12.13: `993209018b1d38d53baa4d3355eafd5f0bcec98e` (`9932090`)
- Baseline verification commands:
  - `git status`
  - `git rev-parse HEAD`
  - `git fetch origin`
  - `git rev-parse origin/feature/chat-image-attachment-v1`
- Baseline result in this setup check:
  - branch: `feature/chat-image-attachment-v1`
  - local HEAD: `993209018b1d38d53baa4d3355eafd5f0bcec98e`
  - origin HEAD: `993209018b1d38d53baa4d3355eafd5f0bcec98e`
  - local = origin: yes
  - working tree clean: yes

## Source Event: v12.12

Reviewed source event summary:

- Execution decision: `NEED REVIEW`
- live admin auth recheck: blocked before live auth calls
- token mode gate: FAIL
- token source: required `NONGA_ADMIN_API_TOKEN` primary, but missing
- no-header command attempted: no
- valid-header command attempted: no
- total live calls: 0
- Gemini execution: no
- smoke endpoint call: no
- post_recheck_health_status: `200`

## Interpretation

- v12.12 did not prove admin auth boundary (`401`/`200`) yet.
- No call was made to `/api/admin/duplicates`.
- No smoke endpoint call was made.
- This was not a server-side auth failure from a live request.
- The gate stopped because `NONGA_ADMIN_API_TOKEN` was missing in current session env.
- v12.13 focuses only on session env setup check and safety documentation.

## Token Env Requirement

- Required primary env var: `NONGA_ADMIN_API_TOKEN`
- Value must be a plain token value, not:
  - empty string
  - literal env expression (for example `$NONGA_ADMIN_API_TOKEN` or `${NONGA_ADMIN_API_TOKEN}`)
  - literal env name (`NONGA_ADMIN_API_TOKEN`)
  - quoted value with surrounding quotes
  - value with leading/trailing whitespace
  - value containing newline
  - value already starting with `Bearer `

## Token Presence Check

Safe check output format (no print token):

- `NONGA_ADMIN_API_TOKEN: present/missing`
- `length: nonzero/zero`
- `token: ***MASKED***`

Current v12.13 session finding:

- `NONGA_ADMIN_API_TOKEN: missing`
- token value printed: no

If missing:

- `HOLD — NONGA_ADMIN_API_TOKEN missing in operator/session env`

## Token Format Check

Safe local-only format checks (without token reveal):

- `format: valid/invalid`
- `leading/trailing whitespace: yes/no`
- `contains newline: yes/no`
- `quoted value risk: yes/no`
- `literal env token risk: yes/no`
- `starts with Bearer prefix: yes/no`

Current v12.13 session format evaluation:

- `format: invalid` (missing token implies not ready)
- `leading/trailing whitespace: no`
- `contains newline: no`
- `quoted value risk: no`
- `literal env token risk: no`
- `starts with Bearer prefix: no`

## Safe Operator Setup Guidance

Set token in the current shell/session without exposing value:

```bash
export NONGA_ADMIN_API_TOKEN="<PASTE_ADMIN_TOKEN_HERE>"
```

PowerShell session equivalent:

```powershell
$env:NONGA_ADMIN_API_TOKEN = "<PASTE_ADMIN_TOKEN_HERE>"
```

Verify presence without printing token:

```bash
node -e 'const v=process.env.NONGA_ADMIN_API_TOKEN; console.log(v ? "NONGA_ADMIN_API_TOKEN=present" : "NONGA_ADMIN_API_TOKEN=missing")'
```

Use safe checker output (masked-only):

```bash
node scripts/check-admin-token-session-env.mts
```

## Secret Handling Rules

- no print token
- no commit token
- no echo full token
- no token in report
- no token in docs/examples beyond `***MASKED***`
- do not place token in tracked files
- if using `.env.local`, confirm `.gitignore` protects it before use
- `.gitignore` check: `.env.local` and `.env.*.local` are ignored

## Guardrail Confirmation

- no live auth recheck
- no Gemini execution
- no smoke endpoint call
- no `POST /api/admin/sales-brain-shadow-smoke`
- no `/api/gemini/*`
- no deploy
- no production
- no public route activation
- no buyer-facing AI
- no real lead
- no PII/customer data
- no secret/token exposure
- no automatic retry
- no use of old authorization for execution

## Recommendation

`HOLD — NONGA_ADMIN_API_TOKEN missing in operator/session env`
