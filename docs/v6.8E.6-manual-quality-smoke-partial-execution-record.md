# v6.8E.6 — Manual Quality Smoke Partial Execution Record

**สถานะ:** PARTIAL — deploy/gates/rollback PASS; deterministic fallback no regression; real Gemini **0/7**  
**วันที่:** 2026-06-16  
**Git baseline (deployed):** `dff54eef6c58c79d2adf02961519f6f18d071f67`  
**Branch:** `feature/chat-image-attachment-v1`  
**Next:** v6.8E.7 — thinking/output budget strategy; structured JSON output only with fail-closed fallback

> Staging-only manual smoke. No secrets, tokens, API keys, raw prompts, or full listings in this document.

---

## 1. Execution summary

| Item | Result |
|------|--------|
| Staging host | `https://nonga-ce93c.web.app` |
| Image deployed | **YES** — `v6.8E.6-system-instruction-dff54ee` |
| Cloud Build ID | `681d12b5-5b18-476d-b007-9403093bdb7d` |
| Image deploy revision | `nonga-staging-00131-h8t` |
| Pilot env revision | `nonga-staging-00132-psq` |
| Final rollback revision | `nonga-staging-00136-dkg` |
| Deterministic fallback | **No regression** — finance / GK / EV / compare / summarize / fit |
| Positive real Gemini | **FAIL** — 0/7 (`real_provider_call_ok`) |
| Negative gates | **PASS** |
| Rollback | **DONE** — safe flags restored; budget daily back to 5 |
| Production | **Not touched** |
| **v6.9 readiness** | **NOT READY** |

---

## 2. Pre-deploy

| Check | Result |
|-------|--------|
| Branch | `feature/chat-image-attachment-v1` |
| HEAD | `dff54ee` (= origin) |
| Working tree | clean |
| Pre-deploy tests | lint + v68e6 + v68e5 + v68e4 + v68e3 + v68e2 + v68e1 + v68e + v68d + v68d1 + v61l1 + v61l2b + v544e — **PASS** |

---

## 3. Deploy

| Field | Value |
|-------|-------|
| Project | `nonga-ce93c` |
| Service | `nonga-staging` |
| Region | `asia-southeast1` |
| Image tag | `v6.8E.6-system-instruction-dff54ee` |
| Deploy type | image-only — env/secrets preserved |
| Production | **Not touched** |

---

## 4. Positive smoke — deterministic OK / real path FAIL

Allowlisted buyer **`X7hg...Zw2`**, session chained from budget search (3 petrol car cards — no EV listing in session).

| Scenario | `pilotPathActive` | `realProviderGateReason` | Fallback len | Deterministic |
|----------|-------------------|--------------------------|--------------|---------------|
| Budget | true | `real_provider_output_unsafe` | ~516 | fallback OK |
| Finance | true | `real_provider_output_unsafe` | **~540** | **PASS** |
| Compare | true | `real_provider_output_unsafe` | ~806 | compare OK |
| Summarize | true | `real_provider_output_unsafe` | ~606 | not compare template |
| Fit | true | `real_provider_output_unsafe` | ~403 | not compare template |
| General knowledge | true | `real_provider_output_unsafe` | **~773** | **PASS** |
| EV follow-up | true | `real_provider_output_unsafe` | **~619** | **PASS** |

### Deterministic fallback — no regression

- Finance ~540 chars — not generic short CTA; covers ประเมินยอดผ่อน / เงินดาวน์ / ระยะผ่อน / ดอกเบี้ย
- GK two layers + disclaimer — not compare template
- EV-specific fallback + missing-data note (petrol session cards)
- Compare/summarize/fit fallbacks unchanged in quality

### Real Gemini path — FAIL (0/7)

| `unsafeReason` (final, redacted logs) | Scenarios |
|---------------------------------------|-----------|
| `too_short` | budget, finance, compare, summarize |
| `missing_final_answer_marker` | fit, generalKnowledge, ev |

- `systemInstruction` split did **not** recover `real_provider_call_ok`
- Marker `คำตอบ:` still absent in real output
- All scenarios used mock/pilot fallback for user-visible text
- No marker leak in user-visible responses

---

## 5. Diagnostics — main root-cause evidence (v6.8E.6)

All 7 scenarios logged `qualitySliceId=v6.8E.6` with consistent pattern:

| Field | Observed (all scenarios) |
|-------|--------------------------|
| `finishReason` | **`MAX_TOKENS`** (first + retry) |
| `outputTokenCount` | **~28–31** |
| `thoughtsTokenCount` | **~733–736** |
| `partCount` | `1` |

**Conclusion:** Model spends most tokens on internal thinking (~734), then hits `MAX_TOKENS` before producing final Thai answer with `คำตอบ:` marker. This is the primary root-cause evidence for the next slice — not prompt shape alone.

Redacted retry samples still show English planning/meta fragments (e.g. drafting/checklist language), not user-safe Thai answers.

---

## 6. Negative smoke — PASS

| Check | Result |
|-------|--------|
| Guest | HTTP 401 |
| Non-allowlisted | `uid_not_allowlisted`, `realProviderNetwork=false` |
| Kill switch ON | `emergency_kill_switch`, `fallbackToLegacy=true` |
| Real provider OFF | `real_provider_flag_off` |
| User-visible OFF | `pilotPathActive=false`, `fallbackToLegacy=true` |

---

## 7. Rollback — DONE

Final revision **`nonga-staging-00136-dkg`** @ 100%:

| Flag | Safe value |
|------|------------|
| `NONGA_AI_EMERGENCY_KILL_SWITCH` | **`true`** |
| `NONGA_AI_USER_VISIBLE_ENABLED` | **`false`** |
| `NONGA_AI_USER_VISIBLE_REAL_PROVIDER_ENABLED` | **`false`** |
| `NONGA_AI_ADMIN_SHADOW_REAL_PROVIDER_ENABLED` | **`false`** |
| `NONGA_AI_CHAT_SHADOW_REAL_PROVIDER_ENABLED` | **`false`** |
| `NONGA_AI_BUDGET_DAILY_LIMIT` | **`5`** |

---

## 8. Readiness

| Item | Status |
|------|--------|
| v6.8E.6 overall | **PARTIAL** |
| v6.9 Controlled Production Pilot | **NOT READY** |
| Next action | **v6.8E.7** — investigate/patch thinking budget or output budget strategy; consider structured JSON output only with fail-closed fallback; keep deterministic fallback as user-visible safe layer |
