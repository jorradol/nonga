# v6.0U — Gemini Shadow Env Enable Staging Record

**สถานะ:** Execution record (docs/tests only) — **Cloud Run staging NONGA_AI_* env enable บันทึกแล้ว, ไม่ deploy ซ้ำ, ไม่เปิด user-visible AI, ไม่เรียก paid AI API, ไม่เปลี่ยน chat runtime behavior**  
**วันที่:** 2026-06-07  
**Git baseline ขณะ enable:** `b25c2c327f0138d2a51a363fc3fb7b432563a35b` (v6.0T record)  
**Branch:** `feature/chat-image-attachment-v1`  
**อ้างอิง:** v6.0P (approval + budget caps), v6.0T (shadow runtime reader deploy), v6.0R (runtime flag reader), v6.0Q (live preflight)

> **v6.0U บันทึกผล env enable จริง** — เปิด Gemini shadow mode บน staging revision `nonga-staging-00049-nsd` แบบ **user-visible off** และ **budget cap 5/50** โดย **image ไม่เปลี่ยน**

---

## สารบัญ

1. [Vision & Execution Summary](#1-vision--execution-summary)
2. [Preflight (Before Enable)](#2-preflight-before-enable)
3. [Enable Command (Executed)](#3-enable-command-executed)
4. [Revision Change](#4-revision-change)
5. [AI Flags After Enable](#5-ai-flags-after-enable)
6. [Post-enable Smoke](#6-post-enable-smoke)
7. [Guardrails & Runtime Status](#7-guardrails--runtime-status)
8. [Rollback Plan (Prepared — Not Needed)](#8-rollback-plan-prepared--not-needed)
9. [Important Notes](#9-important-notes)
10. [Forbidden Changes (v6.0U)](#10-forbidden-changes-v60u)
11. [Next Steps](#11-next-steps)

---

## 1. Vision & Execution Summary

### เป้าหมาย v6.0U

เปิด `NONGA_AI_*` env flags บน Cloud Run staging เพื่อให้ Gemini shadow mode เริ่มทำงานแบบ **user-visible off** ตาม v6.0P approval:

- **`gcloud run services update`** — env flags only, **ไม่ deploy image ใหม่**
- **`NONGA_AI_USER_VISIBLE_ENABLED=false`** — ห้ามเปิด user-visible AI
- Budget cap: daily=5, monthly=50
- Post-enable read-only smoke

### Execution outcome

| Area | Result |
|------|--------|
| Preflight | ✅ git clean, HEAD = origin @ `b25c2c3` |
| Before enable | rev `nonga-staging-00048-nwc`, AI flags **absent/off** |
| Env enable | ✅ rev **`nonga-staging-00049-nsd`**, traffic **100%** |
| Image | ✅ **unchanged** — `v6.0T-shadow-runtime-reader-db29970` |
| AI flags after enable | ✅ all 8 set per v6.0P approval |
| Post-enable smoke | ✅ health/cars 200, revenue 401, signup false |
| Local flag test | ✅ `test:v60r-runtime-shadow-flag-reader-user-visible-off` PASS |
| Rollback | ❌ **not needed** — smoke passed |
| User-visible AI | ❌ none — shadow not wired to chat |
| Paid AI API | ❌ not called |

---

## 2. Preflight (Before Enable)

Recorded at enable execution time:

| Check | Value |
|-------|-------|
| **git status** | clean (nothing to commit, working tree clean) |
| **branch** | `feature/chat-image-attachment-v1` |
| **HEAD** | `b25c2c327f0138d2a51a363fc3fb7b432563a35b` |
| **origin/feature/chat-image-attachment-v1** | `b25c2c327f0138d2a51a363fc3fb7b432563a35b` |
| **HEAD = origin** | ✅ yes |
| **gcloud active project** | `nonga-ce93c` |
| **Cloud Run service** | `nonga-staging` |
| **Region** | `asia-southeast1` |
| **Previous revision** | `nonga-staging-00048-nwc` |
| **Image** | `v6.0T-shadow-runtime-reader-db29970` |
| **Public signup** | **`false`** (`VITE_NONGA_PUBLIC_SIGNUP_ENABLED=false`) |
| **Pre-enable `/api/health`** | **200** — `ok:true`, `publicSignupEnabled:false` |

### AI flags before enable (all absent/off)

| Flag | Status |
|------|--------|
| `NONGA_AI_PROVIDER` | **absent/off** |
| `NONGA_AI_MODE` | **absent/off** |
| `NONGA_AI_FIRST_ENABLED` | **absent/off** |
| `NONGA_AI_SHADOW_MODE_ENABLED` | **absent/off** |
| `NONGA_AI_USER_VISIBLE_ENABLED` | **absent/off** |
| `NONGA_AI_EMERGENCY_KILL_SWITCH` | **absent/off** |
| `NONGA_AI_BUDGET_DAILY_LIMIT` | **absent/off** |
| `NONGA_AI_BUDGET_MONTHLY_LIMIT` | **absent/off** |

### Secret metadata (no values)

| Env var | Ref |
|---------|-----|
| `GEMINI_API_KEY` | `gemini-api-key:latest` (metadata only) |

**Did not run** `gcloud secrets versions access` — no secret values in this record.

---

## 3. Enable Command (Executed)

```bash
# Executed — env-only update, no new image deploy
gcloud run services update nonga-staging \
  --project=nonga-ce93c \
  --region=asia-southeast1 \
  --update-env-vars="NONGA_AI_PROVIDER=gemini,NONGA_AI_MODE=high,NONGA_AI_FIRST_ENABLED=true,NONGA_AI_SHADOW_MODE_ENABLED=true,NONGA_AI_USER_VISIBLE_ENABLED=false,NONGA_AI_EMERGENCY_KILL_SWITCH=false,NONGA_AI_BUDGET_DAILY_LIMIT=5,NONGA_AI_BUDGET_MONTHLY_LIMIT=50"
```

### Env vars applied

| Flag | Value |
|------|-------|
| `NONGA_AI_PROVIDER` | `gemini` |
| `NONGA_AI_MODE` | `high` |
| `NONGA_AI_FIRST_ENABLED` | `true` |
| `NONGA_AI_SHADOW_MODE_ENABLED` | `true` |
| `NONGA_AI_USER_VISIBLE_ENABLED` | **`false`** |
| `NONGA_AI_EMERGENCY_KILL_SWITCH` | `false` |
| `NONGA_AI_BUDGET_DAILY_LIMIT` | `5` |
| `NONGA_AI_BUDGET_MONTHLY_LIMIT` | `50` |

---

## 4. Revision Change

| Field | Value |
|-------|-------|
| **Update type** | **env-only** — no new Cloud Run image deploy |
| **Previous revision** | `nonga-staging-00048-nwc` |
| **New revision** | **`nonga-staging-00049-nsd`** |
| **Traffic** | **100%** → `00049-nsd` |
| **Image (unchanged)** | `asia-southeast1-docker.pkg.dev/nonga-ce93c/nonga-staging/nonga-staging:v6.0T-shadow-runtime-reader-db29970` |
| **Service URL** | `https://nonga-staging-265743710806.asia-southeast1.run.app` |
| **Hosting smoke base** | `https://nonga-ce93c.web.app` |

---

## 5. AI Flags After Enable

On revision `nonga-staging-00049-nsd`:

| Flag | Value after enable |
|------|-------------------|
| `NONGA_AI_PROVIDER` | `gemini` |
| `NONGA_AI_MODE` | `high` |
| `NONGA_AI_FIRST_ENABLED` | `true` |
| `NONGA_AI_SHADOW_MODE_ENABLED` | `true` |
| `NONGA_AI_USER_VISIBLE_ENABLED` | **`false`** |
| `NONGA_AI_EMERGENCY_KILL_SWITCH` | `false` |
| `NONGA_AI_BUDGET_DAILY_LIMIT` | `5` |
| `NONGA_AI_BUDGET_MONTHLY_LIMIT` | `50` |

### Other env preserved

| Env | Value |
|-----|-------|
| `VITE_NONGA_PUBLIC_SIGNUP_ENABLED` | `false` |
| `GEMINI_API_KEY` | `gemini-api-key:latest` (ref only) |
| `NONGA_DATA_BACKEND` | `firestore` |
| `NONGA_IMAGE_BACKEND` | `firebase-storage` |
| `NONGA_SETTLEMENT_DATA_BACKEND` | `firestore` |
| `NONGA_SETTLEMENT_FIRESTORE_WRITES_ENABLED` | `true` |
| `NONGA_SUCCESS_FEE_RECORD_ENABLED` | `true` |

---

## 6. Post-enable Smoke

Read-only GET smoke via hosting URL (not AI provider):

| Endpoint | HTTP | Key fields |
|----------|------|------------|
| New revision serving 100% | ✅ | `nonga-staging-00049-nsd` |
| `GET /api/health` | **200** | `ok:true`, `publicSignupEnabled:false` |
| `GET /api/cars?limit=1` | **200** | `success:true` |
| `GET /api/admin/revenue/preview` (unauth) | **401** | auth required |
| `GET /api/my/revenue/preview` (unauth) | **401** | auth required |

### Local static flag test

| Command | Result |
|---------|--------|
| `npm run test:v60r-runtime-shadow-flag-reader-user-visible-off` | **PASS** — 76 checks, **ไม่เรียก Gemini provider** |

**No paid AI API call** during enable or smoke. **Rollback not needed.**

---

## 7. Guardrails & Runtime Status

| Guardrail | Status |
|-----------|--------|
| User-visible AI response | ❌ **none** — no AI response shown to user |
| Shadow runtime wired to chat | ❌ **not wired** — `useChat` / orchestrator unchanged (v6.0R) |
| Paid AI API call | ❌ **not called** |
| Payment / invoice / settlement writes | ❌ **no changes** |
| Buyer lead / seller reveal / outcome | ❌ **no regression** (read-only smoke only) |
| Real stock import | ❌ **not done** — still blocked |
| Public signup | ❌ **false** — unchanged |

---

## 8. Rollback Plan (Prepared — Not Needed)

If smoke fails — rollback command prepared but **not executed** in v6.0U (smoke passed):

```bash
# Prepared — NOT RUN in v6.0U (smoke passed, no rollback needed)
gcloud run services update nonga-staging \
  --project=nonga-ce93c \
  --region=asia-southeast1 \
  --update-env-vars="NONGA_AI_EMERGENCY_KILL_SWITCH=true,NONGA_AI_SHADOW_MODE_ENABLED=false,NONGA_AI_FIRST_ENABLED=false,NONGA_AI_MODE=off,NONGA_AI_USER_VISIBLE_ENABLED=false"
```

---

## 9. Important Notes

- **Env flags พร้อมแล้วบน staging** — runtime reader บน Cloud Run อ่านค่า `NONGA_AI_*` ได้แล้ว
- **v6.0R ยังเป็น mock shadow + ไม่ wired เข้า chat path** — `useChat.ts` และ `chatSearchOrchestrator.ts` ไม่ import shadow runtime modules
- การเปิด env นี้เป็นการ **เตรียม runtime reader บน Cloud Run** ตาม v6.0P approval
- **ยังไม่หมายความว่าผู้ใช้จะเห็น AI response** — `NONGA_AI_USER_VISIBLE_ENABLED=false` และ chat path ยัง legacy
- **ยังไม่หมายความว่ามี live Gemini call** — `SALES_BRAIN_REAL_PROVIDER_NETWORK_ENABLED=false` ใน code; real provider enable ต้องมี slice แยก
- ต้องมี **slice wiring + real provider enable** แยกต่างหาก ก่อน shadow/live conversation smoke

---

## 10. Forbidden Changes (v6.0U)

| Forbidden | v6.0U status |
|-----------|--------------|
| Cloud Run **image** deploy ใหม่ | ❌ not done — env-only update, same image |
| Hosting deploy | ❌ not done |
| Firestore rules deploy | ❌ not done |
| `NONGA_AI_USER_VISIBLE_ENABLED=true` | ❌ not set — remains **false** |
| Secrets create/update / `secrets versions access` | ❌ not done |
| Paid AI API / fetch to AI provider | ❌ not done |
| User-visible AI / AI response shown to user | ❌ not done |
| Production | ❌ not touched |
| Payment / invoice / settlement write logic changes | ❌ not done |
| Buyer lead / seller reveal / outcome changes | ❌ not done |
| Public signup enable | ❌ not done — staging `false` |
| Real stock import | ❌ not done — still blocked |

CI static test uses **readFileSync only** — no fetch, no gcloud, no Firebase, no AI API.

---

## 11. Next Steps

1. Future slice: **wire shadow runtime into chat path** (separate approval)
2. Future slice: **real provider enable** + shadow conversation smoke
3. Monitor staging logs for flag resolution (no user-visible output expected)
4. Notify ลุง → plan real stock import (still blocked until full shadow smoke passes)
