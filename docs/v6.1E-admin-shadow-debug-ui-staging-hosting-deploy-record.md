# v6.1E — Admin Shadow Debug UI Staging Hosting Deploy Record

**สถานะ:** Execution record (docs/tests only) — **Firebase Hosting deploy บันทึกแล้ว, Cloud Run redeploy skipped, Admin Shadow Debug UI live บน primary URL, ไม่ gcloud env update, ไม่เปิด NONGA_AI_USER_VISIBLE_ENABLED, user-visible chat ยัง legacy 100%**  
**วันที่:** 2026-06-08  
**Git baseline ขณะ deploy:** `125e2d54c08bf055937db9f658942daad9f1f755` (v6.1D)  
**Branch:** `feature/chat-image-attachment-v1`  
**อ้างอิง:** v6.1D (admin read-only shadow debug UI panel), v6.1C (Cloud Run admin route deploy), v6.1B (server-side shadow debug wiring), v6.1A (staging shadow integration smoke)

> **v6.1E บันทึกผล Firebase Hosting deploy จริง** — นำ client bundle v6.1D Admin Shadow Debug UI ขึ้น primary URL `nonga-ce93c.web.app` โดย **Cloud Run redeploy skipped** (server route มีอยู่แล้วจาก v6.1C), **NONGA_AI_USER_VISIBLE_ENABLED=false**, **mock provider only / no paid Gemini call**, และ **user-visible chat ยัง legacy 100%**

---

## สารบัญ

1. [Vision & Execution Summary](#1-vision--execution-summary)
2. [Preflight (Before Deploy)](#2-preflight-before-deploy)
3. [Tests / Build Before Deploy](#3-tests--build-before-deploy)
4. [Cloud Run Deploy (Skipped)](#4-cloud-run-deploy-skipped)
5. [Firebase Hosting Deploy (Executed)](#5-firebase-hosting-deploy-executed)
6. [Post-deploy API Smoke](#6-post-deploy-api-smoke)
7. [Admin UI Smoke](#7-admin-ui-smoke)
8. [Chat Smoke (Primary URL)](#8-chat-smoke-primary-url)
9. [Rollback Plan (Not Needed)](#9-rollback-plan-not-needed)
10. [Git Status After Deploy](#10-git-status-after-deploy)
11. [Compliance (v6.1E)](#11-compliance-v61e)
12. [Next Steps](#12-next-steps)

---

## 1. Vision & Execution Summary

### เป้าหมาย v6.1E

Deploy Firebase Hosting staging เพื่อให้ Admin Shadow Debug UI (v6.1D) ใช้งานบน primary URL โดย:

- **Deploy Hosting only** — client bundle ใหม่ขึ้น `nonga-ce93c.web.app`
- **Skip Cloud Run redeploy** — v6.1D เป็น client UI only; server route มีอยู่แล้วบน `nonga-staging-00051-fbd`
- **ไม่เปิด** `NONGA_AI_USER_VISIBLE_ENABLED=true`
- **ไม่** gcloud env update / Firestore rules deploy
- Post-deploy read-only API smoke + admin-gated UI/API smoke + safe chat smoke

### Execution outcome

| Area | Result |
|------|--------|
| Preflight | ✅ git clean, HEAD = origin @ `125e2d5` |
| Before deploy | Cloud Run `nonga-staging-00051-fbd`, Hosting bundle `index-NVEc0A-_.js` |
| Cloud Run deploy | ❌ **SKIPPED** — server route already live from v6.1C |
| Firebase Hosting deploy | ✅ **SUCCESS** — live bundle `index-BpyOr2_V.js` |
| Post-deploy API smoke | ✅ health/cars 200, revenue 401, signup false |
| Admin UI smoke | ✅ guest gate, dealer 403, admin API + bundle markers |
| Chat smoke | ✅ legacy response exact, no AI/Gemini shown |
| Rollback | ❌ **not needed** — smoke passed |
| User-visible AI | ❌ unchanged — legacy only |
| Paid Gemini / provider network | ❌ not called — mock provider only |
| Real stock import | ❌ still blocked |

---

## 2. Preflight (Before Deploy)

Recorded at deploy execution time:

| Check | Value |
|-------|-------|
| **git status** | clean (nothing to commit, working tree clean) |
| **branch** | `feature/chat-image-attachment-v1` |
| **HEAD** | `125e2d54c08bf055937db9f658942daad9f1f755` |
| **origin/feature/chat-image-attachment-v1** | `125e2d54c08bf055937db9f658942daad9f1f755` |
| **HEAD = origin** | ✅ yes |
| **Firebase project** | `nonga-ce93c` |
| **Cloud Run service** | `nonga-staging` |
| **Cloud Run revision** | `nonga-staging-00051-fbd` @ **100%** |
| **Cloud Run image** | `v6.1C-admin-shadow-smoke-debug-0883faf` |
| **Hosting bundle (before)** | `index-NVEc0A-_.js` |
| **Public signup** | **`false`** (`publicSignupEnabled:false`) |
| **GEMINI_API_KEY ref** | `gemini-api-key:latest` (metadata only — **no value accessed**) |

### NONGA_AI_* flags (unchanged on Cloud Run revision)

| Flag | Value |
|------|-------|
| `NONGA_AI_PROVIDER` | `gemini` |
| `NONGA_AI_MODE` | `high` |
| `NONGA_AI_FIRST_ENABLED` | `true` |
| `NONGA_AI_SHADOW_MODE_ENABLED` | `true` |
| `NONGA_AI_USER_VISIBLE_ENABLED` | **`false`** |
| `NONGA_AI_EMERGENCY_KILL_SWITCH` | `false` |
| `NONGA_AI_BUDGET_DAILY_LIMIT` | `5` |
| `NONGA_AI_BUDGET_MONTHLY_LIMIT` | `50` |

**Did not run** `gcloud secrets versions access` — no secret values in this record.

---

## 3. Tests / Build Before Deploy

All passed before Firebase Hosting deploy:

| Command | Result |
|---------|--------|
| `npm run test:v61d-admin-shadow-debug-ui-read-only-panel` | **PASS** |
| `npm run test:v61b-server-side-shadow-evaluation-admin-debug-user-visible-off` | **PASS** |
| `npm run test:v61a-staging-shadow-integration-smoke-user-visible-still-off` | **PASS** |
| `npm run lint` | **PASS** |
| `npm run build:staging:hosting` | **PASS** |

Local build output bundle: **`index-BpyOr2_V.js`**

---

## 4. Cloud Run Deploy (Skipped)

Cloud Run image-only redeploy was **not performed** in v6.1E.

| Reason | Detail |
|--------|--------|
| v6.1D scope | **Client UI only** — no server code changes |
| Existing revision | `nonga-staging-00051-fbd` already serves `POST /api/admin/sales-brain-shadow-smoke` from v6.1B/v6.1C |
| Conclusion | **No image rebuild / redeploy needed** |

**Did not run** `gcloud run deploy` or `gcloud run services update` in this slice.

---

## 5. Firebase Hosting Deploy (Executed)

```bash
# Executed — Hosting only, do not re-run without ops approval
npx -y firebase-tools@latest deploy --only hosting --project nonga-ce93c
```

| Field | Value |
|-------|-------|
| **Deploy type** | **Firebase Hosting only** |
| **Status** | **SUCCESS** |
| **Previous bundle** | `index-NVEc0A-_.js` |
| **Live bundle** | **`index-BpyOr2_V.js`** |
| **Hosting URL** | `https://nonga-ce93c.web.app` |
| **Admin UI path** | `https://nonga-ce93c.web.app/admin/shadow-smoke` |
| **Deploy scope** | `--only hosting` (no Cloud Run, no Firestore rules) |

**Did not run** `gcloud secrets versions access` — no secret values in this record.

---

## 6. Post-deploy API Smoke

Read-only GET smoke via `https://nonga-ce93c.web.app` (API proxy → Cloud Run):

| Endpoint | HTTP | Key fields |
|----------|------|------------|
| `GET /api/health` | **200** | `ok:true`, `publicSignupEnabled:false` |
| `GET /api/cars?limit=1` | **200** | `success:true` |
| `GET /api/admin/revenue/preview` (unauth) | **401** | auth required |
| `GET /api/my/revenue/preview` (unauth) | **401** | auth required |

`NONGA_AI_USER_VISIBLE_ENABLED` remains **`false`** — unchanged after deploy.

**No paid AI API call** during deploy or smoke. **User-visible AI response** unchanged — legacy orchestrator/template only.

---

## 7. Admin UI Smoke

### Guest (`/admin/shadow-smoke`)

| Check | Result |
|-------|--------|
| URL | `https://nonga-ce93c.web.app/admin/shadow-smoke` |
| Expected | login/admin gate — not a public debug page |
| Observed message | **"กรุณาเข้าสู่ระบบก่อนใช้งานส่วนนี้ครับ"** |
| Result | **PASS** |

### Dealer

| Check | Result |
|-------|--------|
| `POST /api/admin/sales-brain-shadow-smoke` with dealer Firebase token | **403** |
| Result | **PASS** |

### Admin / superadmin

| Check | Result |
|-------|--------|
| Live bundle markers in `index-BpyOr2_V.js` | **PASS** — `AI Shadow Smoke`, `admin-shadow-smoke`, `SS-01`, `SS-08`, `userVisibleOff`, `providerNetwork` |
| Admin API SS-01 | **200** — `success:true`, `readOnly:true`, `userVisibleOff:true`, `providerNetwork:false`, `provider:mock` |
| Admin API SS-05..SS-08 | **200** safe |
| No PII / secret dump in response | **PASS** |
| Admin browser panel click SS-01 | **SKIPPED** — automated browser had no Firebase login session; endpoint confirmed via API smoke + bundle markers |
| Admin menu location | Admin Dashboard sidebar only — **no Header link** for guest/dealer/member |

Safety checks:

| Check | Result |
|-------|--------|
| Paid Gemini API call | ❌ **none** |
| Raw phone / email in API response | ❌ **none observed** |
| Secret / env dump | ❌ **none** |
| AI response shown to public user | ❌ **none** |

---

## 8. Chat Smoke (Primary URL)

Safe chat smoke via primary Hosting URL:

| Check | Result |
|-------|--------|
| **URL** | `https://nonga-ce93c.web.app/chat` |
| Input | `"เริ่มใหม่"` |
| User-visible response | **legacy 100%** — exact text: `"ยกเลิกข้อมูลเดิมแล้วครับ พิมพ์ข้อมูลรถคันใหม่ที่ต้องการลงขายได้เลยครับ"` |
| AI/Gemini response แทน legacy | ❌ **none** |
| Result | **PASS** |

**No paid AI API call** during chat smoke.

---

## 9. Rollback Plan (Not Needed)

Rollback **not needed** — API smoke, guest gate, admin API, and chat legacy all passed.

If Hosting UI regression occurs in future — rollback Hosting release to previous version; **do not** modify env/secrets to fix issues in this slice.

Cloud Run traffic rollback **not required** — Cloud Run was not redeployed in v6.1E.

---

## 10. Git Status After Deploy

| Check | Value |
|-------|-------|
| **branch** | `feature/chat-image-attachment-v1` |
| **HEAD** | up to date with `origin/feature/chat-image-attachment-v1` |
| **working tree** | clean |

**No docs auto-commit** in deploy slice — this record committed separately.

---

## 11. Compliance (v6.1E)

| Item | v6.1E status |
|------|--------------|
| Firebase Hosting deploy | ✅ **performed** — Hosting only |
| Cloud Run redeploy | ❌ **skipped** — not needed |
| `gcloud run services update` for env | ❌ not done |
| `NONGA_AI_*` env changes | ❌ not done |
| `NONGA_AI_USER_VISIBLE_ENABLED=true` | ❌ not done — remains **false** |
| Secrets create/update / `secrets versions access` | ❌ not done |
| Paid AI API / fetch to AI provider | ❌ not done |
| User-visible chat / AI response to public | ❌ not changed |
| Production | ❌ not touched |
| Firestore rules deploy | ❌ not done |
| Payment / invoice / settlement write logic | ❌ not done |
| Buyer lead / seller reveal / outcome changes | ❌ not done |
| Public signup enable | ❌ not done — staging `false` |
| Real stock import | ❌ not done — still blocked |
| Public debug page | ❌ not created — admin-gated only |

CI static test uses **readFileSync only** — no fetch, no gcloud, no Firebase CLI, no AI API.

---

## 12. Next Steps

1. Optional: manual admin browser smoke with Firebase login to click SS-01..SS-08 in UI (read-only)
2. Monitor admin shadow debug usage — admin/superadmin only, synthetic cases only
3. Do **not** enable `NONGA_AI_USER_VISIBLE_ENABLED=true` without explicit approval
