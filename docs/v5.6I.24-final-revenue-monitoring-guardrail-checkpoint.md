# v5.6I.24 — Final Revenue Monitoring & Guardrail Checkpoint (Recorded)

**สถานะ:** Execution record + static tests only — **read-only checks บันทึกแล้ว, ไม่ deploy, ไม่ update env, ไม่เขียน Firestore**  
**Git baseline ขณะรัน:** `e49032d184888db485d83bb976cb36f09136e38a` (v5.6I.23)  
**วันที่บันทึก:** 2026-06-07  
**Target:** staging **`nonga-ce93c`** เท่านั้น  
**Branch:** `feature/chat-image-attachment-v1`  
**อ้างอิง:** v5.6I.23 re-enable success, v5.6I.19 smoke harness, v5.6I.20 approval packet

> เอกสารนี้บันทึกผล **read-only guardrail checkpoint หลังเปิด settlement persistence สำเร็จ** — test v56i24 เป็น static validation ของ record นี้ (**ไม่ fetch staging / ไม่ gcloud / ไม่เขียน Firestore**)

---

## สรุปผล (plain language)

| เรื่อง | ผล |
|--------|-----|
| Preflight git | **PASS** — clean, HEAD = origin @ `e49032d` |
| Cloud Run staging | rev **`nonga-staging-00047-7xc`**, traffic **100%**, flags firestore/true/true |
| API read-only | health/cars **200**; unauth revenue **401** |
| Firestore client deny | **403** ทั้ง 4 settlement collections |
| Admin SDK counts | **1 / 4 / 4 / 0** — ตรง v5.6I.23, ไม่มี docs เพิ่ม/ลด |
| PII safety | **PASS** — ไม่พบ buyer contact fields; revenue API payloads clean |
| Rollback | command **พร้อม** — **ยังไม่รัน** |
| Deploy / env / Firestore write | **ไม่ทำ** |

---

## 1. Git (read-only)

| Check | Result |
|-------|--------|
| `git status` | clean |
| branch | `feature/chat-image-attachment-v1` |
| HEAD | `e49032d184888db485d83bb976cb36f09136e38a` |
| `origin/feature/chat-image-attachment-v1` | ตรงกับ HEAD |

---

## 2. Cloud Run staging (read-only describe)

**คำสั่งที่รัน (read-only):**

```bash
gcloud config get-value project
gcloud run services describe nonga-staging \
  --project=nonga-ce93c \
  --region=asia-southeast1 \
  --format="yaml(status.latestReadyRevisionName,status.traffic,spec.template.spec.containers[0].env)"
```

| Field | Recorded value |
|-------|----------------|
| gcloud active project | `nonga-ce93c` |
| service | `nonga-staging` |
| region | `asia-southeast1` |
| current revision | **`nonga-staging-00047-7xc`** |
| traffic | **100%** |

**Settlement env flags (ยังเปิดอยู่):**

| Env | Value |
|-----|-------|
| `NONGA_SETTLEMENT_DATA_BACKEND` | **`firestore`** |
| `NONGA_SETTLEMENT_FIRESTORE_WRITES_ENABLED` | **`true`** |
| `NONGA_SUCCESS_FEE_RECORD_ENABLED` | **`true`** |
| `VITE_NONGA_PUBLIC_SIGNUP_ENABLED` | **`false`** |

Inventory/image backends คงเดิม: `NONGA_DATA_BACKEND=firestore`, `NONGA_IMAGE_BACKEND=firebase-storage`

**Secrets (ref only — ไม่บันทึกค่า):**

- `GEMINI_API_KEY` ← `secretKeyRef: gemini-api-key:latest`
- `FIREBASE_SERVICE_ACCOUNT_JSON` ← `secretKeyRef: firebase-service-account-json:latest`

---

## 3. API read-only smoke

Base: `https://nonga-ce93c.web.app`

| Endpoint | HTTP status |
|----------|-------------|
| `GET /api/health` | **200** |
| `GET /api/cars?limit=1` | **200** |
| `GET /api/admin/revenue/preview` (no auth) | **401** |
| `GET /api/my/revenue/preview` (no auth) | **401** |

---

## 4. Firestore client direct deny (unauth REST — read-only probe)

Project: `nonga-ce93c` — GET doc + LIST collection (ไม่สร้าง/แก้/ลบข้อมูล)

| Collection | GET | LIST |
|------------|-----|------|
| `successFeeRecords` | **403** | **403** |
| `settlementAdjustments` | **403** | **403** |
| `settlementAuditLogs` | **403** | **403** |
| `settlementIdempotencyRecords` | **403** | **403** |

---

## 5. Admin SDK read-only doc counts (metadata only — ไม่เปิด PII)

| Collection | Doc count |
|------------|-----------|
| `settlementAdjustments` | **1** |
| `settlementAuditLogs` | **4** |
| `settlementIdempotencyRecords` | **4** |
| `successFeeRecords` | **0** |

**เปรียบเทียบ v5.6I.23:** counts **ตรงกัน** — ไม่มี docs เพิ่ม/ลดในรอบ guardrail checkpoint นี้ (read-only)

---

## 6. PII safety (read-only — redacted report)

### Forbidden fields (ต้องไม่ปรากฏ)

- `buyerPhone`
- `buyerEmail`
- `buyerName`
- `contactPhone`

### ผลตรวจ

| Surface | Result |
|---------|--------|
| Settlement Firestore docs (`settlementAdjustments`, `settlementAuditLogs`, `settlementIdempotencyRecords`) | **PASS** — ไม่พบ forbidden fields |
| `responseSnapshot` ใน idempotency records | **PASS** — clean |
| Admin revenue preview API (auth GET) | **PASS** — ไม่มี buyer PII ใน payload |
| Seller revenue preview API (auth GET) | **PASS** — ไม่มี buyer PII ใน payload |

Sample doc field names (redacted): `listingId`, `settlementId`, `paidAmount`, `reason`, `action`, `updatedByRole` — ไม่มี buyer contact data

---

## 7. Rollback readiness

**Rollback command พร้อม — ยังไม่รัน:**

```bash
# DO NOT RUN จาก commit docs-only นี้
gcloud run services update nonga-staging \
  --project=nonga-ce93c \
  --region=asia-southeast1 \
  --update-env-vars="NONGA_SETTLEMENT_DATA_BACKEND=memory,NONGA_SETTLEMENT_FIRESTORE_WRITES_ENABLED=false,NONGA_SUCCESS_FEE_RECORD_ENABLED=false"
```

---

## 8. Guardrail summary — staging revenue/settlement ปิดหมวดได้

| Guardrail | Status |
|-----------|--------|
| Settlement persistence enabled | **ON** (firestore/true/true) |
| Auth gate on revenue APIs | **PASS** (401 unauth) |
| Client Firestore deny | **PASS** (403 × 4) |
| Durable docs stable | **PASS** (1/4/4/0 = v5.6I.23) |
| PII safety | **PASS** |
| Public signup | **OFF** |
| Rollback command | **READY** (not executed) |

---

## Forbidden changes (v5.6I.24 slice — confirmed)

- ไม่ deploy Cloud Run / Hosting / Firestore rules
- ไม่ `gcloud run deploy`
- ไม่ `gcloud run services update`
- ไม่เปลี่ยน env flags / secrets
- ไม่เขียน Firestore / ลบ / แก้ smoke records
- ไม่ migration / backfill
- ไม่แตะ production
- ไม่ payment / invoice / Gemini changes
- ไม่แก้ runtime code ในรอบ v5.6I.24 docs

---

## Files (v5.6I.24)

| File | เปลี่ยน |
|------|--------|
| `docs/v5.6I.24-final-revenue-monitoring-guardrail-checkpoint.md` | **NEW** — record นี้ |
| `scripts/test-v56i24-final-revenue-monitoring-guardrail-checkpoint.mts` | **NEW** — static validation |
| `package.json` | `test:v56i24-final-revenue-monitoring-guardrail-checkpoint` |
