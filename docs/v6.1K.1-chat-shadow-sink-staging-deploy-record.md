# v6.1K.1 — Chat Shadow Sink Staging Deploy Record

**สถานะ:** Execution record — **Cloud Run image-only deployed, `POST /api/admin/chat-shadow-sink` live, smoke PASS, Hosting skipped, user-visible off, chat real provider flag off/default**  
**วันที่:** 2026-06-08  
**Git commit (slice):** `952a42f56fd1fd1905baa3a6918d38d14b79256e` (v6.1K)  
**Branch:** `feature/chat-image-attachment-v1`  
**อ้างอิง:** v6.1K (chat-path shadow sink module), v6.1J (admin full matrix), v6.1H.4 (previous staging revision)

> **v6.1K.1 นำ route admin-only chat shadow sink ขึ้น staging** — image-only deploy, **ไม่** `--update-env-vars`, **`NONGA_AI_USER_VISIBLE_ENABLED=false`**, **`NONGA_AI_CHAT_SHADOW_REAL_PROVIDER_ENABLED` absent/default off**, public `/chat` legacy 100%

---

## สารบัญ

1. [Vision & Execution Summary](#1-vision--execution-summary)
2. [Preflight (Before Deploy)](#2-preflight-before-deploy)
3. [Tests / Build Before Deploy](#3-tests--build-before-deploy)
4. [Cloud Build (Executed)](#4-cloud-build-executed)
5. [Cloud Run Deploy (Image-only)](#5-cloud-run-deploy-image-only)
6. [Env Flags (Unchanged)](#6-env-flags-unchanged)
7. [Post-deploy API Smoke](#7-post-deploy-api-smoke)
8. [Public Chat (Unchanged)](#8-public-chat-unchanged)
9. [Hosting vs Cloud Run](#9-hosting-vs-cloud-run)
10. [Rollback Plan](#10-rollback-plan)
11. [Compliance (v6.1K.1)](#11-compliance-v61k1)
12. [Next Steps](#12-next-steps)

---

## 1. Vision & Execution Summary

### เป้าหมาย v6.1K.1

นำโค้ด v6.1K (`POST /api/admin/chat-shadow-sink`) ขึ้น Cloud Run staging โดย:

- **Image-only deploy** — preserve env/secrets
- **ไม่** Firebase Hosting deploy (client bundle unchanged)
- **ไม่** เปิด `NONGA_AI_USER_VISIBLE_ENABLED=true`
- **ไม่** เปิด `NONGA_AI_CHAT_SHADOW_REAL_PROVIDER_ENABLED` (absent / default off)
- Public `/chat` ยัง legacy orchestrator

### Execution outcome

| Area | Result |
|------|--------|
| Preflight | ✅ HEAD = origin `952a42f`, git clean |
| Tests / build (pre-deploy) | ✅ v61k, v61j, v61h4, v61h, lint, build:staging:hosting — **PASS** |
| Cloud Build | ✅ **SUCCESS** — `9644dd35-9299-44d8-b736-857f77540f41` |
| Cloud Run deploy | ✅ rev **`nonga-staging-00058-xwh`**, traffic **100%** |
| Env flags | ✅ unchanged — no `--update-env-vars` |
| Hosting deploy | ❌ **SKIPPED** |
| Post-deploy API smoke | ✅ health, auth guards, CP-01..CP-05, SS-01 — **PASS** |
| Public chat `"เริ่มใหม่"` | ✅ exact legacy |
| Rollback | ❌ **not needed** — smoke passed |
| User-visible AI | ❌ unchanged — `NONGA_AI_USER_VISIBLE_ENABLED=false` |
| Chat real provider live | ❌ off — CP-02 `providerNetwork:false` |

---

## 2. Preflight (Before Deploy)

| Check | Value |
|-------|-------|
| **git status** | clean |
| **branch** | `feature/chat-image-attachment-v1` |
| **HEAD** | `952a42f56fd1fd1905baa3a6918d38d14b79256e` |
| **origin/feature/chat-image-attachment-v1** | `952a42f56fd1fd1905baa3a6918d38d14b79256e` |
| **Firebase project** | `nonga-ce93c` |
| **Cloud Run service** | `nonga-staging` |
| **Region** | `asia-southeast1` |
| **Previous revision** | `nonga-staging-00057-bq2` @ **100%** |
| **Previous image** | `v6.1H.4-admin-shadow-gemini-model-fix` |
| **Public signup** | **`false`** |
| **`NONGA_AI_USER_VISIBLE_ENABLED`** | **`false`** |
| **`NONGA_AI_CHAT_SHADOW_REAL_PROVIDER_ENABLED`** | **absent** (default off) |
| **`NONGA_AI_ADMIN_SHADOW_REAL_PROVIDER_ENABLED`** | **`true`** |
| **`GEMINI_API_KEY` ref** | `gemini-api-key:latest` (metadata only) |

**Did not run** `gcloud secrets versions access` — no secret values in this record.

---

## 3. Tests / Build Before Deploy

| Command | Result |
|---------|--------|
| `npm run test:v61k-chat-path-shadow-real-provider-sink-only` | **PASS** |
| `npm run test:v61j-admin-shadow-conversation-smoke-full-matrix` | **PASS** |
| `npm run test:v61h4-admin-shadow-gemini-model-fix-staging-deploy-record` | **PASS** |
| `npm run test:v61h-real-gemini-shadow-call-admin-only-staging-smoke` | **PASS** |
| `npm run lint` | **PASS** |
| `npm run build:staging:hosting` | **PASS** (local Vite only — not deployed to Hosting) |

---

## 4. Cloud Build (Executed)

```bash
gcloud builds submit --project=nonga-ce93c \
  --config=cloudbuild.v53f.yaml \
  --substitutions=_IMAGE_URI=asia-southeast1-docker.pkg.dev/nonga-ce93c/nonga-staging/nonga-staging:v6.1K.1-chat-path-shadow-sink,...
```

| Field | Value |
|-------|-------|
| **Cloud Build ID** | `9644dd35-9299-44d8-b736-857f77540f41` |
| **Status** | **SUCCESS** (~2m19s) |
| **Image tag** | `v6.1K.1-chat-path-shadow-sink` |
| **Image URI** | `asia-southeast1-docker.pkg.dev/nonga-ce93c/nonga-staging/nonga-staging:v6.1K.1-chat-path-shadow-sink` |
| **Image digest** | `sha256:0cf221536c2fea79215cd26aa793214e8ce3f9f42b9a747f8ef3bae46900f84e` |

**Did not run** `gcloud secrets versions access` during build.

---

## 5. Cloud Run Deploy (Image-only)

```bash
# Executed — image-only, no --set-env-vars / --update-env-vars
gcloud run services update nonga-staging \
  --project=nonga-ce93c \
  --region=asia-southeast1 \
  --image=asia-southeast1-docker.pkg.dev/nonga-ce93c/nonga-staging/nonga-staging:v6.1K.1-chat-path-shadow-sink
```

| Field | Value |
|-------|-------|
| **Deploy type** | **image-only** |
| **Previous revision** | `nonga-staging-00057-bq2` |
| **New revision** | **`nonga-staging-00058-xwh`** @ **100%** |
| **Env/secrets** | **preserved** — no `--set-env-vars`, no `--update-env-vars` |
| **Service URL** | `https://nonga-staging-265743710806.asia-southeast1.run.app` |
| **Smoke base (Hosting rewrite)** | `https://a.nongbot.org` |

---

## 6. Env Flags (Unchanged)

On revision `nonga-staging-00058-xwh` — **unchanged from pre-deploy**:

| Flag | Value after deploy |
|------|---------------------|
| `NONGA_AI_USER_VISIBLE_ENABLED` | **`false`** |
| `NONGA_AI_CHAT_SHADOW_REAL_PROVIDER_ENABLED` | **absent** (default off) |
| `NONGA_AI_ADMIN_SHADOW_REAL_PROVIDER_ENABLED` | **`true`** |
| `NONGA_AI_PROVIDER` | `gemini` |
| `NONGA_AI_SHADOW_MODE_ENABLED` | `true` |
| `NONGA_AI_EMERGENCY_KILL_SWITCH` | `false` |
| `NONGA_AI_BUDGET_DAILY_LIMIT` | `5` |
| `NONGA_AI_BUDGET_MONTHLY_LIMIT` | `50` |
| `VITE_NONGA_PUBLIC_SIGNUP_ENABLED` | **`false`** |
| `GEMINI_API_KEY` | secret ref `gemini-api-key:latest` (unchanged) |

**Did not run** `gcloud run services update --update-env-vars` in this slice.

---

## 7. Post-deploy API Smoke

Smoke base: **`https://a.nongbot.org`** (Firebase Hosting → Cloud Run rewrite)

### Health

| Endpoint | HTTP | Key fields |
|----------|------|------------|
| `GET /api/health` | **200** | `ok:true`, `publicSignupEnabled:false` |

### Chat shadow sink route

```
POST /api/admin/chat-shadow-sink
Content-Type: application/json
Authorization: Bearer <Firebase ID token>
{ "scenarioId": "CP-01" }
```

| Case | HTTP | Result |
|------|------|--------|
| Unauth `{"scenarioId":"CP-01"}` | **401** | **PASS** |
| Dealer Firebase token `{"scenarioId":"CP-01"}` | **403** | **PASS** |
| Admin `CP-01` | **200** | **PASS** — `success`, `readOnly`, `userVisibleOff`, `sinkOnly`, `sliceId:v6.1K` |
| Admin `CP-02` | **200** | **PASS** — flag off: `providerNetwork:false`, gate ≠ `real_provider_call_ok` |
| Admin `CP-03` | **200** | **PASS** |
| Admin `CP-04` | **200** | **PASS** |
| Admin `CP-05` | **200** | **PASS** |

### Admin SS-01 regression

| Case | HTTP | Key fields |
|------|------|------------|
| Admin `POST /api/admin/sales-brain-shadow-smoke` `{"caseId":"SS-01"}` | **200** | `success:true`, `userVisibleOff:true` |

### Safety checks on responses

| Check | Result |
|-------|--------|
| Raw phone / email in response | ❌ **none observed** |
| Secret / env dump in response | ❌ **none** — no `GEMINI_API_KEY` or `FIREBASE_SERVICE_ACCOUNT_JSON` values |
| AI response shown to public user | ❌ **none** — admin-only sink payload |
| CP-02 live real Gemini (chat flag off) | ❌ **none** — `providerNetwork:false` |

**Did not run** `gcloud secrets versions access` during smoke.

---

## 8. Public Chat (Unchanged)

Safe chat smoke via primary Hosting URL (API → Cloud Run; **client bundle unchanged** — no Hosting deploy in v6.1K.1):

| Check | Result |
|-------|--------|
| **URL** | `https://a.nongbot.org/chat` |
| Message | `"เริ่มใหม่"` |
| User-visible response | **legacy 100%** — exact: `ยกเลิกข้อมูลเดิมแล้วครับ พิมพ์ข้อมูลรถคันใหม่ที่ต้องการลงขายได้เลยครับ` |
| AI/Gemini replacing legacy | ❌ **none** |
| Raw PII in UI | ❌ **not observed** |

**No paid chat-path real provider call** — `NONGA_AI_CHAT_SHADOW_REAL_PROVIDER_ENABLED` absent; client `wireShadowChatPath` flags-only unchanged.

---

## 9. Hosting vs Cloud Run

| Surface | Status |
|---------|--------|
| **Cloud Run revision** | ✅ `nonga-staging-00058-xwh` @ 100% — includes v6.1K chat shadow sink route |
| **Firebase Hosting** | ❌ **Not redeployed** in v6.1K.1 |
| **API routes via a.nongbot.org** | Proxy to Cloud Run — `/api/admin/chat-shadow-sink` reachable |
| **Chat UI** | Client bundle unchanged — legacy user-visible behavior preserved |

---

## 10. Rollback Plan (Prepared — Not Run)

Rollback **not needed** — health, route guards, chat shadow sink smoke, SS-01 regression, and public chat smoke passed.

```bash
# Prepared only — do not run unless smoke fails
gcloud run services update nonga-staging \
  --project=nonga-ce93c \
  --region=asia-southeast1 \
  --image=asia-southeast1-docker.pkg.dev/nonga-ce93c/nonga-staging/nonga-staging:v6.1H.4-admin-shadow-gemini-model-fix
```

Previous rollback target: revision `nonga-staging-00057-bq2` @ image `v6.1H.4-admin-shadow-gemini-model-fix`

---

## 11. Compliance (v6.1K.1)

| Check | Status |
|-------|--------|
| `NONGA_AI_USER_VISIBLE_ENABLED=true` | ❌ not set — remains **false** |
| `NONGA_AI_CHAT_SHADOW_REAL_PROVIDER_ENABLED=true` | ❌ not set — **absent/default off** |
| Public AI response | ❌ not changed |
| Production deploy | ❌ not done |
| Firebase Hosting deploy | ❌ not done |
| Firestore rules deploy | ❌ not done |
| Env update (`--update-env-vars`) | ❌ not done |
| `gcloud secrets versions access` | ❌ not done |
| Payment / invoice / settlement write | ❌ not touched |
| Buyer lead / seller reveal / outcome | ❌ not touched |
| Public signup | ❌ not opened |
| Real stock import | ❌ still blocked |
| v6.1L user-visible approval phrase | ❌ not in scope |

---

## 12. Next Steps

1. Optional: enable `NONGA_AI_CHAT_SHADOW_REAL_PROVIDER_ENABLED=true` for CP-02 live Gemini pilot — **separate slice, report before env update**
2. Manual admin UI panel for chat shadow sink (future) — read-only, admin-only
3. **v6.1L** — controlled user-visible pilot requires separate approval gate
4. Do **not** enable `NONGA_AI_USER_VISIBLE_ENABLED=true` without explicit v6.1L approval
