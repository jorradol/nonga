/**
 * Fixture-only local synthetic image. Production builds must not import this
 * as the only listing placeholder — use listingImages with compile-time fold.
 */
export const FIXTURE_LOCAL_IMAGE = "/fixture/placeholder-car.svg";
